# Third Party Dependency Licenses

This file lists the licenses of the Rust dependencies linked into the
`space_query` binaries. It is generated by `cargo about generate about.hbs`.
Reference attributions for the TNS thin implementation (python-oracledb, go-ora)
are maintained separately in `THIRD_PARTY_NOTICES.md`.

## Overview

{{#each overview}}
- {{{name}}} ({{count}})
{{/each}}

## License texts

{{#each licenses}}
### {{{name}}}

Used by:
{{#each used_by}}
- {{crate.name}} {{crate.version}}
{{/each}}

```
{{{text}}}
```

{{/each}}
