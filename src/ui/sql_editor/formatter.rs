use crate::db::connection::DatabaseType;
use crate::db::{FormatItem, QueryExecutor, ToolCommand};
use crate::sql_delimiter::{
    line_closes_delimiter_frame_below_snapshot_before_token, DelimiterFrameState,
    DelimiterLineStartSnapshot,
};
use crate::sql_format::{SqlFormatFrameId, SqlFormatFrameIdAllocator, SqlFormatScopedFrame};
use crate::sql_text::{
    self, FormatIndentedParenOwnerKind, FORMAT_BLOCK_END_QUALIFIER_KEYWORDS,
    FORMAT_BLOCK_START_KEYWORDS, FORMAT_CLAUSE_KEYWORDS, FORMAT_CONDITION_KEYWORDS,
    FORMAT_CREATE_SUFFIX_BREAK_KEYWORDS, FORMAT_JOIN_MODIFIER_KEYWORDS,
};
use crate::ui::sql_depth::{
    apply_paren_token, split_top_level_keyword_groups, split_top_level_symbol_groups,
    ParenDepthState,
};
use std::borrow::Cow;
use std::cell::Cell;
use std::collections::VecDeque;

use super::SqlEditorWidget;
use super::SqlToken;
use super::SqlTokenSpan;

struct MeaningfulTokenLinks {
    prev: Vec<Option<usize>>,
    next: Vec<Option<usize>>,
}

impl MeaningfulTokenLinks {
    fn prev_index(&self, idx: usize) -> Option<usize> {
        self.prev.get(idx).copied().flatten()
    }

    fn next_index(&self, idx: usize) -> Option<usize> {
        self.next.get(idx).copied().flatten()
    }
}

fn is_statement_prefix_word_start_byte(byte: u8) -> bool {
    byte.is_ascii_alphabetic() || byte == b'_'
}

fn is_statement_prefix_word_continue_byte(byte: u8) -> bool {
    is_statement_prefix_word_start_byte(byte)
        || byte.is_ascii_digit()
        || matches!(byte, b'$' | b'#')
}

fn leading_statement_words_upper(statement: &str, max_words: usize) -> Vec<String> {
    let bytes = statement.as_bytes();
    let mut words = Vec::with_capacity(max_words);
    let mut idx = 0usize;

    while idx < bytes.len() && words.len() < max_words {
        match bytes[idx] {
            byte if byte.is_ascii_whitespace() => {
                idx += 1;
            }
            b'-' if bytes.get(idx + 1) == Some(&b'-') => {
                idx += 2;
                while idx < bytes.len() && bytes[idx] != b'\n' {
                    idx += 1;
                }
            }
            b'#' => {
                while idx < bytes.len() && bytes[idx] != b'\n' {
                    idx += 1;
                }
            }
            b'/' if bytes.get(idx + 1) == Some(&b'*') => {
                idx += 2;
                while idx + 1 < bytes.len() && !(bytes[idx] == b'*' && bytes[idx + 1] == b'/') {
                    idx += 1;
                }
                idx = idx.saturating_add(2).min(bytes.len());
            }
            byte if is_statement_prefix_word_start_byte(byte) => {
                let start = idx;
                idx += 1;
                while idx < bytes.len() && is_statement_prefix_word_continue_byte(bytes[idx]) {
                    idx += 1;
                }
                if let Some(word) = statement.get(start..idx) {
                    words.push(word.to_ascii_uppercase());
                } else {
                    break;
                }
            }
            _ => break,
        }
    }

    words
}

fn statement_starts_with_create_trigger_prefix(statement: &str) -> bool {
    let words = leading_statement_words_upper(statement, 8);
    words.first().is_some_and(|word| word == "CREATE")
        && words.iter().skip(1).any(|word| word == "TRIGGER")
}

fn statement_starts_with_alter_trigger_prefix(statement: &str) -> bool {
    let words = leading_statement_words_upper(statement, 2);
    matches!(words.as_slice(), [first, second] if first == "ALTER" && second == "TRIGGER")
}

fn ascii_uppercase_cow(text: &str) -> Cow<'_, str> {
    if text.bytes().any(|byte| byte.is_ascii_lowercase()) {
        Cow::Owned(text.to_ascii_uppercase())
    } else {
        Cow::Borrowed(text)
    }
}

#[derive(Clone, Copy, Debug, Default)]
struct FormatItemStaticAnalysis {
    is_sqlplus_comment_line: bool,
    is_create_trigger_statement: bool,
    is_alter_trigger_statement: bool,
    is_prompt: bool,
}

impl FormatItemStaticAnalysis {
    fn analyze(item: &FormatItem) -> Self {
        match item {
            FormatItem::Statement(statement) => {
                let is_sqlplus_comment_line = crate::sql_text::is_sqlplus_comment_line(statement);
                if is_sqlplus_comment_line {
                    return Self {
                        is_sqlplus_comment_line,
                        ..Self::default()
                    };
                }

                Self {
                    is_sqlplus_comment_line,
                    is_create_trigger_statement: statement_starts_with_create_trigger_prefix(
                        statement,
                    ),
                    is_alter_trigger_statement: statement_starts_with_alter_trigger_prefix(
                        statement,
                    ),
                    ..Self::default()
                }
            }
            FormatItem::ToolCommand(ToolCommand::Prompt { .. }) => Self {
                is_prompt: true,
                ..Self::default()
            },
            FormatItem::Verbatim(text) => Self {
                is_prompt: QueryExecutor::parse_tool_command_if_candidate(text)
                    .is_some_and(|cmd| matches!(cmd, ToolCommand::Prompt { .. })),
                ..Self::default()
            },
            FormatItem::ToolCommand(_) | FormatItem::Slash => Self::default(),
        }
    }
}

#[derive(Clone, Copy, Debug, Default)]
struct FormatterTokenLineProfile {
    start_newline_count: usize,
    end_newline_count: usize,
}

#[derive(Clone, Copy, Debug, Default)]
struct FormatterTokenContext<'a> {
    gap_from_prev_has_newline: bool,
    prev_non_comment_idx: Option<usize>,
    second_prev_non_comment_idx: Option<usize>,
    immediate_prev_prev: Option<&'a SqlToken>,
    immediate_prev: Option<&'a SqlToken>,
    immediate_next: Option<&'a SqlToken>,
    immediate_next_next: Option<&'a SqlToken>,
    next_non_comment_indices: [Option<usize>; 3],
    next_word_indices: [Option<usize>; 3],
}

impl<'a> FormatterTokenContext<'a> {
    fn prev_non_comment_idx(self) -> Option<usize> {
        self.prev_non_comment_idx
    }

    fn second_prev_non_comment_idx(self) -> Option<usize> {
        self.second_prev_non_comment_idx
    }

    fn prev_non_comment_token(self, tokens: &'a [SqlToken]) -> Option<&'a SqlToken> {
        self.prev_non_comment_idx()
            .and_then(|token_idx| tokens.get(token_idx))
    }

    fn second_prev_non_comment_token(self, tokens: &'a [SqlToken]) -> Option<&'a SqlToken> {
        self.second_prev_non_comment_idx()
            .and_then(|token_idx| tokens.get(token_idx))
    }

    fn next_word_idx(self, slot: usize) -> Option<usize> {
        self.next_word_indices.get(slot).copied().flatten()
    }

    fn immediate_prev(self, _tokens: &'a [SqlToken]) -> Option<&'a SqlToken> {
        self.immediate_prev
    }

    fn immediate_prev_prev(self, _tokens: &'a [SqlToken]) -> Option<&'a SqlToken> {
        self.immediate_prev_prev
    }

    fn immediate_next(self, _tokens: &'a [SqlToken]) -> Option<&'a SqlToken> {
        self.immediate_next
    }

    fn immediate_next_next(self, _tokens: &'a [SqlToken]) -> Option<&'a SqlToken> {
        self.immediate_next_next
    }

    fn next_non_comment_idx(self) -> Option<usize> {
        self.next_non_comment_indices[0]
    }

    fn second_next_non_comment_idx(self) -> Option<usize> {
        self.next_non_comment_indices[1]
    }

    fn third_next_non_comment_idx(self) -> Option<usize> {
        self.next_non_comment_indices[2]
    }

    fn next_non_comment_token(self, tokens: &'a [SqlToken]) -> Option<&'a SqlToken> {
        self.next_non_comment_idx()
            .and_then(|token_idx| tokens.get(token_idx))
    }

    fn second_next_non_comment_token(self, tokens: &'a [SqlToken]) -> Option<&'a SqlToken> {
        self.second_next_non_comment_idx()
            .and_then(|token_idx| tokens.get(token_idx))
    }

    fn third_next_non_comment_token(self, tokens: &'a [SqlToken]) -> Option<&'a SqlToken> {
        self.third_next_non_comment_idx()
            .and_then(|token_idx| tokens.get(token_idx))
    }

    fn next_words(self, tokens: &'a [SqlToken]) -> [Option<&'a str>; 3] {
        [
            self.next_word_indices[0]
                .and_then(|token_idx| tokens.get(token_idx))
                .and_then(|token| match token {
                    SqlToken::Word(word) => Some(word.as_str()),
                    _ => None,
                }),
            self.next_word_indices[1]
                .and_then(|token_idx| tokens.get(token_idx))
                .and_then(|token| match token {
                    SqlToken::Word(word) => Some(word.as_str()),
                    _ => None,
                }),
            self.next_word_indices[2]
                .and_then(|token_idx| tokens.get(token_idx))
                .and_then(|token| match token {
                    SqlToken::Word(word) => Some(word.as_str()),
                    _ => None,
                }),
        ]
    }
}

struct FormatterTokenContextCursor<'a> {
    tokens: &'a [SqlToken],
    current_idx: usize,
    prev_non_comment_idx: Option<usize>,
    second_prev_non_comment_idx: Option<usize>,
    next_non_comment_scan_idx: usize,
    next_word_scan_idx: usize,
    upcoming_non_comment_indices: [Option<usize>; 3],
    upcoming_word_indices: [Option<usize>; 3],
}

impl<'a> FormatterTokenContextCursor<'a> {
    fn new(tokens: &'a [SqlToken]) -> Self {
        let mut cursor = Self {
            tokens,
            current_idx: 0,
            prev_non_comment_idx: None,
            second_prev_non_comment_idx: None,
            next_non_comment_scan_idx: 1,
            next_word_scan_idx: 1,
            upcoming_non_comment_indices: [None; 3],
            upcoming_word_indices: [None; 3],
        };
        cursor.fill_upcoming_non_comment_indices();
        cursor.fill_upcoming_word_indices();
        cursor
    }

    #[inline]
    fn push_upcoming_index(slots: &mut [Option<usize>; 3], idx: usize) {
        for slot in slots.iter_mut() {
            if slot.is_none() {
                *slot = Some(idx);
                return;
            }
        }
    }

    #[inline]
    fn discard_consumed_indices(slots: &mut [Option<usize>; 3], current_idx: usize) {
        while slots[0].is_some_and(|queued_idx| queued_idx <= current_idx) {
            slots[0] = slots[1];
            slots[1] = slots[2];
            slots[2] = None;
        }
    }

    fn fill_upcoming_non_comment_indices(&mut self) {
        while self.upcoming_non_comment_indices[2].is_none()
            && self.next_non_comment_scan_idx < self.tokens.len()
        {
            let idx = self.next_non_comment_scan_idx;
            self.next_non_comment_scan_idx = self.next_non_comment_scan_idx.saturating_add(1);
            if !matches!(self.tokens.get(idx), Some(SqlToken::Comment(_))) {
                Self::push_upcoming_index(&mut self.upcoming_non_comment_indices, idx);
            }
        }
    }

    fn fill_upcoming_word_indices(&mut self) {
        while self.upcoming_word_indices[2].is_none() && self.next_word_scan_idx < self.tokens.len()
        {
            let idx = self.next_word_scan_idx;
            self.next_word_scan_idx = self.next_word_scan_idx.saturating_add(1);
            if matches!(self.tokens.get(idx), Some(SqlToken::Word(_))) {
                Self::push_upcoming_index(&mut self.upcoming_word_indices, idx);
            }
        }
    }

    fn advance_to(&mut self, idx: usize) -> FormatterTokenContext<'a> {
        debug_assert!(idx < self.tokens.len());
        debug_assert!(idx >= self.current_idx);

        while self.current_idx < idx {
            if !matches!(
                self.tokens.get(self.current_idx),
                Some(SqlToken::Comment(_))
            ) {
                self.second_prev_non_comment_idx = self.prev_non_comment_idx;
                self.prev_non_comment_idx = Some(self.current_idx);
            }
            self.current_idx = self.current_idx.saturating_add(1);

            Self::discard_consumed_indices(
                &mut self.upcoming_non_comment_indices,
                self.current_idx,
            );
            Self::discard_consumed_indices(&mut self.upcoming_word_indices, self.current_idx);

            self.fill_upcoming_non_comment_indices();
            self.fill_upcoming_word_indices();
        }

        let next_non_comment_indices = self.upcoming_non_comment_indices;
        let next_word_indices = self.upcoming_word_indices;

        FormatterTokenContext {
            gap_from_prev_has_newline: false,
            prev_non_comment_idx: self.prev_non_comment_idx,
            second_prev_non_comment_idx: self.second_prev_non_comment_idx,
            immediate_prev_prev: idx
                .checked_sub(2)
                .and_then(|prev_idx| self.tokens.get(prev_idx)),
            immediate_prev: idx
                .checked_sub(1)
                .and_then(|prev_idx| self.tokens.get(prev_idx)),
            immediate_next: self.tokens.get(idx.saturating_add(1)),
            immediate_next_next: self.tokens.get(idx.saturating_add(2)),
            next_non_comment_indices,
            next_word_indices,
        }
    }
}

#[derive(Clone, Copy, Debug, Default)]
struct FormatterTokenSpanRange {
    start: usize,
    end: usize,
}

struct FormatterStatementInput<'a> {
    statement: &'a str,
    mysql_compatible: bool,
    tokens: Vec<SqlToken>,
    token_spans: Vec<FormatterTokenSpanRange>,
    metadata: Option<FormatterStatementMetadata>,
    is_package_body_statement: bool,
    is_create_trigger_statement: bool,
    statement_has_apply: bool,
}

impl<'a> FormatterStatementInput<'a> {
    fn build(statement: &'a str, mysql_compatible: bool) -> Self {
        let mut token_spans =
            super::query_text::tokenize_sql_spanned_with_mysql_compat(statement, mysql_compatible);
        let mut tokens = Vec::with_capacity(token_spans.len());
        let mut token_ranges = Vec::with_capacity(token_spans.len());
        let mut metadata_builder = Some(FormatterStatementMetadataBuilder::new(
            token_spans.len(),
            false,
        ));
        let mut leading_words_upper: Vec<String> = Vec::with_capacity(8);
        let mut statement_has_apply = false;

        for (idx, span) in token_spans.iter_mut().enumerate() {
            let token = std::mem::replace(&mut span.token, SqlToken::Symbol(String::new()));
            if let SqlToken::Word(word) = &token {
                if leading_words_upper.len() < 8 {
                    leading_words_upper.push(word.to_ascii_uppercase());
                }
                if word.eq_ignore_ascii_case("APPLY") {
                    statement_has_apply = true;
                }
            }

            if metadata_builder.is_some()
                && FormatterStatementStructure::leading_words_start_create_table(
                    leading_words_upper.as_slice(),
                )
            {
                metadata_builder = None;
            }

            token_ranges.push(FormatterTokenSpanRange {
                start: span.start,
                end: span.end,
            });
            if let Some(builder) = metadata_builder.as_mut() {
                builder.observe(idx, &token);
            }
            tokens.push(token);
        }

        Self {
            statement,
            mysql_compatible,
            tokens,
            token_spans: token_ranges,
            metadata: metadata_builder.map(FormatterStatementMetadataBuilder::finish),
            is_package_body_statement:
                FormatterStatementStructure::leading_words_start_package_body(
                    leading_words_upper.as_slice(),
                ),
            is_create_trigger_statement:
                FormatterStatementStructure::leading_words_start_create_trigger(
                    leading_words_upper.as_slice(),
                ),
            statement_has_apply,
        }
    }

    fn word_upper(&self, idx: usize) -> Option<Cow<'_, str>> {
        match self.tokens.get(idx) {
            Some(SqlToken::Word(word)) => Some(ascii_uppercase_cow(word)),
            _ => None,
        }
    }
}

struct FormatterStatementRenderResult {
    formatted: String,
    has_code: bool,
    ends_with_semicolon: bool,
    should_append_missing_terminator: bool,
    is_create_trigger_statement: bool,
}

struct FormatterTokenLineCursor<'a> {
    bytes: &'a [u8],
    spans: &'a [FormatterTokenSpanRange],
    next_idx: usize,
    scan_pos: usize,
    newline_count: usize,
    current_profile: FormatterTokenLineProfile,
}

impl<'a> FormatterTokenLineCursor<'a> {
    fn new(bytes: &'a [u8], spans: &'a [FormatterTokenSpanRange]) -> Self {
        Self {
            bytes,
            spans,
            next_idx: 0,
            scan_pos: 0,
            newline_count: 0,
            current_profile: FormatterTokenLineProfile::default(),
        }
    }

    fn profile(&mut self, idx: usize) -> FormatterTokenLineProfile {
        while self.next_idx <= idx {
            let Some(span) = self.spans.get(self.next_idx) else {
                break;
            };
            let gap = self.bytes.get(self.scan_pos..span.start).unwrap_or(&[]);
            self.newline_count = self
                .newline_count
                .saturating_add(gap.iter().filter(|byte| **byte == b'\n').count());
            let token_start_newline_count = self.newline_count;

            let token_bytes = self.bytes.get(span.start..span.end).unwrap_or(&[]);
            self.newline_count = self
                .newline_count
                .saturating_add(token_bytes.iter().filter(|byte| **byte == b'\n').count());
            let token_end_newline_count = self.newline_count;
            self.scan_pos = span.end;
            self.current_profile = FormatterTokenLineProfile {
                start_newline_count: token_start_newline_count,
                end_newline_count: token_end_newline_count,
            };
            self.next_idx = self.next_idx.saturating_add(1);
        }

        self.current_profile
    }
}

#[derive(Default)]
struct FormatterTailState<'a> {
    saw_non_comment_code: bool,
    saw_mysql_executable_comment: bool,
    trailing_delimiter_stack: Vec<&'static str>,
    trailing_delimiters_balanced: bool,
    trailing_terminal_token: Option<&'a SqlToken>,
    previous_meaningful_token: Option<&'a SqlToken>,
    last_meaningful_token: Option<&'a SqlToken>,
}

impl<'a> FormatterTailState<'a> {
    fn new() -> Self {
        Self {
            trailing_delimiters_balanced: true,
            ..Self::default()
        }
    }

    fn observe(&mut self, token: &'a SqlToken) {
        match token {
            SqlToken::Comment(comment) => {
                if SqlEditorWidget::is_mysql_executable_comment_token(comment) {
                    self.saw_mysql_executable_comment = true;
                    self.trailing_terminal_token = Some(token);
                }
                return;
            }
            SqlToken::Word(_) | SqlToken::String(_) => {
                self.saw_non_comment_code = true;
                self.trailing_terminal_token = Some(token);
            }
            SqlToken::Symbol(symbol) => {
                self.saw_non_comment_code = true;
                match symbol.as_str() {
                    "(" => self.trailing_delimiter_stack.push(")"),
                    "[" => self.trailing_delimiter_stack.push("]"),
                    "{" => self.trailing_delimiter_stack.push("}"),
                    ")" | "]" | "}" => {
                        let Some(expected_close) = self.trailing_delimiter_stack.pop() else {
                            self.trailing_delimiters_balanced = false;
                            self.previous_meaningful_token = self.last_meaningful_token.take();
                            self.last_meaningful_token = Some(token);
                            return;
                        };
                        if expected_close != symbol.as_str() {
                            self.trailing_delimiters_balanced = false;
                        }
                    }
                    ";" => {
                        self.trailing_delimiter_stack.clear();
                        self.trailing_delimiters_balanced = true;
                        self.trailing_terminal_token = None;
                    }
                    _ => {}
                }
                if symbol != "/" && symbol != ";" {
                    self.trailing_terminal_token = Some(token);
                }
            }
        }

        self.previous_meaningful_token = self.last_meaningful_token.take();
        self.last_meaningful_token = Some(token);
    }

    fn has_code(&self, statement: &str) -> bool {
        !SqlEditorWidget::is_sqlplus_remark_comment_statement(statement)
            && (self.saw_non_comment_code || self.saw_mysql_executable_comment)
    }

    fn ends_with_semicolon(&self) -> bool {
        matches!(self.last_meaningful_token, Some(SqlToken::Symbol(sym)) if sym == ";")
            || matches!(
                (self.previous_meaningful_token, self.last_meaningful_token),
                (
                    Some(SqlToken::Symbol(previous)),
                    Some(SqlToken::Symbol(last))
                ) if previous == ";" && last == "/"
            )
    }

    fn should_append_missing_terminator(&self) -> bool {
        self.trailing_delimiters_balanced
            && self.trailing_delimiter_stack.is_empty()
            && self
                .trailing_terminal_token
                .is_some_and(SqlEditorWidget::token_can_terminate_statement)
    }
}

#[derive(Clone, Copy, Debug, Default)]
struct LiveQueryApplyFrame {
    start_paren_depth: usize,
    started_inside_paren: bool,
    has_apply: bool,
    in_apply_clause: bool,
}

impl LiveQueryApplyFrame {
    fn has_apply_context(self) -> bool {
        self.has_apply || self.in_apply_clause
    }
}

#[derive(Default)]
struct LiveQueryApplyTracker {
    active_frames: Vec<LiveQueryApplyFrame>,
    apply_clause_paren_depths: Vec<usize>,
    apply_paren_pending: bool,
}

impl LiveQueryApplyTracker {
    fn context(&self) -> bool {
        self.active_frames
            .last()
            .is_some_and(|frame| frame.has_apply_context())
    }

    fn observe(&mut self, token: &SqlToken, current_paren_depth: usize) {
        match token {
            SqlToken::Word(word) => {
                if word.eq_ignore_ascii_case("SELECT") {
                    self.active_frames.push(LiveQueryApplyFrame {
                        start_paren_depth: current_paren_depth,
                        started_inside_paren: current_paren_depth > 0,
                        has_apply: false,
                        in_apply_clause: self
                            .apply_clause_paren_depths
                            .last()
                            .is_some_and(|depth| *depth == current_paren_depth),
                    });
                    self.apply_paren_pending = false;
                } else if word.eq_ignore_ascii_case("APPLY") {
                    if let Some(frame) = self.active_frames.last_mut() {
                        frame.has_apply = true;
                    }
                    self.apply_paren_pending = true;
                } else {
                    self.apply_paren_pending = false;
                }
            }
            SqlToken::Symbol(symbol) if symbol == "(" => {
                if self.apply_paren_pending {
                    self.apply_clause_paren_depths
                        .push(current_paren_depth.saturating_add(1));
                    self.apply_paren_pending = false;
                }
            }
            SqlToken::Symbol(symbol) if symbol == ")" => {
                let after_close_depth = current_paren_depth.saturating_sub(1);
                while self
                    .apply_clause_paren_depths
                    .last()
                    .is_some_and(|depth| after_close_depth < *depth)
                {
                    let _ = self.apply_clause_paren_depths.pop();
                }
                while self.active_frames.last().is_some_and(|frame| {
                    frame.started_inside_paren && after_close_depth < frame.start_paren_depth
                }) {
                    let _ = self.active_frames.pop();
                }
                self.apply_paren_pending = false;
            }
            SqlToken::Symbol(symbol) if symbol == ";" => {
                while self
                    .active_frames
                    .last()
                    .is_some_and(|frame| !frame.started_inside_paren)
                {
                    let _ = self.active_frames.pop();
                }
                self.apply_paren_pending = false;
            }
            SqlToken::Comment(_) => {}
            _ => {
                self.apply_paren_pending = false;
            }
        }
    }
}

struct StatementWordLinks {
    prev_word_in_statement: Vec<Option<usize>>,
}

impl StatementWordLinks {
    fn prev_word_index(&self, idx: usize) -> Option<usize> {
        self.prev_word_in_statement.get(idx).copied().flatten()
    }
}

#[derive(Default)]
struct RenderedLineTracker {
    line_starts: Vec<usize>,
    scanned_len: usize,
}

impl RenderedLineTracker {
    fn sync(&mut self, out: &str) {
        if self.line_starts.is_empty() {
            self.line_starts.push(0);
        }

        if out.len() < self.scanned_len {
            self.scanned_len = out.len();
            while self.line_starts.len() > 1
                && self
                    .line_starts
                    .last()
                    .copied()
                    .is_some_and(|start| start > self.scanned_len)
            {
                let _ = self.line_starts.pop();
            }
        }

        if self.scanned_len >= out.len() {
            return;
        }

        let suffix_start = self.scanned_len;
        let Some(suffix) = out.get(suffix_start..) else {
            self.line_starts.clear();
            self.line_starts.push(0);
            self.scanned_len = 0;
            self.sync(out);
            return;
        };

        for (offset, byte) in suffix.bytes().enumerate() {
            if byte == b'\n' {
                self.line_starts.push(suffix_start + offset + 1);
            }
        }
        self.scanned_len = out.len();
    }

    fn current_line<'a>(&mut self, out: &'a str) -> &'a str {
        self.sync(out);
        let line_start = self.line_starts.last().copied().unwrap_or(0);
        out.get(line_start..).unwrap_or("")
    }

    fn line_before_trailing_newlines<'a>(&mut self, out: &'a str) -> &'a str {
        self.sync(out);
        let trimmed_len = out.trim_end_matches('\n').len();
        self.line_ending_at(out, trimmed_len)
    }

    fn active_query_owner_line<'a>(
        &mut self,
        out: &'a str,
        opener_starts_new_line: bool,
    ) -> &'a str {
        if opener_starts_new_line {
            self.line_before_trailing_newlines(out)
        } else {
            self.current_line(out)
        }
    }

    fn active_query_owner_line_info(
        &mut self,
        out: &str,
        opener_starts_new_line: bool,
    ) -> RenderedLineInfo {
        RenderedLineInfo::analyze(self.active_query_owner_line(out, opener_starts_new_line))
    }

    fn current_line_indent(&mut self, out: &str, fallback: usize) -> usize {
        let line = self.current_line(out);
        if line.is_empty() {
            fallback
        } else {
            rendered_line_indent(line)
        }
    }

    fn line_ending_at<'a>(&mut self, out: &'a str, end: usize) -> &'a str {
        if end == 0 {
            return "";
        }

        self.sync(out);
        let line_idx = self
            .line_starts
            .partition_point(|line_start| *line_start < end)
            .saturating_sub(1);
        let line_start = self.line_starts.get(line_idx).copied().unwrap_or(0);
        out.get(line_start..end).unwrap_or("")
    }

    fn previous_line<'a>(&mut self, out: &'a str) -> &'a str {
        self.sync(out);
        if self.line_starts.len() < 2 {
            return "";
        }

        let current_line_start = self.line_starts.last().copied().unwrap_or(0);
        let previous_line_end = current_line_start.saturating_sub(1);
        self.line_ending_at(out, previous_line_end)
    }
}

#[derive(Clone, Copy)]
struct RenderedLineInfo {
    indent: usize,
    starts_with_select: bool,
    starts_with_set: bool,
    starts_with_from: bool,
    ends_with_open_paren: bool,
    ends_with_as: bool,
    is_create_query_body_header: bool,
    ends_with_value: bool,
    ends_with_join_or_apply: bool,
}

impl RenderedLineInfo {
    fn analyze(line: &str) -> Self {
        let trimmed_start = line.trim_start();
        let ends_with_open_paren = line.trim_end().ends_with('(');
        let trailing_identifier =
            sql_text::trailing_identifier_words_before_inline_comment(line, 1)
                .into_iter()
                .next_back();
        let ends_with_as = trailing_identifier.is_some_and(|word| word.eq_ignore_ascii_case("AS"));
        let ends_with_value =
            trailing_identifier.is_some_and(|word| word.eq_ignore_ascii_case("VALUE"));
        let ends_with_join_or_apply = trailing_identifier.is_some_and(|word| {
            word.eq_ignore_ascii_case("JOIN") || word.eq_ignore_ascii_case("APPLY")
        });
        let is_create_query_body_header = ends_with_as
            && !ends_with_open_paren
            && sql_text::starts_with_keyword_token(trimmed_start, "CREATE")
            && sql_text::line_is_create_query_body_header(line);

        Self {
            indent: rendered_line_indent(line),
            starts_with_select: sql_text::starts_with_keyword_token(trimmed_start, "SELECT"),
            starts_with_set: sql_text::starts_with_keyword_token(trimmed_start, "SET"),
            starts_with_from: sql_text::starts_with_keyword_token(trimmed_start, "FROM"),
            ends_with_open_paren,
            ends_with_as,
            is_create_query_body_header,
            ends_with_value,
            ends_with_join_or_apply,
        }
    }
}

#[derive(Clone, Copy)]
struct QueryLikeOwnerLineInfo {
    indent: usize,
    ends_with_open_paren: bool,
    ends_with_as: bool,
    is_create_query_body_header: bool,
    ends_with_value: bool,
    ends_with_join_or_apply: bool,
    starts_with_select: bool,
    starts_with_set: bool,
    starts_with_from: bool,
    owner_kind: Option<sql_text::FormatQueryOwnerKind>,
}

impl QueryLikeOwnerLineInfo {
    fn from_rendered_line_info(
        line_info: RenderedLineInfo,
        owner_kind: Option<sql_text::FormatQueryOwnerKind>,
    ) -> Self {
        Self {
            indent: line_info.indent,
            ends_with_open_paren: line_info.ends_with_open_paren,
            ends_with_as: line_info.ends_with_as,
            is_create_query_body_header: line_info.is_create_query_body_header,
            ends_with_value: line_info.ends_with_value,
            ends_with_join_or_apply: line_info.ends_with_join_or_apply,
            starts_with_select: line_info.starts_with_select,
            starts_with_set: line_info.starts_with_set,
            starts_with_from: line_info.starts_with_from,
            owner_kind,
        }
    }

    fn starts_with_active_clause(self, current_clause: Option<&str>) -> bool {
        match current_clause {
            Some("SELECT") => self.starts_with_select,
            Some("SET") => self.starts_with_set,
            Some("FROM") => self.starts_with_from,
            _ => false,
        }
    }
}

fn rendered_line_indent(line: &str) -> usize {
    // Leading indentation is always plain ASCII spaces, so byte-iteration is
    // both byte-offset compliant (per CLAUDE.md) and avoids per-call UTF-8
    // decoding on a hot path that runs once per token rendering.
    SqlEditorWidget::safe_indent_div(line.bytes().take_while(|b| *b == b' ').count())
}

fn push_spaces(out: &mut String, count: usize) {
    out.extend(std::iter::repeat_n(' ', count));
}

fn newline_with_spaces(
    out: &mut String,
    indent_spaces: usize,
    at_line_start: &mut bool,
    needs_space: &mut bool,
    line_indent: &mut usize,
    current_line_significant_paren_depth: &Cell<usize>,
) {
    if !out.is_empty() && !out.ends_with('\n') {
        out.push('\n');
    }
    push_spaces(out, indent_spaces);
    *line_indent = SqlEditorWidget::safe_indent_div(indent_spaces);
    *at_line_start = false;
    *needs_space = false;
    current_line_significant_paren_depth.set(0);
}

fn ensure_indent(out: &mut String, at_line_start: &mut bool, line_indent: usize) {
    if *at_line_start {
        push_spaces(out, line_indent.saturating_mul(4));
        *at_line_start = false;
    }
}

#[derive(Clone, Copy, Default)]
struct FormatBlockDepthFrame {
    owner_depth: usize,
    /// The `indent_level` assigned to this block's body when the frame was
    /// pushed. Used in debug builds to detect drift at statement boundaries
    /// inside the block (see `FormatFrameStack::statement_base_indent`).
    body_indent_level: usize,
}

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
enum BlockKind {
    Begin,
    Case,
    CompoundTrigger,
    Declare,
    Exception,
    If,
    Loop,
    PackageBody,
    Repeat,
    While,
}

impl BlockKind {
    const COUNT: usize = 10;

    fn from_keyword(keyword: &str) -> Option<Self> {
        match keyword {
            "BEGIN" => Some(Self::Begin),
            "CASE" => Some(Self::Case),
            "COMPOUND_TRIGGER" => Some(Self::CompoundTrigger),
            "DECLARE" => Some(Self::Declare),
            "EXCEPTION" => Some(Self::Exception),
            "IF" => Some(Self::If),
            "LOOP" => Some(Self::Loop),
            "PACKAGE_BODY" => Some(Self::PackageBody),
            "REPEAT" => Some(Self::Repeat),
            "WHILE" => Some(Self::While),
            _ => None,
        }
    }

    fn as_str(self) -> &'static str {
        match self {
            Self::Begin => "BEGIN",
            Self::Case => "CASE",
            Self::CompoundTrigger => "COMPOUND_TRIGGER",
            Self::Declare => "DECLARE",
            Self::Exception => "EXCEPTION",
            Self::If => "IF",
            Self::Loop => "LOOP",
            Self::PackageBody => "PACKAGE_BODY",
            Self::Repeat => "REPEAT",
            Self::While => "WHILE",
        }
    }

    fn supports_qualified_end(self) -> bool {
        matches!(
            self,
            Self::Case | Self::If | Self::Loop | Self::Repeat | Self::While
        )
    }

    fn closes_plain_end(self) -> bool {
        matches!(
            self,
            Self::Begin | Self::CompoundTrigger | Self::Declare | Self::PackageBody
        )
    }

    fn index(self) -> usize {
        match self {
            Self::Begin => 0,
            Self::Case => 1,
            Self::CompoundTrigger => 2,
            Self::Declare => 3,
            Self::Exception => 4,
            Self::If => 5,
            Self::Loop => 6,
            Self::PackageBody => 7,
            Self::Repeat => 8,
            Self::While => 9,
        }
    }
}

trait BlockKindMatcher {
    fn matches(&self, kind: BlockKind) -> bool;
    fn as_block_kind(&self) -> Option<BlockKind>;
}

impl BlockKindMatcher for BlockKind {
    fn matches(&self, kind: BlockKind) -> bool {
        *self == kind
    }

    fn as_block_kind(&self) -> Option<BlockKind> {
        Some(*self)
    }
}

impl BlockKindMatcher for &'static str {
    fn matches(&self, kind: BlockKind) -> bool {
        kind.as_str() == *self
    }

    fn as_block_kind(&self) -> Option<BlockKind> {
        BlockKind::from_keyword(self)
    }
}

struct FormatterStatementStructure;

impl FormatterStatementStructure {
    fn recent_statement_words_before<'a>(
        tokens: &'a [SqlToken],
        recent_statement_word_indices: &VecDeque<usize>,
        max_words: usize,
    ) -> Vec<&'a str> {
        recent_statement_word_indices
            .iter()
            .rev()
            .take(max_words)
            .filter_map(|word_idx| match tokens.get(*word_idx) {
                Some(SqlToken::Word(word)) => Some(word.as_str()),
                _ => None,
            })
            .collect()
    }

    fn paren_multiline_clause_owner_kind_at_open_paren(
        tokens: &[SqlToken],
        recent_statement_word_indices: &VecDeque<usize>,
    ) -> Option<FormatIndentedParenOwnerKind> {
        let mut recent_words = [None; 8];
        let mut recent_word_count = 0usize;

        for word_idx in recent_statement_word_indices.iter().rev().take(8) {
            let Some(SqlToken::Word(word)) = tokens.get(*word_idx) else {
                continue;
            };
            recent_words[recent_word_count] = Some(word.as_str());
            recent_word_count = recent_word_count.saturating_add(1);
            if recent_word_count == recent_words.len() {
                break;
            }
        }

        if recent_word_count == 0 {
            return None;
        }

        let last_word = recent_words[0]?;
        let penultimate_word = if recent_word_count > 1 {
            recent_words[1]
        } else {
            None
        };
        let third_from_end_word = if recent_word_count > 2 {
            recent_words[2]
        } else {
            None
        };
        let first_word = recent_words[recent_word_count - 1]?;
        let second_word = if recent_word_count > 1 {
            recent_words[recent_word_count - 2]
        } else {
            None
        };
        let third_word = if recent_word_count > 2 {
            recent_words[recent_word_count - 3]
        } else {
            None
        };

        if last_word.eq_ignore_ascii_case("OVER") {
            Some(FormatIndentedParenOwnerKind::AnalyticOver)
        } else if last_word.eq_ignore_ascii_case("GROUP")
            && penultimate_word.is_some_and(|word| word.eq_ignore_ascii_case("WITHIN"))
        {
            Some(FormatIndentedParenOwnerKind::WithinGroup)
        } else if last_word.eq_ignore_ascii_case("KEEP") {
            Some(FormatIndentedParenOwnerKind::Keep)
        } else if FormatIndentedParenOwnerKind::ModelSubclause.starts_body_header_words(
            first_word,
            second_word,
            third_word,
        ) {
            Some(FormatIndentedParenOwnerKind::ModelSubclause)
        } else if first_word.eq_ignore_ascii_case("WINDOW") && last_word.eq_ignore_ascii_case("AS")
        {
            Some(FormatIndentedParenOwnerKind::Window)
        } else if last_word.eq_ignore_ascii_case("MATCH_RECOGNIZE") {
            Some(FormatIndentedParenOwnerKind::MatchRecognize)
        } else if last_word.eq_ignore_ascii_case("PIVOT")
            || (last_word.eq_ignore_ascii_case("XML")
                && penultimate_word.is_some_and(|word| word.eq_ignore_ascii_case("PIVOT")))
        {
            Some(FormatIndentedParenOwnerKind::Pivot)
        } else if last_word.eq_ignore_ascii_case("UNPIVOT")
            || (last_word.eq_ignore_ascii_case("NULLS")
                && penultimate_word.is_some_and(|word| {
                    word.eq_ignore_ascii_case("INCLUDE") || word.eq_ignore_ascii_case("EXCLUDE")
                })
                && third_from_end_word.is_some_and(|word| word.eq_ignore_ascii_case("UNPIVOT")))
        {
            Some(FormatIndentedParenOwnerKind::Unpivot)
        } else if last_word.eq_ignore_ascii_case("COLUMNS") {
            Some(FormatIndentedParenOwnerKind::StructuredColumns)
        } else {
            None
        }
    }

    fn leading_words_start_create_trigger(words: &[String]) -> bool {
        words.first().is_some_and(|word| word == "CREATE")
            && words.iter().skip(1).any(|word| word == "TRIGGER")
    }

    fn leading_words_start_create_table(words: &[String]) -> bool {
        let mut idx = 0usize;
        if words.get(idx).map(String::as_str) != Some("CREATE") {
            return false;
        }
        idx = idx.saturating_add(1);

        if words.get(idx).map(String::as_str) == Some("OR") {
            idx = idx.saturating_add(1);
            if words.get(idx).map(String::as_str) != Some("REPLACE") {
                return false;
            }
            idx = idx.saturating_add(1);
        }

        if matches!(
            words.get(idx).map(String::as_str),
            Some("GLOBAL" | "PRIVATE")
        ) {
            idx = idx.saturating_add(1);
            if words.get(idx).map(String::as_str) != Some("TEMPORARY") {
                return false;
            }
            idx = idx.saturating_add(1);
        }

        words.get(idx).map(String::as_str) == Some("TABLE")
    }

    fn leading_words_start_package_body(words: &[String]) -> bool {
        let mut idx = 0usize;
        if words.get(idx).map(String::as_str) != Some("CREATE") {
            return false;
        }
        idx = idx.saturating_add(1);

        if words.get(idx).map(String::as_str) == Some("OR") {
            idx = idx.saturating_add(1);
            if words.get(idx).map(String::as_str) != Some("REPLACE") {
                return false;
            }
            idx = idx.saturating_add(1);
        }

        while matches!(
            words.get(idx).map(String::as_str),
            Some("EDITIONABLE" | "NONEDITIONABLE")
        ) {
            idx = idx.saturating_add(1);
        }

        matches!(
            (
                words.get(idx).map(String::as_str),
                words.get(idx.saturating_add(1)).map(String::as_str)
            ),
            (Some("PACKAGE"), Some("BODY"))
        )
    }
}

fn append_comment_prefix_token(prefix_text: &mut String, token: &SqlToken) {
    if matches!(token, SqlToken::Comment(_)) {
        return;
    }

    if !prefix_text.is_empty() {
        prefix_text.push(' ');
    }

    match token {
        SqlToken::Word(word) => prefix_text.push_str(word),
        SqlToken::String(literal) => prefix_text.push_str(literal),
        SqlToken::Symbol(symbol) => prefix_text.push_str(symbol),
        SqlToken::Comment(_) => {}
    }
}

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
enum ParenFormatFrameKind {
    Compact,
    WrappedLayout,
    ColumnList,
    QueryLike,
}

impl ParenFormatFrameKind {
    fn is_query_like(self) -> bool {
        matches!(self, Self::QueryLike)
    }

    fn is_column_list(self) -> bool {
        matches!(self, Self::ColumnList)
    }

    fn opens_indented(self) -> bool {
        matches!(
            self,
            Self::WrappedLayout | Self::ColumnList | Self::QueryLike
        )
    }

    fn closes_indented(self) -> bool {
        matches!(
            self,
            Self::WrappedLayout | Self::ColumnList | Self::QueryLike
        )
    }

    fn counts_for_paren_extra(self) -> bool {
        !matches!(self, Self::ColumnList | Self::QueryLike)
    }

    fn suppresses_comma_breaks(self) -> bool {
        matches!(self, Self::Compact)
    }

    fn indent_level_delta(self) -> usize {
        if self.is_query_like() {
            2
        } else if self.opens_indented() {
            1
        } else {
            0
        }
    }
}

#[derive(Clone, Copy, PartialEq, Eq)]
struct ParenFormatFrame {
    kind: ParenFormatFrameKind,
    open_line_indent: usize,
    opened_at_line_start: bool,
    indent_level_delta: usize,
    close_indent_override: Option<usize>,
    query_like_layout: Option<QueryLikeParenLayout>,
    body_indent: Option<usize>,
    pending_multiline_child_continuation: bool,
    post_multiline_child_tail_token_count: u8,
}

impl ParenFormatFrame {
    fn new(
        kind: ParenFormatFrameKind,
        open_line_indent: usize,
        opened_at_line_start: bool,
        indent_level_delta: usize,
        close_indent_override: Option<usize>,
    ) -> Self {
        Self {
            kind,
            open_line_indent,
            opened_at_line_start,
            indent_level_delta,
            close_indent_override,
            query_like_layout: None,
            body_indent: None,
            pending_multiline_child_continuation: false,
            post_multiline_child_tail_token_count: 0,
        }
    }

    fn with_query_like_layout(mut self, query_like_layout: Option<QueryLikeParenLayout>) -> Self {
        self.query_like_layout = query_like_layout;
        self
    }

    fn is_query_like(self) -> bool {
        self.kind.is_query_like()
    }

    fn is_column_list(self) -> bool {
        self.kind.is_column_list()
    }

    fn opens_indented(self) -> bool {
        self.kind.opens_indented()
    }

    fn closes_indented(self) -> bool {
        self.kind.closes_indented()
    }

    fn counts_for_paren_extra(self) -> bool {
        self.kind.counts_for_paren_extra()
    }

    fn suppresses_comma_breaks(self) -> bool {
        self.kind.suppresses_comma_breaks()
    }

    fn indent_level_delta(self) -> usize {
        self.indent_level_delta
    }

    fn close_indent(self) -> usize {
        if self.closes_indented() {
            self.query_like_layout
                .map(|layout| layout.close_depth)
                .or(self.close_indent_override)
                .unwrap_or(self.open_line_indent)
        } else {
            0
        }
    }

    fn compact_multiline_close_indent(self) -> usize {
        self.open_line_indent
    }

    fn mark_multiline_child_closed(&mut self) {
        self.pending_multiline_child_continuation = true;
        self.post_multiline_child_tail_token_count = 0;
    }

    fn note_continuation_token(&mut self) {
        if self.pending_multiline_child_continuation {
            self.pending_multiline_child_continuation = false;
        }
        self.post_multiline_child_tail_token_count = self
            .post_multiline_child_tail_token_count
            .saturating_add(1)
            .min(2);
    }

    fn has_pending_multiline_child_continuation(self) -> bool {
        self.pending_multiline_child_continuation
    }

    fn sibling_body_indent(self) -> Option<usize> {
        (!self.is_query_like() && !self.is_column_list()).then_some(
            self.body_indent
                .unwrap_or_else(|| self.open_line_indent.saturating_add(1)),
        )
    }

    fn record_body_indent(&mut self, indent: usize) {
        if self.is_query_like() || self.is_column_list() || indent <= self.open_line_indent {
            return;
        }

        self.body_indent = Some(
            self.body_indent
                .map(|current| current.min(indent))
                .unwrap_or(indent),
        );
    }

    fn has_simple_multiline_child_tail(self) -> bool {
        self.post_multiline_child_tail_token_count == 1
    }

    fn contributes_multiline_close(self) -> bool {
        self.opens_indented()
            || self.pending_multiline_child_continuation
            || self.has_simple_multiline_child_tail()
    }
}

#[derive(Clone, Copy, PartialEq, Eq)]
struct QueryLikeParenLayout {
    owner_depth: usize,
    child_head_depth: usize,
    close_depth: usize,
}

#[derive(Clone, Copy, Debug, Default)]
struct ParenBodyAnalysis {
    contains_query_like_child: bool,
    contains_analytic_clause_owner_child: bool,
    has_top_level_comma: bool,
}

struct FormatterStatementMetadata {
    meaningful_token_links: MeaningfulTokenLinks,
    statement_word_links: StatementWordLinks,
    matching_paren_close_indices: Vec<Option<usize>>,
    paren_body_analyses: Vec<Option<ParenBodyAnalysis>>,
    fetch_into_has_multiple_targets: Vec<bool>,
    within_group_analytic_groups: Vec<bool>,
}

#[derive(Clone, Copy, Default)]
struct ParenBodyScanState {
    has_top_level_comma: bool,
    contains_query_like_child: bool,
    contains_analytic_clause_owner_child: bool,
    saw_within: bool,
    within_group_group_idx: Option<usize>,
}

struct FormatterStatementMetadataBuilder {
    include_navigation_links: bool,
    prev_meaningful: Vec<Option<usize>>,
    next_meaningful: Vec<Option<usize>>,
    prev_word: Vec<Option<usize>>,
    last_meaningful_idx: Option<usize>,
    last_word_idx: Option<usize>,
    next_meaningful_fill_start: usize,
    matching_paren_close_indices: Vec<Option<usize>>,
    paren_body_analyses: Vec<Option<ParenBodyAnalysis>>,
    open_paren_stack: Vec<(usize, ParenBodyScanState)>,
    token_paren_depth: usize,
    latest_into_at_depth: Vec<Option<usize>>,
    fetch_into_has_multiple_targets: Vec<bool>,
    within_group_analytic_groups: Vec<bool>,
    pending_within_group_group_idx: Option<usize>,
    pending_closed_within_group_group_idx: Option<usize>,
    saw_within_keyword: bool,
}

impl FormatterStatementMetadataBuilder {
    fn new(token_len: usize, include_navigation_links: bool) -> Self {
        Self {
            include_navigation_links,
            prev_meaningful: if include_navigation_links {
                vec![None; token_len]
            } else {
                Vec::new()
            },
            next_meaningful: if include_navigation_links {
                vec![None; token_len]
            } else {
                Vec::new()
            },
            prev_word: if include_navigation_links {
                vec![None; token_len]
            } else {
                Vec::new()
            },
            last_meaningful_idx: None,
            last_word_idx: None,
            next_meaningful_fill_start: 0,
            matching_paren_close_indices: vec![None; token_len],
            paren_body_analyses: vec![None; token_len],
            open_paren_stack: Vec::new(),
            token_paren_depth: 0,
            latest_into_at_depth: Vec::new(),
            fetch_into_has_multiple_targets: vec![false; token_len],
            within_group_analytic_groups: vec![false; token_len],
            pending_within_group_group_idx: None,
            pending_closed_within_group_group_idx: None,
            saw_within_keyword: false,
        }
    }

    fn observe(&mut self, idx: usize, token: &SqlToken) {
        if !matches!(token, SqlToken::Comment(_)) {
            self.resolve_pending_closed_within_group(token);
        }

        if self.include_navigation_links {
            self.prev_meaningful[idx] = self.last_meaningful_idx;
            self.prev_word[idx] = self.last_word_idx;
        }

        let is_meaningful = !matches!(token, SqlToken::Comment(_));
        if is_meaningful {
            if self.include_navigation_links {
                for unresolved_idx in self.next_meaningful_fill_start..idx {
                    self.next_meaningful[unresolved_idx] = Some(idx);
                }
                self.next_meaningful_fill_start = idx;
            }
            self.last_meaningful_idx = Some(idx);
            if matches!(token, SqlToken::Word(_)) {
                self.last_word_idx = Some(idx);
            }
        }

        if self.latest_into_at_depth.len() <= self.token_paren_depth {
            self.latest_into_at_depth
                .resize_with(self.token_paren_depth + 1, || None);
        }

        match token {
            SqlToken::Word(word) => {
                let within_group_sequence =
                    self.saw_within_keyword && word.eq_ignore_ascii_case("GROUP");
                if let Some((_, state)) = self.open_paren_stack.last_mut() {
                    if word.eq_ignore_ascii_case("CASE")
                        || crate::sql_text::is_subquery_head_keyword(word)
                    {
                        state.contains_query_like_child = true;
                    }
                    if word.eq_ignore_ascii_case("OVER") || word.eq_ignore_ascii_case("KEEP") {
                        state.contains_analytic_clause_owner_child = true;
                    }

                    if word.eq_ignore_ascii_case("WITHIN") {
                        state.saw_within = true;
                    } else if state.saw_within && word.eq_ignore_ascii_case("GROUP") {
                        state.contains_analytic_clause_owner_child = true;
                        state.saw_within = false;
                    } else {
                        state.saw_within = false;
                    }
                }

                if word.eq_ignore_ascii_case("WITHIN") {
                    self.saw_within_keyword = true;
                } else if within_group_sequence {
                    self.pending_within_group_group_idx = Some(idx);
                    self.saw_within_keyword = false;
                } else {
                    self.saw_within_keyword = false;
                }

                if word.eq_ignore_ascii_case("INTO") {
                    self.latest_into_at_depth[self.token_paren_depth] = Some(idx);
                } else if word.eq_ignore_ascii_case("LIMIT")
                    || word.eq_ignore_ascii_case("BULK")
                    || word.eq_ignore_ascii_case("FROM")
                    || word.eq_ignore_ascii_case("WHERE")
                {
                    self.latest_into_at_depth[self.token_paren_depth] = None;
                }
            }
            SqlToken::Symbol(symbol) if symbol == "(" => {
                self.saw_within_keyword = false;
                if let Some((_, state)) = self.open_paren_stack.last_mut() {
                    state.saw_within = false;
                }

                self.open_paren_stack.push((
                    idx,
                    ParenBodyScanState {
                        within_group_group_idx: self.pending_within_group_group_idx.take(),
                        ..ParenBodyScanState::default()
                    },
                ));
                self.token_paren_depth = self.token_paren_depth.saturating_add(1);
            }
            SqlToken::Symbol(symbol) if symbol == ")" => {
                self.saw_within_keyword = false;
                if let Some((open_idx, state)) = self.open_paren_stack.pop() {
                    self.matching_paren_close_indices[open_idx] = Some(idx);
                    self.paren_body_analyses[open_idx] = Some(ParenBodyAnalysis {
                        contains_query_like_child: state.contains_query_like_child,
                        contains_analytic_clause_owner_child: state
                            .contains_analytic_clause_owner_child,
                        has_top_level_comma: state.has_top_level_comma,
                    });

                    if let Some(group_idx) = state.within_group_group_idx {
                        self.pending_closed_within_group_group_idx = Some(group_idx);
                    }

                    if let Some((_, parent)) = self.open_paren_stack.last_mut() {
                        parent.contains_query_like_child |= state.contains_query_like_child;
                        parent.contains_analytic_clause_owner_child |=
                            state.contains_analytic_clause_owner_child;
                        parent.saw_within = false;
                    }
                } else {
                    self.pending_within_group_group_idx = None;
                }

                if self.token_paren_depth < self.latest_into_at_depth.len() {
                    self.latest_into_at_depth[self.token_paren_depth] = None;
                }
                self.token_paren_depth = self.token_paren_depth.saturating_sub(1);
            }
            SqlToken::Symbol(symbol) if symbol == "," => {
                self.saw_within_keyword = false;
                if let Some(into_idx) = self.latest_into_at_depth[self.token_paren_depth] {
                    self.fetch_into_has_multiple_targets[into_idx] = true;
                }

                if let Some((_, state)) = self.open_paren_stack.last_mut() {
                    state.has_top_level_comma = true;
                    state.saw_within = false;
                }
            }
            SqlToken::Symbol(symbol) if symbol == ";" => {
                self.latest_into_at_depth[self.token_paren_depth] = None;
                self.pending_within_group_group_idx = None;
                self.saw_within_keyword = false;
            }
            SqlToken::Comment(_) => {}
            _ => {
                self.pending_within_group_group_idx = None;
                self.saw_within_keyword = false;
                if let Some((_, state)) = self.open_paren_stack.last_mut() {
                    state.saw_within = false;
                }
            }
        }
    }

    fn resolve_pending_closed_within_group(&mut self, token: &SqlToken) {
        let Some(group_idx) = self.pending_closed_within_group_group_idx.take() else {
            return;
        };

        if matches!(token, SqlToken::Word(word) if word.eq_ignore_ascii_case("OVER")) {
            self.within_group_analytic_groups[group_idx] = true;
        }
    }

    fn finish(self) -> FormatterStatementMetadata {
        FormatterStatementMetadata {
            meaningful_token_links: MeaningfulTokenLinks {
                prev: self.prev_meaningful,
                next: self.next_meaningful,
            },
            statement_word_links: StatementWordLinks {
                prev_word_in_statement: self.prev_word,
            },
            matching_paren_close_indices: self.matching_paren_close_indices,
            paren_body_analyses: self.paren_body_analyses,
            fetch_into_has_multiple_targets: self.fetch_into_has_multiple_targets,
            within_group_analytic_groups: self.within_group_analytic_groups,
        }
    }
}

#[derive(Clone, Copy, Default)]
struct ActiveParenMetrics {
    query_like_count: usize,
    paren_extra_count: usize,
    compact_non_query_since_last_query: usize,
    paren_extra_since_last_query: usize,
    last_compact_open_line_indent: Option<usize>,
    match_recognize_count: usize,
    analytic_over_count: usize,
    window_clause_count: usize,
    keep_count: usize,
}

#[derive(Clone, Default)]
struct FormatRuntimeState {
    query_base_depth: Option<usize>,
    current_clause: Option<String>,
    select_list_layout_state: SelectListLayoutState,
    statement_has_with_clause: bool,
    query_body_clause_base_depth: Option<usize>,
    open_cursor_state: OpenCursorFormatState,
}

#[derive(Clone)]
struct QueryRuntimeFrame {
    scope: FormatScope,
    state: FormatRuntimeState,
}

#[derive(Clone, Copy, Default, PartialEq, Eq)]
struct ParenSemanticFlags {
    match_recognize: bool,
    analytic_over: bool,
    window_clause: bool,
    keep: bool,
}

#[derive(Clone)]
struct ParenStackFrame {
    id: SqlFormatFrameId,
    frame: ParenFormatFrame,
    semantic_flags: ParenSemanticFlags,
    previous_active_paren_metrics: ActiveParenMetrics,
    previous_paren_frame_idx: Option<usize>,
    previous_paren_frame_id: Option<SqlFormatFrameId>,
    wrapped_owner_kind: Option<FormatIndentedParenOwnerKind>,
    open_cursor_restore_state: Option<OpenCursorFormatState>,
}

#[derive(Clone)]
struct BlockStackFrame {
    id: SqlFormatFrameId,
    kind: BlockKind,
    depth_frame: FormatBlockDepthFrame,
    previous_base_indent_level: usize,
    previous_block_frame_idx: Option<usize>,
    previous_block_frame_id: Option<SqlFormatFrameId>,
    case_branch_started: bool,
    exception_branch_started: bool,
    mysql_handler_begin: bool,
}

struct PoppedBlockFrame {
    kind: BlockKind,
    depth_frame: FormatBlockDepthFrame,
    mysql_handler_begin: bool,
}

#[derive(Clone, Copy, PartialEq, Eq)]
enum ConditionOwnerKind {
    JoinOn,
    Where,
    Case,
    ControlHeader,
}

impl ConditionOwnerKind {
    const COUNT: usize = 4;

    fn index(self) -> usize {
        match self {
            Self::JoinOn => 0,
            Self::Where => 1,
            Self::Case => 2,
            Self::ControlHeader => 3,
        }
    }
}

#[derive(Clone, Copy)]
struct ConditionOwnerFrame {
    kind: ConditionOwnerKind,
    scope: FormatScope,
    indent: usize,
    parenthesized: bool,
}

#[derive(Clone, Copy, PartialEq, Eq)]
struct BetweenPendingFrame {
    scope: FormatScope,
}

#[derive(Clone, Copy)]
struct PendingSplitEndSuffixFrame {
    scope: FormatScope,
    indent: usize,
    kind: PendingSplitEndSuffixKind,
}

#[derive(Clone, Copy)]
struct PendingPlsqlLabelFrame {
    scope: FormatScope,
    body_indent: usize,
}

#[derive(Clone, Copy)]
struct MySqlHandlerPendingBodyIndentFrame {
    scope: FormatScope,
    indent: usize,
}

#[derive(Clone, Copy, PartialEq, Eq)]
enum ScopedIndentKind {
    MergeWhenBranch,
    CursorSql,
    ForallBody,
    MySqlHandlerBody,
    SplitControlBody,
}

impl ScopedIndentKind {
    const COUNT: usize = 5;

    fn index(self) -> usize {
        match self {
            Self::MergeWhenBranch => 0,
            Self::CursorSql => 1,
            Self::ForallBody => 2,
            Self::MySqlHandlerBody => 3,
            Self::SplitControlBody => 4,
        }
    }
}

#[derive(Clone, Copy)]
struct ScopedIndentFrame {
    kind: ScopedIndentKind,
    scope: FormatScope,
    delta: usize,
}

#[derive(Clone)]
enum FormatFrame {
    Paren(Box<ParenStackFrame>),
    Block(BlockStackFrame),
    QueryRuntime(QueryRuntimeFrame),
    ScopedIndent(ScopedIndentFrame),
    ConstructFlag(ConstructFlagFrame),
    ConstructValue(ConstructValueFrame),
    ConditionOwner(ConditionOwnerFrame),
    BetweenPending(BetweenPendingFrame),
    PendingSplitEndSuffix(PendingSplitEndSuffixFrame),
    PendingPlsqlLabel(PendingPlsqlLabelFrame),
    MySqlHandlerPendingBodyIndent(MySqlHandlerPendingBodyIndentFrame),
    TriggerHeader(FormatScope),
    PlsqlContext(FormatScope),
    CompoundTrigger(CompoundTriggerFrame),
    WithCte(WithCteFrame),
    OpenCursorRestore(OpenCursorFormatState),
}

struct PoppedParenFrame {
    frame: ParenFormatFrame,
    restored_runtime_state: bool,
    open_cursor_restore_state: Option<OpenCursorFormatState>,
}

#[cfg(test)]
#[derive(Clone, Copy, Debug, PartialEq, Eq)]
enum FormatFrameTransitionKind {
    TokenEntrySync,
    StatementBoundaryReset,
    PushParen {
        kind: ParenFormatFrameKind,
    },
    PopParen {
        kind: ParenFormatFrameKind,
        restored_runtime_state: bool,
    },
    PushBlock {
        kind: BlockKind,
    },
    PopBlock {
        kind: BlockKind,
    },
}

#[cfg(test)]
#[derive(Clone, Copy, Debug, PartialEq, Eq)]
struct FormatFrameTransitionEvent {
    kind: FormatFrameTransitionKind,
    scope_before: FormatScope,
    scope_after: FormatScope,
    indent_before: usize,
    indent_after: usize,
}

struct FormatFrameStack {
    frames: Vec<FormatFrame>,
    paren_depth: usize,
    block_depth: usize,
    active_paren_metrics: ActiveParenMetrics,
    current_runtime_state: FormatRuntimeState,
    base_indent_level: usize,
    last_paren_frame_id_cache: Option<SqlFormatFrameId>,
    last_block_frame_id_cache: Option<SqlFormatFrameId>,
    last_paren_frame_idx: Option<usize>,
    last_block_frame_idx: Option<usize>,
    block_kind_frame_indices: Vec<Vec<usize>>,
    query_runtime_frame_indices: Vec<usize>,
    condition_owner_frame_indices: Vec<Vec<usize>>,
    scoped_indent_frame_indices: Vec<Vec<usize>>,
    pending_split_end_suffix_frame_idx: Option<usize>,
    pending_plsql_label_frame_idx: Option<usize>,
    mysql_handler_pending_body_indent_frame_idx: Option<usize>,
    trigger_header_frame_indices: Vec<usize>,
    plsql_context_frame_indices: Vec<usize>,
    compound_trigger_frame_indices: Vec<usize>,
    with_cte_frame_indices: Vec<usize>,
    construct_flag_scopes: Vec<Vec<FormatScope>>,
    construct_flag_frame_indices: Vec<Vec<usize>>,
    construct_value_frame_indices: Vec<Vec<usize>>,
    frame_id_allocator: SqlFormatFrameIdAllocator,
    #[cfg(test)]
    transition_trace: Vec<FormatFrameTransitionEvent>,
}

impl Default for FormatFrameStack {
    fn default() -> Self {
        Self {
            frames: Vec::new(),
            paren_depth: 0,
            block_depth: 0,
            active_paren_metrics: ActiveParenMetrics::default(),
            current_runtime_state: FormatRuntimeState::default(),
            base_indent_level: 0,
            last_paren_frame_id_cache: None,
            last_block_frame_id_cache: None,
            last_paren_frame_idx: None,
            last_block_frame_idx: None,
            block_kind_frame_indices: vec![Vec::new(); BlockKind::COUNT],
            query_runtime_frame_indices: Vec::new(),
            condition_owner_frame_indices: vec![Vec::new(); ConditionOwnerKind::COUNT],
            scoped_indent_frame_indices: vec![Vec::new(); ScopedIndentKind::COUNT],
            pending_split_end_suffix_frame_idx: None,
            pending_plsql_label_frame_idx: None,
            mysql_handler_pending_body_indent_frame_idx: None,
            trigger_header_frame_indices: Vec::new(),
            plsql_context_frame_indices: Vec::new(),
            compound_trigger_frame_indices: Vec::new(),
            with_cte_frame_indices: Vec::new(),
            construct_flag_scopes: vec![Vec::new(); ConstructFlagKind::COUNT],
            construct_flag_frame_indices: vec![Vec::new(); ConstructFlagKind::COUNT],
            construct_value_frame_indices: vec![Vec::new(); ConstructValueKind::COUNT],
            frame_id_allocator: SqlFormatFrameIdAllocator::default(),
            #[cfg(test)]
            transition_trace: Vec::new(),
        }
    }
}

impl FormatFrameStack {
    fn current_query_runtime_frame_idx(&self) -> Option<usize> {
        let idx = *self.query_runtime_frame_indices.last()?;
        #[cfg(debug_assertions)]
        {
            match self.frames.get(idx) {
                Some(FormatFrame::QueryRuntime(frame)) => {
                    debug_assert!(
                        frame.scope.contains(self.current_scope()),
                        "query runtime frame survived outside its scope"
                    );
                }
                _ => debug_assert!(false, "query runtime frame index drift"),
            }
        }
        Some(idx)
    }

    fn last_paren_stack_frame(&self) -> Option<&ParenStackFrame> {
        let idx = self.last_paren_frame_idx?;
        match self.frames.get(idx) {
            Some(FormatFrame::Paren(frame)) => Some(frame),
            _ => None,
        }
    }

    fn last_paren_stack_frame_mut(&mut self) -> Option<&mut ParenStackFrame> {
        let idx = self.last_paren_frame_idx?;
        match self.frames.get_mut(idx) {
            Some(FormatFrame::Paren(frame)) => Some(frame),
            _ => None,
        }
    }

    fn last_block_frame(&self) -> Option<&BlockStackFrame> {
        let idx = self.last_block_frame_idx?;
        match self.frames.get(idx) {
            Some(FormatFrame::Block(frame)) => Some(frame),
            _ => None,
        }
    }

    fn last_block_frame_mut(&mut self) -> Option<&mut BlockStackFrame> {
        let idx = self.last_block_frame_idx?;
        match self.frames.get_mut(idx) {
            Some(FormatFrame::Block(frame)) => Some(frame),
            _ => None,
        }
    }

    #[cfg(test)]
    fn record_transition(
        &mut self,
        kind: FormatFrameTransitionKind,
        scope_before: FormatScope,
        indent_before: usize,
        indent_after: usize,
    ) {
        let next_event = FormatFrameTransitionEvent {
            kind,
            scope_before,
            scope_after: self.current_scope(),
            indent_before,
            indent_after,
        };

        if matches!(next_event.kind, FormatFrameTransitionKind::TokenEntrySync)
            && self.transition_trace.last().is_some_and(|event| {
                matches!(event.kind, FormatFrameTransitionKind::TokenEntrySync)
            })
        {
            if let Some(last_event) = self.transition_trace.last_mut() {
                *last_event = next_event;
            }
            return;
        }

        self.transition_trace.push(next_event);
    }

    #[cfg(test)]
    fn transition_trace(&self) -> &[FormatFrameTransitionEvent] {
        self.transition_trace.as_slice()
    }

    fn token_entry_cleanup_scope(current_scope: FormatScope) -> FormatScope {
        FormatScope::with_frame_ids(
            current_scope.paren_depth.saturating_add(1),
            current_scope.block_depth,
            None,
            current_scope.block_frame_id,
        )
    }

    fn next_frame_id(&mut self) -> SqlFormatFrameId {
        self.frame_id_allocator.next_id()
    }

    fn push_frame(&mut self, frame: FormatFrame) {
        let next_idx = self.frames.len();
        match &frame {
            FormatFrame::Paren(frame) => {
                self.paren_depth = self.paren_depth.saturating_add(1);
                self.active_paren_metrics = {
                    let mut metrics = frame.previous_active_paren_metrics;
                    if frame.frame.is_query_like() {
                        metrics.query_like_count = metrics.query_like_count.saturating_add(1);
                        metrics.compact_non_query_since_last_query = 0;
                        metrics.paren_extra_since_last_query = 0;
                        metrics.last_compact_open_line_indent = None;
                    } else {
                        if frame.frame.suppresses_comma_breaks() {
                            metrics.compact_non_query_since_last_query =
                                metrics.compact_non_query_since_last_query.saturating_add(1);
                            metrics.last_compact_open_line_indent =
                                Some(frame.frame.open_line_indent);
                        }
                        if frame.frame.counts_for_paren_extra() {
                            metrics.paren_extra_since_last_query =
                                metrics.paren_extra_since_last_query.saturating_add(1);
                        }
                    }
                    if frame.frame.counts_for_paren_extra() {
                        metrics.paren_extra_count = metrics.paren_extra_count.saturating_add(1);
                    }
                    if frame.semantic_flags.match_recognize {
                        metrics.match_recognize_count =
                            metrics.match_recognize_count.saturating_add(1);
                    }
                    if frame.semantic_flags.analytic_over {
                        metrics.analytic_over_count = metrics.analytic_over_count.saturating_add(1);
                    }
                    if frame.semantic_flags.window_clause {
                        metrics.window_clause_count = metrics.window_clause_count.saturating_add(1);
                    }
                    if frame.semantic_flags.keep {
                        metrics.keep_count = metrics.keep_count.saturating_add(1);
                    }
                    metrics
                };
                self.last_paren_frame_id_cache = Some(frame.id);
                self.last_paren_frame_idx = Some(next_idx);
                if frame.frame.opens_indented() {
                    self.base_indent_level = self
                        .base_indent_level
                        .saturating_add(frame.frame.indent_level_delta());
                }
            }
            FormatFrame::Block(frame) => {
                self.block_depth = self.block_depth.saturating_add(1);
                self.last_block_frame_id_cache = Some(frame.id);
                self.last_block_frame_idx = Some(next_idx);
                self.block_kind_frame_indices[frame.kind.index()].push(next_idx);
                self.base_indent_level = frame.depth_frame.body_indent_level;
            }
            FormatFrame::QueryRuntime(_) => {
                self.query_runtime_frame_indices.push(next_idx);
                if let FormatFrame::QueryRuntime(frame) = &frame {
                    self.current_runtime_state = frame.state.clone();
                }
            }
            FormatFrame::ScopedIndent(frame) => {
                self.scoped_indent_frame_indices[frame.kind.index()].push(next_idx);
                self.base_indent_level = self.base_indent_level.saturating_add(frame.delta);
            }
            FormatFrame::ConstructFlag(frame) => {
                if frame.active {
                    self.construct_flag_scopes[frame.kind.index()].push(frame.scope);
                    self.construct_flag_frame_indices[frame.kind.index()].push(next_idx);
                }
            }
            FormatFrame::ConstructValue(frame) => {
                self.construct_value_frame_indices[frame.kind.index()].push(next_idx);
            }
            FormatFrame::ConditionOwner(frame) => {
                self.condition_owner_frame_indices[frame.kind.index()].push(next_idx);
            }
            FormatFrame::PendingSplitEndSuffix(_) => {
                self.pending_split_end_suffix_frame_idx = Some(next_idx);
            }
            FormatFrame::PendingPlsqlLabel(_) => {
                self.pending_plsql_label_frame_idx = Some(next_idx);
            }
            FormatFrame::MySqlHandlerPendingBodyIndent(_) => {
                self.mysql_handler_pending_body_indent_frame_idx = Some(next_idx);
            }
            FormatFrame::TriggerHeader(_) => {
                self.trigger_header_frame_indices.push(next_idx);
            }
            FormatFrame::PlsqlContext(_) => {
                self.plsql_context_frame_indices.push(next_idx);
            }
            FormatFrame::CompoundTrigger(_) => {
                self.compound_trigger_frame_indices.push(next_idx);
            }
            FormatFrame::WithCte(_) => {
                self.with_cte_frame_indices.push(next_idx);
            }
            FormatFrame::BetweenPending(_) | FormatFrame::OpenCursorRestore(_) => {}
        }
        self.frames.push(frame);
    }

    fn push_existing_frame(&mut self, frame: FormatFrame) {
        self.push_frame(frame);
    }

    fn pop_tail_frame(&mut self) -> Option<FormatFrame> {
        loop {
            let frame = self.frames.pop()?;
            let popped_idx = self.frames.len();
            match &frame {
                FormatFrame::Paren(frame) => {
                    self.paren_depth = self.paren_depth.saturating_sub(1);
                    self.active_paren_metrics = frame.previous_active_paren_metrics;
                    self.last_paren_frame_id_cache = frame.previous_paren_frame_id;
                    self.last_paren_frame_idx = frame.previous_paren_frame_idx;
                    if frame.frame.closes_indented() {
                        self.base_indent_level = self
                            .base_indent_level
                            .saturating_sub(frame.frame.indent_level_delta());
                    }
                }
                FormatFrame::Block(frame) => {
                    self.block_depth = self.block_depth.saturating_sub(1);
                    self.last_block_frame_id_cache = frame.previous_block_frame_id;
                    self.last_block_frame_idx = frame.previous_block_frame_idx;
                    let popped_block_kind_idx =
                        self.block_kind_frame_indices[frame.kind.index()].pop();
                    debug_assert_eq!(popped_block_kind_idx, Some(popped_idx));
                    self.base_indent_level = frame.previous_base_indent_level;
                }
                FormatFrame::QueryRuntime(_) => {
                    let popped_query_runtime_idx = self.query_runtime_frame_indices.pop();
                    debug_assert_eq!(popped_query_runtime_idx, Some(popped_idx));
                    self.current_runtime_state = self
                        .query_runtime_frame_indices
                        .last()
                        .and_then(|idx| match self.frames.get(*idx) {
                            Some(FormatFrame::QueryRuntime(frame)) => Some(frame.state.clone()),
                            _ => None,
                        })
                        .unwrap_or_default();
                }
                FormatFrame::ScopedIndent(frame) => {
                    let popped_scoped_indent_idx =
                        self.scoped_indent_frame_indices[frame.kind.index()].pop();
                    debug_assert_eq!(popped_scoped_indent_idx, Some(popped_idx));
                    self.base_indent_level = self.base_indent_level.saturating_sub(frame.delta);
                }
                FormatFrame::ConstructFlag(frame) => {
                    if frame.active {
                        let scopes = &mut self.construct_flag_scopes[frame.kind.index()];
                        let popped_scope = scopes.pop();
                        debug_assert_eq!(popped_scope, Some(frame.scope));
                        let frame_indices =
                            &mut self.construct_flag_frame_indices[frame.kind.index()];
                        let popped_frame_idx = frame_indices.pop();
                        debug_assert_eq!(popped_frame_idx, Some(popped_idx));
                    }
                }
                FormatFrame::ConstructValue(frame) => {
                    let popped_construct_value_idx =
                        self.construct_value_frame_indices[frame.kind.index()].pop();
                    debug_assert_eq!(popped_construct_value_idx, Some(popped_idx));
                }
                FormatFrame::ConditionOwner(frame) => {
                    let popped_condition_owner_idx =
                        self.condition_owner_frame_indices[frame.kind.index()].pop();
                    debug_assert_eq!(popped_condition_owner_idx, Some(popped_idx));
                }
                FormatFrame::PendingSplitEndSuffix(_) => {
                    debug_assert_eq!(self.pending_split_end_suffix_frame_idx, Some(popped_idx));
                    self.pending_split_end_suffix_frame_idx = None;
                }
                FormatFrame::PendingPlsqlLabel(_) => {
                    debug_assert_eq!(self.pending_plsql_label_frame_idx, Some(popped_idx));
                    self.pending_plsql_label_frame_idx = None;
                }
                FormatFrame::MySqlHandlerPendingBodyIndent(_) => {
                    debug_assert_eq!(
                        self.mysql_handler_pending_body_indent_frame_idx,
                        Some(popped_idx)
                    );
                    self.mysql_handler_pending_body_indent_frame_idx = None;
                }
                FormatFrame::TriggerHeader(_) => {
                    let popped_trigger_header_idx = self.trigger_header_frame_indices.pop();
                    debug_assert_eq!(popped_trigger_header_idx, Some(popped_idx));
                }
                FormatFrame::PlsqlContext(_) => {
                    let popped_plsql_context_idx = self.plsql_context_frame_indices.pop();
                    debug_assert_eq!(popped_plsql_context_idx, Some(popped_idx));
                }
                FormatFrame::CompoundTrigger(_) => {
                    let popped_compound_trigger_idx = self.compound_trigger_frame_indices.pop();
                    debug_assert_eq!(popped_compound_trigger_idx, Some(popped_idx));
                }
                FormatFrame::WithCte(_) => {
                    let popped_with_cte_idx = self.with_cte_frame_indices.pop();
                    debug_assert_eq!(popped_with_cte_idx, Some(popped_idx));
                }
                FormatFrame::BetweenPending(_) | FormatFrame::OpenCursorRestore(_) => {}
            }
            if !matches!(&frame, FormatFrame::ConstructFlag(frame) if !frame.active) {
                return Some(frame);
            }
        }
    }

    fn pop_tail_frame_adjust_indent(&mut self, indent_level: &mut usize) -> Option<FormatFrame> {
        let frame = self.pop_tail_frame()?;
        match &frame {
            FormatFrame::Paren(frame) => {
                if frame.frame.closes_indented() {
                    *indent_level = indent_level.saturating_sub(frame.frame.indent_level_delta());
                }
            }
            FormatFrame::Block(_) => {
                *indent_level = self.base_indent_level;
            }
            FormatFrame::ScopedIndent(frame) => {
                *indent_level = indent_level.saturating_sub(frame.delta);
            }
            FormatFrame::QueryRuntime(_)
            | FormatFrame::ConstructFlag(_)
            | FormatFrame::ConstructValue(_)
            | FormatFrame::ConditionOwner(_)
            | FormatFrame::BetweenPending(_)
            | FormatFrame::PendingSplitEndSuffix(_)
            | FormatFrame::PendingPlsqlLabel(_)
            | FormatFrame::MySqlHandlerPendingBodyIndent(_)
            | FormatFrame::TriggerHeader(_)
            | FormatFrame::PlsqlContext(_)
            | FormatFrame::CompoundTrigger(_)
            | FormatFrame::WithCte(_)
            | FormatFrame::OpenCursorRestore(_) => {}
        }
        Some(frame)
    }

    fn restore_tail_frame_with_indent(&mut self, mut frame: FormatFrame, indent_level: &mut usize) {
        match &mut frame {
            FormatFrame::Paren(frame) => {
                if frame.frame.opens_indented() {
                    *indent_level = indent_level.saturating_add(frame.frame.indent_level_delta());
                }
            }
            FormatFrame::Block(frame) => {
                frame.previous_base_indent_level = self.base_indent_level;
                *indent_level = frame.depth_frame.body_indent_level;
            }
            FormatFrame::ScopedIndent(frame) => {
                *indent_level = indent_level.saturating_add(frame.delta);
            }
            FormatFrame::QueryRuntime(_)
            | FormatFrame::ConstructFlag(_)
            | FormatFrame::ConstructValue(_)
            | FormatFrame::ConditionOwner(_)
            | FormatFrame::BetweenPending(_)
            | FormatFrame::PendingSplitEndSuffix(_)
            | FormatFrame::PendingPlsqlLabel(_)
            | FormatFrame::MySqlHandlerPendingBodyIndent(_)
            | FormatFrame::TriggerHeader(_)
            | FormatFrame::PlsqlContext(_)
            | FormatFrame::CompoundTrigger(_)
            | FormatFrame::WithCte(_)
            | FormatFrame::OpenCursorRestore(_) => {}
        }
        self.push_existing_frame(frame);
    }

    fn remove_last_frame_matching_via_tail_pop(
        &mut self,
        indent_level: &mut usize,
        mut should_remove: impl FnMut(&FormatFrame) -> bool,
    ) -> bool {
        self.take_last_frame_matching_via_tail_pop(indent_level, |frame| should_remove(frame))
            .is_some()
    }

    fn take_last_frame_matching_via_tail_pop(
        &mut self,
        indent_level: &mut usize,
        mut should_take: impl FnMut(&FormatFrame) -> bool,
    ) -> Option<FormatFrame> {
        let mut frames_to_restore = Vec::new();

        while let Some(frame) = self.pop_tail_frame_adjust_indent(indent_level) {
            if should_take(&frame) {
                for frame in frames_to_restore.into_iter().rev() {
                    self.restore_tail_frame_with_indent(frame, indent_level);
                }
                self.realign_indent_level_to_base(indent_level);
                #[cfg(debug_assertions)]
                {
                    self.debug_assert_integrity();
                    self.debug_assert_indent_level(*indent_level);
                }
                return Some(frame);
            }
            frames_to_restore.push(frame);
        }

        for frame in frames_to_restore.into_iter().rev() {
            self.restore_tail_frame_with_indent(frame, indent_level);
        }
        self.realign_indent_level_to_base(indent_level);
        #[cfg(debug_assertions)]
        {
            self.debug_assert_integrity();
            self.debug_assert_indent_level(*indent_level);
        }
        None
    }

    fn pop_tail_auxiliary_frames_for_scope(
        &mut self,
        next_scope: FormatScope,
        indent_level: &mut usize,
    ) {
        while self
            .frames
            .last()
            .is_some_and(|frame| frame.expires_for_scope(next_scope))
        {
            if let Some(frame) = self.pop_tail_frame() {
                if let FormatFrame::ScopedIndent(frame) = frame {
                    *indent_level = indent_level.saturating_sub(frame.delta);
                }
            } else {
                break;
            }
        }
    }

    fn current_scope(&self) -> FormatScope {
        FormatScope::with_frame_ids(
            self.paren_depth,
            self.block_depth,
            self.last_paren_frame_id(),
            self.last_block_frame_id(),
        )
    }

    fn sync_for_token_entry(&mut self, indent_level: &mut usize) -> FormatScope {
        let current_scope = self.current_scope();
        #[cfg(test)]
        let indent_before = *indent_level;
        self.pop_between_pending_at_or_above(Self::token_entry_cleanup_scope(current_scope));
        self.sync_to_scope(current_scope, indent_level);
        #[cfg(test)]
        self.record_transition(
            FormatFrameTransitionKind::TokenEntrySync,
            current_scope,
            indent_before,
            *indent_level,
        );
        current_scope
    }

    fn paren_depth(&self) -> usize {
        self.paren_depth
    }

    fn block_depth(&self) -> usize {
        self.block_depth
    }

    fn all_paren_frames_are_query_like(&self) -> bool {
        self.paren_depth == self.active_paren_metrics().query_like_count
    }

    fn has_query_like_paren(&self) -> bool {
        self.active_paren_metrics().query_like_count > 0
    }

    fn paren_extra_depth(&self) -> usize {
        self.active_paren_metrics().paren_extra_count
    }

    fn paren_extra_depth_within_current_query_frame(&self) -> usize {
        self.active_paren_metrics().paren_extra_since_last_query
    }

    fn has_compact_non_query_frame_since_last_query(&self) -> bool {
        self.active_paren_metrics()
            .compact_non_query_since_last_query
            > 0
    }

    fn last_compact_paren_open_line_indent(&self) -> Option<usize> {
        self.active_paren_metrics().last_compact_open_line_indent
    }

    fn paren_is_empty(&self) -> bool {
        self.paren_depth == 0
    }

    fn current_query_runtime_frame(&self) -> Option<&QueryRuntimeFrame> {
        let idx = self.current_query_runtime_frame_idx()?;
        match self.frames.get(idx) {
            Some(FormatFrame::QueryRuntime(frame)) => Some(frame),
            _ => None,
        }
    }

    fn current_query_runtime_scope(&self) -> Option<FormatScope> {
        self.current_query_runtime_frame().map(|frame| frame.scope)
    }

    fn runtime_frame_mut_for_update(
        &mut self,
        should_create: bool,
    ) -> Option<&mut QueryRuntimeFrame> {
        if let Some(idx) = self.current_query_runtime_frame_idx() {
            return match self.frames.get_mut(idx) {
                Some(FormatFrame::QueryRuntime(frame)) => Some(frame),
                _ => None,
            };
        }
        if !should_create {
            return None;
        }
        let current_scope = self.current_scope();
        self.push_frame(FormatFrame::QueryRuntime(QueryRuntimeFrame {
            scope: current_scope,
            state: FormatRuntimeState::default(),
        }));
        let idx = *self.query_runtime_frame_indices.last()?;
        match self.frames.get_mut(idx) {
            Some(FormatFrame::QueryRuntime(frame)) => Some(frame),
            _ => None,
        }
    }

    fn push_query_runtime_frame(&mut self, scope: FormatScope, state: FormatRuntimeState) {
        self.push_frame(FormatFrame::QueryRuntime(QueryRuntimeFrame {
            scope,
            state,
        }));
    }

    fn current_query_base_depth(&self) -> Option<usize> {
        self.current_runtime_state.query_base_depth
    }

    fn set_current_query_base_depth(&mut self, depth: Option<usize>) {
        if let Some(frame) = self.runtime_frame_mut_for_update(depth.is_some()) {
            frame.state.query_base_depth = depth;
        }
        self.current_runtime_state.query_base_depth = depth;
    }

    fn current_clause(&self) -> Option<&str> {
        self.current_runtime_state.current_clause.as_deref()
    }

    fn set_current_clause(&mut self, current_clause: Option<String>) {
        if let Some(frame) = self.runtime_frame_mut_for_update(current_clause.is_some()) {
            frame.state.current_clause = current_clause.clone();
        }
        self.current_runtime_state.current_clause = current_clause;
    }

    fn set_current_clause_ref(&mut self, current_clause: Option<&str>) {
        if self.current_runtime_state.current_clause.as_deref() == current_clause {
            return;
        }
        self.set_current_clause(current_clause.map(str::to_string));
    }

    fn select_list_layout_state(&self) -> SelectListLayoutState {
        self.current_runtime_state.select_list_layout_state
    }

    fn set_select_list_layout_state(&mut self, select_list_layout_state: SelectListLayoutState) {
        if let Some(frame) = self.runtime_frame_mut_for_update(
            select_list_layout_state != SelectListLayoutState::Inactive,
        ) {
            frame.state.select_list_layout_state = select_list_layout_state;
        }
        self.current_runtime_state.select_list_layout_state = select_list_layout_state;
    }

    fn statement_has_with_clause(&self) -> bool {
        self.current_runtime_state.statement_has_with_clause || self.with_cte_in_definitions()
    }

    fn set_statement_has_with_clause(&mut self, statement_has_with_clause: bool) {
        if let Some(frame) = self.runtime_frame_mut_for_update(statement_has_with_clause) {
            frame.state.statement_has_with_clause = statement_has_with_clause;
        }
        self.current_runtime_state.statement_has_with_clause = statement_has_with_clause;
    }

    fn query_body_clause_base_depth(&self) -> Option<usize> {
        self.current_runtime_state.query_body_clause_base_depth
    }

    fn set_query_body_clause_base_depth(&mut self, query_body_clause_base_depth: Option<usize>) {
        if let Some(frame) =
            self.runtime_frame_mut_for_update(query_body_clause_base_depth.is_some())
        {
            frame.state.query_body_clause_base_depth = query_body_clause_base_depth;
        }
        self.current_runtime_state.query_body_clause_base_depth = query_body_clause_base_depth;
    }

    fn open_cursor_state(&self) -> OpenCursorFormatState {
        self.current_runtime_state.open_cursor_state
    }

    fn set_open_cursor_state(&mut self, open_cursor_state: OpenCursorFormatState) {
        if let Some(frame) =
            self.runtime_frame_mut_for_update(open_cursor_state != OpenCursorFormatState::None)
        {
            frame.state.open_cursor_state = open_cursor_state;
        }
        self.current_runtime_state.open_cursor_state = open_cursor_state;
    }

    fn activate_construct_flag(&mut self, kind: ConstructFlagKind, scope: FormatScope) {
        if self.last_construct_flag_scope(kind) == Some(scope) {
            return;
        }
        self.push_frame(FormatFrame::ConstructFlag(ConstructFlagFrame {
            kind,
            scope,
            active: true,
        }));
    }

    fn deactivate_construct_flag(&mut self, kind: ConstructFlagKind) -> bool {
        self.take_active_construct_flag(kind).is_some()
    }

    fn deactivate_construct_flag_at_or_above_paren_depth(
        &mut self,
        kind: ConstructFlagKind,
        paren_depth: usize,
    ) {
        while self
            .last_construct_flag_scope(kind)
            .is_some_and(|scope| scope.paren_depth >= paren_depth)
        {
            let _ = self.take_active_construct_flag(kind);
        }
    }

    fn construct_flag_is_active(&self, kind: ConstructFlagKind) -> bool {
        self.last_construct_flag_scope(kind).is_some()
    }

    fn construct_flag_is_active_at_paren_depth(
        &self,
        kind: ConstructFlagKind,
        paren_depth: usize,
    ) -> bool {
        self.last_construct_flag_scope(kind)
            .is_some_and(|scope| scope.paren_depth == paren_depth)
    }

    fn last_construct_flag_scope(&self, kind: ConstructFlagKind) -> Option<FormatScope> {
        self.construct_flag_scopes[kind.index()].last().copied()
    }

    fn take_active_construct_flag(&mut self, kind: ConstructFlagKind) -> Option<FormatScope> {
        let kind_idx = kind.index();
        let frame_idx = self.construct_flag_frame_indices[kind_idx].pop()?;
        let scope = self.construct_flag_scopes[kind_idx].pop()?;
        match self.frames.get_mut(frame_idx) {
            Some(FormatFrame::ConstructFlag(frame))
                if frame.active && frame.kind == kind && frame.scope == scope =>
            {
                frame.active = false;
                self.discard_inactive_construct_flags_at_tail();
                Some(scope)
            }
            _ => None,
        }
    }

    fn discard_inactive_construct_flags_at_tail(&mut self) {
        while self.frames.last().is_some_and(
            |frame| matches!(frame, FormatFrame::ConstructFlag(frame) if !frame.active),
        ) {
            let _ = self.frames.pop();
        }
    }

    fn replace_construct_value(
        &mut self,
        kind: ConstructValueKind,
        scope: FormatScope,
        value: String,
    ) {
        let existing_idx = self.construct_value_frame_indices[kind.index()]
            .last()
            .copied();
        if let Some(idx) = existing_idx {
            match self.frames.get_mut(idx) {
                Some(FormatFrame::ConstructValue(frame)) if frame.scope == scope => {
                    frame.value = value;
                    return;
                }
                _ => {}
            }
        }
        self.push_frame(FormatFrame::ConstructValue(ConstructValueFrame {
            kind,
            scope,
            value,
        }));
    }

    fn clear_construct_value(&mut self, kind: ConstructValueKind) {
        let mut indent_level = self.statement_base_indent();
        while self.remove_last_frame_matching_via_tail_pop(
            &mut indent_level,
            |frame| matches!(frame, FormatFrame::ConstructValue(frame) if frame.kind == kind),
        ) {}
    }

    fn construct_value_is_some(&self, kind: ConstructValueKind) -> bool {
        self.construct_value_as_deref(kind).is_some()
    }

    fn construct_value_as_deref(&self, kind: ConstructValueKind) -> Option<&str> {
        let idx = *self.construct_value_frame_indices[kind.index()].last()?;
        match self.frames.get(idx) {
            Some(FormatFrame::ConstructValue(frame)) => Some(frame.value.as_str()),
            _ => None,
        }
    }

    /// Push a paren frame and atomically apply its indent delta to
    /// `indent_level` when the frame opens indented. This keeps the scalar
    /// `indent_level` symmetric with `pop_paren`'s restore.
    fn push_paren(
        &mut self,
        frame: ParenFormatFrame,
        semantic_flags: ParenSemanticFlags,
        capture_runtime_state: bool,
        new_query_base_depth: Option<usize>,
        wrapped_owner_kind: Option<FormatIndentedParenOwnerKind>,
        open_cursor_restore_state: Option<OpenCursorFormatState>,
        indent_level: &mut usize,
    ) {
        #[cfg(test)]
        let scope_before = self.current_scope();
        #[cfg(test)]
        let indent_before = *indent_level;
        let frame_id = self.next_frame_id();
        let should_push_runtime_state = capture_runtime_state || new_query_base_depth.is_some();
        if frame.opens_indented() {
            *indent_level = indent_level.saturating_add(frame.indent_level_delta());
        }
        self.push_frame(FormatFrame::Paren(Box::new(ParenStackFrame {
            id: frame_id,
            frame,
            semantic_flags,
            previous_active_paren_metrics: self.active_paren_metrics,
            previous_paren_frame_idx: self.last_paren_frame_idx,
            previous_paren_frame_id: self.last_paren_frame_id(),
            wrapped_owner_kind,
            open_cursor_restore_state,
        })));
        if should_push_runtime_state {
            let mut next_runtime_state = self
                .current_query_runtime_frame()
                .map(|frame| frame.state.clone())
                .unwrap_or_default();
            if let Some(depth) = new_query_base_depth {
                next_runtime_state.query_base_depth = Some(depth);
            }
            self.push_query_runtime_frame(self.current_scope(), next_runtime_state);
        }
        #[cfg(debug_assertions)]
        {
            self.debug_assert_integrity();
            self.debug_assert_indent_level(*indent_level);
        }
        #[cfg(test)]
        self.record_transition(
            FormatFrameTransitionKind::PushParen { kind: frame.kind },
            scope_before,
            indent_before,
            *indent_level,
        );
    }

    /// Pop a paren frame and atomically undo the indent delta applied at push
    /// time when the frame closes indented.
    fn pop_paren(&mut self, indent_level: &mut usize) -> Option<PoppedParenFrame> {
        let current_scope = self.current_scope();
        #[cfg(test)]
        let scope_before = current_scope;
        #[cfg(test)]
        let indent_before = *indent_level;
        let restored_runtime_state = self.current_query_runtime_scope() == Some(current_scope);
        let next_scope = self
            .last_paren_stack_frame()
            .map(|frame| {
                FormatScope::with_frame_ids(
                    self.paren_depth.saturating_sub(1),
                    self.block_depth,
                    frame.previous_paren_frame_id,
                    self.last_block_frame_id(),
                )
            })
            .unwrap_or_else(|| {
                FormatScope::new(self.paren_depth.saturating_sub(1), self.block_depth)
            });
        self.pop_tail_auxiliary_frames_for_scope(next_scope, indent_level);
        let Some(FormatFrame::Paren(frame)) = self.pop_tail_frame() else {
            return None;
        };
        let frame = *frame;
        if frame.frame.closes_indented() {
            *indent_level = indent_level.saturating_sub(frame.frame.indent_level_delta());
        }
        let popped = Some(PoppedParenFrame {
            frame: frame.frame,
            restored_runtime_state,
            open_cursor_restore_state: frame.open_cursor_restore_state,
        });
        #[cfg(debug_assertions)]
        {
            self.debug_assert_integrity();
            self.debug_assert_indent_level(*indent_level);
        }
        #[cfg(test)]
        self.record_transition(
            FormatFrameTransitionKind::PopParen {
                kind: frame.frame.kind,
                restored_runtime_state,
            },
            scope_before,
            indent_before,
            *indent_level,
        );
        popped
    }

    fn last_paren(&self) -> Option<&ParenFormatFrame> {
        self.last_paren_stack_frame().map(|frame| &frame.frame)
    }

    fn last_paren_frame_id(&self) -> Option<SqlFormatFrameId> {
        self.last_paren_frame_id_cache
    }

    fn last_paren_mut(&mut self) -> Option<&mut ParenFormatFrame> {
        self.last_paren_stack_frame_mut()
            .map(|frame| &mut frame.frame)
    }

    fn last_paren_wrapped_owner_kind(&self) -> Option<FormatIndentedParenOwnerKind> {
        self.last_paren_stack_frame()
            .and_then(|frame| frame.wrapped_owner_kind)
    }

    fn in_match_recognize_paren(&self) -> bool {
        self.active_paren_metrics().match_recognize_count > 0
    }

    fn in_analytic_over_paren(&self) -> bool {
        self.active_paren_metrics().analytic_over_count > 0
    }

    fn in_window_paren(&self) -> bool {
        self.active_paren_metrics().window_clause_count > 0
    }

    fn in_keep_paren(&self) -> bool {
        self.active_paren_metrics().keep_count > 0
    }

    /// Push a block frame and atomically update `indent_level`.
    ///
    /// `new_indent_level` is the indent level that should be active while the
    /// block is open. `pop_block` reconstructs the surviving indent from the
    /// remaining frame stack instead of replaying a captured pre-push value.
    fn push_block(
        &mut self,
        kind: BlockKind,
        owner_depth: usize,
        new_indent_level: usize,
        indent_level: &mut usize,
    ) {
        #[cfg(test)]
        let scope_before = self.current_scope();
        #[cfg(test)]
        let indent_before = *indent_level;
        let frame_id = self.next_frame_id();
        *indent_level = new_indent_level;
        self.push_frame(FormatFrame::Block(BlockStackFrame {
            id: frame_id,
            kind,
            depth_frame: FormatBlockDepthFrame {
                owner_depth,
                body_indent_level: new_indent_level,
            },
            previous_base_indent_level: self.base_indent_level,
            previous_block_frame_idx: self.last_block_frame_idx,
            previous_block_frame_id: self.last_block_frame_id(),
            case_branch_started: false,
            exception_branch_started: false,
            mysql_handler_begin: false,
        }));
        #[cfg(debug_assertions)]
        {
            self.debug_assert_integrity();
            self.debug_assert_indent_level(*indent_level);
        }
        #[cfg(test)]
        self.record_transition(
            FormatFrameTransitionKind::PushBlock { kind },
            scope_before,
            indent_before,
            *indent_level,
        );
    }

    /// Pop a block frame and restore `indent_level` from the surviving frame
    /// stack. This avoids replaying stale pre-pop indent snapshots after scope
    /// sync has already expired auxiliary indent frames that were active when
    /// the block opened. Returns `None` (and leaves `indent_level` untouched)
    /// if the tail frame is not a block.
    fn pop_block(&mut self, indent_level: &mut usize) -> Option<PoppedBlockFrame> {
        #[cfg(test)]
        let scope_before = self.current_scope();
        #[cfg(test)]
        let indent_before = *indent_level;
        let next_scope = self
            .last_block_frame()
            .map(|frame| {
                FormatScope::with_frame_ids(
                    self.paren_depth,
                    self.block_depth.saturating_sub(1),
                    self.last_paren_frame_id(),
                    frame.previous_block_frame_id,
                )
            })
            .unwrap_or_else(|| {
                FormatScope::new(self.paren_depth, self.block_depth.saturating_sub(1))
            });
        self.pop_tail_auxiliary_frames_for_scope(next_scope, indent_level);
        let Some(FormatFrame::Block(frame)) = self.pop_tail_frame() else {
            return None;
        };
        *indent_level = self.expected_indent_level();
        let popped = Some(PoppedBlockFrame {
            kind: frame.kind,
            depth_frame: frame.depth_frame,
            mysql_handler_begin: frame.mysql_handler_begin,
        });
        #[cfg(debug_assertions)]
        {
            self.debug_assert_integrity();
            self.debug_assert_indent_level(*indent_level);
        }
        #[cfg(test)]
        self.record_transition(
            FormatFrameTransitionKind::PopBlock { kind: frame.kind },
            scope_before,
            indent_before,
            *indent_level,
        );
        popped
    }

    fn last_block_kind(&self) -> Option<BlockKind> {
        self.last_block_frame().map(|frame| frame.kind)
    }

    fn last_block_frame_id(&self) -> Option<SqlFormatFrameId> {
        self.last_block_frame_id_cache
    }

    fn last_block_kind_is(&self, kind: impl BlockKindMatcher) -> bool {
        self.last_block_kind()
            .is_some_and(|current| kind.matches(current))
    }

    fn set_last_block_kind(&mut self, kind: BlockKind) -> bool {
        let Some(idx) = self.last_block_frame_idx else {
            return false;
        };
        let current_kind = match self.frames.get(idx) {
            Some(FormatFrame::Block(frame)) => frame.kind,
            _ => return false,
        };
        if current_kind == kind {
            return true;
        }
        let popped_block_kind_idx = self.block_kind_frame_indices[current_kind.index()].pop();
        debug_assert_eq!(popped_block_kind_idx, Some(idx));
        self.block_kind_frame_indices[kind.index()].push(idx);
        match self.frames.get_mut(idx) {
            Some(FormatFrame::Block(frame)) => {
                frame.kind = kind;
                true
            }
            _ => false,
        }
    }

    fn mark_last_block_mysql_handler_begin(&mut self) -> bool {
        match self.last_block_frame_mut() {
            Some(frame) => {
                frame.mysql_handler_begin = true;
                true
            }
            None => false,
        }
    }

    fn set_last_case_branch_started(&mut self, started: bool) -> bool {
        match self.last_block_of_kind_mut(BlockKind::Case) {
            Some(frame) => {
                frame.case_branch_started = started;
                true
            }
            None => false,
        }
    }

    fn last_case_branch_started(&self) -> bool {
        self.last_block_of_kind(BlockKind::Case)
            .map(|frame| frame.case_branch_started)
            .unwrap_or(false)
    }

    fn set_last_exception_branch_started(&mut self, started: bool) -> bool {
        match self.last_block_of_kind_mut(BlockKind::Exception) {
            Some(frame) => {
                frame.exception_branch_started = started;
                true
            }
            None => false,
        }
    }

    fn last_exception_branch_started(&self) -> bool {
        self.last_block_of_kind(BlockKind::Exception)
            .map(|frame| frame.exception_branch_started)
            .unwrap_or(false)
    }

    fn last_block_depth_frame(&self) -> Option<FormatBlockDepthFrame> {
        self.last_block_frame().map(|frame| frame.depth_frame)
    }

    fn last_block_of_kind(&self, kind: BlockKind) -> Option<&BlockStackFrame> {
        let idx = *self.block_kind_frame_indices[kind.index()].last()?;
        match self.frames.get(idx) {
            Some(FormatFrame::Block(frame)) => Some(frame),
            _ => None,
        }
    }

    fn last_block_of_kind_mut(&mut self, kind: BlockKind) -> Option<&mut BlockStackFrame> {
        let idx = *self.block_kind_frame_indices[kind.index()].last()?;
        match self.frames.get_mut(idx) {
            Some(FormatFrame::Block(frame)) => Some(frame),
            _ => None,
        }
    }

    fn last_block_owner_depth(&self) -> Option<usize> {
        self.last_block_depth_frame().map(|frame| frame.owner_depth)
    }

    fn package_body_member_indent(&self) -> Option<usize> {
        if !self.last_block_kind_is(BlockKind::PackageBody) {
            return None;
        }
        self.last_block_owner_depth()
            .map(|depth| depth.saturating_add(1))
    }

    fn expected_indent_level(&self) -> usize {
        self.base_indent_level
    }

    fn statement_base_indent(&self) -> usize {
        self.expected_indent_level()
    }

    fn active_paren_metrics(&self) -> ActiveParenMetrics {
        self.active_paren_metrics
    }

    /// Realign `indent_level` to the current `statement_base_indent()`.
    ///
    /// This is the only sanctioned way for call sites outside this impl to
    /// reset `indent_level` to the surviving frame stack's derived base
    /// without pushing or popping a frame. Under the stack's invariant
    /// (`indent_level == expected_indent_level()`) this is a no-op in the
    /// happy path; in release builds it also acts as a defensive realignment
    /// if a caller has drifted, and in debug builds the post-condition
    /// assertion makes any residual drift loud.
    fn realign_indent_level_to_base(&self, indent_level: &mut usize) {
        *indent_level = self.statement_base_indent();
        #[cfg(debug_assertions)]
        self.debug_assert_indent_level(*indent_level);
    }

    fn contains_block_kind(&self, kind: impl BlockKindMatcher) -> bool {
        if let Some(kind) = kind.as_block_kind() {
            return !self.block_kind_frame_indices[kind.index()].is_empty();
        }
        let mut current = self.last_block_frame();
        while let Some(frame) = current {
            if kind.matches(frame.kind) {
                return true;
            }
            current = frame
                .previous_block_frame_idx
                .and_then(|idx| match self.frames.get(idx) {
                    Some(FormatFrame::Block(frame)) => Some(frame),
                    _ => None,
                });
        }
        false
    }

    fn push_condition_owner(
        &mut self,
        kind: ConditionOwnerKind,
        scope: FormatScope,
        indent: usize,
        parenthesized: bool,
    ) {
        self.push_frame(FormatFrame::ConditionOwner(ConditionOwnerFrame {
            kind,
            scope,
            indent,
            parenthesized,
        }));
    }

    fn pop_condition_owners_for_push(&mut self, kind: ConditionOwnerKind, scope: FormatScope) {
        while self.frames.last().is_some_and(|frame| match frame {
            FormatFrame::ConditionOwner(owner) if owner.kind == kind => match kind {
                ConditionOwnerKind::JoinOn | ConditionOwnerKind::Where => {
                    !owner.scope.contains(scope) || owner.scope == scope
                }
                ConditionOwnerKind::Case => !owner.scope.contains(scope) || owner.scope == scope,
                ConditionOwnerKind::ControlHeader => false,
            },
            _ => false,
        }) {
            let _ = self.pop_tail_frame();
        }
    }

    fn pop_last_condition_owner(
        &mut self,
        kind: ConditionOwnerKind,
    ) -> Option<ConditionOwnerFrame> {
        let mut indent_level = self.statement_base_indent();
        match self.take_last_frame_matching_via_tail_pop(
            &mut indent_level,
            |frame| matches!(frame, FormatFrame::ConditionOwner(frame) if frame.kind == kind),
        ) {
            Some(FormatFrame::ConditionOwner(frame)) => Some(frame),
            _ => None,
        }
    }

    fn last_condition_owner(&self, kind: ConditionOwnerKind) -> Option<ConditionOwnerFrame> {
        let idx = *self.condition_owner_frame_indices[kind.index()].last()?;
        match self.frames.get(idx) {
            Some(FormatFrame::ConditionOwner(owner)) => Some(*owner),
            _ => None,
        }
    }

    fn push_between_pending(&mut self, scope: FormatScope) {
        self.push_frame(FormatFrame::BetweenPending(BetweenPendingFrame { scope }));
    }

    fn between_pending_matches(&self, scope: FormatScope) -> bool {
        matches!(
            self.frames.last(),
            Some(FormatFrame::BetweenPending(frame)) if frame.scope == scope
        )
    }

    fn pop_between_pending_if_matches(&mut self, scope: FormatScope) -> bool {
        if !self.between_pending_matches(scope) {
            return false;
        }
        self.pop_tail_frame().is_some()
    }

    fn pop_between_pending_at_or_above(&mut self, scope: FormatScope) {
        while self.frames.last().is_some_and(|frame| {
            matches!(frame, FormatFrame::BetweenPending(frame) if !frame.scope.contains(scope))
        }) {
            let _ = self.pop_tail_frame();
        }
    }

    fn pending_split_end_suffix_indent(&self) -> Option<usize> {
        let idx = self.pending_split_end_suffix_frame_idx?;
        match self.frames.get(idx) {
            Some(FormatFrame::PendingSplitEndSuffix(frame)) => Some(frame.indent),
            _ => None,
        }
    }

    #[cfg_attr(not(test), allow(dead_code))]
    fn pending_split_end_suffix_kind(&self) -> Option<PendingSplitEndSuffixKind> {
        let idx = self.pending_split_end_suffix_frame_idx?;
        match self.frames.get(idx) {
            Some(FormatFrame::PendingSplitEndSuffix(frame)) => Some(frame.kind),
            _ => None,
        }
    }

    fn set_pending_split_end_suffix(
        &mut self,
        scope: FormatScope,
        indent: usize,
        kind: PendingSplitEndSuffixKind,
    ) {
        let _ = self.clear_pending_split_end_suffix();
        self.push_frame(FormatFrame::PendingSplitEndSuffix(
            PendingSplitEndSuffixFrame {
                scope,
                indent,
                kind,
            },
        ));
    }

    fn clear_pending_split_end_suffix(&mut self) -> bool {
        let mut indent_level = self.statement_base_indent();
        self.remove_last_frame_matching_via_tail_pop(&mut indent_level, |frame| {
            matches!(frame, FormatFrame::PendingSplitEndSuffix(_))
        })
    }

    fn advance_pending_split_end_suffix(&mut self, word_upper: &str) {
        let Some(idx) = self.pending_split_end_suffix_frame_idx else {
            return;
        };
        let next_kind = match self.frames.get(idx) {
            Some(FormatFrame::PendingSplitEndSuffix(frame)) => frame.kind.advance(word_upper),
            _ => None,
        };
        match next_kind {
            Some(next_kind) => {
                if let Some(FormatFrame::PendingSplitEndSuffix(frame)) = self.frames.get_mut(idx) {
                    frame.kind = next_kind;
                }
            }
            None => {
                let _ = self.clear_pending_split_end_suffix();
            }
        }
    }

    fn pending_plsql_label_body_indent(&self) -> Option<usize> {
        let idx = self.pending_plsql_label_frame_idx?;
        match self.frames.get(idx) {
            Some(FormatFrame::PendingPlsqlLabel(frame)) => Some(frame.body_indent),
            _ => None,
        }
    }

    fn set_pending_plsql_label_body_indent(&mut self, scope: FormatScope, body_indent: usize) {
        let _ = self.clear_pending_plsql_label_body_indent();
        self.push_frame(FormatFrame::PendingPlsqlLabel(PendingPlsqlLabelFrame {
            scope,
            body_indent,
        }));
    }

    fn clear_pending_plsql_label_body_indent(&mut self) -> bool {
        let mut indent_level = self.statement_base_indent();
        self.remove_last_frame_matching_via_tail_pop(&mut indent_level, |frame| {
            matches!(frame, FormatFrame::PendingPlsqlLabel(_))
        })
    }

    fn mysql_handler_pending_body_indent(&self) -> Option<usize> {
        let idx = self.mysql_handler_pending_body_indent_frame_idx?;
        match self.frames.get(idx) {
            Some(FormatFrame::MySqlHandlerPendingBodyIndent(frame)) => Some(frame.indent),
            _ => None,
        }
    }

    fn set_mysql_handler_pending_body_indent(&mut self, scope: FormatScope, indent: usize) {
        let _ = self.clear_mysql_handler_pending_body_indent();
        self.push_frame(FormatFrame::MySqlHandlerPendingBodyIndent(
            MySqlHandlerPendingBodyIndentFrame { scope, indent },
        ));
    }

    fn clear_mysql_handler_pending_body_indent(&mut self) -> bool {
        let mut indent_level = self.statement_base_indent();
        self.remove_last_frame_matching_via_tail_pop(&mut indent_level, |frame| {
            matches!(frame, FormatFrame::MySqlHandlerPendingBodyIndent(_))
        })
    }

    fn push_scoped_indent(
        &mut self,
        kind: ScopedIndentKind,
        scope: FormatScope,
        delta: usize,
        indent_level: &mut usize,
    ) {
        if self
            .last_scoped_indent(kind)
            .is_some_and(|frame| frame.scope == scope)
        {
            return;
        }
        *indent_level = indent_level.saturating_add(delta);
        self.push_frame(FormatFrame::ScopedIndent(ScopedIndentFrame {
            kind,
            scope,
            delta,
        }));
        #[cfg(debug_assertions)]
        {
            self.debug_assert_integrity();
            self.debug_assert_indent_level(*indent_level);
        }
    }

    fn scoped_indent_is_active(&self, kind: ScopedIndentKind) -> bool {
        self.last_scoped_indent(kind).is_some()
    }

    fn last_scoped_indent(&self, kind: ScopedIndentKind) -> Option<ScopedIndentFrame> {
        let idx = *self.scoped_indent_frame_indices[kind.index()].last()?;
        match self.frames.get(idx) {
            Some(FormatFrame::ScopedIndent(frame)) => Some(*frame),
            _ => None,
        }
    }

    fn deactivate_scoped_indent(
        &mut self,
        kind: ScopedIndentKind,
        indent_level: &mut usize,
    ) -> bool {
        let removed = self.remove_last_frame_matching_via_tail_pop(
            indent_level,
            |frame| matches!(frame, FormatFrame::ScopedIndent(frame) if frame.kind == kind),
        );
        if !removed {
            return false;
        }
        self.realign_indent_level_to_base(indent_level);
        #[cfg(debug_assertions)]
        {
            self.debug_assert_integrity();
            self.debug_assert_indent_level(*indent_level);
        }
        true
    }

    #[cfg_attr(not(test), allow(dead_code))]
    fn clear_scoped_indent(&mut self, kind: ScopedIndentKind, indent_level: &mut usize) {
        while self.remove_last_frame_matching_via_tail_pop(
            indent_level,
            |frame| matches!(frame, FormatFrame::ScopedIndent(frame) if frame.kind == kind),
        ) {}
        self.realign_indent_level_to_base(indent_level);
        #[cfg(debug_assertions)]
        {
            self.debug_assert_integrity();
            self.debug_assert_indent_level(*indent_level);
        }
    }

    fn push_trigger_header(&mut self, scope: FormatScope) {
        if self.last_trigger_header_scope() == Some(scope) {
            return;
        }
        self.push_frame(FormatFrame::TriggerHeader(scope));
    }

    fn last_trigger_header_scope(&self) -> Option<FormatScope> {
        let idx = *self.trigger_header_frame_indices.last()?;
        match self.frames.get(idx) {
            Some(FormatFrame::TriggerHeader(scope)) => Some(*scope),
            _ => None,
        }
    }

    fn trigger_header_is_active(&self) -> bool {
        self.last_trigger_header_scope().is_some()
    }

    fn clear_trigger_header(&mut self) {
        let mut indent_level = self.statement_base_indent();
        while self
            .take_last_frame_matching_via_tail_pop(&mut indent_level, |frame| {
                matches!(frame, FormatFrame::TriggerHeader(_))
            })
            .is_some()
        {}
    }

    fn push_plsql_context(&mut self, scope: FormatScope) {
        if self.plsql_context_scope() == Some(scope) {
            return;
        }
        self.push_frame(FormatFrame::PlsqlContext(scope));
    }

    fn plsql_context_scope(&self) -> Option<FormatScope> {
        let idx = *self.plsql_context_frame_indices.last()?;
        match self.frames.get(idx) {
            Some(FormatFrame::PlsqlContext(scope)) => Some(*scope),
            _ => None,
        }
    }

    fn plsql_context_is_active(&self) -> bool {
        self.plsql_context_scope().is_some()
    }

    fn push_compound_trigger(&mut self, scope: FormatScope) {
        self.push_frame(FormatFrame::CompoundTrigger(CompoundTriggerFrame {
            scope,
            phase: CompoundTriggerPhase::AwaitingOuterBodyStart,
        }));
    }

    fn last_compound_trigger_mut(&mut self) -> Option<&mut CompoundTriggerFrame> {
        let idx = *self.compound_trigger_frame_indices.last()?;
        match self.frames.get_mut(idx) {
            Some(FormatFrame::CompoundTrigger(frame)) => Some(frame),
            _ => None,
        }
    }

    fn last_compound_trigger(&self) -> Option<&CompoundTriggerFrame> {
        let idx = *self.compound_trigger_frame_indices.last()?;
        match self.frames.get(idx) {
            Some(FormatFrame::CompoundTrigger(frame)) => Some(frame),
            _ => None,
        }
    }

    fn compound_trigger_awaiting_outer_body_start(&self) -> bool {
        self.last_compound_trigger().is_some_and(|frame| {
            matches!(frame.phase, CompoundTriggerPhase::AwaitingOuterBodyStart)
        })
    }

    fn compound_trigger_is_in_outer_body(&self) -> bool {
        self.last_compound_trigger().is_some_and(|frame| {
            matches!(
                frame.phase,
                CompoundTriggerPhase::InOuterBody
                    | CompoundTriggerPhase::AwaitingTimingPointBodyStart
            )
        })
    }

    fn compound_trigger_enter_outer_body(&mut self) {
        if let Some(frame) = self.last_compound_trigger_mut() {
            frame.phase = CompoundTriggerPhase::InOuterBody;
        }
    }

    fn compound_trigger_start_timing_point(&mut self) {
        if let Some(frame) = self.last_compound_trigger_mut() {
            frame.phase = CompoundTriggerPhase::AwaitingTimingPointBodyStart;
        }
    }

    fn compound_trigger_begin_timing_point_body(&mut self) {
        if let Some(frame) = self.last_compound_trigger_mut() {
            if matches!(
                frame.phase,
                CompoundTriggerPhase::AwaitingTimingPointBodyStart
            ) {
                frame.phase = CompoundTriggerPhase::InOuterBody;
            }
        }
    }

    fn clear_compound_trigger(&mut self) {
        let mut indent_level = self.statement_base_indent();
        while self
            .take_last_frame_matching_via_tail_pop(&mut indent_level, |frame| {
                matches!(frame, FormatFrame::CompoundTrigger(_))
            })
            .is_some()
        {}
    }

    fn push_with_cte_frame(&mut self, scope: FormatScope) {
        self.push_frame(FormatFrame::WithCte(WithCteFrame {
            scope,
            paren_depth: 0,
            owner_depth: None,
            plsql_state: WithPlsqlFormatState::None,
        }));
    }

    fn last_with_cte_mut(&mut self) -> Option<&mut WithCteFrame> {
        let idx = *self.with_cte_frame_indices.last()?;
        match self.frames.get_mut(idx) {
            Some(FormatFrame::WithCte(frame)) => Some(frame),
            _ => None,
        }
    }

    fn last_with_cte(&self) -> Option<&WithCteFrame> {
        let idx = *self.with_cte_frame_indices.last()?;
        match self.frames.get(idx) {
            Some(FormatFrame::WithCte(frame)) => Some(frame),
            _ => None,
        }
    }

    fn pop_last_with_cte(&mut self) -> bool {
        let mut indent_level = self.statement_base_indent();
        self.take_last_frame_matching_via_tail_pop(&mut indent_level, |frame| {
            matches!(frame, FormatFrame::WithCte(_))
        })
        .is_some()
    }

    fn sync_to_scope(&mut self, current_scope: FormatScope, indent_level: &mut usize) {
        #[cfg(debug_assertions)]
        let frames_len_before = self.frames.len();
        #[cfg(debug_assertions)]
        let base_indent_before = self.base_indent_level;
        #[cfg(debug_assertions)]
        let indent_before = *indent_level;

        self.pop_tail_auxiliary_frames_for_scope(current_scope, indent_level);
        self.realign_indent_level_to_base(indent_level);

        #[cfg(debug_assertions)]
        {
            let structural_state_changed = self.frames.len() != frames_len_before
                || self.base_indent_level != base_indent_before
                || *indent_level != indent_before;
            if structural_state_changed {
                self.debug_assert_integrity_for_scope(current_scope);
                self.debug_assert_indent_level(*indent_level);
            }
        }
    }

    fn push_open_cursor_restore(&mut self, state: OpenCursorFormatState) {
        self.push_frame(FormatFrame::OpenCursorRestore(state));
    }

    fn pop_open_cursor_restore(&mut self) -> Option<OpenCursorFormatState> {
        let Some(FormatFrame::OpenCursorRestore(state)) = self.frames.last() else {
            return None;
        };
        let state = *state;
        let _ = self.pop_tail_frame();
        Some(state)
    }

    /// Clear all non-persistent frames at a statement boundary and restore
    /// `indent_level` from the surviving frame stack via
    /// `statement_base_indent()`. Surviving frames (`Block`, MySQL handler
    /// scoped indent, `PlsqlContext`, `CompoundTrigger`, `WithCte`) remain the
    /// single source of truth for the base indent, so cleanup happens by
    /// tail-pop and replay instead of middle-frame retention.
    fn clear_statement_frames(&mut self, indent_level: &mut usize) {
        let mut frames_to_restore = Vec::new();
        while let Some(frame) = self.pop_tail_frame_adjust_indent(indent_level) {
            if frame.survives_statement_boundary() {
                frames_to_restore.push(frame);
            }
        }
        for frame in frames_to_restore.into_iter().rev() {
            self.restore_tail_frame_with_indent(frame, indent_level);
        }
        self.realign_indent_level_to_base(indent_level);
        #[cfg(debug_assertions)]
        {
            self.debug_assert_statement_boundary_integrity();
            self.debug_assert_indent_level(*indent_level);
        }
    }

    fn reset_for_statement_boundary(&mut self, indent_level: &mut usize) {
        #[cfg(test)]
        let scope_before = self.current_scope();
        #[cfg(test)]
        let indent_before = *indent_level;
        self.pop_between_pending_at_or_above(FormatScope::new(0, 0));
        self.clear_statement_frames(indent_level);
        #[cfg(test)]
        self.record_transition(
            FormatFrameTransitionKind::StatementBoundaryReset,
            scope_before,
            indent_before,
            *indent_level,
        );
    }

    #[cfg(debug_assertions)]
    fn debug_assert_integrity(&self) {
        debug_assert!(
            self.paren_depth <= self.frames.len(),
            "paren depth exceeds frame count"
        );
        debug_assert!(
            self.block_depth <= self.frames.len(),
            "block depth exceeds frame count"
        );

        let last_paren_id = self
            .last_paren_frame_idx
            .and_then(|idx| match self.frames.get(idx) {
                Some(FormatFrame::Paren(frame)) => Some(frame.id),
                _ => None,
            })
            .or_else(|| {
                self.last_paren_frame_id_cache.and_then(|_| {
                    self.frames.iter().rev().find_map(|frame| match frame {
                        FormatFrame::Paren(frame) => Some(frame.id),
                        _ => None,
                    })
                })
            });
        let last_block_id = self
            .last_block_frame_idx
            .and_then(|idx| match self.frames.get(idx) {
                Some(FormatFrame::Block(frame)) => Some(frame.id),
                _ => None,
            })
            .or_else(|| {
                self.last_block_frame_id_cache.and_then(|_| {
                    self.frames.iter().rev().find_map(|frame| match frame {
                        FormatFrame::Block(frame) => Some(frame.id),
                        _ => None,
                    })
                })
            });

        debug_assert_eq!(
            self.paren_depth == 0,
            self.last_paren_frame_id_cache.is_none(),
            "paren depth/index drift"
        );
        debug_assert_eq!(
            self.block_depth == 0,
            self.last_block_frame_id_cache.is_none(),
            "block depth/index drift"
        );
        debug_assert_eq!(
            self.last_paren_frame_id(),
            last_paren_id,
            "last paren frame id drift"
        );
        debug_assert_eq!(
            self.last_block_frame_id(),
            last_block_id,
            "last block frame id drift"
        );
    }

    #[cfg(debug_assertions)]
    fn debug_assert_indent_level(&self, indent_level: usize) {
        let expected = self.expected_indent_level();
        debug_assert_eq!(
            indent_level, expected,
            "indent_level drift: got {indent_level}, expected {expected}"
        );
    }

    #[cfg(debug_assertions)]
    fn debug_assert_integrity_for_scope(&self, current_scope: FormatScope) {
        self.debug_assert_integrity();
        for frame in &self.frames {
            match frame {
                FormatFrame::ScopedIndent(frame) => debug_assert!(
                    frame.scope.contains(current_scope),
                    "scoped indent survived outside its scope"
                ),
                FormatFrame::ConstructFlag(frame) => debug_assert!(
                    !frame.active || frame.scope.contains(current_scope),
                    "construct flag survived outside its scope"
                ),
                FormatFrame::ConstructValue(frame) => debug_assert!(
                    frame.scope.contains(current_scope),
                    "construct value survived outside its scope"
                ),
                FormatFrame::ConditionOwner(frame) => debug_assert!(
                    frame.scope.contains(current_scope),
                    "condition owner survived outside its scope"
                ),
                FormatFrame::BetweenPending(frame) => debug_assert!(
                    frame.scope.contains(current_scope),
                    "between-pending frame survived outside its scope"
                ),
                FormatFrame::PendingSplitEndSuffix(frame) => debug_assert!(
                    frame.scope.contains(current_scope),
                    "split END suffix frame survived outside its scope"
                ),
                FormatFrame::PendingPlsqlLabel(frame) => debug_assert!(
                    frame.scope.contains(current_scope),
                    "PL/SQL label frame survived outside its scope"
                ),
                FormatFrame::MySqlHandlerPendingBodyIndent(frame) => debug_assert!(
                    frame.scope.contains(current_scope),
                    "MySQL handler pending body-indent frame survived outside its scope"
                ),
                FormatFrame::QueryRuntime(frame) => debug_assert!(
                    frame.scope.contains(current_scope),
                    "query runtime frame survived outside its scope"
                ),
                FormatFrame::TriggerHeader(scope) | FormatFrame::PlsqlContext(scope) => {
                    debug_assert!(
                        scope.contains(current_scope),
                        "scoped formatter frame survived outside its scope"
                    );
                }
                FormatFrame::CompoundTrigger(frame) => debug_assert!(
                    frame.scope.contains(current_scope),
                    "compound trigger frame survived outside its scope"
                ),
                FormatFrame::WithCte(frame) => debug_assert!(
                    frame.scope.contains(current_scope),
                    "with-cte frame survived outside its scope"
                ),
                FormatFrame::Paren(_)
                | FormatFrame::Block(_)
                | FormatFrame::OpenCursorRestore(_) => {}
            }
        }
    }

    #[cfg(debug_assertions)]
    fn debug_assert_statement_boundary_integrity(&self) {
        self.debug_assert_integrity();
        for frame in &self.frames {
            debug_assert!(
                frame.survives_statement_boundary(),
                "non-persistent frame survived statement boundary cleanup"
            );
        }
    }
}

#[derive(Clone, Copy)]
enum PendingSplitEndSuffixKind {
    SingleWord,
    TimingSuffix {
        consumed_timing_keyword: bool,
        consumed_each: bool,
    },
}

impl PendingSplitEndSuffixKind {
    fn from_first_suffix_word(word_upper: &str) -> Self {
        if matches!(word_upper, "BEFORE" | "AFTER") {
            Self::TimingSuffix {
                consumed_timing_keyword: false,
                consumed_each: false,
            }
        } else {
            Self::SingleWord
        }
    }

    fn advance(self, word_upper: &str) -> Option<Self> {
        match self {
            Self::SingleWord => None,
            Self::TimingSuffix {
                consumed_timing_keyword: false,
                consumed_each: false,
            } => {
                if matches!(word_upper, "BEFORE" | "AFTER") {
                    Some(Self::TimingSuffix {
                        consumed_timing_keyword: true,
                        consumed_each: false,
                    })
                } else {
                    None
                }
            }
            Self::TimingSuffix {
                consumed_timing_keyword: true,
                consumed_each: false,
            } => {
                if word_upper == "EACH" {
                    Some(Self::TimingSuffix {
                        consumed_timing_keyword: true,
                        consumed_each: true,
                    })
                } else {
                    None
                }
            }
            Self::TimingSuffix {
                consumed_timing_keyword: true,
                consumed_each: true,
            } => None,
            Self::TimingSuffix {
                consumed_timing_keyword: false,
                consumed_each: true,
            } => None,
        }
    }
}

#[derive(Clone, Copy, Debug, Default, PartialEq, Eq)]
struct FormatScope {
    paren_depth: usize,
    block_depth: usize,
    paren_frame_id: Option<SqlFormatFrameId>,
    block_frame_id: Option<SqlFormatFrameId>,
}

impl FormatScope {
    fn new(paren_depth: usize, block_depth: usize) -> Self {
        Self::with_frame_ids(paren_depth, block_depth, None, None)
    }

    fn with_frame_ids(
        paren_depth: usize,
        block_depth: usize,
        paren_frame_id: Option<SqlFormatFrameId>,
        block_frame_id: Option<SqlFormatFrameId>,
    ) -> Self {
        Self {
            paren_depth,
            block_depth,
            paren_frame_id,
            block_frame_id,
        }
    }

    fn contains(self, other: Self) -> bool {
        SqlFormatScopedFrame::new(self.paren_depth, self.paren_frame_id).contains(
            SqlFormatScopedFrame::new(other.paren_depth, other.paren_frame_id),
        ) && SqlFormatScopedFrame::new(self.block_depth, self.block_frame_id).contains(
            SqlFormatScopedFrame::new(other.block_depth, other.block_frame_id),
        )
    }
}

impl FormatFrame {
    fn expires_for_scope(&self, next_scope: FormatScope) -> bool {
        match self {
            FormatFrame::ConditionOwner(owner) => match owner.kind {
                ConditionOwnerKind::JoinOn
                | ConditionOwnerKind::Where
                | ConditionOwnerKind::Case
                | ConditionOwnerKind::ControlHeader => !owner.scope.contains(next_scope),
            },
            FormatFrame::BetweenPending(frame) => !frame.scope.contains(next_scope),
            FormatFrame::PendingSplitEndSuffix(frame) => !frame.scope.contains(next_scope),
            FormatFrame::PendingPlsqlLabel(frame) => !frame.scope.contains(next_scope),
            FormatFrame::MySqlHandlerPendingBodyIndent(frame) => !frame.scope.contains(next_scope),
            FormatFrame::ScopedIndent(frame) => !frame.scope.contains(next_scope),
            FormatFrame::QueryRuntime(frame) => !frame.scope.contains(next_scope),
            FormatFrame::ConstructFlag(frame) => !frame.active || !frame.scope.contains(next_scope),
            FormatFrame::ConstructValue(frame) => !frame.scope.contains(next_scope),
            FormatFrame::TriggerHeader(scope) | FormatFrame::PlsqlContext(scope) => {
                !scope.contains(next_scope)
            }
            FormatFrame::CompoundTrigger(frame) => !frame.scope.contains(next_scope),
            FormatFrame::WithCte(frame) => !frame.scope.contains(next_scope),
            FormatFrame::Paren(_) | FormatFrame::Block(_) | FormatFrame::OpenCursorRestore(_) => {
                false
            }
        }
    }

    fn survives_statement_boundary(&self) -> bool {
        matches!(
            self,
            FormatFrame::Block(_)
                | FormatFrame::ScopedIndent(ScopedIndentFrame {
                    kind: ScopedIndentKind::MySqlHandlerBody | ScopedIndentKind::SplitControlBody,
                    ..
                })
                | FormatFrame::PlsqlContext(_)
                | FormatFrame::CompoundTrigger(_)
                | FormatFrame::WithCte(_)
        )
    }
}

#[derive(Clone, Copy, PartialEq, Eq)]
enum ConstructFlagKind {
    BulkCollectActive,
    CommentOnActive,
    CreateIndexPending,
    CreatePending,
    CreateSequenceActive,
    CreateSynonymActive,
    CreateTableParenExpected,
    CursorDeclPending,
    ExecuteImmediateActive,
    FetchActive,
    ForallPending,
    GrantRevokeActive,
    InsertAllActive,
    MergeActive,
    ModelActive,
    ModelReferencePending,
    ReferentialActionPending,
    ReferentialOnActive,
    ReturningActive,
    RoutineDeclPending,
    SearchCycleClauseActive,
}

impl ConstructFlagKind {
    const COUNT: usize = 21;

    fn index(self) -> usize {
        match self {
            Self::BulkCollectActive => 0,
            Self::CommentOnActive => 1,
            Self::CreateIndexPending => 2,
            Self::CreatePending => 3,
            Self::CreateSequenceActive => 4,
            Self::CreateSynonymActive => 5,
            Self::CreateTableParenExpected => 6,
            Self::CursorDeclPending => 7,
            Self::ExecuteImmediateActive => 8,
            Self::FetchActive => 9,
            Self::ForallPending => 10,
            Self::GrantRevokeActive => 11,
            Self::InsertAllActive => 12,
            Self::MergeActive => 13,
            Self::ModelActive => 14,
            Self::ModelReferencePending => 15,
            Self::ReferentialActionPending => 16,
            Self::ReferentialOnActive => 17,
            Self::ReturningActive => 18,
            Self::RoutineDeclPending => 19,
            Self::SearchCycleClauseActive => 20,
        }
    }
}

#[derive(Clone, Copy, PartialEq, Eq)]
enum ConstructValueKind {
    CreateObject,
}

impl ConstructValueKind {
    const COUNT: usize = 1;

    fn index(self) -> usize {
        match self {
            Self::CreateObject => 0,
        }
    }
}

#[derive(Clone, Copy)]
struct ConstructFlagFrame {
    kind: ConstructFlagKind,
    scope: FormatScope,
    active: bool,
}

#[derive(Clone)]
struct ConstructValueFrame {
    kind: ConstructValueKind,
    scope: FormatScope,
    value: String,
}

// For huge buffers, avoid an additional full/prefix reformat pass when remapping cursor position.
const CURSOR_MAPPING_FULL_REFORMAT_THRESHOLD_BYTES: usize = 2 * 1024 * 1024;
// Large documents should avoid expensive cache copies and exact cursor-token
// remapping passes even when they stay relatively compact in bytes.
const LARGE_DOCUMENT_FORMAT_THRESHOLD_BYTES: usize = 8 * 1024 * 1024;
const LARGE_DOCUMENT_FORMAT_THRESHOLD_LINES: usize = 300_000;

#[derive(Clone, Copy, Default, PartialEq, Eq)]
enum OpenCursorFormatState {
    #[default]
    None,
    AwaitingFor,
    InSelect {
        anchor_indent: usize,
        select_paren_depth: Option<usize>,
    },
}

#[derive(Clone, Copy, PartialEq, Eq)]
enum SelectListBreakState {
    None,
}

#[derive(Clone, Copy, Default, PartialEq, Eq)]
enum SelectListLayoutState {
    #[default]
    Inactive,
    Pending {
        anchor: usize,
        indent: usize,
    },
    Multiline {
        indent: usize,
        hanging_indent_spaces: Option<usize>,
    },
}

#[derive(Clone, Copy, PartialEq, Eq)]
enum InlineCommentContinuationState {
    None,
    Operand { indent: usize },
}

impl SelectListLayoutState {
    fn activate(&mut self, anchor: usize, indent: usize) {
        *self = Self::Pending { anchor, indent };
    }

    fn clear(&mut self) {
        *self = Self::Inactive;
    }

    fn is_multiline(self) -> bool {
        matches!(self, Self::Multiline { .. })
    }

    fn has_active_indent(self) -> bool {
        matches!(self, Self::Pending { .. } | Self::Multiline { .. })
    }

    fn indentation_or(self, fallback: usize) -> usize {
        match self {
            Self::Pending { indent, .. } | Self::Multiline { indent, .. } => indent,
            Self::Inactive => fallback,
        }
    }

    fn hanging_indent_spaces(self, out: &str, fallback_indent: usize) -> usize {
        match self {
            Self::Pending { anchor, indent } => {
                if anchor < out.len() && out.as_bytes().get(anchor) == Some(&b' ') {
                    let line_start = out[..anchor].rfind('\n').map(|idx| idx + 1).unwrap_or(0);
                    anchor
                        .saturating_sub(line_start)
                        .saturating_add(1)
                        .max(indent * 4)
                } else {
                    indent.max(fallback_indent) * 4
                }
            }
            Self::Multiline {
                indent,
                hanging_indent_spaces,
            } => hanging_indent_spaces.unwrap_or_else(|| indent.max(fallback_indent) * 4),
            Self::Inactive => fallback_indent * 4,
        }
    }

    fn force_newline_if_possible(self, out: &mut String) -> Self {
        match self {
            Self::Pending { anchor, indent } => {
                if anchor < out.len() && out.as_bytes().get(anchor) == Some(&b' ') {
                    let indentation = " ".repeat(indent * 4);
                    out.replace_range(anchor..anchor + 1, &format!("\n{indentation}"));
                    Self::Multiline {
                        indent,
                        hanging_indent_spaces: None,
                    }
                } else {
                    self
                }
            }
            _ => self,
        }
    }
}

impl SelectListBreakState {
    fn clear(&mut self) {
        *self = Self::None;
    }
}

#[derive(Clone, Copy, PartialEq, Eq)]
enum ExitConditionState {
    None,
    AwaitingWhen,
}

#[derive(Clone, Copy, PartialEq, Eq)]
enum MySqlHandlerFormatState {
    None,
    AwaitingConditionStart,
    AwaitingNotFoundTail,
    AwaitingSqlstateValueOrLiteral,
    AwaitingSqlstateLiteral,
    AwaitingNextConditionOrBody,
}

impl MySqlHandlerFormatState {
    fn start(&mut self) {
        *self = Self::AwaitingConditionStart;
    }

    fn clear(&mut self) {
        *self = Self::None;
    }

    fn body_line_pending(self) -> bool {
        matches!(self, Self::AwaitingNextConditionOrBody)
    }

    fn consume_word(&mut self, word_upper: &str) -> bool {
        match *self {
            Self::None => false,
            Self::AwaitingConditionStart => {
                *self = match word_upper {
                    "NOT" => Self::AwaitingNotFoundTail,
                    "SQLSTATE" => Self::AwaitingSqlstateValueOrLiteral,
                    _ => Self::AwaitingNextConditionOrBody,
                };
                true
            }
            Self::AwaitingNotFoundTail => {
                if word_upper == "FOUND" {
                    *self = Self::AwaitingNextConditionOrBody;
                    true
                } else {
                    *self = Self::None;
                    false
                }
            }
            Self::AwaitingSqlstateValueOrLiteral => {
                if word_upper == "VALUE" {
                    *self = Self::AwaitingSqlstateLiteral;
                    true
                } else {
                    *self = Self::None;
                    false
                }
            }
            Self::AwaitingSqlstateLiteral => {
                *self = Self::None;
                false
            }
            Self::AwaitingNextConditionOrBody => {
                *self = Self::None;
                false
            }
        }
    }

    fn consume_string(&mut self) -> bool {
        match *self {
            Self::AwaitingConditionStart => {
                *self = Self::AwaitingNextConditionOrBody;
                true
            }
            Self::AwaitingSqlstateValueOrLiteral | Self::AwaitingSqlstateLiteral => {
                *self = Self::AwaitingNextConditionOrBody;
                true
            }
            Self::AwaitingNotFoundTail | Self::AwaitingNextConditionOrBody => {
                *self = Self::None;
                false
            }
            Self::None => false,
        }
    }

    fn on_symbol(&mut self, symbol: &str) {
        match (*self, symbol) {
            (Self::AwaitingNextConditionOrBody, ",") => {
                *self = Self::AwaitingConditionStart;
            }
            (_, ";") => {
                *self = Self::None;
            }
            _ => {}
        }
    }
}

impl ExitConditionState {
    fn on_keyword(&mut self, keyword: &str) {
        match (keyword, *self) {
            ("EXIT" | "CONTINUE", _) => {
                *self = Self::AwaitingWhen;
            }
            ("WHEN", Self::AwaitingWhen) => {
                *self = Self::None;
            }
            _ => {}
        }
    }

    fn is_exit_when(self, keyword: &str) -> bool {
        keyword == "WHEN" && matches!(self, Self::AwaitingWhen)
    }

    fn clear(&mut self) {
        *self = Self::None;
    }
}

#[derive(Clone, PartialEq, Eq)]
struct WithCteFrame {
    scope: FormatScope,
    paren_depth: usize,
    owner_depth: Option<usize>,
    plsql_state: WithPlsqlFormatState,
}

#[derive(Clone, Copy, PartialEq, Eq)]
struct WithPlsqlPendingDeclaration {
    starts_body: bool,
}

#[derive(Clone, Copy, PartialEq, Eq)]
enum WithPlsqlBodyFrameKind {
    Routine,
    Block,
    Case,
    If,
    Loop,
}

#[derive(Clone, Copy, PartialEq, Eq)]
struct WithPlsqlBodyFrame {
    kind: WithPlsqlBodyFrameKind,
    awaiting_begin: bool,
}

impl WithPlsqlBodyFrame {
    fn routine() -> Self {
        Self {
            kind: WithPlsqlBodyFrameKind::Routine,
            awaiting_begin: true,
        }
    }

    fn nested(kind: WithPlsqlBodyFrameKind) -> Self {
        Self {
            kind,
            awaiting_begin: false,
        }
    }
}

#[derive(Clone, PartialEq, Eq)]
enum WithPlsqlFormatState {
    None,
    Collecting {
        active_body_frames: Vec<WithPlsqlBodyFrame>,
        pending_routine_declaration: Option<WithPlsqlPendingDeclaration>,
        pending_end: bool,
    },
    AwaitingMainQuery,
}

#[derive(Clone, Copy, PartialEq, Eq)]
enum CompoundTriggerPhase {
    AwaitingOuterBodyStart,
    InOuterBody,
    AwaitingTimingPointBodyStart,
}

#[derive(Clone)]
struct CompoundTriggerFrame {
    scope: FormatScope,
    phase: CompoundTriggerPhase,
}

#[derive(Clone, Copy, PartialEq, Eq)]
enum CommentAttachment {
    Previous,
    Next,
    Block,
    FileHeader,
}

impl FormatFrameStack {
    fn pop_with_plsql_body_frame(
        active_body_frames: &mut Vec<WithPlsqlBodyFrame>,
        expected_kind: Option<WithPlsqlBodyFrameKind>,
    ) {
        match expected_kind {
            Some(expected_kind) => {
                if active_body_frames
                    .last()
                    .is_some_and(|frame| frame.kind == expected_kind)
                {
                    let _ = active_body_frames.pop();
                }
            }
            None => {
                let _ = active_body_frames.pop();
            }
        }
    }

    fn with_cte_on_word(&mut self, keyword: &str, prev_word_upper: Option<&str>) {
        let upper = keyword.to_ascii_uppercase();
        let mut close_current_frame = false;

        if let Some(frame) = self.last_with_cte_mut() {
            match &mut frame.plsql_state {
                WithPlsqlFormatState::None | WithPlsqlFormatState::AwaitingMainQuery => {
                    if frame.paren_depth == 0
                        && sql_text::is_with_plsql_declaration_keyword(upper.as_str())
                    {
                        frame.plsql_state = WithPlsqlFormatState::Collecting {
                            active_body_frames: Vec::new(),
                            pending_routine_declaration: Some(WithPlsqlPendingDeclaration {
                                starts_body: sql_text::with_plsql_declaration_starts_routine_body(
                                    upper.as_str(),
                                ),
                            }),
                            pending_end: false,
                        };
                    } else if frame.paren_depth == 0
                        && matches!(
                            upper.as_str(),
                            "SELECT" | "UPDATE" | "DELETE" | "INSERT" | "MERGE"
                        )
                    {
                        close_current_frame = true;
                    }
                }
                WithPlsqlFormatState::Collecting {
                    active_body_frames,
                    pending_routine_declaration,
                    pending_end,
                } => {
                    let mut consumed_end_qualifier = false;
                    if *pending_end {
                        match upper.as_str() {
                            "CASE" => {
                                Self::pop_with_plsql_body_frame(
                                    active_body_frames,
                                    Some(WithPlsqlBodyFrameKind::Case),
                                );
                                consumed_end_qualifier = true;
                            }
                            "IF" => {
                                Self::pop_with_plsql_body_frame(
                                    active_body_frames,
                                    Some(WithPlsqlBodyFrameKind::If),
                                );
                                consumed_end_qualifier = true;
                            }
                            "LOOP" => {
                                Self::pop_with_plsql_body_frame(
                                    active_body_frames,
                                    Some(WithPlsqlBodyFrameKind::Loop),
                                );
                                consumed_end_qualifier = true;
                            }
                            _ => {
                                Self::pop_with_plsql_body_frame(active_body_frames, None);
                            }
                        }
                        *pending_end = false;
                    }

                    if !consumed_end_qualifier {
                        if frame.paren_depth == 0
                            && sql_text::is_with_plsql_declaration_keyword(upper.as_str())
                        {
                            *pending_routine_declaration = Some(WithPlsqlPendingDeclaration {
                                starts_body: sql_text::with_plsql_declaration_starts_routine_body(
                                    upper.as_str(),
                                ),
                            });
                        } else if matches!(upper.as_str(), "AS" | "IS")
                            && pending_routine_declaration
                                .is_some_and(|declaration| declaration.starts_body)
                        {
                            active_body_frames.push(WithPlsqlBodyFrame::routine());
                            *pending_routine_declaration = None;
                        } else if upper == "BEGIN" {
                            if let Some(frame) = active_body_frames.last_mut() {
                                if frame.kind == WithPlsqlBodyFrameKind::Routine
                                    && frame.awaiting_begin
                                {
                                    frame.awaiting_begin = false;
                                } else {
                                    active_body_frames.push(WithPlsqlBodyFrame::nested(
                                        WithPlsqlBodyFrameKind::Block,
                                    ));
                                }
                            }
                        } else if upper == "DECLARE" {
                            if !active_body_frames.is_empty() {
                                active_body_frames.push(WithPlsqlBodyFrame::nested(
                                    WithPlsqlBodyFrameKind::Block,
                                ));
                            }
                        } else if upper == "CASE" {
                            if !active_body_frames.is_empty() {
                                active_body_frames
                                    .push(WithPlsqlBodyFrame::nested(WithPlsqlBodyFrameKind::Case));
                            }
                        } else if upper == "IF" {
                            if !active_body_frames.is_empty() {
                                active_body_frames
                                    .push(WithPlsqlBodyFrame::nested(WithPlsqlBodyFrameKind::If));
                            }
                        } else if upper == "LOOP" {
                            if !active_body_frames.is_empty() {
                                active_body_frames
                                    .push(WithPlsqlBodyFrame::nested(WithPlsqlBodyFrameKind::Loop));
                            }
                        } else if upper == "END" && !active_body_frames.is_empty() {
                            *pending_end = true;
                        }
                    }
                }
            }
        }

        if close_current_frame {
            let _ = self.pop_last_with_cte();
        }

        let starts_cte_definitions =
            upper == "WITH" && !matches!(prev_word_upper, Some("START" | "ROW" | "ROWS"));
        if starts_cte_definitions {
            self.push_with_cte_frame(self.current_scope());
        }
    }

    fn with_cte_record_owner_depth(&mut self, owner_depth: usize) {
        if let Some(frame) = self.last_with_cte_mut() {
            if frame.paren_depth == 0
                && matches!(
                    frame.plsql_state,
                    WithPlsqlFormatState::None | WithPlsqlFormatState::AwaitingMainQuery
                )
            {
                frame.owner_depth = Some(owner_depth);
            }
        }
    }

    fn with_cte_separator_indent(&self, fallback: usize) -> usize {
        self.last_with_cte()
            .and_then(|frame| frame.owner_depth)
            .unwrap_or(fallback)
    }

    fn with_cte_on_open_paren(&mut self) {
        for frame in &mut self.frames {
            if let FormatFrame::WithCte(frame) = frame {
                frame.paren_depth = frame.paren_depth.saturating_add(1);
            }
        }
    }

    fn with_cte_on_close_paren(&mut self) {
        for frame in &mut self.frames {
            if let FormatFrame::WithCte(frame) = frame {
                frame.paren_depth = frame.paren_depth.saturating_sub(1);
            }
        }
    }

    fn with_cte_on_separator(&mut self) {
        if let Some(frame) = self.last_with_cte_mut() {
            if let WithPlsqlFormatState::Collecting {
                active_body_frames,
                pending_routine_declaration,
                pending_end,
            } = &mut frame.plsql_state
            {
                if *pending_end {
                    Self::pop_with_plsql_body_frame(active_body_frames, None);
                    *pending_end = false;
                }
                if frame.paren_depth > 0 {
                    return;
                }
                *pending_routine_declaration = None;

                if active_body_frames.is_empty() {
                    frame.plsql_state = WithPlsqlFormatState::AwaitingMainQuery;
                }
            }
        }
    }

    fn with_cte_can_close_on_select(&self) -> bool {
        self.last_with_cte().is_some_and(|frame| {
            frame.paren_depth == 0
                && matches!(
                    frame.plsql_state,
                    WithPlsqlFormatState::None | WithPlsqlFormatState::AwaitingMainQuery
                )
        })
    }

    fn with_cte_collecting_routine_declaration_body_start(&self) -> bool {
        self.last_with_cte().is_some_and(|frame| {
            matches!(
                frame.plsql_state,
                WithPlsqlFormatState::Collecting {
                    pending_routine_declaration: Some(WithPlsqlPendingDeclaration {
                        starts_body: true
                    }),
                    ..
                }
            )
        })
    }

    fn with_cte_collecting_plsql_declaration_header(&self) -> bool {
        self.last_with_cte().is_some_and(|frame| {
            matches!(
                frame.plsql_state,
                WithPlsqlFormatState::Collecting {
                    pending_routine_declaration: Some(_),
                    ..
                }
            )
        })
    }

    fn with_cte_keeps_top_level_semicolon_inside_with_definitions(&self) -> bool {
        self.last_with_cte().is_some_and(|frame| {
            matches!(frame.plsql_state, WithPlsqlFormatState::AwaitingMainQuery)
        })
    }

    fn with_cte_in_definitions(&self) -> bool {
        self.last_with_cte().is_some()
    }
}

impl OpenCursorFormatState {
    fn base_indent(self, indent_level: usize) -> usize {
        match self {
            Self::InSelect { anchor_indent, .. } => indent_level.max(anchor_indent),
            _ => indent_level,
        }
    }

    fn in_select(self) -> bool {
        matches!(self, Self::InSelect { .. })
    }

    fn set_select_depth(&mut self, depth: usize) {
        if let Self::InSelect {
            select_paren_depth, ..
        } = self
        {
            if select_paren_depth.is_none() {
                *select_paren_depth = Some(depth);
            }
        }
    }

    fn select_depth(self) -> Option<usize> {
        match self {
            Self::InSelect {
                select_paren_depth, ..
            } => select_paren_depth,
            _ => None,
        }
    }
}

#[allow(dead_code)]
impl SqlEditorWidget {
    const INDENT_TAB_WIDTH: usize = 4;

    fn normalized_indent_tab_width() -> usize {
        Self::INDENT_TAB_WIDTH.max(1)
    }

    fn safe_indent_div(value: usize) -> usize {
        value
            .checked_div(Self::normalized_indent_tab_width())
            .unwrap_or(0)
    }

    fn mysql_formatter_profile_enabled() -> bool {
        std::env::var_os("SPACE_PROFILE_MYSQL_FORMATTER").is_some()
    }

    fn profile_mysql_formatter_stage<F>(
        stage: &str,
        input_len: usize,
        enabled: bool,
        f: F,
    ) -> FormatterStatementRenderResult
    where
        F: FnOnce() -> FormatterStatementRenderResult,
    {
        if !enabled {
            return f();
        }

        let started_at = std::time::Instant::now();
        let output = f();
        eprintln!(
            "[mysql-formatter] stage={stage} input_bytes={input_len} output_bytes={} elapsed_ms={:.3}",
            output.formatted.len(),
            started_at.elapsed().as_secs_f64() * 1000.0
        );
        output
    }

    fn previous_word_upper(tokens: &[SqlToken], start_idx: usize) -> Option<(String, usize)> {
        let mut idx = start_idx;
        while idx > 0 {
            idx = idx.saturating_sub(1);
            match tokens.get(idx) {
                Some(SqlToken::Comment(_)) => continue,
                Some(SqlToken::Word(word)) => return Some((word.to_ascii_uppercase(), idx)),
                _ => return None,
            }
        }

        None
    }

    fn leading_words_upper_from_tokens(tokens: &[SqlToken], max_words: usize) -> Vec<String> {
        tokens
            .iter()
            .filter_map(|token| match token {
                SqlToken::Word(word) => Some(word.to_ascii_uppercase()),
                _ => None,
            })
            .take(max_words)
            .collect()
    }

    fn statement_starts_with_package_body_header(tokens: &[SqlToken]) -> bool {
        let leading_words = Self::leading_words_upper_from_tokens(tokens, 8);
        FormatterStatementStructure::leading_words_start_package_body(leading_words.as_slice())
    }

    fn statement_contains_keyword(tokens: &[SqlToken], keyword: &str) -> bool {
        tokens.iter().any(
            |token| matches!(token, SqlToken::Word(word) if word.eq_ignore_ascii_case(keyword)),
        )
    }

    fn query_like_paren_layout(
        owner_line_info: QueryLikeOwnerLineInfo,
        owner_depth: usize,
        current_query_base: Option<usize>,
        standalone_opener: bool,
    ) -> QueryLikeParenLayout {
        if standalone_opener {
            let close_depth = if owner_line_info.ends_with_open_paren {
                owner_depth.saturating_add(1)
            } else {
                owner_depth
            };
            let child_head_depth = close_depth.saturating_add(1);
            return QueryLikeParenLayout {
                owner_depth,
                child_head_depth,
                close_depth,
            };
        }

        if owner_line_info.ends_with_as || owner_line_info.is_create_query_body_header {
            let child_head_depth = owner_depth.saturating_add(1);
            return QueryLikeParenLayout {
                owner_depth,
                child_head_depth,
                close_depth: owner_depth,
            };
        }

        if owner_line_info.ends_with_value {
            let child_head_depth = owner_depth.saturating_add(1);
            return QueryLikeParenLayout {
                owner_depth,
                child_head_depth,
                close_depth: child_head_depth,
            };
        }

        if owner_line_info.ends_with_join_or_apply {
            let child_head_depth = owner_depth.saturating_add(1);
            return QueryLikeParenLayout {
                owner_depth,
                child_head_depth,
                close_depth: owner_depth,
            };
        }

        let child_head_depth = owner_line_info
            .owner_kind
            .map(|kind| kind.formatter_child_query_head_depth(owner_depth, current_query_base))
            .unwrap_or_else(|| owner_depth.saturating_add(1));

        QueryLikeParenLayout {
            owner_depth,
            child_head_depth,
            close_depth: child_head_depth.saturating_sub(1),
        }
    }

    #[cfg(test)]
    fn is_log_errors_into_clause(
        tokens: &[SqlToken],
        into_idx: usize,
        statement_word_links: Option<&StatementWordLinks>,
    ) -> bool {
        let prev_word_idx = statement_word_links
            .and_then(|links| links.prev_word_index(into_idx))
            .or_else(|| Self::previous_word_upper(tokens, into_idx).map(|(_, idx)| idx));
        let Some(prev_idx) = prev_word_idx else {
            return false;
        };
        let Some(SqlToken::Word(prev_word)) = tokens.get(prev_idx) else {
            return false;
        };
        if !prev_word.eq_ignore_ascii_case("ERRORS") {
            return false;
        }

        let second_prev_word_idx = statement_word_links
            .and_then(|links| links.prev_word_index(prev_idx))
            .or_else(|| Self::previous_word_upper(tokens, prev_idx).map(|(_, idx)| idx));
        second_prev_word_idx
            .and_then(|idx| tokens.get(idx))
            .is_some_and(
                |token| matches!(token, SqlToken::Word(word) if word.eq_ignore_ascii_case("LOG")),
            )
    }

    #[cfg(test)]
    fn is_multiset_set_operator(
        tokens: &[SqlToken],
        idx: usize,
        prev_word_upper: Option<&str>,
        statement_word_links: Option<&StatementWordLinks>,
    ) -> bool {
        if matches!(prev_word_upper, Some("MULTISET")) {
            return true;
        }

        statement_word_links
            .and_then(|links| links.prev_word_index(idx))
            .and_then(|prev_idx| tokens.get(prev_idx))
            .is_some_and(
                |token| matches!(token, SqlToken::Word(word) if word.eq_ignore_ascii_case("MULTISET")),
            )
    }

    fn is_log_errors_into_clause_from_indices(
        tokens: &[SqlToken],
        recent_statement_word_indices: &VecDeque<usize>,
    ) -> bool {
        let mut first_is_errors = false;
        let mut second_is_log = false;
        Self::scan_recent_statement_words_from_indices(
            tokens,
            recent_statement_word_indices,
            2,
            |word, pos| {
                match pos {
                    0 => first_is_errors = word.eq_ignore_ascii_case("ERRORS"),
                    1 => second_is_log = word.eq_ignore_ascii_case("LOG"),
                    _ => {}
                }
                true
            },
        );
        first_is_errors && second_is_log
    }

    fn is_multiset_set_operator_from_indices(
        tokens: &[SqlToken],
        prev_word_upper: Option<&str>,
        recent_statement_word_indices: &VecDeque<usize>,
    ) -> bool {
        if matches!(prev_word_upper, Some("MULTISET")) {
            return true;
        }

        recent_statement_word_indices
            .back()
            .and_then(|word_idx| tokens.get(*word_idx))
            .is_some_and(
                |token| matches!(token, SqlToken::Word(word) if word.eq_ignore_ascii_case("MULTISET")),
            )
    }

    fn is_inline_clause_phrase(
        tokens: &[SqlToken],
        keyword: &str,
        prev_word_upper: Option<&str>,
        current_scope: FormatScope,
        format_stack: &FormatFrameStack,
        recent_statement_word_indices: &VecDeque<usize>,
    ) -> bool {
        if format_stack.construct_flag_is_active(ConstructFlagKind::ForallPending)
            && keyword == "VALUES"
        {
            return true;
        }
        if keyword == "INTO"
            && Self::is_log_errors_into_clause_from_indices(tokens, recent_statement_word_indices)
        {
            return true;
        }
        if keyword == "LIMIT" && matches!(prev_word_upper, Some("REJECT")) {
            return true;
        }
        if crate::sql_text::is_format_set_operator_keyword(keyword)
            && Self::is_multiset_set_operator_from_indices(
                tokens,
                prev_word_upper,
                recent_statement_word_indices,
            )
        {
            return true;
        }
        if format_stack.construct_flag_is_active_at_paren_depth(
            ConstructFlagKind::ModelActive,
            current_scope.paren_depth,
        ) && keyword == "UPDATE"
            && matches!(prev_word_upper, Some("RULES"))
        {
            return true;
        }

        false
    }

    fn has_compact_non_query_frame_since_last_query(format_stack: &FormatFrameStack) -> bool {
        format_stack.has_compact_non_query_frame_since_last_query()
    }

    fn suppresses_condition_break(
        format_stack: &FormatFrameStack,
        keyword: &str,
        prev_word_upper: Option<&str>,
        trigger_header_active: bool,
    ) -> bool {
        if keyword == "OR" && matches!(prev_word_upper, Some("CREATE")) {
            return true;
        }
        if trigger_header_active && keyword == "OR" {
            return true;
        }
        if keyword == "ON"
            && format_stack.construct_flag_is_active(ConstructFlagKind::CreateIndexPending)
        {
            return true;
        }
        if keyword == "ON"
            && format_stack.construct_flag_is_active(ConstructFlagKind::CommentOnActive)
        {
            return true;
        }
        if keyword == "ON"
            && format_stack.construct_flag_is_active(ConstructFlagKind::ModelReferencePending)
        {
            return true;
        }
        if format_stack.construct_flag_is_active(ConstructFlagKind::GrantRevokeActive)
            && keyword == "ON"
        {
            return true;
        }
        if keyword == "ON"
            && (matches!(prev_word_upper, Some("REFERENCES"))
                || format_stack
                    .construct_flag_is_active(ConstructFlagKind::ReferentialActionPending))
        {
            return true;
        }
        false
    }

    fn suppresses_clause_break(
        format_stack: &FormatFrameStack,
        tokens: &[SqlToken],
        keyword: &str,
        prev_word_upper: Option<&str>,
        in_plsql_block: bool,
        suppress_comma_break_depth: usize,
        has_subquery_in_paren_stack: bool,
        inside_function_local_non_query_paren: bool,
        merge_when_branch_active: bool,
        current_scope: FormatScope,
        trigger_header_active: bool,
        is_analytic_within_group: bool,
        is_fetch_into_single_target: bool,
        in_analytic_over_paren: bool,
        in_keep_paren: bool,
        recent_statement_word_indices: &VecDeque<usize>,
    ) -> bool {
        if in_analytic_over_paren {
            return true;
        }
        if in_keep_paren && keyword == "ORDER" {
            return true;
        }
        if Self::is_inline_clause_phrase(
            tokens,
            keyword,
            prev_word_upper,
            current_scope,
            format_stack,
            recent_statement_word_indices,
        ) {
            return true;
        }
        if keyword == "INTO" && matches!(prev_word_upper, Some("INSERT" | "MERGE")) {
            return true;
        }
        if keyword == "FROM" && matches!(prev_word_upper, Some("DELETE")) {
            return true;
        }
        if keyword == "WITH" && matches!(prev_word_upper, Some("START" | "ROW" | "ROWS")) {
            return true;
        }
        if keyword == "ORDER"
            && (suppress_comma_break_depth > 0
                || matches!(prev_word_upper, Some("SEQUENTIAL" | "AUTOMATIC")))
        {
            return true;
        }
        if trigger_header_active && matches!(keyword, "INSERT" | "UPDATE" | "DELETE") {
            return true;
        }
        if keyword == "UPDATE" && matches!(prev_word_upper, Some("FOR")) && !in_plsql_block {
            return true;
        }
        if format_stack.construct_flag_is_active(ConstructFlagKind::ReturningActive)
            && keyword == "INTO"
        {
            return true;
        }
        if format_stack.construct_flag_is_active(ConstructFlagKind::FetchActive)
            && keyword == "INTO"
            && is_fetch_into_single_target
        {
            return true;
        }
        if format_stack.construct_flag_is_active(ConstructFlagKind::BulkCollectActive)
            && keyword == "INTO"
        {
            return true;
        }
        if format_stack.construct_flag_is_active(ConstructFlagKind::GrantRevokeActive)
            && matches!(
                keyword,
                "SELECT"
                    | "INSERT"
                    | "UPDATE"
                    | "DELETE"
                    | "EXECUTE"
                    | "ALTER"
                    | "DROP"
                    | "CREATE"
                    | "INDEX"
                    | "REFERENCES"
                    | "ALL"
                    | "PRIVILEGES"
                    | "MERGE"
                    | "ON"
            )
        {
            return true;
        }
        if format_stack.construct_flag_is_active(ConstructFlagKind::CreateSequenceActive)
            && matches!(keyword, "START" | "CONNECT")
        {
            return true;
        }
        if format_stack.construct_flag_is_active(ConstructFlagKind::FetchActive)
            && matches!(keyword, "LIMIT" | "BULK")
        {
            return true;
        }
        if format_stack.construct_flag_is_active(ConstructFlagKind::ExecuteImmediateActive)
            && matches!(keyword, "INTO" | "USING")
        {
            return true;
        }
        if merge_when_branch_active && matches!(keyword, "SET" | "VALUES" | "INTO") {
            return true;
        }
        if format_stack.construct_flag_is_active(ConstructFlagKind::ReferentialOnActive)
            && matches!(keyword, "DELETE" | "UPDATE" | "SET")
        {
            return true;
        }
        let frame_guarded_function_local_clause_starter =
            matches!(keyword, "FROM" | "WITH" | "SET" | "INSERT");
        let non_subquery_paren_clause_starter =
            keyword == "FROM" || sql_text::is_non_subquery_paren_suppressed_clause_start(keyword);
        if suppress_comma_break_depth > 0
            && ((inside_function_local_non_query_paren
                && frame_guarded_function_local_clause_starter)
                || (!has_subquery_in_paren_stack && non_subquery_paren_clause_starter))
        {
            return true;
        }
        if format_stack.construct_flag_is_active_at_paren_depth(
            ConstructFlagKind::SearchCycleClauseActive,
            current_scope.paren_depth,
        ) && keyword == "SET"
        {
            return true;
        }
        if is_analytic_within_group {
            return true;
        }
        if keyword == "SET" && matches!(prev_word_upper, Some("CHARACTER")) {
            return true;
        }
        if keyword == "SET"
            && Self::is_mysql_signal_set_clause_from_indices(tokens, recent_statement_word_indices)
        {
            return true;
        }
        if keyword == "UPDATE"
            && Self::is_mysql_on_duplicate_key_update_from_indices(
                tokens,
                recent_statement_word_indices,
            )
        {
            return true;
        }
        false
    }

    fn significant_tokens<'a>(
        tokens: &'a [SqlToken],
    ) -> impl DoubleEndedIterator<Item = &'a SqlToken> + 'a {
        tokens
            .iter()
            .filter(|token| !matches!(token, SqlToken::Comment(_)))
    }

    fn starts_with_end_suffix_terminator_tokens(tokens: &[SqlToken]) -> bool {
        let mut meaningful = Self::significant_tokens(tokens);
        matches!(
            (meaningful.next(), meaningful.next()),
            (Some(SqlToken::Word(first)), Some(SqlToken::Word(second)))
                if first.eq_ignore_ascii_case("END")
                    && crate::sql_text::is_format_plain_end_suffix_keyword(second)
        )
    }

    #[cfg_attr(not(test), allow(dead_code))]
    fn starts_with_end_suffix_terminator(line: &str) -> bool {
        let tokens = Self::tokenize_sql(line);
        Self::starts_with_end_suffix_terminator_tokens(&tokens)
    }

    #[allow(dead_code)]
    fn tokens_continue_plsql_condition_terminator(tokens: &[SqlToken], idx: usize) -> bool {
        for token in tokens.iter().skip(idx.saturating_add(1)) {
            match token {
                SqlToken::Comment(comment) if comment.contains('\n') => break,
                SqlToken::Comment(_) => {}
                SqlToken::Symbol(sym) if sym == ")" => {}
                SqlToken::Symbol(sym) if sym == ";" || sym == "," => break,
                SqlToken::Word(word)
                    if word.eq_ignore_ascii_case("THEN") || word.eq_ignore_ascii_case("LOOP") =>
                {
                    return true;
                }
                _ => break,
            }
        }

        false
    }

    fn starts_with_plain_end_tokens(tokens: &[SqlToken]) -> bool {
        let mut meaningful = Self::significant_tokens(tokens);
        matches!(meaningful.next(), Some(SqlToken::Word(first)) if first.eq_ignore_ascii_case("END"))
            && !matches!(
                meaningful.next(),
                Some(SqlToken::Word(second))
                    if crate::sql_text::is_format_plain_end_suffix_keyword(second)
            )
    }

    #[cfg_attr(not(test), allow(dead_code))]
    fn starts_with_plain_end(line: &str) -> bool {
        let tokens = Self::tokenize_sql(line);
        Self::starts_with_plain_end_tokens(&tokens)
    }

    fn starts_with_bare_end_tokens(tokens: &[SqlToken]) -> bool {
        let mut meaningful = Self::significant_tokens(tokens);
        match meaningful.next() {
            Some(SqlToken::Word(first)) if first.eq_ignore_ascii_case("END") => {}
            _ => return false,
        }

        match meaningful.next() {
            None => true,
            Some(SqlToken::Symbol(symbol)) if symbol == ";" => meaningful.next().is_none(),
            Some(SqlToken::Word(word))
                if crate::sql_text::is_format_plain_end_suffix_keyword(word) =>
            {
                false
            }
            _ => false,
        }
    }

    #[cfg(test)]
    fn starts_with_mysql_custom_delimited_end(line: &str) -> bool {
        let trimmed = line.trim_start();
        let Some(prefix) = trimmed.get(..3) else {
            return false;
        };
        if !prefix.eq_ignore_ascii_case("END") {
            return false;
        }

        let mut delimiter_end = None;
        for (idx, ch) in trimmed[3..].trim_start().char_indices() {
            if matches!(ch, '$' | '/') {
                delimiter_end = Some(idx + ch.len_utf8());
                continue;
            }
            break;
        }

        let Some(delimiter_end) = delimiter_end else {
            return false;
        };

        let rest = &trimmed[3..].trim_start()[delimiter_end..];
        let trailing = rest.trim_start();
        trailing.is_empty()
            || trailing.starts_with("--")
            || trailing.starts_with('#')
            || trailing.starts_with("/*")
    }

    #[cfg(test)]
    fn starts_with_block_terminating_end(line: &str, tokens: &[SqlToken]) -> bool {
        Self::starts_with_bare_end_tokens(tokens)
            || Self::starts_with_mysql_custom_delimited_end(line)
    }

    #[cfg_attr(not(test), allow(dead_code))]
    fn starts_with_bare_end(line: &str) -> bool {
        let tokens = Self::tokenize_sql(line);
        Self::starts_with_bare_end_tokens(&tokens)
    }

    fn line_ends_with_semicolon_before_inline_comment(line: &str) -> bool {
        crate::sql_text::line_ends_with_semicolon_before_inline_comment(line)
    }

    fn resolve_format_preferred_db_type(
        source: &str,
        preferred_db_type: Option<DatabaseType>,
    ) -> Option<DatabaseType> {
        preferred_db_type.or_else(|| {
            (!sql_text::sql_uses_mysql_compatible_syntax(source)).then_some(DatabaseType::Oracle)
        })
    }

    fn paren_opens_analytic_layout(
        current_clause: Option<&str>,
        prev_word_upper: Option<&str>,
    ) -> bool {
        matches!(prev_word_upper, Some("OVER"))
            || (matches!(current_clause, Some("WINDOW")) && matches!(prev_word_upper, Some("AS")))
    }

    #[cfg_attr(not(test), allow(dead_code))]
    pub(super) fn format_for_auto_formatting(source: &str, selected_only: bool) -> String {
        Self::format_for_auto_formatting_with_db_type(source, selected_only, None)
    }

    pub(super) fn format_for_auto_formatting_with_db_type(
        source: &str,
        selected_only: bool,
        preferred_db_type: Option<DatabaseType>,
    ) -> String {
        let preferred_db_type = Self::resolve_format_preferred_db_type(source, preferred_db_type);
        let items = Self::normalize_format_items(
            super::query_text::split_format_items_for_db_type(source, preferred_db_type),
        );
        let preserve_missing_selection_terminator =
            selected_only && !Self::format_items_have_statement(&items);
        let append_missing_terminator = !preserve_missing_selection_terminator;
        let formatted = Self::format_sql_basic_with_terminator_policy_from_items(
            &items,
            append_missing_terminator,
            preferred_db_type,
        );

        if preserve_missing_selection_terminator {
            Self::preserve_selected_text_terminator(source, formatted)
        } else {
            formatted
        }
    }

    fn format_items_have_statement(items: &[FormatItem]) -> bool {
        items.iter().any(Self::format_item_has_statement)
    }

    fn format_item_has_statement(item: &FormatItem) -> bool {
        let FormatItem::Statement(statement) = item else {
            return false;
        };

        let stripped = QueryExecutor::strip_comments(statement);
        let without_trailing_semicolons = stripped.trim_end().trim_end_matches(';').trim_end();
        !without_trailing_semicolons.is_empty()
    }

    pub(super) fn normalize_index(text: &str, index: i32) -> usize {
        if index <= 0 {
            0
        } else {
            Self::clamp_to_char_boundary(text, index as usize)
        }
    }

    pub(super) fn clamp_to_char_boundary(text: &str, index: usize) -> usize {
        let mut idx = index.min(text.len());
        if text.is_char_boundary(idx) {
            return idx;
        }

        // Clamp invalid UTF-8 byte offsets to the previous valid boundary.
        while idx > 0 && !text.is_char_boundary(idx) {
            idx -= 1;
        }
        idx
    }

    #[cfg_attr(not(test), allow(dead_code))]
    pub(super) fn map_cursor_after_format(source: &str, formatted: &str, original_pos: i32) -> i32 {
        Self::map_cursor_after_format_with_policy(source, formatted, original_pos, false)
    }

    #[cfg_attr(not(test), allow(dead_code))]
    pub(super) fn map_cursor_after_format_with_policy(
        source: &str,
        formatted: &str,
        original_pos: i32,
        selected_only: bool,
    ) -> i32 {
        Self::map_cursor_after_format_with_policy_for_db_type(
            source,
            formatted,
            original_pos,
            selected_only,
            None,
        )
    }

    pub(super) fn map_cursor_after_format_with_policy_for_db_type(
        source: &str,
        formatted: &str,
        original_pos: i32,
        _selected_only: bool,
        preferred_db_type: Option<DatabaseType>,
    ) -> i32 {
        if original_pos <= 0 {
            return 0;
        }

        let source_pos = Self::clamp_to_char_boundary(source, original_pos as usize);
        if Self::should_use_fast_cursor_mapping(source) {
            if source.is_empty() || formatted.is_empty() {
                return 0;
            }
            let scaled_pos = (source_pos as u128)
                .saturating_mul(formatted.len() as u128)
                .checked_div(source.len() as u128)
                .unwrap_or(0);
            return Self::clamp_to_char_boundary(formatted, scaled_pos as usize) as i32;
        }

        let mut formatted_pos = Self::map_cursor_after_format_linear_for_db_type(
            source,
            source_pos,
            formatted,
            preferred_db_type,
        )
        .unwrap_or_else(|| {
            (source_pos as u128)
                .saturating_mul(formatted.len() as u128)
                .checked_div(source.len().max(1) as u128)
                .unwrap_or(0) as usize
        });
        formatted_pos = Self::clamp_to_char_boundary(formatted, formatted_pos.min(formatted.len()));
        formatted_pos = Self::advance_over_inserted_layout_whitespace(
            source,
            source_pos,
            formatted,
            formatted_pos,
        );
        formatted_pos as i32
    }

    fn should_use_fast_cursor_mapping(source: &str) -> bool {
        source.len() >= CURSOR_MAPPING_FULL_REFORMAT_THRESHOLD_BYTES
            || Self::exceeds_large_document_threshold(source)
    }

    fn exceeds_large_document_threshold(text: &str) -> bool {
        if text.len() >= LARGE_DOCUMENT_FORMAT_THRESHOLD_BYTES {
            return true;
        }

        Self::has_more_than_n_lines(text, LARGE_DOCUMENT_FORMAT_THRESHOLD_LINES)
    }

    fn has_more_than_n_lines(text: &str, max_lines: usize) -> bool {
        if text.is_empty() {
            return false;
        }

        let mut lines = 1usize;
        for byte in text.bytes() {
            if byte == b'\n' {
                lines = lines.saturating_add(1);
                if lines > max_lines {
                    return true;
                }
            }
        }

        false
    }

    fn map_cursor_after_format_linear_for_db_type(
        source: &str,
        source_pos: usize,
        formatted: &str,
        preferred_db_type: Option<DatabaseType>,
    ) -> Option<usize> {
        if formatted.is_empty() {
            return Some(0);
        }

        let mysql_compatible = sql_text::mysql_compatibility_for_sql(source, preferred_db_type);
        let source_spans =
            super::query_text::tokenize_sql_spanned_with_mysql_compat(source, mysql_compatible);
        let formatted_spans =
            super::query_text::tokenize_sql_spanned_with_mysql_compat(formatted, mysql_compatible);
        if source_spans.is_empty() || formatted_spans.is_empty() {
            return Some(source_pos.min(formatted.len()));
        }

        let next_source_idx = source_spans.partition_point(|span| span.end <= source_pos);
        let previous_source_idx = next_source_idx.checked_sub(1);
        let anchor_source_idx = if source_spans
            .get(next_source_idx)
            .is_some_and(|span| source_pos >= span.start)
        {
            Some(next_source_idx)
        } else {
            None
        };
        let max_source_idx = anchor_source_idx.or(previous_source_idx).or_else(|| {
            (!source_spans.is_empty()).then_some(source_spans.len().saturating_sub(1))
        })?;

        let token_alignment = Self::build_cursor_mapping_token_alignment(
            &source_spans,
            &formatted_spans,
            max_source_idx,
        );

        let formatted_pos = if let Some(source_idx) = anchor_source_idx {
            let source_span = source_spans.get(source_idx)?;
            let formatted_idx = token_alignment.get(source_idx).copied().flatten()?;
            let formatted_span = formatted_spans.get(formatted_idx)?;
            let token_offset = source_pos.saturating_sub(source_span.start);
            formatted_span.start.saturating_add(
                token_offset.min(formatted_span.end.saturating_sub(formatted_span.start)),
            )
        } else if let Some(source_idx) = (next_source_idx < source_spans.len())
            .then_some(next_source_idx)
            .filter(|_| {
                source
                    .as_bytes()
                    .get(source_pos)
                    .is_none_or(|byte| !byte.is_ascii_whitespace())
            })
        {
            token_alignment
                .get(source_idx)
                .copied()
                .flatten()
                .and_then(|formatted_idx| formatted_spans.get(formatted_idx))
                .map(|span| span.start)
                .or_else(|| {
                    previous_source_idx
                        .and_then(|prev_idx| token_alignment.get(prev_idx).copied().flatten())
                        .and_then(|formatted_idx| formatted_spans.get(formatted_idx))
                        .map(|span| span.end)
                })?
        } else if let Some(previous_idx) = previous_source_idx {
            token_alignment
                .get(previous_idx)
                .copied()
                .flatten()
                .and_then(|formatted_idx| formatted_spans.get(formatted_idx))
                .map(|span| span.end)
                .unwrap_or(0)
        } else {
            token_alignment
                .first()
                .copied()
                .flatten()
                .and_then(|formatted_idx| formatted_spans.get(formatted_idx))
                .map(|span| span.start)
                .unwrap_or(0)
        };

        Some(formatted_pos.min(formatted.len()))
    }

    fn build_cursor_mapping_token_alignment(
        source_spans: &[SqlTokenSpan],
        formatted_spans: &[SqlTokenSpan],
        max_source_idx: usize,
    ) -> Vec<Option<usize>> {
        let mut alignment = vec![None; source_spans.len()];
        let mut formatted_idx = 0usize;
        let last_source_idx = max_source_idx.min(source_spans.len().saturating_sub(1));

        for (source_span, alignment_slot) in source_spans
            .iter()
            .zip(alignment.iter_mut())
            .take(last_source_idx.saturating_add(1))
        {
            while let Some(formatted_span) = formatted_spans.get(formatted_idx) {
                if Self::cursor_mapping_tokens_equivalent(&source_span.token, &formatted_span.token)
                {
                    *alignment_slot = Some(formatted_idx);
                    formatted_idx = formatted_idx.saturating_add(1);
                    break;
                }

                if Self::cursor_mapping_can_skip_formatted_token(&formatted_span.token) {
                    formatted_idx = formatted_idx.saturating_add(1);
                    continue;
                }

                formatted_idx = formatted_idx.saturating_add(1);
            }
        }

        alignment
    }

    fn cursor_mapping_tokens_equivalent(source: &SqlToken, formatted: &SqlToken) -> bool {
        match (source, formatted) {
            (SqlToken::Word(left), SqlToken::Word(right)) => left.eq_ignore_ascii_case(right),
            (SqlToken::String(left), SqlToken::String(right))
            | (SqlToken::Comment(left), SqlToken::Comment(right))
            | (SqlToken::Symbol(left), SqlToken::Symbol(right)) => left == right,
            _ => false,
        }
    }

    fn cursor_mapping_can_skip_formatted_token(token: &SqlToken) -> bool {
        matches!(token, SqlToken::Symbol(symbol) if symbol == ";")
    }

    pub(super) fn advance_over_inserted_layout_whitespace(
        source: &str,
        source_pos: usize,
        formatted: &str,
        formatted_pos: usize,
    ) -> usize {
        let source_byte = match source.as_bytes().get(source_pos) {
            Some(byte) => *byte,
            None => return formatted_pos,
        };

        if source_byte.is_ascii_whitespace() {
            return formatted_pos;
        }

        let mut cursor = formatted_pos;
        while let Some(byte) = formatted.as_bytes().get(cursor) {
            if !byte.is_ascii_whitespace() {
                break;
            }
            cursor += 1;
        }

        Self::clamp_to_char_boundary(formatted, cursor)
    }

    pub(super) fn preserve_selected_text_terminator(source: &str, formatted: String) -> String {
        if Self::source_has_explicit_semicolon_terminator(source) {
            return formatted;
        }

        if let Some(without_semicolon) = Self::remove_trailing_statement_semicolon(&formatted) {
            return without_semicolon;
        }

        formatted
    }

    fn removable_trailing_semicolon_span(formatted: &str) -> Option<(usize, usize)> {
        let mut trimmed_len = formatted.trim_end().len();
        while trimmed_len > 0 {
            let prefix = &formatted[..trimmed_len];
            let line_start = prefix.rfind('\n').map_or(0, |idx| idx + 1);
            let line = &prefix[line_start..trimmed_len];
            let line_trimmed = line.trim_start();

            let is_trailing_comment_line = line_trimmed.starts_with("--")
                || Self::is_sqlplus_remark_comment_statement(line_trimmed);

            if !is_trailing_comment_line {
                break;
            }

            trimmed_len = prefix[..line_start].trim_end().len();
        }

        if trimmed_len == 0 {
            return None;
        }
        let trimmed = &formatted[..trimmed_len];
        if let Some(last_line_start) = trimmed.rfind('\n').map(|idx| idx + 1).or(Some(0)) {
            let last_line = &trimmed[last_line_start..];
            let leading_ws = last_line.len().saturating_sub(last_line.trim_start().len());
            let last_line_trimmed = &last_line[leading_ws..];
            if let Some(rest) = last_line_trimmed.strip_prefix(';') {
                let rest_trimmed = rest.trim_start();
                if Self::is_sqlplus_remark_comment_statement(rest_trimmed) {
                    let semicolon_start = last_line_start + leading_ws;
                    return Some((semicolon_start, semicolon_start + 1));
                }
            }
        }

        let spans = super::query_text::tokenize_sql_spanned(trimmed);

        let mut semicolon_span: Option<(usize, usize)> = None;

        for span in spans.iter().rev() {
            match &span.token {
                SqlToken::Comment(comment_text) if comment_text.starts_with("--") => continue,
                SqlToken::Comment(_) => continue,
                SqlToken::Symbol(sym) if sym == "/" => continue,
                SqlToken::Symbol(sym) if sym == ";" => {
                    if Self::semicolon_belongs_to_non_sql_line(trimmed, span.start) {
                        continue;
                    }
                    semicolon_span = Some((span.start, span.end));
                    break;
                }
                _ => break,
            }
        }

        semicolon_span
    }

    fn remove_trailing_statement_semicolon(formatted: &str) -> Option<String> {
        let (semicolon_start, semicolon_end) = Self::removable_trailing_semicolon_span(formatted)?;

        let mut out = String::with_capacity(
            formatted
                .len()
                .saturating_sub(semicolon_end.saturating_sub(semicolon_start)),
        );
        out.push_str(&formatted[..semicolon_start]);
        out.push_str(&formatted[semicolon_end..]);
        Some(out)
    }

    fn append_statement_terminator(
        formatted_statement: &mut String,
        terminator: &str,
        mysql_compatible: bool,
    ) {
        let trim_len = formatted_statement.trim_end().len();
        if trim_len == 0 || terminator.is_empty() {
            return;
        }

        let trimmed = &formatted_statement[..trim_len];
        let mut insert_at = trim_len;
        let mut has_trailing_comment = false;

        let mut trailing_comment_start = trim_len;
        loop {
            let prefix = &trimmed[..trailing_comment_start];
            let line_start = prefix.rfind('\n').map_or(0, |idx| idx + 1);
            let line = &prefix[line_start..trailing_comment_start];
            let line_trimmed = line.trim_start();
            if Self::is_sqlplus_remark_comment_statement(line_trimmed) {
                has_trailing_comment = true;
                insert_at = line_start;
                trailing_comment_start = prefix[..line_start].trim_end().len();
                if trailing_comment_start == 0 {
                    break;
                }
                continue;
            }
            break;
        }

        let spans = if mysql_compatible {
            super::query_text::tokenize_sql_spanned_with_mysql_compat(trimmed, true)
        } else {
            super::query_text::tokenize_sql_spanned(trimmed)
        };
        for span in spans.iter().rev() {
            match &span.token {
                SqlToken::Comment(comment) if Self::is_mysql_executable_comment_token(comment) => {
                    break;
                }
                SqlToken::Comment(_) => {
                    has_trailing_comment = true;
                    insert_at = insert_at.min(span.start);
                }
                SqlToken::Symbol(sym) if sym == "/" => continue,
                _ => break,
            }
        }

        if has_trailing_comment {
            while insert_at > 0 {
                match trimmed.as_bytes().get(insert_at - 1) {
                    Some(b' ' | b'\t' | b'\n' | b'\r') => insert_at -= 1,
                    _ => break,
                }
            }
            let suffix = &formatted_statement[insert_at..trim_len];
            let separator = match suffix.as_bytes().first() {
                Some(b' ' | b'\t' | b'\n' | b'\r') => terminator.to_string(),
                _ => format!("{terminator} "),
            };
            formatted_statement.insert_str(insert_at, &separator);
        } else {
            formatted_statement.insert_str(insert_at, terminator);
        }
    }

    fn normalize_mysql_trigger_terminator_indent(
        formatted_statement: &mut String,
        terminator: &str,
    ) {
        let trim_len = formatted_statement.trim_end().len();
        if trim_len == 0 {
            return;
        }

        let trimmed = &formatted_statement[..trim_len];
        let Some(line_start) = trimmed.rfind('\n').map(|idx| idx + 1).or(Some(0)) else {
            return;
        };
        let line = &trimmed[line_start..];
        let line_trimmed = line.trim_start();
        let Some(prefix) = line_trimmed.get(..3) else {
            return;
        };
        if !prefix.eq_ignore_ascii_case("END") {
            return;
        }
        let rest = &line_trimmed[3..];
        if !rest.starts_with(terminator) {
            return;
        }

        let mut normalized = String::with_capacity(formatted_statement.len());
        normalized.push_str(&trimmed[..line_start]);
        normalized.push_str(line_trimmed);
        normalized.push_str(&formatted_statement[trim_len..]);
        *formatted_statement = normalized;
    }

    fn append_missing_statement_terminator(formatted_statement: &mut String) {
        Self::append_statement_terminator(formatted_statement, ";", false);
    }

    fn has_inline_statement_separator_candidate(formatted: &str) -> bool {
        let mut saw_semicolon_on_line = false;

        for byte in formatted.bytes() {
            match byte {
                b';' => saw_semicolon_on_line = true,
                b'\n' | b'\r' => saw_semicolon_on_line = false,
                b' ' | b'\t' if saw_semicolon_on_line => {}
                _ if saw_semicolon_on_line => return true,
                _ => {}
            }
        }

        false
    }

    fn collect_inline_statement_split_points(formatted: &str) -> Vec<usize> {
        let spans = super::query_text::tokenize_sql_spanned_with_mysql_compat(
            formatted,
            formatted.as_bytes().contains(&b'#'),
        );
        let mut split_points: Vec<usize> = Vec::new();
        let bytes = formatted.as_bytes();
        let mut previous_end = 0usize;
        let mut pending_split_after_semicolon: Option<usize> = None;
        let line_is_non_sql = |line_start: usize| {
            let line_start = line_start.min(formatted.len());
            let line_end = formatted[line_start..]
                .find('\n')
                .map_or(formatted.len(), |offset| line_start + offset);
            let current_line = formatted.get(line_start..line_end).unwrap_or("");
            let trimmed = current_line.trim();
            trimmed.starts_with("--")
                || trimmed.starts_with('#')
                || Self::is_sqlplus_remark_comment_statement(trimmed)
                || QueryExecutor::parse_tool_command_if_candidate(trimmed)
                    .is_some_and(|command| !matches!(command, ToolCommand::Unsupported { .. }))
        };
        let mut current_line_is_non_sql = line_is_non_sql(0);

        for span in &spans {
            let Some(gap) = formatted.get(previous_end..span.start) else {
                previous_end = span.end;
                continue;
            };
            if let Some(last_newline_offset) = gap.rfind('\n') {
                pending_split_after_semicolon = None;
                current_line_is_non_sql = line_is_non_sql(previous_end + last_newline_offset + 1);
            }

            if let Some(semicolon_end) = pending_split_after_semicolon {
                match &span.token {
                    SqlToken::Comment(comment) if comment.contains('\n') => {
                        pending_split_after_semicolon = None;
                    }
                    SqlToken::Comment(_) => {}
                    _ => {
                        let mut split_at = span.start;
                        while split_at > semicolon_end {
                            match bytes.get(split_at.saturating_sub(1)) {
                                Some(b' ' | b'\t') => split_at = split_at.saturating_sub(1),
                                _ => break,
                            }
                        }
                        split_points.push(split_at.max(semicolon_end));
                        pending_split_after_semicolon = None;
                    }
                }
            }

            if matches!(&span.token, SqlToken::Symbol(sym) if sym == ";")
                && !current_line_is_non_sql
            {
                pending_split_after_semicolon = Some(span.end);
            }

            let Some(token_text) = formatted.get(span.start..span.end) else {
                previous_end = span.end;
                continue;
            };
            if let Some(last_newline_offset) = token_text.rfind('\n') {
                pending_split_after_semicolon = None;
                current_line_is_non_sql = line_is_non_sql(span.start + last_newline_offset + 1);
            }

            previous_end = span.end;
        }

        split_points
    }

    fn split_inline_statement_separators(mut formatted: String) -> String {
        formatted.truncate(formatted.trim_end().len());
        if formatted.is_empty() || !Self::has_inline_statement_separator_candidate(&formatted) {
            return formatted;
        }

        let split_points = Self::collect_inline_statement_split_points(&formatted);
        if split_points.is_empty() {
            return formatted;
        }

        let mut out = String::with_capacity(formatted.len().saturating_add(split_points.len()));
        let mut cursor = 0usize;

        for split_at in split_points {
            if split_at <= cursor || split_at > formatted.len() {
                continue;
            }

            out.push_str(&formatted[cursor..split_at]);
            if !out.ends_with('\n') {
                out.push('\n');
            }
            cursor = split_at;
        }

        out.push_str(&formatted[cursor..]);

        out
    }

    #[cfg(test)]
    fn split_inline_statement_separators_for_test(formatted: &str) -> String {
        Self::split_inline_statement_separators(formatted.to_string())
    }

    #[cfg_attr(not(test), allow(dead_code))]
    pub(crate) fn format_sql_basic(sql: &str) -> String {
        Self::format_sql_basic_with_terminator_policy_for_db_type(sql, true, None)
    }

    #[cfg_attr(not(test), allow(dead_code))]
    pub(crate) fn format_sql_basic_no_cache(sql: &str) -> String {
        Self::format_sql_basic_with_terminator_policy_for_db_type(sql, true, None)
    }

    #[cfg_attr(not(test), allow(dead_code))]
    pub(crate) fn format_sql_basic_no_cache_for_db_type(
        sql: &str,
        db_type: DatabaseType,
    ) -> String {
        Self::format_sql_basic_with_terminator_policy_for_db_type(sql, true, Some(db_type))
    }

    #[cfg_attr(not(test), allow(dead_code))]
    pub(crate) fn format_sql_basic_for_db_type(sql: &str, db_type: DatabaseType) -> String {
        Self::format_sql_basic_with_terminator_policy_for_db_type(sql, true, Some(db_type))
    }

    fn normalize_format_items(items: Vec<FormatItem>) -> Vec<FormatItem> {
        let mut normalized: Vec<FormatItem> = Vec::with_capacity(items.len());
        let mut index = 0usize;

        while index < items.len() {
            if let Some((combined, consumed)) =
                Self::merge_fragmented_unterminated_plsql_label(&items, index)
            {
                normalized.push(FormatItem::Statement(combined));
                index += consumed;
                continue;
            }

            if let Some((combined, consumed)) =
                Self::merge_fragmented_with_single_letter_cte(&items, index)
            {
                normalized.push(FormatItem::Statement(combined));
                index += consumed;
                continue;
            }

            let item = items[index].clone();
            let should_merge = match (normalized.last(), &item) {
                (Some(FormatItem::Statement(previous)), FormatItem::Statement(fragment)) => {
                    Self::should_merge_order_modifier_fragment(previous, fragment)
                }
                _ => false,
            };

            if should_merge {
                if let Some(FormatItem::Statement(previous)) = normalized.last_mut() {
                    Self::append_statement_fragment(previous, &item);
                    index += 1;
                    continue;
                }
            }

            normalized.push(item);
            index += 1;
        }

        normalized
    }

    fn merge_fragmented_unterminated_plsql_label(
        items: &[FormatItem],
        start_index: usize,
    ) -> Option<(String, usize)> {
        let head = match items.get(start_index) {
            Some(FormatItem::Statement(statement)) => statement,
            _ => return None,
        };
        let trailing = match items.get(start_index + 1) {
            Some(FormatItem::Statement(statement)) => statement.trim_start(),
            _ => return None,
        };
        if trailing.is_empty() || !Self::statement_has_unterminated_plsql_label(head) {
            return None;
        }

        Some((format!("{}\n{trailing}", head.trim_end()), 2))
    }

    fn statement_has_unterminated_plsql_label(statement: &str) -> bool {
        if !statement.contains("<<") && !statement.contains(">>") {
            return false;
        }

        let tokens = Self::tokenize_sql(statement);
        let mut idx = 0usize;
        let mut open_label_count = 0usize;

        while idx < tokens.len() {
            match tokens.get(idx) {
                Some(SqlToken::Word(word)) => {
                    let opens_label = word.starts_with("<<");
                    let closes_label = word.ends_with(">>");
                    if opens_label {
                        if closes_label && word.len() > 4 {
                            // Fully closed `<<label>>` token.
                        } else {
                            open_label_count = open_label_count.saturating_add(1);
                        }
                    } else if closes_label {
                        open_label_count = open_label_count.saturating_sub(1);
                    }
                }
                Some(SqlToken::Symbol(sym)) if sym == "<<" => {
                    open_label_count = open_label_count.saturating_add(1);
                }
                Some(SqlToken::Symbol(sym)) if sym == ">>" => {
                    open_label_count = open_label_count.saturating_sub(1);
                }
                Some(SqlToken::Symbol(sym)) if sym == "<" => {
                    if matches!(tokens.get(idx + 1), Some(SqlToken::Symbol(next)) if next == "<") {
                        open_label_count = open_label_count.saturating_add(1);
                        idx = idx.saturating_add(1);
                    }
                }
                Some(SqlToken::Symbol(sym)) if sym == ">" => {
                    if matches!(tokens.get(idx + 1), Some(SqlToken::Symbol(next)) if next == ">") {
                        open_label_count = open_label_count.saturating_sub(1);
                        idx = idx.saturating_add(1);
                    }
                }
                _ => {}
            }

            idx = idx.saturating_add(1);
        }

        open_label_count > 0
    }

    fn merge_fragmented_with_single_letter_cte(
        items: &[FormatItem],
        start_index: usize,
    ) -> Option<(String, usize)> {
        let with_head = match items.get(start_index) {
            Some(FormatItem::Statement(statement))
                if statement.trim().eq_ignore_ascii_case("WITH") =>
            {
                statement.trim()
            }
            _ => return None,
        };

        let cte_name = match items.get(start_index + 1) {
            Some(FormatItem::ToolCommand(ToolCommand::Unsupported { raw, .. })) => raw.trim(),
            _ => return None,
        };
        if !Self::is_single_letter_cte_identifier(cte_name) {
            return None;
        }

        let trailing = match items.get(start_index + 2) {
            Some(FormatItem::Statement(statement)) => statement.trim_start(),
            _ => return None,
        };
        if !Self::starts_with_ascii_keyword_token_ci(trailing, "AS") {
            return None;
        }

        Some((format!("{with_head} {cte_name}\n{trailing}"), 3))
    }

    fn is_single_letter_cte_identifier(value: &str) -> bool {
        let mut chars = value.chars();
        let Some(ch) = chars.next() else {
            return false;
        };
        chars.next().is_none() && (ch.is_ascii_alphabetic() || ch == '_')
    }

    fn should_merge_order_modifier_fragment(previous: &str, fragment: &str) -> bool {
        let previous_trimmed = previous.trim_end();
        if previous_trimmed.is_empty()
            || Self::line_ends_with_semicolon_before_inline_comment(previous_trimmed)
        {
            return false;
        }

        let fragment_trimmed = fragment.trim_start();
        if fragment_trimmed.is_empty() {
            return false;
        }

        let starts_order_modifier =
            Self::starts_with_ascii_keyword_token_ci(fragment_trimmed, "ASC")
                || Self::starts_with_ascii_keyword_token_ci(fragment_trimmed, "DESC")
                || Self::starts_with_ascii_keyword_token_ci(fragment_trimmed, "NULLS");
        if !starts_order_modifier {
            return false;
        }

        let previous_search_window =
            Self::tail_search_window(previous_trimmed, 4096).unwrap_or(previous_trimmed);
        Self::contains_ascii_case_insensitive(previous_search_window, "ORDER BY")
            || Self::contains_ascii_case_insensitive(previous_search_window, "ORDER SIBLINGS BY")
    }

    fn starts_with_ascii_keyword_token_ci(text: &str, keyword: &str) -> bool {
        let text_bytes = text.as_bytes();
        let keyword_bytes = keyword.as_bytes();
        if text_bytes.len() < keyword_bytes.len() {
            return false;
        }
        if !text_bytes[..keyword_bytes.len()].eq_ignore_ascii_case(keyword_bytes) {
            return false;
        }

        !text_bytes
            .get(keyword_bytes.len())
            .is_some_and(|next| next.is_ascii_alphanumeric() || *next == b'_')
    }

    fn contains_ascii_case_insensitive(haystack: &str, needle: &str) -> bool {
        let haystack_bytes = haystack.as_bytes();
        let needle_bytes = needle.as_bytes();
        if needle_bytes.is_empty() {
            return true;
        }
        if haystack_bytes.len() < needle_bytes.len() {
            return false;
        }

        haystack_bytes
            .windows(needle_bytes.len())
            .rev()
            .any(|window| window.eq_ignore_ascii_case(needle_bytes))
    }

    fn tail_search_window(text: &str, max_bytes: usize) -> Option<&str> {
        if text.len() <= max_bytes {
            return None;
        }

        let mut start = text.len().saturating_sub(max_bytes);
        while start < text.len() && !text.is_char_boundary(start) {
            start = start.saturating_add(1);
        }
        text.get(start..)
    }

    fn append_statement_fragment(previous: &mut String, item: &FormatItem) {
        let FormatItem::Statement(fragment) = item else {
            return;
        };

        let fragment_trimmed = fragment.trim_start();
        if fragment_trimmed.is_empty() {
            return;
        }

        let needs_space = previous
            .chars()
            .last()
            .is_some_and(|ch| !ch.is_whitespace());
        if needs_space {
            previous.push(' ');
        }
        previous.push_str(fragment_trimmed);
    }

    fn format_sql_basic_with_terminator_policy_for_db_type(
        sql: &str,
        append_missing_terminator: bool,
        preferred_db_type: Option<DatabaseType>,
    ) -> String {
        let preferred_db_type = Self::resolve_format_preferred_db_type(sql, preferred_db_type);
        let normalized_items = Self::normalize_format_items(
            super::query_text::split_format_items_for_db_type(sql, preferred_db_type),
        );
        Self::format_sql_basic_with_terminator_policy_from_items(
            &normalized_items,
            append_missing_terminator,
            preferred_db_type,
        )
    }

    fn format_sql_basic_with_terminator_policy_from_items(
        items: &[FormatItem],
        append_missing_terminator: bool,
        preferred_db_type: Option<DatabaseType>,
    ) -> String {
        Self::format_sql_basic_core_from_normalized_items(
            items,
            append_missing_terminator,
            preferred_db_type,
        )
    }

    fn format_sql_basic_core_from_normalized_items(
        items: &[FormatItem],
        append_missing_terminator: bool,
        preferred_db_type: Option<DatabaseType>,
    ) -> String {
        let estimated_input_len: usize = items
            .iter()
            .map(|item| match item {
                FormatItem::Statement(statement) | FormatItem::Verbatim(statement) => {
                    statement.len()
                }
                FormatItem::ToolCommand(_) => 16,
                FormatItem::Slash => 1,
            })
            .sum();
        let mut formatted = String::with_capacity(estimated_input_len.saturating_add(64));
        if items.is_empty() {
            return String::new();
        }

        let mut select_list_break_state = SelectListBreakState::None;
        let mut active_mysql_delimiter: Option<String> = None;
        let mut current_item_analysis = items
            .first()
            .map(FormatItemStaticAnalysis::analyze)
            .unwrap_or_default();
        for (idx, item) in items.iter().enumerate() {
            let next_item = items.get(idx + 1);
            let next_item_analysis = next_item
                .map(FormatItemStaticAnalysis::analyze)
                .unwrap_or_default();

            match item {
                FormatItem::Statement(statement) => {
                    let mysql_compatible_statement = active_mysql_delimiter.is_some()
                        || sql_text::mysql_compatibility_for_sql(statement, preferred_db_type);
                    let mysql_profile =
                        mysql_compatible_statement && Self::mysql_formatter_profile_enabled();
                    let statement_input =
                        FormatterStatementInput::build(statement, mysql_compatible_statement);
                    let formatted_result = Self::profile_mysql_formatter_stage(
                        "format_statement",
                        statement.len(),
                        mysql_profile,
                        || Self::format_statement(&statement_input, select_list_break_state),
                    );
                    let FormatterStatementRenderResult {
                        formatted: mut formatted_statement,
                        has_code,
                        ends_with_semicolon,
                        should_append_missing_terminator,
                        is_create_trigger_statement,
                    } = formatted_result;
                    if let Some(delimiter) = active_mysql_delimiter.as_deref() {
                        Self::append_statement_terminator(
                            &mut formatted_statement,
                            delimiter,
                            true,
                        );
                        if is_create_trigger_statement {
                            Self::normalize_mysql_trigger_terminator_indent(
                                &mut formatted_statement,
                                delimiter,
                            );
                        }
                    } else if append_missing_terminator
                        && has_code
                        && !ends_with_semicolon
                        && should_append_missing_terminator
                    {
                        Self::append_missing_statement_terminator(&mut formatted_statement);
                    }
                    formatted.push_str(&formatted_statement);
                }
                FormatItem::ToolCommand(command) => {
                    if let ToolCommand::MysqlDelimiter { delimiter } = command {
                        active_mysql_delimiter = if delimiter == ";" {
                            None
                        } else {
                            Some(delimiter.clone())
                        };
                    }
                    formatted.push_str(&Self::format_tool_command(command));
                    select_list_break_state.clear();
                }
                FormatItem::Verbatim(text) => {
                    formatted.push_str(text);
                    select_list_break_state.clear();
                }
                FormatItem::Slash => {
                    formatted.push('/');
                    select_list_break_state.clear();
                }
            }

            if let Some(next_item) = next_item {
                formatted.push_str(Self::item_separator(
                    item,
                    next_item,
                    current_item_analysis,
                    next_item_analysis,
                ));
            }

            current_item_analysis = next_item_analysis;
        }

        formatted
    }

    fn item_separator(
        current: &FormatItem,
        next: &FormatItem,
        current_analysis: FormatItemStaticAnalysis,
        next_analysis: FormatItemStaticAnalysis,
    ) -> &'static str {
        if matches!(next, FormatItem::Slash)
            || Self::keeps_tight_spacing(current, next, current_analysis, next_analysis)
        {
            "\n"
        } else {
            "\n\n"
        }
    }

    fn keeps_tight_spacing(
        current: &FormatItem,
        next: &FormatItem,
        current_analysis: FormatItemStaticAnalysis,
        next_analysis: FormatItemStaticAnalysis,
    ) -> bool {
        match (current, next) {
            (FormatItem::Statement(_), FormatItem::Statement(_)) => {
                (current_analysis.is_sqlplus_comment_line && next_analysis.is_sqlplus_comment_line)
                    || (current_analysis.is_create_trigger_statement
                        && next_analysis.is_alter_trigger_statement)
            }
            (FormatItem::Slash, FormatItem::Statement(_)) => {
                next_analysis.is_alter_trigger_statement
            }
            _ if current_analysis.is_prompt && next_analysis.is_prompt => true,
            (
                FormatItem::ToolCommand(ToolCommand::ClearBreaks),
                FormatItem::ToolCommand(ToolCommand::ClearComputes),
            )
            | (
                FormatItem::ToolCommand(ToolCommand::ClearComputes),
                FormatItem::ToolCommand(ToolCommand::ClearBreaks),
            ) => true,
            _ => false,
        }
    }

    fn is_create_trigger_statement_tokens(tokens: &[SqlToken]) -> bool {
        let mut word_idx = 0usize;
        let mut has_trigger_in_prefix = false;

        for token in tokens {
            let SqlToken::Word(word) = token else {
                continue;
            };

            if word_idx == 0 && !word.eq_ignore_ascii_case("CREATE") {
                return false;
            }

            if word_idx < 8 && word.eq_ignore_ascii_case("TRIGGER") {
                has_trigger_in_prefix = true;
            }

            word_idx += 1;
        }

        word_idx > 0 && has_trigger_in_prefix
    }

    fn is_mysql_executable_comment_token(comment: &str) -> bool {
        crate::sql_text::is_mysql_executable_comment_start(comment.trim_start().as_bytes(), 0)
    }

    fn statement_has_code(statement: &str, tokens: &[SqlToken]) -> bool {
        let trimmed = statement.trim_start();
        if let Some(first_word) = trimmed.split_whitespace().next() {
            if first_word.eq_ignore_ascii_case("REM") || first_word.eq_ignore_ascii_case("REMARK") {
                return false;
            }
        }

        tokens.iter().any(|token| match token {
            SqlToken::Comment(comment) => Self::is_mysql_executable_comment_token(comment),
            _ => true,
        })
    }

    fn next_meaningful_token_index(tokens: &[SqlToken], start_idx: usize) -> Option<usize> {
        let mut idx = start_idx.saturating_add(1);
        while let Some(token) = tokens.get(idx) {
            if !matches!(token, SqlToken::Comment(_)) {
                return Some(idx);
            }
            idx = idx.saturating_add(1);
        }

        None
    }

    fn previous_meaningful_token_index(tokens: &[SqlToken], start_idx: usize) -> Option<usize> {
        let mut idx = start_idx;
        while idx > 0 {
            idx = idx.saturating_sub(1);
            if !matches!(tokens.get(idx), Some(SqlToken::Comment(_))) {
                return Some(idx);
            }
        }

        None
    }

    fn build_statement_metadata(tokens: &[SqlToken]) -> FormatterStatementMetadata {
        let mut metadata_builder = FormatterStatementMetadataBuilder::new(tokens.len(), true);
        for (idx, token) in tokens.iter().enumerate() {
            metadata_builder.observe(idx, token);
        }
        metadata_builder.finish()
    }

    fn build_statement_token_metadata(
        tokens: &[SqlToken],
    ) -> (
        MeaningfulTokenLinks,
        StatementWordLinks,
        Vec<Option<usize>>,
        Vec<Option<ParenBodyAnalysis>>,
        Vec<bool>,
        Vec<bool>,
    ) {
        let metadata = Self::build_statement_metadata(tokens);
        (
            metadata.meaningful_token_links,
            metadata.statement_word_links,
            metadata.matching_paren_close_indices,
            metadata.paren_body_analyses,
            metadata.fetch_into_has_multiple_targets,
            metadata.within_group_analytic_groups,
        )
    }

    fn build_meaningful_token_links(tokens: &[SqlToken]) -> MeaningfulTokenLinks {
        Self::build_statement_token_metadata(tokens).0
    }

    fn build_statement_word_links(tokens: &[SqlToken]) -> StatementWordLinks {
        Self::build_statement_token_metadata(tokens).1
    }

    fn build_matching_paren_close_indices_and_body_analysis(
        tokens: &[SqlToken],
    ) -> (Vec<Option<usize>>, Vec<Option<ParenBodyAnalysis>>) {
        let metadata = Self::build_statement_token_metadata(tokens);
        (metadata.2, metadata.3)
    }

    fn build_fetch_into_has_multiple_targets(tokens: &[SqlToken]) -> Vec<bool> {
        Self::build_statement_token_metadata(tokens).4
    }

    fn mysql_identifier_left_boundary_symbol(sym: &str) -> bool {
        matches!(
            sym,
            "(" | ","
                | "."
                | "="
                | "+"
                | "-"
                | "*"
                | "/"
                | "%"
                | "<"
                | ">"
                | "<="
                | ">="
                | "<>"
                | "!="
                | ":="
                | "=>"
        )
    }

    fn mysql_identifier_right_boundary_symbol(sym: &str) -> bool {
        matches!(
            sym,
            ")" | ","
                | "."
                | ";"
                | "="
                | "+"
                | "-"
                | "*"
                | "/"
                | "%"
                | "<"
                | ">"
                | "<="
                | ">="
                | "<>"
                | "!="
                | ":="
                | "=>"
        )
    }

    fn mysql_keyword_must_stay_keyword_in_expression(word: &str) -> bool {
        matches!(
            word,
            "AND"
                | "BETWEEN"
                | "CASE"
                | "DIV"
                | "ELSE"
                | "END"
                | "EXISTS"
                | "FALSE"
                | "IN"
                | "INTERVAL"
                | "IS"
                | "LIKE"
                | "LIKE2"
                | "LIKE4"
                | "LIKEC"
                | "MOD"
                | "NOT"
                | "NULL"
                | "OR"
                | "REGEXP"
                | "RLIKE"
                | "THEN"
                | "TRUE"
                | "WHEN"
        )
    }

    fn mysql_formatter_keyword(word: &str) -> bool {
        sql_text::is_oracle_sql_keyword(word) || sql_text::is_mysql_sql_keyword(word)
    }

    fn token_is_alias_keyword(token: Option<&SqlToken>) -> bool {
        matches!(
            token,
            Some(SqlToken::Word(word))
                if word.eq_ignore_ascii_case("AS") || word.eq_ignore_ascii_case("IS")
        )
    }

    fn next_word_sequence_starts_join_clause(
        tokens: &[SqlToken],
        start_idx: usize,
        meaningful_token_links: Option<&MeaningfulTokenLinks>,
    ) -> bool {
        let mut prefix_words = [None; 4];
        let mut prefix_word_count = 0usize;
        let mut cursor = Some(start_idx);

        while let Some(token_idx) = cursor {
            let Some(token) = tokens.get(token_idx) else {
                break;
            };
            match token {
                SqlToken::Word(word) => {
                    prefix_words[prefix_word_count] = Some(word.as_str());
                    prefix_word_count += 1;
                    if prefix_word_count >= prefix_words.len() {
                        break;
                    }
                    cursor = Self::next_meaningful_token_index_with_links(
                        tokens,
                        token_idx,
                        meaningful_token_links,
                    );
                }
                SqlToken::Comment(_) => {
                    cursor = Self::next_meaningful_token_index_with_links(
                        tokens,
                        token_idx,
                        meaningful_token_links,
                    );
                }
                SqlToken::String(_) | SqlToken::Symbol(_) => break,
            }
        }

        let Some(first_word) = prefix_words[0] else {
            return false;
        };

        if first_word.eq_ignore_ascii_case("JOIN") || first_word.eq_ignore_ascii_case("APPLY") {
            return true;
        }

        let starts_with_join_modifier = first_word.eq_ignore_ascii_case("OUTER")
            || FORMAT_JOIN_MODIFIER_KEYWORDS
                .iter()
                .any(|keyword| first_word.eq_ignore_ascii_case(keyword));

        starts_with_join_modifier
            && prefix_words[1..]
                .iter()
                .flatten()
                .any(|word| word.eq_ignore_ascii_case("JOIN") || word.eq_ignore_ascii_case("APPLY"))
    }

    fn token_ends_identifier_slot(
        tokens: &[SqlToken],
        idx: usize,
        meaningful_token_links: Option<&MeaningfulTokenLinks>,
    ) -> bool {
        let next_idx =
            Self::next_meaningful_token_index_with_links(tokens, idx, meaningful_token_links);
        let Some(next_idx) = next_idx else {
            return true;
        };
        let Some(next_token) = tokens.get(next_idx) else {
            return true;
        };

        match next_token {
            SqlToken::Symbol(sym) => matches!(sym.as_str(), "," | ")" | ";"),
            SqlToken::Word(word) => {
                let upper = word.to_ascii_uppercase();
                upper == "ON"
                    || upper == "USING"
                    || Self::is_dml_clause_starter(upper.as_str())
                    || crate::sql_text::is_format_set_operator_keyword(upper.as_str())
                    || Self::next_word_sequence_starts_join_clause(
                        tokens,
                        next_idx,
                        meaningful_token_links,
                    )
            }
            SqlToken::String(_) | SqlToken::Comment(_) => false,
        }
    }

    fn next_word_sequence_starts_join_clause_from_words(next_words: &[Option<&str>; 3]) -> bool {
        let Some(first_word) = next_words[0] else {
            return false;
        };

        if first_word.eq_ignore_ascii_case("JOIN") || first_word.eq_ignore_ascii_case("APPLY") {
            return true;
        }

        let starts_with_join_modifier = first_word.eq_ignore_ascii_case("OUTER")
            || FORMAT_JOIN_MODIFIER_KEYWORDS
                .iter()
                .any(|keyword| first_word.eq_ignore_ascii_case(keyword));

        starts_with_join_modifier
            && next_words[1..]
                .iter()
                .flatten()
                .any(|word| word.eq_ignore_ascii_case("JOIN") || word.eq_ignore_ascii_case("APPLY"))
    }

    fn token_ends_identifier_slot_from_context(
        next_token: Option<&SqlToken>,
        next_words: &[Option<&str>; 3],
    ) -> bool {
        match next_token {
            None => true,
            Some(SqlToken::Symbol(sym)) => matches!(sym.as_str(), "," | ")" | ";"),
            Some(SqlToken::Word(word)) => {
                let upper = word.to_ascii_uppercase();
                upper == "ON"
                    || upper == "USING"
                    || Self::is_dml_clause_starter(upper.as_str())
                    || crate::sql_text::is_format_set_operator_keyword(upper.as_str())
                    || Self::next_word_sequence_starts_join_clause_from_words(next_words)
            }
            Some(SqlToken::String(_) | SqlToken::Comment(_)) => false,
        }
    }

    fn formatter_keyword_should_render_as_identifier_from_context(
        word_upper: &str,
        current_clause: Option<&str>,
        prev_token: Option<&SqlToken>,
        next_token: Option<&SqlToken>,
        next_words: &[Option<&str>; 3],
    ) -> bool {
        let keyword_can_be_identifier = matches!(
            word_upper,
            "MODEL"
                | "WINDOW"
                | "MATCH_RECOGNIZE"
                | "QUALIFY"
                | "PIVOT"
                | "UNPIVOT"
                | "SEARCH"
                | "CYCLE"
        );
        if !keyword_can_be_identifier {
            return false;
        }

        let prev_is_boundary = matches!(
            prev_token,
            Some(SqlToken::Symbol(sym)) if Self::mysql_identifier_left_boundary_symbol(sym)
        );
        let next_token_is_dot = matches!(next_token, Some(SqlToken::Symbol(sym)) if sym == ".");
        let prev_token_is_dot = matches!(prev_token, Some(SqlToken::Symbol(sym)) if sym == ".");
        let previous_is_current_clause_head = matches!(
            (prev_token, current_clause),
            (Some(SqlToken::Word(prev_word)), Some(current_clause_word))
                if prev_word.eq_ignore_ascii_case(current_clause_word)
        );
        let follows_alias_keyword = Self::token_is_alias_keyword(prev_token);
        let next_is_alias_keyword = Self::token_is_alias_keyword(next_token);
        let next_ends_identifier_slot =
            Self::token_ends_identifier_slot_from_context(next_token, next_words);
        let in_select_clause = matches!(current_clause, Some("SELECT"));
        let in_table_alias_clause = matches!(
            current_clause,
            Some("FROM" | "UPDATE" | "DELETE" | "INTO" | "MERGE" | "USING")
        );
        let in_identifier_qualified_clause = matches!(
            current_clause,
            Some("SELECT" | "FROM" | "WHERE" | "ON" | "GROUP" | "HAVING" | "ORDER")
        );
        let in_alias_capable_clause = in_identifier_qualified_clause
            || matches!(current_clause, Some("SET"))
            || in_table_alias_clause;

        if (next_token_is_dot || prev_token_is_dot) && in_identifier_qualified_clause {
            return true;
        }

        if matches!(
            current_clause,
            Some("SELECT" | "WHERE" | "HAVING" | "ON" | "SET" | "GROUP" | "ORDER")
        ) && (prev_is_boundary || previous_is_current_clause_head)
            && (next_ends_identifier_slot || next_is_alias_keyword)
        {
            return true;
        }

        if follows_alias_keyword && in_alias_capable_clause {
            return true;
        }

        (in_select_clause || in_table_alias_clause)
            && !prev_is_boundary
            && !previous_is_current_clause_head
            && next_ends_identifier_slot
    }

    fn mysql_keyword_should_render_as_identifier_from_context(
        word: &str,
        word_upper: &str,
        current_clause: Option<&str>,
        on_duplicate_key_update_active: bool,
        prev_token: Option<&SqlToken>,
        next_token: Option<&SqlToken>,
        next_words: &[Option<&str>; 3],
    ) -> bool {
        if !word.bytes().any(|byte| byte.is_ascii_lowercase()) {
            return false;
        }

        if !Self::mysql_formatter_keyword(word_upper) {
            return false;
        }
        if Self::mysql_keyword_must_stay_keyword_in_expression(word_upper) {
            return false;
        }

        let next_is_open_paren = matches!(next_token, Some(SqlToken::Symbol(sym)) if sym == "(");
        if next_is_open_paren && Self::mysql_keyword_prefers_space_before_paren(word_upper) {
            return word_upper == "VALUES" && on_duplicate_key_update_active;
        }

        if Self::formatter_keyword_should_render_as_identifier_from_context(
            word_upper,
            current_clause,
            prev_token,
            next_token,
            next_words,
        ) {
            return true;
        }

        let prev_is_boundary = matches!(
            prev_token,
            Some(SqlToken::Symbol(sym)) if Self::mysql_identifier_left_boundary_symbol(sym)
        );
        let next_is_boundary = matches!(
            next_token,
            Some(SqlToken::Symbol(sym)) if Self::mysql_identifier_right_boundary_symbol(sym)
        ) || next_token.is_none();
        let previous_is_current_clause_head = matches!(
            (prev_token, current_clause),
            (Some(SqlToken::Word(prev_word)), Some(current_clause_word))
                if prev_word.eq_ignore_ascii_case(current_clause_word)
        );
        let follows_alias_keyword = Self::token_is_alias_keyword(prev_token);
        let next_is_alias_keyword = Self::token_is_alias_keyword(next_token);
        let in_select_clause = matches!(current_clause, Some("SELECT"));
        let in_table_alias_clause = matches!(
            current_clause,
            Some("FROM" | "UPDATE" | "DELETE" | "INTO" | "MERGE" | "USING")
        );
        let next_ends_identifier_slot =
            Self::token_ends_identifier_slot_from_context(next_token, next_words);

        if matches!(
            current_clause,
            Some("SELECT" | "WHERE" | "HAVING" | "ON" | "SET" | "GROUP" | "ORDER")
        ) && (prev_is_boundary || previous_is_current_clause_head)
            && (next_is_boundary || next_is_alias_keyword || next_ends_identifier_slot)
        {
            return true;
        }

        if follows_alias_keyword {
            return true;
        }

        if (in_select_clause || in_table_alias_clause)
            && !prev_is_boundary
            && !previous_is_current_clause_head
            && (next_is_boundary || next_ends_identifier_slot)
        {
            return true;
        }

        prev_is_boundary && next_is_boundary
    }

    fn formatter_keyword_should_render_as_identifier(
        tokens: &[SqlToken],
        idx: usize,
        current_clause: Option<&str>,
        meaningful_token_links: Option<&MeaningfulTokenLinks>,
    ) -> bool {
        let Some(SqlToken::Word(word)) = tokens.get(idx) else {
            return false;
        };
        let upper = word.to_ascii_uppercase();
        let prev_token = meaningful_token_links
            .and_then(|links| links.prev_index(idx))
            .and_then(|token_idx| tokens.get(token_idx))
            .or_else(|| {
                tokens[..idx]
                    .iter()
                    .rev()
                    .find(|token| !matches!(token, SqlToken::Comment(_)))
            });
        let next_token = meaningful_token_links
            .and_then(|links| links.next_index(idx))
            .and_then(|token_idx| tokens.get(token_idx))
            .or_else(|| {
                tokens
                    .iter()
                    .enumerate()
                    .skip(idx.saturating_add(1))
                    .find(|(_, token)| !matches!(token, SqlToken::Comment(_)))
                    .map(|(_, token)| token)
            });
        let mut next_words = [None; 3];
        let mut word_slot = 0usize;
        let mut word_idx = idx;
        while word_slot < 3 {
            let Some(next_meaningful_idx) = meaningful_token_links
                .and_then(|links| links.next_index(word_idx))
                .or_else(|| Self::next_meaningful_token_index(tokens, word_idx))
            else {
                break;
            };
            if let Some(SqlToken::Word(word)) = tokens.get(next_meaningful_idx) {
                next_words[word_slot] = Some(word.as_str());
                word_slot += 1;
            }
            word_idx = next_meaningful_idx.saturating_add(1);
        }

        Self::formatter_keyword_should_render_as_identifier_from_context(
            upper.as_str(),
            current_clause,
            prev_token,
            next_token,
            &next_words,
        )
    }

    fn mysql_keyword_should_render_as_identifier(
        tokens: &[SqlToken],
        idx: usize,
        current_clause: Option<&str>,
        on_duplicate_key_update_active: bool,
        meaningful_token_links: Option<&MeaningfulTokenLinks>,
    ) -> bool {
        let Some(SqlToken::Word(word)) = tokens.get(idx) else {
            return false;
        };
        let upper = word.to_ascii_uppercase();
        let prev_token = meaningful_token_links
            .and_then(|links| links.prev_index(idx))
            .and_then(|token_idx| tokens.get(token_idx))
            .or_else(|| {
                tokens[..idx]
                    .iter()
                    .rev()
                    .find(|token| !matches!(token, SqlToken::Comment(_)))
            });
        let next_token = meaningful_token_links
            .and_then(|links| links.next_index(idx))
            .and_then(|token_idx| tokens.get(token_idx))
            .or_else(|| {
                tokens
                    .iter()
                    .enumerate()
                    .skip(idx.saturating_add(1))
                    .find(|(_, token)| !matches!(token, SqlToken::Comment(_)))
                    .map(|(_, token)| token)
            });
        let mut next_words = [None; 3];
        let mut word_slot = 0usize;
        let mut word_idx = idx;
        while word_slot < 3 {
            let Some(next_meaningful_idx) = meaningful_token_links
                .and_then(|links| links.next_index(word_idx))
                .or_else(|| Self::next_meaningful_token_index(tokens, word_idx))
            else {
                break;
            };
            if let Some(SqlToken::Word(word)) = tokens.get(next_meaningful_idx) {
                next_words[word_slot] = Some(word.as_str());
                word_slot += 1;
            }
            word_idx = next_meaningful_idx.saturating_add(1);
        }

        Self::mysql_keyword_should_render_as_identifier_from_context(
            word,
            upper.as_str(),
            current_clause,
            on_duplicate_key_update_active,
            prev_token,
            next_token,
            &next_words,
        )
    }

    fn count_star_call_should_stay_tight(
        tokens: &[SqlToken],
        open_paren_idx: usize,
        meaningful_token_links: Option<&MeaningfulTokenLinks>,
        statement_word_links: Option<&StatementWordLinks>,
    ) -> bool {
        let prev_word_idx = statement_word_links
            .and_then(|links| links.prev_word_index(open_paren_idx))
            .or_else(|| Self::previous_word_upper(tokens, open_paren_idx).map(|(_, idx)| idx));
        let Some(prev_word) = prev_word_idx
            .and_then(|idx| tokens.get(idx))
            .and_then(|token| match token {
                SqlToken::Word(word) => Some(word.as_str()),
                _ => None,
            })
        else {
            return false;
        };
        if !prev_word.eq_ignore_ascii_case("COUNT") {
            return false;
        }

        let Some(star_idx) = meaningful_token_links
            .and_then(|links| links.next_index(open_paren_idx))
            .or_else(|| Self::next_meaningful_token_index(tokens, open_paren_idx))
        else {
            return false;
        };
        let Some(close_idx) = meaningful_token_links
            .and_then(|links| links.next_index(star_idx))
            .or_else(|| Self::next_meaningful_token_index(tokens, star_idx))
        else {
            return false;
        };

        matches!(tokens.get(star_idx), Some(SqlToken::Symbol(sym)) if sym == "*")
            && matches!(tokens.get(close_idx), Some(SqlToken::Symbol(sym)) if sym == ")")
    }

    fn mysql_keyword_prefers_space_before_paren(word: &str) -> bool {
        matches!(
            word,
            "ALL"
                | "AND"
                | "AS"
                | "BEGIN"
                | "BETWEEN"
                | "BY"
                | "CASE"
                | "CHECK"
                | "ELSE"
                | "ELSEIF"
                | "ELSIF"
                | "EXISTS"
                | "FOR"
                | "FROM"
                | "GROUP"
                | "HAVING"
                | "IF"
                | "IN"
                | "INTO"
                | "JOIN"
                | "KEY"
                | "LIMIT"
                | "NOT"
                | "ON"
                | "OR"
                | "ORDER"
                | "OVER"
                | "PARTITION"
                | "PRIMARY"
                | "REFERENCES"
                | "REPEAT"
                | "RETURN"
                | "RETURNS"
                | "SELECT"
                | "SET"
                | "TABLE"
                | "THEN"
                | "UNIQUE"
                | "UNTIL"
                | "USING"
                | "VALUES"
                | "WHEN"
                | "WHERE"
                | "WHILE"
                | "WINDOW"
        )
    }

    fn mysql_paren_belongs_to_insert_target_column_list(
        tokens: &[SqlToken],
        open_paren_idx: usize,
        statement_word_links: Option<&StatementWordLinks>,
    ) -> bool {
        let mut has_into = false;
        let mut has_insert_or_replace = false;

        Self::scan_recent_statement_words(
            tokens,
            open_paren_idx,
            6,
            statement_word_links,
            |word, pos| {
                if pos > 0 && word.eq_ignore_ascii_case("INTO") {
                    has_into = true;
                }
                if word.eq_ignore_ascii_case("INSERT") || word.eq_ignore_ascii_case("REPLACE") {
                    has_insert_or_replace = true;
                }
                !(has_into && has_insert_or_replace)
            },
        );

        has_into && has_insert_or_replace
    }

    fn mysql_paren_belongs_to_insert_target_column_list_from_indices(
        tokens: &[SqlToken],
        recent_statement_word_indices: &VecDeque<usize>,
    ) -> bool {
        let mut has_into = false;
        let mut has_insert_or_replace = false;

        Self::scan_recent_statement_words_from_indices(
            tokens,
            recent_statement_word_indices,
            6,
            |word, pos| {
                if pos > 0 && word.eq_ignore_ascii_case("INTO") {
                    has_into = true;
                }
                if word.eq_ignore_ascii_case("INSERT") || word.eq_ignore_ascii_case("REPLACE") {
                    has_insert_or_replace = true;
                }
                !(has_into && has_insert_or_replace)
            },
        );

        has_into && has_insert_or_replace
    }

    fn mysql_paren_follows_named_key_definition(
        tokens: &[SqlToken],
        open_paren_idx: usize,
        meaningful_token_links: Option<&MeaningfulTokenLinks>,
        statement_word_links: Option<&StatementWordLinks>,
    ) -> bool {
        let prev_word_idx = statement_word_links
            .and_then(|links| links.prev_word_index(open_paren_idx))
            .or_else(|| Self::previous_word_upper(tokens, open_paren_idx).map(|(_, idx)| idx));
        let Some(prev_word_idx) = prev_word_idx else {
            return false;
        };
        let Some(key_idx) = meaningful_token_links
            .and_then(|links| links.prev_index(prev_word_idx))
            .or_else(|| Self::previous_meaningful_token_index(tokens, prev_word_idx))
        else {
            return false;
        };
        let Some(SqlToken::Word(key_word)) = tokens.get(key_idx) else {
            return false;
        };
        if !key_word.eq_ignore_ascii_case("KEY") && !key_word.eq_ignore_ascii_case("INDEX") {
            return false;
        }
        true
    }

    fn mysql_word_paren_should_stay_tight(
        tokens: &[SqlToken],
        open_paren_idx: usize,
        on_duplicate_key_update_active: bool,
        meaningful_token_links: Option<&MeaningfulTokenLinks>,
        statement_word_links: Option<&StatementWordLinks>,
        matching_paren_close_indices: Option<&[Option<usize>]>,
    ) -> bool {
        let prev_word_idx = statement_word_links
            .and_then(|links| links.prev_word_index(open_paren_idx))
            .or_else(|| Self::previous_word_upper(tokens, open_paren_idx).map(|(_, idx)| idx));
        let Some(prev_word_idx) = prev_word_idx else {
            return false;
        };
        let Some(prev_word) = tokens.get(prev_word_idx).and_then(|token| match token {
            SqlToken::Word(word) => Some(word.to_ascii_uppercase()),
            _ => None,
        }) else {
            return false;
        };
        let prev_word = prev_word.as_str();

        let prev_meaningful_idx = meaningful_token_links
            .and_then(|links| links.prev_index(open_paren_idx))
            .or_else(|| Self::previous_meaningful_token_index(tokens, open_paren_idx));
        let Some(prev_meaningful_idx) = prev_meaningful_idx else {
            return false;
        };

        // Tight MySQL word-paren spacing is only valid when the immediate token
        // before `(` is the callee/type word itself. If an operator/symbol sits
        // between the nearest word and `(` (e.g. `x = (` or `a + (`), preserve
        // normal operator spacing.
        if prev_meaningful_idx != prev_word_idx {
            return false;
        }

        if prev_word == "IF"
            && Self::mysql_if_is_scalar_function(
                tokens,
                prev_meaningful_idx,
                meaningful_token_links,
                matching_paren_close_indices,
            )
        {
            return true;
        }

        if prev_word == "VALUES" {
            return on_duplicate_key_update_active;
        }

        if Self::mysql_keyword_prefers_space_before_paren(prev_word) {
            return false;
        }

        let insert_target_column_list = Self::mysql_paren_belongs_to_insert_target_column_list(
            tokens,
            open_paren_idx,
            statement_word_links,
        );
        if Self::mysql_paren_follows_named_key_definition(
            tokens,
            open_paren_idx,
            meaningful_token_links,
            statement_word_links,
        ) {
            return false;
        }
        if Self::paren_opens_call_argument_list(tokens, open_paren_idx, statement_word_links)
            || insert_target_column_list
        {
            return !insert_target_column_list;
        }

        true
    }

    fn mysql_word_paren_should_stay_tight_from_context(
        tokens: &[SqlToken],
        on_duplicate_key_update_active: bool,
        prev_word_upper: Option<&str>,
        previous_non_comment_token: Option<&SqlToken>,
        second_previous_non_comment_token: Option<&SqlToken>,
        recent_statement_word_indices: &VecDeque<usize>,
        mysql_scalar_if_function: bool,
    ) -> bool {
        let Some(prev_word) = prev_word_upper else {
            return false;
        };

        let prev_meaningful_is_word = matches!(
            previous_non_comment_token,
            Some(SqlToken::Word(word)) if word.eq_ignore_ascii_case(prev_word)
        );
        if !prev_meaningful_is_word {
            return false;
        }

        if prev_word == "IF" && mysql_scalar_if_function {
            return true;
        }

        if prev_word == "VALUES" {
            return on_duplicate_key_update_active;
        }

        if Self::mysql_keyword_prefers_space_before_paren(prev_word) {
            return false;
        }

        let insert_target_column_list =
            Self::mysql_paren_belongs_to_insert_target_column_list_from_indices(
                tokens,
                recent_statement_word_indices,
            );
        let follows_named_key_definition = matches!(
            second_previous_non_comment_token,
            Some(SqlToken::Word(word))
                if word.eq_ignore_ascii_case("KEY") || word.eq_ignore_ascii_case("INDEX")
        );
        if follows_named_key_definition {
            return false;
        }

        if Self::paren_opens_call_argument_list_from_indices(tokens, recent_statement_word_indices)
            || insert_target_column_list
        {
            return !insert_target_column_list;
        }

        true
    }

    fn mysql_if_is_scalar_function_from_context(
        current_clause: Option<&str>,
        on_duplicate_key_update_active: bool,
        in_plsql_block: bool,
        at_line_start: bool,
        previous_non_comment_token: Option<&SqlToken>,
    ) -> bool {
        if on_duplicate_key_update_active {
            return true;
        }

        if matches!(
            current_clause,
            Some(
                "SELECT"
                    | "SET"
                    | "WHERE"
                    | "HAVING"
                    | "ORDER"
                    | "GROUP"
                    | "VALUES"
                    | "RETURNING"
                    | "ON"
                    | "WHEN"
            )
        ) {
            return true;
        }

        if !in_plsql_block && !at_line_start {
            return matches!(
                previous_non_comment_token,
                Some(SqlToken::Symbol(symbol))
                    if matches!(
                        symbol.as_str(),
                        "(" | "," | "=" | "<" | ">" | "<=" | ">=" | "<>" | "!=" | "<=>"
                    )
            );
        }

        false
    }

    fn mysql_if_is_scalar_function(
        tokens: &[SqlToken],
        if_idx: usize,
        meaningful_token_links: Option<&MeaningfulTokenLinks>,
        matching_paren_close_indices: Option<&[Option<usize>]>,
    ) -> bool {
        let open_idx = meaningful_token_links
            .and_then(|links| links.next_index(if_idx))
            .or_else(|| Self::next_meaningful_token_index(tokens, if_idx));
        let Some(open_idx) = open_idx else {
            return false;
        };
        if !matches!(tokens.get(open_idx), Some(SqlToken::Symbol(sym)) if sym == "(") {
            return false;
        }

        let Some(close_idx) =
            Self::matching_paren_close_index(tokens, open_idx, matching_paren_close_indices)
        else {
            return false;
        };
        !matches!(
            meaningful_token_links
                .and_then(|links| links.next_index(close_idx))
                .or_else(|| Self::next_meaningful_token_index(tokens, close_idx))
                .and_then(|idx| tokens.get(idx)),
            Some(SqlToken::Word(word)) if word.eq_ignore_ascii_case("THEN")
        )
    }

    fn matching_paren_close_index(
        tokens: &[SqlToken],
        open_idx: usize,
        matching_paren_close_indices: Option<&[Option<usize>]>,
    ) -> Option<usize> {
        if let Some(close_idx) = matching_paren_close_indices
            .and_then(|indices| indices.get(open_idx))
            .copied()
            .flatten()
        {
            return Some(close_idx);
        }
        if !matches!(tokens.get(open_idx), Some(SqlToken::Symbol(sym)) if sym == "(") {
            return None;
        }

        let mut depth = 0usize;
        for (idx, token) in tokens.iter().enumerate().skip(open_idx) {
            match token {
                SqlToken::Symbol(sym) if sym == "(" => {
                    depth = depth.saturating_add(1);
                }
                SqlToken::Symbol(sym) if sym == ")" => {
                    depth = depth.saturating_sub(1);
                    if depth == 0 {
                        return Some(idx);
                    }
                }
                _ => {}
            }
        }

        None
    }

    fn paren_opens_call_argument_list(
        tokens: &[SqlToken],
        open_idx: usize,
        statement_word_links: Option<&StatementWordLinks>,
    ) -> bool {
        let mut second_recent_is_call = false;
        Self::scan_recent_statement_words(
            tokens,
            open_idx,
            2,
            statement_word_links,
            |word, pos| {
                if pos == 1 && word.eq_ignore_ascii_case("CALL") {
                    second_recent_is_call = true;
                    return false;
                }
                true
            },
        );
        second_recent_is_call
    }

    fn scan_recent_statement_words_from_indices<F>(
        tokens: &[SqlToken],
        recent_statement_word_indices: &VecDeque<usize>,
        max_words: usize,
        mut visitor: F,
    ) where
        F: FnMut(&str, usize) -> bool,
    {
        if max_words == 0 {
            return;
        }

        let mut seen = 0usize;
        for word_idx in recent_statement_word_indices.iter().rev().copied() {
            let Some(SqlToken::Word(word)) = tokens.get(word_idx) else {
                continue;
            };
            if !visitor(word.as_str(), seen) {
                break;
            }
            seen = seen.saturating_add(1);
            if seen >= max_words {
                break;
            }
        }
    }

    fn recent_statement_words_before_from_indices<'a>(
        tokens: &'a [SqlToken],
        recent_statement_word_indices: &VecDeque<usize>,
        max_words: usize,
    ) -> Vec<&'a str> {
        FormatterStatementStructure::recent_statement_words_before(
            tokens,
            recent_statement_word_indices,
            max_words,
        )
    }

    fn paren_opens_call_argument_list_from_indices(
        tokens: &[SqlToken],
        recent_statement_word_indices: &VecDeque<usize>,
    ) -> bool {
        let mut second_recent_is_call = false;
        Self::scan_recent_statement_words_from_indices(
            tokens,
            recent_statement_word_indices,
            2,
            |word, pos| {
                if pos == 1 && word.eq_ignore_ascii_case("CALL") {
                    second_recent_is_call = true;
                    return false;
                }
                true
            },
        );
        second_recent_is_call
    }

    fn is_mysql_declare_cursor_for_clause_from_indices(
        tokens: &[SqlToken],
        recent_statement_word_indices: &VecDeque<usize>,
    ) -> bool {
        let mut has_declare = false;
        let mut has_cursor = false;
        Self::scan_recent_statement_words_from_indices(
            tokens,
            recent_statement_word_indices,
            6,
            |word, _| {
                if word.eq_ignore_ascii_case("DECLARE") {
                    has_declare = true;
                } else if word.eq_ignore_ascii_case("CURSOR") {
                    has_cursor = true;
                }
                !(has_declare && has_cursor)
            },
        );
        has_declare && has_cursor
    }

    fn is_mysql_declare_handler_for_clause_from_indices(
        tokens: &[SqlToken],
        recent_statement_word_indices: &VecDeque<usize>,
    ) -> bool {
        let mut has_declare = false;
        let mut has_handler = false;
        Self::scan_recent_statement_words_from_indices(
            tokens,
            recent_statement_word_indices,
            6,
            |word, _| {
                if word.eq_ignore_ascii_case("DECLARE") {
                    has_declare = true;
                } else if word.eq_ignore_ascii_case("HANDLER") {
                    has_handler = true;
                }
                !(has_declare && has_handler)
            },
        );
        has_declare && has_handler
    }

    fn is_mysql_if_exists_modifier_from_indices(
        tokens: &[SqlToken],
        next_word: Option<&str>,
        recent_statement_word_indices: &VecDeque<usize>,
    ) -> bool {
        if !matches!(
            next_word,
            Some(word) if word.eq_ignore_ascii_case("EXISTS") || word.eq_ignore_ascii_case("NOT")
        ) {
            return false;
        }

        let mut has_ddl_verb = false;
        Self::scan_recent_statement_words_from_indices(
            tokens,
            recent_statement_word_indices,
            6,
            |word, _| {
                if word.eq_ignore_ascii_case("DROP")
                    || word.eq_ignore_ascii_case("CREATE")
                    || word.eq_ignore_ascii_case("ALTER")
                {
                    has_ddl_verb = true;
                    return false;
                }
                true
            },
        );
        has_ddl_verb
    }

    fn is_mysql_signal_set_clause_from_indices(
        tokens: &[SqlToken],
        recent_statement_word_indices: &VecDeque<usize>,
    ) -> bool {
        let mut has_signal = false;
        Self::scan_recent_statement_words_from_indices(
            tokens,
            recent_statement_word_indices,
            8,
            |word, _| {
                if word.eq_ignore_ascii_case("SIGNAL") || word.eq_ignore_ascii_case("RESIGNAL") {
                    has_signal = true;
                    return false;
                }
                true
            },
        );
        has_signal
    }

    fn is_mysql_on_duplicate_key_update_from_indices(
        tokens: &[SqlToken],
        recent_statement_word_indices: &VecDeque<usize>,
    ) -> bool {
        let mut first_is_key = false;
        let mut second_is_duplicate = false;
        let mut third_is_on = false;
        Self::scan_recent_statement_words_from_indices(
            tokens,
            recent_statement_word_indices,
            3,
            |word, pos| {
                match pos {
                    0 => first_is_key = word.eq_ignore_ascii_case("KEY"),
                    1 => second_is_duplicate = word.eq_ignore_ascii_case("DUPLICATE"),
                    2 => third_is_on = word.eq_ignore_ascii_case("ON"),
                    _ => {}
                }
                true
            },
        );
        first_is_key && second_is_duplicate && third_is_on
    }

    fn scan_recent_statement_words<F>(
        tokens: &[SqlToken],
        idx: usize,
        max_words: usize,
        statement_word_links: Option<&StatementWordLinks>,
        mut visitor: F,
    ) where
        F: FnMut(&str, usize) -> bool,
    {
        if max_words == 0 {
            return;
        }

        if let Some(links) = statement_word_links {
            let mut seen = 0usize;
            let mut word_idx = links.prev_word_index(idx);
            while let Some(current_word_idx) = word_idx {
                let Some(SqlToken::Word(word)) = tokens.get(current_word_idx) else {
                    break;
                };
                if !visitor(word.as_str(), seen) {
                    break;
                }
                seen = seen.saturating_add(1);
                if seen >= max_words {
                    break;
                }
                word_idx = links.prev_word_index(current_word_idx);
            }
            return;
        }

        let capped_idx = idx.min(tokens.len());
        let mut seen = 0usize;
        for token in tokens[..capped_idx].iter().rev() {
            match token {
                SqlToken::Symbol(sym) if sym == ";" => break,
                SqlToken::Word(word) => {
                    if !visitor(word.as_str(), seen) {
                        break;
                    }
                    seen = seen.saturating_add(1);
                    if seen >= max_words {
                        break;
                    }
                }
                _ => {}
            }
        }
    }

    fn recent_statement_words_before<'a>(
        tokens: &'a [SqlToken],
        idx: usize,
        max_words: usize,
        statement_word_links: Option<&StatementWordLinks>,
    ) -> Vec<&'a str> {
        if let Some(links) = statement_word_links {
            let mut words = Vec::with_capacity(max_words);
            let mut word_idx = links.prev_word_index(idx);
            while let Some(current_word_idx) = word_idx {
                let Some(SqlToken::Word(word)) = tokens.get(current_word_idx) else {
                    break;
                };
                words.push(word.as_str());
                if words.len() >= max_words {
                    break;
                }
                word_idx = links.prev_word_index(current_word_idx);
            }
            return words;
        }

        let capped_idx = idx.min(tokens.len());
        let mut words = Vec::with_capacity(max_words);
        for token in tokens[..capped_idx].iter().rev() {
            match token {
                SqlToken::Symbol(sym) if sym == ";" => break,
                SqlToken::Word(word) => {
                    words.push(word.as_str());
                    if words.len() >= max_words {
                        break;
                    }
                }
                _ => {}
            }
        }

        words
    }

    fn is_mysql_declare_cursor_for_clause(
        tokens: &[SqlToken],
        idx: usize,
        statement_word_links: Option<&StatementWordLinks>,
    ) -> bool {
        let mut has_declare = false;
        let mut has_cursor = false;
        Self::scan_recent_statement_words(tokens, idx, 6, statement_word_links, |word, _| {
            if word.eq_ignore_ascii_case("DECLARE") {
                has_declare = true;
            } else if word.eq_ignore_ascii_case("CURSOR") {
                has_cursor = true;
            }
            !(has_declare && has_cursor)
        });
        has_declare && has_cursor
    }

    fn is_mysql_declare_handler_for_clause(
        tokens: &[SqlToken],
        idx: usize,
        statement_word_links: Option<&StatementWordLinks>,
    ) -> bool {
        let mut has_declare = false;
        let mut has_handler = false;
        Self::scan_recent_statement_words(tokens, idx, 6, statement_word_links, |word, _| {
            if word.eq_ignore_ascii_case("DECLARE") {
                has_declare = true;
            } else if word.eq_ignore_ascii_case("HANDLER") {
                has_handler = true;
            }
            !(has_declare && has_handler)
        });
        has_declare && has_handler
    }

    fn is_mysql_if_exists_modifier(
        tokens: &[SqlToken],
        idx: usize,
        next_word: Option<&str>,
        statement_word_links: Option<&StatementWordLinks>,
    ) -> bool {
        if !matches!(
            next_word,
            Some(word) if word.eq_ignore_ascii_case("EXISTS") || word.eq_ignore_ascii_case("NOT")
        ) {
            return false;
        }

        let mut has_ddl_verb = false;
        Self::scan_recent_statement_words(tokens, idx, 6, statement_word_links, |word, _| {
            if word.eq_ignore_ascii_case("DROP")
                || word.eq_ignore_ascii_case("CREATE")
                || word.eq_ignore_ascii_case("ALTER")
            {
                has_ddl_verb = true;
                return false;
            }
            true
        });
        has_ddl_verb
    }

    /// Returns true when SET is a sub-clause of a MySQL SIGNAL/RESIGNAL statement.
    /// In MySQL, SIGNAL SQLSTATE '...' SET MESSAGE_TEXT = ... has SET as a
    /// continuation of the SIGNAL statement, not a new independent clause.
    fn is_mysql_signal_set_clause(
        tokens: &[SqlToken],
        idx: usize,
        statement_word_links: Option<&StatementWordLinks>,
    ) -> bool {
        let mut has_signal = false;
        Self::scan_recent_statement_words(tokens, idx, 8, statement_word_links, |word, _| {
            if word.eq_ignore_ascii_case("SIGNAL") || word.eq_ignore_ascii_case("RESIGNAL") {
                has_signal = true;
                return false;
            }
            true
        });
        has_signal
    }

    /// Returns true when UPDATE is the keyword following ON DUPLICATE KEY,
    /// forming MySQL's ON DUPLICATE KEY UPDATE clause.
    fn is_mysql_on_duplicate_key_update(
        tokens: &[SqlToken],
        idx: usize,
        statement_word_links: Option<&StatementWordLinks>,
    ) -> bool {
        let mut first_is_key = false;
        let mut second_is_duplicate = false;
        let mut third_is_on = false;
        Self::scan_recent_statement_words(tokens, idx, 3, statement_word_links, |word, pos| {
            match pos {
                0 => first_is_key = word.eq_ignore_ascii_case("KEY"),
                1 => second_is_duplicate = word.eq_ignore_ascii_case("DUPLICATE"),
                2 => third_is_on = word.eq_ignore_ascii_case("ON"),
                _ => {}
            }
            true
        });
        first_is_key && second_is_duplicate && third_is_on
    }

    /// Returns true when the current token starts MySQL's
    /// `ON DUPLICATE KEY UPDATE` clause.
    fn starts_mysql_on_duplicate_key_update_clause(
        tokens: &[SqlToken],
        idx: usize,
        meaningful_links: Option<&MeaningfulTokenLinks>,
    ) -> bool {
        let Some(SqlToken::Word(word)) = tokens.get(idx) else {
            return false;
        };
        if !word.eq_ignore_ascii_case("ON") {
            return false;
        }

        let next_meaningful_index = |from_idx: usize| {
            if let Some(links) = meaningful_links {
                return links.next_index(from_idx);
            }

            let mut probe_idx = from_idx.saturating_add(1);
            while probe_idx < tokens.len() {
                if !matches!(tokens.get(probe_idx), Some(SqlToken::Comment(_))) {
                    return Some(probe_idx);
                }
                probe_idx = probe_idx.saturating_add(1);
            }
            None
        };
        let meaningful_word = |token_idx: usize| match tokens.get(token_idx) {
            Some(SqlToken::Word(value)) => Some(value.as_str()),
            _ => None,
        };

        let duplicate_idx = match next_meaningful_index(idx) {
            Some(next_idx) => next_idx,
            None => return false,
        };
        let key_idx = match next_meaningful_index(duplicate_idx) {
            Some(next_idx) => next_idx,
            None => return false,
        };
        let update_idx = match next_meaningful_index(key_idx) {
            Some(next_idx) => next_idx,
            None => return false,
        };

        meaningful_word(duplicate_idx).is_some_and(|value| value.eq_ignore_ascii_case("DUPLICATE"))
            && meaningful_word(key_idx).is_some_and(|value| value.eq_ignore_ascii_case("KEY"))
            && meaningful_word(update_idx).is_some_and(|value| value.eq_ignore_ascii_case("UPDATE"))
    }

    fn is_sqlplus_remark_comment_statement(statement: &str) -> bool {
        statement
            .split_whitespace()
            .next()
            .is_some_and(|first_word| {
                first_word.eq_ignore_ascii_case("REM") || first_word.eq_ignore_ascii_case("REMARK")
            })
    }

    fn source_has_explicit_semicolon_terminator(statement: &str) -> bool {
        let trimmed = statement.trim_end();
        if trimmed.is_empty() {
            return false;
        }

        let spans = super::query_text::tokenize_sql_spanned(trimmed);
        spans.iter().rev().any(|span| {
            matches!(&span.token, SqlToken::Symbol(sym) if sym == ";")
                && !Self::semicolon_belongs_to_non_sql_line(trimmed, span.start)
                && Self::trailing_segment_allows_semicolon_terminator(&trimmed[span.end..])
        })
    }

    fn semicolon_belongs_to_non_sql_line(statement: &str, semicolon_start: usize) -> bool {
        let line_start = statement[..semicolon_start]
            .rfind('\n')
            .map_or(0, |idx| idx + 1);
        let line_end = statement[semicolon_start..]
            .find('\n')
            .map_or(statement.len(), |idx| semicolon_start + idx);
        let line = statement[line_start..line_end].trim();

        if line.starts_with("--")
            || line.starts_with('#')
            || Self::is_sqlplus_remark_comment_statement(line)
        {
            return true;
        }

        QueryExecutor::parse_tool_command_if_candidate(line)
            .is_some_and(|command| !matches!(command, ToolCommand::Unsupported { .. }))
    }

    fn trailing_segment_allows_semicolon_terminator(suffix: &str) -> bool {
        let mut rest = suffix.trim_start();
        if rest.is_empty() {
            return true;
        }

        loop {
            let line_end = rest.find('\n').unwrap_or(rest.len());
            let line = rest[..line_end].trim();

            if line.is_empty() {
                rest = rest[line_end..].trim_start();
                if rest.is_empty() {
                    return true;
                }
                continue;
            }

            if line == "/" {
                rest = rest[line_end..].trim_start();
                if rest.is_empty() {
                    return true;
                }
                continue;
            }

            if line.starts_with("--")
                || line.starts_with('#')
                || Self::is_sqlplus_remark_comment_statement(line)
            {
                rest = rest[line_end..].trim_start();
                if rest.is_empty() {
                    return true;
                }
                continue;
            }

            return false;
        }
    }

    #[cfg(test)]
    fn statement_ends_with_semicolon(statement: &str) -> bool {
        let tokens = Self::tokenize_sql(statement);
        if Self::statement_ends_with_semicolon_tokens(&tokens) {
            return true;
        }

        let trimmed = statement.trim_end();
        if trimmed.is_empty() {
            return false;
        }

        let spans = super::query_text::tokenize_sql_spanned(trimmed);
        spans
            .iter()
            .rev()
            .find(|span| matches!(&span.token, SqlToken::Symbol(sym) if sym == ";"))
            .is_some_and(|span| {
                Self::trailing_segment_allows_semicolon_terminator(&trimmed[span.end..])
            })
    }

    fn statement_ends_with_semicolon_tokens(tokens: &[SqlToken]) -> bool {
        let mut saw_sqlplus_slash = false;
        for token in tokens.iter().rev() {
            match token {
                SqlToken::Comment(_) => continue,
                SqlToken::Symbol(sym) if sym == "/" => {
                    saw_sqlplus_slash = true;
                }
                SqlToken::Symbol(sym) if sym == ";" => return true,
                _ => return saw_sqlplus_slash,
            }
        }
        false
    }

    fn should_append_missing_statement_terminator(tokens: &[SqlToken]) -> bool {
        let trailing_tokens = Self::tokens_after_last_semicolon(tokens);
        if !Self::delimiter_pairs_are_balanced(trailing_tokens) {
            return false;
        }

        let mut trailing_token = None;

        for token in trailing_tokens.iter().rev() {
            match token {
                SqlToken::Comment(comment) if !Self::is_mysql_executable_comment_token(comment) => {
                    continue;
                }
                SqlToken::Symbol(sym) if sym == "/" => continue,
                other => {
                    trailing_token = Some(other);
                    break;
                }
            }
        }

        trailing_token.is_some_and(Self::token_can_terminate_statement)
    }

    fn tokens_after_last_semicolon(tokens: &[SqlToken]) -> &[SqlToken] {
        let mut last_semicolon_idx = None;

        for (idx, token) in tokens.iter().enumerate() {
            if matches!(token, SqlToken::Symbol(sym) if sym == ";") {
                last_semicolon_idx = Some(idx);
            }
        }

        if let Some(semicolon_idx) = last_semicolon_idx {
            &tokens[semicolon_idx.saturating_add(1)..]
        } else {
            tokens
        }
    }

    fn delimiter_pairs_are_balanced(tokens: &[SqlToken]) -> bool {
        let mut delimiter_stack: Vec<&'static str> = Vec::new();

        for token in tokens {
            let SqlToken::Symbol(symbol) = token else {
                continue;
            };

            match symbol.as_str() {
                "(" => delimiter_stack.push(")"),
                "[" => delimiter_stack.push("]"),
                "{" => delimiter_stack.push("}"),
                ")" | "]" | "}" => {
                    let Some(expected_close) = delimiter_stack.pop() else {
                        return false;
                    };
                    if expected_close != symbol.as_str() {
                        return false;
                    }
                }
                _ => {}
            }
        }

        delimiter_stack.is_empty()
    }

    fn token_can_terminate_statement(token: &SqlToken) -> bool {
        match token {
            SqlToken::Word(word) => Self::word_token_can_terminate_statement(word),
            SqlToken::String(literal) => Self::string_token_can_terminate_statement(literal),
            SqlToken::Symbol(symbol) => matches!(symbol.as_str(), ")" | "]"),
            SqlToken::Comment(comment) => Self::is_mysql_executable_comment_token(comment),
        }
    }

    fn word_token_can_terminate_statement(word: &str) -> bool {
        if word.starts_with('"') {
            return word.ends_with('"') && word.len() > 1;
        }

        if word.starts_with("<<") {
            return word.ends_with(">>") && word.len() > 3;
        }

        true
    }

    fn string_token_can_terminate_statement(literal: &str) -> bool {
        if literal.starts_with('$') {
            return Self::has_closed_dollar_quote(literal);
        }

        literal.ends_with('\'') && literal.len() > 1
    }

    fn has_closed_dollar_quote(literal: &str) -> bool {
        let bytes = literal.as_bytes();
        if bytes.first().copied() != Some(b'$') {
            return false;
        }

        let close_tag_idx =
            bytes.iter().enumerate().skip(1).find_map(
                |(idx, byte)| {
                    if *byte == b'$' {
                        Some(idx)
                    } else {
                        None
                    }
                },
            );

        let Some(close_tag_idx) = close_tag_idx else {
            return false;
        };

        let tag_end = close_tag_idx + 1;
        let Some(tag) = literal.get(..tag_end) else {
            return false;
        };

        literal.len() >= tag.len().saturating_mul(2) && literal.ends_with(tag)
    }

    fn classify_comment_attachment(
        has_rendered_content: bool,
        at_line_start: bool,
        has_leading_newline: bool,
        is_multiline_block_comment: bool,
    ) -> CommentAttachment {
        if !has_rendered_content {
            return CommentAttachment::FileHeader;
        }

        if has_leading_newline {
            return CommentAttachment::Next;
        }

        if at_line_start {
            if is_multiline_block_comment {
                CommentAttachment::Block
            } else {
                CommentAttachment::Next
            }
        } else {
            CommentAttachment::Previous
        }
    }

    fn mysql_identifier_requires_quotes(segment: &str) -> bool {
        if segment.is_empty() {
            return true;
        }

        let mut chars = segment.chars();
        let Some(first) = chars.next() else {
            return true;
        };

        if first.is_ascii_digit() || !(first.is_ascii_alphabetic() || matches!(first, '_' | '$')) {
            return true;
        }

        if chars.any(|ch| !(ch.is_ascii_alphanumeric() || matches!(ch, '_' | '$'))) {
            return true;
        }

        sql_text::is_mysql_sql_keyword(&segment.to_ascii_uppercase())
    }

    fn quote_mysql_identifier(identifier: &str) -> String {
        format!("`{}`", identifier.replace('`', "``"))
    }

    fn format_mysql_identifier(identifier: &str) -> String {
        let trimmed = identifier.trim();
        if trimmed.is_empty() {
            return String::new();
        }

        if trimmed.contains('`') || trimmed.contains('"') || trimmed.contains('\'') {
            return trimmed.to_string();
        }

        if trimmed.contains('.') {
            return trimmed
                .split('.')
                .map(|segment| {
                    if Self::mysql_identifier_requires_quotes(segment) {
                        Self::quote_mysql_identifier(segment)
                    } else {
                        segment.to_string()
                    }
                })
                .collect::<Vec<_>>()
                .join(".");
        }

        if Self::mysql_identifier_requires_quotes(trimmed) {
            Self::quote_mysql_identifier(trimmed)
        } else {
            trimmed.to_string()
        }
    }

    pub(super) fn format_tool_command(command: &ToolCommand) -> String {
        match command {
            ToolCommand::Var { name, data_type } => {
                format!("VAR {} {}", name, data_type.display())
            }
            ToolCommand::Print { name } => match name {
                Some(name) => format!("PRINT {}", name),
                None => "PRINT".to_string(),
            },
            ToolCommand::SetServerOutput {
                enabled,
                size,
                unlimited,
            } => {
                if !*enabled {
                    "SET SERVEROUTPUT OFF".to_string()
                } else if *unlimited {
                    "SET SERVEROUTPUT ON SIZE UNLIMITED".to_string()
                } else if let Some(size) = size {
                    format!("SET SERVEROUTPUT ON SIZE {}", size)
                } else {
                    "SET SERVEROUTPUT ON".to_string()
                }
            }
            ToolCommand::ShowErrors {
                object_type,
                object_name,
            } => {
                if let (Some(obj_type), Some(obj_name)) = (object_type, object_name) {
                    format!("SHOW ERRORS {} {}", obj_type, obj_name)
                } else {
                    "SHOW ERRORS".to_string()
                }
            }
            ToolCommand::ShowUser => "SHOW USER".to_string(),
            ToolCommand::ShowAll => "SHOW ALL".to_string(),
            ToolCommand::Describe { name } => format!("DESCRIBE {}", name),
            ToolCommand::Prompt { text } => {
                if text.trim().is_empty() {
                    "PROMPT".to_string()
                } else {
                    format!("PROMPT {}", text)
                }
            }
            ToolCommand::Pause { message } => match message {
                Some(text) if !text.trim().is_empty() => format!("PAUSE {}", text),
                _ => "PAUSE".to_string(),
            },
            ToolCommand::Accept { name, prompt } => match prompt {
                Some(text) => {
                    format!(
                        "ACCEPT {} PROMPT '{}'",
                        name,
                        Self::escape_sql_literal(text)
                    )
                }
                None => format!("ACCEPT {}", name),
            },
            ToolCommand::Define { name, value } => format!("DEFINE {} = {}", name, value),
            ToolCommand::Undefine { name } => format!("UNDEFINE {}", name),
            ToolCommand::ColumnNewValue {
                column_name,
                variable_name,
            } => format!("COLUMN {} NEW_VALUE {}", column_name, variable_name),
            ToolCommand::BreakOn { column_name } => format!("BREAK ON {}", column_name),
            ToolCommand::BreakOff => "BREAK OFF".to_string(),
            ToolCommand::ClearBreaks => "CLEAR BREAKS".to_string(),
            ToolCommand::ClearComputes => "CLEAR COMPUTES".to_string(),
            ToolCommand::ClearBreaksComputes => "CLEAR BREAKS\nCLEAR COMPUTES".to_string(),
            ToolCommand::Compute {
                mode,
                of_column,
                on_column,
            } => {
                let mode_text = match mode {
                    crate::db::ComputeMode::Sum => "SUM",
                    crate::db::ComputeMode::Count => "COUNT",
                };
                match (of_column.as_deref(), on_column.as_deref()) {
                    (Some(of_col), Some(on_col)) => {
                        format!("COMPUTE {} OF {} ON {}", mode_text, of_col, on_col)
                    }
                    _ => format!("COMPUTE {}", mode_text),
                }
            }
            ToolCommand::ComputeOff => "COMPUTE OFF".to_string(),
            ToolCommand::SetErrorContinue { enabled } => {
                if *enabled {
                    "SET ERRORCONTINUE ON".to_string()
                } else {
                    "SET ERRORCONTINUE OFF".to_string()
                }
            }
            ToolCommand::SetAutoCommit { enabled } => {
                if *enabled {
                    "SET AUTOCOMMIT ON".to_string()
                } else {
                    "SET AUTOCOMMIT OFF".to_string()
                }
            }
            ToolCommand::SetDefine {
                enabled,
                define_char,
            } => {
                if let Some(ch) = define_char {
                    format!("SET DEFINE '{}'", ch)
                } else if *enabled {
                    "SET DEFINE ON".to_string()
                } else {
                    "SET DEFINE OFF".to_string()
                }
            }
            ToolCommand::SetScan { enabled } => {
                if *enabled {
                    "SET SCAN ON".to_string()
                } else {
                    "SET SCAN OFF".to_string()
                }
            }
            ToolCommand::SetVerify { enabled } => {
                if *enabled {
                    "SET VERIFY ON".to_string()
                } else {
                    "SET VERIFY OFF".to_string()
                }
            }
            ToolCommand::SetEcho { enabled } => {
                if *enabled {
                    "SET ECHO ON".to_string()
                } else {
                    "SET ECHO OFF".to_string()
                }
            }
            ToolCommand::SetTiming { enabled } => {
                if *enabled {
                    "SET TIMING ON".to_string()
                } else {
                    "SET TIMING OFF".to_string()
                }
            }
            ToolCommand::SetFeedback { enabled } => {
                if *enabled {
                    "SET FEEDBACK ON".to_string()
                } else {
                    "SET FEEDBACK OFF".to_string()
                }
            }
            ToolCommand::SetHeading { enabled } => {
                if *enabled {
                    "SET HEADING ON".to_string()
                } else {
                    "SET HEADING OFF".to_string()
                }
            }
            ToolCommand::SetPageSize { size } => format!("SET PAGESIZE {}", size),
            ToolCommand::SetLineSize { size } => format!("SET LINESIZE {}", size),
            ToolCommand::SetTrimSpool { enabled } => {
                if *enabled {
                    "SET TRIMSPOOL ON".to_string()
                } else {
                    "SET TRIMSPOOL OFF".to_string()
                }
            }
            ToolCommand::SetTrimOut { enabled } => {
                if *enabled {
                    "SET TRIMOUT ON".to_string()
                } else {
                    "SET TRIMOUT OFF".to_string()
                }
            }
            ToolCommand::SetSqlBlankLines { enabled } => {
                if *enabled {
                    "SET SQLBLANKLINES ON".to_string()
                } else {
                    "SET SQLBLANKLINES OFF".to_string()
                }
            }
            ToolCommand::SetTab { enabled } => {
                if *enabled {
                    "SET TAB ON".to_string()
                } else {
                    "SET TAB OFF".to_string()
                }
            }
            ToolCommand::SetColSep { separator } => format!("SET COLSEP {}", separator),
            ToolCommand::SetNull { null_text } => format!("SET NULL {}", null_text),
            ToolCommand::Spool { path, append } => match path {
                Some(path) if *append => format!("SPOOL {} APPEND", path),
                Some(path) => format!("SPOOL {}", path),
                None if *append => "SPOOL APPEND".to_string(),
                None => "SPOOL OFF".to_string(),
            },
            ToolCommand::WheneverSqlError { exit, action } => {
                let mode = if *exit { "EXIT" } else { "CONTINUE" };
                match action.as_deref() {
                    Some(extra) if !extra.trim().is_empty() => {
                        format!("WHENEVER SQLERROR {} {}", mode, extra.trim())
                    }
                    _ => format!("WHENEVER SQLERROR {}", mode),
                }
            }
            ToolCommand::WheneverOsError { exit } => {
                if *exit {
                    "WHENEVER OSERROR EXIT".to_string()
                } else {
                    "WHENEVER OSERROR CONTINUE".to_string()
                }
            }
            ToolCommand::Exit => "EXIT".to_string(),
            ToolCommand::Quit => "QUIT".to_string(),
            ToolCommand::RunScript {
                path,
                relative_to_caller,
            } => {
                if *relative_to_caller {
                    format!("@@{}", path)
                } else {
                    format!("@{}", path)
                }
            }
            ToolCommand::Connect {
                username,
                password,
                host,
                port,
                service_name,
            } => {
                // 자동 포맷팅 결과를 AI(Codex/Claude)가 재마스킹하지 않도록 실제 비밀번호를 그대로 유지한다.
                if host.trim().is_empty() {
                    format!("CONNECT {}/{}@{}", username, password, service_name)
                } else {
                    format!(
                        "CONNECT {}/{}@{}:{}/{}",
                        username, password, host, port, service_name
                    )
                }
            }
            ToolCommand::Disconnect => "DISCONNECT".to_string(),
            // MySQL-specific commands — format as-is
            ToolCommand::Use { database } => {
                format!("USE {};", Self::format_mysql_identifier(database))
            }
            ToolCommand::ShowDatabases => "SHOW DATABASES".to_string(),
            ToolCommand::ShowTables => "SHOW TABLES".to_string(),
            ToolCommand::ShowColumns { table, schema } => match schema {
                Some(schema_name) => {
                    format!(
                        "SHOW COLUMNS FROM {} FROM {}",
                        Self::format_mysql_identifier(table),
                        Self::format_mysql_identifier(schema_name)
                    )
                }
                None => format!("SHOW COLUMNS FROM {}", Self::format_mysql_identifier(table)),
            },
            ToolCommand::ShowCreateTable { table } => {
                format!("SHOW CREATE TABLE {}", Self::format_mysql_identifier(table))
            }
            ToolCommand::ShowProcessList => "SHOW PROCESSLIST".to_string(),
            ToolCommand::ShowVariables { filter } => match filter {
                Some(f) => format!("SHOW VARIABLES LIKE '{}'", Self::escape_sql_literal(f)),
                None => "SHOW VARIABLES".to_string(),
            },
            ToolCommand::ShowStatus { filter } => match filter {
                Some(f) => format!("SHOW STATUS LIKE '{}'", Self::escape_sql_literal(f)),
                None => "SHOW STATUS".to_string(),
            },
            ToolCommand::MysqlDelimiter { delimiter } => format!("DELIMITER {}", delimiter),
            ToolCommand::ShowWarnings => "SHOW WARNINGS".to_string(),
            ToolCommand::MysqlShowErrors => "SHOW ERRORS".to_string(),
            ToolCommand::MysqlSource { path } => format!("SOURCE {}", path),
            ToolCommand::Unsupported { raw, .. } => raw.clone(),
        }
    }

    fn format_statement(
        input: &FormatterStatementInput<'_>,
        select_list_break_state_on_start: SelectListBreakState,
    ) -> FormatterStatementRenderResult {
        let statement = input.statement;
        let tokens = input.tokens.as_slice();
        let token_spans = input.token_spans.as_slice();
        let mysql_compatible = input.mysql_compatible;

        if Self::is_sqlplus_remark_comment_statement(statement) {
            return FormatterStatementRenderResult {
                formatted: statement.to_string(),
                has_code: false,
                ends_with_semicolon: false,
                should_append_missing_terminator: false,
                is_create_trigger_statement: input.is_create_trigger_statement,
            };
        }

        if let Some(formatted) = Self::format_create_table(tokens, mysql_compatible) {
            return FormatterStatementRenderResult {
                formatted,
                has_code: Self::statement_has_code(statement, tokens),
                ends_with_semicolon: Self::statement_ends_with_semicolon_tokens(tokens),
                should_append_missing_terminator: Self::should_append_missing_statement_terminator(
                    tokens,
                ),
                is_create_trigger_statement: input.is_create_trigger_statement,
            };
        }

        let clause_keywords = FORMAT_CLAUSE_KEYWORDS;
        let join_modifiers = FORMAT_JOIN_MODIFIER_KEYWORDS;
        let join_keyword = "JOIN";
        let outer_keyword = "OUTER";
        let condition_keywords = FORMAT_CONDITION_KEYWORDS; // ELSE handled separately for IF blocks
                                                            // BEGIN is handled separately to support DECLARE ... BEGIN ... END blocks
                                                            // CASE is handled separately for SELECT vs PL/SQL context
                                                            // LOOP is handled separately for FOR ... LOOP on same line
        let block_start_keywords = FORMAT_BLOCK_START_KEYWORDS;
        let block_end_qualifiers = FORMAT_BLOCK_END_QUALIFIER_KEYWORDS; // END LOOP, END IF, END CASE, END REPEAT
        let owned_metadata;
        let metadata = if let Some(metadata) = input.metadata.as_ref() {
            metadata
        } else {
            owned_metadata = Self::build_statement_metadata(tokens);
            &owned_metadata
        };
        let _meaningful_token_links = &metadata.meaningful_token_links;
        let _statement_word_links = &metadata.statement_word_links;
        let matching_paren_close_indices = &metadata.matching_paren_close_indices;
        let paren_body_analyses = &metadata.paren_body_analyses;
        let fetch_into_has_multiple_targets = &metadata.fetch_into_has_multiple_targets;
        let within_group_analytic_groups = &metadata.within_group_analytic_groups;

        let mut out = String::with_capacity(
            statement
                .len()
                .saturating_add(statement.len() >> 2)
                .saturating_add(16),
        );
        let mut indent_level = 0usize;
        let mut suppress_comma_break_depth = 0usize;
        let mut format_stack = FormatFrameStack::default();
        let mut at_line_start = true;
        let mut needs_space = false;
        let mut has_rendered_content = false;
        let mut line_indent = 0usize;
        let mut line_start_token_paren_depth = 0usize;
        let mut delimiter_frame_state = DelimiterFrameState::default();
        let mut line_delimiter_frame_state = DelimiterFrameState::default();
        let mut line_closes_delimiter_frame_below_start = false;
        let mut line_start_delimiter_snapshot = DelimiterLineStartSnapshot::default();
        let current_rendered_line_paren_depth = Cell::new(0usize);
        let mut join_modifier_active = false;
        let mut after_for_while = false; // Track FOR/WHILE for LOOP on same line
        let mut prev_word_idx: Option<usize> = None;
        let mut recent_statement_word_indices: VecDeque<usize> = VecDeque::with_capacity(8);
        let mut pending_package_member_separator = false;
        let mut active_package_member_name: Option<String> = None;
        let mut mysql_handler_state = MySqlHandlerFormatState::None;
        let mut select_list_break_state = select_list_break_state_on_start;
        let mut exit_condition_state = ExitConditionState::None;
        let mut insert_all_branch_body_indent: Option<usize> = None;
        // MySQL ON DUPLICATE KEY UPDATE: tracks when VALUES() is a function, not a clause
        let mut on_duplicate_key_update_active = false;
        let mut inline_comment_continuation_state = InlineCommentContinuationState::None;
        let is_package_body_statement = input.is_package_body_statement;
        let statement_has_apply = input.statement_has_apply;
        let mut query_apply_context_tracker = LiveQueryApplyTracker::default();
        let mut statement_tail_state = FormatterTailState::new();
        let mut token_line_cursor =
            FormatterTokenLineCursor::new(statement.as_bytes(), token_spans);
        let mut token_context_cursor = FormatterTokenContextCursor::new(tokens);
        let mut rendered_line_tracker = RenderedLineTracker::default();
        let mut comment_prefix_text = String::new();
        let newline_with = |out: &mut String,
                            indent_level: usize,
                            extra: usize,
                            at_line_start: &mut bool,
                            needs_space: &mut bool,
                            line_indent: &mut usize| {
            if !out.is_empty() && !out.ends_with('\n') {
                let current_line_is_whitespace_only = out
                    .rsplit('\n')
                    .next()
                    .is_some_and(|line| !line.is_empty() && line.bytes().all(|b| b == b' '));
                if current_line_is_whitespace_only {
                    if let Some(last_newline_idx) = out.rfind('\n') {
                        out.truncate(last_newline_idx + 1);
                    } else {
                        out.clear();
                    }
                } else {
                    out.push('\n');
                }
            }
            *line_indent = indent_level + extra;
            *at_line_start = true;
            *needs_space = false;
            current_rendered_line_paren_depth.set(0);
        };
        macro_rules! base_indent {
            ($indent_level:expr) => {
                format_stack.open_cursor_state().base_indent($indent_level)
            };
        }
        macro_rules! current_clause {
            () => {
                format_stack.current_clause()
            };
        }
        macro_rules! open_cursor_state {
            () => {
                format_stack.open_cursor_state()
            };
        }
        macro_rules! select_list_layout_state {
            () => {
                format_stack.select_list_layout_state()
            };
        }
        macro_rules! statement_has_with_clause {
            () => {
                format_stack.statement_has_with_clause()
            };
        }
        macro_rules! query_body_clause_base_depth {
            () => {
                format_stack.query_body_clause_base_depth()
            };
        }
        macro_rules! clause_indent {
            ($indent_level:expr, $open_cursor_state:expr, $keyword:expr, $open_for_select_active:expr, $in_cursor_sql:expr $(,)?) => {{
                let _ = (
                    $open_cursor_state,
                    $keyword,
                    $open_for_select_active,
                    $in_cursor_sql,
                );
                base_indent!($indent_level)
            }};
        }
        macro_rules! list_item_indent {
            ($indent_level:expr, $open_cursor_state:expr, $select_list_layout_state:expr $(,)?) => {{
                let _ = ($open_cursor_state, $select_list_layout_state);
                let base = base_indent!($indent_level);
                format_stack
                    .select_list_layout_state()
                    .indentation_or(base + 1)
            }};
        }
        macro_rules! clause_list_indent {
            ($indent_level:expr, $open_cursor_state:expr, $select_list_layout_state:expr, $current_clause:expr, $merge_active:expr $(,)?) => {{
                let _ = (
                    $open_cursor_state,
                    $select_list_layout_state,
                    $current_clause,
                    $merge_active,
                );
                let base = base_indent!($indent_level);
                list_item_indent!($indent_level, $open_cursor_state, $select_list_layout_state)
                    .max(base + 1)
            }};
        }
        macro_rules! active_list_indent {
            ($indent_level:expr, $open_cursor_state:expr, $select_list_layout_state:expr, $current_clause:expr, $merge_active:expr, $in_column_list:expr $(,)?) => {{
                let _ = ($open_cursor_state, $select_list_layout_state);
                if $in_column_list {
                    base_indent!($indent_level)
                } else {
                    clause_list_indent!(
                        $indent_level,
                        $open_cursor_state,
                        $select_list_layout_state,
                        $current_clause,
                        $merge_active
                    )
                }
            }};
        }
        let trim_trailing_space = |out: &mut String| {
            while out.ends_with(' ') {
                out.pop();
            }
        };
        let word_upper_at = |lookahead_idx: usize| input.word_upper(lookahead_idx);
        let token_gap_contains_newline = |from_idx: usize, to_idx: usize| {
            let Some(from_span) = token_spans.get(from_idx) else {
                return false;
            };
            let Some(to_span) = token_spans.get(to_idx) else {
                return false;
            };
            statement
                .as_bytes()
                .get(from_span.end..to_span.start)
                .is_some_and(|gap| gap.contains(&b'\n'))
        };
        let token_starts_on_same_line_after =
            |from_idx: usize, to_idx: usize| !token_gap_contains_newline(from_idx, to_idx);
        macro_rules! set_current_clause {
            ($value:expr) => {{
                format_stack.set_current_clause_ref($value);
            }};
        }
        macro_rules! clear_select_list_layout_state {
            () => {{
                let mut next_layout = format_stack.select_list_layout_state();
                next_layout.clear();
                format_stack.set_select_list_layout_state(next_layout);
            }};
        }
        macro_rules! activate_select_list_layout_state {
            ($anchor:expr, $indent:expr) => {{
                let mut next_layout = format_stack.select_list_layout_state();
                next_layout.activate($anchor, $indent);
                format_stack.set_select_list_layout_state(next_layout);
            }};
        }
        macro_rules! set_select_list_layout_state {
            ($value:expr) => {{
                format_stack.set_select_list_layout_state($value);
            }};
        }
        macro_rules! set_open_cursor_state {
            ($value:expr) => {{
                format_stack.set_open_cursor_state($value);
            }};
        }
        macro_rules! set_statement_has_with_clause {
            ($value:expr) => {{
                format_stack.set_statement_has_with_clause($value);
            }};
        }
        macro_rules! set_query_body_clause_base_depth {
            ($value:expr) => {{
                format_stack.set_query_body_clause_base_depth($value);
            }};
        }
        macro_rules! force_select_list_newline {
            ($out:expr) => {{
                let next_layout = format_stack
                    .select_list_layout_state()
                    .force_newline_if_possible($out);
                format_stack.set_select_list_layout_state(next_layout);
            }};
        }
        macro_rules! construct_flag_active {
            ($kind:ident) => {
                format_stack.construct_flag_is_active(ConstructFlagKind::$kind)
            };
        }
        macro_rules! construct_flag_active_at_paren_depth {
            ($kind:ident, $paren_depth:expr) => {
                format_stack
                    .construct_flag_is_active_at_paren_depth(ConstructFlagKind::$kind, $paren_depth)
            };
        }
        macro_rules! activate_construct_flag {
            ($kind:ident, $scope:expr) => {{
                format_stack.activate_construct_flag(ConstructFlagKind::$kind, $scope);
            }};
        }
        macro_rules! deactivate_construct_flag {
            ($kind:ident) => {{
                let _ = format_stack.deactivate_construct_flag(ConstructFlagKind::$kind);
            }};
        }
        macro_rules! deactivate_construct_flag_at_or_above_paren_depth {
            ($kind:ident, $paren_depth:expr) => {{
                format_stack.deactivate_construct_flag_at_or_above_paren_depth(
                    ConstructFlagKind::$kind,
                    $paren_depth,
                );
            }};
        }
        macro_rules! construct_value_as_deref {
            ($kind:ident) => {
                format_stack.construct_value_as_deref(ConstructValueKind::$kind)
            };
        }
        macro_rules! construct_value_is_some {
            ($kind:ident) => {
                format_stack.construct_value_is_some(ConstructValueKind::$kind)
            };
        }
        macro_rules! replace_construct_value {
            ($kind:ident, $scope:expr, $value:expr) => {{
                format_stack.replace_construct_value(ConstructValueKind::$kind, $scope, $value);
            }};
        }
        macro_rules! clear_construct_value {
            ($kind:ident) => {{
                format_stack.clear_construct_value(ConstructValueKind::$kind);
            }};
        }

        let mut mysql_begin_not_atomic_header_open = false;
        let mut idx = 0;
        let mut lexical_paren_depth_state = ParenDepthState::default();
        let mut previous_token_end_newline_count = 0usize;
        while idx < tokens.len() {
            if at_line_start {
                comment_prefix_text.clear();
            }
            let rendered_token_idx = idx;
            let current_token_paren_depth = lexical_paren_depth_state.depth();
            let current_scope = format_stack.sync_for_token_entry(&mut indent_level);
            let prev_word_upper = prev_word_idx.and_then(|prev_idx| input.word_upper(prev_idx));
            let prev_word_upper = prev_word_upper.as_deref();
            let top_level_select_item_context = format_stack.all_paren_frames_are_query_like();
            if top_level_select_item_context && select_list_layout_state!().has_active_indent() {
                let current_clause_name = if matches!(format_stack.current_clause(), Some("SET")) {
                    "SET"
                } else {
                    "SELECT"
                };
                let token_keeps_clause_structural = match tokens.get(idx) {
                    Some(SqlToken::Word(word)) => {
                        let upper = ascii_uppercase_cow(word);
                        Self::is_dml_clause_starter(upper.as_ref())
                            || crate::sql_text::is_format_set_operator_keyword(upper.as_ref())
                    }
                    _ => false,
                };
                if !token_keeps_clause_structural {
                    set_current_clause!(Some(current_clause_name));
                }
            }
            if at_line_start {
                if let Some(frame) = format_stack.last_paren_mut() {
                    frame.record_body_indent(line_indent);
                }
            }
            let in_plsql_block = format_stack.plsql_context_is_active();
            let current_query_has_apply = query_apply_context_tracker.context();
            if let Some(label_indent) = format_stack.pending_plsql_label_body_indent() {
                if !matches!(tokens[idx], SqlToken::Comment(_)) {
                    if !at_line_start && out.ends_with('\n') {
                        at_line_start = true;
                    }
                    if at_line_start {
                        line_indent = line_indent.max(label_indent);
                    }
                }
            }
            if at_line_start && !matches!(tokens[idx], SqlToken::Comment(_)) {
                if let InlineCommentContinuationState::Operand { indent } =
                    inline_comment_continuation_state
                {
                    line_indent = indent;
                    inline_comment_continuation_state = InlineCommentContinuationState::None;
                }
            }
            let current_token_profile = token_line_cursor.profile(idx);
            let mut token_context = token_context_cursor.advance_to(idx);
            token_context.gap_from_prev_has_newline =
                current_token_profile.start_newline_count > previous_token_end_newline_count;
            let token_gap_has_newline = token_context.gap_from_prev_has_newline;
            let immediate_prev_prev_token = token_context.immediate_prev_prev(tokens);
            let immediate_prev_token = token_context.immediate_prev(tokens);
            let immediate_next_token = token_context.immediate_next(tokens);
            let immediate_next_next_token = token_context.immediate_next_next(tokens);
            let prev_non_comment_idx = token_context.prev_non_comment_idx();
            let prev_non_comment_upper = prev_non_comment_idx.and_then(word_upper_at);
            let prev_non_comment_upper = prev_non_comment_upper.as_deref();
            let next_non_comment_idx = token_context.next_non_comment_idx();
            let second_next_non_comment_idx = token_context.second_next_non_comment_idx();
            let third_next_non_comment_idx = token_context.third_next_non_comment_idx();
            let next_words = token_context.next_words(tokens);
            let next_word = next_words[0];
            let third_word = next_words[1];
            let next_non_comment = token_context.next_non_comment_token(tokens);
            let second_next_non_comment_token = token_context.second_next_non_comment_token(tokens);
            let loop_previous_non_comment_token = token_context.prev_non_comment_token(tokens);
            let loop_second_previous_non_comment_token =
                token_context.second_prev_non_comment_token(tokens);
            let next_word_is =
                |expected: &str| next_word.is_some_and(|word| word.eq_ignore_ascii_case(expected));
            let marks_multiline_child_continuation = match &tokens[idx] {
                SqlToken::Comment(_) => false,
                SqlToken::Symbol(sym) if sym == ")" || sym == "," => false,
                _ => true,
            };
            if marks_multiline_child_continuation {
                if let Some(frame) = format_stack.last_paren_mut() {
                    if frame.has_pending_multiline_child_continuation()
                        || frame.post_multiline_child_tail_token_count > 0
                    {
                        frame.note_continuation_token();
                    }
                }
            }
            if at_line_start {
                line_start_token_paren_depth = current_token_paren_depth;
                line_start_delimiter_snapshot =
                    delimiter_frame_state.line_start_snapshot(line_start_token_paren_depth);
                line_delimiter_frame_state = line_start_delimiter_snapshot.frame_state();
                line_closes_delimiter_frame_below_start = false;
            }

            match &tokens[idx] {
                SqlToken::Word(word) => {
                    let upper = ascii_uppercase_cow(word);
                    let upper = upper.as_ref();
                    let opens_unterminated_plsql_label =
                        word.starts_with("<<") && !word.ends_with(">>");
                    if opens_unterminated_plsql_label {
                        format_stack.set_pending_plsql_label_body_indent(
                            current_scope,
                            base_indent!(indent_level),
                        );
                    } else if word.ends_with(">>")
                        || format_stack.pending_plsql_label_body_indent().is_some()
                    {
                        let _ = format_stack.clear_pending_plsql_label_body_indent();
                    }
                    let mysql_labeled_block_header_word = mysql_compatible
                        && matches!(immediate_next_token, Some(SqlToken::Symbol(sym)) if sym == ":")
                        && immediate_next_next_token.is_some_and(|token| {
                            matches!(
                                token,
                                SqlToken::Word(next_word)
                                    if crate::sql_text::is_mysql_block_label_target(next_word)
                            )
                        });
                    let mysql_labeled_block_target_word = mysql_compatible
                        && crate::sql_text::is_mysql_block_label_target(upper)
                        && matches!(immediate_prev_token, Some(SqlToken::Symbol(sym)) if sym == ":")
                        && immediate_prev_prev_token
                            .is_some_and(|token| matches!(token, SqlToken::Word(_)));
                    let is_formatter_keyword = |value: &str| {
                        sql_text::is_oracle_sql_keyword(value)
                            || (mysql_compatible && sql_text::is_mysql_sql_keyword(value))
                    };
                    let mysql_keyword_identifier = mysql_compatible
                        && Self::mysql_keyword_should_render_as_identifier_from_context(
                            word,
                            upper,
                            format_stack.current_clause(),
                            on_duplicate_key_update_active,
                            loop_previous_non_comment_token,
                            next_non_comment,
                            &next_words,
                        );
                    let as_belongs_to_constructor_result_clause = upper == "AS"
                        && matches!(prev_word_upper, Some("SELF"))
                        && next_word_is("RESULT");
                    let mysql_begin_not_atomic_header = mysql_compatible
                        && upper == "BEGIN"
                        && next_word_is("NOT")
                        && third_word.is_some_and(|word| word.eq_ignore_ascii_case("ATOMIC"));
                    let inside_function_local_non_query_paren =
                        Self::has_compact_non_query_frame_since_last_query(&format_stack);
                    let with_cte_keyword_inside_function_local =
                        upper == "WITH" && inside_function_local_non_query_paren;
                    let with_plsql_body_starts_here = matches!(upper, "AS" | "IS")
                        && !as_belongs_to_constructor_result_clause
                        && format_stack.with_cte_collecting_routine_declaration_body_start();
                    if !with_cte_keyword_inside_function_local {
                        format_stack.with_cte_on_word(upper, prev_word_upper);
                    }
                    let with_plsql_declaration_header =
                        sql_text::is_with_plsql_declaration_keyword(upper)
                            && (prev_word_upper == Some("WITH")
                                || format_stack.with_cte_collecting_plsql_declaration_header());
                    let in_sql_case_clause = matches!(
                        format_stack.current_clause(),
                        Some(
                            "SELECT"
                                | "WHERE"
                                | "ORDER"
                                | "GROUP"
                                | "HAVING"
                                | "VALUES"
                                | "SET"
                                | "INTO"
                                | "RETURNING"
                        )
                    );
                    let is_keyword = is_formatter_keyword(upper);
                    let formatter_keyword_identifier = is_keyword
                        && Self::formatter_keyword_should_render_as_identifier_from_context(
                            upper,
                            format_stack.current_clause(),
                            loop_previous_non_comment_token,
                            next_non_comment,
                            &next_words,
                        );
                    let is_analytic_within_group = upper == "GROUP"
                        && matches!(prev_word_upper, Some("WITHIN"))
                        && within_group_analytic_groups
                            .get(idx)
                            .copied()
                            .unwrap_or(false);
                    let is_within_group = upper == "GROUP"
                        && matches!(prev_word_upper, Some("WITHIN"))
                        && !is_analytic_within_group;
                    let active_phase1_wrapped_owner_kind =
                        Self::active_phase1_wrapped_owner_kind_at_depth(
                            &format_stack,
                            format_stack.paren_depth(),
                        );
                    let mut newline_after_keyword = false;
                    let mut newline_after_keyword_extra = 0usize;
                    let between_pending = format_stack.between_pending_matches(current_scope);
                    let is_between_and = upper == "AND" && between_pending;
                    let is_exit_when = exit_condition_state.is_exit_when(upper);
                    let should_break_condition = condition_keywords.contains(&upper)
                        && !(is_between_and
                            || is_exit_when
                            || Self::suppresses_condition_break(
                                &format_stack,
                                upper,
                                prev_word_upper,
                                format_stack.trigger_header_is_active(),
                            )
                            // ON inside non-subquery parens (e.g. JSON_VALUE ... ON ERROR)
                            // should not cause a condition line break.
                            || (upper == "ON"
                                && inside_function_local_non_query_paren));
                    let top_level_query_body_base_indent = query_body_clause_base_depth!()
                        .filter(|_| format_stack.paren_is_empty())
                        .unwrap_or_else(|| base_indent!(indent_level));
                    let mysql_declare_cursor_for_clause = mysql_compatible
                        && upper == "FOR"
                        && Self::is_mysql_declare_cursor_for_clause_from_indices(
                            tokens,
                            &recent_statement_word_indices,
                        );
                    let mysql_declare_handler_for_clause = mysql_compatible
                        && upper == "FOR"
                        && Self::is_mysql_declare_handler_for_clause_from_indices(
                            tokens,
                            &recent_statement_word_indices,
                        );
                    let mysql_handler_body_line_pending = mysql_handler_state.body_line_pending();
                    let mysql_handler_block_begin = mysql_handler_body_line_pending
                        && upper == "BEGIN"
                        && !mysql_declare_handler_for_clause;
                    let mysql_declare_for_clause =
                        mysql_declare_cursor_for_clause || mysql_declare_handler_for_clause;
                    let mysql_if_exists_modifier = upper == "IF"
                        && Self::is_mysql_if_exists_modifier_from_indices(
                            tokens,
                            next_word,
                            &recent_statement_word_indices,
                        );
                    let mysql_scalar_if_function = mysql_compatible
                        && upper == "IF"
                        && matches!(next_non_comment, Some(SqlToken::Symbol(sym)) if sym == "(")
                        && !mysql_if_exists_modifier
                        && Self::mysql_if_is_scalar_function_from_context(
                            format_stack.current_clause(),
                            on_duplicate_key_update_active,
                            in_plsql_block,
                            at_line_start,
                            loop_previous_non_comment_token,
                        );
                    let is_trigger_event_keyword = format_stack.trigger_header_is_active()
                        && matches!(upper, "INSERT" | "UPDATE" | "DELETE");
                    let is_compound_trigger_timing_header = format_stack
                        .compound_trigger_is_in_outer_body()
                        && format_stack.last_block_kind_is("COMPOUND_TRIGGER")
                        && (matches!(upper, "BEFORE" | "AFTER")
                            && next_word.is_some_and(|word| {
                                word.eq_ignore_ascii_case("STATEMENT")
                                    || word.eq_ignore_ascii_case("EACH")
                            })
                            || (upper == "INSTEAD" && next_word_is("OF")));
                    let is_create_index_on =
                        upper == "ON" && construct_flag_active!(CreateIndexPending);
                    let follows_alias_keyword = matches!(prev_word_upper, Some("AS" | "IS"));
                    let in_table_alias_clause = matches!(
                        format_stack.current_clause(),
                        Some("FROM" | "UPDATE" | "DELETE" | "INTO" | "MERGE" | "USING")
                    );
                    let mut closed_control_header_body_indent = None;
                    let in_select_clause = matches!(format_stack.current_clause(), Some("SELECT"));
                    let next_word_is_clause_keyword =
                        next_non_comment_idx.is_none_or(|token_idx| {
                            word_upper_at(token_idx)
                                .as_deref()
                                .is_some_and(is_formatter_keyword)
                        });
                    let next_token_ends_select_item = matches!(
                        next_non_comment,
                        Some(SqlToken::Symbol(sym)) if matches!(sym.as_str(), "," | ")")
                    ) || next_word_is_clause_keyword;
                    let next_token_ends_from_alias = matches!(
                        next_non_comment,
                        Some(SqlToken::Symbol(sym)) if matches!(sym.as_str(), "," | ")")
                    ) || next_word_is_clause_keyword;
                    let next_token_is_dot =
                        matches!(next_non_comment, Some(SqlToken::Symbol(sym)) if sym == ".");
                    let closes_case_expression =
                        upper == "END" && format_stack.last_block_kind_is("CASE");
                    let treat_control_keyword_as_identifier =
                        sql_text::is_plsql_control_keyword(upper)
                            && !closes_case_expression
                            && !next_word_is("THEN")
                            && (follows_alias_keyword
                                || (in_table_alias_clause && next_token_ends_from_alias)
                                || (in_select_clause
                                    && !next_word_is("AS")
                                    && next_token_ends_select_item)
                                || (next_token_is_dot
                                    && matches!(
                                        format_stack.current_clause(),
                                        Some(
                                            "SELECT"
                                                | "FROM"
                                                | "WHERE"
                                                | "ON"
                                                | "GROUP"
                                                | "HAVING"
                                                | "ORDER"
                                        )
                                    )));
                    let mysql_compound_declare =
                        mysql_compatible && upper == "DECLARE" && in_plsql_block;
                    let follows_alias_control_keyword = follows_alias_keyword
                        && sql_text::is_plsql_control_keyword(upper)
                        && !next_word_is("THEN");
                    let keyword_preserves_original_case =
                        mysql_keyword_identifier || formatter_keyword_identifier;
                    let keyword_suppresses_structural_handling = keyword_preserves_original_case
                        || treat_control_keyword_as_identifier
                        || follows_alias_control_keyword;
                    let mysql_create_function_header_attribute = mysql_compatible
                        && matches!(construct_value_as_deref!(CreateObject), Some("FUNCTION"))
                        && matches!(upper, "RETURNS" | "DETERMINISTIC");
                    let split_control_body_should_close = format_stack
                        .last_scoped_indent(ScopedIndentKind::SplitControlBody)
                        .is_some_and(|frame| frame.scope == current_scope)
                        && ((matches!(upper, "ELSE" | "ELSIF" | "ELSEIF")
                            && format_stack.last_block_kind_is("IF"))
                            || (upper == "END"
                                && format_stack.last_block_kind().is_some_and(|kind| {
                                    matches!(kind, BlockKind::If | BlockKind::While)
                                })));
                    if split_control_body_should_close {
                        let _ = format_stack.deactivate_scoped_indent(
                            ScopedIndentKind::SplitControlBody,
                            &mut indent_level,
                        );
                    }
                    if mysql_handler_block_begin {
                        format_stack.push_scoped_indent(
                            ScopedIndentKind::MySqlHandlerBody,
                            current_scope,
                            1,
                            &mut indent_level,
                        );
                    }
                    if mysql_handler_body_line_pending && !at_line_start {
                        let handler_body_indent = format_stack
                            .mysql_handler_pending_body_indent()
                            .unwrap_or_else(|| {
                                let base = line_indent.max(base_indent!(indent_level));
                                if mysql_handler_block_begin {
                                    base
                                } else {
                                    base.saturating_add(1)
                                }
                            });
                        newline_with(
                            &mut out,
                            handler_body_indent,
                            0,
                            &mut at_line_start,
                            &mut needs_space,
                            &mut line_indent,
                        );
                    }
                    if mysql_handler_body_line_pending
                        && at_line_start
                        && !mysql_handler_block_begin
                    {
                        let handler_body_indent = format_stack
                            .mysql_handler_pending_body_indent()
                            .unwrap_or_else(|| {
                                line_indent
                                    .max(base_indent!(indent_level))
                                    .saturating_add(1)
                            });
                        line_indent = line_indent.max(handler_body_indent);
                    }
                    if mysql_labeled_block_header_word && !at_line_start {
                        newline_with(
                            &mut out,
                            base_indent!(indent_level),
                            0,
                            &mut at_line_start,
                            &mut needs_space,
                            &mut line_indent,
                        );
                    }
                    let should_treat_as_block_start = block_start_keywords.contains(&upper)
                        && !(format_stack.pending_split_end_suffix_indent().is_some()
                            || mysql_if_exists_modifier
                            || mysql_scalar_if_function
                            || mysql_compound_declare
                            || treat_control_keyword_as_identifier
                            || follows_alias_control_keyword);
                    let follows_type_method_modifier =
                        matches!(prev_word_upper, Some("MEMBER" | "STATIC" | "CONSTRUCTOR"));
                    let at_package_body_member_depth =
                        is_package_body_statement && indent_level == 1;
                    if upper == "END" && !treat_control_keyword_as_identifier {
                        let end_word_idx = idx;
                        let mut last_rendered_end_word_idx = end_word_idx;
                        let active_end_block = format_stack.last_block_kind();
                        let end_qualifier_idx = next_non_comment_idx.filter(|lookahead_idx| {
                            matches!(tokens.get(*lookahead_idx), Some(SqlToken::Word(_)))
                                && token_starts_on_same_line_after(idx, *lookahead_idx)
                        });
                        let end_qualifier = end_qualifier_idx.and_then(word_upper_at);
                        let split_end_qualifier = if end_qualifier.is_none() {
                            next_non_comment_idx
                                .filter(|lookahead_idx| {
                                    token_gap_contains_newline(idx, *lookahead_idx)
                                })
                                .and_then(word_upper_at)
                        } else {
                            None
                        };
                        // Check if this is END LOOP, END IF, END CASE, etc.
                        let mut end_tail: Vec<String> = Vec::new();
                        if let Some(qualifier) = end_qualifier.as_deref() {
                            match qualifier {
                                "LOOP" | "IF" | "CASE" | "REPEAT" | "FOR" | "WHILE" => {
                                    let qualifier_belongs_to_case_end = active_end_block
                                        == Some(BlockKind::Case)
                                        && qualifier == "CASE";
                                    let qualifier_is_case_expression_follower = active_end_block
                                        == Some(BlockKind::Case)
                                        && qualifier != "CASE";
                                    if qualifier_belongs_to_case_end
                                        || !qualifier_is_case_expression_follower
                                    {
                                        end_tail.push(qualifier.to_string());
                                    }
                                }
                                "BEFORE" | "AFTER" => {
                                    end_tail.push(qualifier.to_string());
                                    let next_suffix_word_idx =
                                        token_context.next_word_idx(1).filter(|lookahead_idx| {
                                            token_starts_on_same_line_after(idx, *lookahead_idx)
                                        });
                                    if let Some(qualifier_part) =
                                        next_suffix_word_idx.and_then(word_upper_at)
                                    {
                                        if qualifier_part == "STATEMENT" {
                                            end_tail.push(qualifier_part.into_owned());
                                        } else if qualifier_part == "EACH" {
                                            end_tail.push(qualifier_part.into_owned());
                                            let row_word = token_context
                                                .next_word_idx(2)
                                                .filter(|lookahead_idx| {
                                                    token_starts_on_same_line_after(
                                                        idx,
                                                        *lookahead_idx,
                                                    )
                                                })
                                                .and_then(word_upper_at);
                                            if row_word.as_deref() == Some("ROW") {
                                                end_tail.push("ROW".to_string());
                                            }
                                        }
                                    }
                                }
                                _ => {}
                            }
                        }
                        let effective_end_qualifier = end_tail
                            .first()
                            .map(String::as_str)
                            .or(split_end_qualifier.as_deref());
                        let is_qualified_end = matches!(
                            effective_end_qualifier,
                            Some("LOOP" | "IF" | "CASE" | "REPEAT" | "FOR" | "WHILE")
                        );
                        let paren_extra =
                            Self::paren_extra_depth_within_current_query_frame(&format_stack);
                        let mut closed_block = None;
                        let mut closed_owner_depth = None;
                        let mut closed_mysql_handler_begin = false;

                        let end_qualifier_is_keyword =
                            end_qualifier.as_deref().is_some_and(|qualifier| {
                                sql_text::is_oracle_sql_keyword(qualifier)
                                    || sql_text::is_mysql_sql_keyword(qualifier)
                            });
                        let case_expression_end = !is_qualified_end
                            && format_stack.last_block_kind_is("CASE")
                            && (end_qualifier.is_none()
                                || end_qualifier_is_keyword
                                || in_sql_case_clause);
                        let closes_active_package_member = is_package_body_statement
                            && end_qualifier
                                .as_deref()
                                .zip(active_package_member_name.as_deref())
                                .is_some_and(|(qualifier, member_name)| {
                                    qualifier.eq_ignore_ascii_case(member_name)
                                })
                            && format_stack.contains_block_kind("PACKAGE_BODY");

                        if is_qualified_end {
                            // END LOOP, END IF, END CASE - pop matching block
                            if let Some(top) = format_stack.last_block_kind() {
                                if top.supports_qualified_end()
                                    && block_end_qualifiers.contains(&top.as_str())
                                {
                                    if let Some(popped_block) =
                                        format_stack.pop_block(&mut indent_level)
                                    {
                                        closed_mysql_handler_begin |=
                                            popped_block.mysql_handler_begin;
                                        closed_owner_depth =
                                            Some(popped_block.depth_frame.owner_depth);
                                    }
                                }
                            }
                        } else if case_expression_end {
                            if let Some(popped_block) = format_stack.pop_block(&mut indent_level) {
                                closed_mysql_handler_begin |= popped_block.mysql_handler_begin;
                                closed_owner_depth = Some(popped_block.depth_frame.owner_depth);
                            }
                        } else {
                            // Plain END - closes BEGIN or DECLARE/PACKAGE_BODY block
                            // Pop until we find BEGIN or DECLARE/PACKAGE_BODY
                            while let Some(popped_block) = format_stack.pop_block(&mut indent_level)
                            {
                                closed_mysql_handler_begin |= popped_block.mysql_handler_begin;
                                let top = popped_block.kind;
                                closed_owner_depth = Some(popped_block.depth_frame.owner_depth);
                                let reached_package_body =
                                    format_stack.last_block_kind_is(BlockKind::PackageBody);
                                if closes_active_package_member {
                                    if reached_package_body {
                                        closed_block = Some(top);
                                        break;
                                    }
                                } else if top.closes_plain_end() {
                                    closed_block = Some(top);
                                    break;
                                }
                            }
                            if matches!(closed_block, Some(BlockKind::Begin | BlockKind::Declare))
                                && (format_stack.last_block_kind_is(BlockKind::PackageBody)
                                    || at_package_body_member_depth)
                            {
                                pending_package_member_separator = true;
                            }
                            if closes_active_package_member && closed_block.is_some() {
                                active_package_member_name = None;
                                pending_package_member_separator = true;
                            }
                        }

                        // `pop_block` now rebuilds `indent_level` from the
                        // surviving frame stack, so no explicit reassignment is
                        // needed on the happy path. When no block frame was
                        // popped (stray END against an empty block stack),
                        // `indent_level` must already be 0 under the
                        // statement-boundary invariant; blindly decrementing
                        // here would either be a no-op (saturating at 0) or
                        // introduce drift against non-block state, so we leave
                        // it alone.
                        #[cfg(debug_assertions)]
                        {
                            if closed_owner_depth.is_none() {
                                debug_assert_eq!(
                                    indent_level,
                                    format_stack.statement_base_indent(),
                                    "stray END with unexpected indent_level drift"
                                );
                            }
                        }
                        let end_line_indent = closed_owner_depth.unwrap_or(indent_level);
                        if is_package_body_statement
                            && !is_qualified_end
                            && !case_expression_end
                            && indent_level == 1
                        {
                            pending_package_member_separator = true;
                        }
                        let end_extra = if closed_owner_depth.is_some() {
                            0
                        } else if case_expression_end && (in_sql_case_clause || !in_plsql_block) {
                            1
                        } else {
                            0
                        };
                        newline_with(
                            &mut out,
                            base_indent!(end_line_indent),
                            end_extra + usize::from(closed_owner_depth.is_none()) * paren_extra,
                            &mut at_line_start,
                            &mut needs_space,
                            &mut line_indent,
                        );

                        // Output "END"
                        ensure_indent(&mut out, &mut at_line_start, line_indent);
                        out.push_str("END");

                        // If qualified (END LOOP/IF/CASE/REPEAT/BEFORE/AFTER), output tail and skip it.
                        let skip_count = end_tail.len();
                        for qualifier in end_tail.iter() {
                            out.push(' ');
                            out.push_str(qualifier);
                        }
                        needs_space = true;
                        if skip_count == 0 {
                            idx += 1;
                        } else {
                            let mut lookahead = idx + 1;
                            let mut words_skipped = 0usize;
                            while lookahead < tokens.len() && words_skipped < skip_count {
                                match &tokens[lookahead] {
                                    SqlToken::Word(_) => {
                                        words_skipped += 1;
                                        last_rendered_end_word_idx = lookahead;
                                    }
                                    SqlToken::Comment(comment) => {
                                        if comment.contains('\n') {
                                            break;
                                        }
                                    }
                                    _ => {}
                                }
                                lookahead += 1;
                            }
                            idx = lookahead;
                        }
                        if closed_mysql_handler_begin {
                            let _ = format_stack.deactivate_scoped_indent(
                                ScopedIndentKind::MySqlHandlerBody,
                                &mut indent_level,
                            );
                        }
                        if skip_count == 0 && split_end_qualifier.is_some() && !case_expression_end
                        {
                            if let Some(qualifier) = split_end_qualifier.as_deref() {
                                format_stack.set_pending_split_end_suffix(
                                    format_stack.current_scope(),
                                    end_line_indent,
                                    PendingSplitEndSuffixKind::from_first_suffix_word(qualifier),
                                );
                            }
                        } else {
                            let _ = format_stack.clear_pending_split_end_suffix();
                        }
                        has_rendered_content = true;
                        prev_word_idx = Some(last_rendered_end_word_idx);
                        recent_statement_word_indices.push_back(last_rendered_end_word_idx);
                        if recent_statement_word_indices.len() > 8 {
                            let _ = recent_statement_word_indices.pop_front();
                        }
                        for processed_idx in rendered_token_idx..idx {
                            if let Some(token) = tokens.get(processed_idx) {
                                query_apply_context_tracker
                                    .observe(token, lexical_paren_depth_state.depth());
                                statement_tail_state.observe(token);
                                apply_paren_token(&mut lexical_paren_depth_state, token);
                                delimiter_frame_state.apply_token(token);
                                line_closes_delimiter_frame_below_start |=
                                    line_delimiter_frame_state.apply_token_with_close_detection(
                                        token,
                                        line_start_delimiter_snapshot.baseline_depth(),
                                    );
                            }
                            previous_token_end_newline_count =
                                token_line_cursor.profile(processed_idx).end_newline_count;
                        }
                        continue;
                    } else if is_compound_trigger_timing_header {
                        format_stack.compound_trigger_start_timing_point();
                        newline_with(
                            &mut out,
                            base_indent!(indent_level),
                            0,
                            &mut at_line_start,
                            &mut needs_space,
                            &mut line_indent,
                        );
                    } else if format_stack.trigger_header_is_active()
                        && matches!(upper, "BEFORE" | "AFTER" | "INSTEAD" | "REFERENCING")
                    {
                        newline_with(
                            &mut out,
                            base_indent!(indent_level),
                            1,
                            &mut at_line_start,
                            &mut needs_space,
                            &mut line_indent,
                        );
                    } else if mysql_create_function_header_attribute {
                        newline_with(
                            &mut out,
                            base_indent!(indent_level),
                            0,
                            &mut at_line_start,
                            &mut needs_space,
                            &mut line_indent,
                        );
                    } else if is_trigger_event_keyword
                        && matches!(prev_word_upper, Some("BEFORE" | "AFTER" | "OF"))
                    {
                        // Keep trigger event verbs on the same line as BEFORE/AFTER/INSTEAD OF.
                    } else if format_stack
                        .scoped_indent_is_active(ScopedIndentKind::MergeWhenBranch)
                        && upper == "SET"
                    {
                        // MERGE UPDATE SET keeps SET inline with UPDATE, but we still need
                        // SET clause context so following list comments/commas align correctly.
                        set_current_clause!(Some(upper));
                        clear_select_list_layout_state!();
                    } else if upper == "ORDER"
                        && active_phase1_wrapped_owner_kind
                            == Some(FormatIndentedParenOwnerKind::WithinGroup)
                    {
                        let owner_relative_body_indent = format_stack
                            .last_paren()
                            .map(|frame| frame.open_line_indent.saturating_add(1))
                            .unwrap_or_else(|| base_indent!(indent_level));
                        if let Some(frame) = format_stack.last_paren_mut() {
                            frame.record_body_indent(owner_relative_body_indent);
                        }
                        newline_with(
                            &mut out,
                            owner_relative_body_indent,
                            0,
                            &mut at_line_start,
                            &mut needs_space,
                            &mut line_indent,
                        );
                        set_current_clause!(Some(upper));
                        clear_select_list_layout_state!();
                    } else if clause_keywords.contains(&upper)
                        && !keyword_suppresses_structural_handling
                        && !mysql_declare_for_clause
                        // VALUES() inside ON DUPLICATE KEY UPDATE is a function, not a clause
                        && (!on_duplicate_key_update_active || upper != "VALUES")
                        && !Self::suppresses_clause_break(
                            &format_stack,
                            tokens,
                            upper,
                            prev_word_upper,
                            in_plsql_block,
                            suppress_comma_break_depth,
                            format_stack.has_query_like_paren(),
                            inside_function_local_non_query_paren,
                            format_stack.scoped_indent_is_active(ScopedIndentKind::MergeWhenBranch),
                            current_scope,
                            format_stack.trigger_header_is_active(),
                            is_analytic_within_group,
                            !fetch_into_has_multiple_targets[idx],
                            format_stack.in_analytic_over_paren(),
                            format_stack.in_keep_paren(),
                            &recent_statement_word_indices,
                        )
                    {
                        // FORALL body is always exactly one structural level
                        // deeper than the FORALL header, regardless of the
                        // surrounding PL/SQL nesting.
                        if construct_flag_active!(ForallPending)
                            && matches!(upper, "INSERT" | "UPDATE" | "DELETE" | "MERGE")
                        {
                            deactivate_construct_flag!(ForallPending);
                            format_stack.push_scoped_indent(
                                ScopedIndentKind::ForallBody,
                                current_scope,
                                1,
                                &mut indent_level,
                            );
                        }
                        if !is_within_group {
                            let insert_all_active_in_scope = construct_flag_active_at_paren_depth!(
                                InsertAllActive,
                                current_scope.paren_depth
                            );
                            let insert_all_branch_indent =
                                insert_all_branch_body_indent.filter(|_| {
                                    insert_all_active_in_scope && matches!(upper, "INTO" | "VALUES")
                                });
                            let insert_all_extra = usize::from(
                                insert_all_active_in_scope
                                    && insert_all_branch_indent.is_none()
                                    && matches!(upper, "INTO" | "VALUES"),
                            );
                            let create_query_body_base_depth =
                                (crate::sql_text::is_subquery_head_keyword(upper)
                                    && format_stack.paren_is_empty())
                                .then(|| {
                                    rendered_line_tracker
                                        .active_query_owner_line_info(&out, at_line_start)
                                })
                                .filter(|owner_line| owner_line.is_create_query_body_header)
                                .map(|owner_line| owner_line.indent.saturating_add(1));
                            if let Some(base_depth) = create_query_body_base_depth {
                                set_query_body_clause_base_depth!(Some(base_depth));
                            }
                            let window_body_header_indent = format_stack
                                .in_window_paren()
                                .then(|| {
                                    FormatIndentedParenOwnerKind::Window
                                        .starts_phase1_body_header_words(
                                            upper, next_word, third_word,
                                        )
                                })
                                .filter(|is_header| *is_header)
                                .and_then(|_| format_stack.last_paren().copied())
                                .map(|frame| frame.open_line_indent.saturating_add(1));
                            let returning_function_option_indent = (upper == "RETURNING")
                                .then_some(())
                                .filter(|_| {
                                    matches!(format_stack.current_clause(), Some("SELECT"))
                                        && format_stack
                                            .last_paren()
                                            .is_some_and(|frame| !frame.is_query_like())
                                })
                                .map(|_| {
                                    let rendered_indent = rendered_line_tracker
                                        .current_line_indent(&out, line_indent);
                                    let owner_indent =
                                        if at_line_start && rendered_indent == 0 && line_indent > 0
                                        {
                                            line_indent
                                        } else {
                                            rendered_indent
                                        };
                                    owner_indent.saturating_add(1)
                                });
                            let returning_owner_body_indent = (upper == "RETURNING"
                                && !matches!(
                                    format_stack.current_clause(),
                                    Some("SELECT" | "SET")
                                )
                                && !format_stack.open_cursor_state().in_select()
                                && !format_stack
                                    .scoped_indent_is_active(ScopedIndentKind::CursorSql))
                            .then(|| {
                                format_stack
                                    .last_paren()
                                    .and_then(|frame| frame.sibling_body_indent())
                            })
                            .flatten();
                            // INSERT ALL: stop at SELECT (the trailing query)
                            if insert_all_active_in_scope && upper == "SELECT" {
                                deactivate_construct_flag!(InsertAllActive);
                                insert_all_branch_body_indent = None;
                            }
                            let query_head_indent = (at_line_start
                                && crate::sql_text::is_subquery_head_keyword(upper))
                            .then_some(line_indent)
                            .filter(|_| {
                                format_stack
                                    .last_paren()
                                    .is_some_and(|frame| frame.is_query_like())
                            });
                            if let Some(returning_indent) =
                                returning_function_option_indent.or(returning_owner_body_indent)
                            {
                                newline_with(
                                    &mut out,
                                    returning_indent,
                                    0,
                                    &mut at_line_start,
                                    &mut needs_space,
                                    &mut line_indent,
                                );
                            } else if let Some(window_indent) = window_body_header_indent {
                                newline_with(
                                    &mut out,
                                    window_indent,
                                    0,
                                    &mut at_line_start,
                                    &mut needs_space,
                                    &mut line_indent,
                                );
                            } else if let Some(query_body_depth) = query_body_clause_base_depth!()
                                .filter(|_| format_stack.paren_is_empty())
                            {
                                newline_with(
                                    &mut out,
                                    query_body_depth,
                                    0,
                                    &mut at_line_start,
                                    &mut needs_space,
                                    &mut line_indent,
                                );
                            } else if let Some(query_indent) = query_head_indent {
                                newline_with(
                                    &mut out,
                                    query_indent,
                                    0,
                                    &mut at_line_start,
                                    &mut needs_space,
                                    &mut line_indent,
                                );
                            } else if let Some(branch_indent) = insert_all_branch_indent {
                                newline_with(
                                    &mut out,
                                    branch_indent,
                                    0,
                                    &mut at_line_start,
                                    &mut needs_space,
                                    &mut line_indent,
                                );
                            } else {
                                let plsql_statement_body_indent = (in_plsql_block
                                    && at_line_start
                                    && format_stack.current_clause().is_none()
                                    && !format_stack.open_cursor_state().in_select()
                                    && format_stack.paren_is_empty())
                                .then_some(line_indent);
                                let select_clause_header_indent =
                                    matches!(format_stack.current_clause(), Some("SELECT"))
                                        .then_some(())
                                        .filter(|_| {
                                            select_list_layout_state!().has_active_indent()
                                                && matches!(
                                                    upper,
                                                    "FROM"
                                                        | "WHERE"
                                                        | "GROUP"
                                                        | "HAVING"
                                                        | "ORDER"
                                                        | "LIMIT"
                                                        | "WINDOW"
                                                )
                                        })
                                        .map(|_| {
                                            select_list_layout_state!()
                                                .indentation_or(
                                                    rendered_line_tracker
                                                        .current_line_indent(&out, line_indent),
                                                )
                                                .saturating_sub(1)
                                        });
                                let clause_header_indent = select_clause_header_indent
                                    .unwrap_or_else(|| {
                                        clause_indent!(
                                            indent_level,
                                            open_cursor_state!(),
                                            upper,
                                            format_stack
                                                .open_cursor_state()
                                                .select_depth()
                                                .is_some_and(|depth| {
                                                    format_stack.paren_depth() == depth
                                                }),
                                            format_stack.scoped_indent_is_active(
                                                ScopedIndentKind::CursorSql,
                                            ),
                                        )
                                    });
                                newline_with(
                                    &mut out,
                                    plsql_statement_body_indent
                                        .map(|indent| indent.max(clause_header_indent))
                                        .unwrap_or(clause_header_indent),
                                    insert_all_extra,
                                    &mut at_line_start,
                                    &mut needs_space,
                                    &mut line_indent,
                                );
                            }
                        }
                        if !is_within_group {
                            if upper != "MODEL" {
                                deactivate_construct_flag_at_or_above_paren_depth!(
                                    ModelActive,
                                    current_scope.paren_depth
                                );
                                deactivate_construct_flag_at_or_above_paren_depth!(
                                    ModelReferencePending,
                                    current_scope.paren_depth
                                );
                            }
                            set_current_clause!(Some(upper));
                            if upper != "SELECT" {
                                clear_select_list_layout_state!();
                            }
                            if crate::sql_text::is_subquery_head_keyword(upper) {
                                format_stack.set_current_query_base_depth(Some(line_indent));
                            }
                            if upper == "SELECT"
                                && format_stack.open_cursor_state().in_select()
                                && !format_stack.with_cte_in_definitions()
                            {
                                // OPEN ... FOR WITH ... SELECT should anchor to the main query
                                // head after CTE definitions, not to inner SELECTs inside the
                                // WITH body. Otherwise a later `FOR` in PIVOT/UNPIVOT or
                                // similar syntax can be misread as a new OPEN ... FOR.
                                let current_paren_depth = format_stack.paren_depth();
                                let mut next_open_cursor_state = format_stack.open_cursor_state();
                                next_open_cursor_state.set_select_depth(current_paren_depth);
                                format_stack.set_open_cursor_state(next_open_cursor_state);
                            }
                            if upper == "WITH" {
                                set_statement_has_with_clause!(true);
                            }
                            if upper == "MODEL"
                                && !construct_flag_active_at_paren_depth!(
                                    ModelActive,
                                    current_scope.paren_depth
                                )
                            {
                                activate_construct_flag!(ModelActive, current_scope);
                            }
                            if matches!(
                                upper,
                                "SELECT"
                                    | "FROM"
                                    | "WHERE"
                                    | "GROUP"
                                    | "HAVING"
                                    | "ORDER"
                                    | "UPDATE"
                                    | "DELETE"
                                    | "INSERT"
                                    | "MERGE"
                            ) || crate::sql_text::is_format_set_operator_keyword(upper)
                            {
                                deactivate_construct_flag!(SearchCycleClauseActive);
                            }
                        }
                    } else if is_analytic_within_group {
                        newline_with(
                            &mut out,
                            list_item_indent!(
                                indent_level,
                                open_cursor_state!(),
                                select_list_layout_state!(),
                            ),
                            0,
                            &mut at_line_start,
                            &mut needs_space,
                            &mut line_indent,
                        );
                    } else if format_stack.trigger_header_is_active() && upper == "WHEN" {
                        // Trigger WHEN clause: align with other trigger header keywords
                        newline_with(
                            &mut out,
                            base_indent!(indent_level),
                            1,
                            &mut at_line_start,
                            &mut needs_space,
                            &mut line_indent,
                        );
                    } else if should_break_condition && !keyword_suppresses_structural_handling {
                        let control_header_condition_indent = matches!(upper, "AND" | "OR")
                            .then(|| {
                                format_stack
                                    .last_condition_owner(ConditionOwnerKind::ControlHeader)
                                    .map(|frame| frame.indent)
                            })
                            .flatten();
                        let in_control_header_condition = control_header_condition_indent.is_some()
                            || (matches!(upper, "AND" | "OR")
                                && format_stack.last_block_kind().is_some_and(|block| {
                                    matches!(block, BlockKind::If | BlockKind::While)
                                })
                                && format_stack.current_clause().is_none());
                        let control_header_is_parenthesized = matches!(upper, "AND" | "OR")
                            .then(|| {
                                format_stack
                                    .last_condition_owner(ConditionOwnerKind::ControlHeader)
                                    .map(|frame| frame.parenthesized)
                            })
                            .flatten()
                            .unwrap_or(false);
                        let control_header_previous_token_is_close_paren = matches!(
                            loop_previous_non_comment_token,
                            Some(SqlToken::Symbol(sym)) if sym == ")"
                        );
                        let clause_base_indent = if let Some(header_indent) =
                            control_header_condition_indent
                        {
                            header_indent
                        } else if in_control_header_condition {
                            if matches!(upper, "AND" | "OR")
                                && format_stack.last_block_kind().is_some_and(|block| {
                                    matches!(block, BlockKind::If | BlockKind::While)
                                })
                            {
                                format_stack
                                    .last_block_owner_depth()
                                    .unwrap_or_else(|| base_indent!(indent_level.saturating_sub(1)))
                            } else {
                                base_indent!(indent_level.saturating_sub(1))
                            }
                        } else if format_stack.paren_is_empty() {
                            top_level_query_body_base_indent
                        } else {
                            clause_indent!(
                                indent_level,
                                open_cursor_state!(),
                                upper,
                                open_cursor_state!()
                                    .select_depth()
                                    .is_some_and(|depth| format_stack.paren_depth() == depth),
                                format_stack.scoped_indent_is_active(ScopedIndentKind::CursorSql),
                            )
                        };
                        let paren_extra = Self::paren_extra_depth(&format_stack);
                        if construct_flag_active_at_paren_depth!(
                            InsertAllActive,
                            current_scope.paren_depth
                        ) && matches!(upper, "WHEN" | "ELSE")
                        {
                            insert_all_branch_body_indent = None;
                        }
                        if upper == "WHEN" && format_stack.last_block_kind_is("CASE") {
                            let _ = format_stack.set_last_case_branch_started(true);
                        }
                        let is_merge_when = upper == "WHEN"
                            && construct_flag_active!(MergeActive)
                            && !format_stack.last_block_kind_is("CASE");
                        let in_exception_block =
                            upper == "WHEN" && format_stack.last_block_kind_is("EXCEPTION");
                        if in_exception_block {
                            let _ = format_stack.set_last_exception_branch_started(false);
                        }
                        let pending_create_query_body_owner_depth = (upper == "ON")
                            .then(|| rendered_line_tracker.current_line(&out))
                            .filter(|owner_line| {
                                matches!(
                                    sql_text::format_query_owner_pending_header_kind(owner_line),
                                    Some(sql_text::PendingFormatQueryOwnerHeaderKind::CreateQueryBody)
                                )
                            })
                            .map(rendered_line_indent);
                        let case_owner_depth = (upper == "WHEN"
                            && format_stack.last_block_kind_is("CASE"))
                        .then(|| format_stack.last_block_owner_depth())
                        .flatten();
                        if is_merge_when {
                            // MERGE WHEN MATCHED/NOT MATCHED: at base indent
                            let _ = format_stack.deactivate_scoped_indent(
                                ScopedIndentKind::MergeWhenBranch,
                                &mut indent_level,
                            );
                            newline_with(
                                &mut out,
                                base_indent!(indent_level),
                                0,
                                &mut at_line_start,
                                &mut needs_space,
                                &mut line_indent,
                            );
                        } else if let Some(owner_depth) = pending_create_query_body_owner_depth {
                            set_query_body_clause_base_depth!(Some(owner_depth.saturating_add(1)));
                            newline_with(
                                &mut out,
                                owner_depth,
                                0,
                                &mut at_line_start,
                                &mut needs_space,
                                &mut line_indent,
                            );
                        } else if in_exception_block {
                            newline_with(
                                &mut out,
                                clause_base_indent,
                                paren_extra,
                                &mut at_line_start,
                                &mut needs_space,
                                &mut line_indent,
                            );
                        } else if let Some(owner_depth) = case_owner_depth {
                            newline_with(
                                &mut out,
                                owner_depth,
                                1,
                                &mut at_line_start,
                                &mut needs_space,
                                &mut line_indent,
                            );
                        } else {
                            let where_condition_owner_frame = format_stack
                                .last_condition_owner(ConditionOwnerKind::Where)
                                .map(|frame| (frame.scope.paren_depth, frame.indent))
                                .filter(|(paren_depth, _)| {
                                    current_scope.paren_depth >= *paren_depth
                                        && matches!(upper, "AND" | "OR")
                                        && matches!(
                                            format_stack.current_clause(),
                                            Some("WHERE" | "HAVING")
                                        )
                                });
                            let join_condition_owner_frame = format_stack
                                .last_condition_owner(ConditionOwnerKind::JoinOn)
                                .map(|frame| (frame.scope.paren_depth, frame.indent))
                                .filter(|(paren_depth, _)| {
                                    current_scope.paren_depth >= *paren_depth
                                        && matches!(upper, "AND" | "OR")
                                });
                            let case_condition_owner_frame = format_stack
                                .last_condition_owner(ConditionOwnerKind::Case)
                                .map(|frame| {
                                    (
                                        frame.scope.paren_depth,
                                        frame.scope.block_depth,
                                        frame.indent,
                                    )
                                })
                                .filter(|(paren_depth, block_depth, _)| {
                                    current_scope.paren_depth >= *paren_depth
                                        && current_scope.block_depth >= *block_depth
                                        && matches!(upper, "AND" | "OR")
                                });
                            if let Some((owner_paren_depth, owner_depth)) =
                                where_condition_owner_frame
                            {
                                newline_with(
                                    &mut out,
                                    owner_depth,
                                    1 + current_scope.paren_depth.saturating_sub(owner_paren_depth),
                                    &mut at_line_start,
                                    &mut needs_space,
                                    &mut line_indent,
                                );
                            } else if let Some((owner_paren_depth, owner_depth)) =
                                join_condition_owner_frame
                            {
                                newline_with(
                                    &mut out,
                                    owner_depth,
                                    1 + current_scope.paren_depth.saturating_sub(owner_paren_depth),
                                    &mut at_line_start,
                                    &mut needs_space,
                                    &mut line_indent,
                                );
                            } else if let Some((owner_paren_depth, _, owner_depth)) =
                                case_condition_owner_frame
                            {
                                newline_with(
                                    &mut out,
                                    owner_depth,
                                    1 + current_scope.paren_depth.saturating_sub(owner_paren_depth),
                                    &mut at_line_start,
                                    &mut needs_space,
                                    &mut line_indent,
                                );
                            } else {
                                let condition_extra = if in_control_header_condition
                                    && matches!(upper, "AND" | "OR")
                                {
                                    if control_header_is_parenthesized {
                                        paren_extra.max(1)
                                    } else if control_header_previous_token_is_close_paren {
                                        paren_extra
                                    } else {
                                        paren_extra.max(1)
                                    }
                                } else if in_control_header_condition {
                                    paren_extra.max(1)
                                } else if upper == "ON" {
                                    1
                                } else {
                                    paren_extra + 1
                                };
                                newline_with(
                                    &mut out,
                                    clause_base_indent,
                                    condition_extra,
                                    &mut at_line_start,
                                    &mut needs_space,
                                    &mut line_indent,
                                );
                            }
                        }
                        if upper == "ON" {
                            set_current_clause!(Some(upper));
                            clear_select_list_layout_state!();
                        }
                    } else if upper == "EXCEPTION" && in_plsql_block {
                        newline_with(
                            &mut out,
                            base_indent!(indent_level.saturating_sub(1)),
                            0,
                            &mut at_line_start,
                            &mut needs_space,
                            &mut line_indent,
                        );
                    } else if construct_flag_active!(ModelReferencePending)
                        && at_line_start
                        && upper != "ON"
                    {
                        newline_with(
                            &mut out,
                            base_indent!(indent_level),
                            1,
                            &mut at_line_start,
                            &mut needs_space,
                            &mut line_indent,
                        );
                    } else if construct_flag_active!(ModelReferencePending) && upper == "ON" {
                        deactivate_construct_flag!(ModelReferencePending);
                    } else if upper == "CREATE" {
                        activate_construct_flag!(CreatePending, current_scope);
                        clear_construct_value!(CreateObject);
                    } else if construct_flag_active!(CreatePending)
                        && (upper == "OR" || upper == "REPLACE")
                    {
                        // part of CREATE OR REPLACE
                    } else if construct_flag_active!(CreatePending) && upper == "PACKAGE" {
                        if next_word_is("BODY") {
                            replace_construct_value!(
                                CreateObject,
                                current_scope,
                                "PACKAGE_BODY".to_string()
                            );
                        } else {
                            replace_construct_value!(
                                CreateObject,
                                current_scope,
                                "PACKAGE".to_string()
                            );
                        }
                        deactivate_construct_flag!(CreatePending);
                    } else if construct_flag_active!(CreatePending) && upper == "TABLE" {
                        activate_construct_flag!(CreateTableParenExpected, current_scope);
                        deactivate_construct_flag!(CreatePending);
                    } else if construct_flag_active!(CreatePending)
                        && matches!(upper, "INDEX" | "UNIQUE")
                    {
                        if upper == "INDEX" {
                            activate_construct_flag!(CreateIndexPending, current_scope);
                            deactivate_construct_flag!(CreatePending);
                        }
                        // UNIQUE stays in construct.create_pending to catch UNIQUE INDEX
                    } else if construct_flag_active!(CreatePending) && upper == "SEQUENCE" {
                        activate_construct_flag!(CreateSequenceActive, current_scope);
                        deactivate_construct_flag!(CreatePending);
                    } else if construct_flag_active!(CreatePending) && upper == "SYNONYM" {
                        activate_construct_flag!(CreateSynonymActive, current_scope);
                        deactivate_construct_flag!(CreatePending);
                    } else if construct_flag_active!(CreatePending)
                        && matches!(upper, "PROCEDURE" | "FUNCTION" | "TYPE" | "TRIGGER")
                    {
                        replace_construct_value!(CreateObject, current_scope, upper.to_string());
                        if upper == "TRIGGER" {
                            format_stack.push_trigger_header(current_scope);
                        }
                        deactivate_construct_flag!(CreatePending);
                    } else if with_plsql_declaration_header {
                        if !at_line_start {
                            newline_with(
                                &mut out,
                                base_indent!(indent_level),
                                1,
                                &mut at_line_start,
                                &mut needs_space,
                                &mut line_indent,
                            );
                        }
                        if matches!(upper, "PROCEDURE" | "FUNCTION") {
                            activate_construct_flag!(RoutineDeclPending, current_scope);
                        } else if upper == "PACKAGE" {
                            replace_construct_value!(
                                CreateObject,
                                current_scope,
                                upper.to_string()
                            );
                        }
                    } else if matches!(upper, "PROCEDURE" | "FUNCTION")
                        && (format_stack.last_block_kind_is("DECLARE")
                            || format_stack.contains_block_kind("PACKAGE_BODY")
                            || at_package_body_member_depth)
                    {
                        if !at_line_start && !follows_type_method_modifier {
                            newline_with(
                                &mut out,
                                base_indent!(indent_level),
                                0,
                                &mut at_line_start,
                                &mut needs_space,
                                &mut line_indent,
                            );
                        }
                        activate_construct_flag!(RoutineDeclPending, current_scope);
                    } else if matches!(upper, "ELSE" | "ELSIF" | "ELSEIF") {
                        // ELSE/ELSIF/ELSEIF in IF block: same level as IF
                        let in_if_block = format_stack.last_block_kind_is("IF");
                        let in_case_block = format_stack.last_block_kind_is("CASE");
                        let paren_extra = Self::paren_extra_depth(&format_stack);
                        if upper == "ELSE" && in_case_block && !in_if_block {
                            let _ = format_stack.set_last_case_branch_started(true);
                        }
                        if in_if_block {
                            newline_with(
                                &mut out,
                                base_indent!(indent_level.saturating_sub(1)),
                                0,
                                &mut at_line_start,
                                &mut needs_space,
                                &mut line_indent,
                            );
                        } else {
                            // ELSE in CASE or other context
                            if let Some(owner_depth) = in_case_block
                                .then(|| format_stack.last_block_owner_depth())
                                .flatten()
                            {
                                newline_with(
                                    &mut out,
                                    owner_depth,
                                    1,
                                    &mut at_line_start,
                                    &mut needs_space,
                                    &mut line_indent,
                                );
                            } else {
                                newline_with(
                                    &mut out,
                                    base_indent!(indent_level),
                                    paren_extra + 1,
                                    &mut at_line_start,
                                    &mut needs_space,
                                    &mut line_indent,
                                );
                            }
                        }
                        if upper == "ELSE"
                            && in_plsql_block
                            && !matches!(format_stack.current_clause(), Some("SELECT"))
                        {
                            newline_after_keyword = true;
                            if in_case_block {
                                newline_after_keyword_extra = 1;
                            }
                        } else if upper == "ELSE" && in_sql_case_clause && next_word_is("CASE") {
                            // Keep ELSE CASE from collapsing into one long SQL expression line.
                            newline_after_keyword = true;
                            newline_after_keyword_extra = 1;
                        }
                    } else if upper == "THEN" {
                        if construct_flag_active!(MergeActive)
                            && !format_stack.last_block_kind_is("CASE")
                        {
                            // MERGE WHEN MATCHED THEN / WHEN NOT MATCHED THEN
                            format_stack.push_scoped_indent(
                                ScopedIndentKind::MergeWhenBranch,
                                current_scope,
                                1,
                                &mut indent_level,
                            );
                            newline_after_keyword = true;
                        } else if construct_flag_active_at_paren_depth!(
                            InsertAllActive,
                            current_scope.paren_depth
                        ) {
                            // INSERT ALL/FIRST branch bodies should stay one
                            // level deeper than WHEN ... THEN headers.
                            insert_all_branch_body_indent = Some(
                                rendered_line_tracker
                                    .current_line_indent(&out, line_indent)
                                    .saturating_add(1),
                            );
                            newline_after_keyword = true;
                            newline_after_keyword_extra = 1;
                        } else if in_plsql_block
                            && !matches!(format_stack.current_clause(), Some("SELECT"))
                        {
                            newline_after_keyword = true;
                            if format_stack.last_block_kind().is_some_and(|s| {
                                matches!(s, BlockKind::Case | BlockKind::Exception)
                            }) {
                                newline_after_keyword_extra = 1;
                                if format_stack.last_block_kind_is("EXCEPTION") {
                                    let _ = format_stack.set_last_exception_branch_started(true);
                                }
                            }
                        } else if in_sql_case_clause && next_word_is("CASE") {
                            // Nested CASE in SQL expressions should start on its own line.
                            newline_after_keyword = true;
                            newline_after_keyword_extra = 1;
                        }
                    } else if (upper == join_keyword || upper == "APPLY")
                        && !keyword_suppresses_structural_handling
                    {
                        if !join_modifier_active {
                            newline_with(
                                &mut out,
                                top_level_query_body_base_indent,
                                0,
                                &mut at_line_start,
                                &mut needs_space,
                                &mut line_indent,
                            );
                        }
                        join_modifier_active = false;
                    } else if join_modifiers.contains(&upper)
                        && !keyword_suppresses_structural_handling
                    {
                        let next_leads_to_join = next_word_is("JOIN")
                            || next_word_is("OUTER")
                            || next_word_is("APPLY")
                            || next_word.is_some_and(|w| {
                                join_modifiers.iter().any(|m| w.eq_ignore_ascii_case(m))
                            });
                        if next_leads_to_join {
                            if !join_modifier_active {
                                newline_with(
                                    &mut out,
                                    top_level_query_body_base_indent,
                                    0,
                                    &mut at_line_start,
                                    &mut needs_space,
                                    &mut line_indent,
                                );
                            }
                            join_modifier_active = true;
                        }
                    } else if upper == outer_keyword && !keyword_suppresses_structural_handling {
                        if (next_word_is("JOIN") || next_word_is("APPLY")) && !join_modifier_active
                        {
                            newline_with(
                                &mut out,
                                top_level_query_body_base_indent,
                                0,
                                &mut at_line_start,
                                &mut needs_space,
                                &mut line_indent,
                            );
                            join_modifier_active = true;
                        }
                    } else if (upper == "SEARCH" || upper == "CYCLE")
                        && !keyword_suppresses_structural_handling
                    {
                        activate_construct_flag!(SearchCycleClauseActive, current_scope);
                        // SEARCH/CYCLE after recursive CTE should start on a
                        // new line at the WITH-clause level.
                        newline_with(
                            &mut out,
                            base_indent!(indent_level),
                            0,
                            &mut at_line_start,
                            &mut needs_space,
                            &mut line_indent,
                        );
                    } else if matches!(upper, "PIVOT" | "UNPIVOT")
                        && !keyword_suppresses_structural_handling
                        && suppress_comma_break_depth == 0
                        && !construct_flag_active!(CreateSynonymActive)
                    {
                        // PIVOT/UNPIVOT should start on a new line as a
                        // clause-level keyword at the same depth as FROM.
                        newline_with(
                            &mut out,
                            clause_indent!(
                                indent_level,
                                open_cursor_state!(),
                                upper,
                                open_cursor_state!()
                                    .select_depth()
                                    .is_some_and(|depth| format_stack.paren_depth() == depth),
                                format_stack.scoped_indent_is_active(ScopedIndentKind::CursorSql),
                            ),
                            0,
                            &mut at_line_start,
                            &mut needs_space,
                            &mut line_indent,
                        );
                    } else {
                        let match_recognize_body_header = format_stack.in_match_recognize_paren()
                            && FormatIndentedParenOwnerKind::MatchRecognize
                                .starts_phase1_body_header_words(upper, next_word, third_word);
                        let analytic_over_body_header = format_stack.in_analytic_over_paren()
                            && FormatIndentedParenOwnerKind::AnalyticOver
                                .starts_phase1_body_header_words(upper, next_word, third_word);
                        let window_body_header = format_stack.in_window_paren()
                            && FormatIndentedParenOwnerKind::Window
                                .starts_phase1_body_header_words(upper, next_word, third_word);
                        if match_recognize_body_header
                            || analytic_over_body_header
                            || window_body_header
                        {
                            let owner_relative_body_indent = (match_recognize_body_header
                                || analytic_over_body_header
                                || window_body_header)
                                .then(|| format_stack.last_paren().copied())
                                .flatten()
                                .map(|frame| frame.open_line_indent.saturating_add(1));
                            newline_with(
                                &mut out,
                                owner_relative_body_indent
                                    .unwrap_or_else(|| base_indent!(indent_level)),
                                usize::from(owner_relative_body_indent.is_none()),
                                &mut at_line_start,
                                &mut needs_space,
                                &mut line_indent,
                            );
                        } else if construct_flag_active_at_paren_depth!(
                            ModelActive,
                            current_scope.paren_depth
                        ) && FormatIndentedParenOwnerKind::ModelSubclause
                            .starts_phase1_body_header_words(upper, next_word, third_word)
                        {
                            if upper == "REFERENCE" {
                                activate_construct_flag!(ModelReferencePending, current_scope);
                            } else {
                                deactivate_construct_flag!(ModelReferencePending);
                            }
                            newline_with(
                                &mut out,
                                base_indent!(indent_level),
                                1,
                                &mut at_line_start,
                                &mut needs_space,
                                &mut line_indent,
                            );
                        } else if active_phase1_wrapped_owner_kind.is_some_and(|kind| {
                            kind.starts_phase1_body_header_words(upper, next_word, third_word)
                        }) {
                            newline_with(
                                &mut out,
                                base_indent!(indent_level),
                                0,
                                &mut at_line_start,
                                &mut needs_space,
                                &mut line_indent,
                            );
                        } else if upper == "OPEN" {
                            set_open_cursor_state!(OpenCursorFormatState::AwaitingFor);
                        } else if upper == "FOR" && mysql_declare_cursor_for_clause {
                            format_stack.push_scoped_indent(
                                ScopedIndentKind::CursorSql,
                                current_scope,
                                1,
                                &mut indent_level,
                            );
                            newline_after_keyword = true;
                        } else if upper == "FOR" && mysql_declare_handler_for_clause {
                            mysql_handler_state.start();
                            let _ = format_stack.clear_mysql_handler_pending_body_indent();
                        } else if upper == "FOR"
                            && next_word_is("UPDATE")
                            && !mysql_declare_for_clause
                            && !in_plsql_block
                            && suppress_comma_break_depth == 0
                        {
                            // FOR UPDATE gets its own line at clause base indent.
                            newline_with(
                                &mut out,
                                base_indent!(indent_level),
                                0,
                                &mut at_line_start,
                                &mut needs_space,
                                &mut line_indent,
                            );
                        } else if (upper == "FOR" || upper == "WHILE")
                            && !mysql_declare_for_clause
                            && !(upper == "FOR"
                                && (construct_flag_active!(CreateSynonymActive)
                                    || suppress_comma_break_depth > 0
                                    || (next_word_is("UPDATE") && !in_plsql_block)))
                        {
                            if upper == "FOR" && format_stack.trigger_header_is_active() {
                                newline_with(
                                    &mut out,
                                    base_indent!(indent_level),
                                    1,
                                    &mut at_line_start,
                                    &mut needs_space,
                                    &mut line_indent,
                                );
                                after_for_while = false;
                            } else if upper == "FOR"
                                && format_stack.open_cursor_state()
                                    == OpenCursorFormatState::AwaitingFor
                            {
                                format_stack.push_open_cursor_restore(open_cursor_state!());
                                set_open_cursor_state!(OpenCursorFormatState::InSelect {
                                    anchor_indent: line_indent.saturating_add(1),
                                    select_paren_depth: None,
                                });
                                newline_after_keyword = true;
                            } else {
                                // FOR/WHILE starts a line, LOOP will follow on same line
                                if !mysql_labeled_block_target_word {
                                    newline_with(
                                        &mut out,
                                        base_indent!(indent_level),
                                        0,
                                        &mut at_line_start,
                                        &mut needs_space,
                                        &mut line_indent,
                                    );
                                }
                                after_for_while = true;
                            }
                        } else if upper == "LOOP" {
                            // LOOP after FOR/WHILE stays on same line
                            if after_for_while {
                                closed_control_header_body_indent = format_stack
                                    .pop_last_condition_owner(ConditionOwnerKind::ControlHeader)
                                    .map(|frame| frame.indent.saturating_add(1));
                            }
                            let mysql_labeled_loop = mysql_compatible
                                && matches!(
                                    immediate_prev_token,
                                    Some(SqlToken::Symbol(sym)) if sym == ":"
                                )
                                && immediate_prev_prev_token
                                    .is_some_and(|token| matches!(token, SqlToken::Word(_)));
                            if !after_for_while && !mysql_labeled_loop {
                                newline_with(
                                    &mut out,
                                    base_indent!(indent_level),
                                    0,
                                    &mut at_line_start,
                                    &mut needs_space,
                                    &mut line_indent,
                                );
                            }
                            after_for_while = false;
                            // LOOP always starts a block body on the next line.
                            newline_after_keyword = true;
                        } else if upper == "REPEAT" {
                            // REPEAT starts a block body on the next line.
                            if !mysql_labeled_block_target_word {
                                newline_with(
                                    &mut out,
                                    base_indent!(indent_level),
                                    0,
                                    &mut at_line_start,
                                    &mut needs_space,
                                    &mut line_indent,
                                );
                            }
                            newline_after_keyword = true;
                        } else if upper == "DO"
                            && mysql_compatible
                            && after_for_while
                            && Self::recent_statement_words_before_from_indices(
                                tokens,
                                &recent_statement_word_indices,
                                8,
                            )
                            .iter()
                            .any(|word| word.eq_ignore_ascii_case("WHILE"))
                        {
                            after_for_while = false;
                            closed_control_header_body_indent = format_stack
                                .pop_last_condition_owner(ConditionOwnerKind::ControlHeader)
                                .map(|frame| frame.indent.saturating_add(1));
                            format_stack.push_block(
                                BlockKind::While,
                                line_indent,
                                indent_level.saturating_add(1),
                                &mut indent_level,
                            );
                            format_stack.push_plsql_context(format_stack.current_scope());
                            newline_after_keyword = true;
                        } else if upper == "CASE" {
                            // CASE in PL/SQL block vs SELECT context
                            let values_case_indent =
                                matches!(format_stack.current_clause(), Some("VALUES"))
                                    .then(|| {
                                        format_stack
                                            .last_compact_paren_open_line_indent()
                                            .map(|indent| indent.saturating_add(1))
                                    })
                                    .flatten();
                            if in_sql_case_clause {
                                let paren_extra = Self::paren_extra_depth(&format_stack);
                                let in_case_branch_body = format_stack.last_block_kind_is("CASE")
                                    && (format_stack.last_case_branch_started()
                                        || matches!(prev_word_upper, Some("THEN")));
                                if let Some(values_case_indent) = values_case_indent {
                                    newline_with(
                                        &mut out,
                                        values_case_indent,
                                        0,
                                        &mut at_line_start,
                                        &mut needs_space,
                                        &mut line_indent,
                                    );
                                } else if in_case_branch_body {
                                    let branch_case_indent = format_stack
                                        .last_block_owner_depth()
                                        .map(|depth| depth.saturating_add(2))
                                        .unwrap_or(line_indent);
                                    let parenthesized_branch_case_indent = format_stack
                                        .last_paren()
                                        .and_then(|frame| frame.sibling_body_indent())
                                        .map(|indent| indent.max(branch_case_indent))
                                        .unwrap_or(branch_case_indent);
                                    newline_with(
                                        &mut out,
                                        parenthesized_branch_case_indent,
                                        0,
                                        &mut at_line_start,
                                        &mut needs_space,
                                        &mut line_indent,
                                    );
                                } else if let Some(paren_body_indent) = format_stack
                                    .last_paren()
                                    .and_then(|frame| frame.sibling_body_indent())
                                {
                                    newline_with(
                                        &mut out,
                                        paren_body_indent,
                                        0,
                                        &mut at_line_start,
                                        &mut needs_space,
                                        &mut line_indent,
                                    );
                                } else if matches!(format_stack.current_clause(), Some("SELECT")) {
                                    let mut select_case_indent = active_list_indent!(
                                        indent_level,
                                        open_cursor_state!(),
                                        select_list_layout_state!(),
                                        format_stack.current_clause(),
                                        construct_flag_active!(MergeActive),
                                        false,
                                    );
                                    if format_stack.paren_is_empty() {
                                        if let Some(base_depth) = query_body_clause_base_depth!() {
                                            select_case_indent = select_case_indent
                                                .max(base_depth.saturating_add(1));
                                        }
                                    }
                                    if select_list_layout_state!().has_active_indent()
                                        && !select_list_layout_state!().is_multiline()
                                    {
                                        set_select_list_layout_state!(
                                            SelectListLayoutState::Multiline {
                                                indent: select_case_indent,
                                                hanging_indent_spaces: None,
                                            }
                                        );
                                    }
                                    newline_with(
                                        &mut out,
                                        select_case_indent,
                                        0,
                                        &mut at_line_start,
                                        &mut needs_space,
                                        &mut line_indent,
                                    );
                                } else {
                                    newline_with(
                                        &mut out,
                                        base_indent!(indent_level),
                                        1 + paren_extra,
                                        &mut at_line_start,
                                        &mut needs_space,
                                        &mut line_indent,
                                    );
                                }
                            } else if in_plsql_block {
                                let paren_extra = Self::paren_extra_depth(&format_stack);
                                let in_case_branch_body = format_stack.last_block_kind_is("CASE")
                                    && format_stack.last_case_branch_started();
                                let previous_non_comment_token = loop_previous_non_comment_token;
                                let second_previous_non_comment_token =
                                    loop_second_previous_non_comment_token;
                                let follows_trailing_operator = previous_non_comment_token
                                    .and_then(Self::format_trailing_meaningful_token_from_sql_token)
                                    .is_some_and(|last| {
                                        let previous = second_previous_non_comment_token
                                            .and_then(Self::format_trailing_meaningful_token_from_sql_token);
                                        crate::sql_text::format_trailing_continuation_operator_kind_from_token_pair(
                                            previous,
                                            last,
                                        )
                                        .is_some()
                                    });
                                let parenthesized_case_indent = if in_case_branch_body {
                                    Some(
                                        format_stack
                                            .last_paren()
                                            .and_then(|frame| frame.sibling_body_indent())
                                            .map(|indent| indent.max(line_indent))
                                            .unwrap_or(line_indent),
                                    )
                                } else {
                                    values_case_indent.or_else(|| {
                                        format_stack
                                            .last_paren()
                                            .and_then(|frame| frame.sibling_body_indent())
                                            .or_else(|| {
                                                (paren_extra > 0).then_some(
                                                    line_indent.saturating_add(paren_extra),
                                                )
                                            })
                                            .or_else(|| {
                                                follows_trailing_operator
                                                    .then_some(line_indent.saturating_add(1))
                                            })
                                    })
                                };
                                newline_with(
                                    &mut out,
                                    parenthesized_case_indent
                                        .unwrap_or_else(|| base_indent!(indent_level)),
                                    0,
                                    &mut at_line_start,
                                    &mut needs_space,
                                    &mut line_indent,
                                );
                            }
                            // In SELECT context, CASE stays inline
                        } else if should_treat_as_block_start {
                            let block_header_indent = base_indent!(indent_level);
                            if !at_line_start || line_indent < block_header_indent {
                                newline_with(
                                    &mut out,
                                    block_header_indent,
                                    0,
                                    &mut at_line_start,
                                    &mut needs_space,
                                    &mut line_indent,
                                );
                            }
                        } else if upper == "BEGIN" {
                            // BEGIN handling: check if we're inside a DECLARE block
                            let inside_declare = format_stack.last_block_kind().is_some_and(|s| {
                                matches!(s, BlockKind::Declare | BlockKind::PackageBody)
                            });
                            if inside_declare {
                                // DECLARE ... BEGIN - BEGIN is at same level as DECLARE.
                                // PACKAGE BODY initializer BEGIN snaps to package owner depth.
                                let begin_indent =
                                    if format_stack.last_block_kind_is("PACKAGE_BODY") {
                                        format_stack
                                            .last_block_owner_depth()
                                            .unwrap_or_else(|| indent_level.saturating_sub(1))
                                    } else {
                                        indent_level.saturating_sub(1)
                                    };
                                newline_with(
                                    &mut out,
                                    base_indent!(begin_indent),
                                    0,
                                    &mut at_line_start,
                                    &mut needs_space,
                                    &mut line_indent,
                                );
                            } else if !mysql_labeled_block_target_word {
                                // BEGIN inside block statements should align with current block depth.
                                newline_with(
                                    &mut out,
                                    base_indent!(indent_level),
                                    0,
                                    &mut at_line_start,
                                    &mut needs_space,
                                    &mut line_indent,
                                );
                            }
                        }
                    }

                    let case_branch_body_line = at_line_start
                        && in_plsql_block
                        && format_stack.last_block_kind_is("CASE")
                        && format_stack.last_case_branch_started()
                        && line_indent <= base_indent!(indent_level)
                        && !word
                            .as_bytes()
                            .first()
                            .is_some_and(|byte| byte.is_ascii_digit())
                        && !matches!(upper, "WHEN" | "ELSE" | "END" | "CASE");
                    let exception_branch_body_line = at_line_start
                        && in_plsql_block
                        && format_stack.last_block_kind_is("EXCEPTION")
                        && format_stack.last_exception_branch_started()
                        && !format_stack.open_cursor_state().in_select()
                        && !format_stack.scoped_indent_is_active(ScopedIndentKind::CursorSql)
                        && line_indent <= base_indent!(indent_level)
                        && !matches!(upper, "WHEN" | "END");
                    let mysql_handler_header_word = if mysql_declare_handler_for_clause {
                        false
                    } else {
                        mysql_handler_state.consume_word(upper)
                    };
                    if mysql_handler_state.body_line_pending() {
                        let pending_indent = rendered_line_tracker
                            .current_line_indent(&out, line_indent)
                            .saturating_add(1);
                        let pending_indent = format_stack
                            .mysql_handler_pending_body_indent()
                            .unwrap_or(pending_indent);
                        format_stack
                            .set_mysql_handler_pending_body_indent(current_scope, pending_indent);
                    }
                    if case_branch_body_line || exception_branch_body_line {
                        line_indent = line_indent.saturating_add(1);
                    }
                    if mysql_handler_block_begin {
                        let handler_body_indent = format_stack
                            .mysql_handler_pending_body_indent()
                            .unwrap_or_else(|| {
                                line_indent
                                    .max(base_indent!(indent_level))
                                    .saturating_add(1)
                            });
                        line_indent = line_indent.max(handler_body_indent);
                        let _ = format_stack.clear_mysql_handler_pending_body_indent();
                    }
                    if mysql_handler_body_line_pending
                        && !mysql_handler_header_word
                        && !mysql_handler_block_begin
                    {
                        let handler_body_indent = format_stack
                            .mysql_handler_pending_body_indent()
                            .unwrap_or_else(|| {
                                line_indent
                                    .max(base_indent!(indent_level))
                                    .saturating_add(1)
                            });
                        line_indent = line_indent.max(handler_body_indent);
                        let _ = format_stack.clear_mysql_handler_pending_body_indent();
                    }
                    let mysql_on_duplicate_values_argument_line = at_line_start
                        && on_duplicate_key_update_active
                        && upper == "VALUES"
                        && format_stack.last_paren().is_some();
                    if mysql_on_duplicate_values_argument_line {
                        if let Some(frame) = format_stack.last_paren().copied() {
                            line_indent = line_indent.max(frame.open_line_indent.saturating_add(1));
                        }
                    }
                    let starts_control_condition_header = in_plsql_block
                        && matches!(upper, "IF" | "WHILE" | "ELSIF" | "ELSEIF")
                        && !mysql_scalar_if_function
                        && format_stack.current_clause().is_none();
                    let pending_control_header_parenthesized = starts_control_condition_header
                        .then_some(
                            matches!(next_non_comment, Some(SqlToken::Symbol(sym)) if sym == "("),
                        );
                    if !at_line_start && token_gap_has_newline {
                        if let Some(split_end_indent) =
                            format_stack.pending_split_end_suffix_indent()
                        {
                            newline_with(
                                &mut out,
                                split_end_indent,
                                0,
                                &mut at_line_start,
                                &mut needs_space,
                                &mut line_indent,
                            );
                        } else if !matches!(upper, "THEN" | "LOOP" | "DO") {
                            if let Some(header_indent) = format_stack
                                .last_condition_owner(ConditionOwnerKind::ControlHeader)
                                .map(|frame| frame.indent)
                            {
                                newline_with(
                                    &mut out,
                                    header_indent,
                                    0,
                                    &mut at_line_start,
                                    &mut needs_space,
                                    &mut line_indent,
                                );
                            }
                        }
                    }
                    let control_condition_continuation_line = format_stack
                        .last_condition_owner(ConditionOwnerKind::ControlHeader)
                        .map(|frame| frame.indent)
                        .filter(|_| {
                            at_line_start
                                && !matches!(
                                    upper,
                                    "AND"
                                        | "OR"
                                        | "ELSIF"
                                        | "ELSEIF"
                                        | "ELSE"
                                        | "END"
                                        | "THEN"
                                        | "LOOP"
                                        | "DO"
                                )
                                && format_stack.current_clause().is_none()
                        });
                    if let Some(header_indent) = control_condition_continuation_line {
                        line_indent = line_indent.max(header_indent);
                    }
                    if let Some(split_end_indent) = format_stack.pending_split_end_suffix_indent() {
                        if at_line_start {
                            line_indent = split_end_indent;
                        }
                    }
                    let mysql_on_duplicate_clause_line = mysql_compatible
                        && at_line_start
                        && upper == "ON"
                        && next_word_is("DUPLICATE")
                        && third_word.is_some_and(|word| word.eq_ignore_ascii_case("KEY"))
                        && next_words[2].is_some_and(|word| word.eq_ignore_ascii_case("UPDATE"));
                    if mysql_on_duplicate_clause_line {
                        line_indent = base_indent!(indent_level);
                    }
                    if at_line_start
                        && upper == "BEGIN"
                        && (format_stack.last_block_kind_is("PACKAGE_BODY")
                            || pending_package_member_separator)
                    {
                        let package_owner_depth = format_stack
                            .last_block_kind()
                            .filter(|block| *block == BlockKind::PackageBody)
                            .and_then(|_| format_stack.last_block_owner_depth())
                            .unwrap_or(0);
                        line_indent = package_owner_depth;
                    }
                    let opens_unterminated_plsql_label_with_embedded_lines =
                        opens_unterminated_plsql_label && word.contains('\n');
                    ensure_indent(&mut out, &mut at_line_start, line_indent);
                    if needs_space {
                        out.push(' ');
                    }
                    let rendered_word = if is_keyword && !keyword_preserves_original_case {
                        upper
                    } else {
                        word
                    };
                    if rendered_word.contains('\n') {
                        let continuation_indent =
                            if opens_unterminated_plsql_label_with_embedded_lines {
                                format_stack
                                    .pending_plsql_label_body_indent()
                                    .unwrap_or(line_indent)
                            } else {
                                line_indent
                            };
                        for (segment_idx, segment) in rendered_word.split('\n').enumerate() {
                            if segment_idx == 0 {
                                out.push_str(segment);
                                continue;
                            }
                            out.push('\n');
                            current_rendered_line_paren_depth.set(0);
                            at_line_start = true;
                            line_indent = line_indent.max(continuation_indent);
                            if segment.is_empty() {
                                continue;
                            }
                            ensure_indent(&mut out, &mut at_line_start, line_indent);
                            out.push_str(segment);
                        }
                    } else {
                        out.push_str(rendered_word);
                    }
                    if upper == "WITH"
                        && is_keyword
                        && !keyword_preserves_original_case
                        && !with_cte_keyword_inside_function_local
                    {
                        format_stack.with_cte_record_owner_depth(line_indent);
                    }
                    needs_space = true;
                    if format_stack.pending_split_end_suffix_indent().is_some() {
                        format_stack.advance_pending_split_end_suffix(upper);
                    }
                    if is_package_body_statement
                        && format_stack.last_block_kind_is("PACKAGE_BODY")
                        && matches!(upper, "PROCEDURE" | "FUNCTION")
                    {
                        active_package_member_name = next_non_comment_idx
                            .and_then(&word_upper_at)
                            .map(Cow::into_owned);
                    }
                    if upper == "SELECT" {
                        activate_select_list_layout_state!(
                            out.len(),
                            line_indent.saturating_add(1)
                        );
                    } else if upper == "WINDOW"
                        && matches!(format_stack.current_clause(), Some("WINDOW"))
                    {
                        newline_after_keyword = true;
                        newline_after_keyword_extra = 1;
                    }

                    if prev_word_upper == Some("COMPOUND") && upper == "TRIGGER" {
                        format_stack.clear_trigger_header();
                        if !format_stack.last_block_kind_is("COMPOUND_TRIGGER") {
                            format_stack.push_block(
                                BlockKind::CompoundTrigger,
                                indent_level,
                                indent_level.saturating_add(1),
                                &mut indent_level,
                            );
                        }
                        let compound_scope = format_stack.current_scope();
                        format_stack.push_compound_trigger(compound_scope);
                        format_stack.compound_trigger_enter_outer_body();
                    }

                    if matches!(upper, "IS" | "AS" | "BEGIN" | "DECLARE") {
                        format_stack.compound_trigger_begin_timing_point_body();
                    }

                    if construct_flag_active!(CreateTableParenExpected)
                        && upper == "AS"
                        && (next_word_is("SELECT")
                            || next_word_is("WITH")
                            || next_word_is("VALUES"))
                    {
                        deactivate_construct_flag!(CreateTableParenExpected);
                    }

                    // CURSOR ... IS/AS → indent the SQL body
                    if upper == "CURSOR" && in_plsql_block {
                        activate_construct_flag!(CursorDeclPending, current_scope);
                    }
                    if matches!(upper, "IS" | "AS") && construct_flag_active!(CursorDeclPending) {
                        deactivate_construct_flag!(CursorDeclPending);
                        let cursor_new_indent = indent_level.max(line_indent).saturating_add(1);
                        let cursor_delta = cursor_new_indent.saturating_sub(indent_level);
                        format_stack.push_scoped_indent(
                            ScopedIndentKind::CursorSql,
                            current_scope,
                            cursor_delta,
                            &mut indent_level,
                        );
                        newline_after_keyword = true;
                    }

                    // FORALL → indent the DML body
                    if upper == "FORALL" && in_plsql_block {
                        activate_construct_flag!(ForallPending, current_scope);
                    }

                    // EXECUTE IMMEDIATE tracking
                    if upper == "EXECUTE" && next_word_is("IMMEDIATE") {
                        activate_construct_flag!(ExecuteImmediateActive, current_scope);
                    }

                    // REFERENCES → suppress ON DELETE/UPDATE (referential actions)
                    if upper == "REFERENCES" {
                        activate_construct_flag!(ReferentialActionPending, current_scope);
                    }
                    if construct_flag_active!(ReferentialActionPending) && upper == "ON" {
                        deactivate_construct_flag!(ReferentialActionPending);
                        activate_construct_flag!(ReferentialOnActive, current_scope);
                    }
                    if construct_flag_active!(ReferentialOnActive)
                        && !matches!(
                            upper,
                            "ON" | "DELETE"
                                | "UPDATE"
                                | "SET"
                                | "CASCADE"
                                | "RESTRICT"
                                | "NO"
                                | "ACTION"
                                | "NULL"
                        )
                    {
                        deactivate_construct_flag!(ReferentialOnActive);
                    }

                    // COMMENT ON tracking
                    if upper == "COMMENT" && next_word_is("ON") {
                        activate_construct_flag!(CommentOnActive, current_scope);
                    }
                    if construct_flag_active!(CommentOnActive) && upper == "ON" {
                        deactivate_construct_flag!(CommentOnActive); // one-shot: suppress ON, then done
                    }

                    // GRANT/REVOKE tracking
                    if matches!(upper, "GRANT" | "REVOKE") && indent_level == 0 {
                        activate_construct_flag!(GrantRevokeActive, current_scope);
                    }
                    // GRANT/REVOKE ends at ON (for privilege grants) or TO/FROM
                    if construct_flag_active!(GrantRevokeActive) && matches!(upper, "TO" | "FROM") {
                        deactivate_construct_flag!(GrantRevokeActive);
                    }

                    // RETURNING → suppress next INTO
                    if upper == "RETURNING" {
                        activate_construct_flag!(ReturningActive, current_scope);
                    }
                    if construct_flag_active!(ReturningActive) && upper == "INTO" {
                        deactivate_construct_flag!(ReturningActive);
                    }

                    // FETCH cursor → suppress INTO/LIMIT/BULK (PL/SQL FETCH, not SQL FETCH FIRST)
                    if upper == "FETCH" && in_plsql_block {
                        activate_construct_flag!(FetchActive, current_scope);
                    }

                    // BULK COLLECT → suppress next INTO
                    if upper == "BULK" && next_word_is("COLLECT") {
                        activate_construct_flag!(BulkCollectActive, current_scope);
                    }
                    if construct_flag_active!(BulkCollectActive) && upper == "INTO" {
                        deactivate_construct_flag!(BulkCollectActive);
                    }

                    // MERGE tracking
                    if upper == "MERGE"
                        && matches!(format_stack.current_clause(), None | Some("MERGE"))
                    {
                        activate_construct_flag!(MergeActive, current_scope);
                    }

                    // INSERT ALL/FIRST tracking
                    if matches!(upper, "ALL" | "FIRST")
                        && matches!(prev_word_upper, Some("INSERT"))
                        && !construct_flag_active_at_paren_depth!(
                            InsertAllActive,
                            current_scope.paren_depth
                        )
                    {
                        activate_construct_flag!(InsertAllActive, current_scope);
                    }

                    let create_block_disallowed_for_compound_trigger =
                        matches!(construct_value_as_deref!(CreateObject), Some("TRIGGER"))
                            && format_stack.compound_trigger_is_in_outer_body();
                    let starts_create_block = matches!(upper, "AS" | "IS")
                        && !as_belongs_to_constructor_result_clause
                        && !format_stack.trigger_header_is_active()
                        && !create_block_disallowed_for_compound_trigger
                        && (construct_value_is_some!(CreateObject)
                            || construct_flag_active!(RoutineDeclPending)
                            || with_plsql_body_starts_here);
                    let starts_compound_trigger_body = starts_create_block
                        && format_stack.compound_trigger_awaiting_outer_body_start()
                        && matches!(construct_value_as_deref!(CreateObject), Some("TRIGGER"));

                    // Handle block start - push to stack and increase indent
                    if should_treat_as_block_start {
                        let new_indent_level = indent_level.max(line_indent).saturating_add(1);
                        if let Some(block_kind) = BlockKind::from_keyword(upper) {
                            format_stack.push_block(
                                block_kind,
                                line_indent,
                                new_indent_level,
                                &mut indent_level,
                            );
                        }
                        if matches!(upper, "BEGIN" | "DECLARE" | "IF") {
                            format_stack.push_plsql_context(format_stack.current_scope());
                        }
                        set_current_clause!(None);
                    } else if upper == "BEGIN" {
                        if matches!(construct_value_as_deref!(CreateObject), Some("TRIGGER")) {
                            format_stack.clear_trigger_header();
                        }
                        let package_initializer_begin = pending_package_member_separator;
                        let inside_declare = format_stack.last_block_kind().is_some_and(|s| {
                            matches!(s, BlockKind::Declare | BlockKind::PackageBody)
                        }) || package_initializer_begin;
                        if inside_declare {
                            // DECLARE ... BEGIN - same block depth.
                            // PACKAGE BODY initialization BEGIN is also same depth as PACKAGE_BODY.
                            if package_initializer_begin {
                                while format_stack
                                    .last_block_kind()
                                    .is_some_and(|block| block != BlockKind::PackageBody)
                                {
                                    let _ = format_stack.pop_block(&mut indent_level);
                                }
                                format_stack.realign_indent_level_to_base(&mut indent_level);
                                pending_package_member_separator = false;
                            } else if format_stack.last_block_kind_is(BlockKind::Declare) {
                                format_stack.set_last_block_kind(BlockKind::Begin);
                            } else if format_stack.package_body_member_indent().is_some() {
                                // Package initializer body always starts one level under
                                // the package owner, regardless of prior member-body depth.
                                format_stack.realign_indent_level_to_base(&mut indent_level);
                            }
                            // DECLARE keeps current depth; PACKAGE BODY resets to owner+1.
                        } else {
                            // Standalone BEGIN block
                            format_stack.push_block(
                                BlockKind::Begin,
                                line_indent,
                                indent_level.saturating_add(1),
                                &mut indent_level,
                            );
                        }
                        format_stack.push_plsql_context(format_stack.current_scope());
                        if mysql_handler_block_begin {
                            let _ = format_stack.mark_last_block_mysql_handler_begin();
                        }
                        if mysql_compatible && construct_value_is_some!(CreateObject) {
                            // MySQL/MariaDB stored routines and triggers enter
                            // their executable body at BEGIN instead of AS/IS.
                            // Once we cross that boundary, body-local `AS`
                            // tokens such as `CAST (... AS CHAR)` or
                            // `alias AS col_name` must no longer be treated as
                            // declaration-section starters.
                            clear_construct_value!(CreateObject);
                            deactivate_construct_flag!(RoutineDeclPending);
                        }
                        set_current_clause!(None);
                    } else if upper == "EXCEPTION" {
                        format_stack.push_block(
                            BlockKind::Exception,
                            line_indent,
                            indent_level,
                            &mut indent_level,
                        );
                        set_current_clause!(None);
                    } else if upper == "LOOP" {
                        let new_indent_level = indent_level.max(line_indent).saturating_add(1);
                        format_stack.push_block(
                            BlockKind::Loop,
                            line_indent,
                            new_indent_level,
                            &mut indent_level,
                        );
                        format_stack.push_plsql_context(format_stack.current_scope());
                        set_current_clause!(None);
                    } else if upper == "REPEAT" {
                        format_stack.push_block(
                            BlockKind::Repeat,
                            line_indent,
                            indent_level.saturating_add(1),
                            &mut indent_level,
                        );
                        format_stack.push_plsql_context(format_stack.current_scope());
                        set_current_clause!(None);
                    } else if upper == "CASE" {
                        let new_indent_level = indent_level.max(line_indent).saturating_add(1);
                        format_stack.push_block(
                            BlockKind::Case,
                            line_indent,
                            new_indent_level,
                            &mut indent_level,
                        );
                        if format_stack.current_clause().is_none() {
                            format_stack.push_plsql_context(format_stack.current_scope());
                        }
                    } else if starts_create_block {
                        if matches!(construct_value_as_deref!(CreateObject), Some("TRIGGER")) {
                            format_stack.clear_trigger_header();
                        }
                        // Treat AS/IS in CREATE PACKAGE/PROC/FUNC/TYPE/TRIGGER and package-body routines as declaration section start
                        let is_package_body = matches!(
                            construct_value_as_deref!(CreateObject),
                            Some("PACKAGE_BODY")
                        );
                        let package_member_owner_depth = is_package_body_statement
                            .then_some(())
                            .filter(|_| !is_package_body)
                            .and_then(|_| format_stack.last_block_kind())
                            .filter(|block| *block == BlockKind::PackageBody)
                            .and_then(|_| {
                                format_stack
                                    .last_block_owner_depth()
                                    .map(|depth| depth.saturating_add(1))
                            });
                        let new_indent_level = if starts_compound_trigger_body {
                            indent_level.saturating_add(1)
                        } else if let Some(member_owner_depth) = package_member_owner_depth {
                            member_owner_depth.saturating_add(1)
                        } else {
                            indent_level.max(line_indent).saturating_add(1)
                        };
                        if starts_compound_trigger_body {
                            format_stack.push_block(
                                BlockKind::CompoundTrigger,
                                indent_level,
                                new_indent_level,
                                &mut indent_level,
                            );
                            format_stack.compound_trigger_enter_outer_body();
                        } else if is_package_body {
                            format_stack.push_block(
                                BlockKind::PackageBody,
                                line_indent,
                                new_indent_level,
                                &mut indent_level,
                            );
                        } else {
                            format_stack.push_block(
                                BlockKind::Declare,
                                package_member_owner_depth.unwrap_or(line_indent),
                                new_indent_level,
                                &mut indent_level,
                            );
                        }
                        format_stack.push_plsql_context(format_stack.current_scope());
                        clear_construct_value!(CreateObject);
                        deactivate_construct_flag!(RoutineDeclPending);
                        set_current_clause!(None);
                        newline_with(
                            &mut out,
                            indent_level,
                            0,
                            &mut at_line_start,
                            &mut needs_space,
                            &mut line_indent,
                        );
                    }

                    if upper == "BEGIN" || (upper == "DECLARE" && !mysql_compound_declare) {
                        if upper == "BEGIN"
                            && matches!(construct_value_as_deref!(CreateObject), Some("TRIGGER"))
                        {
                            clear_construct_value!(CreateObject);
                            deactivate_construct_flag!(RoutineDeclPending);
                        }
                        format_stack.compound_trigger_begin_timing_point_body();
                        if !mysql_begin_not_atomic_header {
                            newline_with(
                                &mut out,
                                base_indent!(indent_level),
                                0,
                                &mut at_line_start,
                                &mut needs_space,
                                &mut line_indent,
                            );
                        }
                    }

                    if mysql_begin_not_atomic_header {
                        mysql_begin_not_atomic_header_open = true;
                    } else if mysql_begin_not_atomic_header_open
                        && upper == "ATOMIC"
                        && matches!(prev_word_upper, Some("NOT"))
                    {
                        mysql_begin_not_atomic_header_open = false;
                        newline_with(
                            &mut out,
                            base_indent!(indent_level),
                            0,
                            &mut at_line_start,
                            &mut needs_space,
                            &mut line_indent,
                        );
                    }

                    if let Some(parenthesized) = pending_control_header_parenthesized {
                        format_stack.push_condition_owner(
                            ConditionOwnerKind::ControlHeader,
                            format_stack.current_scope(),
                            line_indent,
                            parenthesized,
                        );
                    }

                    if newline_after_keyword {
                        let current_output_line = rendered_line_tracker.current_line(&out);
                        let current_output_line_indent = rendered_line_indent(current_output_line);
                        let previous_output_line = rendered_line_tracker.previous_line(&out);
                        let case_branch_body_indent = (in_plsql_block
                            && format_stack.last_block_kind_is("CASE")
                            && matches!(upper, "THEN" | "ELSE"))
                        .then_some(
                            current_output_line_indent.saturating_add(newline_after_keyword_extra),
                        );
                        let control_condition_body_indent = matches!(upper, "THEN" | "LOOP" | "DO")
                            .then(|| {
                                let rendered_body_indent =
                                    current_output_line_indent.saturating_add(1);
                                let split_close_condition_body_indent = matches!(upper, "THEN")
                                    .then_some(())
                                    .filter(|_| {
                                        let trimmed_current = current_output_line.trim_start();
                                        let trimmed_previous = previous_output_line.trim_end();
                                        (trimmed_current == "AND"
                                            || trimmed_current.starts_with("AND ")
                                            || trimmed_current == "OR"
                                            || trimmed_current.starts_with("OR "))
                                            && trimmed_previous.ends_with(')')
                                    })
                                    .map(|_| rendered_body_indent);
                                closed_control_header_body_indent
                                    .or(split_close_condition_body_indent)
                                    .or_else(|| {
                                        matches!(upper, "THEN")
                                            .then(|| {
                                                format_stack
                                                    .last_condition_owner(
                                                        ConditionOwnerKind::ControlHeader,
                                                    )
                                                    .map(|frame| frame.indent.saturating_add(1))
                                            })
                                            .flatten()
                                    })
                                    .or_else(|| {
                                        matches!(upper, "LOOP" | "DO")
                                            .then_some(rendered_body_indent)
                                    })
                            })
                            .flatten();
                        let control_body_extra_delta =
                            control_condition_body_indent.and_then(|_| {
                                let base_body_indent = base_indent!(indent_level);
                                let rendered_body_indent =
                                    current_output_line_indent.saturating_add(1);
                                rendered_body_indent
                                    .checked_sub(base_body_indent)
                                    .filter(|delta| *delta > 0)
                            });
                        newline_with(
                            &mut out,
                            case_branch_body_indent
                                .or(control_condition_body_indent)
                                .unwrap_or_else(|| base_indent!(indent_level)),
                            usize::from(
                                control_condition_body_indent.is_none()
                                    && case_branch_body_indent.is_none(),
                            ) * newline_after_keyword_extra,
                            &mut at_line_start,
                            &mut needs_space,
                            &mut line_indent,
                        );
                        if let Some(delta) = control_body_extra_delta {
                            format_stack.push_scoped_indent(
                                ScopedIndentKind::SplitControlBody,
                                current_scope,
                                delta,
                                &mut indent_level,
                            );
                        }
                    }

                    if upper == "BETWEEN" {
                        format_stack.push_between_pending(current_scope);
                    } else if upper == "AND" && between_pending {
                        let _ = format_stack.pop_between_pending_if_matches(current_scope);
                    }
                    if matches!(upper, "THEN" | "LOOP" | "DO") {
                        let _ = format_stack
                            .pop_last_condition_owner(ConditionOwnerKind::ControlHeader);
                    }
                    if is_create_index_on {
                        deactivate_construct_flag!(CreateIndexPending);
                    }
                    exit_condition_state.on_keyword(upper);

                    // MySQL ON DUPLICATE KEY UPDATE: activate flag so that VALUES(col)
                    // inside the UPDATE assignment list is treated as a function call.
                    // Treat the assignment list itself like SET so comma siblings
                    // indent one body level deeper than the ON DUPLICATE owner.
                    if upper == "UPDATE"
                        && format_stack.paren_is_empty()
                        && Self::is_mysql_on_duplicate_key_update_from_indices(
                            tokens,
                            &recent_statement_word_indices,
                        )
                    {
                        on_duplicate_key_update_active = true;
                        set_current_clause!(Some("SET"));
                        clear_select_list_layout_state!();
                    }
                    if should_break_condition
                        && upper == "ON"
                        && !format_stack.trigger_header_is_active()
                    {
                        format_stack.pop_condition_owners_for_push(
                            ConditionOwnerKind::JoinOn,
                            current_scope,
                        );
                        format_stack.push_condition_owner(
                            ConditionOwnerKind::JoinOn,
                            current_scope,
                            line_indent,
                            false,
                        );
                    } else if clause_keywords.contains(&upper)
                        && matches!(upper, "WHERE" | "HAVING")
                    {
                        format_stack.pop_condition_owners_for_push(
                            ConditionOwnerKind::Where,
                            current_scope,
                        );
                        format_stack.push_condition_owner(
                            ConditionOwnerKind::Where,
                            current_scope,
                            line_indent,
                            false,
                        );
                    } else if upper == join_keyword
                        || upper == "APPLY"
                        || (clause_keywords.contains(&upper) && upper != "ON")
                    {
                        format_stack.pop_condition_owners_for_push(
                            ConditionOwnerKind::JoinOn,
                            current_scope,
                        );
                        format_stack.pop_condition_owners_for_push(
                            ConditionOwnerKind::Where,
                            current_scope,
                        );
                    }
                    if upper == "WHEN" && format_stack.last_block_kind_is("CASE") {
                        format_stack
                            .pop_condition_owners_for_push(ConditionOwnerKind::Case, current_scope);
                        format_stack.push_condition_owner(
                            ConditionOwnerKind::Case,
                            current_scope,
                            line_indent,
                            false,
                        );
                    } else if matches!(upper, "THEN" | "ELSE") {
                        let _ = format_stack.pop_last_condition_owner(ConditionOwnerKind::Case);
                    }

                    prev_word_idx = Some(idx);
                }
                SqlToken::String(literal) => {
                    let mysql_handler_body_line_pending =
                        at_line_start && mysql_handler_state.body_line_pending();
                    let mysql_handler_header_literal = mysql_handler_state.consume_string();
                    if mysql_handler_state.body_line_pending() {
                        let pending_indent = rendered_line_tracker
                            .current_line_indent(&out, line_indent)
                            .saturating_add(1);
                        let pending_indent = format_stack
                            .mysql_handler_pending_body_indent()
                            .unwrap_or(pending_indent);
                        format_stack
                            .set_mysql_handler_pending_body_indent(current_scope, pending_indent);
                    }
                    if mysql_handler_body_line_pending && !mysql_handler_header_literal {
                        let handler_body_indent = format_stack
                            .mysql_handler_pending_body_indent()
                            .unwrap_or_else(|| {
                                line_indent
                                    .max(base_indent!(indent_level))
                                    .saturating_add(1)
                            });
                        line_indent = line_indent.max(handler_body_indent);
                        let _ = format_stack.clear_mysql_handler_pending_body_indent();
                    }
                    ensure_indent(&mut out, &mut at_line_start, line_indent);
                    if needs_space {
                        out.push(' ');
                    }
                    out.push_str(literal);
                    needs_space = true;
                    if literal.contains('\n') {
                        at_line_start = literal.ends_with('\n');
                    }
                }
                SqlToken::Comment(comment) => {
                    let mut has_leading_newline = comment.starts_with('\n');
                    let raw_comment_body = if has_leading_newline {
                        &comment[1..]
                    } else {
                        comment.as_str()
                    };
                    let trimmed_comment = raw_comment_body.trim_end_matches('\n');
                    let is_block_comment =
                        trimmed_comment.starts_with("/*") && trimmed_comment.ends_with("*/");
                    let is_hint_comment = trimmed_comment.starts_with("/*+");
                    let hint_after_select =
                        is_hint_comment && matches!(prev_word_upper, Some("SELECT"));
                    if hint_after_select {
                        has_leading_newline = false;
                        trim_trailing_space(&mut out);
                        if out.ends_with('\n') {
                            out.pop();
                        }
                        at_line_start = false;
                    }
                    let comment_body = if has_leading_newline {
                        &comment[1..]
                    } else {
                        raw_comment_body
                    };
                    let is_multiline_block_comment =
                        is_block_comment && trimmed_comment.contains('\n');
                    let next_is_word_like = matches!(
                        immediate_next_token,
                        Some(SqlToken::Word(_) | SqlToken::String(_))
                    );
                    let in_select_list = matches!(format_stack.current_clause(), Some("SELECT"));
                    let in_set_clause = matches!(format_stack.current_clause(), Some("SET"));
                    let top_level_select_list = in_select_list
                        && suppress_comma_break_depth == 0
                        && format_stack.paren_is_empty();
                    let top_level_set_list = in_set_clause
                        && suppress_comma_break_depth == 0
                        && format_stack.paren_is_empty();
                    let active_list_layout = select_list_layout_state!().has_active_indent();
                    let comment_structural_prefix = comment_prefix_text.as_str();
                    let previous_non_comment_token = loop_previous_non_comment_token;
                    let second_previous_non_comment_token = loop_second_previous_non_comment_token;
                    let previous_token_requires_same_depth = previous_non_comment_token
                        .is_some_and(|token| match token {
                            SqlToken::Word(word) => {
                                word.eq_ignore_ascii_case("WITH")
                                    || word.eq_ignore_ascii_case("KEEP")
                            }
                            _ => false,
                        });
                    let previous_token = second_previous_non_comment_token
                        .and_then(Self::format_trailing_meaningful_token_from_sql_token);
                    let last_token = previous_non_comment_token
                        .and_then(Self::format_trailing_meaningful_token_from_sql_token);
                    let temporal_boundary_comment_continuation = matches!(
                        (
                            second_previous_non_comment_token,
                            previous_non_comment_token
                        ),
                        (
                            Some(SqlToken::Word(previous_word)),
                            Some(SqlToken::Word(last_word))
                        ) if (previous_word.eq_ignore_ascii_case("BETWEEN")
                            || previous_word.eq_ignore_ascii_case("OF"))
                            && crate::sql_text::is_format_temporal_boundary_keyword(
                                last_word
                            )
                    );
                    let trailing_operator_continuation = last_token.is_some_and(|last| {
                        crate::sql_text::format_trailing_continuation_operator_kind_from_token_pair(
                            previous_token,
                            last,
                        )
                        .is_some()
                    });
                    let continuation_keyword = previous_non_comment_token
                        .and(prev_non_comment_idx)
                        .and_then(&word_upper_at)
                        .is_some_and(|last_upper| {
                            crate::sql_text::is_format_comment_continuation_keyword(
                                last_upper.as_ref(),
                            )
                        });
                    let comment_header_continuation_kind = if previous_token_requires_same_depth {
                        Some(crate::sql_text::FormatInlineCommentHeaderContinuationKind::SameDepth)
                    } else if temporal_boundary_comment_continuation {
                        Some(
                            crate::sql_text::FormatInlineCommentHeaderContinuationKind::OneDeeperThanCurrentLine,
                        )
                    } else {
                        Self::comment_header_continuation_kind_for_prefix(comment_structural_prefix)
                    };
                    let keeps_next_line_continuation = trailing_operator_continuation
                        || continuation_keyword
                        || comment_header_continuation_kind.is_some();
                    let attachment = Self::classify_comment_attachment(
                        has_rendered_content,
                        at_line_start,
                        has_leading_newline,
                        is_multiline_block_comment,
                    );
                    let comment_ends_source_line = comment_body.contains('\n');
                    let attached_previous_keeps_inline = matches!(
                        attachment,
                        CommentAttachment::Previous
                    ) && !(comment_ends_source_line
                        && matches!(next_non_comment, Some(SqlToken::Symbol(s)) if s == ","));
                    if (top_level_select_list || top_level_set_list)
                        && !has_leading_newline
                        && !is_hint_comment
                        && !attached_previous_keeps_inline
                    {
                        force_select_list_newline!(&mut out);
                    }

                    if is_multiline_block_comment
                        && !at_line_start
                        && !matches!(attachment, CommentAttachment::Previous)
                    {
                        newline_with(
                            &mut out,
                            0,
                            0,
                            &mut at_line_start,
                            &mut needs_space,
                            &mut line_indent,
                        );
                    }

                    if matches!(
                        attachment,
                        CommentAttachment::Next | CommentAttachment::Block
                    ) && has_leading_newline
                        && !at_line_start
                    {
                        trim_trailing_space(&mut out);
                        newline_with(
                            &mut out,
                            base_indent!(indent_level),
                            0,
                            &mut at_line_start,
                            &mut needs_space,
                            &mut line_indent,
                        );
                    } else if matches!(attachment, CommentAttachment::Previous) && !at_line_start {
                        trim_trailing_space(&mut out);
                        if !out.ends_with('\n') {
                            out.push(' ');
                        }
                    }

                    let comment_starts_line = at_line_start;
                    if comment_starts_line {
                        let base = base_indent!(indent_level);
                        let paren_extra = Self::paren_extra_depth(&format_stack);
                        let in_column_list = format_stack
                            .last_paren()
                            .is_some_and(|frame| frame.is_column_list());
                        let current_list_indent =
                            if matches!(format_stack.current_clause(), Some("SET"))
                                && construct_flag_active!(MergeActive)
                            {
                                base
                            } else {
                                active_list_indent!(
                                    indent_level,
                                    open_cursor_state!(),
                                    select_list_layout_state!(),
                                    format_stack.current_clause(),
                                    construct_flag_active!(MergeActive),
                                    in_column_list,
                                )
                            };
                        let next_is_comma = matches!(
                            next_non_comment,
                            Some(SqlToken::Symbol(s)) if s == ","
                        );
                        let next_is_close_paren = matches!(
                            next_non_comment,
                            Some(SqlToken::Symbol(s)) if s == ")"
                        );
                        let next_word_upper_ref = next_non_comment_idx.and_then(&word_upper_at);
                        let next_word_upper = next_word_upper_ref.as_deref();
                        let next_is_condition_keyword = if let Some(upper) = next_word_upper {
                            condition_keywords.contains(&upper)
                                && !(upper == "AND"
                                    && format_stack.between_pending_matches(current_scope))
                        } else {
                            false
                        };
                        let next_is_else = matches!(
                            next_non_comment,
                            Some(SqlToken::Word(w))
                                if w.eq_ignore_ascii_case("ELSE")
                                    || w.eq_ignore_ascii_case("ELSIF")
                                    || w.eq_ignore_ascii_case("EXCEPTION")
                        );
                        let in_clause_list_body = matches!(
                            format_stack.current_clause(),
                            Some("FROM" | "GROUP" | "HAVING" | "ORDER" | "WINDOW" | "INTO")
                        );
                        let in_confirmed_list = in_set_clause
                            || in_clause_list_body
                            || in_column_list
                            || ((in_select_list || active_list_layout)
                                && select_list_layout_state!().is_multiline())
                            || next_is_comma
                            || next_is_condition_keyword;
                        let top_level_trailing_comment = next_non_comment.is_none()
                            && format_stack.paren_is_empty()
                            && format_stack.block_depth() == 0;
                        if let Some(split_end_indent) =
                            format_stack.pending_split_end_suffix_indent()
                        {
                            line_indent = split_end_indent;
                        } else if top_level_trailing_comment {
                            line_indent = 0;
                        } else if next_is_close_paren {
                            line_indent = format_stack
                                .last_paren()
                                .map(|frame| {
                                    if frame.closes_indented() {
                                        frame.close_indent()
                                    } else {
                                        frame.compact_multiline_close_indent()
                                    }
                                })
                                .unwrap_or(base);
                        } else if next_is_else {
                            // Align comment with ELSE/ELSIF/EXCEPTION which
                            // renders at one level below the current body in
                            // IF blocks, or at base+1 in CASE blocks.
                            // EXCEPTION drops to the enclosing BEGIN level
                            // just like ELSE does in IF blocks.
                            let in_if_block = format_stack.last_block_kind_is("IF");
                            let in_case_block = format_stack.last_block_kind_is("CASE");
                            let next_is_exception = next_word_upper == Some("EXCEPTION");
                            line_indent = if next_is_exception {
                                // EXCEPTION always drops to the enclosing
                                // BEGIN level regardless of block type.
                                base_indent!(indent_level.saturating_sub(1))
                            } else if in_if_block {
                                base_indent!(indent_level.saturating_sub(1))
                            } else if in_case_block {
                                format_stack
                                    .last_block_owner_depth()
                                    .map(|depth| depth.saturating_add(1))
                                    .unwrap_or_else(|| base.saturating_add(1))
                            } else {
                                base + 1 + paren_extra
                            };
                        } else if next_is_condition_keyword {
                            // Condition keywords (AND/OR/WHEN/ON) are
                            // indented at base+1, matching the token
                            // handler's paren_extra offset.
                            let case_branch_indent = next_word_upper_ref
                                .as_deref()
                                .filter(|word| *word == "WHEN")
                                .filter(|_| format_stack.last_block_kind_is("CASE"))
                                .and_then(|_| {
                                    format_stack
                                        .last_block_owner_depth()
                                        .map(|depth| depth.saturating_add(1))
                                });
                            let join_condition_indent = next_word_upper
                                .filter(|word| matches!(*word, "AND" | "OR"))
                                .and_then(|_| {
                                    format_stack
                                        .last_condition_owner(ConditionOwnerKind::JoinOn)
                                        .map(|frame| (frame.scope.paren_depth, frame.indent))
                                })
                                .filter(|(paren_depth, _)| {
                                    current_scope.paren_depth >= *paren_depth
                                })
                                .map(|(owner_paren_depth, owner_depth)| {
                                    owner_depth.saturating_add(1).saturating_add(
                                        current_scope.paren_depth.saturating_sub(owner_paren_depth),
                                    )
                                });
                            line_indent = case_branch_indent
                                .or(join_condition_indent)
                                .unwrap_or_else(|| {
                                    base.saturating_add(1).saturating_add(paren_extra)
                                });
                        } else if in_confirmed_list {
                            line_indent = current_list_indent;
                        } else if line_indent == 0 {
                            line_indent = format_stack
                                .last_paren()
                                .and_then(|frame| frame.sibling_body_indent())
                                .unwrap_or(base);
                        }
                        ensure_indent(&mut out, &mut at_line_start, line_indent);
                    }

                    let rendered_comment_body = if matches!(attachment, CommentAttachment::Previous)
                        && !comment_starts_line
                        && comment_body.trim_start().starts_with("--")
                    {
                        comment_body.trim_start()
                    } else {
                        comment_body
                    };

                    out.push_str(rendered_comment_body);

                    needs_space = true;
                    if hint_after_select {
                        if !out.ends_with('\n') {
                            out.push('\n');
                            current_rendered_line_paren_depth.set(0);
                        }
                        at_line_start = true;
                        needs_space = false;
                        line_indent = base_indent!(indent_level).saturating_add(1);
                        set_select_list_layout_state!(SelectListLayoutState::Multiline {
                            indent: line_indent,
                            hanging_indent_spaces: None,
                        });
                    } else if is_multiline_block_comment {
                        at_line_start = true;
                        needs_space = false;
                        if !out.ends_with('\n') {
                            out.push('\n');
                            current_rendered_line_paren_depth.set(0);
                        }
                        let in_column_list = format_stack
                            .last_paren()
                            .is_some_and(|frame| frame.is_column_list());
                        if in_select_list
                            || in_set_clause
                            || active_list_layout
                            || in_column_list
                            || hint_after_select
                        {
                            let list_indent = active_list_indent!(
                                indent_level,
                                open_cursor_state!(),
                                select_list_layout_state!(),
                                format_stack.current_clause(),
                                construct_flag_active!(MergeActive),
                                in_column_list,
                            );
                            line_indent = list_indent;
                            if hint_after_select {
                                set_select_list_layout_state!(SelectListLayoutState::Multiline {
                                    indent: list_indent,
                                    hanging_indent_spaces: None,
                                });
                            }
                        } else {
                            line_indent = base_indent!(indent_level);
                        }
                    } else if comment_body.ends_with('\n') || comment_body.contains('\n') {
                        at_line_start = true;
                        needs_space = false;
                        let starts_after_open_paren = previous_non_comment_token.is_some_and(
                            |token| matches!(token, SqlToken::Symbol(sym) if sym == "("),
                        );
                        if starts_after_open_paren && !comment_starts_line {
                            let opener_body_indent = format_stack
                                .last_paren()
                                .map(|frame| {
                                    if frame.opens_indented() {
                                        base_indent!(indent_level)
                                    } else {
                                        rendered_line_tracker
                                            .current_line_indent(&out, line_indent)
                                            .saturating_add(1)
                                    }
                                })
                                .unwrap_or_else(|| {
                                    rendered_line_tracker
                                        .current_line_indent(&out, line_indent)
                                        .saturating_add(1)
                                });
                            inline_comment_continuation_state =
                                InlineCommentContinuationState::Operand {
                                    indent: opener_body_indent,
                                };
                        } else if keeps_next_line_continuation && !comment_starts_line {
                            let in_column_list = format_stack
                                .last_paren()
                                .is_some_and(|frame| frame.is_column_list());
                            let base = base_indent!(indent_level);
                            let list_continuation_indent =
                                if matches!(format_stack.current_clause(), Some("SET"))
                                    && construct_flag_active!(MergeActive)
                                {
                                    base
                                } else {
                                    active_list_indent!(
                                        indent_level,
                                        open_cursor_state!(),
                                        select_list_layout_state!(),
                                        format_stack.current_clause(),
                                        construct_flag_active!(MergeActive),
                                        in_column_list,
                                    )
                                };
                            let continuation_indent = if matches!(
                                comment_header_continuation_kind,
                                Some(crate::sql_text::FormatInlineCommentHeaderContinuationKind::SameDepth)
                            ) {
                                line_indent
                            } else if in_select_list
                                || in_set_clause
                                || active_list_layout
                                || in_column_list
                                || hint_after_select
                            {
                                list_continuation_indent
                            } else if matches!(format_stack.current_clause(), Some("ON" | "USING")) {
                                line_indent
                            } else if comment_header_continuation_kind.is_some() {
                                let fallback_indent = base.saturating_add(usize::from(
                                    format_stack.current_clause().is_some() || suppress_comma_break_depth > 0,
                                ));
                                let min_indent = Self::resolve_comment_header_continuation_indent(
                                    comment_header_continuation_kind,
                                    line_indent,
                                    base,
                                    line_indent,
                                    fallback_indent,
                                );
                                line_indent.max(min_indent)
                            } else {
                                line_indent.max(
                                    base.saturating_add(usize::from(
                                        format_stack.current_clause().is_some() || suppress_comma_break_depth > 0,
                                    )),
                                )
                            };
                            inline_comment_continuation_state =
                                InlineCommentContinuationState::Operand {
                                    indent: continuation_indent,
                                };
                        } else {
                            let in_column_list = format_stack
                                .last_paren()
                                .is_some_and(|frame| frame.is_column_list());
                            if in_select_list
                                || in_set_clause
                                || active_list_layout
                                || in_column_list
                                || hint_after_select
                            {
                                let list_indent =
                                    if in_set_clause && construct_flag_active!(MergeActive) {
                                        base_indent!(indent_level)
                                    } else {
                                        active_list_indent!(
                                            indent_level,
                                            open_cursor_state!(),
                                            select_list_layout_state!(),
                                            format_stack.current_clause(),
                                            construct_flag_active!(MergeActive),
                                            in_column_list,
                                        )
                                    };
                                line_indent = list_indent;
                                if (in_select_list || in_set_clause)
                                    && !select_list_layout_state!().is_multiline()
                                    && comment_starts_line
                                {
                                    set_select_list_layout_state!(
                                        SelectListLayoutState::Multiline {
                                            indent: list_indent,
                                            hanging_indent_spaces: None,
                                        }
                                    );
                                }
                            }
                        }
                    } else if is_block_comment && next_is_word_like {
                        let keep_inline_as_is_alias_comment = matches!(
                            previous_non_comment_token,
                            Some(SqlToken::Word(previous_word))
                                if previous_word.eq_ignore_ascii_case("AS")
                                    || previous_word.eq_ignore_ascii_case("IS")
                        );
                        let keep_inline_alias_comment = keep_inline_as_is_alias_comment
                            || Self::block_comment_keeps_inline_terminal_close_alias_tail(
                                rendered_line_tracker.current_line(&out),
                                tokens,
                                token_context,
                                format_stack.current_clause(),
                            );
                        if !keep_inline_alias_comment {
                            if keeps_next_line_continuation {
                                let in_column_list = format_stack
                                    .last_paren()
                                    .is_some_and(|frame| frame.is_column_list());
                                let base = base_indent!(indent_level);
                                let starts_after_open_paren =
                                    previous_non_comment_token.is_some_and(
                                        |token| {
                                            matches!(token, SqlToken::Symbol(sym) if sym == "(")
                                        },
                                    );
                                let list_continuation_indent =
                                    if matches!(format_stack.current_clause(), Some("SET"))
                                        && construct_flag_active!(MergeActive)
                                    {
                                        base
                                    } else {
                                        active_list_indent!(
                                            indent_level,
                                            open_cursor_state!(),
                                            select_list_layout_state!(),
                                            format_stack.current_clause(),
                                            construct_flag_active!(MergeActive),
                                            in_column_list,
                                        )
                                    };
                                let continuation_indent = if starts_after_open_paren {
                                    let opener_body_indent =
                                        rendered_line_tracker.current_line_indent(
                                            &out,
                                            line_indent,
                                        )
                                            .saturating_add(1);
                                    format_stack
                                        .last_paren()
                                        .and_then(|frame| frame.sibling_body_indent())
                                        .map(|indent| indent.min(opener_body_indent))
                                        .unwrap_or(opener_body_indent)
                                } else if matches!(
                                    comment_header_continuation_kind,
                                    Some(crate::sql_text::FormatInlineCommentHeaderContinuationKind::SameDepth)
                                ) {
                                    line_indent
                                } else if in_select_list
                                    || in_set_clause
                                    || active_list_layout
                                    || in_column_list
                                    || hint_after_select
                                {
                                    list_continuation_indent
                                } else if matches!(
                                    format_stack.current_clause(),
                                    Some("ON" | "USING")
                                ) {
                                    line_indent
                                } else if comment_header_continuation_kind.is_some() {
                                    let fallback_indent =
                                        base.saturating_add(usize::from(
                                            format_stack.current_clause().is_some()
                                                || suppress_comma_break_depth > 0,
                                        ));
                                    let min_indent =
                                        Self::resolve_comment_header_continuation_indent(
                                            comment_header_continuation_kind,
                                            line_indent,
                                            base,
                                            line_indent,
                                            fallback_indent,
                                        );
                                    line_indent.max(min_indent)
                                } else {
                                    line_indent.max(
                                        base.saturating_add(usize::from(
                                            format_stack.current_clause().is_some()
                                                || suppress_comma_break_depth > 0,
                                        )),
                                    )
                                };
                                newline_with(
                                    &mut out,
                                    0,
                                    0,
                                    &mut at_line_start,
                                    &mut needs_space,
                                    &mut line_indent,
                                );
                                inline_comment_continuation_state =
                                    InlineCommentContinuationState::Operand {
                                        indent: continuation_indent,
                                    };
                            } else {
                                let in_column_list = format_stack
                                    .last_paren()
                                    .is_some_and(|frame| frame.is_column_list());
                                let list_extra = if in_select_list
                                    || in_set_clause
                                    || active_list_layout
                                    || in_column_list
                                    || hint_after_select
                                {
                                    active_list_indent!(
                                        indent_level,
                                        open_cursor_state!(),
                                        select_list_layout_state!(),
                                        format_stack.current_clause(),
                                        construct_flag_active!(MergeActive),
                                        in_column_list,
                                    )
                                    .saturating_sub(base_indent!(indent_level))
                                } else {
                                    0
                                };
                                newline_with(
                                    &mut out,
                                    base_indent!(indent_level),
                                    list_extra,
                                    &mut at_line_start,
                                    &mut needs_space,
                                    &mut line_indent,
                                );
                            }
                            if hint_after_select {
                                set_select_list_layout_state!(SelectListLayoutState::Multiline {
                                    indent: base_indent!(indent_level) + 1,
                                    hanging_indent_spaces: None,
                                });
                            }
                        }
                    } else if comment_starts_line {
                    }
                }
                SqlToken::Symbol(sym) => {
                    let mysql_handler_condition_separator =
                        sym == "," && mysql_handler_state.body_line_pending();
                    mysql_handler_state.on_symbol(sym.as_str());
                    if mysql_handler_condition_separator || sym == ";" {
                        let _ = format_stack.clear_mysql_handler_pending_body_indent();
                    }
                    match sym.as_str() {
                        "," => {
                            format_stack.with_cte_on_separator();
                            let next_is_inline_comment = matches!(
                                immediate_next_token,
                                Some(SqlToken::Comment(comment))
                                    if !comment.starts_with('\n') && comment.contains('\n')
                            );
                            let next_is_newline_attached_comment = matches!(
                                immediate_next_token,
                                Some(SqlToken::Comment(comment)) if comment.starts_with('\n')
                            );
                            if statement_has_with_clause!()
                                && matches!(format_stack.current_clause(), Some("SELECT"))
                                && !format_stack.open_cursor_state().in_select()
                                && suppress_comma_break_depth == 0
                            {
                                force_select_list_newline!(&mut out);
                            }
                            trim_trailing_space(&mut out);
                            if out.ends_with('\n') || at_line_start {
                                let in_column_list = format_stack
                                    .last_paren()
                                    .is_some_and(|frame| frame.is_column_list());
                                let previous_is_line_comment = matches!(
                                    immediate_prev_token,
                                    Some(SqlToken::Comment(comment))
                                        if comment.trim_start().starts_with("--")
                                );
                                let comma_starts_clause_list_item = previous_is_line_comment
                                    && matches!(
                                        format_stack.current_clause(),
                                        Some(
                                            "FROM"
                                                | "GROUP"
                                                | "HAVING"
                                                | "ORDER"
                                                | "WINDOW"
                                                | "INTO"
                                        )
                                    );
                                let aligns_clause_list_indent = comma_starts_clause_list_item
                                    || (line_indent == 0
                                        && (matches!(
                                            format_stack.current_clause(),
                                            Some("SELECT" | "SET")
                                        ) || select_list_layout_state!().has_active_indent()
                                            || in_column_list
                                            || previous_is_line_comment));
                                if aligns_clause_list_indent {
                                    line_indent = active_list_indent!(
                                        indent_level,
                                        open_cursor_state!(),
                                        select_list_layout_state!(),
                                        format_stack.current_clause(),
                                        construct_flag_active!(MergeActive),
                                        in_column_list,
                                    );
                                    if format_stack.paren_is_empty() {
                                        if let Some(base_depth) = query_body_clause_base_depth!() {
                                            line_indent =
                                                line_indent.max(base_depth.saturating_add(1));
                                        }
                                    }
                                }
                                ensure_indent(&mut out, &mut at_line_start, line_indent);
                            }
                            out.push(',');
                            format_stack.pop_between_pending_at_or_above(current_scope);
                            let is_with_cte_separator = format_stack.with_cte_can_close_on_select();
                            if format_stack
                                .last_paren()
                                .is_some_and(|frame| frame.is_column_list())
                                || is_with_cte_separator
                            {
                                let fallback_with_cte_separator_indent =
                                    query_body_clause_base_depth!()
                                        .filter(|_| format_stack.paren_is_empty())
                                        .unwrap_or_else(|| base_indent!(indent_level));
                                let with_cte_separator_indent = if is_with_cte_separator {
                                    format_stack.with_cte_separator_indent(
                                        fallback_with_cte_separator_indent,
                                    )
                                } else {
                                    fallback_with_cte_separator_indent
                                };
                                newline_with(
                                    &mut out,
                                    with_cte_separator_indent,
                                    0,
                                    &mut at_line_start,
                                    &mut needs_space,
                                    &mut line_indent,
                                );
                            } else if next_is_inline_comment {
                                needs_space = true;
                                if matches!(format_stack.current_clause(), Some("SELECT")) {
                                    let select_list_indent = base_indent!(indent_level) + 1;
                                    set_select_list_layout_state!(
                                        SelectListLayoutState::Multiline {
                                            indent: select_list_indent,
                                            hanging_indent_spaces: current_query_has_apply
                                                .then_some(
                                                    select_list_layout_state!()
                                                        .hanging_indent_spaces(
                                                            &out,
                                                            select_list_indent
                                                        ),
                                                ),
                                        }
                                    );
                                }
                            } else {
                                let follows_multiline_child_close =
                                    format_stack.last_paren().is_some_and(|frame| {
                                        frame.has_pending_multiline_child_continuation()
                                    });
                                let active_phase1_wrapped_owner_kind =
                                    Self::active_phase1_wrapped_owner_kind_at_depth(
                                        &format_stack,
                                        format_stack.paren_depth(),
                                    );
                                let parent_frame_sibling_indent = format_stack
                                    .last_paren()
                                    .and_then(|frame| frame.sibling_body_indent());
                                let clause_list_indent_follows_current_query_frame =
                                    format_stack.last_paren().is_none_or(|frame| {
                                        frame.is_query_like() || frame.is_column_list()
                                    });
                                let next_starts_parenthesized_sibling = matches!(next_non_comment, Some(SqlToken::Symbol(sym)) if sym == "(");
                                let keeps_mixed_close_key_subquery_inline =
                                    follows_multiline_child_close
                                        && matches!(
                                            next_non_comment_idx
                                                .and_then(|token_idx| tokens.get(token_idx)),
                                            Some(SqlToken::String(_))
                                        )
                                        && matches!(
                                            second_next_non_comment_idx.and_then(|token_idx| tokens.get(token_idx)),
                                            Some(SqlToken::Symbol(sym)) if sym == ","
                                        )
                                        && matches!(
                                            third_next_non_comment_idx.and_then(|token_idx| tokens.get(token_idx)),
                                            Some(SqlToken::Symbol(sym)) if sym == "("
                                        );
                                let keeps_mixed_close_value_key_inline =
                                    follows_multiline_child_close
                                        && format_stack
                                            .last_paren()
                                            .is_some_and(|frame| frame.suppresses_comma_breaks())
                                        && (matches!(next_non_comment, Some(SqlToken::String(_)))
                                            || matches!(
                                                next_non_comment,
                                                Some(SqlToken::Word(word))
                                                    if word.eq_ignore_ascii_case("KEY")
                                            ));
                                let allows_compact_close_continuation_comma_break =
                                    matches!(format_stack.current_clause(), Some("SELECT"))
                                        && follows_multiline_child_close
                                        && (select_list_layout_state!().is_multiline()
                                            || format_stack.open_cursor_state().in_select()
                                            || format_stack.scoped_indent_is_active(
                                                ScopedIndentKind::CursorSql,
                                            ));
                                let keeps_multiline_values_argument_comma_break = format_stack
                                    .last_paren()
                                    .copied()
                                    .filter(|frame| frame.suppresses_comma_breaks())
                                    .is_some_and(|frame| {
                                        matches!(format_stack.current_clause(), Some("VALUES"))
                                            && rendered_line_tracker
                                                .current_line_indent(&out, line_indent)
                                                > frame.open_line_indent
                                    });
                                let allows_structural_sibling_comma_break =
                                    follows_multiline_child_close
                                        && (next_starts_parenthesized_sibling
                                            || matches!(
                                                format_stack.current_clause(),
                                                Some("VALUES")
                                            ))
                                        || keeps_multiline_values_argument_comma_break;
                                if (suppress_comma_break_depth == 0
                                    || allows_compact_close_continuation_comma_break
                                    || allows_structural_sibling_comma_break)
                                    && !keeps_mixed_close_key_subquery_inline
                                    && !keeps_mixed_close_value_key_inline
                                    && !construct_flag_active!(ExecuteImmediateActive)
                                    && !construct_flag_active!(GrantRevokeActive)
                                    && !format_stack.trigger_header_is_active()
                                {
                                    let query_like_select_list_indent = format_stack
                                        .last_paren()
                                        .copied()
                                        .filter(|frame| frame.is_query_like())
                                        .filter(|_| {
                                            matches!(
                                                format_stack.current_clause(),
                                                Some("SELECT" | "SET")
                                            ) && select_list_layout_state!().has_active_indent()
                                        })
                                        .map(|_| {
                                            select_list_layout_state!()
                                                .indentation_or(line_indent.saturating_add(1))
                                        });
                                    let current_list_indent = top_level_select_item_context
                                        .then_some(())
                                        .filter(|_| {
                                            matches!(
                                                format_stack.current_clause(),
                                                Some("SELECT" | "SET")
                                            )
                                        })
                                        .map(|_| {
                                            active_list_indent!(
                                                indent_level,
                                                open_cursor_state!(),
                                                select_list_layout_state!(),
                                                format_stack.current_clause(),
                                                construct_flag_active!(MergeActive),
                                                false,
                                            )
                                        });
                                    let clause_list_indent = matches!(
                                        format_stack.current_clause(),
                                        Some(
                                            "FROM"
                                                | "GROUP"
                                                | "HAVING"
                                                | "ORDER"
                                                | "WINDOW"
                                                | "INTO"
                                        )
                                    )
                                    .then_some(())
                                    .filter(|_| {
                                        !matches!(
                                            active_phase1_wrapped_owner_kind,
                                            Some(
                                                FormatIndentedParenOwnerKind::WithinGroup
                                                    | FormatIndentedParenOwnerKind::Keep
                                                    | FormatIndentedParenOwnerKind::Window
                                                    | FormatIndentedParenOwnerKind::AnalyticOver
                                                    | FormatIndentedParenOwnerKind::MatchRecognize
                                                    | FormatIndentedParenOwnerKind::Pivot
                                                    | FormatIndentedParenOwnerKind::Unpivot
                                            )
                                        )
                                    })
                                    .filter(|_| clause_list_indent_follows_current_query_frame)
                                    .map(|_| {
                                        let mut list_indent = active_list_indent!(
                                            indent_level,
                                            open_cursor_state!(),
                                            select_list_layout_state!(),
                                            format_stack.current_clause(),
                                            construct_flag_active!(MergeActive),
                                            false,
                                        );
                                        if format_stack.paren_is_empty() {
                                            if let Some(base_depth) =
                                                query_body_clause_base_depth!()
                                            {
                                                list_indent =
                                                    list_indent.max(base_depth.saturating_add(1));
                                            }
                                        }
                                        list_indent
                                    });
                                    let wrapped_owner_clause_sibling =
                                        format_stack.last_paren().is_some_and(|frame| {
                                            matches!(
                                                frame.kind,
                                                ParenFormatFrameKind::WrappedLayout
                                            ) && matches!(
                                                format_stack.current_clause(),
                                                Some("WINDOW" | "ORDER" | "GROUP")
                                            )
                                        });
                                    let owner_relative_parent_sibling_indent =
                                        parent_frame_sibling_indent.filter(|_| {
                                            follows_multiline_child_close
                                                || wrapped_owner_clause_sibling
                                                || matches!(
                                                    active_phase1_wrapped_owner_kind,
                                                    Some(
                                                        FormatIndentedParenOwnerKind::WithinGroup
                                                            | FormatIndentedParenOwnerKind::Keep
                                                            | FormatIndentedParenOwnerKind::Window
                                                            | FormatIndentedParenOwnerKind::AnalyticOver
                                                            | FormatIndentedParenOwnerKind::MatchRecognize
                                                            | FormatIndentedParenOwnerKind::Pivot
                                                            | FormatIndentedParenOwnerKind::Unpivot
                                                    )
                                                )
                                        });
                                    if let Some(list_indent) =
                                        query_like_select_list_indent.or(current_list_indent)
                                    {
                                        if !select_list_layout_state!().is_multiline() {
                                            set_select_list_layout_state!(
                                                SelectListLayoutState::Multiline {
                                                    indent: list_indent,
                                                    hanging_indent_spaces: None,
                                                }
                                            );
                                        }
                                    }
                                    let comma_extra_indent = if mysql_handler_condition_separator
                                        || matches!(
                                            active_phase1_wrapped_owner_kind,
                                            Some(
                                                FormatIndentedParenOwnerKind::WithinGroup
                                                    | FormatIndentedParenOwnerKind::Keep
                                            )
                                        )
                                        || (matches!(format_stack.current_clause(), Some("SELECT"))
                                            && format_stack.scoped_indent_is_active(
                                                ScopedIndentKind::CursorSql,
                                            )) {
                                        0
                                    } else {
                                        1
                                    };
                                    let comma_follows_line_comment = matches!(
                                        immediate_prev_token,
                                        Some(SqlToken::Comment(comment))
                                            if comment.trim_start().starts_with("--")
                                    );
                                    let previous_non_comment_is_case_end =
                                        loop_previous_non_comment_token.is_some_and(|token| {
                                            matches!(
                                                token,
                                                SqlToken::Word(word)
                                                    if word.eq_ignore_ascii_case("END")
                                            )
                                        });
                                    let current_rendered_line_indent = rendered_line_indent(
                                        rendered_line_tracker.current_line(&out),
                                    );
                                    let case_end_sibling_indent = previous_non_comment_is_case_end
                                        .then_some(())
                                        .filter(|_| {
                                            matches!(format_stack.current_clause(), Some("VALUES"))
                                                || format_stack.last_paren().is_some_and(|frame| {
                                                    frame.suppresses_comma_breaks()
                                                })
                                        })
                                        .map(|_| current_rendered_line_indent);
                                    if matches!(format_stack.current_clause(), Some("SET"))
                                        && construct_flag_active!(MergeActive)
                                    {
                                        let merge_set_continuation_extra =
                                            usize::from(!comma_follows_line_comment);
                                        newline_with(
                                            &mut out,
                                            base_indent!(indent_level),
                                            merge_set_continuation_extra,
                                            &mut at_line_start,
                                            &mut needs_space,
                                            &mut line_indent,
                                        );
                                    } else if let Some(case_end_sibling_indent) =
                                        case_end_sibling_indent
                                    {
                                        // CASE ... END, inside VALUES must snap the next
                                        // sibling argument back to the parent call frame.
                                        newline_with(
                                            &mut out,
                                            case_end_sibling_indent,
                                            0,
                                            &mut at_line_start,
                                            &mut needs_space,
                                            &mut line_indent,
                                        );
                                    } else if current_query_has_apply
                                        && matches!(format_stack.current_clause(), Some("SELECT"))
                                    {
                                        // The select list is already multiline after the first comma.
                                        let select_list_indent = base_indent!(indent_level) + 1;
                                        let hanging_indent_spaces = select_list_layout_state!()
                                            .hanging_indent_spaces(&out, select_list_indent);
                                        newline_with_spaces(
                                            &mut out,
                                            hanging_indent_spaces,
                                            &mut at_line_start,
                                            &mut needs_space,
                                            &mut line_indent,
                                            &current_rendered_line_paren_depth,
                                        );
                                        set_select_list_layout_state!(
                                            SelectListLayoutState::Multiline {
                                                indent: select_list_indent,
                                                hanging_indent_spaces: Some(hanging_indent_spaces),
                                            }
                                        );
                                    } else if matches!(
                                        format_stack.current_clause(),
                                        Some("SELECT" | "SET")
                                    ) && (current_token_paren_depth
                                        < line_start_token_paren_depth
                                        || follows_multiline_child_close
                                        || line_closes_delimiter_frame_below_start)
                                    {
                                        // Mixed close-comma SELECT/SET siblings must snap to the
                                        // active list frame depth after the current line closes
                                        // one or more frames.
                                        let stable_root_list_indent =
                                            format_stack.paren_is_empty().then(|| {
                                                query_body_clause_base_depth!()
                                                    .unwrap_or_else(|| base_indent!(indent_level))
                                                    .saturating_add(1)
                                            });
                                        let frame_based_sibling_indent =
                                            parent_frame_sibling_indent
                                                .or(stable_root_list_indent)
                                                .unwrap_or_else(|| {
                                                    select_list_layout_state!().indentation_or(
                                                        active_list_indent!(
                                                            indent_level,
                                                            open_cursor_state!(),
                                                            select_list_layout_state!(),
                                                            format_stack.current_clause(),
                                                            construct_flag_active!(MergeActive),
                                                            false,
                                                        ),
                                                    )
                                                });
                                        newline_with(
                                            &mut out,
                                            frame_based_sibling_indent,
                                            0,
                                            &mut at_line_start,
                                            &mut needs_space,
                                            &mut line_indent,
                                        );
                                    } else if let Some(query_like_indent) =
                                        query_like_select_list_indent
                                    {
                                        let resolved_query_like_indent = query_like_indent;
                                        if !select_list_layout_state!().is_multiline() {
                                            set_select_list_layout_state!(
                                                SelectListLayoutState::Multiline {
                                                    indent: resolved_query_like_indent,
                                                    hanging_indent_spaces: None,
                                                }
                                            );
                                        }
                                        newline_with(
                                            &mut out,
                                            resolved_query_like_indent,
                                            0,
                                            &mut at_line_start,
                                            &mut needs_space,
                                            &mut line_indent,
                                        );
                                    } else if let Some(sibling_indent) =
                                        owner_relative_parent_sibling_indent
                                    {
                                        // Owner-relative wrapped frames keep comma siblings on
                                        // the frame body depth; they should not inherit
                                        // clause-local +1 continuation depth.
                                        newline_with(
                                            &mut out,
                                            sibling_indent,
                                            0,
                                            &mut at_line_start,
                                            &mut needs_space,
                                            &mut line_indent,
                                        );
                                    } else if let Some(list_indent) = clause_list_indent {
                                        newline_with(
                                            &mut out,
                                            list_indent,
                                            0,
                                            &mut at_line_start,
                                            &mut needs_space,
                                            &mut line_indent,
                                        );
                                    } else if top_level_select_item_context
                                        && matches!(
                                            format_stack.current_clause(),
                                            Some("SELECT" | "SET")
                                        )
                                        && select_list_layout_state!().has_active_indent()
                                    {
                                        let structural_select_indent = select_list_layout_state!()
                                            .indentation_or(active_list_indent!(
                                                indent_level,
                                                open_cursor_state!(),
                                                select_list_layout_state!(),
                                                format_stack.current_clause(),
                                                construct_flag_active!(MergeActive),
                                                false,
                                            ));
                                        newline_with(
                                            &mut out,
                                            structural_select_indent,
                                            0,
                                            &mut at_line_start,
                                            &mut needs_space,
                                            &mut line_indent,
                                        );
                                    } else if let Some(sibling_indent) = parent_frame_sibling_indent
                                    {
                                        // Wrapped layout siblings must snap back to the parent
                                        // frame body depth instead of inheriting clause-local
                                        // comma indentation from the previous child.
                                        newline_with(
                                            &mut out,
                                            sibling_indent,
                                            0,
                                            &mut at_line_start,
                                            &mut needs_space,
                                            &mut line_indent,
                                        );
                                    } else {
                                        newline_with(
                                            &mut out,
                                            base_indent!(indent_level),
                                            comma_extra_indent,
                                            &mut at_line_start,
                                            &mut needs_space,
                                            &mut line_indent,
                                        );
                                    }
                                } else {
                                    if !next_is_newline_attached_comment {
                                        out.push(' ');
                                    }
                                    needs_space = false;
                                }
                            }
                        }
                        ";" => {
                            format_stack.with_cte_on_separator();
                            let keep_tight_top_level_spacing = format_stack
                                .with_cte_keeps_top_level_semicolon_inside_with_definitions();
                            trim_trailing_space(&mut out);
                            out.push(';');
                            let _ = format_stack.clear_pending_split_end_suffix();
                            on_duplicate_key_update_active = false;
                            mysql_handler_state.clear();
                            insert_all_branch_body_indent = None;
                            exit_condition_state.clear();
                            let package_initializer_next =
                                pending_package_member_separator && next_word_is("BEGIN");
                            if pending_package_member_separator
                                && (next_word_is("PROCEDURE") || next_word_is("FUNCTION"))
                            {
                                out.push_str("\n\n");
                                current_rendered_line_paren_depth.set(0);
                            }
                            if !package_initializer_next {
                                pending_package_member_separator = false;
                            }
                            suppress_comma_break_depth = 0;
                            format_stack.reset_for_statement_boundary(&mut indent_level);
                            select_list_break_state.clear();
                            let should_reset_paren_tracking =
                                indent_level == 0 || format_stack.block_depth() == 0;
                            if should_reset_paren_tracking {
                                format_stack.clear_compound_trigger();
                            }
                            let next_is_inline_line_comment = matches!(
                                immediate_next_token,
                                Some(SqlToken::Comment(comment))
                                    if !comment.starts_with('\n')
                                        && comment.trim_start().starts_with("--")
                            );
                            if !next_is_inline_line_comment {
                                newline_with(
                                    &mut out,
                                    indent_level,
                                    0,
                                    &mut at_line_start,
                                    &mut needs_space,
                                    &mut line_indent,
                                );
                                if indent_level == 0 && !keep_tight_top_level_spacing {
                                    out.push('\n');
                                    current_rendered_line_paren_depth.set(0);
                                    at_line_start = true;
                                    needs_space = false;
                                }
                            }
                        }
                        "(" => {
                            format_stack.with_cte_on_open_paren();
                            let is_query_paren = matches!(
                                next_non_comment,
                                Some(SqlToken::Word(word))
                                    if crate::sql_text::is_subquery_head_keyword(word)
                            ) && !matches!(
                                next_non_comment,
                                Some(SqlToken::Word(word))
                                    if mysql_compatible
                                        && on_duplicate_key_update_active
                                        && word.eq_ignore_ascii_case("VALUES")
                            );
                            let open_paren_followed_by_newline = next_non_comment_idx
                                .is_some_and(|next_idx| token_gap_contains_newline(idx, next_idx));
                            let direct_wraps_subquery_or_case_head = matches!(
                                next_non_comment,
                                Some(SqlToken::Word(word))
                                    if word.eq_ignore_ascii_case("CASE")
                                        || crate::sql_text::is_subquery_head_keyword(word.as_str())
                            ) || matches!(
                                (next_non_comment, second_next_non_comment_token),
                                (
                                    Some(SqlToken::Symbol(symbol)),
                                    Some(SqlToken::Word(word))
                                ) if symbol == "("
                                    && (word.eq_ignore_ascii_case("CASE")
                                        || crate::sql_text::is_subquery_head_keyword(word.as_str()))
                            );
                            let paren_body_has_newline = open_paren_followed_by_newline;
                            let is_analytic_over_paren = Self::paren_opens_analytic_layout(
                                format_stack.current_clause(),
                                prev_word_upper,
                            );
                            let multiline_clause_owner_kind =
                                FormatterStatementStructure::paren_multiline_clause_owner_kind_at_open_paren(
                                    tokens,
                                    &recent_statement_word_indices,
                                )
                                .or_else(|| {
                                    (matches!(format_stack.current_clause(), Some("WINDOW"))
                                        && matches!(prev_word_upper, Some("AS")))
                                    .then_some(FormatIndentedParenOwnerKind::Window)
                                });
                            let paren_body_analysis = Self::analyze_paren_body(
                                tokens,
                                idx,
                                Some(matching_paren_close_indices),
                                Some(paren_body_analyses),
                            );
                            let body_contains_query_like_child = direct_wraps_subquery_or_case_head
                                || paren_body_analysis.contains_query_like_child;
                            let wraps_subquery_or_case_head = direct_wraps_subquery_or_case_head
                                || matches!(
                                    next_non_comment,
                                    Some(SqlToken::Symbol(symbol)) if symbol == "("
                                ) && body_contains_query_like_child;
                            let paren_has_top_level_comma = paren_body_analysis.has_top_level_comma;
                            let wrapped_owner_kind = multiline_clause_owner_kind.filter(|kind| {
                                matches!(
                                    kind,
                                    FormatIndentedParenOwnerKind::WithinGroup
                                        | FormatIndentedParenOwnerKind::Keep
                                ) && body_contains_query_like_child
                            });
                            let force_within_group_wrapped_layout = matches!(
                                multiline_clause_owner_kind,
                                Some(FormatIndentedParenOwnerKind::WithinGroup)
                            )
                                && body_contains_query_like_child;
                            let forced_wrapped_owner_kind = force_within_group_wrapped_layout
                                .then_some(FormatIndentedParenOwnerKind::WithinGroup);
                            let is_keep_paren = matches!(prev_word_upper, Some("KEEP"));
                            let is_multiline_clause_paren = matches!(
                                multiline_clause_owner_kind,
                                Some(
                                    FormatIndentedParenOwnerKind::AnalyticOver
                                        | FormatIndentedParenOwnerKind::Window
                                        | FormatIndentedParenOwnerKind::MatchRecognize
                                        | FormatIndentedParenOwnerKind::Pivot
                                        | FormatIndentedParenOwnerKind::Unpivot
                                )
                            ) || is_analytic_over_paren;
                            let is_subquery = is_query_paren || is_multiline_clause_paren;
                            let is_column_list = Self::paren_opens_structured_column_list(
                                prev_word_upper,
                                construct_flag_active!(CreateTableParenExpected),
                            );
                            let preserves_mysql_on_duplicate_multiline_call = mysql_compatible
                                && on_duplicate_key_update_active
                                && !is_subquery
                                && !is_column_list
                                && multiline_clause_owner_kind.is_none()
                                && prev_word_upper.is_some()
                                && (paren_body_has_newline || paren_has_top_level_comma);
                            let is_multiline_routine_parameter_list = mysql_compatible
                                && paren_body_has_newline
                                && Self::recent_statement_words_before_from_indices(
                                    tokens,
                                    &recent_statement_word_indices,
                                    4,
                                )
                                .iter()
                                .any(|word| {
                                    word.eq_ignore_ascii_case("PROCEDURE")
                                        || word.eq_ignore_ascii_case("FUNCTION")
                                });
                            let is_call_argument_list =
                                Self::paren_opens_call_argument_list_from_indices(
                                    tokens,
                                    &recent_statement_word_indices,
                                ) && paren_body_has_newline;
                            let has_nested_analytic_clause_owner_child =
                                paren_body_analysis.contains_analytic_clause_owner_child;
                            let wraps_function_call_child = wraps_subquery_or_case_head
                                && multiline_clause_owner_kind.is_none()
                                && wrapped_owner_kind.is_none()
                                && !force_within_group_wrapped_layout
                                && !is_analytic_over_paren
                                && !is_call_argument_list
                                && !preserves_mysql_on_duplicate_multiline_call
                                && !matches!(
                                    prev_word_upper,
                                    Some(
                                        "IF" | "THEN"
                                            | "ELSE"
                                            | "WHEN"
                                            | "EXISTS"
                                            | "IN"
                                            | "ANY"
                                            | "ALL"
                                            | "SOME"
                                            | "FROM"
                                            | "JOIN"
                                            | "APPLY"
                                            | "AS"
                                            | "SELECT"
                                    )
                                );
                            let paren_frame_kind = if multiline_clause_owner_kind
                                == Some(FormatIndentedParenOwnerKind::Window)
                                || is_analytic_over_paren
                                || is_call_argument_list
                                || preserves_mysql_on_duplicate_multiline_call
                            {
                                ParenFormatFrameKind::WrappedLayout
                            } else if is_subquery {
                                ParenFormatFrameKind::QueryLike
                            } else if is_column_list || is_multiline_routine_parameter_list {
                                ParenFormatFrameKind::ColumnList
                            } else if has_nested_analytic_clause_owner_child {
                                // Nested owner-relative multiline clauses inside ordinary
                                // call/expression parens (for example `... OVER (...)`) should
                                // still open an owner/body frame so sibling arguments can snap
                                // back to the parent call depth after the nested close.
                                ParenFormatFrameKind::WrappedLayout
                            } else if wrapped_owner_kind.is_some()
                                || forced_wrapped_owner_kind.is_some()
                                || (wraps_subquery_or_case_head && !wraps_function_call_child)
                            {
                                ParenFormatFrameKind::WrappedLayout
                            } else {
                                ParenFormatFrameKind::Compact
                            };
                            // CASE is always expanded to multiple lines by the formatter.
                            // Triggering the promotion only when `paren_body_has_newline` is
                            // true (i.e. CASE was already multiline in the *input*) creates a
                            // cycle: first pass keeps `SELECT func(` on one line, formatter
                            // expands CASE inside, second pass sees the newline and splits
                            // SELECT from func(. Removing the input-dependent gate makes the
                            // decision structural: whenever the first SELECT list item opens
                            // a paren whose first token is CASE, always promote SELECT to its
                            // own line so both passes produce the same canonical output.
                            let promotes_first_clause_owner_to_body_depth = matches!(format_stack.current_clause(), Some("SELECT" | "SET"))
                                    && !at_line_start
                                    && format_stack.paren_is_empty()
                                    && suppress_comma_break_depth == 0
                                    && select_list_layout_state!().has_active_indent()
                                    && !select_list_layout_state!().is_multiline()
                                    // Keep the existing dedicated `SELECT (` first-item path.
                                    && !Self::paren_starts_first_clause_list_item(
                                        format_stack.current_clause(),
                                        prev_word_upper,
                                        is_query_paren,
                                        format_stack.paren_depth(),
                                    )
                                    && matches!(
                                        next_non_comment,
                                        Some(SqlToken::Word(word))
                                            if word.eq_ignore_ascii_case("CASE")
                                    );
                            if promotes_first_clause_owner_to_body_depth {
                                // Frame-first normalization: when the first SELECT/SET list
                                // item opens a paren containing CASE (which is always
                                // formatted multiline), promote SELECT onto its own line so
                                // all sibling owners reuse the same parent list frame.
                                force_select_list_newline!(&mut out);
                                line_indent = active_list_indent!(
                                    indent_level,
                                    open_cursor_state!(),
                                    select_list_layout_state!(),
                                    format_stack.current_clause(),
                                    construct_flag_active!(MergeActive),
                                    false,
                                );
                            }
                            if Self::paren_starts_first_clause_list_item(
                                format_stack.current_clause(),
                                prev_word_upper,
                                is_query_paren,
                                format_stack.paren_depth(),
                            ) {
                                newline_with(
                                    &mut out,
                                    base_indent!(indent_level),
                                    1,
                                    &mut at_line_start,
                                    &mut needs_space,
                                    &mut line_indent,
                                );
                            } else if paren_frame_kind.opens_indented()
                                && !at_line_start
                                && rendered_line_tracker
                                    .line_before_trailing_newlines(&out)
                                    .trim_end()
                                    .ends_with('(')
                            {
                                newline_with(
                                    &mut out,
                                    line_indent.saturating_add(1),
                                    0,
                                    &mut at_line_start,
                                    &mut needs_space,
                                    &mut line_indent,
                                );
                            }

                            let paren_started_at_line_start = at_line_start;
                            let function_like_wrapped_paren =
                                matches!(paren_frame_kind, ParenFormatFrameKind::WrappedLayout)
                                    && wraps_function_call_child;
                            let rendered_owner_line = rendered_line_tracker.current_line(&out);
                            let token_query_owner_kind =
                                match (loop_previous_non_comment_token, prev_non_comment_upper) {
                                    (_, Some("FROM" | "USING" | "JOIN")) => {
                                        Some(sql_text::FormatQueryOwnerKind::Clause)
                                    }
                                    (_, Some("APPLY" | "LATERAL" | "TABLE")) => {
                                        Some(sql_text::FormatQueryOwnerKind::FromItem)
                                    }
                                    (_, Some("IN" | "EXISTS" | "ANY" | "SOME" | "ALL")) => {
                                        Some(sql_text::FormatQueryOwnerKind::Condition)
                                    }
                                    (Some(SqlToken::Symbol(symbol)), _)
                                        if matches!(
                                            symbol.as_str(),
                                            "=" | "<" | ">" | "<=" | ">=" | "<>" | "!=" | "<=>"
                                        ) =>
                                    {
                                        Some(sql_text::FormatQueryOwnerKind::Operator)
                                    }
                                    (_, Some("ON"))
                                        if Self::recent_statement_words_before_from_indices(
                                            tokens,
                                            &recent_statement_word_indices,
                                            4,
                                        )
                                        .iter()
                                        .skip(1)
                                        .any(|recent| recent.eq_ignore_ascii_case("REFERENCE")) =>
                                    {
                                        Some(sql_text::FormatQueryOwnerKind::Clause)
                                    }
                                    _ => None,
                                };
                            let wrapped_query_owner_paren = !paren_started_at_line_start
                                && matches!(paren_frame_kind, ParenFormatFrameKind::WrappedLayout)
                                && token_query_owner_kind.is_some();
                            let rendered_owner_depth = rendered_line_indent(rendered_owner_line);
                            let in_column_list = format_stack
                                .last_paren()
                                .is_some_and(|frame| frame.is_column_list());
                            let parent_frame_sibling_indent = format_stack
                                .last_paren()
                                .and_then(|frame| frame.sibling_body_indent());
                            let clause_family_body_indent = matches!(
                                format_stack.current_clause(),
                                Some(
                                    "SELECT"
                                        | "SET"
                                        | "ORDER"
                                        | "GROUP"
                                        | "HAVING"
                                        | "WINDOW"
                                        | "INTO"
                                        | "RETURNING"
                                )
                            )
                            .then(|| {
                                active_list_indent!(
                                    indent_level,
                                    open_cursor_state!(),
                                    select_list_layout_state!(),
                                    format_stack.current_clause(),
                                    construct_flag_active!(MergeActive),
                                    in_column_list,
                                )
                            });
                            let previous_meaningful_is_open_paren = matches!(
                                loop_previous_non_comment_token,
                                Some(SqlToken::Symbol(symbol)) if symbol == "("
                            );
                            let open_paren_mysql_scalar_function = mysql_compatible
                                && matches!(prev_word_upper, Some("IF"))
                                && Self::mysql_if_is_scalar_function_from_context(
                                    format_stack.current_clause(),
                                    on_duplicate_key_update_active,
                                    in_plsql_block,
                                    at_line_start,
                                    loop_previous_non_comment_token,
                                );
                            let compact_function_wrapper =
                                matches!(paren_frame_kind, ParenFormatFrameKind::Compact)
                                    && (wraps_function_call_child
                                        || body_contains_query_like_child);
                            let inside_case_branch_condition =
                                format_stack.last_block_kind_is("CASE");
                            let semantic_open_line_indent = if paren_started_at_line_start {
                                line_indent
                            } else if paren_frame_kind.opens_indented()
                                && !paren_frame_kind.is_query_like()
                                && !function_like_wrapped_paren
                            {
                                if multiline_clause_owner_kind.is_some() {
                                    rendered_owner_depth
                                } else if let Some(parent_sibling_indent) =
                                    parent_frame_sibling_indent
                                {
                                    if matches!(
                                        format_stack.current_clause(),
                                        Some("SELECT" | "SET")
                                    ) && matches!(
                                        paren_frame_kind,
                                        ParenFormatFrameKind::WrappedLayout
                                    ) && !previous_meaningful_is_open_paren
                                    {
                                        rendered_owner_depth
                                    } else {
                                        parent_sibling_indent.max(rendered_owner_depth)
                                    }
                                } else if previous_meaningful_is_open_paren {
                                    parent_frame_sibling_indent.unwrap_or(rendered_owner_depth)
                                } else {
                                    rendered_owner_depth
                                }
                            } else if matches!(
                                format_stack.current_clause(),
                                Some("VALUES" | "FROM")
                            ) || matches!(
                                prev_word_upper,
                                Some("JSON_TABLE" | "XMLTABLE")
                            ) || wrapped_query_owner_paren
                            {
                                rendered_owner_depth
                            } else if matches!(paren_frame_kind, ParenFormatFrameKind::Compact)
                                || function_like_wrapped_paren
                            {
                                if compact_function_wrapper {
                                    if previous_meaningful_is_open_paren {
                                        parent_frame_sibling_indent.unwrap_or(rendered_owner_depth)
                                    } else if let Some(parent_sibling_indent) =
                                        parent_frame_sibling_indent
                                    {
                                        parent_sibling_indent.max(rendered_owner_depth)
                                    } else if inside_case_branch_condition
                                        && sql_text::line_starts_with_identifier_sequence_before_inline_comment(
                                            rendered_owner_line,
                                            &["WHEN"],
                                        )
                                    {
                                        rendered_owner_depth.saturating_add(1)
                                    } else {
                                        rendered_owner_depth
                                    }
                                } else if matches!(
                                    format_stack.current_clause(),
                                    Some(
                                        "SELECT"
                                            | "SET"
                                            | "ORDER"
                                            | "GROUP"
                                            | "HAVING"
                                            | "WINDOW"
                                            | "INTO"
                                            | "RETURNING"
                                    )
                                ) {
                                    base_indent!(indent_level).saturating_add(1)
                                } else if previous_meaningful_is_open_paren {
                                    parent_frame_sibling_indent
                                        .unwrap_or_else(|| line_indent.saturating_add(1))
                                } else {
                                    line_indent
                                }
                            } else {
                                let parent_body_indent =
                                    (!matches!(paren_frame_kind, ParenFormatFrameKind::Compact))
                                        .then_some(parent_frame_sibling_indent)
                                        .flatten();
                                parent_body_indent
                                    .or(clause_family_body_indent)
                                    .unwrap_or(line_indent)
                            };
                            let query_like_owner_info =
                                paren_frame_kind.is_query_like().then(|| {
                                    QueryLikeOwnerLineInfo::from_rendered_line_info(
                                        rendered_line_tracker.active_query_owner_line_info(
                                            &out,
                                            paren_started_at_line_start,
                                        ),
                                        token_query_owner_kind,
                                    )
                                });
                            let standalone_owner_follows_opener = query_like_owner_info
                                .is_some_and(|owner_info| owner_info.ends_with_open_paren);
                            let standalone_clause_list_query_item = paren_started_at_line_start
                                && paren_frame_kind.is_query_like()
                                && !standalone_owner_follows_opener
                                && ((matches!(
                                    format_stack.current_clause(),
                                    Some("SELECT" | "SET")
                                ) && select_list_layout_state!().has_active_indent())
                                    || matches!(
                                        format_stack.current_clause(),
                                        Some("ORDER" | "GROUP" | "HAVING" | "WINDOW" | "INTO")
                                    ));
                            if standalone_clause_list_query_item {
                                line_indent = active_list_indent!(
                                    indent_level,
                                    open_cursor_state!(),
                                    select_list_layout_state!(),
                                    format_stack.current_clause(),
                                    construct_flag_active!(MergeActive),
                                    false,
                                );
                            }
                            let query_like_owner_depth = query_like_owner_info
                                .map(|owner_info| {
                                    let inline_clause_owner_depth = owner_info
                                        .starts_with_active_clause(current_clause!())
                                        .then(|| base_indent!(indent_level).saturating_add(1));
                                    let owner_line_open_depth =
                                        current_rendered_line_paren_depth.get();
                                    if standalone_clause_list_query_item {
                                        line_indent
                                    } else if paren_started_at_line_start {
                                        if owner_info.ends_with_open_paren {
                                            format_stack
                                                .last_paren()
                                                .map(|frame| frame.open_line_indent)
                                                .unwrap_or(semantic_open_line_indent)
                                        } else {
                                            owner_info.indent
                                        }
                                    } else if paren_frame_kind.is_query_like() {
                                        if let Some(clause_owner_depth) = inline_clause_owner_depth
                                        {
                                            clause_owner_depth
                                        } else if owner_info.ends_with_value {
                                            format_stack
                                                .last_paren()
                                                .map(|frame| frame.open_line_indent)
                                                .unwrap_or(semantic_open_line_indent)
                                        } else {
                                            owner_info.indent.saturating_add(owner_line_open_depth)
                                        }
                                    } else {
                                        semantic_open_line_indent
                                    }
                                })
                                .unwrap_or(line_indent);
                            let query_like_layout = query_like_owner_info.map(|owner_info| {
                                Self::query_like_paren_layout(
                                    owner_info,
                                    query_like_owner_depth,
                                    format_stack.current_query_base_depth(),
                                    paren_started_at_line_start,
                                )
                            });
                            if paren_frame_kind.is_query_like()
                                && paren_started_at_line_start
                                && !standalone_clause_list_query_item
                            {
                                line_indent = query_like_layout
                                    .map(|layout| layout.close_depth)
                                    .unwrap_or(query_like_owner_depth);
                            }
                            ensure_indent(&mut out, &mut at_line_start, line_indent);
                            let keeps_aggregate_call_tight = statement_has_apply
                                && matches!(prev_word_upper, Some("AVG" | "COUNT" | "MAX" | "MIN"));
                            let keeps_count_star_call_tight =
                                matches!(prev_word_upper, Some("COUNT"))
                                    && matches!(
                                        (next_non_comment, second_next_non_comment_token),
                                        (
                                            Some(SqlToken::Symbol(first)),
                                            Some(SqlToken::Symbol(second))
                                        ) if first == "*" && second == ")"
                                    );
                            let keeps_mysql_word_paren_tight = mysql_compatible
                                && Self::mysql_word_paren_should_stay_tight_from_context(
                                    tokens,
                                    on_duplicate_key_update_active,
                                    prev_word_upper,
                                    loop_previous_non_comment_token,
                                    loop_second_previous_non_comment_token,
                                    &recent_statement_word_indices,
                                    open_paren_mysql_scalar_function,
                                );
                            if needs_space
                                && !keeps_aggregate_call_tight
                                && !keeps_count_star_call_tight
                                && !keeps_mysql_word_paren_tight
                            {
                                out.push(' ');
                            }
                            out.push('(');
                            current_rendered_line_paren_depth
                                .set(current_rendered_line_paren_depth.get().saturating_add(1));
                            deactivate_construct_flag!(CreateTableParenExpected);
                            let paren_indent_level_delta = query_like_layout
                                .map(|layout| layout.child_head_depth.saturating_sub(indent_level))
                                .unwrap_or_else(|| {
                                    if paren_frame_kind.opens_indented() {
                                        semantic_open_line_indent
                                            .saturating_add(1)
                                            .saturating_sub(indent_level)
                                    } else {
                                        paren_frame_kind.indent_level_delta()
                                    }
                                });
                            let branch_wrapper_close_indent = (paren_started_at_line_start
                                && format_stack.last_block_kind_is("CASE")
                                && matches!(prev_word_upper, Some("THEN" | "ELSE")))
                            .then_some(semantic_open_line_indent.saturating_sub(1));
                            let paren_close_indent_override = query_like_layout
                                .map(|layout| layout.close_depth)
                                .or(branch_wrapper_close_indent);
                            let capture_runtime_state = paren_frame_kind.is_query_like()
                                || multiline_clause_owner_kind.is_some();
                            let new_query_base_depth = query_like_layout
                                .map(|layout| layout.child_head_depth)
                                .filter(|_| paren_frame_kind.opens_indented());
                            let paren_semantic_flags = ParenSemanticFlags {
                                match_recognize: multiline_clause_owner_kind
                                    == Some(FormatIndentedParenOwnerKind::MatchRecognize),
                                analytic_over: is_analytic_over_paren,
                                window_clause: multiline_clause_owner_kind
                                    == Some(FormatIndentedParenOwnerKind::Window),
                                keep: is_keep_paren,
                            };
                            format_stack.push_paren(
                                ParenFormatFrame::new(
                                    paren_frame_kind,
                                    semantic_open_line_indent,
                                    paren_started_at_line_start,
                                    paren_indent_level_delta,
                                    paren_close_indent_override,
                                )
                                .with_query_like_layout(query_like_layout),
                                paren_semantic_flags,
                                capture_runtime_state,
                                new_query_base_depth,
                                wrapped_owner_kind.or(forced_wrapped_owner_kind),
                                None,
                                &mut indent_level,
                            );
                            if paren_frame_kind.opens_indented() {
                                // `indent_level` was already updated atomically
                                // inside `push_paren` using the frame's
                                // `indent_level_delta`. Here we only drive the
                                // newline/line_indent bookkeeping.
                                let keeps_inline_comment_after_open_paren = matches!(
                                    immediate_next_token,
                                    Some(SqlToken::Comment(comment))
                                        if !comment.starts_with('\n') && comment.contains('\n')
                                );
                                if keeps_inline_comment_after_open_paren {
                                    line_indent = if matches!(
                                        paren_frame_kind,
                                        ParenFormatFrameKind::WrappedLayout
                                            | ParenFormatFrameKind::ColumnList
                                    ) {
                                        semantic_open_line_indent.saturating_add(1)
                                    } else {
                                        base_indent!(indent_level)
                                    };
                                } else {
                                    let next_line_indent = query_like_layout
                                        .map(|layout| layout.child_head_depth)
                                        .unwrap_or_else(|| {
                                            if matches!(
                                                paren_frame_kind,
                                                ParenFormatFrameKind::WrappedLayout
                                                    | ParenFormatFrameKind::ColumnList
                                            ) {
                                                semantic_open_line_indent.saturating_add(1)
                                            } else {
                                                base_indent!(indent_level)
                                            }
                                        });
                                    newline_with(
                                        &mut out,
                                        next_line_indent,
                                        0,
                                        &mut at_line_start,
                                        &mut needs_space,
                                        &mut line_indent,
                                    );
                                }
                            } else if paren_frame_kind.suppresses_comma_breaks() {
                                suppress_comma_break_depth += 1;
                            }
                            needs_space = false;
                        }
                        ")" => {
                            format_stack.with_cte_on_close_paren();
                            trim_trailing_space(&mut out);
                            let popped_paren = format_stack.pop_paren(&mut indent_level);
                            let (
                                paren_frame_kind,
                                _restored_runtime_state,
                                open_cursor_restore_state,
                            ) = popped_paren
                                .map(|frame| {
                                    (
                                        frame.frame,
                                        frame.restored_runtime_state,
                                        frame.open_cursor_restore_state,
                                    )
                                })
                                .unwrap_or((
                                    ParenFormatFrame::new(
                                        ParenFormatFrameKind::Compact,
                                        0,
                                        false,
                                        0,
                                        None,
                                    ),
                                    false,
                                    None,
                                ));
                            let current_line = rendered_line_tracker
                                .active_query_owner_line(&out, at_line_start)
                                .trim_end();
                            let line_ends_with_end =
                                sql_text::line_ends_with_identifier_sequence_before_inline_comment(
                                    current_line,
                                    &["END"],
                                );
                            let line_ends_with_end_case =
                                sql_text::line_ends_with_identifier_sequence_before_inline_comment(
                                    current_line,
                                    &["END", "CASE"],
                                );
                            let close_case_paren_on_newline = !paren_frame_kind.closes_indented()
                                && (line_ends_with_end || line_ends_with_end_case)
                                && !(line_ends_with_end_case
                                    && matches!(
                                        next_non_comment,
                                        Some(SqlToken::Symbol(sym)) if sym == ";"
                                    ));
                            let close_compact_continuation_on_newline = !paren_frame_kind
                                .closes_indented()
                                && (paren_frame_kind.has_pending_multiline_child_continuation()
                                    || paren_frame_kind.has_simple_multiline_child_tail());
                            let close_compact_before_parent_close =
                                close_compact_continuation_on_newline
                                    && matches!(
                                        next_non_comment,
                                        Some(SqlToken::Symbol(sym)) if sym == ")"
                                    );
                            let close_follows_child_close_line =
                                rendered_line_tracker.current_line(&out).trim() == ")";
                            if paren_frame_kind.suppresses_comma_breaks() {
                                suppress_comma_break_depth =
                                    suppress_comma_break_depth.saturating_sub(1);
                            }
                            if paren_frame_kind.closes_indented() {
                                // `indent_level` was already restored by
                                // `pop_paren` using the frame's
                                // `indent_level_delta`. No manual decrement
                                // needed here.
                                let close_indent = if paren_frame_kind.close_indent() > 0 {
                                    paren_frame_kind.close_indent()
                                } else {
                                    base_indent!(indent_level)
                                };
                                newline_with(
                                    &mut out,
                                    close_indent,
                                    0,
                                    &mut at_line_start,
                                    &mut needs_space,
                                    &mut line_indent,
                                );
                                ensure_indent(&mut out, &mut at_line_start, line_indent);
                            } else if close_compact_continuation_on_newline
                                && !close_follows_child_close_line
                            {
                                let close_indent = if matches!(
                                    next_non_comment,
                                    Some(SqlToken::Symbol(sym)) if sym == ","
                                ) {
                                    format_stack
                                        .last_paren()
                                        .and_then(|frame| frame.sibling_body_indent())
                                        .unwrap_or(
                                            paren_frame_kind.compact_multiline_close_indent(),
                                        )
                                } else {
                                    paren_frame_kind.compact_multiline_close_indent()
                                };
                                newline_with(
                                    &mut out,
                                    close_indent,
                                    0,
                                    &mut at_line_start,
                                    &mut needs_space,
                                    &mut line_indent,
                                );
                                ensure_indent(&mut out, &mut at_line_start, line_indent);
                            }
                            if close_case_paren_on_newline {
                                let closes_plsql_condition_terminator =
                                    next_word_is("THEN") || next_word_is("LOOP");
                                let closes_control_branch_tail = (next_word_is("ELSE")
                                    || next_word_is("WHEN"))
                                    && format_stack.last_block_kind().is_some_and(|block| {
                                        matches!(block, BlockKind::If | BlockKind::While)
                                    })
                                    && format_stack
                                        .last_condition_owner(ConditionOwnerKind::ControlHeader)
                                        .is_some();
                                if (in_plsql_block && closes_plsql_condition_terminator)
                                    || closes_control_branch_tail
                                {
                                    let close_case_indent_level = indent_level.saturating_sub(1);
                                    newline_with(
                                        &mut out,
                                        close_case_indent_level,
                                        0,
                                        &mut at_line_start,
                                        &mut needs_space,
                                        &mut line_indent,
                                    );
                                } else {
                                    newline_with(
                                        &mut out,
                                        paren_frame_kind.compact_multiline_close_indent(),
                                        0,
                                        &mut at_line_start,
                                        &mut needs_space,
                                        &mut line_indent,
                                    );
                                }
                                ensure_indent(&mut out, &mut at_line_start, line_indent);
                            }
                            if open_cursor_state!()
                                .select_depth()
                                .is_some_and(|depth| format_stack.paren_depth() < depth)
                            {
                                let restored_open_cursor_state = format_stack
                                    .pop_open_cursor_restore()
                                    .or(open_cursor_restore_state)
                                    .unwrap_or(OpenCursorFormatState::None);
                                set_open_cursor_state!(restored_open_cursor_state);
                            }
                            if format_stack.paren_is_empty()
                                && matches!(
                                    next_non_comment,
                                    Some(SqlToken::Symbol(sym)) if sym == ","
                                )
                                && select_list_layout_state!().has_active_indent()
                            {
                                set_current_clause!(Some(
                                    if matches!(format_stack.current_clause(), Some("SET")) {
                                        "SET"
                                    } else {
                                        "SELECT"
                                    }
                                ));
                            }
                            if at_line_start
                                && !paren_frame_kind.closes_indented()
                                && line_indent
                                    < (if matches!(
                                        next_non_comment,
                                        Some(SqlToken::Symbol(sym)) if sym == ","
                                    ) {
                                        format_stack
                                            .last_paren()
                                            .and_then(|frame| frame.sibling_body_indent())
                                            .unwrap_or(
                                                paren_frame_kind.compact_multiline_close_indent(),
                                            )
                                    } else {
                                        paren_frame_kind.compact_multiline_close_indent()
                                    })
                            {
                                line_indent = if matches!(
                                    next_non_comment,
                                    Some(SqlToken::Symbol(sym)) if sym == ","
                                ) {
                                    format_stack
                                        .last_paren()
                                        .and_then(|frame| frame.sibling_body_indent())
                                        .unwrap_or(
                                            paren_frame_kind.compact_multiline_close_indent(),
                                        )
                                } else {
                                    paren_frame_kind.compact_multiline_close_indent()
                                };
                            }
                            ensure_indent(&mut out, &mut at_line_start, line_indent);
                            out.push(')');
                            current_rendered_line_paren_depth
                                .set(current_rendered_line_paren_depth.get().saturating_sub(1));
                            if let Some(parent_frame) = format_stack.last_paren_mut() {
                                if paren_frame_kind.contributes_multiline_close()
                                    && !close_compact_before_parent_close
                                {
                                    parent_frame.mark_multiline_child_closed();
                                }
                            }
                            needs_space = true;
                        }
                        "." => {
                            trim_trailing_space(&mut out);
                            out.push('.');
                            needs_space = false;
                        }
                        _ => {
                            ensure_indent(&mut out, &mut at_line_start, line_indent);
                            let is_mysql_block_label_colon = sym == ":"
                                && immediate_prev_token
                                    .is_some_and(|token| matches!(token, SqlToken::Word(_)))
                                && immediate_next_token.is_some_and(|token| {
                                    matches!(
                                        token,
                                        SqlToken::Word(next_word)
                                            if crate::sql_text::is_mysql_block_label_target(next_word)
                                    )
                                });
                            let is_plsql_attribute_prefix =
                                Self::is_plsql_attribute_prefix(sym, immediate_next_token);
                            // Don't add space between consecutive ampersands (&&var substitution)
                            let repeats_ampersand = sym == "&" && out.ends_with('&');
                            let repeats_at_sign = sym == "@" && out.ends_with('@');
                            let should_insert_space = needs_space
                                && !(repeats_ampersand
                                    || repeats_at_sign
                                    || is_mysql_block_label_colon
                                    || is_plsql_attribute_prefix);
                            if should_insert_space {
                                out.push(' ');
                            }
                            if is_mysql_block_label_colon {
                                trim_trailing_space(&mut out);
                            }
                            out.push_str(sym);
                            // For bind variables (:name) and assignment (:=), don't add space after colon
                            // Check if this is ":" and next token is a Word (bind variable)
                            let is_bind_var_colon = sym == ":"
                                && immediate_next_token
                                    .is_some_and(|token| matches!(token, SqlToken::Word(_)));
                            // For substitution variables (&var, &&var), don't add space after &
                            let is_ampersand_prefix = sym == "&"
                                && immediate_next_token.is_some_and(|token| {
                                    matches!(token, SqlToken::Word(_))
                                        || matches!(token, SqlToken::Symbol(s) if s == "&")
                                });
                            let is_mysql_user_variable_prefix = sym == "@"
                                && immediate_next_token.is_some_and(|token| {
                                    matches!(token, SqlToken::Word(_))
                                        || matches!(token, SqlToken::Symbol(s) if s == "@")
                                });
                            needs_space = is_mysql_block_label_colon
                                || (!is_bind_var_colon
                                    && !is_ampersand_prefix
                                    && !is_mysql_user_variable_prefix
                                    && !is_plsql_attribute_prefix);
                        }
                    }
                }
            }

            has_rendered_content = has_rendered_content
                || tokens
                    .get(rendered_token_idx)
                    .is_some_and(|token| match token {
                        SqlToken::Comment(comment) => !comment.trim().is_empty(),
                        _ => true,
                    });
            match tokens.get(idx) {
                Some(SqlToken::Word(_)) => {
                    recent_statement_word_indices.push_back(idx);
                    if recent_statement_word_indices.len() > 8 {
                        let _ = recent_statement_word_indices.pop_front();
                    }
                }
                Some(SqlToken::Symbol(sym)) if sym == ";" => {
                    recent_statement_word_indices.clear();
                }
                _ => {}
            }
            if let Some(token) = tokens.get(idx) {
                query_apply_context_tracker.observe(token, lexical_paren_depth_state.depth());
                statement_tail_state.observe(token);
                apply_paren_token(&mut lexical_paren_depth_state, token);
                delimiter_frame_state.apply_token(token);
                line_closes_delimiter_frame_below_start |= line_delimiter_frame_state
                    .apply_token_with_close_detection(
                        token,
                        line_start_delimiter_snapshot.baseline_depth(),
                    );
            }
            for consumed_idx in rendered_token_idx..=idx {
                if let Some(token) = tokens.get(consumed_idx) {
                    append_comment_prefix_token(&mut comment_prefix_text, token);
                }
            }
            previous_token_end_newline_count = current_token_profile.end_newline_count;
            idx += 1;
        }

        FormatterStatementRenderResult {
            formatted: Self::split_inline_statement_separators(out),
            has_code: statement_tail_state.has_code(statement),
            ends_with_semicolon: statement_tail_state.ends_with_semicolon(),
            should_append_missing_terminator: statement_tail_state
                .should_append_missing_terminator(),
            is_create_trigger_statement: input.is_create_trigger_statement,
        }
    }

    fn is_dml_clause_starter(trimmed_upper: &str) -> bool {
        matches!(
            trimmed_upper,
            "SELECT"
                | "WITH"
                | "FROM"
                | "WHERE"
                | "GROUP"
                | "HAVING"
                | "ORDER"
                | "VALUES"
                | "SET"
                | "CONNECT"
                | "START"
                | "UNION"
                | "INTERSECT"
                | "MINUS"
                | "EXCEPT"
                | "RETURNING"
                | "MODEL"
                | "WINDOW"
                | "MATCH_RECOGNIZE"
                | "PIVOT"
                | "UNPIVOT"
                | "QUALIFY"
                | "OFFSET"
        )
    }

    #[allow(dead_code)]
    fn fetch_into_has_multiple_targets(tokens: &[SqlToken], into_idx: usize) -> bool {
        let mut paren_depth = 0usize;
        let mut lookahead = into_idx.saturating_add(1);

        while lookahead < tokens.len() {
            match &tokens[lookahead] {
                SqlToken::Comment(comment) if comment.contains('\n') => {}
                SqlToken::Comment(_) => {}
                SqlToken::Symbol(sym) => {
                    // Matched bytes are all ASCII; UTF-8 guarantees ASCII bytes
                    // never appear inside multi-byte sequences, so byte iteration
                    // is equivalent and avoids per-char UTF-8 decoding.
                    for &b in sym.as_bytes() {
                        match b {
                            b'(' => paren_depth = paren_depth.saturating_add(1),
                            b')' => paren_depth = paren_depth.saturating_sub(1),
                            b',' if paren_depth == 0 => return true,
                            b';' if paren_depth == 0 => return false,
                            _ => {}
                        }
                    }
                }
                SqlToken::Word(word) if paren_depth == 0 => {
                    if word.eq_ignore_ascii_case("LIMIT")
                        || word.eq_ignore_ascii_case("BULK")
                        || word.eq_ignore_ascii_case("FROM")
                        || word.eq_ignore_ascii_case("WHERE")
                    {
                        return false;
                    }
                }
                _ => {}
            }

            lookahead = lookahead.saturating_add(1);
        }

        false
    }

    #[cfg(test)]
    fn line_is_standalone_open_paren_before_inline_comment(line: &str) -> bool {
        crate::sql_text::line_is_standalone_open_paren_before_inline_comment(line)
    }

    #[cfg(test)]
    fn structural_line_upper(line: &str) -> String {
        crate::sql_text::auto_format_structural_tail(line).to_ascii_uppercase()
    }

    #[cfg(test)]
    fn line_starts_structural_operator_rhs_with_next_upper(
        line: &str,
        next_code_upper: Option<&str>,
    ) -> bool {
        if crate::sql_text::starts_with_auto_format_structural_continuation_boundary_without_expression_owner(line) {
            return true;
        }

        if !Self::line_is_standalone_open_paren_before_inline_comment(line) {
            return false;
        }

        next_code_upper.is_some_and(|next_upper| {
            crate::sql_text::SUBQUERY_HEAD_KEYWORDS
                .iter()
                .any(|keyword| crate::sql_text::starts_with_keyword_token(next_upper, keyword))
        })
    }

    #[cfg(test)]
    fn line_starts_structural_operator_rhs(line: &str, next_code_trimmed: Option<&str>) -> bool {
        let next_code_upper = next_code_trimmed.map(Self::structural_line_upper);
        Self::line_starts_structural_operator_rhs_with_next_upper(line, next_code_upper.as_deref())
    }

    /// Returns `true` when `line` contains a CASE keyword that is not closed
    /// by a matching END on the same line.  This detects mid-line CASE
    /// expressions such as `SELECT col, CASE` or `SET col = CASE` so that
    /// the formatter can open a [`DmlCaseLayoutFrame`] even when the CASE
    /// keyword does not start the line.
    #[cfg(test)]
    fn line_has_trailing_unclosed_case(line: &str) -> bool {
        let tokens = super::query_text::tokenize_sql(line);
        let mut open_cases = 0usize;
        let mut prev_was_end = false;

        for token in &tokens {
            if let SqlToken::Word(word) = token {
                if word.eq_ignore_ascii_case("CASE") {
                    if prev_was_end {
                        // `END CASE` — this closes a CASE, not opens one.
                        open_cases = open_cases.saturating_sub(1);
                        prev_was_end = false;
                    } else {
                        open_cases += 1;
                    }
                } else {
                    if word.eq_ignore_ascii_case("END") {
                        // Bare END (without CASE qualifier) also closes a CASE.
                        open_cases = open_cases.saturating_sub(1);
                    }
                    prev_was_end = word.eq_ignore_ascii_case("END");
                }
            } else {
                prev_was_end = false;
            }
        }

        open_cases > 0
    }

    fn paren_multiline_clause_owner_kind(
        tokens: &[SqlToken],
        open_idx: usize,
        statement_word_links: Option<&StatementWordLinks>,
    ) -> Option<FormatIndentedParenOwnerKind> {
        const MAX_PRECEDING_WORDS: usize = 8;

        let mut preceding_words = Self::recent_statement_words_before(
            tokens,
            open_idx,
            MAX_PRECEDING_WORDS,
            statement_word_links,
        );

        if preceding_words.is_empty() {
            return None;
        }

        preceding_words.reverse();
        crate::sql_text::format_indented_paren_owner_kind_from_words(&preceding_words)
    }

    fn matching_close_paren_index(
        tokens: &[SqlToken],
        open_idx: usize,
        matching_paren_close_indices: Option<&[Option<usize>]>,
    ) -> Option<usize> {
        if let Some(close_idx) = matching_paren_close_indices
            .and_then(|indices| indices.get(open_idx))
            .copied()
            .flatten()
        {
            return Some(close_idx);
        }

        let mut paren_depth = 0usize;

        for (idx, token) in tokens.iter().enumerate().skip(open_idx) {
            match token {
                SqlToken::Symbol(sym) if sym == "(" => {
                    paren_depth = paren_depth.saturating_add(1);
                }
                SqlToken::Symbol(sym) if sym == ")" => {
                    paren_depth = paren_depth.saturating_sub(1);
                    if paren_depth == 0 {
                        return Some(idx);
                    }
                }
                _ => {}
            }
        }

        None
    }

    fn analyze_paren_body(
        _tokens: &[SqlToken],
        open_idx: usize,
        _matching_paren_close_indices: Option<&[Option<usize>]>,
        paren_body_analysis: Option<&[Option<ParenBodyAnalysis>]>,
    ) -> ParenBodyAnalysis {
        paren_body_analysis
            .and_then(|cache| cache.get(open_idx))
            .copied()
            .flatten()
            .unwrap_or_default()
    }

    #[allow(dead_code)]
    fn paren_body_contains_nested_multiline_child(
        tokens: &[SqlToken],
        open_idx: usize,
        matching_paren_close_indices: Option<&[Option<usize>]>,
        open_paren_multiline_child_head_prefix_counts: Option<&[usize]>,
        meaningful_token_links: Option<&MeaningfulTokenLinks>,
        statement_word_links: Option<&StatementWordLinks>,
    ) -> bool {
        let Some(close_idx) =
            Self::matching_close_paren_index(tokens, open_idx, matching_paren_close_indices)
        else {
            return false;
        };

        if let Some(prefix_counts) = open_paren_multiline_child_head_prefix_counts {
            let start = open_idx.saturating_add(1).min(tokens.len());
            let end = close_idx.min(tokens.len());
            if end <= start {
                return false;
            }

            let start_count = prefix_counts.get(start).copied().unwrap_or(0);
            let end_count = prefix_counts.get(end).copied().unwrap_or(start_count);
            return end_count > start_count;
        }

        for idx in open_idx.saturating_add(1)..close_idx {
            if !matches!(tokens.get(idx), Some(SqlToken::Symbol(sym)) if sym == "(") {
                continue;
            }

            let next_non_comment_idx = meaningful_token_links
                .and_then(|links| links.next_index(idx))
                .or_else(|| {
                    tokens
                        .iter()
                        .enumerate()
                        .skip(idx.saturating_add(1))
                        .find(|(_, token)| !matches!(token, SqlToken::Comment(_)))
                        .map(|(token_idx, _)| token_idx)
                });
            let next_non_comment = next_non_comment_idx.and_then(|token_idx| tokens.get(token_idx));
            let opens_query_like_child = matches!(
                next_non_comment,
                Some(SqlToken::Word(word))
                    if crate::sql_text::is_subquery_head_keyword(word)
            );
            let opens_wrapped_layout_child =
                Self::paren_starts_wrapped_layout_head(tokens, idx, meaningful_token_links);
            let opens_owner_relative_child =
                Self::paren_multiline_clause_owner_kind(tokens, idx, statement_word_links)
                    .is_some();
            let prev_word_upper = statement_word_links
                .and_then(|links| links.prev_word_index(idx))
                .and_then(|token_idx| tokens.get(token_idx))
                .and_then(|token| match token {
                    SqlToken::Word(word) => Some(word.to_ascii_uppercase()),
                    _ => None,
                })
                .or_else(|| Self::previous_word_upper(tokens, idx).map(|(word, _)| word));
            let opens_column_list_child =
                Self::paren_opens_structured_column_list(prev_word_upper.as_deref(), false);

            if opens_query_like_child
                || opens_wrapped_layout_child
                || opens_owner_relative_child
                || opens_column_list_child
            {
                return true;
            }
        }

        false
    }

    #[allow(dead_code)]
    fn paren_body_contains_nested_analytic_clause_owner(
        tokens: &[SqlToken],
        open_idx: usize,
        matching_paren_close_indices: Option<&[Option<usize>]>,
        statement_word_links: Option<&StatementWordLinks>,
    ) -> bool {
        let Some(close_idx) =
            Self::matching_close_paren_index(tokens, open_idx, matching_paren_close_indices)
        else {
            return false;
        };

        for idx in open_idx.saturating_add(1)..close_idx {
            if !matches!(tokens.get(idx), Some(SqlToken::Symbol(sym)) if sym == "(") {
                continue;
            }

            if let Some(owner_kind) =
                Self::paren_multiline_clause_owner_kind(tokens, idx, statement_word_links)
            {
                if matches!(
                    owner_kind,
                    FormatIndentedParenOwnerKind::AnalyticOver
                        | FormatIndentedParenOwnerKind::WithinGroup
                        | FormatIndentedParenOwnerKind::Keep
                ) {
                    return true;
                }
            }
        }

        false
    }

    #[allow(dead_code)]
    fn paren_body_has_top_level_comma(
        tokens: &[SqlToken],
        open_idx: usize,
        matching_paren_close_indices: Option<&[Option<usize>]>,
    ) -> bool {
        let Some(close_idx) =
            Self::matching_close_paren_index(tokens, open_idx, matching_paren_close_indices)
        else {
            return false;
        };

        let mut nested_depth = 0usize;
        for token in tokens
            .iter()
            .skip(open_idx.saturating_add(1))
            .take(close_idx.saturating_sub(open_idx.saturating_add(1)))
        {
            match token {
                SqlToken::Symbol(sym) if sym == "(" => {
                    nested_depth = nested_depth.saturating_add(1);
                }
                SqlToken::Symbol(sym) if sym == ")" => {
                    nested_depth = nested_depth.saturating_sub(1);
                }
                SqlToken::Symbol(sym) if sym == "," && nested_depth == 0 => {
                    return true;
                }
                _ => {}
            }
        }

        false
    }

    #[allow(dead_code)]
    fn paren_owner_requires_wrapped_layout(
        tokens: &[SqlToken],
        open_idx: usize,
        owner_kind: FormatIndentedParenOwnerKind,
        matching_paren_close_indices: Option<&[Option<usize>]>,
        open_paren_multiline_child_head_prefix_counts: Option<&[usize]>,
        meaningful_token_links: Option<&MeaningfulTokenLinks>,
        statement_word_links: Option<&StatementWordLinks>,
    ) -> bool {
        matches!(
            owner_kind,
            FormatIndentedParenOwnerKind::WithinGroup | FormatIndentedParenOwnerKind::Keep
        ) && Self::paren_body_contains_nested_multiline_child(
            tokens,
            open_idx,
            matching_paren_close_indices,
            open_paren_multiline_child_head_prefix_counts,
            meaningful_token_links,
            statement_word_links,
        )
    }

    fn active_phase1_wrapped_owner_kind_at_depth(
        format_stack: &FormatFrameStack,
        paren_depth: usize,
    ) -> Option<FormatIndentedParenOwnerKind> {
        (format_stack.paren_depth() == paren_depth)
            .then(|| format_stack.last_paren_wrapped_owner_kind())
            .flatten()
    }

    fn paren_starts_first_clause_list_item(
        current_clause: Option<&str>,
        prev_word_upper: Option<&str>,
        is_query_paren: bool,
        paren_depth: usize,
    ) -> bool {
        if paren_depth > 0 {
            return false;
        }

        matches!(
            (current_clause, prev_word_upper),
            (Some("SELECT"), Some("SELECT"))
        ) || (is_query_paren
            && matches!(
                (current_clause, prev_word_upper),
                (Some("GROUP" | "ORDER"), Some("BY"))
            ))
    }

    fn paren_extra_depth(format_stack: &FormatFrameStack) -> usize {
        format_stack.paren_extra_depth()
    }

    fn paren_extra_depth_within_current_query_frame(format_stack: &FormatFrameStack) -> usize {
        format_stack.paren_extra_depth_within_current_query_frame()
    }

    #[cfg_attr(not(test), allow(dead_code))]
    fn line_closes_paren_frame_below_line_start_before_token(
        tokens: &[SqlToken],
        line_start_idx: usize,
        token_idx: usize,
        line_start_snapshot: &DelimiterLineStartSnapshot,
    ) -> bool {
        line_closes_delimiter_frame_below_snapshot_before_token(
            tokens,
            line_start_idx,
            token_idx,
            line_start_snapshot,
        )
    }

    fn token_can_own_wrapped_layout(previous_non_comment: Option<&SqlToken>) -> bool {
        match previous_non_comment {
            None => true,
            Some(SqlToken::Symbol(symbol)) => matches!(
                symbol.as_str(),
                "(" | ","
                    | "="
                    | ":="
                    | "+"
                    | "-"
                    | "*"
                    | "/"
                    | "||"
                    | "|"
                    | "<"
                    | ">"
                    | "<="
                    | ">="
                    | "<>"
                    | "!="
                    | "=>"
            ),
            Some(SqlToken::Word(word)) => {
                sql_text::is_oracle_sql_keyword(&word.to_ascii_uppercase())
            }
            _ => false,
        }
    }

    #[allow(dead_code)]
    fn paren_starts_wrapped_layout_head(
        tokens: &[SqlToken],
        open_idx: usize,
        meaningful_token_links: Option<&MeaningfulTokenLinks>,
    ) -> bool {
        let previous_non_comment = meaningful_token_links
            .and_then(|links| links.prev_index(open_idx))
            .and_then(|token_idx| tokens.get(token_idx))
            .or_else(|| {
                tokens[..open_idx]
                    .iter()
                    .rev()
                    .find(|token| !matches!(token, SqlToken::Comment(_)))
            });
        if !Self::token_can_own_wrapped_layout(previous_non_comment) {
            return false;
        }

        let mut saw_wrapper_paren = false;

        for token in tokens.iter().skip(open_idx.saturating_add(1)) {
            match token {
                SqlToken::Comment(_) => {}
                SqlToken::Symbol(symbol) if symbol == "(" => {
                    saw_wrapper_paren = true;
                }
                SqlToken::Word(word) => {
                    if word.eq_ignore_ascii_case("CASE") {
                        return true;
                    }

                    return saw_wrapper_paren && crate::sql_text::is_subquery_head_keyword(word);
                }
                _ => return false,
            }
        }

        false
    }

    fn non_comment_tokens_prefix_text(tokens: &[SqlToken], idx: usize) -> Option<String> {
        let mut prefix = String::new();

        for token in tokens[..idx]
            .iter()
            .filter(|token| !matches!(token, SqlToken::Comment(_)))
        {
            if !prefix.is_empty() {
                prefix.push(' ');
            }
            prefix.push_str(&Self::token_text(token));
        }

        (!prefix.is_empty()).then_some(prefix)
    }

    fn next_meaningful_token_index_with_links(
        tokens: &[SqlToken],
        idx: usize,
        meaningful_token_links: Option<&MeaningfulTokenLinks>,
    ) -> Option<usize> {
        meaningful_token_links
            .and_then(|links| links.next_index(idx))
            .or_else(|| Self::next_meaningful_token_index(tokens, idx))
    }

    fn close_alias_tail_token_text(token: &SqlToken) -> Option<&str> {
        match token {
            SqlToken::Word(word) => Some(word.as_str()),
            SqlToken::String(literal) => Some(literal.as_str()),
            SqlToken::Symbol(_) | SqlToken::Comment(_) => None,
        }
    }

    fn allows_terminal_close_alias_tail_in_clause(current_clause: Option<&str>) -> bool {
        matches!(
            current_clause,
            Some("SELECT" | "FROM" | "UPDATE" | "DELETE" | "INTO" | "MERGE" | "USING")
        )
    }

    fn token_ends_terminal_close_alias_tail(token: Option<&SqlToken>) -> bool {
        match token {
            None => true,
            Some(SqlToken::Symbol(symbol)) => matches!(symbol.as_str(), "," | ")" | ";"),
            Some(SqlToken::Word(word)) => {
                let upper = word.to_ascii_uppercase();
                Self::is_dml_clause_starter(upper.as_str())
                    || crate::sql_text::is_format_set_operator_keyword(upper.as_str())
            }
            Some(SqlToken::String(_) | SqlToken::Comment(_)) => false,
        }
    }

    fn block_comment_keeps_inline_terminal_close_alias_tail(
        current_line: &str,
        tokens: &[SqlToken],
        token_context: FormatterTokenContext<'_>,
        current_clause: Option<&str>,
    ) -> bool {
        if !Self::allows_terminal_close_alias_tail_in_clause(current_clause) {
            return false;
        }

        let Some(next_token) = token_context.next_non_comment_token(tokens) else {
            return false;
        };
        let Some(next_text) = Self::close_alias_tail_token_text(next_token) else {
            return false;
        };

        let (alias_tail_suffix, boundary_token) =
            if next_text.eq_ignore_ascii_case("AS") || next_text.eq_ignore_ascii_case("IS") {
                let Some(alias_token) = token_context.second_next_non_comment_token(tokens) else {
                    return false;
                };
                let Some(alias_text) = Self::close_alias_tail_token_text(alias_token) else {
                    return false;
                };
                (
                    format!("{next_text} {alias_text}"),
                    token_context.third_next_non_comment_token(tokens),
                )
            } else {
                (
                    next_text.to_string(),
                    token_context.second_next_non_comment_token(tokens),
                )
            };

        let current_line = current_line.trim_end();
        if current_line.is_empty() {
            return false;
        }

        let mut alias_tail_line = current_line.to_string();
        if !alias_tail_line.ends_with(' ') && !alias_tail_line.ends_with('\t') {
            alias_tail_line.push(' ');
        }
        alias_tail_line.push_str(alias_tail_suffix.as_str());

        crate::sql_text::line_has_leading_significant_close_paren(alias_tail_line.as_str())
            && crate::sql_text::auto_format_structural_tail_is_alias_fragment(
                alias_tail_line.as_str(),
            )
            && Self::token_ends_terminal_close_alias_tail(boundary_token)
    }

    fn format_trailing_meaningful_token_from_sql_token(
        token: &SqlToken,
    ) -> Option<crate::sql_text::FormatTrailingMeaningfulToken<'_>> {
        match token {
            SqlToken::Word(word) => Some(crate::sql_text::FormatTrailingMeaningfulToken::Word(
                word.as_str(),
            )),
            SqlToken::Symbol(symbol) => Some(
                crate::sql_text::FormatTrailingMeaningfulToken::Symbol(symbol.as_str()),
            ),
            SqlToken::String(_) | SqlToken::Comment(_) => {
                Some(crate::sql_text::FormatTrailingMeaningfulToken::Other)
            }
        }
    }

    fn comment_header_continuation_kind(
        tokens: &[SqlToken],
        idx: usize,
    ) -> Option<crate::sql_text::FormatInlineCommentHeaderContinuationKind> {
        Self::non_comment_tokens_prefix_text(tokens, idx)
            .and_then(|prefix| crate::sql_text::format_inline_comment_continuation_kind(&prefix))
    }

    fn comment_header_continuation_kind_for_prefix(
        structural_prefix: &str,
    ) -> Option<crate::sql_text::FormatInlineCommentHeaderContinuationKind> {
        (!structural_prefix.is_empty())
            .then_some(structural_prefix)
            .and_then(crate::sql_text::format_inline_comment_continuation_kind)
    }

    fn resolve_comment_header_continuation_indent(
        continuation_kind: Option<crate::sql_text::FormatInlineCommentHeaderContinuationKind>,
        same_depth_anchor: usize,
        query_base_anchor: usize,
        current_line_depth: usize,
        fallback_indent: usize,
    ) -> usize {
        continuation_kind
            .map(|kind| {
                crate::sql_text::resolve_format_header_continuation_depth_with_anchors(
                    kind,
                    same_depth_anchor,
                    current_line_depth,
                    Some(query_base_anchor),
                )
            })
            .unwrap_or(fallback_indent)
    }

    #[cfg(test)]
    fn is_plsql_like_tokens(statement: &str, tokens: &[SqlToken]) -> bool {
        let words: Vec<&str> = tokens
            .iter()
            .filter_map(|token| match token {
                SqlToken::Word(word) => Some(word.as_str()),
                _ => None,
            })
            .collect();

        if let Some(first) = words.first().copied() {
            if first.eq_ignore_ascii_case("SELECT")
                || first.eq_ignore_ascii_case("INSERT")
                || first.eq_ignore_ascii_case("UPDATE")
                || first.eq_ignore_ascii_case("DELETE")
                || first.eq_ignore_ascii_case("MERGE")
            {
                return false;
            }
            if first.eq_ignore_ascii_case("WITH") {
                let mut next_index = 1usize;
                if words
                    .get(next_index)
                    .is_some_and(|word| word.eq_ignore_ascii_case("RECURSIVE"))
                {
                    next_index += 1;
                }
                if words.get(next_index).is_some_and(|word| {
                    word.eq_ignore_ascii_case("FUNCTION") || word.eq_ignore_ascii_case("PROCEDURE")
                }) {
                    return true;
                }
                return false;
            }
        }

        for word in &words {
            if word.eq_ignore_ascii_case("BEGIN") || word.eq_ignore_ascii_case("DECLARE") {
                return true;
            }
            if word.eq_ignore_ascii_case("CREATE") {
                let object_type = Self::parse_ddl_object_type(statement);
                return matches!(
                    object_type,
                    "Procedure"
                        | "Function"
                        | "Package"
                        | "Package Body"
                        | "Type"
                        | "Type Body"
                        | "Trigger"
                );
            }
        }

        Self::is_plsql_control_fragment(&words)
    }

    #[cfg(test)]
    fn is_plsql_control_fragment(words: &[&str]) -> bool {
        if words.is_empty() {
            return false;
        }

        let first = words[0];
        if first.eq_ignore_ascii_case("IF")
            && words.iter().any(|word| word.eq_ignore_ascii_case("THEN"))
        {
            return true;
        }

        if first.eq_ignore_ascii_case("ELSIF") || first.eq_ignore_ascii_case("ELSEIF") {
            return true;
        }

        if first.eq_ignore_ascii_case("WHILE")
            && words.iter().any(|word| word.eq_ignore_ascii_case("LOOP"))
        {
            return true;
        }

        words.windows(2).any(|pair| {
            pair[0].eq_ignore_ascii_case("END")
                && (pair[1].eq_ignore_ascii_case("IF")
                    || pair[1].eq_ignore_ascii_case("LOOP")
                    || pair[1].eq_ignore_ascii_case("CASE"))
        })
    }

    #[cfg(test)]
    fn is_plsql_like_statement(statement: &str) -> bool {
        let tokens = Self::tokenize_sql(statement);
        Self::is_plsql_like_tokens(statement, &tokens)
    }

    fn paren_opens_structured_column_list(
        prev_word_upper: Option<&str>,
        create_table_paren_expected: bool,
    ) -> bool {
        // Oracle table functions expose nested column definitions through
        // `... COLUMNS (...)`; those parentheses should format like a column
        // list instead of a generic function-argument group.
        create_table_paren_expected || matches!(prev_word_upper, Some("COLUMNS"))
    }

    fn paren_opens_multiline_routine_parameter_list(
        tokens: &[SqlToken],
        token_spans: &[SqlTokenSpan],
        statement: &str,
        open_idx: usize,
        matching_paren_close_indices: Option<&[Option<usize>]>,
        meaningful_token_links: Option<&MeaningfulTokenLinks>,
    ) -> bool {
        Self::paren_opens_routine_parameter_list(tokens, open_idx, meaningful_token_links)
            && Self::paren_body_contains_newline(
                tokens,
                token_spans,
                statement,
                open_idx,
                matching_paren_close_indices,
            )
    }

    fn paren_opens_routine_parameter_list(
        tokens: &[SqlToken],
        open_idx: usize,
        meaningful_token_links: Option<&MeaningfulTokenLinks>,
    ) -> bool {
        const MAX_LOOKBACK_TOKENS: usize = 6;

        let mut lookback = 0usize;
        let mut saw_identifier = false;

        let mut lookback_idx = meaningful_token_links
            .and_then(|links| links.prev_index(open_idx))
            .or_else(|| Self::previous_meaningful_token_index(tokens, open_idx));
        while let Some(current_idx) = lookback_idx {
            let Some(token) = tokens.get(current_idx) else {
                break;
            };
            match token {
                SqlToken::Word(word) => {
                    lookback = lookback.saturating_add(1);
                    if lookback > MAX_LOOKBACK_TOKENS {
                        return false;
                    }

                    if word.eq_ignore_ascii_case("PROCEDURE")
                        || word.eq_ignore_ascii_case("FUNCTION")
                    {
                        return saw_identifier;
                    }

                    if word.eq_ignore_ascii_case("RETURN")
                        || word.eq_ignore_ascii_case("RETURNS")
                        || word.eq_ignore_ascii_case("IN")
                        || word.eq_ignore_ascii_case("OUT")
                        || word.eq_ignore_ascii_case("INOUT")
                        || word.eq_ignore_ascii_case("DEFAULT")
                    {
                        return false;
                    }

                    saw_identifier = true;
                }
                SqlToken::Symbol(symbol) if symbol == "." => {
                    lookback = lookback.saturating_add(1);
                    if lookback > MAX_LOOKBACK_TOKENS || !saw_identifier {
                        return false;
                    }
                }
                SqlToken::String(_) => return false,
                SqlToken::Symbol(_) => return false,
                SqlToken::Comment(_) => {}
            }
            lookback_idx = meaningful_token_links
                .and_then(|links| links.prev_index(current_idx))
                .or_else(|| Self::previous_meaningful_token_index(tokens, current_idx));
        }

        false
    }

    fn paren_body_contains_newline(
        tokens: &[SqlToken],
        token_spans: &[SqlTokenSpan],
        statement: &str,
        open_idx: usize,
        matching_paren_close_indices: Option<&[Option<usize>]>,
    ) -> bool {
        if tokens.len() != token_spans.len() {
            return false;
        }

        let Some(close_idx) =
            Self::matching_close_paren_index(tokens, open_idx, matching_paren_close_indices)
        else {
            return false;
        };
        let Some(open_span) = token_spans.get(open_idx) else {
            return false;
        };
        let Some(close_span) = token_spans.get(close_idx) else {
            return false;
        };

        if open_span.end > close_span.start {
            return false;
        }

        statement
            .get(open_span.end..close_span.start)
            .is_some_and(|body| body.contains('\n'))
    }

    fn paren_contains_query_like_child(
        tokens: &[SqlToken],
        open_idx: usize,
        _matching_paren_close_indices: Option<&[Option<usize>]>,
    ) -> bool {
        let (matching_paren_close_indices, paren_body_analyses) =
            Self::build_matching_paren_close_indices_and_body_analysis(tokens);
        Self::analyze_paren_body(
            tokens,
            open_idx,
            Some(matching_paren_close_indices.as_slice()),
            Some(paren_body_analyses.as_slice()),
        )
        .contains_query_like_child
    }

    fn paren_contains_analytic_clause_owner_child(
        tokens: &[SqlToken],
        open_idx: usize,
        _matching_paren_close_indices: Option<&[Option<usize>]>,
    ) -> bool {
        let (matching_paren_close_indices, paren_body_analyses) =
            Self::build_matching_paren_close_indices_and_body_analysis(tokens);
        Self::analyze_paren_body(
            tokens,
            open_idx,
            Some(matching_paren_close_indices.as_slice()),
            Some(paren_body_analyses.as_slice()),
        )
        .contains_analytic_clause_owner_child
    }

    fn paren_has_top_level_comma(
        tokens: &[SqlToken],
        open_idx: usize,
        _matching_paren_close_indices: Option<&[Option<usize>]>,
    ) -> bool {
        let (matching_paren_close_indices, paren_body_analyses) =
            Self::build_matching_paren_close_indices_and_body_analysis(tokens);
        Self::analyze_paren_body(
            tokens,
            open_idx,
            Some(matching_paren_close_indices.as_slice()),
            Some(paren_body_analyses.as_slice()),
        )
        .has_top_level_comma
    }

    #[cfg(test)]
    fn parse_ddl_object_type(statement: &str) -> &'static str {
        let upper = statement.to_uppercase();
        QueryExecutor::parse_ddl_object_type(&upper)
    }

    fn format_create_table(tokens: &[SqlToken], mysql_compatible: bool) -> Option<String> {
        if tokens.is_empty() {
            return None;
        }

        // Guard: only apply CREATE TABLE formatting when TABLE is the actual
        // object keyword in the CREATE header. This avoids false matches like
        // CREATE PACKAGE BODY ... TYPE ... IS TABLE OF ...
        let mut word_positions: Vec<(usize, &str)> = Vec::new();
        for (idx, token) in tokens.iter().enumerate() {
            if let SqlToken::Word(word) = token {
                word_positions.push((idx, word.as_str()));
            }
        }

        let create_word_idx = word_positions
            .iter()
            .position(|(_, word)| word.eq_ignore_ascii_case("CREATE"))?;

        let mut header_idx = create_word_idx + 1;
        while let Some((_, word)) = word_positions.get(header_idx) {
            if word.eq_ignore_ascii_case("OR")
                || word.eq_ignore_ascii_case("REPLACE")
                || word.eq_ignore_ascii_case("EDITIONABLE")
                || word.eq_ignore_ascii_case("NONEDITIONABLE")
            {
                header_idx += 1;
                continue;
            }
            break;
        }

        if (word_positions
            .get(header_idx)
            .is_some_and(|(_, word)| word.eq_ignore_ascii_case("GLOBAL"))
            || word_positions
                .get(header_idx)
                .is_some_and(|(_, word)| word.eq_ignore_ascii_case("PRIVATE")))
            && word_positions
                .get(header_idx + 1)
                .is_some_and(|(_, word)| word.eq_ignore_ascii_case("TEMPORARY"))
        {
            header_idx += 2;
        }

        let (_, create_object) = word_positions.get(header_idx)?;
        if !create_object.eq_ignore_ascii_case("TABLE") {
            return None;
        }

        let mut seen_table = false;
        let mut ctas = false;
        let mut open_idx: Option<usize> = None;
        let mut close_idx: Option<usize> = None;
        let mut idx = 0usize;
        let mut word_position_idx = 0usize;
        let mut paren_depth_state = ParenDepthState::default();

        while idx < tokens.len() {
            let token = &tokens[idx];
            let current_paren_depth = paren_depth_state.depth();
            match token {
                SqlToken::Word(word) => {
                    let next_word = word_positions
                        .get(word_position_idx.saturating_add(1))
                        .map(|(_, value)| *value);
                    if !seen_table && word.eq_ignore_ascii_case("TABLE") {
                        seen_table = true;
                    } else if seen_table
                        && word.eq_ignore_ascii_case("AS")
                        && next_word.is_some_and(|next| {
                            next.eq_ignore_ascii_case("SELECT") || next.eq_ignore_ascii_case("WITH")
                        })
                    {
                        ctas = true;
                    }
                    word_position_idx = word_position_idx.saturating_add(1);
                }
                SqlToken::Symbol(sym) if sym == "(" => {
                    if current_paren_depth == 0 && seen_table && !ctas && open_idx.is_none() {
                        open_idx = Some(idx);
                    }
                }
                SqlToken::Symbol(sym) if sym == ")" => {
                    if current_paren_depth == 1 && open_idx.is_some() && close_idx.is_none() {
                        close_idx = Some(idx);
                        break;
                    }
                }
                _ => {}
            }
            apply_paren_token(&mut paren_depth_state, token);
            idx += 1;
        }

        let (open_idx, close_idx) = match (open_idx, close_idx) {
            (Some(open_idx), Some(close_idx)) => (open_idx, close_idx),
            _ => return None,
        };

        let prefix_tokens = &tokens[..open_idx];
        let column_tokens = &tokens[open_idx + 1..close_idx];
        let suffix_tokens = &tokens[close_idx + 1..];

        let mut columns: Vec<Vec<SqlToken>> = Vec::new();
        for group in split_top_level_symbol_groups(column_tokens, ",") {
            columns.push(group.into_iter().cloned().collect());
        }

        if columns.is_empty() {
            return None;
        }

        // (is_constraint, name, type_str, rest_str, leading_comments, trailing_comments)
        let mut formatted_cols: Vec<(bool, String, String, String, Vec<String>, Vec<String>)> =
            Vec::new();
        let mut max_name = 0usize;
        let mut max_type = 0usize;

        for column in &columns {
            let mut iter = column.iter().filter(|t| !matches!(t, SqlToken::Comment(_)));
            let first = iter.next();
            let is_constraint = match first {
                Some(SqlToken::Word(word)) => {
                    word.eq_ignore_ascii_case("CONSTRAINT")
                        || word.eq_ignore_ascii_case("PRIMARY")
                        || word.eq_ignore_ascii_case("UNIQUE")
                        || word.eq_ignore_ascii_case("FOREIGN")
                        || word.eq_ignore_ascii_case("CHECK")
                }
                _ => false,
            };

            // Separate leading comments (before first non-comment token)
            // from trailing comments (after first non-comment token)
            let mut leading_comments: Vec<String> = Vec::new();
            let mut trailing_comments: Vec<String> = Vec::new();
            let mut seen_code = false;
            for token in column {
                match token {
                    SqlToken::Comment(comment) => {
                        let body = comment.strip_prefix('\n').unwrap_or(comment);
                        let trimmed = body.trim_end_matches('\n');
                        if !trimmed.is_empty() {
                            if seen_code {
                                trailing_comments.push(trimmed.to_string());
                            } else {
                                leading_comments.push(trimmed.to_string());
                            }
                        }
                    }
                    _ => {
                        seen_code = true;
                    }
                }
            }

            if is_constraint {
                let non_comment_tokens: Vec<SqlToken> = column
                    .iter()
                    .filter(|t| !matches!(t, SqlToken::Comment(_)))
                    .cloned()
                    .collect();
                let text = Self::join_tokens_spaced_for_create_table(
                    &non_comment_tokens,
                    0,
                    mysql_compatible,
                );
                formatted_cols.push((
                    true,
                    text,
                    String::new(),
                    String::new(),
                    leading_comments,
                    trailing_comments,
                ));
                continue;
            }

            let mut tokens_iter = column
                .iter()
                .filter(|t| !matches!(t, SqlToken::Comment(_)))
                .peekable();
            let name_token = tokens_iter.next();
            // Preserve the original case for column names: they are identifiers, not
            // keywords, even if the word happens to match a SQL keyword (e.g. `profile`).
            let name = name_token
                .map(|t| match t {
                    SqlToken::Word(w) => w.clone(),
                    _ => Self::token_text(t),
                })
                .unwrap_or_default();

            let mut type_tokens: Vec<SqlToken> = Vec::new();
            let mut rest_tokens: Vec<SqlToken> = Vec::new();
            let mut in_type = true;

            for token in tokens_iter {
                let is_constraint_token = match token {
                    SqlToken::Word(word) => {
                        sql_text::is_format_column_constraint_keyword(word.as_str())
                    }
                    _ => false,
                };
                if in_type && is_constraint_token {
                    in_type = false;
                }
                if in_type {
                    type_tokens.push(token.clone());
                } else {
                    rest_tokens.push(token.clone());
                }
            }

            let type_str =
                Self::join_tokens_compact_for_create_table(&type_tokens, mysql_compatible);
            let rest_str =
                Self::join_tokens_spaced_for_create_table(&rest_tokens, 0, mysql_compatible);

            max_name = max_name.max(name.len());
            max_type = max_type.max(type_str.len());
            formatted_cols.push((
                false,
                name,
                type_str,
                rest_str,
                leading_comments,
                trailing_comments,
            ));
        }

        let mut out = String::new();
        let prefix = Self::join_tokens_spaced_for_create_table(prefix_tokens, 0, mysql_compatible);
        out.push_str(prefix.trim_end());
        out.push_str(" (\n");

        let indent = " ".repeat(4);
        for (idx, (is_constraint, name, type_str, rest_str, leading_comments, trailing_comments)) in
            formatted_cols.into_iter().enumerate()
        {
            // Output leading comments (originally before the column)
            for comment in &leading_comments {
                out.push_str(&indent);
                out.push_str(comment);
                out.push('\n');
            }
            out.push_str(&indent);
            if is_constraint {
                out.push_str(&name);
            } else {
                let name_pad = max_name.saturating_sub(name.len());
                let type_pad = max_type.saturating_sub(type_str.len());
                out.push_str(&name);
                if !type_str.is_empty() {
                    out.push_str(&" ".repeat(name_pad + 1));
                    out.push_str(&type_str);
                    if !rest_str.is_empty() {
                        out.push_str(&" ".repeat(type_pad + 1));
                        out.push_str(&rest_str);
                    }
                }
            }
            if idx + 1 < columns.len() {
                out.push(',');
            }
            out.push('\n');
            // Output trailing comments (originally after the column definition)
            for comment in &trailing_comments {
                out.push_str(&indent);
                out.push_str(comment);
                out.push('\n');
            }
        }
        out.push(')');

        let suffix = Self::format_create_suffix_for_create_table(suffix_tokens, mysql_compatible);
        if !suffix.is_empty() {
            out.push('\n');
            out.push_str(&suffix);
        }

        Some(out.trim_end().to_string())
    }

    fn token_text(token: &SqlToken) -> String {
        match token {
            SqlToken::Word(word) => {
                let upper = word.to_uppercase();
                if sql_text::is_oracle_sql_keyword(upper.as_str()) {
                    upper
                } else {
                    word.clone()
                }
            }
            SqlToken::String(literal) => literal.clone(),
            SqlToken::Comment(comment) => comment.clone(),
            SqlToken::Symbol(sym) => sym.clone(),
        }
    }

    fn token_text_for_create_table(
        token: &SqlToken,
        mysql_compatible: bool,
        previous_non_comment_token: Option<&SqlToken>,
        next_non_comment_token: Option<&SqlToken>,
        next_words: &[Option<&str>; 3],
    ) -> String {
        if mysql_compatible {
            match token {
                SqlToken::Word(word) => {
                    let upper = word.to_ascii_uppercase();
                    if Self::mysql_keyword_should_render_as_identifier_from_context(
                        word,
                        upper.as_str(),
                        None,
                        false,
                        previous_non_comment_token,
                        next_non_comment_token,
                        next_words,
                    ) {
                        word.clone()
                    } else if sql_text::is_mysql_sql_keyword(upper.as_str()) {
                        upper
                    } else {
                        word.clone()
                    }
                }
                SqlToken::String(literal) => literal.clone(),
                SqlToken::Comment(comment) => comment.clone(),
                SqlToken::Symbol(sym) => sym.clone(),
            }
        } else {
            Self::token_text(token)
        }
    }

    fn token_is_word_like(token: &SqlToken) -> bool {
        matches!(token, SqlToken::Word(_))
    }

    fn is_plsql_attribute_prefix(sym: &str, next_token: Option<&SqlToken>) -> bool {
        sym == "%" && next_token.map(Self::token_is_word_like).unwrap_or(false)
    }

    fn join_tokens_compact_for_create_table(tokens: &[SqlToken], mysql_compatible: bool) -> String {
        let mut out = String::new();
        let mut needs_space = false;
        let mut recent_statement_word_indices: VecDeque<usize> = VecDeque::with_capacity(8);
        let mut token_context_cursor = FormatterTokenContextCursor::new(tokens);

        for (idx, token) in tokens.iter().enumerate() {
            let token_context = token_context_cursor.advance_to(idx);
            let previous_non_comment_token = token_context.prev_non_comment_token(tokens);
            let second_previous_non_comment_token =
                token_context.second_prev_non_comment_token(tokens);
            let next_non_comment_token = token_context.next_non_comment_token(tokens);
            let next_words = token_context.next_words(tokens);
            let prev_word_upper_owned = recent_statement_word_indices
                .back()
                .and_then(|word_idx| tokens.get(*word_idx))
                .and_then(|recent| match recent {
                    SqlToken::Word(word) => Some(word.to_ascii_uppercase()),
                    _ => None,
                });
            let text = Self::token_text_for_create_table(
                token,
                mysql_compatible,
                previous_non_comment_token,
                next_non_comment_token,
                &next_words,
            );
            match token {
                SqlToken::Symbol(sym) if sym == "(" => {
                    let keeps_mysql_word_paren_tight = mysql_compatible
                        && Self::mysql_word_paren_should_stay_tight_from_context(
                            tokens,
                            false,
                            prev_word_upper_owned.as_deref(),
                            previous_non_comment_token,
                            second_previous_non_comment_token,
                            &recent_statement_word_indices,
                            false,
                        );
                    if needs_space && !keeps_mysql_word_paren_tight {
                        out.push(' ');
                    }
                    out.push_str(&text);
                    needs_space = false;
                }
                SqlToken::Symbol(sym) if sym == ")" => {
                    out.push_str(&text);
                    needs_space = true;
                }
                SqlToken::Symbol(sym) if sym == "," => {
                    out.push_str(&text);
                    out.push(' ');
                    needs_space = false;
                }
                SqlToken::Symbol(sym)
                    if Self::is_plsql_attribute_prefix(sym, next_non_comment_token) =>
                {
                    out.push_str(&text);
                    needs_space = false;
                }
                _ => {
                    if needs_space {
                        out.push(' ');
                    }
                    out.push_str(&text);
                    needs_space = true;
                }
            }

            match token {
                SqlToken::Symbol(symbol) if symbol == ";" => {
                    recent_statement_word_indices.clear();
                }
                SqlToken::Word(_) => {
                    recent_statement_word_indices.push_back(idx);
                    if recent_statement_word_indices.len() > 8 {
                        let _ = recent_statement_word_indices.pop_front();
                    }
                }
                _ => {}
            }
        }
        out.trim().to_string()
    }

    fn join_tokens_spaced_for_create_table(
        tokens: &[SqlToken],
        indent_level: usize,
        mysql_compatible: bool,
    ) -> String {
        let mut out = String::new();
        let mut needs_space = false;
        let indent = " ".repeat(indent_level * 4);
        let mut at_line_start = true;
        let mut recent_statement_word_indices: VecDeque<usize> = VecDeque::with_capacity(8);
        let mut token_context_cursor = FormatterTokenContextCursor::new(tokens);

        for (idx, token) in tokens.iter().enumerate() {
            let token_context = token_context_cursor.advance_to(idx);
            let previous_non_comment_token = token_context.prev_non_comment_token(tokens);
            let second_previous_non_comment_token =
                token_context.second_prev_non_comment_token(tokens);
            let next_non_comment_token = token_context.next_non_comment_token(tokens);
            let next_words = token_context.next_words(tokens);
            let prev_word_upper_owned = recent_statement_word_indices
                .back()
                .and_then(|word_idx| tokens.get(*word_idx))
                .and_then(|recent| match recent {
                    SqlToken::Word(word) => Some(word.to_ascii_uppercase()),
                    _ => None,
                });
            let text = Self::token_text_for_create_table(
                token,
                mysql_compatible,
                previous_non_comment_token,
                next_non_comment_token,
                &next_words,
            );
            match token {
                SqlToken::Comment(comment) => {
                    if !at_line_start {
                        out.push(' ');
                    } else if !indent.is_empty() {
                        out.push_str(&indent);
                    }
                    out.push_str(comment);
                    if comment.ends_with('\n') {
                        at_line_start = true;
                        needs_space = false;
                    } else {
                        at_line_start = false;
                        needs_space = true;
                    }
                }
                SqlToken::Symbol(sym) if sym == "." => {
                    out.push('.');
                    needs_space = false;
                    at_line_start = false;
                }
                SqlToken::Symbol(sym) if sym == "(" => {
                    let keeps_mysql_word_paren_tight = mysql_compatible
                        && Self::mysql_word_paren_should_stay_tight_from_context(
                            tokens,
                            false,
                            prev_word_upper_owned.as_deref(),
                            previous_non_comment_token,
                            second_previous_non_comment_token,
                            &recent_statement_word_indices,
                            false,
                        );
                    if needs_space && !keeps_mysql_word_paren_tight {
                        out.push(' ');
                    }
                    out.push('(');
                    needs_space = false;
                    at_line_start = false;
                }
                SqlToken::Symbol(sym) if sym == ")" => {
                    out.push(')');
                    needs_space = true;
                    at_line_start = false;
                }
                SqlToken::Symbol(sym) if sym == "," => {
                    out.push(',');
                    out.push(' ');
                    needs_space = false;
                    at_line_start = false;
                }
                SqlToken::Symbol(sym)
                    if Self::is_plsql_attribute_prefix(sym, next_non_comment_token) =>
                {
                    out.push('%');
                    needs_space = false;
                    at_line_start = false;
                }
                SqlToken::Symbol(sym) => {
                    if needs_space {
                        out.push(' ');
                    }
                    out.push_str(sym);
                    needs_space = true;
                    at_line_start = false;
                }
                _ => {
                    if at_line_start && !indent.is_empty() {
                        out.push_str(&indent);
                    }
                    if needs_space {
                        out.push(' ');
                    }
                    out.push_str(&text);
                    needs_space = true;
                    at_line_start = false;
                }
            }

            match token {
                SqlToken::Symbol(symbol) if symbol == ";" => {
                    recent_statement_word_indices.clear();
                }
                SqlToken::Word(_) => {
                    recent_statement_word_indices.push_back(idx);
                    if recent_statement_word_indices.len() > 8 {
                        let _ = recent_statement_word_indices.pop_front();
                    }
                }
                _ => {}
            }
        }

        out.trim().to_string()
    }

    fn format_create_suffix_for_create_table(
        tokens: &[SqlToken],
        mysql_compatible: bool,
    ) -> String {
        if tokens.is_empty() {
            return String::new();
        }

        let break_keywords = FORMAT_CREATE_SUFFIX_BREAK_KEYWORDS;

        let mut parts: Vec<Vec<SqlToken>> = Vec::new();

        for part in split_top_level_keyword_groups(tokens, break_keywords) {
            parts.push(part.into_iter().cloned().collect());
        }

        let mut out = String::new();
        for (idx, part) in parts.iter().enumerate() {
            if idx > 0 {
                out.push('\n');
            }
            out.push_str(&Self::join_tokens_spaced_for_create_table(
                part,
                0,
                mysql_compatible,
            ));
        }
        out.trim().to_string()
    }

    /// 토크나이저는 공통 로직(`query_text`)로 위임합니다.
    pub(crate) fn tokenize_sql(sql: &str) -> Vec<SqlToken> {
        Self::tokenize_sql_with_mysql_compat(sql, sql_text::mysql_compatibility_for_sql(sql, None))
    }

    fn tokenize_sql_with_mysql_compat(sql: &str, mysql_compatible: bool) -> Vec<SqlToken> {
        super::query_text::tokenize_sql_with_mysql_compat(sql, mysql_compatible)
    }

    pub(super) fn escape_sql_literal(value: &str) -> String {
        value.replace('\'', "''")
    }
}

#[cfg(test)]
mod inline_statement_separator_tests {
    use super::SqlEditorWidget;

    #[test]
    fn inline_statement_separator_candidate_guard_skips_terminal_semicolons() {
        assert!(!SqlEditorWidget::has_inline_statement_separator_candidate(
            "SELECT 1;"
        ));
        assert!(!SqlEditorWidget::has_inline_statement_separator_candidate(
            "SELECT 1;   \n"
        ));
        assert!(!SqlEditorWidget::has_inline_statement_separator_candidate(
            "SELECT 1;\nSELECT 2;"
        ));
    }

    #[test]
    fn inline_statement_separator_candidate_guard_detects_same_line_followups() {
        assert!(SqlEditorWidget::has_inline_statement_separator_candidate(
            "SELECT 1; SELECT 2;"
        ));
        assert!(SqlEditorWidget::has_inline_statement_separator_candidate(
            "SELECT 1; -- note"
        ));
    }

    #[test]
    fn split_inline_statement_separators_keeps_same_line_comments_attached() {
        assert_eq!(
            SqlEditorWidget::split_inline_statement_separators_for_test("SELECT 1; -- note"),
            "SELECT 1; -- note"
        );
    }

    #[test]
    fn split_inline_statement_separators_breaks_same_line_statements() {
        assert_eq!(
            SqlEditorWidget::split_inline_statement_separators_for_test("SELECT 1; SELECT 2;"),
            "SELECT 1;\n SELECT 2;"
        );
    }
}

#[cfg(test)]
mod formatter_scope_state_tests {
    use super::{
        BlockKind, ConstructFlagKind, ConstructValueKind, FormatFrameStack, FormatScope,
        ParenFormatFrame, ParenFormatFrameKind, ParenSemanticFlags, ScopedIndentKind,
        SqlEditorWidget,
    };

    #[test]
    fn format_scope_same_depth_different_frame_ids_do_not_contain_each_other() {
        let left = FormatScope::with_frame_ids(1, 1, Some(10), Some(20));
        let right = FormatScope::with_frame_ids(1, 1, Some(11), Some(20));

        assert!(!left.contains(right));
        assert!(!right.contains(left));
    }

    #[test]
    fn format_frame_stack_scoped_indent_same_scope_activation_does_not_double_apply() {
        let mut stack = FormatFrameStack::default();
        let scope = FormatScope::new(1, 2);
        let mut indent_level = 0usize;

        stack.push_scoped_indent(ScopedIndentKind::CursorSql, scope, 1, &mut indent_level);
        stack.push_scoped_indent(ScopedIndentKind::CursorSql, scope, 1, &mut indent_level);
        assert_eq!(
            indent_level, 1,
            "same-scope re-activation must not double-apply the delta"
        );

        let _ = stack.deactivate_scoped_indent(ScopedIndentKind::CursorSql, &mut indent_level);
        assert_eq!(indent_level, 0);
        assert!(!stack.scoped_indent_is_active(ScopedIndentKind::CursorSql));
    }

    #[test]
    fn format_frame_stack_scoped_indent_same_scope_activation_ignores_intervening_auxiliary_frame()
    {
        let mut stack = FormatFrameStack::default();
        let scope = FormatScope::new(1, 2);
        let mut indent_level = 0usize;

        stack.push_scoped_indent(ScopedIndentKind::CursorSql, scope, 1, &mut indent_level);
        stack.push_between_pending(scope);
        stack.push_scoped_indent(ScopedIndentKind::CursorSql, scope, 1, &mut indent_level);

        assert_eq!(
            indent_level, 1,
            "same-scope re-activation must stay idempotent even when an auxiliary frame sits above the existing indent"
        );
        assert!(stack.between_pending_matches(scope));

        let _ = stack.deactivate_scoped_indent(ScopedIndentKind::CursorSql, &mut indent_level);
        assert_eq!(indent_level, 0);
        assert!(!stack.scoped_indent_is_active(ScopedIndentKind::CursorSql));
        assert!(stack.between_pending_matches(scope));
    }

    #[test]
    fn format_frame_stack_scoped_indent_sync_scope_expires_same_depth_sibling_frame() {
        let mut stack = FormatFrameStack::default();
        let mut indent_level = 0usize;

        stack.push_paren(
            ParenFormatFrame::new(ParenFormatFrameKind::Compact, 0, false, 0, None),
            ParenSemanticFlags::default(),
            false,
            None,
            None,
            None,
            &mut indent_level,
        );
        let original_scope = stack.current_scope();
        stack.push_scoped_indent(
            ScopedIndentKind::MergeWhenBranch,
            original_scope,
            2,
            &mut indent_level,
        );
        let _ = stack.pop_paren(&mut indent_level);
        stack.push_paren(
            ParenFormatFrame::new(ParenFormatFrameKind::Compact, 0, false, 0, None),
            ParenSemanticFlags::default(),
            false,
            None,
            None,
            None,
            &mut indent_level,
        );

        stack.sync_to_scope(stack.current_scope(), &mut indent_level);
        assert_eq!(indent_level, 0);
        assert!(!stack.scoped_indent_is_active(ScopedIndentKind::MergeWhenBranch));
    }

    #[test]
    fn format_frame_stack_clear_scoped_indent_unwinds_all_matching_frames() {
        let mut stack = FormatFrameStack::default();
        let mut indent_level = 0usize;

        stack.push_scoped_indent(
            ScopedIndentKind::CursorSql,
            FormatScope::new(0, 1),
            1,
            &mut indent_level,
        );
        stack.push_scoped_indent(
            ScopedIndentKind::CursorSql,
            FormatScope::new(1, 1),
            2,
            &mut indent_level,
        );
        stack.push_scoped_indent(
            ScopedIndentKind::ForallBody,
            FormatScope::new(2, 1),
            3,
            &mut indent_level,
        );
        assert_eq!(indent_level, 6);

        stack.clear_scoped_indent(ScopedIndentKind::CursorSql, &mut indent_level);
        assert_eq!(indent_level, 3);
        assert!(!stack.scoped_indent_is_active(ScopedIndentKind::CursorSql));
        assert!(stack.scoped_indent_is_active(ScopedIndentKind::ForallBody));
    }

    #[test]
    fn format_frame_stack_construct_flag_same_scope_activation_is_idempotent() {
        let mut stack = FormatFrameStack::default();
        let scope = FormatScope::new(0, 0);

        stack.activate_construct_flag(ConstructFlagKind::CreatePending, scope);
        stack.activate_construct_flag(ConstructFlagKind::CreatePending, scope);
        let _ = stack.deactivate_construct_flag(ConstructFlagKind::CreatePending);

        assert!(
            !stack.construct_flag_is_active(ConstructFlagKind::CreatePending),
            "same-scope activation should not require multiple deactivations"
        );
    }

    #[test]
    fn format_frame_stack_construct_value_restores_outer_scope_after_inner_scope_exits() {
        let mut stack = FormatFrameStack::default();
        let mut indent_level = 0usize;
        let outer_scope = FormatScope::new(0, 0);
        let inner_scope = FormatScope::new(1, 0);

        stack.replace_construct_value(
            ConstructValueKind::CreateObject,
            outer_scope,
            "OUTER".to_string(),
        );
        stack.replace_construct_value(
            ConstructValueKind::CreateObject,
            inner_scope,
            "INNER".to_string(),
        );
        assert_eq!(
            stack.construct_value_as_deref(ConstructValueKind::CreateObject),
            Some("INNER")
        );

        stack.sync_to_scope(outer_scope, &mut indent_level);

        assert_eq!(
            stack.construct_value_as_deref(ConstructValueKind::CreateObject),
            Some("OUTER"),
            "construct values should restore the outer frame after the inner scope closes"
        );
    }

    #[test]
    fn format_frame_stack_construct_flag_sync_scope_expires_same_depth_sibling_frame() {
        let mut stack = FormatFrameStack::default();
        let mut indent_level = 0usize;

        stack.push_paren(
            ParenFormatFrame::new(ParenFormatFrameKind::Compact, 0, false, 0, None),
            ParenSemanticFlags::default(),
            false,
            None,
            None,
            None,
            &mut indent_level,
        );
        let original_scope = stack.current_scope();
        stack.activate_construct_flag(ConstructFlagKind::ModelActive, original_scope);
        let _ = stack.pop_paren(&mut indent_level);
        stack.push_paren(
            ParenFormatFrame::new(ParenFormatFrameKind::Compact, 0, false, 0, None),
            ParenSemanticFlags::default(),
            false,
            None,
            None,
            None,
            &mut indent_level,
        );

        stack.sync_to_scope(stack.current_scope(), &mut indent_level);
        assert!(!stack.construct_flag_is_active(ConstructFlagKind::ModelActive));
    }

    #[test]
    fn format_frame_stack_deactivate_construct_flag_at_or_above_paren_depth_keeps_outer_flag_and_indent(
    ) {
        let mut stack = FormatFrameStack::default();
        let mut indent_level = 0usize;

        stack.push_block(BlockKind::Begin, 0, 2, &mut indent_level);
        let outer_scope = stack.current_scope();
        stack.activate_construct_flag(ConstructFlagKind::CreatePending, outer_scope);
        stack.push_paren(
            ParenFormatFrame::new(ParenFormatFrameKind::Compact, 0, false, 0, None),
            ParenSemanticFlags::default(),
            false,
            None,
            None,
            None,
            &mut indent_level,
        );
        let inner_scope = stack.current_scope();
        stack.activate_construct_flag(ConstructFlagKind::CreatePending, inner_scope);
        stack.push_scoped_indent(
            ScopedIndentKind::CursorSql,
            inner_scope,
            1,
            &mut indent_level,
        );

        stack
            .deactivate_construct_flag_at_or_above_paren_depth(ConstructFlagKind::CreatePending, 1);

        assert_eq!(indent_level, 3);
        assert_eq!(
            stack.last_construct_flag_scope(ConstructFlagKind::CreatePending),
            Some(outer_scope)
        );
        assert!(stack.scoped_indent_is_active(ScopedIndentKind::CursorSql));
        assert_eq!(stack.paren_depth(), 1);
        assert_eq!(stack.block_depth(), 1);
    }

    #[test]
    fn format_frame_stack_clear_construct_value_keeps_surviving_indent_frames() {
        let mut stack = FormatFrameStack::default();
        let mut indent_level = 0usize;

        stack.push_block(BlockKind::Begin, 0, 2, &mut indent_level);
        let outer_scope = stack.current_scope();
        stack.replace_construct_value(
            ConstructValueKind::CreateObject,
            outer_scope,
            "OUTER".to_string(),
        );
        stack.push_scoped_indent(
            ScopedIndentKind::CursorSql,
            outer_scope,
            1,
            &mut indent_level,
        );
        stack.push_paren(
            ParenFormatFrame::new(ParenFormatFrameKind::Compact, 0, false, 0, None),
            ParenSemanticFlags::default(),
            false,
            None,
            None,
            None,
            &mut indent_level,
        );
        stack.replace_construct_value(
            ConstructValueKind::CreateObject,
            stack.current_scope(),
            "INNER".to_string(),
        );

        stack.clear_construct_value(ConstructValueKind::CreateObject);

        assert_eq!(indent_level, 3);
        assert!(!stack.construct_value_is_some(ConstructValueKind::CreateObject));
        assert!(stack.scoped_indent_is_active(ScopedIndentKind::CursorSql));
        assert!(stack.last_block_kind_is("BEGIN"));
        assert_eq!(stack.paren_depth(), 1);
        assert_eq!(stack.block_depth(), 1);
    }

    #[test]
    fn format_sql_search_cycle_scope_does_not_leak_into_following_update_set_clause() {
        let source = "WITH r (id, parent_id) AS (SELECT 1, 0 FROM dual UNION ALL SELECT id + 1, parent_id FROM r WHERE id < 2) SEARCH DEPTH FIRST BY id SET ord CYCLE id SET is_cycle TO 'Y' DEFAULT 'N' UPDATE demo SET status = 'DONE' WHERE id IN (SELECT id FROM r);";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        let lines: Vec<&str> = formatted.lines().collect();
        let leading_spaces = |line: &str| line.len().saturating_sub(line.trim_start().len());

        let update_idx = lines
            .iter()
            .position(|line| line.trim_start().starts_with("UPDATE demo"))
            .unwrap_or_else(|| panic!("UPDATE line, got:\n{}", formatted));
        let set_idx = lines
            .iter()
            .position(|line| line.trim_start().starts_with("SET status = 'DONE'"))
            .unwrap_or_else(|| panic!("SET line, got:\n{}", formatted));
        let where_idx = lines
            .iter()
            .position(|line| line.trim_start().starts_with("WHERE id IN ("))
            .unwrap_or_else(|| panic!("WHERE line, got:\n{}", formatted));

        assert_eq!(
            leading_spaces(lines[update_idx]),
            0,
            "UPDATE after SEARCH/CYCLE should return to top-level depth, got:\n{}",
            formatted
        );
        assert_eq!(
            leading_spaces(lines[set_idx]),
            0,
            "SET after SEARCH/CYCLE should open a real UPDATE SET clause instead of inheriting recursive CTE SET suppression, got:\n{}",
            formatted
        );
        assert_eq!(
            leading_spaces(lines[where_idx]),
            0,
            "WHERE after UPDATE SET should stay aligned with the UPDATE clause, got:\n{}",
            formatted
        );
        assert!(
            formatted.contains("SEARCH DEPTH FIRST BY id SET ord"),
            "SEARCH clause should keep its inline SET payload, got:\n{}",
            formatted
        );
        assert!(
            formatted.contains("CYCLE id SET is_cycle TO 'Y' DEFAULT 'N'"),
            "CYCLE clause should keep its inline SET payload, got:\n{}",
            formatted
        );
    }

    #[test]
    fn normalized_indent_tab_width_never_returns_zero() {
        assert!(
            SqlEditorWidget::normalized_indent_tab_width() > 0,
            "indent tab width must never be zero to avoid division/modulo panics"
        );
    }
}

#[cfg(test)]
mod formatter_regression_tests {
    use super::SqlEditorWidget;
    use crate::db::{FormatItem, QueryExecutor, ScriptItem, SessionState};
    use crate::ui::sql_editor::execution::PROGRESS_ROWS_INITIAL_BATCH;
    use crate::ui::sql_editor::QueryProgress;
    use std::fs;
    use std::path::PathBuf;
    use std::sync::{mpsc, Arc, Mutex};
    use std::time::Duration;

    fn load_formatter_test_file(name: &str) -> String {
        let mut path = PathBuf::from(env!("CARGO_MANIFEST_DIR"));
        path.push("test");
        path.push(name);
        fs::read_to_string(path).unwrap_or_default()
    }

    fn count_statement_items(items: &[ScriptItem]) -> usize {
        items
            .iter()
            .filter(|item| matches!(item, ScriptItem::Statement(_)))
            .count()
    }

    fn count_tool_command_items(items: &[ScriptItem]) -> usize {
        items
            .iter()
            .filter(|item| matches!(item, ScriptItem::ToolCommand(_)))
            .count()
    }

    #[test]
    fn does_not_force_select_line_break_after_malformed_statement() {
        let sql = "select fn(a, b;\nselect x, y from dual;";
        let formatted = SqlEditorWidget::format_sql_basic(sql);

        assert!(
            formatted.contains("SELECT x,\n    y\nFROM DUAL;"),
            "Subsequent valid statement should return to normal comma layout, got: {}",
            formatted
        );
        assert!(
            !formatted.contains("SELECT\n    x,\n    y\nFROM DUAL;"),
            "Malformed prior statement must not force legacy recovery layout on next statement, got: {}",
            formatted
        );
    }

    #[test]
    fn malformed_statement_resets_paren_tracking_before_following_statement() {
        let sql = "SELECT fn(a, b;\nSELECT c, d, e FROM dual;";
        let formatted = SqlEditorWidget::format_sql_basic(sql);

        assert!(
            formatted.contains("SELECT c,\n    d,\n    e\nFROM DUAL;"),
            "Following statement should format with normal SELECT-list wrapping, got:\n{}",
            formatted
        );
        assert!(
            !formatted.contains("SELECT\n    c,\n    d,\n    e\nFROM DUAL;"),
            "Formatter should not keep stale malformed-state recovery layout, got:\n{}",
            formatted
        );
    }

    #[test]
    fn comments_do_not_change_paren_tracking_state() {
        let sql = "select a, /* comment with (, ), and , */ b from dual;";
        let formatted = SqlEditorWidget::format_sql_basic(sql);

        assert!(formatted.contains("/* comment with (, ), and , */"));
        assert!(
            formatted
                .contains("SELECT\n    a,\n    /* comment with (, ), and , */\n    b\nFROM DUAL;")
                || formatted
                    .contains("SELECT a,\n    /* comment with (, ), and , */\n    b\nFROM DUAL;"),
            "Comment-preserving select formatting should remain stable, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_sql_basic_preserves_prompt_banner_lines_verbatim() {
        let sql =
            "PROMPT =======================================================================\n\
PROMPT [END] If you saw outputs + cursor print + summary selects, parsing/execution is OK\n\
PROMPT =======================================================================";

        let formatted = SqlEditorWidget::format_sql_basic(sql);

        assert_eq!(formatted, sql);
    }

    #[test]
    fn format_sql_basic_preserves_prompt_lines_with_original_case_and_indentation() {
        let sql = "  prompt first line\n\
prompt second line\n\
    PROMPT third line";

        let formatted = SqlEditorWidget::format_sql_basic(sql);

        assert_eq!(formatted, sql);

        let formatted_again = SqlEditorWidget::format_sql_basic(&formatted);
        assert_eq!(formatted_again, sql);
    }

    #[test]
    fn keeps_multiline_string_continuation_lines_without_depth_reindent() {
        let sql = "BEGIN
DBMS_OUTPUT.PUT_LINE('first line
second line
third line');
END;";
        let formatted = SqlEditorWidget::format_sql_basic(sql);

        assert!(formatted.contains("DBMS_OUTPUT.PUT_LINE ('first line\nsecond line\nthird line');"));
    }

    #[test]
    fn keeps_ampersand_substitution_variables_together() {
        let sql = "SELECT &&pp FROM dual";
        let formatted = SqlEditorWidget::format_sql_basic(sql);
        assert!(
            formatted.contains("&&pp"),
            "&&pp should stay together, got: {}",
            formatted
        );

        let sql2 = "SELECT &var1 FROM dual";
        let formatted2 = SqlEditorWidget::format_sql_basic(sql2);
        assert!(
            formatted2.contains("&var1"),
            "&var1 should stay together, got: {}",
            formatted2
        );
    }

    #[test]
    fn keeps_merge_into_together() {
        let sql = "MERGE INTO target_table t USING source_table s ON (t.id = s.id) WHEN MATCHED THEN UPDATE SET t.name = s.name";
        let formatted = SqlEditorWidget::format_sql_basic(sql);
        assert!(
            formatted.contains("MERGE INTO target_table"),
            "MERGE INTO should stay on the same line, got: {}",
            formatted
        );
    }

    #[test]
    fn keeps_start_with_together() {
        let sql = "SELECT employee_id, manager_id FROM employees START WITH manager_id IS NULL CONNECT BY PRIOR employee_id = manager_id";
        let formatted = SqlEditorWidget::format_sql_basic(sql);
        assert!(
            formatted.contains("START WITH"),
            "START WITH should stay on the same line, got: {}",
            formatted
        );
    }

    #[test]
    fn formats_where_in_subquery_with_deep_indent_and_alias() {
        let source = "select a.topic, a.TOPIC from help a where a.SEQ in (select seq from help) b";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        let preserved = SqlEditorWidget::preserve_selected_text_terminator(source, formatted);

        assert_eq!(
            preserved.trim_end(),
            "SELECT a.topic,\n    a.TOPIC\nFROM help a\nWHERE a.SEQ IN (\n        SELECT seq\n        FROM help\n    ) b"
        );
    }

    #[test]
    fn deeply_nested_subqueries_keep_progressive_depth_indentation() {
        let source = r#"BEGIN
  SELECT col1
  INTO v_col
  FROM t1
  WHERE EXISTS (
    SELECT 1
    FROM t2
    WHERE t2.id IN (
      SELECT t3.id
      FROM t3
      WHERE t3.flag = 'Y'
    )
  );
END;"#;

        let formatted = SqlEditorWidget::format_sql_basic(source);

        assert!(
            formatted.contains("WHERE EXISTS (\n            SELECT 1"),
            "first nested subquery should stay indented under EXISTS, got:\n{}",
            formatted
        );
        assert!(
            formatted.contains("WHERE t2.id IN (\n                    SELECT t3.id"),
            "second nested subquery should stay indented under IN, got:\n{}",
            formatted
        );
        assert!(
            formatted.contains("WHERE t3.flag = 'Y'\n                )"),
            "closing parenthesis should dedent one level from deepest query body, got:\n{}",
            formatted
        );
    }

    #[test]
    fn keeps_repeat_block_as_single_indented_block() {
        let sql = r#"BEGIN
  REPEAT
    DBMS_OUTPUT.PUT_LINE('start');
    i := i + 1;
  UNTIL i >= 3
  END REPEAT;
END;"#;
        let formatted = SqlEditorWidget::format_sql_basic(sql);

        assert!(
            formatted.contains("END REPEAT;"),
            "REPEAT block terminator should stay on a single line, got: {}",
            formatted
        );

        let repeat_end_line = formatted
            .lines()
            .find(|line| line.trim().starts_with("END REPEAT;"))
            .expect("formatted output should contain END REPEAT line");
        let end_line = formatted.lines().find(|line| line.trim() == "END");

        assert!(
            end_line.unwrap_or("    ").starts_with("    "),
            "END should be indented"
        );
        assert!(
            formatted.contains("DBMS_OUTPUT.PUT_LINE"),
            "REPEAT body should remain present, got: {}",
            formatted
        );
        assert!(
            repeat_end_line.starts_with("    "),
            "END REPEAT should match block indent"
        );

        let formatted_again = SqlEditorWidget::format_sql_basic(&formatted);
        assert_eq!(
            formatted, formatted_again,
            "Formatting should be idempotent for REPEAT blocks"
        );
    }

    #[test]
    fn tab_off_keeps_tab_character_in_script_output() {
        let line = "A\tB";
        let rendered = SqlEditorWidget::format_script_output_line(line, false, false);
        assert_eq!(rendered, "A\tB");
    }

    #[test]
    fn tab_on_expands_tab_character_in_script_output() {
        let line = "A\tB";
        let rendered = SqlEditorWidget::format_script_output_line(line, false, true);
        assert_eq!(rendered, "A       B");
    }

    #[test]
    fn nested_case_expression_in_plsql_aligns_else_correctly() {
        let sql = r#"CREATE OR REPLACE PACKAGE BODY oqt_mega_pkg AS
FUNCTION f_deep(p_grp IN NUMBER, p_n IN NUMBER, p_txt IN VARCHAR2) RETURN NUMBER IS
  v NUMBER := 0;
BEGIN
  v :=
    CASE
      WHEN p_grp < 0 THEN -1000
      WHEN p_grp = 0 THEN
        CASE
          WHEN p_n > 10 THEN 100
          ELSE 10
        END
      ELSE
        CASE
          WHEN INSTR(NVL(p_txt,'x'), 'END;') > 0 THEN 777
          ELSE LENGTH(NVL(p_txt,'')) + p_n
        END
    END;
  RETURN v;
END;
END oqt_mega_pkg;"#;
        let formatted = SqlEditorWidget::format_sql_basic(sql);
        let lines: Vec<&str> = formatted.lines().collect();
        let indent = |line: &str| line.len().saturating_sub(line.trim_start().len());

        let assignment_idx = lines
            .iter()
            .position(|line| line.trim_start().starts_with("v :="))
            .expect("assignment line");
        let case_idx = lines
            .iter()
            .enumerate()
            .skip(assignment_idx + 1)
            .find(|(_, line)| line.trim_start() == "CASE")
            .map(|(idx, _)| idx)
            .expect("CASE line");
        let outer_when_idx = lines
            .iter()
            .enumerate()
            .skip(case_idx + 1)
            .find(|(_, line)| line.trim_start().starts_with("WHEN p_grp < 0 THEN"))
            .map(|(idx, _)| idx)
            .expect("outer WHEN line");
        let inner_when_idx = lines
            .iter()
            .enumerate()
            .skip(outer_when_idx + 1)
            .find(|(_, line)| line.trim_start().starts_with("WHEN p_n > 10 THEN"))
            .map(|(idx, _)| idx)
            .expect("inner WHEN line");
        let inner_end_idx = lines
            .iter()
            .enumerate()
            .skip(inner_when_idx + 1)
            .find(|(_, line)| line.trim_start() == "END")
            .map(|(idx, _)| idx)
            .expect("inner END line");
        let outer_else_idx = lines
            .iter()
            .enumerate()
            .skip(inner_end_idx + 1)
            .find(|(_, line)| line.trim_start() == "ELSE")
            .map(|(idx, _)| idx)
            .expect("outer ELSE line");
        let outer_else_case_idx = lines
            .iter()
            .enumerate()
            .skip(outer_else_idx + 1)
            .find(|(_, line)| line.trim_start() == "CASE")
            .map(|(idx, _)| idx)
            .expect("outer ELSE CASE line");
        let outer_end_idx = lines
            .iter()
            .enumerate()
            .skip(outer_else_case_idx + 1)
            .find(|(_, line)| line.trim_start() == "END;")
            .map(|(idx, _)| idx)
            .expect("outer END line");

        assert_eq!(
            indent(lines[case_idx]),
            indent(lines[assignment_idx]).saturating_add(4),
            "CASE after := should indent one level deeper than the assignment owner, got:\n{}",
            formatted
        );
        assert_eq!(
            indent(lines[outer_when_idx]),
            indent(lines[case_idx]).saturating_add(4),
            "outer WHEN should indent one level deeper than CASE, got:\n{}",
            formatted
        );
        assert_eq!(
            indent(lines[outer_else_idx]),
            indent(lines[outer_when_idx]),
            "outer ELSE should align with outer WHEN after nested CASE closes, got:\n{}",
            formatted
        );
        assert_eq!(
            indent(lines[outer_else_case_idx]),
            indent(lines[outer_else_idx]).saturating_add(4),
            "CASE inside outer ELSE should indent one level deeper than ELSE, got:\n{}",
            formatted
        );
        assert_eq!(
            indent(lines[outer_end_idx]),
            indent(lines[case_idx]),
            "outer CASE END should align with the CASE header, got:\n{}",
            formatted
        );

        // Idempotent
        let formatted_again = SqlEditorWidget::format_sql_basic(&formatted);
        assert_eq!(
            formatted, formatted_again,
            "Formatting should be idempotent for nested CASE expressions"
        );
    }

    #[test]
    fn package_body_named_end_with_if_prefix_is_not_treated_as_end_if_suffix() {
        let sql = r#"CREATE OR REPLACE PACKAGE BODY if_owner AS
FUNCTION run_check RETURN NUMBER IS
BEGIN
  IF 1 = 1 THEN
    RETURN 1;
  END IF;
END run_check;
BEGIN
  NULL;
END if_owner;"#;
        let formatted = SqlEditorWidget::format_sql_basic(sql);

        let end_if_owner_line = formatted
            .lines()
            .find(|line| line.trim().eq_ignore_ascii_case("END if_owner;"));
        assert!(
            end_if_owner_line.is_some_and(|line| !line.starts_with(' ')),
            "Package named END label should close package body at top-level depth, got:
{}",
            formatted
        );

        let end_if_line = formatted
            .lines()
            .find(|line| line.trim().eq_ignore_ascii_case("END IF;"));
        assert!(
            end_if_line.is_some_and(|line| line.starts_with("        ")),
            "Nested END IF should remain more indented than END package label, got:
{}",
            formatted
        );
    }

    #[test]
    fn package_body_final_end_label_aligns_to_top_level() {
        let sql = r#"CREATE OR REPLACE PACKAGE BODY fmt_pkg_extreme AS
PROCEDURE run_extreme IS
BEGIN
  NULL;
END run_extreme;

BEGIN
  NULL;
END fmt_pkg_extreme;"#;

        let formatted = SqlEditorWidget::format_sql_basic(sql);
        let end_pkg_line = formatted
            .lines()
            .find(|line| line.trim().eq_ignore_ascii_case("END fmt_pkg_extreme;"));

        assert!(
            end_pkg_line.is_some_and(|line| !line.starts_with(' ')),
            "Final package END label should align with CREATE at top-level, got:\n{}",
            formatted
        );
    }

    #[test]
    fn package_body_initializer_begin_stays_at_top_level_after_case_declaration() {
        let sql = r#"CREATE OR REPLACE PACKAGE BODY fmt_pkg_extreme AS
g_last_mode VARCHAR2 (30) := 'BOOT';
FUNCTION calc_mode RETURN VARCHAR2 IS
BEGIN
  RETURN
  CASE
    WHEN 1 = 1 THEN
      'WEEKDAY_BOOT'
    ELSE
      'WEEKEND_BOOT'
  END;
END calc_mode;

BEGIN
    g_last_mode :=
    CASE
        WHEN TO_CHAR (SYSDATE, 'DY', 'NLS_DATE_LANGUAGE=ENGLISH') IN ('SAT', 'SUN') THEN
            'WEEKEND_BOOT'
        ELSE
            'WEEKDAY_BOOT'
    END;
    AUDIT ('INIT', 'package initialized. mode=' || g_last_mode);
END fmt_pkg_extreme;"#;

        let formatted = SqlEditorWidget::format_sql_basic(sql);
        let lines: Vec<&str> = formatted.lines().collect();
        let indent = |line: &str| line.len().saturating_sub(line.trim_start().len());
        let assignment_idx = lines
            .iter()
            .position(|line| line.trim_start().starts_with("g_last_mode :="))
            .expect("assignment line");
        let case_idx = lines
            .iter()
            .enumerate()
            .skip(assignment_idx + 1)
            .find(|(_, line)| line.trim_start() == "CASE")
            .map(|(idx, _)| idx)
            .expect("CASE line");

        assert!(
            formatted.contains("END calc_mode;\nBEGIN\n    g_last_mode :="),
            "package body initializer BEGIN should not remain indented under declarations, got:\n{}",
            formatted
        );
        assert_eq!(
            indent(lines[case_idx]),
            indent(lines[assignment_idx]).saturating_add(4),
            "initializer CASE should indent one level deeper than the := owner, got:\n{}",
            formatted
        );
        assert!(
            !formatted.contains("END calc_mode;\n\n    BEGIN\n        g_last_mode :="),
            "initializer BEGIN/body should not be shifted one extra level, got:\n{}",
            formatted
        );
    }

    #[test]
    fn package_body_header_detection_ignores_literals_and_comments() {
        let package_header_sql = r#"/* lead */ CREATE OR REPLACE PACKAGE BODY pkg_demo AS
BEGIN
  NULL;
END pkg_demo;"#;
        let package_tokens = SqlEditorWidget::tokenize_sql(package_header_sql);
        assert!(
            SqlEditorWidget::statement_starts_with_package_body_header(&package_tokens),
            "real package body header should be detected from meaningful tokens"
        );

        let literal_sql = "SELECT 'CREATE OR REPLACE PACKAGE BODY pkg_demo' AS txt FROM dual;";
        let literal_tokens = SqlEditorWidget::tokenize_sql(literal_sql);
        assert!(
            !SqlEditorWidget::statement_starts_with_package_body_header(&literal_tokens),
            "package-body phrase inside a string literal must not be treated as a structural header"
        );

        let comment_sql = "-- CREATE OR REPLACE PACKAGE BODY pkg_demo\nSELECT 1 FROM dual;";
        let comment_tokens = SqlEditorWidget::tokenize_sql(comment_sql);
        assert!(
            !SqlEditorWidget::statement_starts_with_package_body_header(&comment_tokens),
            "package-body phrase inside comments must not be treated as a structural header"
        );
    }

    #[test]
    fn apply_keyword_detection_uses_meaningful_tokens_only() {
        let apply_sql = "SELECT * FROM src CROSS APPLY (SELECT 1 AS v FROM dual) d;";
        let apply_tokens = SqlEditorWidget::tokenize_sql(apply_sql);
        assert!(
            SqlEditorWidget::statement_contains_keyword(&apply_tokens, "APPLY"),
            "real APPLY keyword should be detected"
        );

        let literal_sql = "SELECT ' APPLY' AS hint_txt, AVG (sal) AS avg_sal FROM emp;";
        let literal_tokens = SqlEditorWidget::tokenize_sql(literal_sql);
        assert!(
            !SqlEditorWidget::statement_contains_keyword(&literal_tokens, "APPLY"),
            "APPLY inside a string literal must not be treated as a statement keyword"
        );

        let comment_sql = "SELECT AVG (sal) AS avg_sal FROM emp -- APPLY marker";
        let comment_tokens = SqlEditorWidget::tokenize_sql(comment_sql);
        assert!(
            !SqlEditorWidget::statement_contains_keyword(&comment_tokens, "APPLY"),
            "APPLY inside comments must not be treated as a statement keyword"
        );
    }

    #[test]
    fn aggregate_call_spacing_is_not_tightened_by_apply_text_inside_literal() {
        let sql = "SELECT ' APPLY' AS hint_txt, AVG (sal) AS avg_sal FROM emp;";
        let formatted = SqlEditorWidget::format_sql_basic(sql);

        assert!(
            formatted.contains("AVG (sal) AS avg_sal"),
            "literal text containing APPLY should not trigger APPLY-specific aggregate spacing, got:\n{}",
            formatted
        );
        assert!(
            !formatted.contains("AVG(sal) AS avg_sal"),
            "formatter should not remove the call gap due to non-structural APPLY text, got:\n{}",
            formatted
        );
    }

    #[test]
    fn package_procedure_if_then_nested_begin_aligns_under_then() {
        let sql = r#"CREATE PACKAGE a AS
    PROCEDURE b (c IN VARCHAR2) IS
    BEGIN
        IF (1 = 1) THEN
        BEGIN
                SELECT * FROM d;
            END;
        END IF;
    END b;
END a;"#;
        let formatted = SqlEditorWidget::format_sql_basic(sql);
        let lines: Vec<&str> = formatted.lines().collect();
        let indent = |line: &str| line.len().saturating_sub(line.trim_start().len());

        let if_idx = lines
            .iter()
            .position(|line| line.trim_start().starts_with("IF (1 = 1) THEN"))
            .unwrap_or(0);
        let begin_idx = lines
            .iter()
            .enumerate()
            .find(|(idx, line)| *idx > if_idx && line.trim_start() == "BEGIN")
            .map(|(idx, _)| idx)
            .unwrap_or(0);
        let select_idx = lines
            .iter()
            .position(|line| line.trim_start().starts_with("SELECT *"))
            .unwrap_or(0);
        let inner_end_idx = lines
            .iter()
            .enumerate()
            .find(|(idx, line)| *idx > select_idx && line.trim_start() == "END;")
            .map(|(idx, _)| idx)
            .unwrap_or(0);
        let end_if_idx = lines
            .iter()
            .position(|line| line.trim_start() == "END IF;")
            .unwrap_or(0);

        assert!(
            indent(lines[begin_idx]) > indent(lines[if_idx]),
            "BEGIN inside IF THEN should indent deeper than IF, got:\n{}",
            formatted
        );
        assert!(
            indent(lines[select_idx]) > indent(lines[begin_idx]),
            "SELECT inside nested BEGIN should indent deeper than BEGIN, got:\n{}",
            formatted
        );
        assert_eq!(
            indent(lines[inner_end_idx]),
            indent(lines[begin_idx]),
            "END; for nested BEGIN should align with BEGIN, got:\n{}",
            formatted
        );
        assert_eq!(
            indent(lines[end_if_idx]),
            indent(lines[if_idx]),
            "END IF should align with IF, got:\n{}",
            formatted
        );
    }

    #[test]
    fn package_body_procedure_if_then_nested_begin_uses_expected_alignment() {
        let input = r#"create package body a as
    procedure b (c in varchar2) as
    begin
        if (1 = 1) then
            begin
                select * from d;
            end;
        end if;
    end b;
end a;"#;
        let expected = r#"CREATE PACKAGE BODY a AS
    PROCEDURE b (c IN VARCHAR2) AS
    BEGIN
        IF (1 = 1) THEN
            BEGIN
                SELECT *
                FROM d;
            END;
        END IF;
    END b;
END a;"#;

        let formatted = SqlEditorWidget::format_sql_basic(input);
        assert_eq!(
            formatted.trim(),
            expected.trim(),
            "package body procedure nested BEGIN alignment regression, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_sql_basic_split_end_if_preserves_owner_depth_across_comment_gap() {
        let source = r#"BEGIN
    IF flag = 1 THEN
        NULL;
    END
    -- split end suffix
    IF;
END;"#;

        let formatted = SqlEditorWidget::format_sql_basic(source);
        let lines: Vec<&str> = formatted.lines().collect();
        let indent = |line: &str| line.len().saturating_sub(line.trim_start().len());

        let if_header_idx = lines
            .iter()
            .position(|line| line.trim_start() == "IF flag = 1 THEN")
            .expect("IF header line");
        let end_idx = lines
            .iter()
            .position(|line| line.trim_start() == "END")
            .expect("split END line");
        let comment_idx = lines
            .iter()
            .position(|line| line.trim_start() == "-- split end suffix")
            .expect("comment line");
        let if_suffix_idx = lines
            .iter()
            .position(|line| line.trim_start() == "IF;")
            .expect("split IF line");

        assert_eq!(
            indent(lines[end_idx]),
            indent(lines[if_header_idx]),
            "split END should align with the IF owner depth, got:\n{}",
            formatted
        );
        assert_eq!(
            indent(lines[if_suffix_idx]),
            indent(lines[end_idx]),
            "split IF qualifier should consume the pending END owner depth, got:\n{}",
            formatted
        );
        assert_eq!(
            indent(lines[comment_idx]),
            indent(lines[end_idx]),
            "comment gap inside split END IF should stay on the retained owner depth, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_sql_basic_split_end_if_preserves_owner_depth_with_inline_block_comment() {
        let source = r#"BEGIN
    IF flag = 1 THEN
        NULL;
    END /* split end suffix */
    IF;
END;"#;

        let formatted = SqlEditorWidget::format_sql_basic(source);
        let lines: Vec<&str> = formatted.lines().collect();
        let indent = |line: &str| line.len().saturating_sub(line.trim_start().len());

        let if_header_idx = lines
            .iter()
            .position(|line| line.trim_start() == "IF flag = 1 THEN")
            .expect("IF header line");
        let end_idx = lines
            .iter()
            .position(|line| line.trim_start() == "END /* split end suffix */")
            .expect("inline-comment END line");
        let if_suffix_idx = lines
            .iter()
            .position(|line| line.trim_start() == "IF;")
            .expect("split IF line");

        assert_eq!(
            indent(lines[end_idx]),
            indent(lines[if_header_idx]),
            "split END with inline block comment should align with the IF owner depth, got:\n{}",
            formatted
        );
        assert_eq!(
            indent(lines[if_suffix_idx]),
            indent(lines[end_idx]),
            "split IF qualifier after inline-comment END should consume the pending END owner depth, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_sql_basic_split_named_end_labels_preserve_owner_depth() {
        let source = r#"CREATE PACKAGE BODY PKG_SPLIT AS
    PROCEDURE P1 IS
    BEGIN
        NULL;
    END
    P1;
END
PKG_SPLIT;"#;

        let formatted = SqlEditorWidget::format_sql_basic(source);
        let lines: Vec<&str> = formatted.lines().collect();
        let indent = |line: &str| line.len().saturating_sub(line.trim_start().len());

        let procedure_end_idx = lines
            .iter()
            .position(|line| line.trim_start() == "END")
            .expect("procedure END line");
        let procedure_label_idx = lines
            .iter()
            .position(|line| line.trim_start() == "P1;")
            .expect("procedure label line");
        let package_end_idx = lines
            .iter()
            .enumerate()
            .skip(procedure_label_idx + 1)
            .find(|(_, line)| line.trim_start() == "END")
            .map(|(idx, _)| idx)
            .expect("package END line");
        let package_label_idx = lines
            .iter()
            .position(|line| line.trim_start() == "PKG_SPLIT;")
            .expect("package label line");

        assert_eq!(
            indent(lines[procedure_label_idx]),
            indent(lines[procedure_end_idx]),
            "split procedure end label should consume the pending END owner depth, got:\n{}",
            formatted
        );
        assert_eq!(
            indent(lines[package_label_idx]),
            indent(lines[package_end_idx]),
            "split package end label should consume the pending END owner depth, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_sql_basic_split_end_before_each_row_preserves_owner_depth_across_multi_step_suffix() {
        let source = r#"CREATE OR REPLACE TRIGGER trg_employee_compound
FOR INSERT ON employees
COMPOUND TRIGGER
    BEFORE EACH ROW IS
    BEGIN
        NULL;
    END
    -- split timing suffix
    BEFORE
    -- split timing middle
    EACH
    ROW;
END trg_employee_compound;"#;

        let formatted = SqlEditorWidget::format_sql_basic(source);
        let lines: Vec<&str> = formatted.lines().collect();
        let indent = |line: &str| line.len().saturating_sub(line.trim_start().len());

        let timing_header_idx = lines
            .iter()
            .position(|line| line.trim_start() == "BEFORE EACH ROW IS")
            .expect("timing header line");
        let end_idx = lines
            .iter()
            .enumerate()
            .skip(timing_header_idx + 1)
            .find(|(_, line)| line.trim_start() == "END")
            .map(|(idx, _)| idx)
            .expect("split END line");
        let comment_idx = lines
            .iter()
            .position(|line| line.trim_start() == "-- split timing suffix")
            .expect("timing comment line");
        let before_idx = lines
            .iter()
            .position(|line| line.trim_start() == "BEFORE")
            .expect("split BEFORE line");
        let middle_comment_idx = lines
            .iter()
            .position(|line| line.trim_start() == "-- split timing middle")
            .expect("middle comment line");
        let each_idx = lines
            .iter()
            .position(|line| line.trim_start() == "EACH")
            .expect("split EACH line");
        let row_idx = lines
            .iter()
            .position(|line| line.trim_start() == "ROW;")
            .expect("split ROW line");

        assert_eq!(
            indent(lines[end_idx]),
            indent(lines[timing_header_idx]),
            "split END should align with the timing header depth, got:\n{}",
            formatted
        );
        assert_eq!(
            indent(lines[before_idx]),
            indent(lines[end_idx]),
            "split BEFORE suffix should consume the pending END owner depth, got:\n{}",
            formatted
        );
        assert_eq!(
            indent(lines[comment_idx]),
            indent(lines[end_idx]),
            "comment gap before split BEFORE should stay on the retained END owner depth, got:\n{}",
            formatted
        );
        assert_eq!(
            indent(lines[each_idx]),
            indent(lines[before_idx]),
            "split EACH suffix should remain on the retained END owner depth, got:\n{}",
            formatted
        );
        assert_eq!(
            indent(lines[middle_comment_idx]),
            indent(lines[each_idx]),
            "comment gap inside split END BEFORE EACH ROW should stay on the retained owner depth, got:\n{}",
            formatted
        );
        assert_eq!(
            indent(lines[row_idx]),
            indent(lines[each_idx]),
            "split ROW suffix should complete on the retained END owner depth, got:\n{}",
            formatted
        );
    }

    #[test]
    fn plsql_if_condition_with_parenthesized_case_keeps_case_and_close_paren_depths_stable() {
        let input = r#"BEGIN
    IF (
        CASE
            WHEN flag = 'Y' THEN 1
            ELSE 0
        END
    ) = 1 THEN
        NULL;
    END IF;
END;"#;
        let formatted = SqlEditorWidget::format_sql_basic(input);
        let lines: Vec<&str> = formatted.lines().collect();
        let indent = |line: &str| line.len().saturating_sub(line.trim_start().len());
        let if_idx = lines
            .iter()
            .position(|line| line.trim_start() == "IF (")
            .unwrap_or(0);
        let case_idx = lines
            .iter()
            .position(|line| line.trim_start() == "CASE")
            .unwrap_or(0);
        let close_paren_idx = lines
            .iter()
            .position(|line| line.trim_start().starts_with(") = 1 THEN"))
            .unwrap_or(0);
        let end_if_idx = lines
            .iter()
            .position(|line| line.trim_start() == "END IF;")
            .unwrap_or(0);

        assert!(
            indent(lines[case_idx]) > indent(lines[if_idx]),
            "CASE inside IF condition should still indent deeper than the IF header, got:\n{}",
            formatted
        );
        assert_eq!(
            indent(lines[close_paren_idx]),
            indent(lines[if_idx]),
            "close paren line should return to the IF header depth, got:\n{}",
            formatted
        );
        assert_eq!(
            indent(lines[end_if_idx]),
            indent(lines[if_idx]),
            "END IF should stay aligned with IF after parenthesized CASE condition continuations, got:\n{}",
            formatted
        );
    }

    #[test]
    fn plsql_if_condition_with_comment_gap_before_parenthesized_case_keeps_pending_open_state() {
        let input = r#"BEGIN
    IF (
        -- gap
        CASE
            WHEN flag = 'Y' THEN 1
            ELSE 0
        END
    ) = 1 THEN
        NULL;
    END IF;
END;"#;
        let formatted = SqlEditorWidget::format_sql_basic(input);
        let lines: Vec<&str> = formatted.lines().collect();
        let indent = |line: &str| line.len().saturating_sub(line.trim_start().len());
        let if_idx = lines
            .iter()
            .position(|line| line.trim_start() == "IF (")
            .expect("IF line");
        let comment_idx = lines
            .iter()
            .position(|line| line.trim_start() == "-- gap")
            .expect("comment gap line");
        let case_idx = lines
            .iter()
            .position(|line| line.trim_start() == "CASE")
            .expect("CASE line");
        let close_paren_idx = lines
            .iter()
            .position(|line| line.trim_start().starts_with(") = 1 THEN"))
            .expect("close paren line");

        assert_eq!(
            indent(lines[comment_idx]),
            indent(lines[case_idx]),
            "comment gap before CASE should keep the pending open-paren body depth, got:\n{}",
            formatted
        );
        assert!(
            indent(lines[case_idx]) > indent(lines[if_idx]),
            "CASE after a comment gap should still indent deeper than the IF header, got:\n{}",
            formatted
        );
        assert_eq!(
            indent(lines[close_paren_idx]),
            indent(lines[if_idx]),
            "close paren line should return to the IF header depth after the comment gap, got:\n{}",
            formatted
        );
        assert_eq!(SqlEditorWidget::format_sql_basic(&formatted), formatted);
    }

    #[test]
    fn plsql_while_condition_with_parenthesized_case_keeps_case_and_close_paren_depths_stable() {
        let input = r#"BEGIN
    WHILE (
        CASE
            WHEN flag = 'Y' THEN 1
            ELSE 0
        END
    ) = 1 LOOP
        NULL;
    END LOOP;
END;"#;
        let formatted = SqlEditorWidget::format_sql_basic(input);
        let lines: Vec<&str> = formatted.lines().collect();
        let indent = |line: &str| line.len().saturating_sub(line.trim_start().len());
        let while_idx = lines
            .iter()
            .position(|line| line.trim_start() == "WHILE (")
            .unwrap_or(0);
        let case_idx = lines
            .iter()
            .position(|line| line.trim_start() == "CASE")
            .unwrap_or(0);
        let close_paren_idx = lines
            .iter()
            .position(|line| line.trim_start().starts_with(") = 1 LOOP"))
            .unwrap_or(0);
        let end_loop_idx = lines
            .iter()
            .position(|line| line.trim_start() == "END LOOP;")
            .unwrap_or(0);

        assert!(
            indent(lines[case_idx]) > indent(lines[while_idx]),
            "CASE inside WHILE condition should still indent deeper than the WHILE header, got:\n{}",
            formatted
        );
        assert_eq!(
            indent(lines[close_paren_idx]),
            indent(lines[while_idx]),
            "close paren line should return to the WHILE header depth, got:\n{}",
            formatted
        );
        assert_eq!(
            indent(lines[end_loop_idx]),
            indent(lines[while_idx]),
            "END LOOP should stay aligned with WHILE after parenthesized CASE condition continuations, got:\n{}",
            formatted
        );
    }

    #[test]
    fn plsql_elsif_condition_with_parenthesized_case_keeps_close_paren_depth_stable() {
        let input = r#"BEGIN
    IF flag = 'N' THEN
        NULL;
    ELSIF (
        CASE
            WHEN flag = 'Y' THEN 1
            ELSE 0
        END
    ) = 1 THEN
        NULL;
    END IF;
END;"#;
        let formatted = SqlEditorWidget::format_sql_basic(input);
        let lines: Vec<&str> = formatted.lines().collect();
        let indent = |line: &str| line.len().saturating_sub(line.trim_start().len());
        let elsif_idx = lines
            .iter()
            .position(|line| line.trim_start() == "ELSIF (")
            .unwrap_or(0);
        let close_paren_idx = lines
            .iter()
            .position(|line| line.trim_start().starts_with(") = 1 THEN"))
            .unwrap_or(0);
        let end_if_idx = lines
            .iter()
            .position(|line| line.trim_start() == "END IF;")
            .unwrap_or(0);

        assert_eq!(
            indent(lines[close_paren_idx]),
            indent(lines[elsif_idx]),
            "ELSIF close paren line should return to the ELSIF header depth, got:\n{}",
            formatted
        );
        assert_eq!(
            indent(lines[end_if_idx]),
            indent(lines[elsif_idx]),
            "END IF should stay aligned with ELSIF/IF block after parenthesized CASE continuations, got:\n{}",
            formatted
        );
    }

    #[test]
    fn plsql_if_multigroup_exists_conditions_keep_owner_depths_stable() {
        let input = r#"BEGIN
    IF EXISTS (
        SELECT 1
        FROM dual
    ) AND EXISTS (
        SELECT 1
        FROM dual
    ) THEN
        NULL;
    END IF;
END;"#;
        let formatted = SqlEditorWidget::format_sql_basic(input);
        let lines: Vec<&str> = formatted.lines().collect();
        let indent = |line: &str| line.len().saturating_sub(line.trim_start().len());
        let if_idx = lines
            .iter()
            .position(|line| line.trim_start().starts_with("IF EXISTS ("))
            .expect("formatted output should contain IF EXISTS header");
        let and_idx = lines
            .iter()
            .position(|line| line.trim_start().starts_with("AND EXISTS ("))
            .expect("formatted output should contain AND EXISTS continuation");
        let first_select_idx = lines
            .iter()
            .position(|line| line.trim_start() == "SELECT 1")
            .expect("formatted output should contain first child SELECT");
        let second_select_idx = lines
            .iter()
            .enumerate()
            .skip(and_idx.saturating_add(1))
            .find(|(_, line)| line.trim_start() == "SELECT 1")
            .map(|(idx, _)| idx)
            .expect("formatted output should contain second child SELECT");
        let close_then_idx = lines
            .iter()
            .position(|line| line.trim_start().starts_with(") THEN"))
            .expect("formatted output should contain close THEN line");
        let end_if_idx = lines
            .iter()
            .position(|line| line.trim_start() == "END IF;")
            .expect("formatted output should contain END IF");

        assert_eq!(
            indent(lines[and_idx]),
            indent(lines[if_idx]),
            "AND/OR continuation inside the same parenthesized IF condition should stay at the IF header depth, got:\n{}",
            formatted
        );
        assert!(
            indent(lines[first_select_idx]) > indent(lines[if_idx]),
            "first EXISTS subquery should stay nested under IF, got:\n{}",
            formatted
        );
        assert!(
            indent(lines[second_select_idx]) > indent(lines[if_idx]),
            "second EXISTS subquery should stay nested under IF, got:\n{}",
            formatted
        );
        assert_eq!(
            indent(lines[close_then_idx]),
            indent(lines[if_idx]),
            "final close paren line should return to the IF header depth, got:\n{}",
            formatted
        );
        assert_eq!(
            indent(lines[end_if_idx]),
            indent(lines[if_idx]),
            "END IF should stay aligned after multi-group parenthesized conditions, got:\n{}",
            formatted
        );
    }

    #[test]
    fn searched_case_when_exists_subquery_keeps_close_paren_at_when_depth() {
        let input = r#"SELECT
    CASE
        WHEN EXISTS (
            SELECT 1
            FROM dual
        ) THEN 1
        ELSE 0
    END AS flag
FROM dual;"#;
        let formatted = SqlEditorWidget::format_sql_basic(input);
        let lines: Vec<&str> = formatted.lines().collect();
        let indent = |line: &str| line.len().saturating_sub(line.trim_start().len());
        let when_idx = lines
            .iter()
            .position(|line| line.trim_start().starts_with("WHEN EXISTS ("))
            .expect("formatted output should contain WHEN EXISTS header");
        let close_then_idx = lines
            .iter()
            .position(|line| line.trim_start().starts_with(") THEN 1"))
            .expect("formatted output should contain close THEN line");
        let select_idx = lines
            .iter()
            .position(|line| line.trim_start() == "SELECT 1")
            .expect("formatted output should contain EXISTS child SELECT");

        assert!(
            indent(lines[select_idx]) > indent(lines[when_idx]),
            "searched CASE EXISTS subquery should stay nested under WHEN, got:\n{}",
            formatted
        );
        assert_eq!(
            indent(lines[close_then_idx]),
            indent(lines[when_idx]),
            "close paren THEN line should return to the WHEN header depth, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_for_auto_formatting_searched_case_multiline_when_condition_uses_case_continuation_depth(
    ) {
        let source = r#"SELECT
    CASE
        WHEN e.salary >= 100000
        AND EXISTS (
            SELECT 1
            FROM qt_fmt_bonus b
            WHERE b.emp_id = e.emp_id
        ) THEN 'ELITE'
        ELSE 'OTHER'
    END AS deep_case_label
FROM qt_fmt_emp e;"#;

        let formatted = SqlEditorWidget::format_for_auto_formatting(source, false);
        let lines: Vec<&str> = formatted.lines().collect();
        let indent = |line: &str| line.len().saturating_sub(line.trim_start().len());

        let when_idx = lines
            .iter()
            .position(|line| line.trim_start() == "WHEN e.salary >= 100000")
            .expect("formatted output should contain searched CASE WHEN header");
        let and_idx = lines
            .iter()
            .position(|line| line.trim_start() == "AND EXISTS (")
            .expect("formatted output should contain multiline WHEN condition continuation");
        let select_idx = lines
            .iter()
            .position(|line| line.trim_start() == "SELECT 1")
            .expect("formatted output should contain EXISTS child SELECT");
        let close_then_idx = lines
            .iter()
            .position(|line| line.trim_start() == ") THEN 'ELITE'")
            .expect("formatted output should contain EXISTS close THEN line");

        assert_eq!(
            indent(lines[and_idx]),
            indent(lines[when_idx]).saturating_add(4),
            "searched CASE condition continuations should be one extra level deeper than WHEN, got:\n{}",
            formatted
        );
        assert_eq!(
            indent(lines[select_idx]),
            indent(lines[and_idx]).saturating_add(4),
            "EXISTS child SELECT should use the promoted AND-owner base depth, got:\n{}",
            formatted
        );
        assert_eq!(
            indent(lines[close_then_idx]),
            indent(lines[and_idx]),
            "EXISTS close-paren THEN line should return to the promoted AND-owner depth, got:\n{}",
            formatted
        );
        assert_eq!(
            SqlEditorWidget::format_for_auto_formatting(&formatted, false),
            formatted,
            "auto formatting should stay stable for multiline searched CASE conditions"
        );
    }

    #[test]
    fn format_for_auto_formatting_when_function_paren_body_uses_case_condition_owner_depth() {
        let source = r#"SELECT
    CASE
        WHEN MAX (
            CASE
                WHEN u.qtr = 'Q2' THEN u.amt
            END
        ) > 0 THEN 'UP'
        ELSE 'SAME'
    END AS trend_flag
FROM u;"#;

        let formatted = SqlEditorWidget::format_for_auto_formatting(source, false);
        let lines: Vec<&str> = formatted.lines().collect();
        let indent = |line: &str| line.len().saturating_sub(line.trim_start().len());

        let when_idx = lines
            .iter()
            .position(|line| line.trim_start().starts_with("WHEN MAX ("))
            .expect("formatted output should contain WHEN MAX owner line");
        let case_idx = lines
            .iter()
            .enumerate()
            .skip(when_idx + 1)
            .find(|(_, line)| line.trim_start() == "CASE")
            .map(|(idx, _)| idx)
            .expect("formatted output should contain nested CASE under MAX");

        assert_eq!(
            indent(lines[case_idx]),
            indent(lines[when_idx]).saturating_add(8),
            "nested CASE inside WHEN-owned MAX should include both the WHEN continuation depth and the MAX paren depth, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_for_auto_formatting_normalizes_unrelated_odd_exists_indent_even_with_apply() {
        let source = r#"SELECT
    d.department_name,
    emp_stats.avg_sal,
    CASE
        WHEN EXISTS (
            SELECT 1
            FROM bonus_data b
            WHERE b.emp_id = d.department_id
      AND b.bonus_amt >= 300
        ) THEN 'Y'
        ELSE 'N'
    END AS has_bonus
FROM departments d
CROSS APPLY (
    SELECT AVG(e.salary) AS avg_sal
    FROM employees e
    WHERE e.department_id = d.department_id
) emp_stats;"#;

        let formatted = SqlEditorWidget::format_for_auto_formatting(source, false);
        let lines: Vec<&str> = formatted.lines().collect();
        let indent = |line: &str| line.len().saturating_sub(line.trim_start().len());

        let select_list_idx = lines
            .iter()
            .position(|line| line.trim_start() == "emp_stats.avg_sal,")
            .expect("formatted output should keep APPLY select-list continuation");
        let exists_when_idx = lines
            .iter()
            .position(|line| line.trim_start() == "WHEN EXISTS (")
            .expect("formatted output should contain EXISTS WHEN header");
        let select_idx = lines
            .iter()
            .position(|line| line.trim_start() == "SELECT 1")
            .expect("formatted output should contain EXISTS child SELECT");
        let where_idx = lines
            .iter()
            .position(|line| line.trim_start() == "WHERE b.emp_id = d.department_id")
            .expect("formatted output should contain EXISTS child WHERE");
        let and_idx = lines
            .iter()
            .position(|line| line.trim_start() == "AND b.bonus_amt >= 300")
            .expect("formatted output should contain EXISTS child AND");
        let close_then_idx = lines
            .iter()
            .position(|line| line.trim_start() == ") THEN 'Y'")
            .expect("formatted output should contain EXISTS close THEN line");

        assert_eq!(
            indent(lines[select_list_idx]),
            4,
            "APPLY-driven top-level select-list item should normalize to canonical structural depth, got:\n{}",
            formatted
        );
        assert!(
            indent(lines[select_idx]) > indent(lines[exists_when_idx]),
            "EXISTS child SELECT should stay nested under the WHEN EXISTS header, got:\n{}",
            formatted
        );
        assert_eq!(
            indent(lines[where_idx]),
            indent(lines[select_idx]),
            "EXISTS child WHERE should reuse the child query base depth, got:\n{}",
            formatted
        );
        assert_eq!(
            indent(lines[and_idx]),
            indent(lines[where_idx]).saturating_add(4),
            "odd manual AND indent inside EXISTS should be normalized to the computed child-query continuation depth even when APPLY exists elsewhere, got:\n{}",
            formatted
        );
        assert_eq!(
            indent(lines[close_then_idx]),
            indent(lines[exists_when_idx]),
            "EXISTS close-paren THEN line should return to the WHEN EXISTS header depth, got:\n{}",
            formatted
        );
        assert_eq!(
            SqlEditorWidget::format_for_auto_formatting(&formatted, false),
            formatted,
            "auto formatting should stay stable after normalizing EXISTS indentation in APPLY statements"
        );
    }

    #[test]
    fn format_for_auto_formatting_normalizes_mixed_tab_space_hanging_indent_in_exists_condition() {
        let source = "SELECT\n    d.department_name\nFROM departments d\nWHERE EXISTS (\n    SELECT 1\n    FROM bonus_data b\n    WHERE b.emp_id = d.department_id\n\t     AND b.bonus_amt >= 300\n);";

        let formatted = SqlEditorWidget::format_for_auto_formatting(source, false);
        let lines: Vec<&str> = formatted.lines().collect();
        let indent = |line: &str| line.len().saturating_sub(line.trim_start().len());

        let where_idx = lines
            .iter()
            .position(|line| line.trim_start() == "WHERE b.emp_id = d.department_id")
            .expect("formatted output should contain EXISTS child WHERE");
        let and_idx = lines
            .iter()
            .position(|line| line.trim_start() == "AND b.bonus_amt >= 300")
            .expect("formatted output should contain EXISTS child AND");

        assert_eq!(
            indent(lines[and_idx]),
            indent(lines[where_idx]).saturating_add(4),
            "mixed tab+space indentation should normalize to canonical continuation depth, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_sql_basic_keeps_nested_subquery_and_relative_to_its_own_where_frame() {
        let source = r#"if (1>0) then
    select 1
    from (
            select 1
            from dual
            where 1 = 1
    and 1 = 1
        )
    where 1 = 1
    and 2 = 2;
end if;"#;

        let formatted = SqlEditorWidget::format_sql_basic(source);
        let lines: Vec<&str> = formatted.lines().collect();
        let indent = |line: &str| line.len().saturating_sub(line.trim_start().len());

        let inner_where_idx = lines
            .iter()
            .position(|line| line.trim_start() == "WHERE 1 = 1")
            .expect("formatted output should contain inner WHERE");
        let inner_and_idx = lines
            .iter()
            .enumerate()
            .skip(inner_where_idx.saturating_add(1))
            .find(|(_, line)| line.trim_start() == "AND 1 = 1")
            .map(|(idx, _)| idx)
            .expect("formatted output should contain inner AND");

        assert_eq!(
            indent(lines[inner_and_idx]),
            indent(lines[inner_where_idx]).saturating_add(4),
            "inner subquery AND should be exactly one level deeper than its own WHERE frame, got:\n{formatted}"
        );
    }

    #[test]
    fn format_sql_basic_keeps_exists_inner_and_relative_to_exists_query_frame() {
        let source = r#"select 1
from dual
where (exists (
        select *
        from dual
        where 1 = 1
                and 2 = 2
    ));"#;

        let formatted = SqlEditorWidget::format_sql_basic(source);
        let lines: Vec<&str> = formatted.lines().collect();
        let indent = |line: &str| line.len().saturating_sub(line.trim_start().len());

        let inner_where_idx = lines
            .iter()
            .position(|line| line.trim_start() == "WHERE 1 = 1")
            .expect("formatted output should contain EXISTS-inner WHERE");
        let inner_and_idx = lines
            .iter()
            .position(|line| line.trim_start() == "AND 2 = 2")
            .expect("formatted output should contain EXISTS-inner AND");

        assert_eq!(
            indent(lines[inner_and_idx]),
            indent(lines[inner_where_idx]).saturating_add(4),
            "EXISTS-inner AND should stay one level deeper than the EXISTS-inner WHERE within the same frame, got:\n{formatted}"
        );
    }

    #[test]
    fn format_sql_basic_keeps_case_end_aligned_to_case_owner_depth_inside_subquery() {
        let source = r#"procedure a (b in number) as
begin
    open cv for
        select 1
        from (
                select
                    case
                        when 1 = 1 then 1
                        else 0
        end b
                from c
            ) d
        where 1 = 1;
end;"#;

        let formatted = SqlEditorWidget::format_sql_basic(source);
        let lines: Vec<&str> = formatted.lines().collect();
        let indent = |line: &str| line.len().saturating_sub(line.trim_start().len());

        let case_idx = lines
            .iter()
            .position(|line| line.trim_start() == "CASE")
            .expect("formatted output should contain CASE owner");
        let end_idx = lines
            .iter()
            .position(|line| line.trim_start().starts_with("END b"))
            .expect("formatted output should contain END b");

        assert_eq!(
            indent(lines[end_idx]),
            indent(lines[case_idx]),
            "CASE terminator should align to its CASE owner depth inside the same frame, got:\n{formatted}"
        );
    }

    #[test]
    fn format_sql_basic_keeps_case_condition_and_and_nested_exists_and_isolated_by_frames() {
        let source = r#"select
    case
        when col1 = 1
        and exists (
                select 1
                from dual
                where 1 = 1
                        and 2 = 2
            ) then 'Y'
        else 'N'
    end as flag
from dual;"#;

        let formatted = SqlEditorWidget::format_sql_basic(source);
        let lines: Vec<&str> = formatted.lines().collect();
        let indent = |line: &str| line.len().saturating_sub(line.trim_start().len());

        let when_idx = lines
            .iter()
            .position(|line| line.trim_start() == "WHEN col1 = 1")
            .expect("formatted output should contain CASE WHEN header");
        let case_and_idx = lines
            .iter()
            .position(|line| line.trim_start() == "AND EXISTS (")
            .expect("formatted output should contain CASE condition AND");
        let inner_where_idx = lines
            .iter()
            .position(|line| line.trim_start() == "WHERE 1 = 1")
            .expect("formatted output should contain nested EXISTS WHERE");
        let inner_and_idx = lines
            .iter()
            .position(|line| line.trim_start() == "AND 2 = 2")
            .expect("formatted output should contain nested EXISTS AND");

        assert_eq!(
            indent(lines[case_and_idx]),
            indent(lines[when_idx]).saturating_add(4),
            "CASE condition AND should remain one level deeper than its WHEN owner frame, got:\n{formatted}"
        );
        assert_eq!(
            indent(lines[inner_and_idx]),
            indent(lines[inner_where_idx]).saturating_add(4),
            "nested EXISTS AND should remain one level deeper than its own WHERE frame without CASE-state leakage, got:\n{formatted}"
        );
    }

    #[test]
    fn format_for_auto_formatting_does_not_preserve_tab_derived_odd_select_list_indent() {
        let source = "SELECT employee_id,\n\t      first_name,\n    last_name\nFROM employees;";

        let formatted = SqlEditorWidget::format_for_auto_formatting(source, false);
        let lines: Vec<&str> = formatted.lines().collect();
        let indent = |line: &str| line.len().saturating_sub(line.trim_start().len());

        let first_name_idx = lines
            .iter()
            .position(|line| {
                line.trim_start()
                    .trim_end_matches(',')
                    .eq_ignore_ascii_case("first_name")
            })
            .unwrap_or_else(|| {
                panic!(
                    "formatted output should contain first_name, got:\n{}",
                    formatted
                )
            });
        let last_name_idx = lines
            .iter()
            .position(|line| {
                line.trim_start()
                    .trim_end_matches(',')
                    .eq_ignore_ascii_case("last_name")
            })
            .unwrap_or_else(|| {
                panic!(
                    "formatted output should contain last_name, got:\n{}",
                    formatted
                )
            });

        assert_eq!(
            indent(lines[first_name_idx]),
            indent(lines[last_name_idx]),
            "tab-derived odd indentation in SELECT list should normalize to canonical continuation depth, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_for_auto_formatting_normalizes_visual_odd_hanging_indent_with_tabs() {
        let source = "SELECT department_id
FROM departments d
WHERE EXISTS (
        SELECT 1
        FROM bonus_data b
        WHERE b.emp_id = d.department_id
\t      AND b.bonus_amt >= 300
    );";

        let formatted = SqlEditorWidget::format_for_auto_formatting(source, false);
        let lines: Vec<&str> = formatted.lines().collect();
        let indent = |line: &str| line.len().saturating_sub(line.trim_start().len());

        let and_idx = lines
            .iter()
            .position(|line| line.trim_start() == "AND b.bonus_amt >= 300")
            .expect("formatted output should contain EXISTS child AND");

        let where_idx = lines
            .iter()
            .position(|line| line.trim_start() == "WHERE b.emp_id = d.department_id")
            .expect("formatted output should contain EXISTS child WHERE");

        assert_eq!(
            indent(lines[and_idx]),
            indent(lines[where_idx]).saturating_add(4),
            "tab-derived odd indentation should normalize to canonical continuation depth in parser-depth layout pass, got:\n{}",
            formatted
        );
    }

    #[test]
    fn plsql_for_split_in_subquery_keeps_child_query_on_for_depth() {
        let input = r#"BEGIN
    FOR rec IN
    (
        SELECT 1
        FROM dual
    ) LOOP
        NULL;
    END LOOP;
END;"#;
        let formatted = SqlEditorWidget::format_sql_basic(input);
        let lines: Vec<&str> = formatted.lines().collect();
        let indent = |line: &str| line.len().saturating_sub(line.trim_start().len());
        let for_idx = lines
            .iter()
            .position(|line| line.trim_start().starts_with("FOR rec IN"))
            .expect("formatted output should contain FOR header");
        let select_idx = lines
            .iter()
            .position(|line| line.trim_start() == "SELECT 1")
            .expect("formatted output should contain child SELECT");
        let close_loop_idx = lines
            .iter()
            .position(|line| line.trim_start() == ") LOOP")
            .expect("formatted output should contain close LOOP line");
        let end_loop_idx = lines
            .iter()
            .position(|line| line.trim_start() == "END LOOP;")
            .expect("formatted output should contain END LOOP");

        assert!(
            indent(lines[select_idx]) > indent(lines[for_idx]),
            "split FOR ... IN subquery should stay nested under FOR, got:\n{}",
            formatted
        );
        assert_eq!(
            indent(lines[close_loop_idx]),
            indent(lines[for_idx]),
            "close LOOP line should return to the FOR header depth, got:\n{}",
            formatted
        );
        assert_eq!(
            indent(lines[end_loop_idx]),
            indent(lines[for_idx]),
            "END LOOP should stay aligned with FOR after split IN subquery, got:\n{}",
            formatted
        );
    }

    #[test]
    fn plsql_else_if_clause_uses_block_depth_without_extra_into_indent() {
        let sql = r#"BEGIN
  IF 1 = 1 THEN
    SELECT col1,
           col2
      INTO v_col1,
           v_col2
      FROM dual;
  ELSEIF v_col1 = 1 THEN
      NULL;
  END IF;
  NULL;
END;"#;
        let formatted = SqlEditorWidget::format_sql_basic(sql);

        assert!(
            formatted.contains("    ELSEIF v_col1 = 1 THEN"),
            "ELSEIF should align to block depth, got:\n{}",
            formatted
        );
        assert!(
            !formatted.contains("        ELSEIF v_col1 = 1 THEN"),
            "ELSEIF should not keep stale INTO-list extra indent, got:\n{}",
            formatted
        );
    }

    #[test]
    fn plsql_elseif_clause_uses_block_depth_without_extra_into_indent() {
        let sql = r#"BEGIN
  IF 1 = 1 THEN
    SELECT col1,
           col2
      INTO v_col1,
           v_col2
      FROM dual;
  ELSIF v_col1 = 1 THEN
      NULL;
  END IF;
  NULL;
END;"#;
        let formatted = SqlEditorWidget::format_sql_basic(sql);

        assert!(
            formatted.contains("    ELSIF v_col1 = 1 THEN"),
            "ELSIF should align to block depth, got:\n{}",
            formatted
        );
        assert!(
            !formatted.contains("        ELSIF v_col1 = 1 THEN"),
            "ELSIF should not keep stale INTO-list extra indent, got:\n{}",
            formatted
        );
    }

    #[test]
    fn plsql_split_elsif_then_opens_body_only_after_then() {
        let sql = r#"BEGIN
  IF v_ready = 'N' THEN
    NULL;
  ELSIF v_ready = 'Y'
  THEN
    OPEN c_emp FOR
    SELECT empno
    FROM emp;
  END IF;
END;"#;
        let formatted = SqlEditorWidget::format_sql_basic(sql);
        let lines: Vec<&str> = formatted.lines().collect();
        let indent = |line: &str| line.len().saturating_sub(line.trim_start().len());

        let elsif_idx = lines
            .iter()
            .position(|line| line.trim_start().starts_with("ELSIF v_ready = 'Y'"))
            .expect("ELSIF line");
        let then_idx = lines
            .iter()
            .position(|line| line.trim_start() == "THEN")
            .unwrap_or(elsif_idx);
        let open_idx = lines
            .iter()
            .position(|line| line.trim_start() == "OPEN c_emp FOR")
            .expect("OPEN cursor line");
        let select_idx = lines
            .iter()
            .enumerate()
            .skip(open_idx + 1)
            .find(|(_, line)| line.trim_start() == "SELECT empno")
            .map(|(idx, _)| idx)
            .expect("SELECT line");

        if then_idx != elsif_idx {
            assert_eq!(
                indent(lines[then_idx]),
                indent(lines[elsif_idx]),
                "split THEN should stay aligned with the ELSIF owner depth, got:\n{}",
                formatted
            );
        }
        assert_eq!(
            indent(lines[open_idx]),
            indent(lines[then_idx]).saturating_add(4),
            "OPEN cursor after split THEN should use the THEN body depth, got:\n{}",
            formatted
        );
        assert_eq!(
            indent(lines[select_idx]),
            indent(lines[open_idx]).saturating_add(4),
            "SELECT after OPEN cursor should stay one level deeper than OPEN, got:\n{}",
            formatted
        );
    }

    #[test]
    fn plsql_statement_after_returning_into_does_not_keep_extra_into_indent() {
        let sql = r#"BEGIN
  UPDATE emp
  SET sal = sal + 1
  RETURNING empno
  INTO v_empno;
  NULL;
END;"#;
        let formatted = SqlEditorWidget::format_sql_basic(sql);

        assert!(
            formatted.contains("    NULL;"),
            "line after RETURNING INTO should align to block depth, got:\n{}",
            formatted
        );
        assert!(
            !formatted.contains("        NULL;"),
            "line after RETURNING INTO should not keep stale INTO-list extra indent, got:\n{}",
            formatted
        );
    }

    #[test]
    fn plsql_statement_after_fetch_into_does_not_keep_extra_into_indent() {
        let sql = r#"BEGIN
  FETCH c1
  INTO v_empno;
  v_total := v_total + 1;
END;"#;
        let formatted = SqlEditorWidget::format_sql_basic(sql);

        assert!(
            formatted.contains("    v_total := v_total + 1;"),
            "line after FETCH INTO should align to block depth, got:\n{}",
            formatted
        );
        assert!(
            !formatted.contains("        v_total := v_total + 1;"),
            "line after FETCH INTO should not keep stale INTO-list extra indent, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_statement_preserves_end_for_and_end_while_suffixes() {
        let for_sql = "BEGIN\n  FOR i IN 1..3 LOOP\n    NULL;\n  END FOR;\nEND;";
        let for_formatted = SqlEditorWidget::format_sql_basic(for_sql);
        assert!(
            for_formatted.contains("END FOR;"),
            "END FOR suffix should be preserved as a single terminator line, got:\n{}",
            for_formatted
        );

        let while_sql = "BEGIN\n  WHILE i < 3 LOOP\n    i := i + 1;\n  END WHILE;\nEND;";
        let while_formatted = SqlEditorWidget::format_sql_basic(while_sql);
        assert!(
            while_formatted.contains("END WHILE;"),
            "END WHILE suffix should be preserved as a single terminator line, got:\n{}",
            while_formatted
        );
    }

    #[test]
    fn paren_case_expression_tracks_searched_case_headers() {
        let sql = r#"BEGIN
  v_val := (
    CASE v_mode
      WHEN 1 THEN 10
      ELSE 0
    END
  );
END;"#;
        let formatted = SqlEditorWidget::format_sql_basic(sql);
        let lines: Vec<&str> = formatted.lines().collect();
        let indent = |line: &str| line.len().saturating_sub(line.trim_start().len());
        let case_idx = lines
            .iter()
            .position(|line| line.trim_start().starts_with("CASE v_mode"))
            .expect("CASE line");
        let when_idx = lines
            .iter()
            .enumerate()
            .skip(case_idx + 1)
            .find(|(_, line)| line.trim_start().starts_with("WHEN 1 THEN"))
            .map(|(idx, _)| idx)
            .expect("WHEN line");
        let else_idx = lines
            .iter()
            .enumerate()
            .skip(when_idx + 1)
            .find(|(_, line)| line.trim_start() == "ELSE")
            .map(|(idx, _)| idx)
            .expect("ELSE line");
        let else_value_idx = lines
            .iter()
            .enumerate()
            .skip(else_idx + 1)
            .find(|(_, line)| line.trim_start() == "0")
            .map(|(idx, _)| idx)
            .expect("ELSE value line");
        let end_idx = lines
            .iter()
            .enumerate()
            .skip(else_value_idx + 1)
            .find(|(_, line)| line.trim_start() == "END")
            .map(|(idx, _)| idx)
            .expect("END line");

        assert!(
            formatted.contains("(\n        CASE v_mode\n            WHEN 1 THEN"),
            "CASE <expr> after '(' should get parenthesized CASE depth indent, got:\n{}",
            formatted
        );
        assert_eq!(
            indent(lines[else_idx]),
            indent(lines[when_idx]),
            "ELSE should align with WHEN in the parenthesized CASE, got:\n{}",
            formatted
        );
        assert_eq!(
            indent(lines[else_value_idx]),
            indent(lines[else_idx]).saturating_add(4),
            "ELSE value should indent one level deeper than ELSE, got:\n{}",
            formatted
        );
        assert_eq!(
            indent(lines[end_idx]),
            indent(lines[case_idx]),
            "CASE <expr> END should keep CASE-block depth, got:\n{}",
            formatted
        );
    }

    #[test]
    fn paren_case_expression_tracks_end_case_terminator() {
        let sql = r#"BEGIN
  v_val := (
    CASE
      WHEN v_mode = 1 THEN 10
      ELSE 0
    END CASE
  );
END;"#;
        let formatted = SqlEditorWidget::format_sql_basic(sql);
        let lines: Vec<&str> = formatted.lines().collect();
        let indent = |line: &str| line.len().saturating_sub(line.trim_start().len());
        let case_idx = lines
            .iter()
            .position(|line| line.trim_start() == "CASE")
            .expect("CASE line");
        let when_idx = lines
            .iter()
            .enumerate()
            .skip(case_idx + 1)
            .find(|(_, line)| line.trim_start().starts_with("WHEN v_mode = 1 THEN"))
            .map(|(idx, _)| idx)
            .expect("WHEN line");
        let else_idx = lines
            .iter()
            .enumerate()
            .skip(when_idx + 1)
            .find(|(_, line)| line.trim_start() == "ELSE")
            .map(|(idx, _)| idx)
            .expect("ELSE line");
        let else_value_idx = lines
            .iter()
            .enumerate()
            .skip(else_idx + 1)
            .find(|(_, line)| line.trim_start() == "0")
            .map(|(idx, _)| idx)
            .expect("ELSE value line");
        let end_case_idx = lines
            .iter()
            .enumerate()
            .skip(else_value_idx + 1)
            .find(|(_, line)| line.trim_start() == "END CASE);")
            .map(|(idx, _)| idx)
            .expect("END CASE line");

        assert!(
            formatted.contains("CASE\n            WHEN v_mode = 1 THEN"),
            "Parenthesized CASE should still format branch depth, got:\n{}",
            formatted
        );
        assert_eq!(
            indent(lines[else_idx]),
            indent(lines[when_idx]),
            "ELSE should align with WHEN in the parenthesized END CASE form, got:\n{}",
            formatted
        );
        assert_eq!(
            indent(lines[else_value_idx]),
            indent(lines[else_idx]).saturating_add(4),
            "ELSE value should indent one level deeper than ELSE in END CASE form, got:\n{}",
            formatted
        );
        assert_eq!(
            indent(lines[end_case_idx]),
            indent(lines[case_idx]),
            "END CASE terminator should stay aligned with CASE header in parenthesized expression, got:\n{}",
            formatted
        );
    }

    #[test]
    fn parenthesized_case_after_operator_owner_uses_paren_body_depth() {
        let sql = r#"BEGIN
    FOR i IN 1..3 LOOP
        v_sum := v_sum + (
            CASE
                WHEN MOD (i, 2) = 0 THEN
                    i * 10
                ELSE
                    i
            END
        );
    END LOOP;
END;"#;
        let formatted = SqlEditorWidget::format_sql_basic(sql);
        let lines: Vec<&str> = formatted.lines().collect();
        let indent = |line: &str| line.len().saturating_sub(line.trim_start().len());
        let owner_idx = lines
            .iter()
            .position(|line| line.trim_start() == "v_sum := v_sum + (")
            .expect("owner line");
        let case_idx = lines
            .iter()
            .enumerate()
            .skip(owner_idx + 1)
            .find(|(_, line)| line.trim_start() == "CASE")
            .map(|(idx, _)| idx)
            .expect("CASE line");
        let when_idx = lines
            .iter()
            .enumerate()
            .skip(case_idx + 1)
            .find(|(_, line)| line.trim_start().starts_with("WHEN MOD (i, 2) = 0 THEN"))
            .map(|(idx, _)| idx)
            .expect("WHEN line");
        let end_idx = lines
            .iter()
            .enumerate()
            .skip(when_idx + 1)
            .find(|(_, line)| line.trim_start() == "END")
            .map(|(idx, _)| idx)
            .expect("END line");
        let close_idx = lines
            .iter()
            .enumerate()
            .skip(end_idx + 1)
            .find(|(_, line)| line.trim_start() == ");")
            .map(|(idx, _)| idx)
            .expect("close line");

        assert_eq!(
            indent(lines[case_idx]),
            indent(lines[owner_idx]).saturating_add(4),
            "CASE after an operator-owned open paren should use the paren body depth, got:\n{}",
            formatted
        );
        assert_eq!(
            indent(lines[when_idx]),
            indent(lines[case_idx]).saturating_add(4),
            "WHEN inside the operator-owned parenthesized CASE should stay one level deeper than CASE, got:\n{}",
            formatted
        );
        assert_eq!(
            indent(lines[end_idx]),
            indent(lines[case_idx]),
            "END inside the operator-owned parenthesized CASE should stay aligned with CASE, got:\n{}",
            formatted
        );
        assert_eq!(
            indent(lines[close_idx]),
            indent(lines[owner_idx]),
            "close paren after the operator-owned CASE should return to the owner depth, got:\n{}",
            formatted
        );
    }

    #[test]
    fn starts_with_end_suffix_terminator_requires_keyword_boundary() {
        assert!(SqlEditorWidget::starts_with_end_suffix_terminator(
            "END IF;"
        ));
        assert!(SqlEditorWidget::starts_with_end_suffix_terminator(
            "END /* keep */ IF;"
        ));
        assert!(SqlEditorWidget::starts_with_end_suffix_terminator(
            "END LOOP"
        ));
        assert!(SqlEditorWidget::starts_with_end_suffix_terminator(
            "END CASE"
        ));
        assert!(SqlEditorWidget::starts_with_end_suffix_terminator(
            "END REPEAT"
        ));
        assert!(!SqlEditorWidget::starts_with_end_suffix_terminator("END"));
        assert!(SqlEditorWidget::starts_with_end_suffix_terminator(
            "END FOR"
        ));
        assert!(SqlEditorWidget::starts_with_end_suffix_terminator(
            "END WHILE"
        ));
        assert!(SqlEditorWidget::starts_with_end_suffix_terminator(
            "END BEFORE EACH ROW"
        ));
        assert!(SqlEditorWidget::starts_with_end_suffix_terminator(
            "END AFTER STATEMENT"
        ));
        assert!(SqlEditorWidget::starts_with_end_suffix_terminator(
            "END INSTEAD OF"
        ));
        assert!(!SqlEditorWidget::starts_with_end_suffix_terminator(
            "END IF_OWNER;"
        ));
        assert!(!SqlEditorWidget::starts_with_end_suffix_terminator(
            "END FORWARD;"
        ));
    }

    #[test]
    fn starts_with_plain_end_excludes_qualified_end_suffixes() {
        assert!(SqlEditorWidget::starts_with_plain_end("END"));
        assert!(SqlEditorWidget::starts_with_plain_end("END pkg;"));
        assert!(SqlEditorWidget::starts_with_plain_end(
            "END /* keep */ pkg;"
        ));
        assert!(!SqlEditorWidget::starts_with_plain_end("END FOR"));
        assert!(!SqlEditorWidget::starts_with_plain_end("END WHILE"));
        assert!(!SqlEditorWidget::starts_with_plain_end("END IF;"));
        assert!(!SqlEditorWidget::starts_with_plain_end("END LOOP"));
        assert!(!SqlEditorWidget::starts_with_plain_end("END CASE"));
        assert!(!SqlEditorWidget::starts_with_plain_end("END REPEAT"));
        assert!(!SqlEditorWidget::starts_with_plain_end(
            "END BEFORE EACH ROW"
        ));
        assert!(!SqlEditorWidget::starts_with_plain_end(
            "END AFTER STATEMENT"
        ));
        assert!(!SqlEditorWidget::starts_with_plain_end("END INSTEAD OF"));
    }

    #[test]
    fn starts_with_bare_end_matches_only_unqualified_end() {
        assert!(SqlEditorWidget::starts_with_bare_end("END"));
        assert!(SqlEditorWidget::starts_with_bare_end("END;"));
        assert!(SqlEditorWidget::starts_with_bare_end("END -- keep"));
        assert!(SqlEditorWidget::starts_with_bare_end("END /* keep */"));
        assert!(!SqlEditorWidget::starts_with_bare_end("END pkg;"));
        assert!(!SqlEditorWidget::starts_with_bare_end("END IF;"));
    }

    #[test]
    fn starts_with_mysql_custom_delimited_end_accepts_mysql_custom_delimiters() {
        let bare_end = SqlEditorWidget::tokenize_sql("END");

        assert!(SqlEditorWidget::starts_with_block_terminating_end(
            "END$$",
            &SqlEditorWidget::tokenize_sql("END$$")
        ));
        assert!(SqlEditorWidget::starts_with_block_terminating_end(
            "END//",
            &SqlEditorWidget::tokenize_sql("END//")
        ));
        assert!(!SqlEditorWidget::starts_with_block_terminating_end(
            "END)",
            &SqlEditorWidget::tokenize_sql("END)")
        ));
        assert!(SqlEditorWidget::starts_with_block_terminating_end(
            "END", &bare_end
        ));
    }

    #[test]
    fn keyword_token_match_handles_exact_keyword_lines() {
        assert!(crate::sql_text::starts_with_keyword_token(
            "SELECT", "SELECT"
        ));
        assert!(crate::sql_text::starts_with_keyword_token("INTO", "INTO"));
        assert!(crate::sql_text::starts_with_keyword_token(
            "SELECT x", "SELECT"
        ));
        assert!(!crate::sql_text::starts_with_keyword_token(
            "SELECTED", "SELECT"
        ));
    }

    #[test]
    fn detects_set_transaction_as_first_statement() {
        let items = vec![ScriptItem::Statement(
            "SET TRANSACTION ISOLATION LEVEL SERIALIZABLE;".to_string(),
        )];
        assert!(SqlEditorWidget::requires_transaction_first_statement(
            &items
        ));
    }

    #[test]
    fn detects_alter_session_isolation_level_as_first_statement() {
        let items = vec![ScriptItem::Statement(
            "ALTER SESSION SET ISOLATION_LEVEL = SERIALIZABLE;".to_string(),
        )];
        assert!(SqlEditorWidget::requires_transaction_first_statement(
            &items
        ));
    }

    #[test]
    fn format_sql_basic_stays_stable_for_deeply_nested_scalar_subquery_parentheses() {
        let nesting = 256usize;
        let mut input = String::from("SELECT ");
        input.push_str(&"(".repeat(nesting));
        input.push_str("SELECT 1 FROM dual");
        input.push_str(&")".repeat(nesting));
        input.push_str(" AS nested_value\nFROM dual;");

        let formatted = SqlEditorWidget::format_sql_basic(&input);
        let formatted_again = SqlEditorWidget::format_sql_basic(&formatted);

        assert_eq!(
            formatted, formatted_again,
            "formatting should stay stable for deeply nested scalar subquery parentheses"
        );
        assert!(
            formatted.contains("FROM DUAL;"),
            "formatted output should preserve outer FROM clause, got:\n{formatted}"
        );
    }

    #[test]
    fn format_sql_basic_stays_stable_for_deeply_nested_from_subqueries() {
        let nesting = 160usize;
        let mut input = String::new();
        for _ in 0..nesting {
            input.push_str("SELECT * FROM (");
        }
        input.push_str("SELECT 1 AS v FROM dual");
        for depth in 1..=nesting {
            input.push_str(") q");
            input.push_str(&depth.to_string());
        }
        input.push(';');

        let formatted = SqlEditorWidget::format_sql_basic(&input);
        let formatted_again = SqlEditorWidget::format_sql_basic(&formatted);

        assert_eq!(
            formatted, formatted_again,
            "formatting should stay stable for deeply nested FROM subqueries"
        );
        assert!(
            formatted.contains(&format!("q{}", nesting)),
            "formatted output should preserve the outermost alias, got:\n{formatted}"
        );
    }

    #[test]
    fn cursor_mapping_tracks_prefix_after_full_reformat() {
        let source = "SELECT a, b FROM dual";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        let source_pos = source
            .find("b FROM")
            .expect("source cursor anchor should exist") as i32;
        let mapped = SqlEditorWidget::map_cursor_after_format(source, &formatted, source_pos);
        let mapped_slice = &formatted[mapped as usize..];
        assert!(
            mapped_slice.trim_start().starts_with("b\nFROM DUAL;"),
            "Mapped cursor should stay near the same token after reformat, got: {}",
            mapped_slice
        );
    }

    #[test]
    fn cursor_mapping_large_source_uses_fast_path_and_keeps_utf8_boundary() {
        let source = "x".repeat(super::CURSOR_MAPPING_FULL_REFORMAT_THRESHOLD_BYTES + 128);
        let formatted = "SELECT\n    1\nFROM DUAL;";
        let source_pos = (source.len() / 2) as i32;

        let mapped =
            SqlEditorWidget::map_cursor_after_format(&source, formatted, source_pos) as usize;

        assert!(
            mapped <= formatted.len(),
            "mapped cursor should stay in bounds"
        );
        assert!(
            formatted.is_char_boundary(mapped),
            "mapped cursor should stay on UTF-8 boundary"
        );
    }

    #[test]
    fn cursor_mapping_large_line_source_uses_fast_path_and_keeps_utf8_boundary() {
        let source = "x\n".repeat(super::LARGE_DOCUMENT_FORMAT_THRESHOLD_LINES + 1);
        assert!(
            source.len() < super::CURSOR_MAPPING_FULL_REFORMAT_THRESHOLD_BYTES,
            "line-threshold fast path test must stay below byte threshold"
        );

        let formatted = "SELECT\n    1\nFROM DUAL;";
        let source_pos = (source.len() / 2) as i32;

        let mapped =
            SqlEditorWidget::map_cursor_after_format(&source, formatted, source_pos) as usize;

        assert!(
            mapped <= formatted.len(),
            "mapped cursor should stay in bounds"
        );
        assert!(
            formatted.is_char_boundary(mapped),
            "mapped cursor should stay on UTF-8 boundary"
        );
    }

    #[test]
    fn cursor_mapping_selection_uses_selection_relative_offset() {
        let source = "SELECT a, b FROM dual";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        let source_pos_within_selection = source
            .find("b FROM")
            .expect("source cursor anchor should exist")
            as i32;
        let mapped_within_selection = SqlEditorWidget::map_cursor_after_format(
            source,
            &formatted,
            source_pos_within_selection,
        );
        let selection_start = 25i32;
        let final_cursor_pos = selection_start + mapped_within_selection;
        let formatted_slice = &formatted[mapped_within_selection as usize..];

        assert!(
            formatted_slice.trim_start().starts_with("b\nFROM DUAL;"),
            "Mapped cursor inside selection should stay near the same token after reformat, got: {}",
            formatted_slice
        );
        assert_eq!(
            final_cursor_pos,
            selection_start + mapped_within_selection,
            "Selection-relative mapping should compose with selection offset"
        );
    }

    #[test]
    fn cursor_mapping_selection_keeps_token_anchor_with_canonical_terminator() {
        let source = "SELECT a, b FROM dual";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        let source_pos_within_selection = source
            .find("b FROM")
            .expect("source cursor anchor should exist")
            as i32;

        let mapped_within_selection = SqlEditorWidget::map_cursor_after_format(
            source,
            &formatted,
            source_pos_within_selection,
        );
        let formatted_slice = &formatted[mapped_within_selection as usize..];

        assert!(
            formatted_slice.trim_start().starts_with("b\nFROM DUAL"),
            "Mapped cursor should stay near same token after selection format, got: {}",
            formatted_slice
        );
        assert!(
            formatted.trim_end().ends_with(';'),
            "Selection formatting should keep canonical semicolon terminator"
        );
    }

    #[test]
    fn cursor_mapping_selected_auto_format_without_terminator_keeps_inline_comment_anchor() {
        let source = "SELECT 1 FROM dual -- trailing note";
        let formatted = SqlEditorWidget::format_for_auto_formatting(source, true);
        let source_pos_within_selection = source
            .find("-- trailing note")
            .expect("source inline comment anchor should exist")
            as i32;

        let mapped_within_selection = SqlEditorWidget::map_cursor_after_format(
            source,
            &formatted,
            source_pos_within_selection,
        );
        let mapped_slice = &formatted[mapped_within_selection as usize..];

        assert!(
            mapped_slice.starts_with("-- trailing note"),
            "Selected formatting without terminator should keep inline-comment cursor anchor, got: {}",
            mapped_slice
        );
    }

    #[test]
    fn cursor_mapping_uses_utf8_byte_offsets() {
        let source = "SELECT 한글, b FROM dual";
        let formatted = SqlEditorWidget::format_sql_basic(source);

        let byte_offset = source
            .find("b FROM")
            .expect("source cursor anchor should exist") as i32;

        let mapped = SqlEditorWidget::map_cursor_after_format(source, &formatted, byte_offset);
        let mapped_slice = &formatted[mapped as usize..];
        assert!(
            mapped_slice.trim_start().starts_with("b\nFROM DUAL;"),
            "Mapped cursor should stay near token with byte-offset mapping, got: {}",
            mapped_slice
        );
    }

    #[test]
    fn cursor_mapping_selected_auto_format_keeps_slash_terminator_anchor() {
        let source = "SELECT 1 FROM dual\n/";
        let formatted = SqlEditorWidget::format_for_auto_formatting(source, true);
        let source_pos = source.find('/').expect("slash anchor should exist") as i32;

        let mapped = SqlEditorWidget::map_cursor_after_format_with_policy(
            source, &formatted, source_pos, true,
        );
        let mapped_slice = &formatted[mapped as usize..];

        assert!(
            mapped_slice.trim_start().starts_with('/'),
            "Selected auto-format cursor mapping should keep slash terminator anchor, got: {}",
            mapped_slice
        );
        assert!(
            formatted.contains("DUAL;\n/"),
            "Selected auto-format should keep the canonical semicolon before SQL*Plus slash terminator, got:\n{}",
            formatted
        );
    }

    #[test]
    fn normalize_index_treats_input_as_byte_offset() {
        let source = "SELECT éa, b FROM dual";
        let byte_offset = source
            .find('b')
            .expect("expected cursor anchor should exist") as i32;

        let normalized = SqlEditorWidget::normalize_index(source, byte_offset);
        assert_eq!(
            normalized, byte_offset as usize,
            "normalize_index should preserve byte offsets as-is"
        );
    }

    #[test]
    fn normalize_index_clamps_non_boundary_utf8_byte_offset() {
        let source = "SELECT 한글, b FROM dual";
        let utf8_start = source.find('한').expect("expected utf-8 anchor");
        let mid_char_offset = utf8_start + 1;
        let normalized = SqlEditorWidget::normalize_index(source, mid_char_offset as i32);
        assert_eq!(
            normalized, utf8_start,
            "normalize_index should clamp invalid UTF-8 byte offsets"
        );
    }

    #[test]
    fn normalize_index_clamps_invalid_utf8_boundary_without_panic() {
        let source = "SELECT 한글, b FROM dual";
        let mid_char_index = source.find("한").expect("expected unicode anchor") + 1;

        let normalized = SqlEditorWidget::normalize_index(source, mid_char_index as i32);
        assert!(source.is_char_boundary(normalized));
        assert!(normalized <= source.len());
    }

    #[test]
    fn format_sql_basic_handles_unterminated_q_quote_without_panic() {
        let sql = "SELECT q'[unterminated FROM dual";
        let result = std::panic::catch_unwind(|| SqlEditorWidget::format_sql_basic(sql));
        assert!(
            result.is_ok(),
            "formatter should not panic on unterminated q-quote literal"
        );
    }

    #[test]
    fn selected_auto_format_cursor_mapping_matches_selected_terminator_policy() {
        let source = "select 1 from dual";
        let formatted = SqlEditorWidget::format_for_auto_formatting(source, true);
        let source_pos = source.find("dual").unwrap_or(0) as i32;

        let mapped = SqlEditorWidget::map_cursor_after_format_with_policy(
            source, &formatted, source_pos, true,
        ) as usize;

        assert!(
            mapped <= formatted.len() && formatted.is_char_boundary(mapped),
            "mapped cursor should stay on a valid UTF-8 boundary"
        );
        assert!(
            formatted[mapped..].trim_start().starts_with("DUAL"),
            "cursor should remain anchored near DUAL token after selected formatting, got: {}",
            &formatted[mapped..]
        );
    }

    #[test]
    fn selected_auto_format_cursor_mapping_handles_trailing_inline_comment_without_injected_semicolon(
    ) {
        let source = "select 1 from dual -- trailing note";
        let formatted = SqlEditorWidget::format_for_auto_formatting(source, true);
        let source_pos = source.find("-- trailing note").unwrap_or(0) as i32;

        let mapped = SqlEditorWidget::map_cursor_after_format_with_policy(
            source, &formatted, source_pos, true,
        ) as usize;

        assert!(
            formatted[mapped..]
                .trim_start()
                .starts_with("-- trailing note"),
            "cursor should remain near inline comment after selected formatting, got: {}",
            &formatted[mapped..]
        );
        assert!(
            !formatted.trim_end().ends_with(';'),
            "selected formatting should preserve missing semicolon"
        );
    }
    #[test]
    fn map_cursor_after_format_clamps_mid_utf8_byte_without_panic() {
        let source = "SELECT 한글, b FROM dual";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        let mid_byte = source.find('한').unwrap_or(0) + 1;

        let result = std::panic::catch_unwind(|| {
            SqlEditorWidget::map_cursor_after_format(source, &formatted, mid_byte as i32)
        });

        assert!(
            result.is_ok(),
            "cursor mapping should not panic on mid-byte cursor offset"
        );
    }

    #[test]
    fn choose_execution_error_message_prioritizes_timeout_over_cancel() {
        let message = SqlEditorWidget::choose_execution_error_message(
            true,
            true,
            Some(Duration::from_secs(9)),
            "ORA-01013".to_string(),
        );
        assert_eq!(message, "Query timed out after 9 seconds");
    }

    #[test]
    fn choose_execution_error_message_uses_cancel_when_not_timed_out() {
        let message = SqlEditorWidget::choose_execution_error_message(
            true,
            false,
            Some(Duration::from_secs(9)),
            "ORA-01013".to_string(),
        );
        assert_eq!(message, "Query cancelled");
    }

    #[test]
    fn choose_execution_error_message_falls_back_to_original_error() {
        let message = SqlEditorWidget::choose_execution_error_message(
            false,
            false,
            Some(Duration::from_secs(9)),
            "ORA-00001: unique constraint".to_string(),
        );
        assert_eq!(message, "ORA-00001: unique constraint");
    }

    #[test]
    fn timeout_error_message_signal_detects_dpi_call_timeout() {
        assert!(
            SqlEditorWidget::timeout_error_message_contains_timeout_signal(
                "DPI-1067: call timeout of 5000 ms reached",
            )
        );
    }

    #[test]
    fn timeout_error_message_signal_detects_timeout_keyword_with_ora_01013() {
        assert!(
            SqlEditorWidget::timeout_error_message_contains_timeout_signal(
                "ORA-01013: user requested cancel of current operation (call timeout reached)",
            )
        );
    }

    #[test]
    fn timeout_error_message_signal_does_not_treat_plain_cancel_as_timeout() {
        assert!(
            !SqlEditorWidget::timeout_error_message_contains_timeout_signal(
                "ORA-01013: user requested cancel of current operation",
            )
        );
    }

    #[test]
    fn timeout_error_message_signal_does_not_treat_lock_wait_timeout_expired_as_call_timeout() {
        assert!(
            !SqlEditorWidget::timeout_error_message_contains_timeout_signal(
                "ORA-00054: resource busy and acquire with NOWAIT specified or timeout expired",
            )
        );
    }

    #[test]
    fn post_execution_output_is_skipped_when_cancel_requested() {
        assert!(!SqlEditorWidget::should_capture_post_execution_output(
            true, false, false
        ));
    }

    #[test]
    fn post_execution_output_is_skipped_when_timed_out() {
        assert!(!SqlEditorWidget::should_capture_post_execution_output(
            false, true, false
        ));
    }

    #[test]
    fn post_execution_output_is_skipped_when_execution_should_stop() {
        assert!(!SqlEditorWidget::should_capture_post_execution_output(
            false, false, true
        ));
    }

    #[test]
    fn post_execution_output_is_allowed_for_normal_completion() {
        assert!(SqlEditorWidget::should_capture_post_execution_output(
            false, false, false
        ));
    }

    #[test]
    fn flush_buffered_rows_drops_pending_rows_when_interrupted() {
        let (sender, receiver) = mpsc::channel();
        let session = Arc::new(Mutex::new(SessionState::default()));
        let mut buffered_rows = vec![vec!["1".to_string()], vec!["2".to_string()]];

        SqlEditorWidget::flush_buffered_rows(&sender, &session, 7, &mut buffered_rows, true);

        assert!(buffered_rows.is_empty());
        assert!(receiver.try_recv().is_err());
    }

    #[test]
    fn flush_buffered_rows_emits_rows_when_not_interrupted() {
        let (sender, receiver) = mpsc::channel();
        let session = Arc::new(Mutex::new(SessionState::default()));
        let mut buffered_rows = vec![vec!["1".to_string()], vec!["2".to_string()]];

        SqlEditorWidget::flush_buffered_rows(&sender, &session, 3, &mut buffered_rows, false);

        assert!(buffered_rows.is_empty());
        let message = receiver
            .try_recv()
            .unwrap_or_else(|err| panic!("expected buffered rows progress message: {err}"));
        match message {
            QueryProgress::Rows { index, rows } => {
                assert_eq!(index, 3);
                assert_eq!(rows.len(), 2);
            }
            _ => panic!("expected QueryProgress::Rows"),
        }
    }

    #[test]
    fn flush_buffered_result_rows_emits_display_rows() {
        let (sender, receiver) = mpsc::channel();
        let session = Arc::new(Mutex::new(SessionState::default()));
        let mut buffered_display_rows = vec![vec!["(null)".to_string()], vec!["2".to_string()]];
        let mut buffered_raw_rows = vec![vec!["".to_string()], vec!["2".to_string()]];

        SqlEditorWidget::flush_buffered_result_rows(
            &sender,
            &session,
            9,
            &mut buffered_display_rows,
            &mut buffered_raw_rows,
        );

        assert!(buffered_display_rows.is_empty());
        assert!(buffered_raw_rows.is_empty());
        let message = receiver
            .try_recv()
            .unwrap_or_else(|err| panic!("expected result rows progress message: {err}"));
        match message {
            QueryProgress::Rows { index, rows } => {
                assert_eq!(index, 9);
                assert_eq!(
                    rows,
                    vec![vec!["(null)".to_string()], vec!["2".to_string()]]
                );
            }
            _ => panic!("expected QueryProgress::Rows"),
        }
    }

    #[test]
    fn emit_select_result_uses_streaming_sized_initial_batch() {
        let (sender, receiver) = mpsc::channel();
        let session = Arc::new(Mutex::new(SessionState::default()));
        let expected_row_count = PROGRESS_ROWS_INITIAL_BATCH + 1;
        let rows = (0..expected_row_count)
            .map(|index| vec![format!("value_{index}")])
            .collect::<Vec<Vec<String>>>();

        SqlEditorWidget::emit_select_result(
            &sender,
            &session,
            "LOCAL",
            4,
            "select * from dual",
            vec!["COL1".to_string()],
            rows,
            true,
            true,
        );

        let messages = receiver.try_iter().collect::<Vec<QueryProgress>>();
        assert_eq!(messages.len(), 5);
        match &messages[0] {
            QueryProgress::StatementStart { index } => assert_eq!(*index, 4),
            _ => panic!("expected QueryProgress::StatementStart"),
        }
        match &messages[1] {
            QueryProgress::SelectStart {
                index,
                columns,
                null_text: _,
            } => {
                assert_eq!(*index, 4);
                assert_eq!(columns, &vec!["COL1".to_string()]);
            }
            _ => panic!("expected QueryProgress::SelectStart"),
        }
        match &messages[2] {
            QueryProgress::Rows { index, rows } => {
                assert_eq!(*index, 4);
                assert_eq!(rows.len(), PROGRESS_ROWS_INITIAL_BATCH);
            }
            _ => panic!("expected initial QueryProgress::Rows"),
        }
        match &messages[3] {
            QueryProgress::Rows { index, rows } => {
                assert_eq!(*index, 4);
                assert_eq!(rows.len(), 1);
            }
            _ => panic!("expected trailing QueryProgress::Rows"),
        }
        match &messages[4] {
            QueryProgress::StatementFinished {
                index,
                result,
                connection_name,
                timed_out,
            } => {
                assert_eq!(*index, 4);
                assert_eq!(connection_name, "LOCAL");
                assert!(!timed_out);
                assert_eq!(result.row_count, expected_row_count);
            }
            _ => panic!("expected QueryProgress::StatementFinished"),
        }
    }

    #[test]
    fn plsql_like_detection_ignores_begin_inside_strings_or_comments() {
        assert!(!SqlEditorWidget::is_plsql_like_statement(
            "SELECT 'BEGIN' AS txt FROM dual;"
        ));
        assert!(!SqlEditorWidget::is_plsql_like_statement(
            "/* DECLARE */ SELECT 1 FROM dual;"
        ));
        assert!(SqlEditorWidget::is_plsql_like_statement(
            "CREATE OR REPLACE PROCEDURE p IS BEGIN NULL; END;"
        ));
    }

    #[test]
    fn plsql_like_detection_ignores_explain_and_open_for() {
        assert!(!SqlEditorWidget::is_plsql_like_statement(
            "EXPLAIN PLAN FOR SELECT 1 FROM dual;"
        ));
        assert!(!SqlEditorWidget::is_plsql_like_statement(
            "OPEN p_rc FOR SELECT empno FROM oqt_t_emp;"
        ));
    }

    #[test]
    fn plsql_like_detection_supports_with_function_factoring() {
        assert!(SqlEditorWidget::is_plsql_like_statement(
            "WITH FUNCTION format_name(p_name IN VARCHAR2) RETURN VARCHAR2 IS\nBEGIN\n  RETURN INITCAP(p_name);\nEND;\nSELECT * FROM dual;"
        ));
    }

    #[test]
    fn plsql_like_detection_supports_or_replace_force_procedure() {
        assert!(SqlEditorWidget::is_plsql_like_statement(
            "CREATE OR REPLACE FORCE PROCEDURE test_proc AS\nBEGIN\n  NULL;\nEND;"
        ));
    }

    #[test]
    fn plsql_like_detection_supports_or_replace_editionable_function() {
        assert!(SqlEditorWidget::is_plsql_like_statement(
            "CREATE OR REPLACE EDITIONABLE FUNCTION test_fn RETURN NUMBER IS\nBEGIN\n  RETURN 1;\nEND;"
        ));
    }

    #[test]
    fn plsql_like_detection_supports_package_body() {
        assert!(SqlEditorWidget::is_plsql_like_statement(
            "CREATE PACKAGE BODY test_pkg AS\n  PROCEDURE proc IS\n  BEGIN\n    NULL;\n  END;\nEND;"
        ));
    }

    #[test]
    fn plsql_like_detection_supports_no_force_function() {
        assert!(SqlEditorWidget::is_plsql_like_statement(
            "CREATE NO FORCE FUNCTION test_fn RETURN NUMBER IS\nBEGIN\n  RETURN 1;\nEND;"
        ));
    }

    #[test]
    fn plsql_like_detection_rejects_create_materialized_view() {
        assert!(!SqlEditorWidget::is_plsql_like_statement(
            "CREATE MATERIALIZED VIEW test_mv AS SELECT 1 FROM dual"
        ));
    }

    #[test]
    fn plsql_like_detection_rejects_create_materialized_view_log() {
        assert!(!SqlEditorWidget::is_plsql_like_statement(
            "CREATE MATERIALIZED VIEW LOG ON test_table"
        ));
    }

    #[test]
    fn plsql_like_detection_rejects_create_view() {
        assert!(!SqlEditorWidget::is_plsql_like_statement(
            "CREATE OR REPLACE VIEW test_view AS SELECT 1 FROM dual"
        ));
    }

    #[test]
    fn trigger_audit_block_keeps_expected_header_and_values_alignment() {
        let sql = r#"create or replace noneditionable trigger "SYSTEM"."OQT_TRG_MEG_CUD" after insert or update or delete on oqt_meg_master for each row begin if inserting then insert into oqt_meg_audit(event_type, table_name, pk_text, detail_text) values ('INSERT', 'OQT_MEG_MASTER', 'master_id='||:NEW.master_id, 'key='||:NEW.master_key||', status='||:NEW.status||', amount='||TO_CHAR(:NEW.amount)); elsif updating then insert into oqt_meg_audit(event_type, table_name, pk_text, detail_text) values ('UPDATE', 'OQT_MEG_MASTER', 'master_id='||:NEW.master_id, 'status:'||:OLD.status||'->'||:NEW.status||', amount:'||TO_CHAR(:OLD.amount)||'->'||TO_CHAR(:NEW.amount)); elsif deleting then insert into oqt_meg_audit(event_type, table_name, pk_text, detail_text) values ('DELETE', 'OQT_MEG_MASTER', 'master_id='||:OLD.master_id, 'key='||:OLD.master_key||', status='||:OLD.status||', amount='||TO_CHAR(:OLD.amount)); end if; end; alter trigger "SYSTEM"."OQT_TRG_MEG_CUD" enable"#;
        let formatted = SqlEditorWidget::format_sql_basic(sql);
        assert!(
            formatted.contains("\n    AFTER INSERT OR UPDATE OR DELETE\n    ON oqt_meg_master"),
            "Trigger ON clause should start on its own trigger header line, got:\n{}",
            formatted
        );
        assert!(
            formatted.contains("\n    FOR EACH ROW\nBEGIN"),
            "FOR EACH ROW should align with trigger header indentation, got:\n{}",
            formatted
        );
        assert!(
            formatted.contains("IF INSERTING THEN")
                && formatted.contains("ELSIF UPDATING THEN")
                && formatted.contains("ELSIF DELETING THEN"),
            "Conditional trigger predicates should be uppercased in IF/ELSIF blocks, got:\n{}",
            formatted
        );
        assert!(
            formatted.contains("END;\nALTER TRIGGER \"SYSTEM\".\"OQT_TRG_MEG_CUD\" ENABLE;"),
            "CREATE/ALTER trigger pair should not be separated by a blank line, got:\n{}",
            formatted
        );
    }

    #[test]
    fn preserve_selected_text_terminator_does_not_add_semicolon_when_selection_had_none() {
        let source = "SELECT 1 FROM dual";
        let formatted = SqlEditorWidget::format_sql_basic(source);

        let preserved = SqlEditorWidget::preserve_selected_text_terminator(source, formatted);
        assert_eq!(
            preserved.trim_end(),
            "SELECT 1
FROM DUAL"
        );
        assert!(!preserved.trim_end().ends_with(';'));
    }

    #[test]
    fn selected_auto_formatting_path_keeps_terminator_before_inline_comment() {
        let source = "select 1 from dual -- trailing note";

        let formatted = SqlEditorWidget::format_for_auto_formatting(source, true);

        assert_eq!(
            formatted,
            "SELECT 1
FROM DUAL; -- trailing note",
            "Selected formatting should keep the canonical statement terminator before inline comment, got:
{}",
            formatted
        );
    }

    #[test]
    fn selected_auto_formatting_path_keeps_terminator_before_newline_comment() {
        let source = "select 1 from dual
-- trailing note";

        let formatted = SqlEditorWidget::format_for_auto_formatting(source, true);

        assert_eq!(
            formatted,
            "SELECT 1
FROM DUAL;
-- trailing note",
            "Selected formatting should keep the canonical statement terminator before newline comment, got:
{}",
            formatted
        );
    }

    #[test]
    fn selected_auto_formatting_path_keeps_canonical_semicolon_for_single_statement() {
        let source = "SELECT 1 FROM dual";

        let formatted = SqlEditorWidget::format_for_auto_formatting(source, true);

        assert_eq!(
            formatted.trim_end(),
            "SELECT 1
FROM DUAL;"
        );
        assert!(
            formatted.trim_end().ends_with(';'),
            "Selected auto-formatting should keep the canonical terminator for a selected statement, got:
{}",
            formatted
        );
    }

    #[test]
    fn full_auto_formatting_path_keeps_canonical_statement_semicolon() {
        let source = "SELECT 1 FROM dual";

        let formatted = SqlEditorWidget::format_for_auto_formatting(source, false);

        assert!(
            formatted.trim_end().ends_with(';'),
            "Full-buffer formatting should keep canonical statement semicolon, got:
{}",
            formatted
        );
    }

    #[test]
    fn selected_auto_formatting_keeps_semicolons_for_multi_statement_selection() {
        let source = "SELECT 1 FROM dual;\nSELECT 2 FROM dual;\nCOMMIT;";

        let formatted = SqlEditorWidget::format_for_auto_formatting(source, true);

        assert!(
            formatted.contains("SELECT 1\nFROM DUAL;"),
            "First statement semicolon should remain in multi-statement selection, got:\n{}",
            formatted
        );
        assert!(
            formatted.contains("SELECT 2\nFROM DUAL;"),
            "Second statement semicolon should remain in multi-statement selection, got:\n{}",
            formatted
        );
        assert!(
            formatted.contains("COMMIT;"),
            "Trailing COMMIT semicolon should remain in multi-statement selection, got:\n{}",
            formatted
        );

        let items = QueryExecutor::split_script_items(&formatted);
        assert_eq!(
            count_statement_items(&items),
            3,
            "Selected formatting must keep all statement boundaries intact, got: {items:?}"
        );
    }

    #[test]
    fn selected_auto_formatting_test15_keeps_statement_boundaries_for_ctrl_enter() {
        let source = load_formatter_test_file("test15.sql");
        let formatted = SqlEditorWidget::format_for_auto_formatting(&source, true);

        // Re-splitting the formatted text must preserve execution boundaries so
        // statement-at-cursor keeps working after select-all formatting.
        let original_items = QueryExecutor::split_script_items(&source);
        let formatted_items = QueryExecutor::split_script_items(&formatted);

        assert_eq!(
            count_statement_items(&formatted_items),
            count_statement_items(&original_items),
            "Selected formatting changed test15.sql statement count"
        );
        assert_eq!(
            count_tool_command_items(&formatted_items),
            count_tool_command_items(&original_items),
            "Selected formatting changed test15.sql tool command count"
        );

        let cursor = formatted
            .find("qt_splitter_boss IS")
            .expect("expected COMMENT ON TABLE statement after selected formatting");
        let bounds = QueryExecutor::statement_bounds_at_cursor(&formatted, cursor)
            .expect("expected COMMENT ON TABLE bounds after selected formatting");
        let statement = &formatted[bounds.0..bounds.1];

        assert!(
            statement.trim_start().starts_with("COMMENT")
                && statement.contains("ON TABLE qt_splitter_boss IS"),
            "Ctrl+Enter boundary should stay on COMMENT statement after selected formatting, got:\n{}",
            statement
        );
        assert!(
            !statement.contains("CREATE OR REPLACE PROCEDURE qt_splitter_proc"),
            "Selected formatting must not merge earlier routine into COMMENT statement, got:\n{}",
            statement
        );
    }

    #[test]
    fn selected_auto_formatting_test16_keeps_statement_boundaries_for_ctrl_enter() {
        let source = load_formatter_test_file("test16.sql");
        let formatted = SqlEditorWidget::format_for_auto_formatting(&source, true);

        // Re-splitting the formatted text must preserve execution boundaries so
        // statement-at-cursor keeps working after select-all formatting.
        let original_items = QueryExecutor::split_script_items(&source);
        let formatted_items = QueryExecutor::split_script_items(&formatted);

        assert_eq!(
            count_statement_items(&formatted_items),
            count_statement_items(&original_items),
            "Selected formatting changed test16.sql statement count"
        );
        assert_eq!(
            count_tool_command_items(&formatted_items),
            count_tool_command_items(&original_items),
            "Selected formatting changed test16.sql tool command count"
        );

        let cursor = formatted
            .find("qt_splitter_ultimate IS")
            .expect("expected COMMENT ON TABLE statement after selected formatting");
        let bounds = QueryExecutor::statement_bounds_at_cursor(&formatted, cursor)
            .expect("expected COMMENT ON TABLE bounds after selected formatting");
        let statement = &formatted[bounds.0..bounds.1];

        assert!(
            statement.trim_start().starts_with("COMMENT")
                && statement.contains("ON TABLE qt_splitter_ultimate IS"),
            "Ctrl+Enter boundary should stay on COMMENT statement after selected formatting, got:\n{}",
            statement
        );
        assert!(
            !statement.contains("CREATE OR REPLACE PACKAGE BODY qt_splitter_ultimate_pkg"),
            "Selected formatting must not merge earlier package body into COMMENT statement, got:\n{}",
            statement
        );
    }

    #[test]
    fn full_auto_formatting_test20_keeps_statement_boundaries() {
        let source = load_formatter_test_file("test20.sql");
        let formatted = SqlEditorWidget::format_for_auto_formatting(&source, false);

        let original_items = QueryExecutor::split_script_items(&source);
        let formatted_items = QueryExecutor::split_script_items(&formatted);

        assert_eq!(
            count_statement_items(&formatted_items),
            count_statement_items(&original_items),
            "Full auto-formatting changed test20.sql statement count"
        );
        assert_eq!(
            count_tool_command_items(&formatted_items),
            count_tool_command_items(&original_items),
            "Full auto-formatting changed test20.sql tool command count"
        );

        let cursor = formatted
            .find("AS msg_preview")
            .expect("expected final audit preview query after full auto-formatting");
        let bounds = QueryExecutor::statement_bounds_at_cursor(&formatted, cursor)
            .expect("expected final audit preview query bounds after full auto-formatting");
        let statement = &formatted[bounds.0..bounds.1];

        assert!(
            statement.trim_start().starts_with("SELECT")
                && statement.contains("FROM qt_fb_audit")
                && statement.contains("AS msg_preview"),
            "Full auto-formatting should keep the final audit preview query isolated, got:\n{}",
            statement
        );
        assert!(
            !statement.contains("FROM qt_fb_view"),
            "Full auto-formatting must not merge the preceding view query into the audit preview query, got:\n{}",
            statement
        );
        assert!(
            !statement.contains("p_module => 'final_validation'"),
            "Full auto-formatting must not merge the final validation block into the audit preview query, got:\n{}",
            statement
        );
    }

    #[test]
    fn full_auto_formatting_test21_keeps_statement_boundaries() {
        let source = load_formatter_test_file("test21.sql");
        let formatted = SqlEditorWidget::format_for_auto_formatting(&source, false);

        let original_items = QueryExecutor::split_script_items(&source);
        let formatted_items = QueryExecutor::split_script_items(&formatted);

        assert_eq!(
            count_statement_items(&formatted_items),
            count_statement_items(&original_items),
            "Full auto-formatting changed test21.sql statement count"
        );
        assert_eq!(
            count_tool_command_items(&formatted_items),
            count_tool_command_items(&original_items),
            "Full auto-formatting changed test21.sql tool command count"
        );

        let cursor = formatted
            .find("AS err_msg_preview")
            .expect("expected final error preview query after full auto-formatting");
        let bounds = QueryExecutor::statement_bounds_at_cursor(&formatted, cursor)
            .expect("expected final error preview query bounds after full auto-formatting");
        let statement = &formatted[bounds.0..bounds.1];

        assert!(
            statement.trim_start().starts_with("SELECT")
                && statement.contains("FROM qt_x_err_log")
                && statement.contains("AS err_msg_preview"),
            "Full auto-formatting should keep the final error preview query isolated, got:\n{}",
            statement
        );
        assert!(
            !statement.contains("FROM qt_x_audit"),
            "Full auto-formatting must not merge the preceding audit preview query into the error preview query, got:\n{}",
            statement
        );
        assert!(
            !statement.contains("p_module => 'final_validation'"),
            "Full auto-formatting must not merge the final validation block into the error preview query, got:\n{}",
            statement
        );
    }

    #[test]
    fn format_sql_basic_keeps_trigger_slash_and_alter_trigger_tightly_grouped() {
        let sql = r#"CREATE OR REPLACE TRIGGER trg_demo
BEFORE INSERT ON demo
BEGIN
    NULL;
END;
/
ALTER TRIGGER trg_demo ENABLE;"#;

        let formatted = SqlEditorWidget::format_sql_basic(sql);

        assert!(
            formatted.contains("END;\n/\nALTER TRIGGER trg_demo ENABLE;"),
            "CREATE TRIGGER + slash + ALTER TRIGGER should stay tightly grouped, got:\n{}",
            formatted
        );
        assert!(
            !formatted.contains("END;\n/\n\nALTER TRIGGER"),
            "Unexpected blank line inserted between slash and ALTER TRIGGER, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_sql_basic_places_statement_semicolon_before_trailing_line_comment() {
        let source = "SELECT 1 FROM dual -- trailing note";

        let formatted = SqlEditorWidget::format_sql_basic(source);

        assert!(
            formatted.contains("FROM DUAL; -- trailing note"),
            "Formatter should place statement terminator before trailing line comment, got:
{}",
            formatted
        );
        assert!(
            !formatted.trim_end().ends_with("note;"),
            "Formatter should not append semicolon into trailing line comment text, got:
{}",
            formatted
        );
    }

    #[test]
    fn format_sql_basic_places_statement_semicolon_before_trailing_block_comment() {
        let source = "SELECT 1 FROM dual /* trailing block */";

        let formatted = SqlEditorWidget::format_sql_basic(source);

        assert!(
            formatted.contains("FROM DUAL;"),
            "Formatter should place statement terminator before trailing block comment, got:\n{}",
            formatted
        );
        let semicolon_idx = formatted.find(';');
        let comment_idx = formatted.find("/* trailing block */");
        assert!(
            matches!((semicolon_idx, comment_idx), (Some(semicolon), Some(comment)) if semicolon < comment),
            "Statement terminator must appear before trailing block comment, got:\n{}",
            formatted
        );
        assert!(
            !formatted.trim_end().ends_with("*/;"),
            "Formatter should not append semicolon after trailing block comment, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_sql_basic_does_not_insert_space_before_newline_line_comment() {
        let source = "SELECT 1 FROM dual\n-- trailing note";

        let formatted = SqlEditorWidget::format_sql_basic(source);

        assert!(
            formatted.contains("FROM DUAL;\n-- trailing note"),
            "Formatter should insert semicolon without trailing space before newline comment, got:\n{}",
            formatted
        );
        assert!(
            !formatted.contains("; \n-- trailing note"),
            "Formatter should not leave whitespace before newline-attached line comment, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_sql_basic_keeps_comma_indent_after_line_comment_in_select_list() {
        let source = "select abc -- comment\n, def\nfrom efg;";

        let formatted = SqlEditorWidget::format_sql_basic(source);

        assert!(
            formatted.contains("SELECT\n    abc -- comment\n    ,\n    def\nFROM efg;"),
            "Formatter should keep select-list depth for leading comma after line comment, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_sql_basic_does_not_insert_space_before_newline_block_comment() {
        let source = "SELECT 1 FROM dual\n/* trailing block */";

        let formatted = SqlEditorWidget::format_sql_basic(source);

        assert!(
            formatted.contains("FROM DUAL;\n/* trailing block */"),
            "Formatter should insert semicolon without trailing space before newline block comment, got:\n{}",
            formatted
        );
        assert!(
            !formatted.contains("; \n/* trailing block */"),
            "Formatter should not leave whitespace before newline-attached block comment, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_sql_basic_does_not_insert_space_before_newline_comments_in_select_clause_from_user_case(
    ) {
        let source = r#"SELECT
d.deptno,
d.dname,
-- [D] scalar subquery
(
/* [E] correlated max */
SELECT MAX(e2.sal)
FROM emp e2
WHERE e2.deptno = d.deptno
) AS max_sal
FROM dept d;"#;

        let formatted = SqlEditorWidget::format_sql_basic(source);

        assert!(
            !formatted.contains("d.dname,\n\n"),
            "Formatter inserted an extra blank line before a newline-attached line comment in SELECT, got:\n{}",
            formatted
        );
        assert!(
            !formatted.contains("(\n\n"),
            "Formatter inserted an extra blank line before a newline-attached block comment in SELECT, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_sql_basic_keeps_join_condition_depth_after_inline_block_comment_on_clause() {
        let source =
            "select * from paid p join amounts a on /* join key */\na.order_id = p.order_id;";
        let formatted = SqlEditorWidget::format_sql_basic(source);

        assert!(
            formatted.contains("JOIN amounts a\n    ON /* join key */\n    a.order_id = p.order_id;"),
            "Inline block comment after ON should keep the following join condition on ON-clause depth, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_sql_basic_keeps_join_condition_depth_after_inline_line_comment_on_clause() {
        let source = "select * from paid p join amounts a on -- join key\na.order_id = p.order_id;";
        let formatted = SqlEditorWidget::format_sql_basic(source);

        assert!(
            formatted.contains("JOIN amounts a\n    ON -- join key\n    a.order_id = p.order_id;"),
            "Inline line comment after ON should keep the following join condition on ON-clause depth, got:\n{}",
            formatted
        );
    }

    #[test]
    fn preserve_selected_text_terminator_removes_inserted_semicolon_before_trailing_comment() {
        let source = "SELECT 1 FROM dual -- trailing note";
        let formatted = SqlEditorWidget::format_sql_basic(source);

        let preserved = SqlEditorWidget::preserve_selected_text_terminator(source, formatted);
        assert!(
            !SqlEditorWidget::statement_ends_with_semicolon(&preserved),
            "Semicolon should be removed when original selection had no terminator, got:
{}",
            preserved
        );
        assert!(
            preserved.trim_end().ends_with("-- trailing note"),
            "Trailing comment should be preserved, got:
{}",
            preserved
        );
    }

    #[test]
    fn preserve_selected_text_terminator_removes_inserted_semicolon_before_newline_comment() {
        let source = "SELECT 1 FROM dual\n-- trailing note";
        let formatted = SqlEditorWidget::format_sql_basic(source);

        let preserved = SqlEditorWidget::preserve_selected_text_terminator(source, formatted);
        assert!(
            !preserved.contains(";\n-- trailing note"),
            "Selection-preserved formatting should remove inserted semicolon for newline comment attachment, got:\n{}",
            preserved
        );
    }

    #[test]
    fn preserve_selected_text_terminator_removes_inserted_semicolon_before_trailing_block_comment()
    {
        let source = "SELECT 1 FROM dual /* trailing block */";
        let formatted = SqlEditorWidget::format_sql_basic(source);

        let preserved = SqlEditorWidget::preserve_selected_text_terminator(source, formatted);
        assert!(
            !SqlEditorWidget::statement_ends_with_semicolon(&preserved),
            "Semicolon should be removed when original selection had no terminator, got:\n{}",
            preserved
        );
        assert!(
            preserved.trim_end().ends_with("/* trailing block */"),
            "Trailing block comment should be preserved, got:\n{}",
            preserved
        );
    }

    #[test]
    fn preserve_selected_text_terminator_removes_inserted_semicolon_before_newline_block_comment() {
        let source = "SELECT 1 FROM dual\n/* trailing block */";
        let formatted = SqlEditorWidget::format_sql_basic(source);

        let preserved = SqlEditorWidget::preserve_selected_text_terminator(source, formatted);
        assert!(
            !preserved.contains(";\n/* trailing block */"),
            "Selection-preserved formatting should remove inserted semicolon for newline block comment attachment, got:\n{}",
            preserved
        );
    }

    #[test]
    fn preserve_selected_text_terminator_removes_inserted_semicolon_when_string_has_comment_markers(
    ) {
        let source = "SELECT '-- keep literal' AS txt FROM dual";
        let formatted = SqlEditorWidget::format_sql_basic(source);

        let preserved = SqlEditorWidget::preserve_selected_text_terminator(source, formatted);

        assert!(
            !SqlEditorWidget::statement_ends_with_semicolon(&preserved),
            "Semicolon should be removed when original selection had no terminator, got:\n{}",
            preserved
        );
        assert!(
            preserved.contains("'-- keep literal'"),
            "String literal containing comment markers should be preserved, got:\n{}",
            preserved
        );
    }

    #[test]
    fn preserve_selected_text_terminator_handles_multibyte_text_before_comment() {
        let formatted = "SELECT '한글' FROM dual;".to_string();
        let without_semicolon = SqlEditorWidget::remove_trailing_statement_semicolon(&formatted)
            .expect("trailing semicolon should be removable");
        assert_eq!(without_semicolon, "SELECT '한글' FROM dual");
    }

    #[test]
    fn preserve_selected_text_terminator_does_not_remove_semicolon_inside_string_literal() {
        let source = "SELECT 'a;b' AS txt FROM dual";
        let formatted = SqlEditorWidget::format_sql_basic(source);

        let preserved = SqlEditorWidget::preserve_selected_text_terminator(source, formatted);

        assert!(
            preserved.contains("'a;b'"),
            "Semicolon inside string literal must remain unchanged, got:\n{}",
            preserved
        );
        assert!(
            !SqlEditorWidget::statement_ends_with_semicolon(&preserved),
            "Formatter should not append semicolon when original selection had none, got:\n{}",
            preserved
        );
    }

    #[test]
    fn preserve_selected_text_terminator_removes_inserted_semicolon_before_trailing_rem_line() {
        let source = "SELECT 1 FROM dual
REM trailing script comment";
        let formatted = SqlEditorWidget::format_sql_basic(source);

        let preserved = SqlEditorWidget::preserve_selected_text_terminator(source, formatted);

        assert!(
            !preserved.contains(';'),
            "Inserted semicolon should be removed when trailing REM line follows a statement with no terminator, got:
{}",
            preserved
        );
        assert!(
            preserved.contains("REM trailing script comment"),
            "Trailing REM line should be preserved, got:
{}",
            preserved
        );
    }

    #[test]
    fn preserve_selected_text_terminator_removes_inserted_semicolon_before_trailing_remark_line() {
        let source = "SELECT 1 FROM dual
REMARK trailing script comment";
        let formatted = SqlEditorWidget::format_sql_basic(source);

        let preserved = SqlEditorWidget::preserve_selected_text_terminator(source, formatted);

        assert!(
            !preserved.contains(';'),
            "Inserted semicolon should be removed when trailing REMARK line follows a statement with no terminator, got:
{}",
            preserved
        );
        assert!(
            preserved.contains("REMARK trailing script comment"),
            "Trailing REMARK line should be preserved, got:
{}",
            preserved
        );
    }

    #[test]
    fn format_sql_basic_places_semicolon_before_trailing_rem_line() {
        let source = "SELECT 1 FROM dual
REM trailing script comment";

        let formatted = SqlEditorWidget::format_sql_basic(source);

        assert!(
            formatted.contains("FROM DUAL;\nREM trailing script comment"),
            "Formatter should place statement terminator before trailing REM line, got:\n{}",
            formatted
        );
        assert!(
            !formatted.contains("REM trailing script comment;"),
            "Formatter should not append semicolon to REM comment text, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_sql_basic_places_semicolon_before_trailing_remark_line() {
        let source = "SELECT 1 FROM dual
REMARK trailing script comment";

        let formatted = SqlEditorWidget::format_sql_basic(source);

        assert!(
            formatted.contains("FROM DUAL;\nREMARK trailing script comment"),
            "Formatter should place statement terminator before trailing REMARK line, got:\n{}",
            formatted
        );
        assert!(
            !formatted.contains("REMARK trailing script comment;"),
            "Formatter should not append semicolon to REMARK comment text, got:\n{}",
            formatted
        );
    }

    #[test]
    fn append_missing_statement_terminator_places_semicolon_before_trailing_indented_rem_line() {
        let mut formatted = "SELECT 1
FROM DUAL
    REM trailing script comment"
            .to_string();

        SqlEditorWidget::append_missing_statement_terminator(&mut formatted);

        assert_eq!(
            formatted,
            "SELECT 1
FROM DUAL;
    REM trailing script comment"
        );
    }

    #[test]
    fn append_missing_statement_terminator_places_semicolon_before_trailing_indented_remark_line() {
        let mut formatted = "SELECT 1
FROM DUAL
	REMARK trailing script comment"
            .to_string();

        SqlEditorWidget::append_missing_statement_terminator(&mut formatted);

        assert_eq!(
            formatted,
            "SELECT 1
FROM DUAL;
	REMARK trailing script comment"
        );
    }

    #[test]
    fn format_sql_basic_does_not_append_semicolon_for_incomplete_trailing_operator() {
        let source = "SELECT 1 +";

        let formatted = SqlEditorWidget::format_sql_basic(source);

        assert_eq!(formatted, "SELECT 1 +");
    }

    #[test]
    fn format_sql_basic_does_not_append_semicolon_for_incomplete_trailing_comma() {
        let source = "SELECT a,";

        let formatted = SqlEditorWidget::format_sql_basic(source);

        assert_eq!(formatted, "SELECT a,");
    }

    #[test]
    fn format_sql_basic_does_not_append_semicolon_for_unbalanced_open_paren() {
        let source = "SELECT (1 + 2 FROM dual";
        let formatted = SqlEditorWidget::format_sql_basic(source);

        assert!(
            !formatted.trim_end().ends_with(';'),
            "formatter must not append semicolon to malformed statement with unbalanced open paren, got:
{}",
            formatted
        );
    }

    #[test]
    fn format_sql_basic_does_not_append_semicolon_for_unbalanced_closing_paren() {
        let source = "SELECT 1 + 2) FROM dual";
        let formatted = SqlEditorWidget::format_sql_basic(source);

        assert!(
            !formatted.trim_end().ends_with(';'),
            "formatter must not append semicolon to malformed statement with unmatched closing paren, got:
{}",
            formatted
        );
    }

    #[test]
    fn format_sql_basic_does_not_append_semicolon_for_unbalanced_brackets() {
        let source = "SELECT arr[1 FROM dual";
        let formatted = SqlEditorWidget::format_sql_basic(source);

        assert!(
            !formatted.trim_end().ends_with(';'),
            "formatter must not append semicolon to malformed statement with unbalanced brackets, got:
{}",
            formatted
        );
    }

    #[test]
    fn format_sql_basic_does_not_append_semicolon_for_cross_nested_paren_bracket_delimiters() {
        let source = "SELECT ([1)] FROM dual";
        let formatted = SqlEditorWidget::format_sql_basic(source);

        assert!(
            !formatted.trim_end().ends_with(';'),
            "formatter must not append semicolon to malformed statement with cross-nested delimiters, got:
{}",
            formatted
        );
    }

    #[test]
    fn format_sql_basic_appends_semicolon_for_balanced_mixed_nested_delimiters() {
        let source = "SELECT ([1]) FROM dual";
        let formatted = SqlEditorWidget::format_sql_basic(source);

        assert!(
            formatted.trim_end().ends_with(';'),
            "formatter should append semicolon when mixed delimiters are properly nested, got:
{}",
            formatted
        );
    }

    #[test]
    fn format_sql_basic_appends_semicolon_for_tail_statement_after_malformed_segment() {
        let source = "SELECT func(a, b;
SELECT c, d FROM dual";
        let formatted = SqlEditorWidget::format_sql_basic(source);

        assert!(
            formatted.contains(
                "SELECT c,
    d
FROM DUAL;"
            ),
            "formatter should keep canonical terminator for trailing valid statement, got:
{}",
            formatted
        );
    }

    #[test]
    fn format_sql_basic_does_not_append_semicolon_for_incomplete_trailing_dot() {
        let source = "SELECT t.";

        let formatted = SqlEditorWidget::format_sql_basic(source);

        assert_eq!(formatted, "SELECT t.");
    }

    #[test]
    fn format_sql_basic_does_not_append_semicolon_for_unterminated_single_quote() {
        let source = "SELECT 'unterminated";

        let formatted = SqlEditorWidget::format_sql_basic(source);

        assert_eq!(formatted, "SELECT 'unterminated");
    }

    #[test]
    fn format_sql_basic_does_not_append_semicolon_for_unterminated_quoted_identifier() {
        let source = "SELECT \"unterminated";

        let formatted = SqlEditorWidget::format_sql_basic(source);

        assert_eq!(formatted, "SELECT \"unterminated");
    }

    #[test]
    fn format_sql_basic_does_not_append_semicolon_for_unterminated_dollar_quote() {
        let source = "SELECT $$unterminated";

        let formatted = SqlEditorWidget::format_sql_basic(source);

        assert_eq!(formatted, "SELECT $$unterminated");
    }

    #[test]
    fn format_sql_basic_does_not_append_semicolon_for_unterminated_plsql_label() {
        let source = "BEGIN <<label
NULL";
        let formatted = SqlEditorWidget::format_sql_basic(source);

        assert_eq!(
            formatted,
            "BEGIN
    <<label
    NULL"
        );
        assert!(
            !formatted.trim_end().ends_with(';'),
            "unterminated PL/SQL label should not receive a terminator, got:
{}",
            formatted
        );
    }

    #[test]
    fn preserve_selected_text_terminator_keeps_semicolon_when_selection_had_one() {
        let source = "SELECT 1 FROM dual;";
        let formatted = SqlEditorWidget::format_sql_basic(source);

        let preserved = SqlEditorWidget::preserve_selected_text_terminator(source, formatted);
        assert!(preserved.trim_end().ends_with(';'));
    }

    #[test]
    fn preserve_selected_text_terminator_respects_trailing_comment_after_semicolon() {
        let source = "SELECT 1 FROM dual; -- keep terminator";
        let formatted = SqlEditorWidget::format_sql_basic(source);

        let preserved = SqlEditorWidget::preserve_selected_text_terminator(source, formatted);
        assert!(
            preserved.trim_end().ends_with("-- keep terminator"),
            "Trailing comment should be preserved, got:
{}",
            preserved
        );
        assert!(
            SqlEditorWidget::statement_ends_with_semicolon(&preserved),
            "Semicolon should remain when selection already ended with semicolon before comment, got:
{}",
            preserved
        );
    }

    #[test]
    fn format_sql_basic_does_not_append_extra_semicolon_into_trailing_comment_text() {
        let source = "SELECT 1 FROM dual -- existing; comment semicolon";

        let formatted = SqlEditorWidget::format_sql_basic(source);

        assert!(
            formatted.contains("FROM DUAL; -- existing; comment semicolon"),
            "Formatter should preserve trailing comment text while inserting SQL terminator, got:
{}",
            formatted
        );
        assert_eq!(
            formatted.matches(';').count(),
            2,
            "Expected one SQL terminator + one comment semicolon only, got:
{}",
            formatted
        );
    }

    #[test]
    fn preserve_selected_text_terminator_ignores_semicolon_inside_trailing_comment() {
        let source = "SELECT 1 FROM dual -- existing; comment semicolon";
        let formatted = SqlEditorWidget::format_sql_basic(source);

        let preserved = SqlEditorWidget::preserve_selected_text_terminator(source, formatted);
        assert!(
            preserved
                .trim_end()
                .ends_with("-- existing; comment semicolon"),
            "Trailing comment text should remain unchanged, got:\n{}",
            preserved
        );
        assert_eq!(
            preserved.matches(';').count(),
            1,
            "No extra semicolon should be appended when source had only comment semicolon, got:\n{}",
            preserved
        );
        assert!(
            !SqlEditorWidget::statement_ends_with_semicolon(&preserved),
            "Statement terminator should stay absent, got:\n{}",
            preserved
        );
    }

    #[test]
    fn preserve_selected_text_terminator_keeps_semicolon_in_comment_only_selection() {
        let source = "-- existing; comment semicolon";
        let formatted = SqlEditorWidget::format_sql_basic(source);

        let preserved = SqlEditorWidget::preserve_selected_text_terminator(source, formatted);
        assert_eq!(
            preserved, "-- existing; comment semicolon",
            "Semicolon inside comment-only selections should not be removed"
        );
    }

    #[test]
    fn preserve_selected_text_terminator_keeps_semicolon_in_sqlplus_remark_comment() {
        let source = "REM existing; comment semicolon";
        let formatted = SqlEditorWidget::format_sql_basic(source);

        let preserved = SqlEditorWidget::preserve_selected_text_terminator(source, formatted);
        assert_eq!(
            preserved, source,
            "Semicolon inside SQL*Plus remark comment should stay untouched, got:\n{}",
            preserved
        );
    }

    #[test]
    fn selected_auto_formatting_keeps_canonical_terminator_before_trailing_prompt() {
        let source = "select 1 from dual
PROMPT done;";

        let formatted = SqlEditorWidget::format_for_auto_formatting(source, true);

        assert!(
            formatted.contains(
                "FROM DUAL;\n\nPROMPT done;"
            ),
            "Selected auto-format should keep the trailing PROMPT line separated from SQL by a blank line, got:
{}",
            formatted
        );
    }

    #[test]
    fn format_sql_basic_inserts_blank_line_before_trailing_prompt_after_set_operator_query() {
        let source = "SELECT empno
FROM a
MINUS
SELECT empno
FROM b
ORDER BY empno;
PROMPT [DONE] Hardcore SELECT tests finished.";

        let formatted = SqlEditorWidget::format_sql_basic(source);

        assert!(
            formatted.contains(
                "ORDER BY empno;\n\nPROMPT [DONE] Hardcore SELECT tests finished."
            ),
            "Formatter should separate trailing PROMPT from the SQL statement with a blank line, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_for_auto_formatting_reassembles_multiline_sqlplus_set_commands() {
        let source = "SET SERVEROUTPUT
    ON SIZE UNLIMITED;
SET TIMING
    ON;
SELECT 1 FROM dual;";

        let formatted = SqlEditorWidget::format_for_auto_formatting(source, false);
        let expected = [
            "SET SERVEROUTPUT ON SIZE UNLIMITED",
            "",
            "SET TIMING ON",
            "",
            "SELECT 1",
            "FROM DUAL;",
        ]
        .join("\n");

        assert_eq!(formatted, expected);
    }

    #[test]
    fn format_for_auto_formatting_keeps_parenthesized_top_level_set_operators_left_aligned() {
        let source = r#"(
    SELECT e.dept_id,
        'HIGH' AS bucket,
        COUNT (*) AS cnt
    FROM qt_fmt_emp e
    WHERE e.salary >= (
        SELECT AVG (x.salary)
        FROM qt_fmt_emp x
        WHERE x.dept_id = e.dept_id
    )
    GROUP BY e.dept_id
    HAVING COUNT (*) > 0
)
    UNION ALL (
        SELECT e.dept_id,
            'LOW' AS bucket,
            COUNT (*) AS cnt
        FROM qt_fmt_emp e
        WHERE e.salary < (
            SELECT AVG (x.salary)
            FROM qt_fmt_emp x
            WHERE x.dept_id = e.dept_id
        )
        GROUP BY e.dept_id
        HAVING COUNT (*) > 0
    )
    MINUS (
        SELECT 9999 AS dept_id,
            'LOW' AS bucket,
            0 AS cnt
        FROM DUAL
    )
    ORDER BY 1,
        2;"#;
        let formatted = SqlEditorWidget::format_for_auto_formatting(source, false);
        let lines: Vec<&str> = formatted.lines().collect();
        let indent = |line: &str| line.len().saturating_sub(line.trim_start().len());
        let union_idx = lines
            .iter()
            .position(|line| line.trim_start().starts_with("UNION ALL"))
            .unwrap_or(0);
        let minus_idx = lines
            .iter()
            .position(|line| line.trim_start().starts_with("MINUS"))
            .unwrap_or(0);
        let order_idx = lines
            .iter()
            .position(|line| line.trim_start().starts_with("ORDER BY"))
            .unwrap_or(0);
        let union_select_idx = lines
            .iter()
            .enumerate()
            .skip(union_idx.saturating_add(1))
            .find(|(_, line)| line.trim_start().starts_with("SELECT e.dept_id"))
            .map(|(idx, _)| idx)
            .unwrap_or(0);
        let minus_select_idx = lines
            .iter()
            .enumerate()
            .skip(minus_idx.saturating_add(1))
            .find(|(_, line)| line.trim_start().starts_with("SELECT 9999 AS dept_id"))
            .map(|(idx, _)| idx)
            .unwrap_or(0);

        assert_eq!(
            indent(lines[union_idx]),
            0,
            "UNION ALL after a top-level ')' should return to depth 0, got:\n{}",
            formatted
        );
        assert_eq!(
            indent(lines[minus_idx]),
            0,
            "MINUS after a top-level ')' should return to depth 0, got:\n{}",
            formatted
        );
        assert_eq!(
            indent(lines[order_idx]),
            0,
            "ORDER BY after parenthesized set operands should stay on depth 0, got:\n{}",
            formatted
        );
        assert_eq!(
            indent(lines[union_select_idx]),
            4,
            "SELECT under UNION ALL should remain one level deeper than the set operator, got:\n{}",
            formatted
        );
        assert_eq!(
            indent(lines[minus_select_idx]),
            4,
            "SELECT under MINUS should remain one level deeper than the set operator, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_for_auto_formatting_keeps_parenthesized_top_level_intersect_left_aligned() {
        let source = r#"(
    SELECT dept_id
    FROM qt_fmt_emp
)
    INTERSECT (
        SELECT dept_id
        FROM qt_fmt_emp_hist
    )
    ORDER BY 1;"#;

        let formatted = SqlEditorWidget::format_for_auto_formatting(source, false);
        let lines: Vec<&str> = formatted.lines().collect();
        let indent = |line: &str| line.len().saturating_sub(line.trim_start().len());
        let intersect_idx = lines
            .iter()
            .position(|line| line.trim_start().starts_with("INTERSECT"))
            .unwrap_or(0);
        let order_idx = lines
            .iter()
            .position(|line| line.trim_start().starts_with("ORDER BY"))
            .unwrap_or(0);
        let rhs_select_idx = lines
            .iter()
            .enumerate()
            .skip(intersect_idx.saturating_add(1))
            .find(|(_, line)| line.trim_start().starts_with("SELECT dept_id"))
            .map(|(idx, _)| idx)
            .unwrap_or(0);

        assert_eq!(
            indent(lines[intersect_idx]),
            0,
            "INTERSECT after a top-level ')' should return to depth 0, got:\n{}",
            formatted
        );
        assert_eq!(
            indent(lines[order_idx]),
            0,
            "ORDER BY after parenthesized INTERSECT operands should stay on depth 0, got:\n{}",
            formatted
        );
        assert_eq!(
            indent(lines[rhs_select_idx]),
            4,
            "SELECT under INTERSECT should remain one level deeper than the set operator, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_for_auto_formatting_keeps_parenthesized_top_level_except_left_aligned() {
        let source = r#"(
    SELECT dept_id
    FROM qt_fmt_emp
)
    EXCEPT (
        SELECT dept_id
        FROM qt_fmt_emp_hist
    )
    ORDER BY 1;"#;

        let formatted = SqlEditorWidget::format_for_auto_formatting(source, false);
        let lines: Vec<&str> = formatted.lines().collect();
        let indent = |line: &str| line.len().saturating_sub(line.trim_start().len());
        let except_idx = lines
            .iter()
            .position(|line| line.trim_start().starts_with("EXCEPT"))
            .unwrap_or(0);
        let order_idx = lines
            .iter()
            .position(|line| line.trim_start().starts_with("ORDER BY"))
            .unwrap_or(0);
        let rhs_select_idx = lines
            .iter()
            .enumerate()
            .skip(except_idx.saturating_add(1))
            .find(|(_, line)| line.trim_start().starts_with("SELECT dept_id"))
            .map(|(idx, _)| idx)
            .unwrap_or(0);

        assert_eq!(
            indent(lines[except_idx]),
            0,
            "EXCEPT after a top-level ')' should return to depth 0, got:\n{}",
            formatted
        );
        assert_eq!(
            indent(lines[order_idx]),
            0,
            "ORDER BY after parenthesized EXCEPT operands should stay on depth 0, got:\n{}",
            formatted
        );
        assert_eq!(
            indent(lines[rhs_select_idx]),
            4,
            "SELECT under EXCEPT should remain one level deeper than the set operator, got:\n{}",
            formatted
        );
    }

    #[test]
    fn source_has_explicit_semicolon_terminator_ignores_trailing_prompt_line() {
        let source = "SELECT 1 FROM dual
PROMPT done;";

        assert!(
            !SqlEditorWidget::source_has_explicit_semicolon_terminator(source),
            "Semicolon in trailing PROMPT line should not count as SQL terminator"
        );
    }

    #[test]
    fn format_sql_basic_preserves_sqlplus_remark_comment_text_case() {
        let sql = "REMARK Keep MixedCase ; punctuation";

        let formatted = SqlEditorWidget::format_sql_basic(sql);

        assert_eq!(formatted, sql);
    }

    #[test]
    fn format_sql_basic_preserves_sqlplus_remark_comment_indentation() {
        let sql = "    REMARK Keep indentation\n\tREM tab-indented";

        let formatted = SqlEditorWidget::format_sql_basic(sql);

        assert_eq!(formatted, sql);
    }

    #[test]
    fn format_sql_basic_preserves_sqlplus_rem_comments_between_statements() {
        let sql = "REM keep this exact comment\nSELECT 1 FROM dual;\nREMARK Keep;This;Too";

        let formatted = SqlEditorWidget::format_sql_basic(sql);

        assert!(
            formatted.contains("REM keep this exact comment"),
            "{}",
            formatted
        );
        assert!(formatted.contains("REMARK Keep;This;Too"), "{}", formatted);
        assert!(formatted.contains("SELECT 1\nFROM DUAL;"), "{}", formatted);
    }

    #[test]
    fn format_sql_basic_preserves_comment_attachment_depth_and_hint_layout() {
        let sql = r#"-- file header keep
CREATE OR REPLACE PACKAGE pkg AS
-- package line comment
PROCEDURE p;
END pkg;
/

CREATE OR REPLACE TRIGGER trg
BEFORE INSERT ON t
BEGIN
-- before if
IF :NEW.id IS NULL THEN
-- if branch
:NEW.id := 1; -- inline   keep    spacing
ELSIF :NEW.id < 0 THEN
/* commented-out code
SELECT *
FROM dual;
*/
:NEW.id := 0;
ELSE
CASE WHEN :NEW.flag = 'Y' THEN
NULL;
END CASE;
END IF;
FOR i IN 1..2 LOOP
-- in loop
NULL;
END LOOP;
EXCEPTION
WHEN OTHERS THEN
-- in exception
NULL;
END;
/
SELECT /*+ INDEX(t idx_t) */ col -- inline tail keep
FROM t;
/* block
  layout
    keep
*/
"#;

        let formatted = SqlEditorWidget::format_sql_basic(sql);

        assert!(
            formatted.starts_with("-- file header keep\n"),
            "{}",
            formatted
        );
        assert!(formatted.contains("    -- before if"), "{}", formatted);
        assert!(formatted.contains("        -- if branch"), "{}", formatted);
        assert!(formatted.contains("        -- in loop"), "{}", formatted);
        assert!(
            formatted.contains("        -- in exception"),
            "{}",
            formatted
        );
        assert!(
            formatted.contains(":= 1; -- inline   keep    spacing"),
            "{}",
            formatted
        );
        assert!(
            formatted.contains("        /* commented-out code\nSELECT *\nFROM dual;\n*/"),
            "{}",
            formatted
        );
        assert!(formatted.contains("/*+ INDEX(t idx_t) */"), "{}", formatted);
        assert!(
            formatted.contains("col -- inline tail keep"),
            "{}",
            formatted
        );
        assert!(
            formatted.contains("/* block\n  layout\n    keep\n*/"),
            "{}",
            formatted
        );
    }

    #[test]
    fn format_sql_basic_preserves_oracle_hint_position_in_case_expression() {
        let sql = "SELECT CASE /*+ NO_EXPAND */ WHEN a = 1 THEN b ELSE c END -- keep\nFROM t";

        let formatted = SqlEditorWidget::format_sql_basic(sql);

        assert!(formatted.contains("/*+ NO_EXPAND */"), "{}", formatted);
        assert!(formatted.contains("END -- keep"), "{}", formatted);
    }

    #[test]
    fn format_tool_command_accept_escapes_single_quote_prompt() {
        let rendered = SqlEditorWidget::format_tool_command(&crate::db::ToolCommand::Accept {
            name: "v_name".to_string(),
            prompt: Some("Owner's value?".to_string()),
        });

        assert_eq!(rendered, "ACCEPT v_name PROMPT 'Owner''s value?'");
    }

    #[test]
    fn format_tool_command_mysql_use_quotes_unsafe_identifier_and_preserves_prequoted_input() {
        let unsafe_rendered = SqlEditorWidget::format_tool_command(&crate::db::ToolCommand::Use {
            database: "qt final-boss".to_string(),
        });
        let quoted_rendered = SqlEditorWidget::format_tool_command(&crate::db::ToolCommand::Use {
            database: "`qt final-boss`".to_string(),
        });

        assert_eq!(unsafe_rendered, "USE `qt final-boss`;");
        assert_eq!(quoted_rendered, "USE `qt final-boss`;");
    }

    #[test]
    fn format_tool_command_mysql_show_columns_quotes_keyword_schema_and_table() {
        let rendered = SqlEditorWidget::format_tool_command(&crate::db::ToolCommand::ShowColumns {
            table: "group".to_string(),
            schema: Some("order".to_string()),
        });

        assert_eq!(rendered, "SHOW COLUMNS FROM `group` FROM `order`");
    }

    #[test]
    fn format_tool_command_mysql_show_variables_escapes_single_quote_literal() {
        let rendered =
            SqlEditorWidget::format_tool_command(&crate::db::ToolCommand::ShowVariables {
                filter: Some("sql_mode_'strict'".to_string()),
            });

        assert_eq!(rendered, "SHOW VARIABLES LIKE 'sql_mode_''strict'''");
    }

    #[test]
    fn statement_ends_with_semicolon_recognizes_sqlplus_slash_terminator() {
        assert!(SqlEditorWidget::statement_ends_with_semicolon(
            "SELECT 1 FROM dual;
/"
        ));
    }

    #[test]
    fn statement_ends_with_semicolon_recognizes_sqlplus_slash_without_semicolon() {
        assert!(SqlEditorWidget::statement_ends_with_semicolon(
            "SELECT 1 FROM dual
/"
        ));
    }

    #[test]
    fn statement_ends_with_semicolon_recognizes_sqlplus_slash_with_trailing_comment() {
        assert!(SqlEditorWidget::statement_ends_with_semicolon(
            "SELECT 1 FROM dual
/
REM keep"
        ));
    }

    #[test]
    fn preserve_selected_text_terminator_removes_inserted_semicolon_before_sqlplus_slash() {
        let source = "BEGIN
  NULL;
END
/";
        let formatted = SqlEditorWidget::format_sql_basic(source);

        let preserved = SqlEditorWidget::preserve_selected_text_terminator(source, formatted);

        assert!(
            preserved.contains("END
/"),
            "Formatter should not keep an inserted semicolon before SQL*Plus slash terminator when source had none, got:
{}",
            preserved
        );
        assert!(
            !preserved.contains(
                "END;
/"
            ),
            "Inserted semicolon before SQL*Plus slash terminator should be removed, got:
{}",
            preserved
        );
    }

    #[test]
    fn preserve_selected_text_terminator_keeps_existing_semicolon_before_sqlplus_slash() {
        let source = "BEGIN
  NULL;
END;
/";
        let formatted = SqlEditorWidget::format_sql_basic(source);

        let preserved = SqlEditorWidget::preserve_selected_text_terminator(source, formatted);

        assert!(
            preserved.contains(
                "END;
/"
            ),
            "Existing semicolon before SQL*Plus slash terminator should remain, got:
{}",
            preserved
        );
    }

    #[test]
    fn statement_ends_with_semicolon_ignores_sqlplus_remark_comment_text() {
        assert!(!SqlEditorWidget::statement_ends_with_semicolon(
            "REM only a comment"
        ));
        assert!(!SqlEditorWidget::statement_ends_with_semicolon(
            "REMARK this is a comment with ; semicolon"
        ));
    }

    #[test]
    fn statement_ends_with_semicolon_recognizes_semicolon_before_inline_sqlplus_remark_comment() {
        assert!(SqlEditorWidget::statement_ends_with_semicolon(
            "SELECT 1 FROM dual; REM trailing comment"
        ));
        assert!(SqlEditorWidget::statement_ends_with_semicolon(
            "SELECT 1 FROM dual; REMARK trailing comment"
        ));
    }

    #[test]
    fn statement_ends_with_semicolon_rejects_semicolon_before_remark_with_following_statement() {
        assert!(!SqlEditorWidget::statement_ends_with_semicolon(
            "SELECT 1 FROM dual; REM note
SELECT 2 FROM dual"
        ));
    }

    #[test]
    fn statement_ends_with_semicolon_recognizes_semicolon_before_line_comment() {
        assert!(SqlEditorWidget::statement_ends_with_semicolon(
            "SELECT 1 FROM dual; -- trailing comment"
        ));
    }

    #[test]
    fn statement_ends_with_semicolon_recognizes_semicolon_before_hash_comment() {
        assert!(SqlEditorWidget::statement_ends_with_semicolon(
            "SELECT 1 FROM dual; # trailing comment"
        ));
    }

    #[test]
    fn source_has_explicit_semicolon_terminator_recognizes_semicolon_before_hash_comment() {
        assert!(SqlEditorWidget::source_has_explicit_semicolon_terminator(
            "SELECT 1 FROM dual; # trailing comment"
        ));
    }

    #[test]
    fn statement_ends_with_semicolon_recognizes_semicolon_before_slash_and_line_comment() {
        assert!(SqlEditorWidget::statement_ends_with_semicolon(
            "BEGIN\n  NULL;\nEND;\n/\n-- keep"
        ));
    }

    #[test]
    fn preserve_selected_text_terminator_keeps_semicolon_before_inline_sqlplus_remark_comment() {
        let source = "SELECT 1 FROM dual; REM trailing comment";
        let formatted = SqlEditorWidget::format_sql_basic(source);

        let preserved = SqlEditorWidget::preserve_selected_text_terminator(source, formatted);

        assert!(
            preserved.contains("FROM DUAL;") && preserved.contains("REM trailing comment"),
            "Existing semicolon and SQL*Plus REM comment should remain, got:\n{}",
            preserved
        );
        assert!(
            SqlEditorWidget::statement_ends_with_semicolon(&preserved),
            "Existing statement terminator before inline SQL*Plus REM comment should remain, got:\n{}",
            preserved
        );
    }

    #[test]
    fn preserve_selected_text_terminator_keeps_semicolon_before_line_comment() {
        let source = "SELECT 1 FROM dual; -- keep terminator";
        let formatted = SqlEditorWidget::format_sql_basic(source);

        let preserved = SqlEditorWidget::preserve_selected_text_terminator(source, formatted);

        assert!(
            preserved.contains("FROM DUAL;"),
            "Existing semicolon before line comment should remain, got:\n{}",
            preserved
        );
        assert!(
            preserved.trim_end().ends_with("-- keep terminator"),
            "Trailing line comment should remain, got:\n{}",
            preserved
        );
        assert!(
            SqlEditorWidget::statement_ends_with_semicolon(&preserved),
            "Existing terminator before line comment should remain explicit, got:\n{}",
            preserved
        );
    }

    #[test]
    fn preserve_selected_text_terminator_keeps_semicolon_before_hash_comment() {
        let source = "SELECT 1 FROM dual; # keep terminator";
        let formatted = SqlEditorWidget::format_sql_basic(source);

        let preserved = SqlEditorWidget::preserve_selected_text_terminator(source, formatted);

        assert!(
            preserved.contains("FROM DUAL;"),
            "Existing semicolon before hash comment should remain, got:\n{}",
            preserved
        );
        assert!(
            preserved.trim_end().ends_with("# keep terminator"),
            "Trailing hash comment should remain, got:\n{}",
            preserved
        );
        assert!(
            SqlEditorWidget::statement_ends_with_semicolon(&preserved),
            "Existing terminator before hash comment should remain explicit, got:\n{}",
            preserved
        );
    }

    #[test]
    fn preserve_selected_text_terminator_keeps_semicolon_before_slash_and_trailing_comment() {
        let source = "BEGIN\n  NULL;\nEND;\n/\n-- keep";
        let formatted = SqlEditorWidget::format_sql_basic(source);

        let preserved = SqlEditorWidget::preserve_selected_text_terminator(source, formatted);

        assert!(
            preserved.contains("END;\n/"),
            "Semicolon before SQL*Plus slash should remain when source had explicit terminator, got:\n{}",
            preserved
        );
        assert!(
            preserved.trim_end().ends_with("-- keep"),
            "Trailing line comment should remain, got:\n{}",
            preserved
        );
    }

    #[test]
    fn format_statement_preserves_compound_trigger_timing_end_qualifier() {
        let sql = r#"CREATE OR REPLACE TRIGGER test_compound_trg
  FOR INSERT ON test_table
  COMPOUND TRIGGER
    BEFORE EACH ROW IS
    BEGIN
      :NEW.status := 'new';
    END BEFORE EACH ROW;
    AFTER STATEMENT IS
    BEGIN
      NULL;
    END AFTER STATEMENT;
  END test_compound_trg;"#;
        let formatted = SqlEditorWidget::format_sql_basic(sql);
        assert!(
            formatted.contains("END BEFORE EACH ROW;"),
            "Compound trigger BEFORE timing qualifier should be preserved, got:\n{}",
            formatted
        );
        assert!(
            formatted.contains("END AFTER STATEMENT;"),
            "Compound trigger AFTER timing qualifier should be preserved, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_sql_basic_compound_trigger_declaration_and_timing_sections_use_stable_base_depth() {
        let sql = r#"CREATE OR REPLACE TRIGGER trg_employee_compound
    FOR INSERT OR UPDATE OR DELETE ON employees COMPOUND TRIGGER TYPE emp_id_set_t IS
    TABLE OF NUMBER INDEX BY PLS_INTEGER;
    g_emp_ids emp_id_set_t;
    g_idx PLS_INTEGER := 0;
    BEFORE STATEMENT IS
    BEGIN
        g_emp_ids.
        DELETE;
        g_idx := 0;
        DBMS_OUTPUT.PUT_LINE ('--- Statement Start ---');
    END BEFORE STATEMENT;

    AFTER EACH ROW IS
    BEGIN
        INSERT INTO employee_history (employee_id, action, old_salary, new_salary, change_date)
        VALUES (NVL (:NEW.employee_id, :OLD.employee_id), 
            CASE
                WHEN INSERTING THEN
                    'INSERT'
                WHEN UPDATING THEN
                    'UPDATE'
                WHEN DELETING THEN
                    'DELETE'
            END, :OLD.salary, :NEW.salary, SYSTIMESTAMP);
    END AFTER EACH ROW;"#;

        let formatted = SqlEditorWidget::format_sql_basic(sql);
        let lines: Vec<&str> = formatted.lines().collect();
        let find_line = |prefix: &str| -> Option<&str> {
            lines
                .iter()
                .copied()
                .find(|line| line.trim_start().starts_with(prefix))
        };
        let leading_spaces = |line: &str| line.len().saturating_sub(line.trim_start().len());

        let before_statement = find_line("BEFORE STATEMENT IS").unwrap_or("");
        let before_begin = lines
            .windows(2)
            .find(|pair| {
                pair[0].trim_start() == "BEFORE STATEMENT IS" && pair[1].trim_start() == "BEGIN"
            })
            .map(|pair| pair[1])
            .unwrap_or("");
        let before_body = find_line("g_emp_ids.").unwrap_or("");
        let after_each_row = find_line("AFTER EACH ROW IS").unwrap_or("");
        let after_begin = lines
            .windows(2)
            .find(|pair| {
                pair[0].trim_start() == "AFTER EACH ROW IS" && pair[1].trim_start() == "BEGIN"
            })
            .map(|pair| pair[1])
            .unwrap_or("");
        let insert_line = find_line("INSERT INTO employee_history").unwrap_or("");
        let values_line = find_line("VALUES (").unwrap_or("");
        let end_before = find_line("END BEFORE STATEMENT;").unwrap_or("");
        let end_after = find_line("END AFTER EACH ROW;").unwrap_or("");

        assert_eq!(
            leading_spaces(before_statement),
            4,
            "compound trigger timing header should stay at outer body depth, got:\n{}",
            formatted
        );
        assert_eq!(
            leading_spaces(before_begin),
            4,
            "timing section BEGIN should align with its header, got:\n{}",
            formatted
        );
        assert_eq!(
            leading_spaces(before_body),
            8,
            "statements in BEFORE section should be exactly one level deeper than BEGIN, got:\n{}",
            formatted
        );
        assert_eq!(
            leading_spaces(after_each_row),
            4,
            "subsequent timing headers should keep the same compound-trigger base depth, got:\n{}",
            formatted
        );
        assert_eq!(
            leading_spaces(after_begin),
            4,
            "subsequent timing BEGIN should align with its header, got:\n{}",
            formatted
        );
        assert_eq!(
            leading_spaces(insert_line),
            8,
            "DML head inside timing section should stay one level deeper than BEGIN, got:\n{}",
            formatted
        );
        assert_eq!(
            leading_spaces(values_line),
            8,
            "DML clause starters inside timing section should reuse the same query base depth, got:\n{}",
            formatted
        );
        assert_eq!(
            leading_spaces(end_before),
            4,
            "END BEFORE STATEMENT should return to timing header depth, got:\n{}",
            formatted
        );
        assert_eq!(
            leading_spaces(end_after),
            4,
            "END AFTER EACH ROW should return to timing header depth, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_sql_basic_compound_trigger_single_line_timing_sections_break_on_outer_body_state() {
        let sql = "CREATE OR REPLACE TRIGGER trg_employee_compound FOR INSERT OR UPDATE OR DELETE ON employees COMPOUND TRIGGER TYPE emp_id_set_t IS TABLE OF NUMBER INDEX BY PLS_INTEGER; g_emp_ids emp_id_set_t; AFTER EACH ROW IS BEGIN INSERT INTO employee_history (employee_id, action) VALUES (:NEW.employee_id, 'INSERT'); END AFTER EACH ROW; END trg_employee_compound;";
        let formatted = SqlEditorWidget::format_sql_basic(sql);
        let lines: Vec<&str> = formatted.lines().collect();
        let find_line = |prefix: &str| -> Option<&str> {
            lines
                .iter()
                .copied()
                .find(|line| line.trim_start().starts_with(prefix))
        };
        let leading_spaces = |line: &str| line.len().saturating_sub(line.trim_start().len());

        let after_each_row = find_line("AFTER EACH ROW IS").unwrap_or("");
        let begin_line = lines
            .windows(2)
            .find(|pair| {
                pair[0].trim_start() == "AFTER EACH ROW IS" && pair[1].trim_start() == "BEGIN"
            })
            .map(|pair| pair[1])
            .unwrap_or("");
        let insert_line = find_line("INSERT INTO employee_history").unwrap_or("");
        let values_line = find_line("VALUES (").unwrap_or("");

        assert_eq!(
            leading_spaces(after_each_row),
            4,
            "single-line timing header should break at the compound trigger outer body depth, got:\n{}",
            formatted
        );
        assert_eq!(
            leading_spaces(begin_line),
            4,
            "timing BEGIN from single-line input should align with the timing header, got:\n{}",
            formatted
        );
        assert_eq!(
            leading_spaces(insert_line),
            8,
            "single-line timing DML head should stay one level deeper than BEGIN, got:\n{}",
            formatted
        );
        assert_eq!(
            leading_spaces(values_line),
            8,
            "single-line timing DML clauses should reuse the same base depth, got:\n{}",
            formatted
        );
    }

    #[test]
    fn formats_values_subquery_with_nested_depth() {
        let sql = "SELECT 1 FROM dual WHERE EXISTS (VALUES (1));";
        let formatted = SqlEditorWidget::format_sql_basic(sql);

        let expected = [
            "SELECT 1",
            "FROM DUAL",
            "WHERE EXISTS (",
            "        VALUES (1)",
            "    );",
        ]
        .join("\n");

        assert_eq!(formatted, expected);
    }

    #[test]
    fn formats_deeply_nested_subqueries_with_consistent_depth() {
        let sql = "SELECT outer_col FROM outer_t o WHERE EXISTS (SELECT 1 FROM (SELECT inner_col FROM inner_t i WHERE i.id IN (SELECT id FROM leaf_t WHERE status = 'Y')) nested_q WHERE nested_q.inner_col = o.outer_col);";
        let formatted = SqlEditorWidget::format_sql_basic(sql);

        let expected = [
            "SELECT outer_col",
            "FROM outer_t o",
            "WHERE EXISTS (",
            "        SELECT 1",
            "        FROM (",
            "                SELECT inner_col",
            "                FROM inner_t i",
            "                WHERE i.id IN (",
            "                        SELECT id",
            "                        FROM leaf_t",
            "                        WHERE status = 'Y'",
            "                    )",
            "            ) nested_q",
            "        WHERE nested_q.inner_col = o.outer_col",
            "    );",
        ]
        .join("\n");

        assert_eq!(formatted, expected);
    }

    #[test]
    fn formats_nested_union_subquery_with_consistent_depth() {
        let sql = "SELECT o.id FROM outer_t o WHERE EXISTS (SELECT 1 FROM (SELECT i.id FROM inner_a i WHERE i.flag = 'Y' UNION ALL SELECT j.id FROM inner_b j WHERE j.flag = 'N') merged WHERE merged.id = o.id);";
        let formatted = SqlEditorWidget::format_sql_basic(sql);

        let expected = [
            "SELECT o.id",
            "FROM outer_t o",
            "WHERE EXISTS (",
            "        SELECT 1",
            "        FROM (",
            "                SELECT i.id",
            "                FROM inner_a i",
            "                WHERE i.flag = 'Y'",
            "                UNION ALL",
            "                SELECT j.id",
            "                FROM inner_b j",
            "                WHERE j.flag = 'N'",
            "            ) merged",
            "        WHERE merged.id = o.id",
            "    );",
        ]
        .join("\n");

        assert_eq!(formatted, expected);
    }

    #[test]
    fn format_for_auto_formatting_keeps_nested_except_branch_on_condition_query_base() {
        let source = "SELECT * FROM dept d WHERE EXISTS (SELECT d.deptno FROM dual EXCEPT SELECT e.deptno FROM emp e);";
        let formatted = SqlEditorWidget::format_for_auto_formatting(source, false);
        let expected = [
            "SELECT *",
            "FROM dept d",
            "WHERE EXISTS (",
            "        SELECT d.deptno",
            "        FROM DUAL",
            "        EXCEPT",
            "        SELECT e.deptno",
            "        FROM emp e",
            "    );",
        ]
        .join("\n");

        assert_eq!(
            formatted, expected,
            "nested EXCEPT branches should stay on the same child query base depth under condition owners"
        );
        assert_eq!(
            SqlEditorWidget::format_for_auto_formatting(&formatted, false),
            expected,
            "nested EXCEPT auto-formatting should remain stable after the query-base fix"
        );
    }

    #[test]
    fn formats_nested_any_all_subqueries_with_condition_owner_stack() {
        let sql = "SELECT deptno FROM dept d WHERE d.deptno = ANY (SELECT e.deptno FROM emp e WHERE e.sal > ALL (SELECT b.sal FROM bonus b WHERE b.empno = e.empno));";
        let formatted = SqlEditorWidget::format_sql_basic(sql);

        let expected = [
            "SELECT deptno",
            "FROM dept d",
            "WHERE d.deptno = ANY (",
            "        SELECT e.deptno",
            "        FROM emp e",
            "        WHERE e.sal > ALL (",
            "                SELECT b.sal",
            "                FROM bonus b",
            "                WHERE b.empno = e.empno",
            "            )",
            "    );",
        ]
        .join("\n");

        assert_eq!(
            formatted, expected,
            "ANY/ALL nested subqueries should inherit the same condition-owner stack rules as IN/EXISTS owners"
        );
        assert_eq!(
            SqlEditorWidget::format_for_auto_formatting(&formatted, false),
            expected,
            "ANY/ALL nested subquery formatting should remain stable after auto-formatting"
        );
    }

    #[test]
    fn format_for_auto_formatting_normalizes_split_any_some_all_owners_to_stable_inline_layout() {
        let scenarios = [("= ANY", "ANY"), ("< SOME", "SOME"), ("> ALL", "ALL")];

        for (comparison, keyword) in scenarios {
            let source = format!(
                "SELECT 1\nFROM outer_t o\nWHERE o.score {comparison}\n(\n    SELECT 1\n    FROM inner_t i\n    WHERE i.score {comparison}\n    (\n        SELECT 1\n        FROM leaf_t l\n        WHERE l.outer_id = o.id\n    )\n    AND i.outer_id = o.id\n)\nAND o.active = 1;"
            );
            let formatted = SqlEditorWidget::format_for_auto_formatting(&source, false);
            let expected = format!(
                "SELECT 1\nFROM outer_t o\nWHERE o.score {comparison} (\n        SELECT 1\n        FROM inner_t i\n        WHERE i.score {comparison} (\n                SELECT 1\n                FROM leaf_t l\n                WHERE l.outer_id = o.id\n            )\n            AND i.outer_id = o.id\n    )\n    AND o.active = 1;"
            );

            assert_eq!(
                formatted, expected,
                "split {keyword} owners should normalize to the canonical inline layout without losing nested condition depth"
            );
            assert_eq!(
                SqlEditorWidget::format_for_auto_formatting(&formatted, false),
                expected,
                "split {keyword} owner formatting should remain stable after re-formatting"
            );
        }
    }

    #[test]
    fn plsql_nested_with_subquery_keeps_cte_body_depth() {
        let sql = r#"BEGIN
  SELECT o.id
  INTO v_id
  FROM outer_t o
  WHERE EXISTS (
    WITH filt AS (
      SELECT id
      FROM inner_t
      WHERE flag = 'Y'
    )
    SELECT id
    FROM filt
    WHERE filt.id = o.id
  );
END;"#;
        let formatted = SqlEditorWidget::format_sql_basic(sql);

        assert!(
            formatted.contains(
                "WITH filt AS (
                SELECT id
                FROM inner_t
                WHERE flag = 'Y'
            )"
            ),
            "CTE body inside nested subquery should indent one level deeper than WITH header, got:
{}",
            formatted
        );
    }

    #[test]
    fn plsql_if_then_with_query_uses_shared_base_depth_for_all_clauses() {
        let sql = r#"BEGIN
  IF 1 = 1 THEN
    WITH filt AS (
      SELECT id
      FROM src_t
    )
    SELECT id
    INTO v_id
    FROM filt;
  END IF;
END;"#;
        let formatted = SqlEditorWidget::format_sql_basic(sql);

        assert!(
            formatted.contains(
                "THEN\n        WITH filt AS (\n            SELECT id\n            FROM src_t"
            ),
            "WITH header and CTE body after THEN should keep a stable base/child depth split, got:\n{}",
            formatted
        );
        assert!(
            formatted.contains("\n        SELECT id\n        INTO v_id\n        FROM filt;"),
            "Main SELECT/INTO/FROM after THEN should reuse the shared query base depth, got:\n{}",
            formatted
        );
    }

    #[test]
    fn plsql_nested_union_subquery_keeps_consistent_depth() {
        let sql = r#"BEGIN
  SELECT o.id
  INTO v_id
  FROM outer_t o
  WHERE EXISTS (
    SELECT 1
    FROM (
      SELECT i.id
      FROM inner_a i
      WHERE i.flag = 'Y'
      UNION ALL
      SELECT j.id
      FROM inner_b j
      WHERE j.flag = 'N'
    ) merged
    WHERE merged.id = o.id
  );
END;"#;
        let formatted = SqlEditorWidget::format_sql_basic(sql);
        let lines: Vec<&str> = formatted.lines().collect();
        let indent = |line: &str| line.chars().take_while(|c| *c == ' ').count();
        let from_idx = lines
            .iter()
            .position(|line| line.trim_start() == "FROM (")
            .unwrap_or(0);
        let nested_select_idx = lines
            .iter()
            .enumerate()
            .skip(from_idx.saturating_add(1))
            .find(|(_, line)| line.trim_start().starts_with("SELECT i.id"))
            .map(|(idx, _)| idx)
            .unwrap_or(0);
        let union_idx = lines
            .iter()
            .position(|line| line.trim_start() == "UNION ALL")
            .unwrap_or(0);
        let union_select_idx = lines
            .iter()
            .enumerate()
            .skip(union_idx.saturating_add(1))
            .find(|(_, line)| line.trim_start().starts_with("SELECT j.id"))
            .map(|(idx, _)| idx)
            .unwrap_or(0);

        assert!(
            indent(lines[nested_select_idx]) > indent(lines[from_idx]),
            "Nested subquery under FROM should increase depth, got:\n{}",
            formatted
        );
        assert_eq!(
            indent(lines[union_select_idx]),
            indent(lines[nested_select_idx]),
            "Set operator branch inside nested subquery should keep same nested depth, got:\n{}",
            formatted
        );
    }

    #[test]
    fn plsql_nested_with_multiple_ctes_keeps_cte_depth_aligned() {
        let sql = r#"BEGIN
  SELECT o.id
  INTO v_id
  FROM outer_t o
  WHERE EXISTS (
    WITH a AS (
      SELECT id
      FROM inner_t
    ), b AS (
      SELECT id
      FROM a
    )
    SELECT id
    FROM b
    WHERE b.id = o.id
  );
END;"#;
        let formatted = SqlEditorWidget::format_sql_basic(sql);

        let expected = [
            "BEGIN",
            "    SELECT o.id",
            "    INTO v_id",
            "    FROM outer_t o",
            "    WHERE EXISTS (",
            "            WITH a AS (",
            "                SELECT id",
            "                FROM inner_t",
            "            ),",
            "            b AS (",
            "                SELECT id",
            "                FROM a",
            "            )",
            "            SELECT id",
            "            FROM b",
            "            WHERE b.id = o.id",
            "        );",
            "END;",
        ]
        .join("\n");

        assert_eq!(formatted, expected);
    }

    #[test]
    fn plsql_nested_with_clause_resets_excess_manual_indent() {
        let sql = r#"BEGIN
  SELECT o.id
  INTO v_id
  FROM outer_t o
  WHERE EXISTS (
                    WITH filt AS (
      SELECT id
      FROM inner_t
      WHERE flag = 'Y'
    )
    SELECT id
    FROM filt
    WHERE filt.id = o.id
  );
END;"#;
        let formatted = SqlEditorWidget::format_sql_basic(sql);

        assert!(
            formatted.contains("WHERE EXISTS (
            WITH filt AS ("),
            "WITH clause should align to nested query depth instead of preserving excess manual indent, got:
{}",
            formatted
        );
    }

    #[test]
    fn format_sql_basic_keeps_test26_reference_layout_exactly() {
        let expected = r#"PROCEDURE A (B IN NUMBER) AS
BEGIN
    SELECT D --4
    FROM E --4
    WHERE F IN (
            --4
            SELECT G -- 12
            FROM ( -- 12
                    SELECT H -- 20
                    FROM J -- 20
                    INNER JOIN K -- 20
                        ON 1 = 1 -- 24
                            AND 2 = 2 -- 28
                            OR 3 = 3 -- 28
                    OUTER JOIN K -- 20
                        ON 1 = 1 -- 24
                            AND 2 = 2 -- 28
                            OR 3 = 3 -- 28
                ) I -- 16
        ); -- 8
END A;

SELECT D
FROM E
WHERE F IN (
        SELECT G --8
        FROM ( --8
                SELECT H --16
                FROM J --16
                INNER JOIN K --16
                    ON 1 = 1 --20
                        AND 2 = 2 -- 24
                OUTER JOIN K -- 16
                    ON 1 = 1 --20
                        AND 2 = 2 -- 24
            ) I --12
    ); --4"#;

        let formatted = SqlEditorWidget::format_sql_basic(expected);

        assert_eq!(
            formatted, expected,
            "Formatting must preserve the reference base-depth layout exactly for nested IN/FROM subqueries with JOIN conditions"
        );
    }

    #[test]
    fn split_format_items_keeps_existing_inline_line_comment_after_semicolon() {
        let items = QueryExecutor::split_format_items("SELECT 1 FROM dual; -- keep terminator");

        assert_eq!(
            items.len(),
            1,
            "Trailing inline comment must stay in the same format item"
        );
        match &items[0] {
            FormatItem::Statement(statement) => {
                assert!(
                    statement.contains("; -- keep terminator"),
                    "Trailing inline comment should remain attached to the statement, got:\n{}",
                    statement
                );
            }
            other => panic!("Expected a statement item, got: {other:?}"),
        }
    }

    #[test]
    fn should_not_merge_order_modifier_fragment_after_semicolon_before_inline_comment() {
        assert!(!SqlEditorWidget::should_merge_order_modifier_fragment(
            "ORDER BY sal; -- done",
            "DESC"
        ));
    }

    #[test]
    fn open_for_with_inline_block_comment_keeps_comment_inline_and_subquery_indented() {
        let sql = r#"FUNCTION get_employee_report (p_dept_id NUMBER, p_min_salary NUMBER DEFAULT 0) RETURN t_refcur IS
    l_rc t_refcur;
BEGIN
    OPEN l_rc FOR
        WITH base AS (
            SELECT e.emp_id,
                e.emp_name,
                e.salary,
                e.bonus_pct,
                d.dept_name, /*asdf*/
                NVL (
                    (
                    SELECT SUM (s.amount)
                    FROM qt_sales s
                    WHERE s.emp_id = e.emp_id
                ),
                0
                ) AS total_sales
            FROM qt_emp e
        )
        SELECT *
        FROM base;
END;"#;

        let formatted = SqlEditorWidget::format_sql_basic(sql);

        assert!(
            formatted.contains(
                "d.dept_name, /*asdf*/\n                NVL (\n                    (\n                        SELECT SUM (s.amount)\n                        FROM qt_sales s\n                        WHERE s.emp_id = e.emp_id\n                    ),\n                    0\n                ) AS total_sales"
            ),
            "{}",
            formatted
        );
    }

    #[test]
    fn open_for_with_inline_line_comment_keeps_next_select_item_depth() {
        let sql = r#"FUNCTION get_employee_report (p_dept_id NUMBER, p_min_salary NUMBER DEFAULT 0) RETURN t_refcur IS
    l_rc t_refcur;
BEGIN
    OPEN l_rc FOR
        WITH base AS (
            SELECT e.emp_id,
                e.emp_name,
                e.salary,
                e.bonus_pct,
                d.dept_name, --asdf
                NVL (
                    (
                    SELECT SUM (s.amount)
                    FROM qt_sales s
                    WHERE s.emp_id = e.emp_id
                ),
                0
                ) AS total_sales
            FROM qt_emp e
        )
        SELECT *
        FROM base;
END;"#;

        let formatted = SqlEditorWidget::format_sql_basic(sql);

        assert!(
            formatted.contains(
                "d.dept_name, --asdf\n                NVL (\n                    (\n                        SELECT SUM (s.amount)\n                        FROM qt_sales s\n                        WHERE s.emp_id = e.emp_id\n                    ),\n                    0\n                ) AS total_sales"
            ),
            "{}",
            formatted
        );
    }

    #[test]
    fn open_for_with_inline_block_comment_after_select_keeps_first_select_item_depth() {
        let sql = r#"CREATE OR REPLACE PROCEDURE test_open_with_proc IS
    p_rc SYS_REFCURSOR;
BEGIN
    OPEN p_rc FOR
        WITH /* A: dept 집계 CTE */
        dept_stats AS (
            SELECT /* B: dept 집계 */
            deptno,
                COUNT(*) AS cnt,
                AVG(sal) AS avg_sal,
                SUM (NVL (comm, /* C: NULL→0 */
                        0)) AS sum_comm
            FROM emp
            GROUP BY deptno
        )
        SELECT *
        FROM dept_stats;
END;"#;

        let formatted = SqlEditorWidget::format_for_auto_formatting(sql, false);

        assert!(
            formatted.contains(
                "WITH /* A: dept 집계 CTE */\n        dept_stats AS (\n            SELECT /* B: dept 집계 */\n                deptno,"
            ),
            "inline block comment after SELECT in CTE body should keep the first select item on list depth, got:\n{}",
            formatted
        );
        assert!(
            !formatted.contains(
                "SELECT /* B: dept 집계 */\n            deptno,"
            ),
            "first select-list item after inline SELECT comment must not stay on the SELECT header depth, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_for_auto_formatting_keeps_select_list_depth_after_multiline_select_hint_and_comment_gap(
    ) {
        let sql = r#"SELECT /*+ FULL(t) PARALLEL(t, 8) USE_HASH(t s)
          INDEX(s idx_status)
          NO_MERGE QB_NAME(main_query) */
t.id,
    /* 이건 일반 주석; END; / */
    s.status
FROM my_table t
JOIN status_table s
    ON t.id = s.id
WHERE /*+ 이건 힌트가 아닌 주석 */
    t.active = 'Y';"#;

        let formatted = SqlEditorWidget::format_for_auto_formatting(sql, false);
        let lines: Vec<&str> = formatted.lines().collect();
        let leading_spaces = |line: &str| line.len().saturating_sub(line.trim_start().len());

        let select_idx = lines
            .iter()
            .position(|line| line.trim_start().starts_with("SELECT /*+ FULL(t)"))
            .unwrap_or(0);
        let first_item_idx = lines
            .iter()
            .position(|line| line.trim_start() == "t.id,")
            .unwrap_or(0);
        let comment_idx = lines
            .iter()
            .position(|line| line.trim_start() == "/* 이건 일반 주석; END; / */")
            .unwrap_or(0);
        let second_item_idx = lines
            .iter()
            .position(|line| line.trim_start() == "s.status")
            .unwrap_or(0);
        let from_idx = lines
            .iter()
            .position(|line| line.trim_start() == "FROM my_table t")
            .unwrap_or(0);

        assert_eq!(
            leading_spaces(lines[first_item_idx]),
            leading_spaces(lines[select_idx]).saturating_add(4),
            "multiline SELECT hint should keep the first select-list item one level deeper than SELECT, got:\n{}",
            formatted
        );
        assert_eq!(
            leading_spaces(lines[comment_idx]),
            leading_spaces(lines[first_item_idx]),
            "comment gap inside the select list should stay on the carried list depth, got:\n{}",
            formatted
        );
        assert_eq!(
            leading_spaces(lines[second_item_idx]),
            leading_spaces(lines[first_item_idx]),
            "later select-list items should reuse the same carried list depth after the comment gap, got:\n{}",
            formatted
        );
        assert!(
            leading_spaces(lines[from_idx]) < leading_spaces(lines[first_item_idx]),
            "FROM should clear the carried select-list depth after the multiline hint block, got:\n{}",
            formatted
        );
    }

    #[test]
    fn plsql_nested_with_clause_does_not_keep_two_level_extra_indent() {
        let sql = r#"BEGIN
    SELECT o.id
    INTO v_id
    FROM outer_t o
    WHERE EXISTS (
                WITH filt AS (
                    SELECT id
                    FROM inner_t
                    WHERE flag = 'Y'
                )
            SELECT id
            FROM filt
            WHERE filt.id = o.id
        );
END;"#;
        let formatted = SqlEditorWidget::format_sql_basic(sql);

        assert!(
            formatted.contains(
                "WHERE EXISTS (
            WITH filt AS ("
            ),
            "WITH clause should not keep two-level extra indent in nested DML depth, got:
{}",
            formatted
        );
    }
    #[test]
    fn formats_nested_with_subquery_with_consistent_depth() {
        let sql = "SELECT o.id FROM outer_t o WHERE EXISTS (WITH filt AS (SELECT id FROM inner_t WHERE flag = 'Y') SELECT id FROM filt WHERE filt.id = o.id);";
        let formatted = SqlEditorWidget::format_sql_basic(sql);

        let expected = [
            "SELECT o.id",
            "FROM outer_t o",
            "WHERE EXISTS (",
            "        WITH filt AS (",
            "            SELECT id",
            "            FROM inner_t",
            "            WHERE flag = 'Y'",
            "        )",
            "        SELECT id",
            "        FROM filt",
            "        WHERE filt.id = o.id",
            "    );",
        ]
        .join("\n");

        assert_eq!(formatted, expected);
    }

    #[test]
    fn formats_nested_cursor_expression_with_consistent_depth() {
        let sql = r#"SELECT
    d.dept_id,
    d.dept_name,
    CURSOR
    (
        SELECT
            e.emp_id,
            e.emp_no,
            e.emp_name,
            CURSOR
            (
                SELECT
                    s.sale_year,
                    SUM(s.sale_amount) AS total_sales
                FROM qt_sales s
                WHERE s.emp_id = e.emp_id
                GROUP BY s.sale_year
                ORDER BY s.sale_year
            ) AS sales_cur
        FROM qt_emp e
        WHERE e.dept_id = d.dept_id
        ORDER BY e.emp_id
    ) AS emp_cur
FROM qt_dept d
ORDER BY d.dept_id"#;
        let formatted = SqlEditorWidget::format_sql_basic(sql);

        let expected = [
            "SELECT d.dept_id,",
            "    d.dept_name,",
            "    CURSOR (",
            "        SELECT e.emp_id,",
            "            e.emp_no,",
            "            e.emp_name,",
            "            CURSOR (",
            "                SELECT s.sale_year,",
            "                    SUM (s.sale_amount) AS total_sales",
            "                FROM qt_sales s",
            "                WHERE s.emp_id = e.emp_id",
            "                GROUP BY s.sale_year",
            "                ORDER BY s.sale_year",
            "            ) AS sales_cur",
            "        FROM qt_emp e",
            "        WHERE e.dept_id = d.dept_id",
            "        ORDER BY e.emp_id",
            "    ) AS emp_cur",
            "FROM qt_dept d",
            "ORDER BY d.dept_id;",
        ]
        .join("\n");

        assert_eq!(formatted, expected);
    }
}

#[cfg(test)]
mod format_comment_indent_tests {
    use crate::ui::sql_editor::SqlEditorWidget;

    fn leading_spaces(line: &str) -> usize {
        line.len().saturating_sub(line.trim_start().len())
    }

    #[test]
    fn format_sql_basic_indents_line_comment_in_select_list_before_comma() {
        let source = "select col1\n-- comment\n,col2\nfrom t1;";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        assert!(
            formatted.contains("SELECT col1\n    -- comment\n    ,\n    col2\nFROM t1;"),
            "Line comment between select items should be indented at list item depth, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_sql_basic_indents_mid_list_comment_at_list_depth() {
        let source = "select col1\n-- comment2\n,col2\nfrom t1;";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        assert!(
            formatted.contains("    -- comment2\n    ,\n    col2"),
            "Mid-list comment (before comma) should be indented at list item depth, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_sql_basic_keeps_comment_at_base_indent_before_from_single_item() {
        let source = "select col1\n-- last column\nfrom t1;";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        assert!(
            formatted.contains("SELECT col1\n-- last column\nFROM t1;"),
            "Inter-clause comment before FROM should stay at base indent, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_sql_basic_indents_comment_in_subquery_select_list() {
        let source = "select * from (select col1\n-- comment\n,col2\nfrom t1);";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        assert!(
            formatted
                .contains("SELECT col1\n            -- comment\n            ,\n            col2"),
            "Comment in subquery select list should be at list item depth, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_sql_basic_indents_comment_after_comma_in_open_for_select_list() {
        let source = "begin\n    open c for\n        select b,\n        -- comment\n            d\n        from e;\nend;";
        let formatted = SqlEditorWidget::format_sql_basic(source);

        assert!(
            formatted.contains("SELECT b,\n            -- comment\n            d\n        FROM e;"),
            "Line comment after a trailing comma should keep select-list depth, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_sql_basic_keeps_inline_comment_alignment_after_multiline_string_literal() {
        let source = "begin\n    a := 'b\n             b';     -- c\nend;";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        let lines: Vec<&str> = formatted.lines().collect();
        let assign_idx = lines
            .iter()
            .position(|line| line.contains("b';") && line.contains("-- c"))
            .unwrap_or(0);
        let assign_line = lines.get(assign_idx).copied().unwrap_or_default();

        assert!(
            assign_line.contains("b'; -- c"),
            "Inline comment spacing after multiline string literal should be preserved, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_sql_basic_indents_open_for_select_clauses_inside_cursor_body() {
        let source = "begin\n    select a\n    from b\n    where 1=1;\n    open cv for\n        select a,\n            b\n    from c,\n            d\n    where 1=1;\nend;";
        let formatted = SqlEditorWidget::format_sql_basic(source);

        assert!(
            formatted.contains(
                "OPEN cv FOR\n        SELECT a,\n            b\n        FROM c,\n            d\n        WHERE 1 = 1;"
            ),
            "OPEN ... FOR nested SELECT should keep FROM/WHERE aligned with SELECT body depth, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_sql_basic_keeps_connect_by_aligned_in_open_for_parenthesized_select() {
        let source = "BEGIN\n    OPEN cv FOR (SELECT employee_id\n                 FROM employees\n                 START WITH manager_id IS NULL\n                 CONNECT BY PRIOR employee_id = manager_id);\nEND;";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        let lines: Vec<&str> = formatted.lines().collect();
        let select_indent = lines
            .iter()
            .find(|line| line.trim_start().starts_with("SELECT employee_id"))
            .map(|line| leading_spaces(line))
            .unwrap_or(0);
        let from_indent = lines
            .iter()
            .find(|line| line.trim_start().starts_with("FROM employees"))
            .map(|line| leading_spaces(line))
            .unwrap_or(0);
        let connect_by_indent = lines
            .iter()
            .find(|line| line.trim_start().starts_with("CONNECT BY PRIOR"))
            .map(|line| leading_spaces(line))
            .unwrap_or(0);

        assert!(
            select_indent > 0,
            "OPEN ... FOR SELECT line should exist, got:\n{}",
            formatted
        );
        assert_eq!(
            from_indent, select_indent,
            "FROM in OPEN ... FOR SELECT should align with SELECT, got:\n{}",
            formatted
        );
        assert_eq!(
            connect_by_indent, select_indent,
            "CONNECT BY in OPEN ... FOR SELECT should align with SELECT, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_sql_basic_keeps_nested_open_for_subquery_closing_paren_at_open_paren_indent() {
        let source = "BEGIN\n    OPEN cv FOR (SELECT e.employee_id\n                 FROM employees e\n                 WHERE EXISTS (SELECT 1\n                               FROM dual\n                               WHERE 1 = 1));\nEND;";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        let lines: Vec<&str> = formatted.lines().collect();

        let open_paren_indent = lines
            .iter()
            .find(|line| line.trim() == "(")
            .map(|line| leading_spaces(line))
            .unwrap_or(0);
        let close_paren_indent = lines
            .iter()
            .rev()
            .find(|line| line.trim() == ");")
            .map(|line| leading_spaces(line))
            .unwrap_or(0);

        assert!(
            open_paren_indent > 0,
            "OPEN ... FOR parenthesized SELECT should include opening parenthesis line, got:\n{}",
            formatted
        );
        assert_eq!(
            close_paren_indent, open_paren_indent,
            "Closing parenthesis of OPEN ... FOR SELECT should align with opening parenthesis, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_sql_basic_keeps_nested_subquery_connect_by_under_inner_select_in_open_for() {
        let source = "BEGIN\n    OPEN cv FOR (SELECT root_id\n                 FROM roots r\n                 WHERE EXISTS (SELECT child_id\n                               FROM children c\n                               START WITH c.parent_id = r.root_id\n                               CONNECT BY PRIOR c.child_id = c.parent_id));\nEND;";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        let lines: Vec<&str> = formatted.lines().collect();

        let inner_select_indent = lines
            .iter()
            .find(|line| line.trim_start().starts_with("SELECT child_id"))
            .map(|line| leading_spaces(line))
            .unwrap_or(0);
        let inner_connect_by_indent = lines
            .iter()
            .find(|line| line.trim_start().starts_with("CONNECT BY PRIOR c.child_id"))
            .map(|line| leading_spaces(line))
            .unwrap_or(0);

        assert!(
            inner_select_indent > 0,
            "Nested SELECT inside OPEN ... FOR should exist, got:\n{}",
            formatted
        );
        assert_eq!(
            inner_connect_by_indent, inner_select_indent,
            "Nested CONNECT BY should align with nested SELECT, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_sql_basic_keeps_non_select_parenthesis_pairs_aligned_in_open_for_select() {
        let source = "BEGIN
    OPEN cv FOR (SELECT e.employee_id
                 FROM employees e
                 WHERE (e.manager_id IS NULL
                        OR e.manager_id = 100));
END;";
        let formatted = SqlEditorWidget::format_sql_basic(source);

        assert!(
            formatted.contains("WHERE (e.manager_id IS NULL\n                OR e.manager_id = 100)"),
            "Non-SELECT parenthesis pair inside OPEN ... FOR WHERE should stay paired and indented, got:
{}",
            formatted
        );
    }

    #[test]
    fn format_sql_basic_keeps_nested_non_select_parenthesis_alignment_in_open_for_select() {
        let source = "BEGIN
    OPEN cv FOR (SELECT e.employee_id
                 FROM employees e
                 WHERE (e.manager_id IN (100, 200)
                        OR e.department_id = 90));
END;";
        let formatted = SqlEditorWidget::format_sql_basic(source);

        assert!(
            formatted.contains("WHERE (e.manager_id IN (100, 200)\n                OR e.department_id = 90)"),
            "Nested non-SELECT parenthesis pairs in OPEN ... FOR WHERE should stay paired and indented, got:
{}",
            formatted
        );
    }

    #[test]
    fn format_sql_basic_keeps_cursor_select_comment_at_select_list_depth_in_package_body() {
        let source = "create or replace package body pkg_fmt as\n    procedure validate_and_process (p_root_id in number, p_mode in varchar2 default 'NORMAL') is\n        cursor c_units (cp_root_id number) is\n        select id,\n            parent_id\n        -- comment\n            ,\n            code,\n            qty,\n            pri\n        from fmtx_unit;\n    begin\n        null;\n    end validate_and_process;\nend pkg_fmt;";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        let lines: Vec<&str> = formatted.lines().collect();
        let select_idx = lines
            .iter()
            .position(|line| line.trim_start() == "SELECT id,")
            .unwrap_or(0);
        let comment_idx = lines
            .iter()
            .position(|line| line.trim() == "-- comment")
            .unwrap_or(0);
        let comma_idx = lines
            .iter()
            .position(|line| line.trim() == ",")
            .unwrap_or(0);
        let code_idx = lines
            .iter()
            .position(|line| line.trim() == "code,")
            .unwrap_or(0);

        assert_eq!(
            leading_spaces(lines[comment_idx]),
            leading_spaces(lines[code_idx]),
            "Cursor SELECT line comment in package body should match select-list depth, got:\n{}",
            formatted
        );
        assert_eq!(
            leading_spaces(lines[comma_idx]),
            leading_spaces(lines[code_idx]),
            "Comma after package-body cursor line comment should stay on select-list depth, got:\n{}",
            formatted
        );
        assert_eq!(
            leading_spaces(lines[code_idx]),
            leading_spaces(lines[select_idx]).saturating_add(4),
            "Package-body cursor select-list items should stay one level deeper than SELECT, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_sql_basic_keeps_cursor_select_block_comment_at_select_list_depth_in_package_body() {
        let source = "create or replace package body pkg_fmt as\n    procedure validate_and_process is\n        cursor c_units is\n        select id,\n            parent_id\n        /* keep\n           block */\n            ,\n            code\n        from fmtx_unit;\n    begin\n        null;\n    end validate_and_process;\nend pkg_fmt;";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        let lines: Vec<&str> = formatted.lines().collect();
        let block_open_idx = lines
            .iter()
            .position(|line| line.trim() == "/* keep")
            .unwrap_or(0);
        let block_close_idx = lines
            .iter()
            .position(|line| line.trim() == "block */")
            .unwrap_or(0);
        let comma_idx = lines
            .iter()
            .position(|line| line.trim() == ",")
            .unwrap_or(0);
        let code_idx = lines
            .iter()
            .position(|line| line.trim() == "code")
            .unwrap_or(0);

        assert_eq!(
            leading_spaces(lines[block_open_idx]),
            leading_spaces(lines[code_idx]),
            "Block comment opener should match code depth, got:\n{}",
            formatted
        );
        assert_eq!(
            lines[block_close_idx], "           block */",
            "Multiline block comment internal line should preserve original indentation, got:\n{}",
            formatted
        );
        assert_eq!(
            leading_spaces(lines[comma_idx]),
            leading_spaces(lines[code_idx]),
            "Comma line should stay aligned with code after block comment, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_sql_basic_keeps_comment_aligned_before_case_close_paren() {
        let source = "begin\n    v_val := case\n        when flag = 1 then (\n            case\n                when score > 0 then 1\n                else 0\n            end\n            -- keep\n        )\n        else 2\n    end;\nend;";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        let lines: Vec<&str> = formatted.lines().collect();
        let comment_idx = lines
            .iter()
            .position(|line| line.trim() == "-- keep")
            .unwrap_or(0);
        let close_paren_idx = lines
            .iter()
            .position(|line| line.trim() == ")")
            .unwrap_or(0);
        let else_idx = lines
            .iter()
            .enumerate()
            .skip(close_paren_idx.saturating_add(1))
            .find_map(|(idx, line)| {
                if line.trim() == "ELSE" {
                    Some(idx)
                } else {
                    None
                }
            })
            .unwrap_or(0);

        assert_eq!(
            leading_spaces(lines[comment_idx]),
            leading_spaces(lines[close_paren_idx]),
            "Comment before parenthesized CASE closer should match close-paren depth, got:\n{}",
            formatted
        );
        assert_eq!(
            leading_spaces(lines[close_paren_idx]),
            leading_spaces(lines[else_idx]),
            "Parenthesized CASE closer should align with following ELSE branch depth, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_sql_basic_keeps_comment_aligned_before_end_case_close_paren() {
        let source = "begin\n    v_val := case\n        when flag = 1 then (\n            case\n                when score > 0 then 1\n                else 0\n            end case\n            -- keep\n        )\n        else 2\n    end;\nend;";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        let lines: Vec<&str> = formatted.lines().collect();
        let comment_idx = lines
            .iter()
            .position(|line| line.trim() == "-- keep")
            .unwrap_or(0);
        let close_paren_idx = lines
            .iter()
            .position(|line| line.trim() == ")")
            .unwrap_or(0);
        let else_idx = lines
            .iter()
            .enumerate()
            .skip(close_paren_idx.saturating_add(1))
            .find_map(|(idx, line)| {
                if line.trim() == "ELSE" {
                    Some(idx)
                } else {
                    None
                }
            })
            .unwrap_or(0);

        assert_eq!(
            leading_spaces(lines[comment_idx]),
            leading_spaces(lines[close_paren_idx]),
            "Comment before parenthesized END CASE closer should match close-paren depth, got:\n{}",
            formatted
        );
        assert_eq!(
            leading_spaces(lines[close_paren_idx]),
            leading_spaces(lines[else_idx]),
            "Parenthesized END CASE closer should align with following ELSE branch depth, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_sql_basic_keeps_open_cursor_using_comment_aligned_with_following_items() {
        let source = "BEGIN\n    IF a IS NOT NULL THEN\n        OPEN c FOR\n            b,\n            USING d,\n            -- e\n            f,\n                g;\n    END IF;\nEND;";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        assert_eq!(
            formatted,
            "BEGIN\n    IF a IS NOT NULL THEN\n        OPEN c FOR\n            b,\n            USING d,\n                -- e\n                f,\n                g;\n    END IF;\nEND;",
            "OPEN ... FOR USING comment and continuation items should align one level deeper than USING, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_sql_basic_keeps_selected_if_fragment_using_comment_aligned_with_following_items() {
        let source = "IF a IS NOT NULL THEN\n    OPEN c FOR\n        b,\n        USING d,\n        -- e\n        f,\n            g;\nEND IF;";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        assert_eq!(
            formatted,
            "IF a IS NOT NULL THEN\n    OPEN c FOR\n        b,\n        USING d,\n            -- e\n            f,\n            g;\nEND IF;",
            "Selected IF fragment should keep USING comment and continuation items one level deeper than USING, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_sql_basic_indents_comment_in_update_set_clause() {
        let source = "update t1 set col1 = 1\n-- comment\n,col2 = 2\n,col3 = 3;";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        assert!(
            formatted.contains("SET col1 = 1\n    -- comment\n    ,\n    col2 = 2"),
            "Comment in UPDATE SET clause should be at list item depth, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_sql_basic_indents_interleaved_comments_and_commas_in_select() {
        let source =
            "select\ncol1\n-- first section\n,col2\n,col3\n-- second section\n,col4\nfrom t1;";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        assert!(
            formatted.contains("    -- first section\n"),
            "First section comment should be indented, got:\n{}",
            formatted
        );
        assert!(
            formatted.contains("    -- second section\n"),
            "Second section comment should be indented, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_sql_basic_indents_comment_in_group_by_list() {
        let source = "select col1, col2, count(*) from t1 group by col1\n-- comment\n,col2;";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        assert!(
            formatted.contains("GROUP BY col1\n    -- comment\n    ,\n    col2;"),
            "Comment in GROUP BY list should be at list item depth, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_sql_basic_indents_comment_in_order_by_list() {
        let source = "select col1, col2 from t1 order by col1\n-- comment\n,col2;";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        assert!(
            formatted.contains("ORDER BY col1\n    -- comment\n    ,\n    col2;"),
            "Comment in ORDER BY list should be at list item depth, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_sql_basic_keeps_parenthesized_order_by_scalar_subquery_on_list_depth() {
        let source = r#"CREATE OR REPLACE PROCEDURE test_open_with_proc IS
    p_rc SYS_REFCURSOR;
BEGIN
    OPEN p_rc FOR
        WITH base AS (
            SELECT e.empno,
                e.ename,
                e.job,
                e.deptno,
                e.sal,
                e.comm,
                e.mgr
            FROM emp e
        ),
        enriched AS (
            SELECT b.*,
                NVL (b.comm, /* AZ: comm NULL→0 */
                    0) AS eff_comm,
                CASE
                    WHEN b.job = 'PRESIDENT' THEN
                        CASE
                            WHEN b.sal > /* BB: elite 기준 */
                            4000 THEN 'ELITE'
                            ELSE 'SENIOR'
                        END
                    WHEN b.job IN (
                        'MANAGER', 'ANALYST') THEN
                            CASE
                                WHEN b.sal >= 3000 THEN 'HIGH'
                                WHEN b.sal >= /* BD: mid 기준 */
                            2000 THEN 'MID'
                                ELSE 'LOW'
                            END
                    ELSE
                        CASE
                            WHEN NVL (b.comm, /* BE: NULL→0 */
                                0) > 0 THEN 'COMMISSION'
                            ELSE 'BASE'
                        END
                END AS pay_tier
            FROM base b
        )
        SELECT e.empno,
            e.ename,
            e.job,
            e.deptno,
            e.sal,
            e.pay_tier,
            (
                SELECT d.dname
                FROM dept d
                WHERE d.deptno = e.deptno
            ) AS dname
        FROM enriched e
        ORDER BY
        (
            SELECT COUNT(*)
            FROM emp e2
            WHERE e2.deptno = e.deptno
        ) DESC,
            e.sal DESC NULLS LAST;
END;
/"#;

        let formatted = SqlEditorWidget::format_sql_basic(source);
        let lines: Vec<&str> = formatted.lines().collect();
        let leading_spaces = |line: &str| line.len().saturating_sub(line.trim_start().len());

        let order_by_idx = lines
            .iter()
            .position(|line| line.trim_start() == "ORDER BY")
            .unwrap_or(0);
        let open_paren_idx = lines
            .iter()
            .enumerate()
            .skip(order_by_idx.saturating_add(1))
            .find(|(_, line)| line.trim_start() == "(")
            .map(|(idx, _)| idx)
            .unwrap_or(0);
        let scalar_select_idx = lines
            .iter()
            .enumerate()
            .skip(open_paren_idx.saturating_add(1))
            .find(|(_, line)| line.trim_start().starts_with("SELECT COUNT"))
            .map(|(idx, _)| idx)
            .unwrap_or(0);
        let close_paren_idx = lines
            .iter()
            .enumerate()
            .skip(scalar_select_idx.saturating_add(1))
            .find(|(_, line)| line.trim_start().starts_with(") DESC,"))
            .map(|(idx, _)| idx)
            .unwrap_or(0);
        let second_sort_key_idx = lines
            .iter()
            .enumerate()
            .skip(close_paren_idx.saturating_add(1))
            .find(|(_, line)| line.trim_start().starts_with("e.sal DESC NULLS LAST;"))
            .map(|(idx, _)| idx)
            .unwrap_or(0);

        assert!(
            leading_spaces(lines[open_paren_idx]) > leading_spaces(lines[order_by_idx]),
            "ORDER BY scalar-subquery sort key should indent deeper than ORDER BY header, got:\n{}",
            formatted
        );
        assert!(
            leading_spaces(lines[scalar_select_idx]) > leading_spaces(lines[open_paren_idx]),
            "Scalar subquery SELECT should indent deeper than its opening paren, got:\n{}",
            formatted
        );
        assert_eq!(
            leading_spaces(lines[close_paren_idx]),
            leading_spaces(lines[open_paren_idx]),
            "Scalar subquery close paren in ORDER BY should align with its opening paren, got:\n{}",
            formatted
        );
        assert_eq!(
            leading_spaces(lines[second_sort_key_idx]),
            leading_spaces(lines[open_paren_idx]),
            "Subsequent ORDER BY sort keys should align with the parenthesized sort key, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_sql_basic_indents_comment_in_from_list() {
        let source = "select * from t1\n-- join next table\n,t2\n,t3 where t1.id = t2.id;";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        assert!(
            formatted.contains("FROM t1\n    -- join next table\n    ,\n    t2"),
            "Comment in FROM table list should be at list item depth, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_sql_basic_indents_comment_in_returning_list() {
        let source = "insert into t1 values (1, 2) returning col1\n-- comment\n,col2 into v1, v2;";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        assert!(
            formatted.contains("RETURNING col1\n    -- comment\n    ,\n    col2"),
            "Comment in RETURNING list should be at list item depth, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_sql_basic_indents_comment_before_and_in_where() {
        let source = "select * from t1 where col1 = 1\n-- check col2\nand col2 = 2\n-- check col3\nand col3 = 3;";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        assert!(
            formatted.contains("    -- check col2\n    AND col2 = 2"),
            "Comment before AND should match AND indent, got:\n{}",
            formatted
        );
        assert!(
            formatted.contains("    -- check col3\n    AND col3 = 3"),
            "Comment before second AND should match AND indent, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_sql_basic_indents_comment_before_and_in_join_on() {
        let source =
            "select * from t1 join t2 on t1.id = t2.id\n-- extra condition\nand t1.status = 'A';";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        assert!(
            formatted.contains("        -- extra condition\n        AND t1.status"),
            "Comment before AND in ON clause should be indented deeper than ON, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_sql_basic_indents_comment_in_insert_column_list() {
        let source = "insert into t1 (col1\n-- comment\n,col2\n,col3) values (1, 2, 3);";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        assert!(
            formatted.contains("    -- comment\n    ,"),
            "Comment in INSERT column list should be indented, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_sql_basic_indents_comment_in_values_list() {
        let source = "insert into t1 (col1, col2, col3) values (1\n-- comment\n,2\n,3);";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        assert!(
            formatted.contains("    -- comment\n    ,"),
            "Comment in VALUES list should be indented, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_sql_basic_indents_comment_in_create_table_columns() {
        let source = "create table t1 (col1 number\n-- comment\n,col2 varchar2(100)\n,col3 date);";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        assert!(
            formatted.contains("col1 NUMBER,\n    -- comment\n    col2"),
            "Comment in CREATE TABLE column list should be at column indent, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_sql_basic_aligns_comment_with_else_in_plsql_if() {
        let source = "begin\nif true then\n-- do something\nnull;\n-- else branch\nelse\nnull;\nend if;\nend;";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        // Comment before ELSE should be at same indent as ELSE (4 spaces)
        assert!(
            formatted.contains("\n    -- else branch\n    ELSE"),
            "Comment before ELSE should align with ELSE keyword, got:\n{}",
            formatted
        );
        // Comment inside IF body should be at body indent (8 spaces)
        assert!(
            formatted.contains("\n        -- do something\n"),
            "Comment inside IF body should be at body indent, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_sql_basic_aligns_comment_with_when_in_case() {
        let source = "select case\n-- first case\nwhen col1 = 1 then 'a'\n-- second case\nwhen col1 = 2 then 'b'\n-- default\nelse 'c'\nend from t1;";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        // Comments before WHEN should be at same indent as WHEN (8 spaces)
        assert!(
            formatted.contains("        -- first case\n        WHEN"),
            "Comment before WHEN should align with WHEN keyword, got:\n{}",
            formatted
        );
        // Comment before ELSE in CASE should align with ELSE
        assert!(
            formatted.contains("        -- default\n        ELSE"),
            "Comment before ELSE in CASE should align with ELSE, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_sql_basic_aligns_comment_before_or_in_where() {
        let source = "select * from t1 where col1 = 1\n-- or branch\nor col2 = 2;";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        assert!(
            formatted.contains("    -- or branch\n    OR"),
            "Comment before OR should align with OR keyword, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_sql_basic_nested_plsql_comment_depth() {
        let source = "begin\nif true then\nfor i in 1..10 loop\n-- deep comment\nnull;\nend loop;\nend if;\nend;";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        assert!(
            formatted.contains("            -- deep comment\n            NULL;"),
            "Comment in nested PL/SQL block should be at correct depth, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_sql_basic_create_table_leading_comment_placement() {
        let source = "create table t1 (\n-- primary key\ncol1 number\n-- description\n,col2 varchar2(100)\n,col3 date);";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        // Leading comment before col1 should appear before col1
        assert!(
            formatted.contains("    -- primary key\n    col1 NUMBER,"),
            "Leading comment should appear before its column, got:\n{}",
            formatted
        );
        // Comment before col2 should appear before col2
        assert!(
            formatted.contains("    -- description\n    col2"),
            "Comment before col2 should appear before col2, got:\n{}",
            formatted
        );
    }

    // ── Pattern 1: next_is_else 누락 케이스 ──

    #[test]
    fn format_sql_basic_aligns_comment_before_exception() {
        let source = "begin\nnull;\n-- handle errors\nexception\nwhen others then\nnull;\nend;";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        // EXCEPTION is at BEGIN level (indent 0), comment should match
        assert!(
            formatted.contains("\n-- handle errors\nEXCEPTION"),
            "Comment before EXCEPTION should align with EXCEPTION at base indent, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_sql_basic_aligns_comment_before_exception_nested() {
        let source =
            "begin\nif true then\nbegin\nnull;\n-- nested exception\nexception\nwhen others then\nnull;\nend;\nend if;\nend;";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        // Nested BEGIN..EXCEPTION: comment should be at nested BEGIN level (8 spaces)
        assert!(
            formatted.contains("        -- nested exception\n        EXCEPTION"),
            "Comment before nested EXCEPTION should align with EXCEPTION, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_sql_basic_aligns_comment_before_elsif() {
        let source =
            "begin\nif a = 1 then\nnull;\n-- check second\nelsif a = 2 then\nnull;\n-- fallback\nelse\nnull;\nend if;\nend;";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        assert!(
            formatted.contains("    -- check second\n    ELSIF"),
            "Comment before ELSIF should align with ELSIF, got:\n{}",
            formatted
        );
        assert!(
            formatted.contains("    -- fallback\n    ELSE"),
            "Comment before ELSE should align with ELSE, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_sql_basic_aligns_block_comment_before_else_in_plsql_if() {
        let source = "begin\nif true then\nnull;\n/* else branch */\nelse\nnull;\nend if;\nend;";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        assert!(
            formatted.contains("    /* else branch */\n    ELSE"),
            "Block comment before ELSE should align with ELSE keyword, got:\n{}",
            formatted
        );
    }

    // ── Pattern 2: next_is_condition_keyword 누락 케이스 ──

    #[test]
    fn format_sql_basic_aligns_comment_before_when_in_plsql_exception() {
        let source =
            "begin\nnull;\nexception\n-- not found\nwhen no_data_found then\nnull;\n-- others\nwhen others then\nnull;\nend;";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        assert!(
            formatted.contains("    -- not found\n    WHEN"),
            "Comment before WHEN in EXCEPTION should align with WHEN, got:\n{}",
            formatted
        );
        assert!(
            formatted.contains("    -- others\n    WHEN"),
            "Comment before second WHEN in EXCEPTION should align with WHEN, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_sql_basic_aligns_comment_before_on_in_join() {
        let source = "select * from t1 join t2\n-- join condition\non t1.id = t2.id;";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        assert!(
            formatted.contains("    -- join condition\n    ON"),
            "Comment before ON in JOIN should align with ON keyword, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_sql_basic_aligns_comment_before_and_in_having() {
        let source =
            "select col1, count(*) from t1 group by col1 having count(*) > 1\n-- extra filter\nand sum(col2) > 10;";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        assert!(
            formatted.contains("    -- extra filter\n    AND"),
            "Comment before AND in HAVING should align with AND keyword, got:\n{}",
            formatted
        );
    }

    // ── Pattern 3: in_confirmed_list 누락 케이스 ──

    #[test]
    fn format_sql_basic_indents_comment_in_having_list() {
        let source = "select col1, col2, count(*) from t1 group by col1\n-- comment\n,col2;";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        assert!(
            formatted.contains("    -- comment\n    ,"),
            "Comment in GROUP BY list should be at list item depth, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_sql_basic_indents_comment_in_window_clause_list() {
        let source = "select col1 from t1 order by col1\n-- secondary sort\n,col2;";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        assert!(
            formatted.contains("    -- secondary sort\n    ,"),
            "Comment between ORDER BY items should be at list item depth, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_sql_basic_indents_comment_in_merge_matched_set_list() {
        let source = "merge into t1 using t2 on (t1.id = t2.id) when matched then update set t1.col1 = t2.col1\n-- update col2\n,t1.col2 = t2.col2;";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        assert!(
            formatted.contains("    -- update col2\n    ,"),
            "Comment in MERGE UPDATE SET list should be at list item depth, got:\n{}",
            formatted
        );
    }

    // ── paren_extra: 괄호 내부에서 condition/ELSE 주석 정렬 ──

    #[test]
    fn format_sql_basic_aligns_comment_before_and_inside_parens() {
        let source = "select * from t1 where func(col1 = 1\n-- extra\nand col2 = 2);";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        let lines: Vec<&str> = formatted.lines().collect();
        let comment_line = lines.iter().find(|l| l.contains("-- extra")).unwrap();
        let and_line = lines
            .iter()
            .find(|l| l.trim_start().starts_with("AND col2"))
            .unwrap();
        assert_eq!(
            leading_spaces(comment_line),
            leading_spaces(and_line),
            "Comment before AND inside parens should match AND indent, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_sql_basic_aligns_comment_before_else_in_case_inside_parens() {
        let source = "select func(case when col1 = 1 then 'a'\n-- default\nelse 'b' end) from t1;";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        let lines: Vec<&str> = formatted.lines().collect();
        let comment_line = lines.iter().find(|l| l.contains("-- default")).unwrap();
        let else_line = lines
            .iter()
            .find(|l| l.trim_start().starts_with("ELSE"))
            .unwrap();
        assert_eq!(
            leading_spaces(comment_line),
            leading_spaces(else_line),
            "Comment before ELSE in CASE inside parens should match ELSE indent, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_sql_basic_aligns_comment_before_when_in_case_inside_parens() {
        let source =
            "select func(case\n-- first branch\nwhen col1 = 1 then 'a'\n-- second branch\nwhen col1 = 2 then 'b' end) from t1;";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        let lines: Vec<&str> = formatted.lines().collect();
        let comment_line = lines
            .iter()
            .find(|l| l.contains("-- first branch"))
            .unwrap();
        let when_line = lines
            .iter()
            .find(|l| l.trim_start().starts_with("WHEN col1 = 1"))
            .unwrap();
        assert_eq!(
            leading_spaces(comment_line),
            leading_spaces(when_line),
            "Comment before WHEN in CASE inside parens should match WHEN indent, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_sql_basic_aligns_comment_before_or_inside_parens() {
        let source = "select * from t1 where (col1 = 1\n-- alt condition\nor col2 = 2);";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        let lines: Vec<&str> = formatted.lines().collect();
        let comment_line = lines
            .iter()
            .find(|l| l.contains("-- alt condition"))
            .unwrap();
        let or_line = lines
            .iter()
            .find(|l| l.trim_start().starts_with("OR col2"))
            .unwrap();
        assert_eq!(
            leading_spaces(comment_line),
            leading_spaces(or_line),
            "Comment before OR inside parens should match OR indent, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_sql_basic_keeps_condition_operand_after_inline_block_comment_on_operator_depth() {
        let source = "select 1 from order_item oi where oi.order_id = v.order_id and oi.qty <= /* X: 0 이하 */ 0;";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        let expected = [
            "SELECT 1",
            "FROM order_item oi",
            "WHERE oi.order_id = v.order_id",
            "    AND oi.qty <= /* X: 0 이하 */",
            "        0;",
        ]
        .join("\n");

        assert_eq!(formatted, expected);
    }

    #[test]
    fn format_sql_basic_select_star_comment_does_not_turn_from_into_rhs_continuation() {
        let source = "select * -- all columns\nfrom emp;";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        let lines: Vec<&str> = formatted.lines().collect();
        let indent = |line: &str| line.len().saturating_sub(line.trim_start().len());

        let select_idx = lines
            .iter()
            .position(|line| line.trim_start().starts_with("SELECT *"))
            .expect("SELECT * line should exist");
        let from_idx = lines
            .iter()
            .position(|line| line.trim_start() == "FROM emp;")
            .expect("FROM line should exist");

        assert!(
            lines[select_idx].contains("-- all columns"),
            "SELECT * inline comment should be preserved, got:\n{}",
            formatted
        );
        assert_eq!(
            indent(lines[from_idx]),
            indent(lines[select_idx]),
            "projection marker `*` must not be misclassified as a trailing continuation operator, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_sql_basic_keeps_inline_comment_operand_depth_inside_not_exists_subquery_on_operator_depth(
    ) {
        let source = r#"SELECT *
FROM (
        /* Q: 인라인뷰 시작 */
        WITH x AS (
            SELECT
                p.order_id,
                p.cust_name,
                p.order_dt,
                a.amt
            FROM paid p
            JOIN amounts a
                ON /* R: join key */
                a.order_id = p.order_id
            WHERE a.amt > /* S: threshold */
                50
        )
        SELECT
            x.*,
            (
                -- [T] 라인수 서브쿼리
                SELECT COUNT (*)
                FROM order_item oi
                WHERE oi.order_id = x.order_id
            ) AS line_cnt
        FROM x
    ) v
WHERE EXISTS (
        /* U: SKU 존재 조건 */
        SELECT 1
        FROM order_item oi
        WHERE oi.order_id = v.order_id
            AND oi.sku LIKE 'SKU-%' -- [V] SKU 패턴
    )
    AND NOT EXISTS (
        -- [W] 음수 수량 배제
        SELECT 1
        FROM order_item oi
        WHERE oi.order_id = v.order_id
            AND oi.qty <= /* X: 0 이하 */
        0
    )
ORDER BY v.amt DESC;"#;
        let formatted = SqlEditorWidget::format_sql_basic(source);
        let lines: Vec<&str> = formatted.lines().collect();
        let indent = |line: &str| line.len().saturating_sub(line.trim_start().len());
        let and_idx = lines
            .iter()
            .position(|line| line.trim_start().starts_with("AND oi.qty <="))
            .expect("formatted SQL should contain the NOT EXISTS operand owner line");
        let operand_idx = lines
            .iter()
            .position(|line| line.trim() == "0")
            .expect("formatted SQL should contain the split operand line");
        let close_idx = lines
            .iter()
            .enumerate()
            .skip(operand_idx.saturating_add(1))
            .find(|(_, line)| line.trim() == ")")
            .map(|(idx, _)| idx)
            .expect("formatted SQL should contain the NOT EXISTS closing parenthesis");

        assert_eq!(
            indent(lines[operand_idx]),
            indent(lines[and_idx]).saturating_add(4),
            "operand after inline block comment inside NOT EXISTS should indent one level deeper than the operator owner, got:\n{}",
            formatted
        );
        assert!(
            indent(lines[close_idx]) < indent(lines[operand_idx]),
            "closing parenthesis after the operand should dedent from the NOT EXISTS condition body, got:\n{}",
            formatted
        );

        let reformatted = SqlEditorWidget::format_sql_basic(&formatted);
        assert_eq!(
            reformatted, formatted,
            "formatting should be stable for NOT EXISTS inline-comment operand continuation"
        );
    }
}

#[cfg(test)]
mod format_indent_gap_tests {
    use crate::sql_delimiter::line_start_snapshot_before_token;
    use crate::ui::sql_editor::SqlEditorWidget;
    use crate::ui::SqlToken;

    fn leading_spaces(line: &str) -> usize {
        line.len().saturating_sub(line.trim_start().len())
    }

    fn find_line_starting_with(lines: &[&str], prefix: &str) -> Option<usize> {
        lines
            .iter()
            .position(|line| line.trim_start().starts_with(prefix))
    }

    // ── CURSOR IS/AS SELECT body indent ──

    #[test]
    fn format_for_auto_formatting_treats_comment_glued_from_as_standalone_header() {
        let source = r#"select
    e.empno
from /* source rows */
    emp e
where e.empno = 1;"#;

        let formatted = SqlEditorWidget::format_for_auto_formatting(source, false);
        let lines: Vec<&str> = formatted.lines().collect();
        let from_idx =
            find_line_starting_with(&lines, "FROM /* source rows */").expect("FROM line");
        let from_item_idx = find_line_starting_with(&lines, "emp e").expect("FROM item line");
        let where_idx = find_line_starting_with(&lines, "WHERE e.empno = 1;").expect("WHERE line");

        assert_eq!(
            leading_spaces(lines[from_item_idx]),
            leading_spaces(lines[from_idx]).saturating_add(4),
            "line after comment-glued bare FROM should render one level deeper than FROM, got:\n{}",
            formatted
        );
        assert_eq!(
            leading_spaces(lines[where_idx]),
            leading_spaces(lines[from_idx]),
            "WHERE after comment-glued bare FROM should return to the query base depth, got:\n{}",
            formatted
        );
        assert_eq!(
            SqlEditorWidget::format_for_auto_formatting(&formatted, false),
            formatted,
            "formatting should stay stable for comment-glued bare FROM headers"
        );
    }

    #[test]
    fn format_for_auto_formatting_clears_select_list_carry_before_final_from_dual() {
        let source = r#"SELECT 'INSERT INTO t VALUES (1; 2; 3);' AS fake_sql,
    'END; / BEGIN' AS trap1,
    'CREATE OR REPLACE PROCEDURE test IS BEGIN NULL; END;' AS trap2,
    q'[She said "it's done"; then left/]' AS trap3
    FROM DUAL;"#;

        let expected = r#"SELECT 'INSERT INTO t VALUES (1; 2; 3);' AS fake_sql,
    'END; / BEGIN' AS trap1,
    'CREATE OR REPLACE PROCEDURE test IS BEGIN NULL; END;' AS trap2,
    q'[She said "it's done"; then left/]' AS trap3
FROM DUAL;"#;

        let formatted = SqlEditorWidget::format_for_auto_formatting(source, false);

        assert_eq!(
            formatted, expected,
            "final FROM DUAL should realign to the query base after the last SELECT item, got:\n{}",
            formatted
        );
        assert_eq!(
            SqlEditorWidget::format_for_auto_formatting(&formatted, false),
            formatted,
            "formatting should remain idempotent for SELECT items containing statement-like text"
        );
    }

    #[test]
    fn format_for_auto_formatting_keeps_with_function_return_in_body_and_end_on_owner_depth() {
        let source = r#"WITH
    FUNCTION calc_depth (p_id NUMBER) RETURN NUMBER IS
        v_depth NUMBER;
    BEGIN
        SELECT MAX (LEVEL)
        INTO v_depth
        FROM org_tree
        START WITH parent_id IS NULL
        CONNECT BY PRIOR node_id = parent_id;
    RETURN v_depth;
END calc_depth;"#;

        let formatted = SqlEditorWidget::format_for_auto_formatting(source, false);
        let lines: Vec<&str> = formatted.lines().collect();
        let begin_idx = find_line_starting_with(&lines, "BEGIN").expect("BEGIN line");
        let return_idx = find_line_starting_with(&lines, "RETURN v_depth;").expect("RETURN line");
        let end_idx = find_line_starting_with(&lines, "END calc_depth;").expect("END line");

        assert_eq!(
            leading_spaces(lines[return_idx]),
            leading_spaces(lines[begin_idx]).saturating_add(4),
            "WITH FUNCTION RETURN should stay on the routine body depth under BEGIN, got:\n{}",
            formatted
        );
        assert_eq!(
            leading_spaces(lines[end_idx]),
            leading_spaces(lines[begin_idx]),
            "WITH FUNCTION END should realign with the routine owner depth, got:\n{}",
            formatted
        );
        assert_eq!(
            SqlEditorWidget::format_for_auto_formatting(&formatted, false),
            formatted,
            "WITH FUNCTION routine body formatting should remain idempotent"
        );
    }

    #[test]
    fn format_for_auto_formatting_keeps_package_body_named_end_before_final_end_stable() {
        let source = r#"create package body a is
    procedure b (c in varchar2) is
    begin
        if (1 = 1) then
            begin
                insert into d (e)
                values (1);
            end;
        end if;
        open v for
            select 1
            from dual;
    end b;
end;"#;
        let expected = r#"CREATE PACKAGE BODY a IS
    PROCEDURE b (c IN VARCHAR2) IS
    BEGIN
        IF (1 = 1) THEN
            BEGIN
                INSERT INTO d (e)
                VALUES (1);
            END;
        END IF;
        OPEN v FOR
            SELECT 1
            FROM DUAL;
    END b;
END;"#;

        let formatted = SqlEditorWidget::format_for_auto_formatting(source, false);

        assert_eq!(
            formatted.trim(),
            expected.trim(),
            "auto-formatting should keep package member END and final END aligned, got:\n{}",
            formatted
        );
        assert_eq!(
            SqlEditorWidget::format_for_auto_formatting(&formatted, false),
            formatted,
            "package body auto-formatting should remain idempotent for named END before final END"
        );
    }

    #[test]
    fn format_sql_basic_no_cache_keeps_package_body_named_end_before_final_end_stable() {
        let source = r#"create package body a is
    procedure b (c in varchar2) is
    begin
        if (1 = 1) then
            begin
                insert into d (e)
                values (1);
            end;
        end if;
        open v for
            select 1
            from dual;
    end b;
end;"#;
        let expected = r#"CREATE PACKAGE BODY a IS
    PROCEDURE b (c IN VARCHAR2) IS
    BEGIN
        IF (1 = 1) THEN
            BEGIN
                INSERT INTO d (e)
                VALUES (1);
            END;
        END IF;
        OPEN v FOR
            SELECT 1
            FROM DUAL;
    END b;
END;"#;

        let formatted = SqlEditorWidget::format_sql_basic_no_cache(source);

        assert_eq!(
            formatted.trim(),
            expected.trim(),
            "no-cache formatting should keep package member END and final END aligned after OPEN ... FOR, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_for_auto_formatting_keeps_package_body_unlabeled_final_end_attached_before_trailing_statement(
    ) {
        let source = r#"create package body a is
    procedure b (c in varchar2) is
    begin
        if (1 = 1) then
            begin
                insert into d (e)
                values (1);
            end;
        end if;
        open v for
            select 1
            from dual;
    end b;
end;
select 1 from dual;"#;
        let expected = r#"CREATE PACKAGE BODY a IS
    PROCEDURE b (c IN VARCHAR2) IS
    BEGIN
        IF (1 = 1) THEN
            BEGIN
                INSERT INTO d (e)
                VALUES (1);
            END;
        END IF;
        OPEN v FOR
            SELECT 1
            FROM DUAL;
    END b;
END;

SELECT 1
FROM DUAL;"#;

        let formatted = SqlEditorWidget::format_for_auto_formatting(source, false);

        assert_eq!(
            formatted.trim(),
            expected.trim(),
            "auto-formatting should keep the final package END attached before the next statement, got:\n{}",
            formatted
        );
    }

    #[test]
    fn split_format_items_keeps_package_body_unlabeled_final_end_with_preceding_statement() {
        let source = r#"create package body a is
    procedure b (c in varchar2) is
    begin
        if (1 = 1) then
            begin
                insert into d (e)
                values (1);
            end;
        end if;
        open v for
            select 1
            from dual;
    end b;
end;
select 1 from dual;"#;

        let items = crate::db::QueryExecutor::split_format_items(source);
        let statements: Vec<&str> = items
            .iter()
            .filter_map(|item| match item {
                crate::db::query::FormatItem::Statement(statement) => Some(statement.as_str()),
                _ => None,
            })
            .collect();

        assert_eq!(
            statements.len(),
            2,
            "format splitter should keep package body + trailing SELECT as two statements, got: {statements:?}"
        );
        assert!(
            statements[0]
                .trim_end()
                .to_ascii_uppercase()
                .ends_with("END;"),
            "package body format item should keep the final END terminator attached, got:\n{}",
            statements[0]
        );
        assert!(
            statements[1].trim_start().starts_with("select 1 from dual"),
            "second format item should stay on the trailing SELECT, got:\n{}",
            statements[1]
        );
    }

    #[test]
    fn format_for_auto_formatting_keeps_trailing_with_function_sibling_cte_on_with_depth() {
        let source = r#"WITH
    FUNCTION calc_depth (p_id NUMBER) RETURN NUMBER IS
        v_depth NUMBER;
    BEGIN
        SELECT MAX (LEVEL)
        INTO v_depth
        FROM org_tree
        START WITH parent_id IS NULL
        CONNECT BY PRIOR node_id = parent_id;
    RETURN v_depth;
END calc_depth;
recursive_tree (node_id, parent_id, node_name, DEPTH, PATH) AS (
    SELECT
        node_id,
        parent_id,
        node_name,
        1 AS DEPTH,
        CAST (node_name AS VARCHAR2 (4000)) AS PATH
    FROM org_tree
    WHERE parent_id IS NULL
    UNION ALL
    SELECT
        t.node_id,
        t.parent_id,
        t.node_name,
        rt.DEPTH + 1,
        rt.PATH || ' > ' || t.node_name
    FROM org_tree t
    JOIN recursive_tree rt
        ON t.parent_id = rt.node_id
    WHERE rt.DEPTH < calc_depth (t.node_id)
),
    aggregated AS (
        SELECT
            parent_id,
            COUNT (*) AS child_count,
            MAX (DEPTH) AS max_depth,
            LISTAGG (node_name, ', ') WITHIN GROUP (ORDER BY node_name) AS children
        FROM recursive_tree
        WHERE DEPTH > 1
        GROUP BY parent_id
    );"#;

        let expected = r#"WITH
    FUNCTION calc_depth (p_id NUMBER) RETURN NUMBER IS
        v_depth NUMBER;
    BEGIN
        SELECT MAX (LEVEL)
        INTO v_depth
        FROM org_tree
        START WITH parent_id IS NULL
        CONNECT BY PRIOR node_id = parent_id;
        RETURN v_depth;
    END calc_depth;
recursive_tree (node_id, parent_id, node_name, DEPTH, PATH) AS (
    SELECT
        node_id,
        parent_id,
        node_name,
        1 AS DEPTH,
        CAST (node_name AS VARCHAR2 (4000)) AS PATH
    FROM org_tree
    WHERE parent_id IS NULL
    UNION ALL
    SELECT
        t.node_id,
        t.parent_id,
        t.node_name,
        rt.DEPTH + 1,
        rt.PATH || ' > ' || t.node_name
    FROM org_tree t
    JOIN recursive_tree rt
        ON t.parent_id = rt.node_id
    WHERE rt.DEPTH < calc_depth (t.node_id)
),
aggregated AS (
    SELECT
        parent_id,
        COUNT(*) AS child_count,
        MAX (DEPTH) AS max_depth,
        LISTAGG (node_name, ', ') WITHIN GROUP (ORDER BY node_name) AS children
    FROM recursive_tree
    WHERE DEPTH > 1
    GROUP BY parent_id
);"#;

        let formatted = SqlEditorWidget::format_for_auto_formatting(source, false);

        assert_eq!(
            formatted, expected,
            "Trailing sibling CTE headers after a WITH FUNCTION body should realign to the WITH owner depth even when no main query follows, got:\n{}",
            formatted
        );
        assert_eq!(
            SqlEditorWidget::format_for_auto_formatting(&formatted, false),
            formatted,
            "Trailing sibling CTE formatting after WITH FUNCTION should remain stable after re-formatting"
        );
    }

    #[test]
    fn format_sql_basic_indents_cursor_is_select_body() {
        let source = "declare\ncursor c1 is\nselect col1, col2\nfrom t1\nwhere col1 > 0;\nbegin\nnull;\nend;";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        assert!(
            formatted.contains("    CURSOR c1 IS\n        SELECT"),
            "SELECT body should be indented under CURSOR IS, got:\n{}",
            formatted
        );
        assert!(
            formatted.contains("        FROM t1"),
            "FROM should be at CURSOR body depth, got:\n{}",
            formatted
        );
        // BEGIN should return to DECLARE level
        assert!(
            formatted.contains("BEGIN\n    NULL;"),
            "BEGIN should return to top level after CURSOR, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_sql_basic_indents_cursor_with_params_is_select_body() {
        let source = "declare\ncursor c1 (p1 number) is\nselect col1\nfrom t1\nwhere col1 = p1;\nbegin\nnull;\nend;";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        assert!(
            formatted.contains("    CURSOR c1 (p1 NUMBER) IS\n        SELECT"),
            "Parameterized CURSOR SELECT should be indented, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_sql_basic_indents_nested_cursor_declaration() {
        let source =
            "begin\ndeclare\ncursor c1 is\nselect col1 from t1;\nbegin\nopen c1;\nend;\nend;";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        assert!(
            formatted.contains("        CURSOR c1 IS\n            SELECT"),
            "Nested CURSOR SELECT should be indented, got:\n{}",
            formatted
        );
    }

    // ── FORALL body indent ──

    #[test]
    fn format_sql_basic_indents_forall_insert_body() {
        let source = "begin\nforall i in 1..v_arr.count\ninsert into t1 values (v_arr(i));\nend;";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        assert!(
            formatted.contains("    FORALL i IN 1..v_arr.COUNT\n        INSERT INTO"),
            "FORALL INSERT body should be indented, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_sql_basic_indents_forall_update_body() {
        let source = "begin\nforall i in 1..v_arr.count\nupdate t1 set col1 = v_arr(i)\nwhere id = v_id(i);\nend;";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        assert!(
            formatted.contains("    FORALL i IN 1..v_arr.COUNT\n        UPDATE"),
            "FORALL UPDATE body should be indented, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_sql_basic_indents_forall_delete_body() {
        let source = "begin\nforall i in 1..v_arr.count\ndelete from t1 where id = v_arr(i);\nend;";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        assert!(
            formatted.contains("    FORALL i IN 1..v_arr.COUNT\n        DELETE"),
            "FORALL DELETE body should be indented, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_sql_basic_nested_forall_update_body_uses_forall_body_depth() {
        let source = "begin\nif v_ready = 'Y' then\nforall i in 1..v_arr.count\nupdate t1 set col1 = v_arr(i)\nwhere id = v_id(i);\nend if;\nend;";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        let lines: Vec<&str> = formatted.lines().collect();
        let indent = |line: &str| line.len().saturating_sub(line.trim_start().len());

        let if_idx = lines
            .iter()
            .position(|line| line.trim_start() == "IF v_ready = 'Y' THEN")
            .expect("should contain IF line");
        let forall_idx = lines
            .iter()
            .position(|line| line.trim_start().starts_with("FORALL i IN 1..v_arr.COUNT"))
            .expect("should contain FORALL line");
        let update_idx = lines
            .iter()
            .position(|line| line.trim_start() == "UPDATE t1")
            .expect("should contain UPDATE line");
        let set_idx = lines
            .iter()
            .position(|line| line.trim_start().starts_with("SET col1 ="))
            .expect("should contain SET line");
        let where_idx = lines
            .iter()
            .position(|line| line.trim_start().starts_with("WHERE id ="))
            .expect("should contain WHERE line");

        assert_eq!(
            indent(lines[forall_idx]),
            indent(lines[if_idx]).saturating_add(4),
            "FORALL should stay one level deeper than the IF body owner, got:\n{}",
            formatted
        );
        assert_eq!(
            indent(lines[update_idx]),
            indent(lines[forall_idx]).saturating_add(4),
            "FORALL DML body should stay one level deeper than FORALL, got:\n{}",
            formatted
        );
        assert_eq!(
            indent(lines[set_idx]),
            indent(lines[update_idx]),
            "SET inside nested FORALL UPDATE should stay on the UPDATE body depth, got:\n{}",
            formatted
        );
        assert_eq!(
            indent(lines[where_idx]),
            indent(lines[update_idx]),
            "WHERE inside nested FORALL UPDATE should stay on the UPDATE body depth, got:\n{}",
            formatted
        );
    }

    // ── EXECUTE IMMEDIATE INTO/USING inline ──

    #[test]
    fn format_sql_basic_keeps_execute_immediate_into_using_inline() {
        let source =
            "begin\nexecute immediate 'select 1 from dual' into v_result using v_param;\nend;";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        assert!(
            formatted
                .contains("EXECUTE IMMEDIATE 'select 1 from dual' INTO v_result USING v_param;"),
            "EXECUTE IMMEDIATE INTO/USING should stay inline, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_sql_basic_keeps_execute_immediate_using_only_inline() {
        let source = "begin\nexecute immediate v_sql using v_param1, v_param2;\nend;";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        assert!(
            formatted.contains("EXECUTE IMMEDIATE v_sql USING v_param1, v_param2;"),
            "EXECUTE IMMEDIATE USING should stay inline, got:\n{}",
            formatted
        );
    }

    // ── CREATE INDEX ON inline ──

    #[test]
    fn format_sql_basic_keeps_create_index_on_inline() {
        let source = "create index idx1 on t1 (col1, col2);";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        assert!(
            formatted.contains("CREATE INDEX idx1 ON t1"),
            "CREATE INDEX ON should stay on same line, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_sql_basic_keeps_create_unique_index_on_inline() {
        let source = "create unique index idx1 on t1 (col1, col2) tablespace users;";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        assert!(
            formatted.contains("CREATE UNIQUE INDEX idx1 ON t1"),
            "CREATE UNIQUE INDEX ON should stay on same line, got:\n{}",
            formatted
        );
    }

    // ── COMMENT ON inline ──

    #[test]
    fn format_sql_basic_keeps_comment_on_table_inline() {
        let source = "comment on table t1 is 'This is a table';";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        assert!(
            formatted.contains("COMMENT ON TABLE"),
            "COMMENT ON TABLE should stay inline, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_sql_basic_keeps_comment_on_column_inline() {
        let source = "comment on column t1.col1 is 'description';";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        assert!(
            formatted.contains("COMMENT ON COLUMN"),
            "COMMENT ON COLUMN should stay inline, got:\n{}",
            formatted
        );
    }

    // ── GRANT/REVOKE inline ──

    #[test]
    fn format_sql_basic_keeps_grant_inline() {
        let source = "grant select, insert on schema1.t1 to user1;";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        assert!(
            formatted.contains("GRANT SELECT, INSERT ON"),
            "GRANT privileges should stay inline, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_sql_basic_keeps_revoke_inline() {
        let source = "revoke select on schema1.t1 from user1;";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        assert!(
            formatted.contains("REVOKE SELECT ON"),
            "REVOKE privileges should stay inline, got:\n{}",
            formatted
        );
    }

    // ── MERGE WHEN MATCHED indent ──

    #[test]
    fn format_sql_basic_merge_when_matched_indent() {
        let source = "merge into t1 using t2 on (t1.id = t2.id) when matched then update set t1.col = t2.col when not matched then insert (col) values (t2.col);";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        let lines: Vec<&str> = formatted.lines().collect();
        // WHEN MATCHED should be at base indent
        let when_matched = lines.iter().find(|l| l.contains("WHEN MATCHED")).unwrap();
        assert!(
            !when_matched.starts_with("    "),
            "WHEN MATCHED should not be deeply indented, got:\n{}",
            formatted
        );
        // UPDATE should be indented under WHEN MATCHED
        let update_line = lines
            .iter()
            .find(|l| l.trim_start().starts_with("UPDATE"))
            .unwrap();
        assert!(
            update_line.starts_with("    "),
            "UPDATE should be indented under WHEN MATCHED, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_sql_basic_merge_branches_keep_branch_depths_stable() {
        let source = "merge into emp_bonus b using src_bonus s on (b.empno = s.empno) when matched then update set b.bonus_amount = case when s.calc_bonus > 1000 then s.calc_bonus else s.calc_bonus + 100 end, b.updated_at = systimestamp where s.sal > 0 and (b.bonus_amount is null or b.bonus_amount <> s.calc_bonus) delete where s.sal < 500 when not matched then insert (b.empno, b.deptno, b.bonus_amount, b.created_at, b.note_text) values (s.empno, s.deptno, s.calc_bonus, systimestamp, case when s.calc_bonus >= 500 then 'HIGH' else 'LOW' end);";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        let lines: Vec<&str> = formatted.lines().collect();
        let leading_spaces = |line: &str| line.len().saturating_sub(line.trim_start().len());
        let find_line = |prefix: &str| -> Option<&str> {
            lines
                .iter()
                .copied()
                .find(|line| line.trim_start().starts_with(prefix))
        };

        let when_matched = find_line("WHEN MATCHED THEN").unwrap_or("");
        let update_set = find_line("UPDATE SET").unwrap_or("");
        let update_where = find_line("WHERE s.sal > 0").unwrap_or("");
        let delete = find_line("DELETE").unwrap_or("");
        let delete_where = find_line("WHERE s.sal < 500").unwrap_or("");
        let when_not_matched = find_line("WHEN NOT MATCHED THEN").unwrap_or("");
        let insert = find_line("INSERT (").unwrap_or("");
        let insert_values_inline = lines.iter().copied().find(|line| {
            let trimmed = line.trim_start();
            trimmed.starts_with("INSERT (") && trimmed.contains(" VALUES (")
        });
        let values = find_line("VALUES (").or(insert_values_inline).unwrap_or("");

        assert!(
            !when_matched.is_empty()
                && !update_set.is_empty()
                && !update_where.is_empty()
                && !delete.is_empty()
                && !delete_where.is_empty()
                && !when_not_matched.is_empty()
                && !insert.is_empty()
                && !values.is_empty(),
            "expected all MERGE branch lines to be present, got:\n{}",
            formatted
        );

        assert_eq!(
            leading_spaces(when_matched),
            leading_spaces(when_not_matched),
            "MERGE branch headers should share the same base depth, got:\n{}",
            formatted
        );
        assert_eq!(
            leading_spaces(update_set),
            leading_spaces(update_where),
            "UPDATE and its WHERE should stay on the same branch depth, got:\n{}",
            formatted
        );
        assert_eq!(
            leading_spaces(delete),
            leading_spaces(delete_where),
            "DELETE and its WHERE should stay on the same branch depth, got:\n{}",
            formatted
        );
        assert_eq!(
            leading_spaces(insert),
            leading_spaces(values),
            "INSERT branch body should keep VALUES on the same depth, got:\n{}",
            formatted
        );
        assert!(
            leading_spaces(update_set) > leading_spaces(when_matched),
            "UPDATE branch body should be deeper than WHEN MATCHED, got:\n{}",
            formatted
        );
        assert!(
            leading_spaces(insert) > leading_spaces(when_not_matched),
            "INSERT branch body should be deeper than WHEN NOT MATCHED, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_sql_basic_keeps_fetch_into_inline() {
        let source = "begin\nfetch cur into v_rec;\nend;";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        assert!(
            formatted.contains("FETCH cur INTO v_rec;"),
            "FETCH INTO should stay inline, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_sql_basic_keeps_fetch_bulk_collect_into_inline() {
        let source = "begin\nfetch cur bulk collect into v_tab limit 100;\nend;";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        assert!(
            formatted.contains("FETCH cur BULK COLLECT INTO v_tab LIMIT 100;"),
            "FETCH BULK COLLECT INTO LIMIT should stay inline, got:\n{}",
            formatted
        );
    }

    // ── RETURNING INTO inline ──

    #[test]
    fn format_sql_basic_keeps_returning_into_inline() {
        let source = "begin\ninsert into t1 (a) values (1) returning id into v_id;\nend;";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        assert!(
            formatted.contains("RETURNING id INTO v_id;"),
            "RETURNING INTO should stay inline, got:\n{}",
            formatted
        );
    }

    // ── BULK COLLECT INTO inline (in SELECT) ──

    #[test]
    fn format_sql_basic_keeps_bulk_collect_into_inline() {
        let source = "begin\nselect col bulk collect into v_tab from t1;\nend;";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        assert!(
            formatted.contains("BULK COLLECT INTO v_tab"),
            "BULK COLLECT INTO should stay inline, got:\n{}",
            formatted
        );
    }

    // ── SELECT FOR UPDATE inline ──

    #[test]
    fn format_sql_basic_keeps_for_update_inline() {
        let source = "select a, b from t1 where id = 1 for update of a nowait;";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        assert!(
            formatted.contains("FOR UPDATE OF"),
            "FOR UPDATE should stay inline, got:\n{}",
            formatted
        );
    }

    // ── ON DELETE CASCADE inline ──

    #[test]
    fn format_sql_basic_keeps_on_delete_cascade_inline() {
        let source = "alter table t1 add constraint fk1 foreign key (col1) references t2(id) on delete cascade;";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        assert!(
            formatted.contains("ON DELETE CASCADE"),
            "ON DELETE CASCADE should stay inline, got:\n{}",
            formatted
        );
    }

    // ── CREATE SEQUENCE inline ──

    #[test]
    fn format_sql_basic_keeps_create_sequence_inline() {
        let source = "create sequence seq1 start with 1 increment by 1 nocache;";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        assert!(
            !formatted.contains("\nSTART"),
            "CREATE SEQUENCE START WITH should not break, got:\n{}",
            formatted
        );
    }

    // ── CREATE SYNONYM FOR inline ──

    #[test]
    fn format_sql_basic_keeps_create_synonym_for_inline() {
        let source = "create or replace synonym syn1 for schema1.t1;";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        assert!(
            formatted.contains("SYNONYM syn1 FOR schema1"),
            "CREATE SYNONYM FOR should stay inline, got:\n{}",
            formatted
        );
    }

    // ── LISTAGG WITHIN GROUP inline ──

    #[test]
    fn format_sql_basic_keeps_listagg_within_group_inline() {
        let source = "select deptno, listagg(ename, ', ') within group (order by ename) as names from emp group by deptno;";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        assert!(
            formatted.contains("WITHIN GROUP"),
            "LISTAGG WITHIN GROUP should stay inline, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_sql_basic_keeps_keep_dense_rank_order_by_inline() {
        let source = "select deptno, max(sal) keep (dense_rank last order by sal, empno) as top_sal from emp group by deptno;";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        assert!(
            formatted.contains("KEEP (DENSE_RANK LAST ORDER BY sal, empno)"),
            "KEEP (DENSE_RANK ... ORDER BY ...) should stay inline, got:\n{}",
            formatted
        );
        assert!(
            !formatted.contains("\nORDER BY sal"),
            "KEEP internal ORDER BY must not be broken like a clause, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_sql_basic_nested_within_group_subquery_realigns_order_items_to_owner_body_depth() {
        let source = "select listagg(e.ename, ', ') within group (order by (select max(b.sal) from bonus b where b.empno = e.empno), e.empno) as names from emp e group by e.deptno;";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        let lines: Vec<&str> = formatted.lines().collect();
        let indent = |line: &str| line.len().saturating_sub(line.trim_start().len());

        let order_idx = lines
            .iter()
            .position(|line| line.trim_start().starts_with("ORDER BY ("))
            .unwrap_or_else(|| panic!("WITHIN GROUP ORDER BY line, got:\n{formatted}"));
        let inner_select_idx = lines
            .iter()
            .position(|line| line.trim_start() == "SELECT MAX (b.sal)")
            .expect("nested SELECT inside WITHIN GROUP");
        let empno_idx = lines
            .iter()
            .position(|line| line.trim_start() == "e.empno")
            .unwrap_or_else(|| panic!("WITHIN GROUP sibling sort item, got:\n{formatted}"));

        assert!(
            indent(lines[inner_select_idx]) > indent(lines[order_idx]),
            "nested SELECT inside WITHIN GROUP should indent deeper than the ORDER BY owner body, got:\n{}",
            formatted
        );
        assert_eq!(
            indent(lines[empno_idx]),
            indent(lines[order_idx]),
            "WITHIN GROUP sibling sort items should realign with the ORDER BY owner body depth after nested subqueries close, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_sql_basic_nested_keep_subquery_realigns_order_items_to_owner_body_depth() {
        let source = "select max(e.sal) keep (dense_rank last order by (select max(b.sal) from bonus b where b.empno = e.empno), e.empno) as top_sal from emp e;";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        let lines: Vec<&str> = formatted.lines().collect();
        let indent = |line: &str| line.len().saturating_sub(line.trim_start().len());

        let body_idx = lines
            .iter()
            .position(|line| line.trim_start().starts_with("DENSE_RANK LAST ORDER BY ("))
            .unwrap_or_else(|| panic!("KEEP body line, got:\n{formatted}"));
        let inner_select_idx = lines
            .iter()
            .position(|line| line.trim_start() == "SELECT MAX (b.sal)")
            .unwrap_or_else(|| panic!("nested SELECT inside KEEP, got:\n{formatted}"));
        let empno_idx = lines
            .iter()
            .position(|line| line.trim_start() == "e.empno")
            .unwrap_or_else(|| panic!("KEEP sibling sort item, got:\n{formatted}"));

        assert!(
            indent(lines[inner_select_idx]) > indent(lines[body_idx]),
            "nested SELECT inside KEEP ORDER BY should indent deeper than the KEEP owner body, got:\n{}",
            formatted
        );
        assert_eq!(
            indent(lines[empno_idx]),
            indent(lines[body_idx]),
            "KEEP sibling sort items should realign with the owner body depth after nested subqueries close, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_sql_basic_keeps_sum_case_indented_inside_with_plsql_cte() {
        let source = r#"WITH
    FUNCTION fmt_mask (p_txt IN VARCHAR2) RETURN VARCHAR2 IS
    BEGIN
        RETURN REGEXP_REPLACE (NVL (p_txt, 'NULL'), '([[:alnum:]])', '*');
    END fmt_mask,
    PROCEDURE noop (p_msg IN VARCHAR2) IS
    BEGIN
        NULL;
    END noop,
    dept_stat AS (
        SELECT
            f.deptno,
            COUNT (*) AS cnt_emp,
            SUM (
            CASE
                WHEN f.band = 'TOP' THEN 1
                ELSE 0
            END
            ) AS cnt_top,
            LISTAGG (f.ename, ', ') WITHIN GROUP (ORDER BY f.sal DESC, f.empno) AS emp_list
        FROM filtered_emp f
        GROUP BY f.deptno
    )
SELECT
    *
FROM dept_stat;"#;
        let formatted = SqlEditorWidget::format_sql_basic(source);
        let lines: Vec<&str> = formatted.lines().collect();

        let leading_spaces = |line: &str| line.len().saturating_sub(line.trim_start().len());
        let sum_idx = lines
            .iter()
            .position(|line| line.trim_start() == "SUM (")
            .unwrap_or(0);
        let case_idx = lines
            .iter()
            .enumerate()
            .skip(sum_idx.saturating_add(1))
            .find(|(_, line)| line.trim_start() == "CASE")
            .map(|(idx, _)| idx)
            .unwrap_or(0);
        let when_idx = lines
            .iter()
            .enumerate()
            .skip(case_idx.saturating_add(1))
            .find(|(_, line)| line.trim_start().starts_with("WHEN f.band = 'TOP' THEN 1"))
            .map(|(idx, _)| idx)
            .unwrap_or(0);
        let else_idx = lines
            .iter()
            .enumerate()
            .skip(when_idx.saturating_add(1))
            .find(|(_, line)| line.trim_start() == "ELSE 0")
            .map(|(idx, _)| idx)
            .unwrap_or(0);
        let end_idx = lines
            .iter()
            .enumerate()
            .skip(else_idx.saturating_add(1))
            .find(|(_, line)| line.trim_start() == "END")
            .map(|(idx, _)| idx)
            .unwrap_or(0);
        let close_idx = lines
            .iter()
            .enumerate()
            .skip(end_idx.saturating_add(1))
            .find(|(_, line)| line.trim_start().starts_with(") AS cnt_top"))
            .map(|(idx, _)| idx)
            .unwrap_or(0);

        assert!(
            leading_spaces(lines[case_idx]) > leading_spaces(lines[sum_idx]),
            "CASE inside SUM should indent deeper than SUM header, got:\n{}",
            formatted
        );
        assert!(
            leading_spaces(lines[when_idx]) > leading_spaces(lines[case_idx]),
            "WHEN inside aggregate CASE should indent deeper than CASE, got:\n{}",
            formatted
        );
        assert_eq!(
            leading_spaces(lines[else_idx]),
            leading_spaces(lines[when_idx]),
            "ELSE inside aggregate CASE should align with WHEN, got:\n{}",
            formatted
        );
        assert_eq!(
            leading_spaces(lines[end_idx]),
            leading_spaces(lines[case_idx]),
            "END inside aggregate CASE should align with CASE, got:\n{}",
            formatted
        );
        assert_eq!(
            leading_spaces(lines[close_idx]),
            leading_spaces(lines[sum_idx]),
            "closing SUM parenthesis should align with SUM header, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_sql_basic_does_not_insert_blank_line_after_with_function_terminator_before_cte() {
        let source = r#"WITH
    FUNCTION calc_depth (p_id NUMBER) RETURN NUMBER IS
        v_depth NUMBER;
    BEGIN
            SELECT MAX (LEVEL)
            INTO v_depth
            FROM org_tree
            START WITH parent_id IS NULL
            CONNECT BY PRIOR node_id = parent_id;
    RETURN v_depth;
END calc_depth;

recursive_tree (node_id, parent_id, node_name, DEPTH, PATH) AS (
    SELECT
        node_id,
        parent_id,
        node_name,
        1 AS DEPTH,
        CAST (node_name AS VARCHAR2 (4000)) AS PATH
    FROM org_tree
    WHERE parent_id IS NULL
    UNION ALL
    SELECT
        t.node_id,
        t.parent_id,
        t.node_name,
        rt.DEPTH + 1,
        rt.PATH || ' > ' || t.node_name
    FROM org_tree t
    JOIN recursive_tree rt
        ON t.parent_id = rt.node_id
    WHERE rt.DEPTH < calc_depth (t.node_id)
),
aggregated AS (
    SELECT
        parent_id,
        COUNT (*) AS child_count,
        MAX (DEPTH) AS max_depth,
        LISTAGG (node_name, ', ') WITHIN GROUP (ORDER BY node_name) AS children
    FROM recursive_tree
    WHERE DEPTH > 1
    GROUP BY parent_id
)
SELECT
    rt.*,
    a.child_count,
    a.max_depth,
    a.children
FROM recursive_tree rt
LEFT JOIN aggregated a
    ON rt.node_id = a.parent_id
ORDER BY rt.PATH;"#;

        let formatted = SqlEditorWidget::format_sql_basic(source);

        assert!(
            !formatted.contains("END calc_depth;\n\nrecursive_tree"),
            "WITH FUNCTION terminator should stay attached to the following CTE, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_sql_basic_keeps_following_cte_siblings_aligned_after_with_function_body() {
        let source = r#"WITH
    FUNCTION calc_depth (p_id NUMBER) RETURN NUMBER IS
        v_depth NUMBER;
    BEGIN
        SELECT MAX (LEVEL)
        INTO v_depth
        FROM org_tree
        START WITH parent_id IS NULL
        CONNECT BY PRIOR node_id = parent_id;
    RETURN v_depth;
END calc_depth;
recursive_tree (node_id, parent_id, node_name, DEPTH, PATH) AS (
    SELECT
        node_id,
        parent_id,
        node_name,
        1 AS DEPTH,
        CAST (node_name AS VARCHAR2 (4000)) AS PATH
    FROM org_tree
    WHERE parent_id IS NULL
    UNION ALL
    SELECT
        t.node_id,
        t.parent_id,
        t.node_name,
        rt.DEPTH + 1,
        rt.PATH || ' > ' || t.node_name
    FROM org_tree t
    JOIN recursive_tree rt
        ON t.parent_id = rt.node_id
    WHERE rt.DEPTH < calc_depth (t.node_id)
),
aggregated AS (
    SELECT
        parent_id,
        COUNT (*) AS child_count,
        MAX (DEPTH) AS max_depth,
        LISTAGG (node_name, ', ') WITHIN GROUP (ORDER BY node_name) AS children
    FROM recursive_tree
    WHERE DEPTH > 1
    GROUP BY parent_id
)
SELECT
    rt.*,
    a.child_count,
    a.max_depth,
    a.children
FROM recursive_tree rt
LEFT JOIN aggregated a
    ON rt.node_id = a.parent_id
ORDER BY rt.PATH;"#;

        let formatted = SqlEditorWidget::format_sql_basic(source);
        let lines: Vec<&str> = formatted.lines().collect();
        let recursive_cte_idx = find_line_starting_with(
            &lines,
            "recursive_tree (node_id, parent_id, node_name, DEPTH, PATH) AS (",
        )
        .expect("recursive CTE line");
        let aggregated_cte_idx =
            find_line_starting_with(&lines, "aggregated AS (").expect("aggregated CTE line");
        let aggregated_select_idx = lines
            .iter()
            .enumerate()
            .skip(aggregated_cte_idx.saturating_add(1))
            .find(|(_, line)| line.trim_start() == "SELECT")
            .map(|(idx, _)| idx)
            .expect("aggregated SELECT line");

        assert_eq!(
            leading_spaces(lines[aggregated_cte_idx]),
            leading_spaces(lines[recursive_cte_idx]),
            "sibling CTE after WITH FUNCTION body should realign with the same WITH owner depth, got:\n{}",
            formatted
        );
        assert_eq!(
            leading_spaces(lines[aggregated_select_idx]),
            leading_spaces(lines[aggregated_cte_idx]).saturating_add(4),
            "aggregated CTE body SELECT should stay exactly one level deeper than its CTE header, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_for_auto_formatting_keeps_create_view_cte_sibling_on_query_body_depth() {
        let source = r#"CREATE OR REPLACE VIEW qt_fmt_emp_v AS
    WITH base_emp AS (
        SELECT
            e.emp_id
        FROM qt_fmt_emp e
    ),
sales_agg AS (
    SELECT
        s.emp_id
    FROM qt_fmt_sales s
)
    SELECT
        b.emp_id
    FROM base_emp b
    LEFT JOIN sales_agg sa
    ON sa.emp_id = b.emp_id;"#;

        let formatted = SqlEditorWidget::format_for_auto_formatting(source, false);
        let lines: Vec<&str> = formatted.lines().collect();
        let with_idx =
            find_line_starting_with(&lines, "WITH base_emp AS (").expect("WITH CTE line");
        let sales_agg_idx =
            find_line_starting_with(&lines, "sales_agg AS (").expect("sales_agg CTE line");

        assert_eq!(
            leading_spaces(lines[with_idx]),
            4,
            "CREATE VIEW query body WITH header should be depth 1, got:\n{}",
            formatted
        );
        assert_eq!(
            leading_spaces(lines[sales_agg_idx]),
            leading_spaces(lines[with_idx]),
            "sibling CTE after close-comma should stay on the same CREATE VIEW query-body depth, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_for_auto_formatting_keeps_recursive_cte_union_branch_select_on_cte_body_depth() {
        let source = r#"WITH r (node_id, parent_id, node_name, lvl, PATH) AS (
    SELECT
        node_id,
        parent_id,
        node_name,
        1 AS lvl,
        '/' || node_name AS PATH
    FROM oqt_t_tree
    WHERE parent_id IS NULL
    UNION ALL
        SELECT
            t.node_id,
            t.parent_id,
            t.node_name,
            r.lvl + 1,
            r.PATH || '/' || t.node_name
        FROM oqt_t_tree t
        JOIN r
            ON t.parent_id = r.node_id
)
SELECT *
FROM r
ORDER BY lvl,
    node_id;"#;

        let formatted = SqlEditorWidget::format_for_auto_formatting(source, false);
        let expected = r#"WITH r (node_id, parent_id, node_name, lvl, PATH) AS (
    SELECT
        node_id,
        parent_id,
        node_name,
        1 AS lvl,
        '/' || node_name AS PATH
    FROM oqt_t_tree
    WHERE parent_id IS NULL
    UNION ALL
    SELECT
        t.node_id,
        t.parent_id,
        t.node_name,
        r.lvl + 1,
        r.PATH || '/' || t.node_name
    FROM oqt_t_tree t
    JOIN r
        ON t.parent_id = r.node_id
)
SELECT *
FROM r
ORDER BY lvl,
    node_id;"#;

        assert_eq!(
            formatted, expected,
            "Recursive CTE set-operator branches should keep the branch SELECT on the same CTE body depth"
        );
    }

    #[test]
    fn format_for_auto_formatting_keeps_non_recursive_cte_union_branch_select_on_cte_body_depth() {
        let source = r#"WITH src AS (
    SELECT
        10 AS dept_id,
        'DEV' AS dept_name
    FROM DUAL
    UNION ALL
        SELECT
            20 AS dept_id,
            'OPS' AS dept_name
        FROM DUAL
)
SELECT *
FROM src;"#;

        let formatted = SqlEditorWidget::format_for_auto_formatting(source, false);
        let expected = r#"WITH src AS (
    SELECT
        10 AS dept_id,
        'DEV' AS dept_name
    FROM DUAL
    UNION ALL
    SELECT
        20 AS dept_id,
        'OPS' AS dept_name
    FROM DUAL
)
SELECT *
FROM src;"#;

        assert_eq!(
            formatted, expected,
            "Non-recursive CTE set-operator branches should keep the branch SELECT on the same CTE body depth"
        );
    }

    #[test]
    fn format_for_auto_formatting_keeps_non_recursive_cte_except_branch_on_cte_body_depth() {
        let source = r#"WITH src AS (
    SELECT
        dept_id
    FROM current_emp
    EXCEPT
        SELECT
            dept_id
        FROM former_emp
)
SELECT *
FROM src;"#;

        let formatted = SqlEditorWidget::format_for_auto_formatting(source, false);
        let lines: Vec<&str> = formatted.lines().collect();
        let indent = |line: &str| line.len().saturating_sub(line.trim_start().len());
        let first_select_idx = lines
            .iter()
            .position(|line| line.trim_start().starts_with("SELECT dept_id"))
            .unwrap_or(0);
        let except_idx = lines
            .iter()
            .position(|line| line.trim_start() == "EXCEPT")
            .unwrap_or(0);
        let second_select_idx = lines
            .iter()
            .enumerate()
            .skip(except_idx.saturating_add(1))
            .find(|(_, line)| line.trim_start().starts_with("SELECT dept_id"))
            .map(|(idx, _)| idx)
            .unwrap_or(0);

        assert_eq!(
            indent(lines[except_idx]),
            indent(lines[first_select_idx]),
            "EXCEPT inside a plain CTE should stay aligned with the first branch SELECT, got:\n{}",
            formatted
        );
        assert_eq!(
            indent(lines[second_select_idx]),
            indent(lines[first_select_idx]),
            "The second SELECT after EXCEPT in a plain CTE should reuse the same CTE body base depth, got:\n{}",
            formatted
        );
        assert_eq!(
            SqlEditorWidget::format_for_auto_formatting(&formatted, false),
            formatted,
            "Non-recursive CTE EXCEPT formatting should remain stable after the shared set-operator fix"
        );
    }

    #[test]
    fn format_for_auto_formatting_keeps_sibling_cte_column_list_on_with_depth() {
        let source = r#"with a as (
select 1
from dual
), b (a, b, c) as (
select 1
from dual
)
select 1
from dual;"#;

        let formatted = SqlEditorWidget::format_for_auto_formatting(source, false);
        let expected = r#"WITH a AS (
    SELECT 1
    FROM DUAL
),
b (a, b, c) AS (
    SELECT 1
    FROM DUAL
)
SELECT 1
FROM DUAL;"#;

        assert_eq!(
            formatted, expected,
            "Sibling CTE headers with column lists should stay aligned to the WITH owner depth, and the main SELECT must not inherit CTE list continuation state"
        );
        assert_eq!(
            SqlEditorWidget::format_for_auto_formatting(&formatted, false),
            expected,
            "CTE sibling column-list formatting should remain stable after re-formatting"
        );
    }

    #[test]
    fn format_for_auto_formatting_keeps_malformed_trailing_cte_comma_select_on_with_depth() {
        let source = r#"with a as (
select 1
from dual
),
    b (a, b, d, e, f, c) as (
        select 1
        from dual
    ),
select 1
from dual;"#;

        let formatted = SqlEditorWidget::format_for_auto_formatting(source, false);
        let expected = r#"WITH a AS (
    SELECT 1
    FROM DUAL
),
b (a, b, d, e, f, c) AS (
    SELECT 1
    FROM DUAL
),
SELECT 1
FROM DUAL;"#;

        assert_eq!(
            formatted, expected,
            "Even when the source keeps a malformed trailing comma after the final CTE, sibling CTE headers and the following SELECT should stay on the WITH owner depth"
        );
        assert_eq!(
            SqlEditorWidget::format_for_auto_formatting(&formatted, false),
            expected,
            "Malformed trailing CTE comma formatting should remain stable after re-formatting"
        );
    }

    #[test]
    fn format_for_auto_formatting_keeps_comment_glued_sibling_cte_column_list_on_with_depth() {
        let source = r#"with a as (
select 1
from dual
),
/* owner */ b (a, b, c) as (
select 1
from dual
)
select 1
from dual;"#;

        let formatted = SqlEditorWidget::format_for_auto_formatting(source, false);
        let lines: Vec<&str> = formatted.lines().collect();
        let indent = |line: &str| line.len().saturating_sub(line.trim_start().len());
        let find_line = |needle: &str| -> usize {
            lines
                .iter()
                .position(|line| line.trim_start() == needle)
                .unwrap_or(0)
        };
        let with_idx = find_line("WITH a AS (");
        let comment_idx = find_line("/* owner */");
        let sibling_cte_idx = find_line("b (a, b, c) AS (");
        let sibling_select_idx = lines
            .iter()
            .enumerate()
            .skip(sibling_cte_idx.saturating_add(1))
            .find(|(_, line)| line.trim_start() == "SELECT 1")
            .map(|(idx, _)| idx)
            .unwrap_or(0);
        let main_select_idx = lines
            .iter()
            .enumerate()
            .skip(sibling_select_idx.saturating_add(1))
            .find(|(_, line)| line.trim_start() == "SELECT 1")
            .map(|(idx, _)| idx)
            .unwrap_or(0);

        assert_eq!(
            indent(lines[comment_idx]),
            indent(lines[with_idx]),
            "leading comment before a sibling column-list CTE should stay aligned with the WITH owner depth, got:\n{}",
            formatted
        );
        assert_eq!(
            indent(lines[sibling_cte_idx]),
            indent(lines[with_idx]),
            "comment-glued sibling CTE header with a column list should stay aligned to the WITH owner depth, got:\n{}",
            formatted
        );
        assert_eq!(
            indent(lines[sibling_select_idx]),
            indent(lines[sibling_cte_idx]).saturating_add(4),
            "comment-glued column-list CTE body SELECT should stay one level deeper than its header, got:\n{}",
            formatted
        );
        assert_eq!(
            indent(lines[main_select_idx]),
            indent(lines[with_idx]),
            "main SELECT after a comment-glued column-list sibling CTE should return to the WITH owner depth, got:\n{}",
            formatted
        );
        assert_eq!(
            SqlEditorWidget::format_for_auto_formatting(&formatted, false),
            formatted,
            "Comment-glued sibling column-list CTE formatting should remain stable after re-formatting"
        );
    }

    #[test]
    fn format_sql_basic_collapses_blank_line_before_case_select_item() {
        let source = r#"WITH dept_data AS (
    SELECT
        10 AS dept_id,
        'DEV' AS dept_name
    FROM DUAL
),
emp_data AS (
    SELECT
        1001 AS emp_id,
        10 AS dept_id,
        'ALICE' AS emp_name,
        9000 AS salary
    FROM DUAL
)
SELECT
    d.dept_id,
    e.emp_id,
    e.salary,

    CASE
        WHEN e.salary >= 9000 THEN 'TOP'
        ELSE 'OTHER'
    END AS salary_band
FROM dept_data d
JOIN emp_data e
    ON e.dept_id = d.dept_id;"#;

        let formatted = SqlEditorWidget::format_sql_basic(source);

        assert!(
            formatted.contains("e.salary,\n    CASE"),
            "CASE select item should stay directly after the previous comma-terminated item, got:\n{}",
            formatted
        );
        assert!(
            !formatted.contains("e.salary,\n\n    CASE"),
            "Formatter should not keep an extra blank line before CASE in the SELECT list, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_sql_basic_for_oracle_db_type_collapses_blank_line_before_case_after_close_as_alias_comma_with_apply(
    ) {
        let source = r#"SELECT
    e.emp_id,
    (
        SELECT MAX(s2.amount)
        FROM qt_sales s2
        WHERE s2.emp_id = e.emp_id
    ) AS max_amount,

    CASE
        WHEN x.sale_cnt = 0 THEN 'NO_SALES'
        ELSE 'HAS_SALES'
    END AS rank_bucket
FROM qt_employees e
CROSS APPLY (
    SELECT COUNT(*) AS sale_cnt
    FROM qt_sales s
    WHERE s.emp_id = e.emp_id
) x;"#;

        let formatted = SqlEditorWidget::format_sql_basic_for_db_type(
            source,
            crate::db::connection::DatabaseType::Oracle,
        );

        assert!(
            formatted.contains(") AS max_amount,\n    CASE"),
            "CASE select item should follow close-alias comma without an empty line in APPLY query, got:\n{}",
            formatted
        );
        assert!(
            !formatted.contains(") AS max_amount,\n\n    CASE"),
            "Formatter should remove blank line before CASE after close-alias comma in APPLY query, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_for_auto_formatting_collapses_blank_line_before_case_after_close_as_alias_comma_with_apply(
    ) {
        let source = r#"SELECT
    e.emp_id,
    (
        SELECT MAX(s2.amount)
        FROM qt_sales s2
        WHERE s2.emp_id = e.emp_id
    ) AS max_amount,

    CASE
        WHEN x.sale_cnt = 0 THEN 'NO_SALES'
        ELSE 'HAS_SALES'
    END AS rank_bucket
FROM qt_employees e
CROSS APPLY (
    SELECT COUNT(*) AS sale_cnt
    FROM qt_sales s
    WHERE s.emp_id = e.emp_id
) x;"#;

        let formatted = SqlEditorWidget::format_for_auto_formatting(source, false);

        assert!(
            formatted.contains(") AS max_amount,\n    CASE"),
            "Auto-format should keep CASE directly after close-alias comma without an empty line in APPLY query, got:\n{}",
            formatted
        );
        assert!(
            !formatted.contains(") AS max_amount,\n\n    CASE"),
            "Auto-format should remove blank line before CASE after close-alias comma in APPLY query, got:\n{}",
            formatted
        );
        assert_eq!(
            SqlEditorWidget::format_for_auto_formatting(&formatted, false),
            formatted,
            "Auto-format result should stay stable after removing close-alias CASE blank line in APPLY query"
        );
    }

    // ── PIVOT FOR inline ──

    #[test]
    fn format_sql_basic_keeps_pivot_for_inline() {
        let source = "select * from t1 pivot (sum(amount) for category in ('A' as a, 'B' as b));";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        assert!(
            formatted.contains("FOR category"),
            "PIVOT FOR should stay inline, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_sql_basic_keeps_extract_from_inline_inside_function() {
        let source = "select extract(year from d.dt) as yyyy from dual d;";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        assert!(
            formatted.contains("YEAR FROM d.dt")
                && !formatted.contains(
                    "YEAR
FROM"
                ),
            "EXTRACT(... FROM ...) should stay inline inside function, got:
{}",
            formatted
        );
    }

    #[test]
    fn format_for_auto_formatting_keeps_multiline_extract_from_inside_function_parens() {
        let source = r#"SELECT
    EXTRACT (
        YEAR -- date part
        FROM d.dt
    ) AS yyyy,
    d.empno
FROM dual d;"#;
        let formatted = SqlEditorWidget::format_for_auto_formatting(source, false);
        let lines: Vec<&str> = formatted.lines().collect();
        let indent = |line: &str| line.len().saturating_sub(line.trim_start().len());

        let function_from_idx = lines
            .iter()
            .position(|line| line.trim_start().starts_with("FROM d.dt"))
            .unwrap_or_else(|| panic!("EXTRACT FROM option line, got:\n{}", formatted));
        let sibling_idx = lines
            .iter()
            .position(|line| line.trim_start() == "d.empno")
            .unwrap_or_else(|| panic!("SELECT-list sibling after EXTRACT, got:\n{}", formatted));
        let query_from_idx = lines
            .iter()
            .position(|line| line.trim_start().eq_ignore_ascii_case("FROM dual d;"))
            .unwrap_or_else(|| panic!("outer query FROM clause, got:\n{}", formatted));

        assert!(
            indent(lines[function_from_idx]) > indent(lines[query_from_idx]),
            "function-local FROM should not be promoted to the outer query FROM depth, got:\n{}",
            formatted
        );
        assert_eq!(
            indent(lines[sibling_idx]),
            indent(lines[query_from_idx]).saturating_add(4),
            "SELECT-list sibling after multiline EXTRACT should return to list depth, got:\n{}",
            formatted
        );
        assert_eq!(
            SqlEditorWidget::format_for_auto_formatting(&formatted, false),
            formatted,
            "multiline EXTRACT auto-formatting should be idempotent"
        );
    }

    #[test]
    fn format_for_auto_formatting_keeps_split_function_local_from_inside_function_parens() {
        let cases = [(
            "EXTRACT",
            r#"SELECT
    EXTRACT
    (
        YEAR -- date part
        FROM d.dt
    ) AS extracted_value,
    d.empno
FROM dual d;"#,
            "FROM d.dt",
        )];
        let indent = |line: &str| line.len().saturating_sub(line.trim_start().len());

        for (function_name, source, function_from_prefix) in cases {
            let formatted = SqlEditorWidget::format_for_auto_formatting(source, false);
            let lines: Vec<&str> = formatted.lines().collect();

            let function_from_idx = lines
                .iter()
                .position(|line| line.trim_start().starts_with(function_from_prefix))
                .unwrap_or_else(|| {
                    panic!("split {function_name} FROM option line, got:\n{formatted}")
                });
            let sibling_idx = lines
                .iter()
                .position(|line| line.trim_start() == "d.empno")
                .unwrap_or_else(|| {
                    panic!("SELECT-list sibling after split {function_name}, got:\n{formatted}")
                });
            let query_from_idx = lines
                .iter()
                .position(|line| line.trim_start().eq_ignore_ascii_case("FROM dual d;"))
                .unwrap_or_else(|| panic!("outer query FROM clause, got:\n{formatted}"));

            assert!(
                indent(lines[function_from_idx]) > indent(lines[query_from_idx]),
                "split {function_name} function-local FROM should not be promoted to the outer query FROM depth, got:\n{formatted}"
            );
            assert_eq!(
                indent(lines[sibling_idx]),
                indent(lines[query_from_idx]).saturating_add(4),
                "SELECT-list sibling after split {function_name} should return to list depth, got:\n{formatted}"
            );
            assert_eq!(
                SqlEditorWidget::format_for_auto_formatting(&formatted, false),
                formatted,
                "split {function_name} auto-formatting should be idempotent"
            );
        }
    }

    #[test]
    fn format_sql_basic_keeps_json_returning_inline_inside_function() {
        let source =
            "select json_value(e.json_profile, '$.level' returning varchar2(30)) as profile_level from emp e;";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        assert!(
            formatted.contains("'$.level' RETURNING VARCHAR2 (30)"),
            "JSON RETURNING should stay inline inside function parens, got:
{}",
            formatted
        );
    }

    #[test]
    fn format_sql_basic_breaks_cross_apply_after_join_condition() {
        let source = "select * from org_enriched oe join qt_fmt_dept d on d.dept_id = oe.dept_id cross apply (select 1 as x from dual) ca;";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        assert!(
            formatted.contains(
                "ON d.dept_id = oe.dept_id
CROSS APPLY ("
            ),
            "CROSS APPLY should start on a new line after JOIN ON, got:
{}",
            formatted
        );
    }

    #[test]
    fn format_for_auto_formatting_keeps_cross_apply_aggregate_subquery_layout_exactly() {
        let source = "select d.department_name, emp_stats.avg_sal, emp_stats.emp_count, top_emp.employee_name, top_emp.salary from departments d cross apply (select avg(e.salary) as avg_sal, count(*) as emp_count, max(e.salary) as max_sal from employees e where e.department_id = d.department_id having count(*) > 5) emp_stats outer apply (select e2.first_name || ' ' || e2.last_name as employee_name, e2.salary from employees e2 where e2.department_id = d.department_id and e2.salary = emp_stats.max_sal fetch first 1 row only) top_emp where emp_stats.avg_sal > (select avg(salary) from employees);";
        let expected = r#"SELECT d.department_name,
    emp_stats.avg_sal,
    emp_stats.emp_count,
    top_emp.employee_name,
    top_emp.salary
FROM departments d
CROSS APPLY (
    SELECT AVG(e.salary) AS avg_sal,
        COUNT(*) AS emp_count,
        MAX(e.salary) AS max_sal
    FROM employees e
    WHERE e.department_id = d.department_id
    HAVING COUNT(*) > 5
) emp_stats
OUTER APPLY (
    SELECT e2.first_name || ' ' || e2.last_name AS employee_name,
        e2.salary
    FROM employees e2
    WHERE e2.department_id = d.department_id
        AND e2.salary = emp_stats.max_sal
    FETCH FIRST 1 ROW ONLY
) top_emp
WHERE emp_stats.avg_sal > (
    SELECT AVG(salary)
    FROM employees
);"#;

        let formatted = SqlEditorWidget::format_for_auto_formatting(source, false);

        assert_eq!(
            formatted, expected,
            "Auto formatting should keep APPLY aggregate subqueries on the exact expected base depths"
        );
        assert_eq!(
            SqlEditorWidget::format_for_auto_formatting(expected, false),
            expected,
            "Auto formatting should be stable for the expected APPLY layout"
        );
    }

    #[test]
    fn format_for_auto_formatting_keeps_general_paren_frame_depth_for_close_continuations() {
        let source = r#"SELECT 'DEPT=' || d.dept_name || ' | EMP=' || e.emp_name || ' | SALES=' || TO_CHAR (NVL (
    (
        SELECT SUM ((s.qty * s.unit_price) - s.discount_amt + s.tax_amt)
        FROM qt_fmt_sales s
        WHERE s.emp_id = e.emp_id
    ), 0
)) || ' | JSON_LEVEL=' || JSON_VALUE (e.json_profile, '$.level' RETURNING VARCHAR2 (30)) || ' | HIER=' || (
    SELECT MAX (SYS_CONNECT_BY_PATH (x.dept_code, '/'))
    FROM qt_fmt_dept x
    START WITH x.dept_id = d.dept_id
    CONNECT BY PRIOR x.parent_dept_id = x.dept_id
) AS summary_line
FROM qt_fmt_emp e
JOIN qt_fmt_dept d
    ON d.dept_id = e.dept_id
ORDER BY e.emp_id;"#;

        let formatted = SqlEditorWidget::format_for_auto_formatting(source, false);
        let expected = r#"SELECT 'DEPT=' || d.dept_name || ' | EMP=' || e.emp_name || ' | SALES=' || TO_CHAR (NVL (
        (
            SELECT SUM ((s.qty * s.unit_price) - s.discount_amt + s.tax_amt)
            FROM qt_fmt_sales s
            WHERE s.emp_id = e.emp_id
        ), 0
    )) || ' | JSON_LEVEL=' || JSON_VALUE (e.json_profile, '$.level' RETURNING VARCHAR2 (30)) || ' | HIER=' || (
        SELECT MAX (SYS_CONNECT_BY_PATH (x.dept_code, '/'))
        FROM qt_fmt_dept x
        START WITH x.dept_id = d.dept_id
        CONNECT BY PRIOR x.parent_dept_id = x.dept_id
    ) AS summary_line
FROM qt_fmt_emp e
JOIN qt_fmt_dept d
    ON d.dept_id = e.dept_id
ORDER BY e.emp_id;"#;

        assert_eq!(
            formatted, expected,
            "General parenthesis frames should preserve close-paren continuation depth and nested child-query base depth"
        );
        assert_eq!(
            SqlEditorWidget::format_for_auto_formatting(expected, false),
            expected,
            "Auto formatting should stay stable after general parenthesis frame normalization"
        );
    }

    #[test]
    fn format_for_auto_formatting_keeps_json_object_value_scalar_subquery_on_paren_frame_depth() {
        let source = r#"SELECT d.dept_id,
    d.dept_name,
    JSON_ARRAYAGG (JSON_OBJECT ('empId' VALUE e.emp_id, 'name' VALUE e.emp_name, 'job' VALUE e.job_title, 'salary' VALUE e.salary, 'sales' VALUE (
        SELECT NVL (SUM ((s.qty * s.unit_price) - s.discount_amt + s.tax_amt), 0)
        FROM qt_fmt_sales s
        WHERE s.emp_id = e.emp_id
            ), 'meta' VALUE JSON_OBJECT ('grade' VALUE e.grade_no, 'status' VALUE e.status, 'hireDate' VALUE TO_CHAR (e.hire_date, 'YYYY-MM-DD')) RETURNING CLOB) ORDER BY e.salary DESC, e.emp_id RETURNING CLOB) AS dept_emp_json
FROM qt_fmt_dept d
LEFT JOIN qt_fmt_emp e
    ON e.dept_id = d.dept_id
GROUP BY d.dept_id,
    d.dept_name
ORDER BY d.dept_id;"#;

        let formatted = SqlEditorWidget::format_for_auto_formatting(source, false);
        let expected = r#"SELECT d.dept_id,
    d.dept_name,
    JSON_ARRAYAGG (JSON_OBJECT ('empId' VALUE e.emp_id, 'name' VALUE e.emp_name, 'job' VALUE e.job_title, 'salary' VALUE e.salary, 'sales' VALUE (
            SELECT NVL (SUM ((s.qty * s.unit_price) - s.discount_amt + s.tax_amt), 0)
            FROM qt_fmt_sales s
            WHERE s.emp_id = e.emp_id
            ), 'meta' VALUE JSON_OBJECT ('grade' VALUE e.grade_no, 'status' VALUE e.status, 'hireDate' VALUE TO_CHAR (e.hire_date, 'YYYY-MM-DD')) RETURNING CLOB) ORDER BY e.salary DESC, e.emp_id RETURNING CLOB) AS dept_emp_json
FROM qt_fmt_dept d
LEFT JOIN qt_fmt_emp e
    ON e.dept_id = d.dept_id
GROUP BY d.dept_id,
    d.dept_name
ORDER BY d.dept_id;"#;

        assert_eq!(
            formatted, expected,
            "Scalar subquery under JSON_OBJECT VALUE should inherit the active parenthesis frame depth"
        );
        assert_eq!(
            SqlEditorWidget::format_for_auto_formatting(expected, false),
            expected,
            "JSON_OBJECT VALUE scalar subquery formatting should remain stable"
        );
    }

    #[test]
    fn format_for_auto_formatting_keeps_nested_json_object_open_paren_frame_for_scalar_subquery() {
        let source = r#"SELECT c.customer_id,
    c.customer_name,
    JSON_OBJECT('segment', c.segment, 'region', (
        SELECT r.region_name
        FROM boss_region r
        WHERE r.region_id = c.region_id
    ), 'stats', JSON_OBJECT('orders', COUNT(DISTINCT o.order_id), 'gross', ROUND(SUM(o.grand_total), 2), 'avg', ROUND(AVG(o.grand_total), 2), 'paid_ratio', ROUND(SUM(o.paid_total) / NULLIF(SUM(o.grand_total), 0), 4)), 'latest_orders', JSON_ARRAYAGG(JSON_OBJECT('order_no', o.order_no, 'date', DATE_FORMAT(o.order_date, '%Y-%m-%d'), 'grand_total', o.grand_total, 'risk', fn_order_risk(o.order_id)))) AS customer_doc
FROM boss_customer c
JOIN boss_order o
    ON o.customer_id = c.customer_id
WHERE o.status <> 'CANCELLED'
    AND EXISTS (
        SELECT 1
        FROM boss_order_item oi
        WHERE oi.order_id = o.order_id
            AND oi.discount_rate >= 0.0700
    )
GROUP BY c.customer_id,
    c.customer_name,
    c.segment,
    c.region_id
HAVING COUNT(DISTINCT o.order_id) >= 2
ORDER BY SUM(o.grand_total) DESC,
    c.customer_id
LIMIT 10;"#;

        let formatted = SqlEditorWidget::format_for_auto_formatting(source, true);
        let lines: Vec<&str> = formatted.lines().collect();

        let region_owner_idx = lines
            .iter()
            .position(|line| line.contains("'region', ("))
            .expect("JSON_OBJECT region owner line");
        let region_select_idx = lines
            .iter()
            .enumerate()
            .skip(region_owner_idx.saturating_add(1))
            .find(|(_, line)| line.trim_start() == "SELECT r.region_name")
            .map(|(idx, _)| idx)
            .expect("JSON_OBJECT region scalar SELECT line");
        let region_from_idx = lines
            .iter()
            .enumerate()
            .skip(region_select_idx.saturating_add(1))
            .find(|(_, line)| line.trim_start() == "FROM boss_region r")
            .map(|(idx, _)| idx)
            .expect("JSON_OBJECT region scalar FROM line");
        let region_where_idx = lines
            .iter()
            .enumerate()
            .skip(region_from_idx.saturating_add(1))
            .find(|(_, line)| line.trim_start() == "WHERE r.region_id = c.region_id")
            .map(|(idx, _)| idx)
            .expect("JSON_OBJECT region scalar WHERE line");

        assert_eq!(
            leading_spaces(lines[region_select_idx]),
            leading_spaces(lines[region_owner_idx]).saturating_add(8),
            "JSON_OBJECT + nested scalar subquery must keep both open-paren frames so SELECT stays two levels deeper than the owner line, got:\n{formatted}"
        );
        assert_eq!(
            leading_spaces(lines[region_from_idx]),
            leading_spaces(lines[region_select_idx]),
            "scalar subquery FROM should stay aligned with SELECT under nested JSON_OBJECT paren frames, got:\n{formatted}"
        );
        assert_eq!(
            leading_spaces(lines[region_where_idx]),
            leading_spaces(lines[region_select_idx]),
            "scalar subquery WHERE should stay aligned with SELECT under nested JSON_OBJECT paren frames, got:\n{formatted}"
        );
    }

    #[test]
    fn format_for_auto_formatting_restores_outer_select_clause_after_nested_json_xml_select_item() {
        let source = r#"SELECT x.employee_id,
    XMLQUERY ('for $i in /employees/employee
         where $i/salary > 5000
         return <result>
             <name>{$i/name/text()}</name>
             <bonus>{$i/salary * 0.1}</bonus>
         </result>' PASSING x.xml_data RETURNING CONTENT) AS xml_result,
        JSON_OBJECT (KEY 'id' VALUE x.employee_id, KEY 'name' VALUE x.emp_name, KEY 'details' VALUE JSON_OBJECT (KEY 'salary' VALUE x.salary, KEY 'department' VALUE x.dept_name, KEY 'skills' VALUE (
                SELECT JSON_ARRAYAGG (JSON_OBJECT (KEY 'skill' VALUE s.skill_name, KEY 'level' VALUE s.proficiency) ORDER BY s.proficiency DESC
                    RETURNING CLOB)
                FROM employee_skills s
                WHERE s.employee_id = x.employee_id
            )), KEY 'metadata' VALUE JSON_OBJECT (KEY 'generated' VALUE TO_CHAR (SYSTIMESTAMP, 'YYYY-MM-DD\"T\"HH24:MI:SS.FF3\"Z\"'), KEY 'version' VALUE '2.0') RETURNING CLOB) AS json_output
        FROM (
            SELECT e.employee_id,
        e.first_name || ' ' || e.last_name AS emp_name,
        e.salary,
        d.department_name AS dept_name,
        XMLTYPE ('<employees><employee><name>' || e.first_name || '</name>' || '<salary>' || e.salary || '</salary></employee></employees>') AS xml_data
            FROM employees e
        JOIN departments d
                ON e.department_id = d.department_id
            WHERE e.salary > (
        SELECT AVG (salary)
        FROM employees
    )
        ) x
        WHERE XMLEXISTS ('/employees/employee[salary > 10000]' PASSING x.xml_data)
        ORDER BY x.salary DESC
        FETCH FIRST 20 ROWS ONLY;"#;

        let expected = r#"SELECT x.employee_id,
    XMLQUERY ('for $i in /employees/employee
         where $i/salary > 5000
         return <result>
             <name>{$i/name/text()}</name>
             <bonus>{$i/salary * 0.1}</bonus>
         </result>' PASSING x.xml_data RETURNING CONTENT) AS xml_result,
    JSON_OBJECT (KEY 'id' VALUE x.employee_id, KEY 'name' VALUE x.emp_name, KEY 'details' VALUE JSON_OBJECT (KEY 'salary' VALUE x.salary, KEY 'department' VALUE x.dept_name, KEY 'skills' VALUE (
            SELECT JSON_ARRAYAGG (JSON_OBJECT (KEY 'skill' VALUE s.skill_name, KEY 'level' VALUE s.proficiency) ORDER BY s.proficiency DESC
                RETURNING CLOB)
            FROM employee_skills s
            WHERE s.employee_id = x.employee_id
            )), KEY 'metadata' VALUE JSON_OBJECT (KEY 'generated' VALUE TO_CHAR (SYSTIMESTAMP, 'YYYY-MM-DD\"T\"HH24:MI:SS.FF3\"Z\"'), KEY 'version' VALUE '2.0') RETURNING CLOB) AS json_output
FROM (
        SELECT e.employee_id,
            e.first_name || ' ' || e.last_name AS emp_name,
            e.salary,
            d.department_name AS dept_name,
            XMLTYPE ('<employees><employee><name>' || e.first_name || '</name>' || '<salary>' || e.salary || '</salary></employee></employees>') AS xml_data
        FROM employees e
        JOIN departments d
            ON e.department_id = d.department_id
        WHERE e.salary > (
            SELECT AVG (salary)
            FROM employees
        )
    ) x
WHERE XMLEXISTS ('/employees/employee[salary > 10000]' PASSING x.xml_data)
ORDER BY x.salary DESC
FETCH FIRST 20 ROWS ONLY;"#;

        let formatted = SqlEditorWidget::format_for_auto_formatting(source, false);

        assert_eq!(
            formatted, expected,
            "nested JSON/XML select items should not leak list/body depth into the outer FROM/WHERE/ORDER/FETCH clauses, got:\n{}",
            formatted
        );
        assert_eq!(
            SqlEditorWidget::format_for_auto_formatting(&formatted, false),
            formatted,
            "nested JSON/XML select-item formatting should remain idempotent"
        );
    }

    #[test]
    fn format_for_auto_formatting_nested_condition_and_wrapper_query_owner_chain_uses_stack_depth()
    {
        let source =
            "SELECT CASE WHEN EXISTS ((SELECT 1 FROM dual)) THEN 'Y' ELSE 'N' END AS flag FROM dual;";
        let formatted = SqlEditorWidget::format_for_auto_formatting(source, false);
        let expected = [
            "SELECT",
            "    CASE",
            "        WHEN EXISTS (",
            "            (",
            "                SELECT 1",
            "                FROM DUAL",
            "            )",
            "        ) THEN 'Y'",
            "        ELSE 'N'",
            "    END AS flag",
            "FROM DUAL;",
        ]
        .join("\n");

        assert_eq!(
            formatted, expected,
            "nested EXISTS + wrapper query-owner chain should consume pending query-head depth in stack order"
        );
        assert_eq!(
            SqlEditorWidget::format_for_auto_formatting(&formatted, false),
            expected,
            "nested EXISTS + wrapper query-owner chain formatting should remain stable"
        );
    }

    #[test]
    fn format_for_auto_formatting_nested_some_query_owner_chain_uses_condition_stack_depth() {
        let source = "SELECT deptno FROM dept d WHERE d.deptno < SOME (SELECT e.deptno FROM emp e WHERE EXISTS (SELECT 1 FROM bonus b WHERE b.empno = e.empno));";
        let formatted = SqlEditorWidget::format_for_auto_formatting(source, false);
        let expected = [
            "SELECT deptno",
            "FROM dept d",
            "WHERE d.deptno < SOME (",
            "        SELECT e.deptno",
            "        FROM emp e",
            "        WHERE EXISTS (",
            "                SELECT 1",
            "                FROM bonus b",
            "                WHERE b.empno = e.empno",
            "            )",
            "    );",
        ]
        .join("\n");

        assert_eq!(
            formatted, expected,
            "SOME-owned nested subqueries should reuse the condition-owner stack depth just like IN/EXISTS/ANY/ALL"
        );
        assert_eq!(
            SqlEditorWidget::format_for_auto_formatting(&formatted, false),
            expected,
            "SOME-owned nested subquery formatting should remain stable"
        );
    }

    #[test]
    fn format_for_auto_formatting_lateral_nested_query_owner_chain_uses_from_item_stack_depth() {
        let source = "SELECT d.deptno, lat.max_sal FROM dept d, LATERAL (SELECT MAX(e.sal) AS max_sal FROM emp e WHERE e.deptno IN (SELECT b.deptno FROM bonus b WHERE b.empno = e.empno)) lat;";
        let formatted = SqlEditorWidget::format_for_auto_formatting(source, false);
        let expected = [
            "SELECT d.deptno,",
            "    lat.max_sal",
            "FROM dept d,",
            "    LATERAL (",
            "        SELECT MAX (e.sal) AS max_sal",
            "        FROM emp e",
            "        WHERE e.deptno IN (",
            "                SELECT b.deptno",
            "                FROM bonus b",
            "                WHERE b.empno = e.empno",
            "            )",
            "    ) lat;",
        ]
        .join("\n");

        assert_eq!(
            formatted, expected,
            "LATERAL-derived nested queries should keep their from-item owner depth and restore it after nested condition subqueries close"
        );
        assert_eq!(
            SqlEditorWidget::format_for_auto_formatting(&formatted, false),
            expected,
            "LATERAL-derived nested query formatting should remain stable"
        );
    }

    #[test]
    fn format_for_auto_formatting_mysql_from_inline_view_alias_close_uses_query_close_depth() {
        let source = r#"SELECT x.month_key,
    JSON_OBJECTAGG(x.segment, x.net_sales) AS segment_to_net_sales
FROM (
        SELECT month_key,
            segment,
            ROUND(SUM(net_sales), 2) AS net_sales
        FROM boss_monthly_stats
        GROUP BY month_key,
            segment
) x
GROUP BY x.month_key
ORDER BY x.month_key;"#;
        let formatted = SqlEditorWidget::format_for_auto_formatting_with_db_type(
            source,
            false,
            Some(crate::db::connection::DatabaseType::MySQL),
        );
        let lines: Vec<&str> = formatted.lines().collect();
        let indent = |line: &str| line.len().saturating_sub(line.trim_start().len());

        let from_idx = find_line_starting_with(&lines, "FROM (").expect("inline-view FROM owner");
        let inner_select_idx =
            find_line_starting_with(&lines, "SELECT month_key,").expect("child SELECT");
        let inner_group_idx =
            find_line_starting_with(&lines, "GROUP BY month_key,").expect("child GROUP BY");
        let close_idx = find_line_starting_with(&lines, ") x").expect("inline-view close alias");
        let outer_group_idx =
            find_line_starting_with(&lines, "GROUP BY x.month_key").expect("outer GROUP BY");

        assert_eq!(
            indent(lines[inner_select_idx]),
            indent(lines[from_idx]).saturating_add(8),
            "child SELECT under MariaDB FROM ( should keep the query-head step relative to the owner, got:\n{}",
            formatted
        );
        assert_eq!(
            indent(lines[inner_group_idx]),
            indent(lines[inner_select_idx]),
            "child GROUP BY should stay on the child query clause depth, got:\n{}",
            formatted
        );
        assert_eq!(
            indent(lines[close_idx]),
            indent(lines[from_idx]).saturating_add(4),
            "close+alias after a MariaDB FROM inline view should use stored query-close depth instead of collapsing to the FROM owner depth, got:\n{}",
            formatted
        );
        assert_eq!(
            indent(lines[outer_group_idx]),
            indent(lines[from_idx]),
            "outer GROUP BY should return to the outer query base after the inline view closes, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_for_auto_formatting_split_table_query_owner_uses_from_item_stack_depth() {
        let source = r#"SELECT
    d.deptno,
    bonus_view.bonus_deptno
FROM dept d,
    TABLE -- collection rows
    (
        SELECT
            b.deptno AS bonus_deptno
        FROM bonus b
        WHERE b.empno IN
        (
            SELECT
                e.empno
            FROM emp e
            WHERE e.deptno = d.deptno
        )
    ) bonus_view;"#;
        let formatted = SqlEditorWidget::format_for_auto_formatting(source, false);
        let expected = [
            "SELECT d.deptno,",
            "    bonus_view.bonus_deptno",
            "FROM dept d,",
            "    TABLE -- collection rows",
            "    (",
            "        SELECT b.deptno AS bonus_deptno",
            "        FROM bonus b",
            "        WHERE b.empno IN (",
            "                SELECT e.empno",
            "                FROM emp e",
            "                WHERE e.deptno = d.deptno",
            "            )",
            "    ) bonus_view;",
        ]
        .join("\n");

        assert_eq!(
            formatted, expected,
            "split TABLE-derived nested queries should keep their from-item owner depth and restore it after nested condition subqueries close"
        );
        assert_eq!(
            SqlEditorWidget::format_for_auto_formatting(&formatted, false),
            expected,
            "split TABLE-derived nested query formatting should remain stable"
        );
    }

    #[test]
    fn format_for_auto_formatting_split_lateral_query_owner_keeps_from_item_list_depth() {
        let source = r#"SELECT
    d.deptno,
    lat.max_sal
FROM dept d,
    LATERAL -- derived rows
    (
        SELECT
            MAX (e.sal) AS max_sal
        FROM emp e
        WHERE e.deptno = d.deptno
    ) lat;"#;
        let formatted = SqlEditorWidget::format_for_auto_formatting(source, false);
        let expected = [
            "SELECT d.deptno,",
            "    lat.max_sal",
            "FROM dept d,",
            "    LATERAL -- derived rows",
            "    (",
            "        SELECT MAX (e.sal) AS max_sal",
            "        FROM emp e",
            "        WHERE e.deptno = d.deptno",
            "    ) lat;",
        ]
        .join("\n");

        assert_eq!(
            formatted, expected,
            "split LATERAL-derived nested queries in a FROM-item list should stay one level deeper than the FROM clause"
        );
        assert_eq!(
            SqlEditorWidget::format_for_auto_formatting(&formatted, false),
            expected,
            "split LATERAL-derived nested query formatting should remain stable"
        );
    }

    #[test]
    fn format_for_auto_formatting_keeps_inline_view_under_scalar_subquery_from_owner_depth() {
        let source = r#"WITH t AS (
    SELECT
        1 AS grp_id,
        'A' AS code,
        10 AS val
    FROM DUAL
    UNION ALL
    SELECT
        1,
        'B',
        20
    FROM DUAL
    UNION ALL
    SELECT
        1,
        'C',
        30
    FROM DUAL
)
SELECT
    x.grp_id,
    (
        SELECT MAX (z.val)
        FROM (
                SELECT
        t2.*,
        DENSE_RANK () OVER (
            PARTITION BY t2.grp_id
            ORDER BY t2.val DESC,
                    t2.code
        ) AS dr
                FROM t t2
                WHERE t2.grp_id = x.grp_id
            ) z
        WHERE z.dr = 1
    ) AS grp_top_val
FROM t x;"#;

        let formatted = SqlEditorWidget::format_for_auto_formatting(source, false);
        let lines: Vec<&str> = formatted.lines().collect();
        let indent = |line: &str| line.len().saturating_sub(line.trim_start().len());
        let scalar_select_idx = lines
            .iter()
            .position(|line| line.trim_start() == "SELECT MAX (z.val)")
            .unwrap_or(0);
        let from_idx = lines
            .iter()
            .enumerate()
            .skip(scalar_select_idx.saturating_add(1))
            .find(|(_, line)| line.trim_start() == "FROM (")
            .map(|(idx, _)| idx)
            .unwrap_or(0);
        let inline_select_idx = lines
            .iter()
            .enumerate()
            .skip(from_idx.saturating_add(1))
            .find(|(_, line)| line.trim_start() == "SELECT")
            .map(|(idx, _)| idx)
            .unwrap_or(0);
        let select_item_idx = lines
            .iter()
            .enumerate()
            .skip(inline_select_idx.saturating_add(1))
            .find(|(_, line)| line.trim_start() == "t2.*,")
            .map(|(idx, _)| idx)
            .unwrap_or(0);
        let analytic_item_idx = lines
            .iter()
            .enumerate()
            .skip(select_item_idx.saturating_add(1))
            .find(|(_, line)| line.trim_start().starts_with("DENSE_RANK () OVER ("))
            .map(|(idx, _)| idx)
            .unwrap_or(0);
        let inline_from_idx = lines
            .iter()
            .enumerate()
            .skip(analytic_item_idx.saturating_add(1))
            .find(|(_, line)| line.trim_start() == "FROM t t2")
            .map(|(idx, _)| idx)
            .unwrap_or(0);
        let inline_close_idx = lines
            .iter()
            .enumerate()
            .skip(inline_from_idx.saturating_add(1))
            .find(|(_, line)| line.trim_start() == ") z")
            .map(|(idx, _)| idx)
            .unwrap_or(0);
        let outer_where_idx = lines
            .iter()
            .enumerate()
            .skip(inline_close_idx.saturating_add(1))
            .find(|(_, line)| line.trim_start() == "WHERE z.dr = 1")
            .map(|(idx, _)| idx)
            .unwrap_or(0);

        assert_eq!(
            indent(lines[from_idx]),
            indent(lines[scalar_select_idx]),
            "scalar-subquery FROM owner should stay aligned with the parent SELECT clause, got:\n{}",
            formatted
        );
        assert_eq!(
            indent(lines[inline_select_idx]),
            indent(lines[from_idx]).saturating_add(8),
            "inline-view SELECT under scalar-subquery FROM should use the shared query-base + child-query step, got:\n{}",
            formatted
        );
        assert_eq!(
            indent(lines[select_item_idx]),
            indent(lines[inline_select_idx]).saturating_add(4),
            "inline-view SELECT-list item should stay one level deeper than the child SELECT header, got:\n{}",
            formatted
        );
        assert_eq!(
            indent(lines[analytic_item_idx]),
            indent(lines[select_item_idx]),
            "comma-separated inline-view SELECT-list siblings should share one canonical list depth, got:\n{}",
            formatted
        );
        assert_eq!(
            indent(lines[inline_from_idx]),
            indent(lines[inline_select_idx]),
            "inline-view FROM should return to the child query clause depth after the SELECT list, got:\n{}",
            formatted
        );
        assert_eq!(
            indent(lines[inline_close_idx]),
            indent(lines[from_idx]).saturating_add(4),
            "inline-view close should realign with the child-query close depth under FROM, got:\n{}",
            formatted
        );
        assert_eq!(
            indent(lines[outer_where_idx]),
            indent(lines[from_idx]),
            "scalar-subquery WHERE should return to the scalar query clause depth after the inline view closes, got:\n{}",
            formatted
        );
        assert_eq!(
            SqlEditorWidget::format_for_auto_formatting(&formatted, false),
            formatted,
            "inline-view scalar-subquery formatting should remain idempotent"
        );
    }

    #[test]
    fn format_for_auto_formatting_generic_split_expression_query_owners_use_owner_relative_depth() {
        let source = r#"SELECT
    d.deptno,
    CURSOR -- employees
    (
        SELECT
            e.empno,
            MULTISET -- bonuses
            (
                SELECT
                    b.bonus
                FROM bonus b
                WHERE b.empno = e.empno
            ) AS bonus_list
        FROM emp e
        WHERE e.deptno = d.deptno
    ) AS emp_cur
FROM dept d;"#;
        let formatted = SqlEditorWidget::format_for_auto_formatting(source, false);
        let expected = [
            "SELECT d.deptno,",
            "    CURSOR -- employees",
            "    (",
            "        SELECT e.empno,",
            "            MULTISET -- bonuses",
            "            (",
            "                SELECT b.bonus",
            "                FROM bonus b",
            "                WHERE b.empno = e.empno",
            "            ) AS bonus_list",
            "        FROM emp e",
            "        WHERE e.deptno = d.deptno",
            "    ) AS emp_cur",
            "FROM dept d;",
        ]
        .join("\n");

        assert_eq!(
            formatted, expected,
            "split CURSOR/MULTISET owners with preserved comments should keep child queries one level deeper than each owner"
        );
        assert_eq!(
            SqlEditorWidget::format_for_auto_formatting(&formatted, false),
            expected,
            "generic split expression-query owners should remain stable after formatting"
        );
    }

    #[test]
    fn format_for_auto_formatting_mixed_close_generic_split_expression_query_owners_use_owner_relative_depth(
    ) {
        let source = r#"SELECT
    d.deptno,
    (
        SELECT 1
        FROM dual
    ) CURSOR -- employees
    (
        SELECT
            e.empno,
            (
                SELECT 1
                FROM dual
            ) MULTISET -- bonuses
            (
                SELECT
                    b.bonus
                FROM bonus b
                WHERE b.empno = e.empno
            ) AS bonus_list
        FROM emp e
        WHERE e.deptno = d.deptno
    ) AS emp_cur
FROM dept d;"#;
        let formatted = SqlEditorWidget::format_for_auto_formatting(source, false);
        let expected = [
            "SELECT d.deptno,",
            "    (",
            "        SELECT 1",
            "        FROM DUAL",
            "    ) CURSOR -- employees",
            "    (",
            "        SELECT e.empno,",
            "            (",
            "                SELECT 1",
            "                FROM DUAL",
            "            ) MULTISET -- bonuses",
            "            (",
            "                SELECT b.bonus",
            "                FROM bonus b",
            "                WHERE b.empno = e.empno",
            "            ) AS bonus_list",
            "        FROM emp e",
            "        WHERE e.deptno = d.deptno",
            "    ) AS emp_cur",
            "FROM dept d;",
        ]
        .join("\n");

        assert_eq!(
            formatted, expected,
            "mixed leading-close CURSOR/MULTISET owners should keep child queries one level deeper than each owner"
        );
        assert_eq!(
            SqlEditorWidget::format_for_auto_formatting(&formatted, false),
            expected,
            "mixed leading-close generic expression-query owners should remain stable after formatting"
        );
    }

    #[test]
    fn format_for_auto_formatting_keeps_outer_match_recognize_subclauses_after_nested_match_recognize(
    ) {
        let source = "select * from sales match_recognize (partition by cust_id order by sale_date measures (select count(*) from (select * from sales match_recognize (partition by cust_id order by sale_date measures match_number() as inner_no one row per match pattern (a) define a as amount > 0)) nested_rows) as inner_cnt all rows per match pattern (a b+) define a as amount < 50, b as amount >= 50);";
        let formatted = SqlEditorWidget::format_for_auto_formatting(source, false);
        let lines: Vec<&str> = formatted.lines().collect();
        let indent = |line: &str| line.len().saturating_sub(line.trim_start().len());

        let outer_all_idx = lines
            .iter()
            .rposition(|line| line.trim_start().starts_with("ALL ROWS PER MATCH"))
            .expect("outer ALL ROWS PER MATCH line");
        let outer_pattern_idx = lines
            .iter()
            .rposition(|line| line.trim_start().starts_with("PATTERN ("))
            .expect("outer PATTERN line");
        let outer_define_idx = lines
            .iter()
            .rposition(|line| line.trim_start().starts_with("DEFINE "))
            .expect("outer DEFINE line");
        let inner_one_row_idx = lines
            .iter()
            .position(|line| line.trim_start().starts_with("ONE ROW PER MATCH"))
            .expect("inner ONE ROW PER MATCH line");

        assert_eq!(
            indent(lines[outer_pattern_idx]),
            indent(lines[outer_all_idx]),
            "outer PATTERN should realign to the outer MATCH_RECOGNIZE owner depth after nested MATCH_RECOGNIZE closes, got:\n{}",
            formatted
        );
        assert_eq!(
            indent(lines[outer_define_idx]),
            indent(lines[outer_all_idx]),
            "outer DEFINE should realign to the outer MATCH_RECOGNIZE owner depth after nested MATCH_RECOGNIZE closes, got:\n{}",
            formatted
        );
        assert!(
            indent(lines[inner_one_row_idx]) > indent(lines[outer_all_idx]),
            "nested MATCH_RECOGNIZE subclauses should stay deeper than the outer MATCH_RECOGNIZE subclauses, got:\n{}",
            formatted
        );
        assert_eq!(
            SqlEditorWidget::format_for_auto_formatting(&formatted, false),
            formatted,
            "nested MATCH_RECOGNIZE auto-formatting should remain stable"
        );
    }

    #[test]
    fn format_for_auto_formatting_keeps_cte_close_depth_after_pivot_and_unpivot_blocks() {
        let source = r#"WITH src AS (
    SELECT
        10 AS dept_id,
        'ALICE' AS emp_name,
        'Q1' AS qtr,
        100 AS amt
    FROM DUAL
    UNION ALL
        SELECT
            10,
            'ALICE',
            'Q2',
            120
        FROM DUAL
        UNION ALL
        SELECT
            10,
            'BOB',
            'Q1',
            90
        FROM DUAL
        UNION ALL
        SELECT
            10,
            'BOB',
            'Q2',
            150
        FROM DUAL
        UNION ALL
        SELECT
            20,
            'DAVE',
            'Q1',
            80
        FROM DUAL
        UNION ALL
        SELECT
            20,
            'DAVE',
            'Q2',
            110
        FROM DUAL
        UNION ALL
        SELECT
            20,
            'ERIN',
            'Q1',
            140
        FROM DUAL
        UNION ALL
        SELECT
            20,
            'ERIN',
            'Q2',
            130
        FROM DUAL
),
p AS (
    SELECT *
    FROM src PIVOT (
        SUM (amt)
        FOR qtr IN ('Q1' AS q1, 'Q2' AS q2)
    )
        ),
u AS (
            SELECT *
            FROM p UNPIVOT (
                amt
                FOR qtr IN (q1 AS 'Q1', q2 AS 'Q2')
            )
                )
SELECT
    u.dept_id,
    u.emp_name,
    MAX (
        CASE
            WHEN u.qtr = 'Q1' THEN u.amt
        END
                ) AS q1_amt,
                MAX (
                    CASE
                        WHEN u.qtr = 'Q2' THEN u.amt
                    END
                ) AS q2_amt,
                CASE
                    WHEN MAX (
                        CASE
                            WHEN u.qtr = 'Q2' THEN u.amt
                        END
                    ) > MAX (
                        CASE
                            WHEN u.qtr = 'Q1' THEN u.amt
                        END
                    ) THEN 'UP'
                    WHEN MAX (
                        CASE
                            WHEN u.qtr = 'Q2' THEN u.amt
                        END
                    ) < MAX (
                        CASE
                            WHEN u.qtr = 'Q1' THEN u.amt
                        END
                    ) THEN 'DOWN'
                    ELSE 'SAME'
                END AS trend_flag
FROM u
GROUP BY u.dept_id,
    u.emp_name
ORDER BY u.dept_id,
    u.emp_name;"#;

        let formatted = SqlEditorWidget::format_for_auto_formatting(source, false);
        let expected = r#"WITH src AS (
    SELECT
        10 AS dept_id,
        'ALICE' AS emp_name,
        'Q1' AS qtr,
        100 AS amt
    FROM DUAL
    UNION ALL
    SELECT
        10,
        'ALICE',
        'Q2',
        120
    FROM DUAL
    UNION ALL
    SELECT
        10,
        'BOB',
        'Q1',
        90
    FROM DUAL
    UNION ALL
    SELECT
        10,
        'BOB',
        'Q2',
        150
    FROM DUAL
    UNION ALL
    SELECT
        20,
        'DAVE',
        'Q1',
        80
    FROM DUAL
    UNION ALL
    SELECT
        20,
        'DAVE',
        'Q2',
        110
    FROM DUAL
    UNION ALL
    SELECT
        20,
        'ERIN',
        'Q1',
        140
    FROM DUAL
    UNION ALL
    SELECT
        20,
        'ERIN',
        'Q2',
        130
    FROM DUAL
),
p AS (
    SELECT *
    FROM src
    PIVOT (
        SUM (amt)
        FOR qtr IN ('Q1' AS q1, 'Q2' AS q2)
    )
),
u AS (
    SELECT *
    FROM p
    UNPIVOT (
        amt
        FOR qtr IN (q1 AS 'Q1', q2 AS 'Q2')
    )
)
SELECT
    u.dept_id,
    u.emp_name,
    MAX (
        CASE
            WHEN u.qtr = 'Q1' THEN u.amt
        END
    ) AS q1_amt,
    MAX (
        CASE
            WHEN u.qtr = 'Q2' THEN u.amt
        END
    ) AS q2_amt,
    CASE
        WHEN MAX (
                CASE
                    WHEN u.qtr = 'Q2' THEN u.amt
                END
            ) > MAX (
                CASE
                    WHEN u.qtr = 'Q1' THEN u.amt
                END
            ) THEN 'UP'
        WHEN MAX (
                CASE
                    WHEN u.qtr = 'Q2' THEN u.amt
                END
            ) < MAX (
                CASE
                    WHEN u.qtr = 'Q1' THEN u.amt
                END
            ) THEN 'DOWN'
        ELSE 'SAME'
    END AS trend_flag
FROM u
GROUP BY u.dept_id,
    u.emp_name
ORDER BY u.dept_id,
    u.emp_name;"#;

        assert_eq!(
            formatted, expected,
            "CTE close parens after PIVOT/UNPIVOT blocks should return to the CTE owner depth instead of inheriting inner block depth"
        );
        assert_eq!(
            SqlEditorWidget::format_for_auto_formatting(expected, false),
            expected,
            "Auto formatting should stay stable for CTE close-paren depth after PIVOT/UNPIVOT blocks"
        );
    }

    #[test]
    fn format_sql_basic_keeps_inline_comment_after_cross_apply_open_paren() {
        let source = "select * from org_enriched oe cross apply (-- inline\nselect oe.dept_id from dual\n) ca;";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        assert!(
            formatted.contains("CROSS APPLY ( -- inline"),
            "inline comment after CROSS APPLY ( should stay on the opener line, got:\n{}",
            formatted
        );
        assert!(
            formatted.contains("SELECT oe.dept_id"),
            "CROSS APPLY body should still be formatted as a nested query, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_sql_basic_keeps_inline_comment_split_exact_apply_owner_depth_across_standalone_open_paren(
    ) {
        let source = "select * from dept d where exists (select 1 from emp e cross apply -- owner\n(select max(b.sal) as max_sal from bonus b where b.empno = e.empno) bonus_view) and d.status = 'A';";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        let lines: Vec<&str> = formatted.lines().collect();
        let indent = |line: &str| line.len().saturating_sub(line.trim_start().len());

        let apply_idx = lines
            .iter()
            .position(|line| line.trim_start().starts_with("CROSS APPLY -- owner"))
            .unwrap_or(0);
        let open_idx = lines
            .iter()
            .enumerate()
            .skip(apply_idx.saturating_add(1))
            .find(|(_, line)| line.trim() == "(")
            .map(|(idx, _)| idx)
            .unwrap_or(0);
        let nested_select_idx = lines
            .iter()
            .enumerate()
            .skip(open_idx.saturating_add(1))
            .find(|(_, line)| line.trim_start().starts_with("SELECT MAX"))
            .map(|(idx, _)| idx)
            .unwrap_or(0);
        let close_idx = lines
            .iter()
            .enumerate()
            .skip(nested_select_idx.saturating_add(1))
            .find(|(_, line)| line.trim_start().starts_with(") bonus_view"))
            .map(|(idx, _)| idx)
            .unwrap_or(0);

        assert_eq!(
            indent(lines[open_idx]),
            indent(lines[apply_idx]),
            "standalone open paren after an inline-comment split exact APPLY owner should stay on the owner depth, got:\n{}",
            formatted
        );
        assert_eq!(
            indent(lines[nested_select_idx]),
            indent(lines[apply_idx]).saturating_add(4),
            "child SELECT under an inline-comment split exact APPLY owner should stay one level deeper than owner, got:\n{}",
            formatted
        );
        assert_eq!(
            indent(lines[close_idx]),
            indent(lines[apply_idx]),
            "inline-comment split exact APPLY closing paren should realign with owner depth, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_sql_basic_keeps_inline_comment_after_merge_using_open_paren() {
        let source = "merge into dst d using (-- inline\nselect 1 as id from dual\n) s on (d.id = s.id) when matched then update set d.id = s.id;";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        assert!(
            formatted.contains("USING ( -- inline"),
            "inline comment after MERGE USING ( should stay on the opener line, got:\n{}",
            formatted
        );
        assert!(
            formatted.contains("SELECT 1 AS id"),
            "MERGE USING body should still be formatted as a nested query, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_sql_basic_keeps_search_cycle_set_inline() {
        let source = "with t as (select 1 as dept_id, 'X' as dept_name from dual union all select 2, 'Y' from dual) search depth first by dept_name set dfs_order cycle dept_id set is_cycle to 'Y' default 'N' select * from t;";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        assert!(
            formatted.contains("SEARCH DEPTH FIRST BY dept_name SET dfs_order"),
            "SEARCH ... SET should stay in a single clause line, got:
{}",
            formatted
        );
        assert!(
            formatted.contains("CYCLE dept_id SET is_cycle TO 'Y' DEFAULT 'N'"),
            "CYCLE ... SET should stay in a single clause line, got:
{}",
            formatted
        );
    }

    #[test]
    fn format_sql_basic_breaks_match_recognize_subclauses() {
        let source = "select * from sales match_recognize (partition by emp_id order by sale_date, sale_id measures match_number() as match_no all rows per match pattern (low+ mid* high) define low as amount < 100, mid as amount between 100 and 500, high as amount > 500);";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        assert!(
            formatted.contains(
                "ORDER BY sale_date,
        sale_id
    MEASURES"
            ),
            "MEASURES should start on its own line inside MATCH_RECOGNIZE, got:
{}",
            formatted
        );
        assert!(
            formatted.contains(
                "ALL ROWS PER MATCH
    PATTERN"
            ),
            "PATTERN should start on its own line inside MATCH_RECOGNIZE, got:
{}",
            formatted
        );
        assert!(
            formatted.contains(
                ")
    DEFINE"
            ),
            "DEFINE should start on its own line inside MATCH_RECOGNIZE, got:
{}",
            formatted
        );
    }

    #[test]
    fn format_sql_basic_keeps_outer_match_recognize_subclauses_after_nested_match_recognize() {
        let source = "select * from sales match_recognize (partition by cust_id order by sale_date measures (select count(*) from (select * from sales match_recognize (partition by cust_id order by sale_date measures match_number() as inner_no one row per match pattern (a) define a as amount > 0)) nested_rows) as inner_cnt all rows per match pattern (a b+) define a as amount < 50, b as amount >= 50);";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        let lines: Vec<&str> = formatted.lines().collect();
        let indent = |line: &str| line.len().saturating_sub(line.trim_start().len());

        let outer_all_idx = lines
            .iter()
            .rposition(|line| line.trim_start().starts_with("ALL ROWS PER MATCH"))
            .expect("outer ALL ROWS PER MATCH line");
        let outer_pattern_idx = lines
            .iter()
            .rposition(|line| line.trim_start().starts_with("PATTERN ("))
            .expect("outer PATTERN line");
        let outer_define_idx = lines
            .iter()
            .rposition(|line| line.trim_start().starts_with("DEFINE "))
            .expect("outer DEFINE line");
        let inner_one_row_idx = lines
            .iter()
            .position(|line| line.trim_start().starts_with("ONE ROW PER MATCH"))
            .expect("inner ONE ROW PER MATCH line");

        assert_eq!(
            indent(lines[outer_pattern_idx]),
            indent(lines[outer_all_idx]),
            "Outer PATTERN should keep the outer MATCH_RECOGNIZE subclause depth after nested MATCH_RECOGNIZE closes, got:\n{}",
            formatted
        );
        assert_eq!(
            indent(lines[outer_define_idx]),
            indent(lines[outer_all_idx]),
            "Outer DEFINE should keep the outer MATCH_RECOGNIZE subclause depth after nested MATCH_RECOGNIZE closes, got:\n{}",
            formatted
        );
        assert!(
            indent(lines[inner_one_row_idx]) > indent(lines[outer_all_idx]),
            "Nested MATCH_RECOGNIZE subclauses should stay deeper than the outer MATCH_RECOGNIZE subclauses, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_sql_basic_breaks_model_subclauses() {
        let source = "select * from sales model partition by (year_key, channel_code) dimension by (month_key) measures (base_amt, proj_amt) rules sequential order (proj_amt[any] = base_amt[cv(month_key)]);";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        assert!(
            formatted.contains(
                "MODEL
    PARTITION BY"
            ),
            "MODEL PARTITION BY should break to a new line, got:
{}",
            formatted
        );
        assert!(
            formatted.contains(
                ")
    DIMENSION BY"
            ),
            "MODEL DIMENSION BY should break to a new line, got:
{}",
            formatted
        );
        assert!(
            formatted.contains(
                ")
    MEASURES"
            ),
            "MODEL MEASURES should break to a new line, got:
{}",
            formatted
        );
        assert!(
            formatted.contains(
                ")
    RULES"
            ),
            "MODEL RULES should break to a new line, got:
{}",
            formatted
        );
    }

    #[test]
    fn formatter_keyword_like_relation_aliases_stay_inline_before_join_and_on() {
        let aliases = [
            "model",
            "window",
            "match_recognize",
            "qualify",
            "pivot",
            "unpivot",
            "search",
            "cycle",
        ];

        for alias in aliases {
            let join_boundary_source =
                format!("select * from t1 {alias} join t2 b on {alias}.id = b.id;");
            let on_boundary_source =
                format!("select * from t1 a join t2 {alias} on 1 = 1 and {alias}.a = 2;");

            for (label, formatted) in [
                (
                    "format_sql_basic/join-boundary",
                    SqlEditorWidget::format_sql_basic(join_boundary_source.as_str()),
                ),
                (
                    "auto-format/join-boundary",
                    SqlEditorWidget::format_for_auto_formatting(
                        join_boundary_source.as_str(),
                        false,
                    ),
                ),
                (
                    "format_sql_basic/on-boundary",
                    SqlEditorWidget::format_sql_basic(on_boundary_source.as_str()),
                ),
                (
                    "auto-format/on-boundary",
                    SqlEditorWidget::format_for_auto_formatting(on_boundary_source.as_str(), false),
                ),
            ] {
                assert!(
                    formatted.contains(format!("FROM t1 {alias}\nJOIN t2 b").as_str())
                        || formatted.contains(format!("JOIN t2 {alias}\n    ON 1 = 1").as_str()),
                    "{label} should keep keyword-like relation alias `{alias}` inline with its relation owner, got:\n{formatted}"
                );
                assert!(
                    formatted.contains(format!("{alias}.id = b.id").as_str())
                        || formatted.contains(format!("{alias}.a = 2").as_str()),
                    "{label} should preserve relation alias references for `{alias}`, got:\n{formatted}"
                );
                assert!(
                    !formatted.contains(format!("\n{}\n", alias.to_ascii_uppercase()).as_str()),
                    "{label} should not split keyword-like relation alias `{alias}` into a standalone clause line, got:\n{formatted}"
                );
            }
        }
    }

    #[test]
    fn formatter_keyword_like_select_aliases_stay_inline_before_from_clause() {
        let aliases = [
            "model",
            "window",
            "match_recognize",
            "qualify",
            "pivot",
            "unpivot",
            "search",
            "cycle",
        ];

        for alias in aliases {
            let source = format!("select 1 {alias}, 2 keep_alias from dual;");

            for (label, formatted) in [
                (
                    "format_sql_basic",
                    SqlEditorWidget::format_sql_basic(source.as_str()),
                ),
                (
                    "auto-format",
                    SqlEditorWidget::format_for_auto_formatting(source.as_str(), false),
                ),
            ] {
                assert!(
                    formatted.contains(format!("1 {alias},").as_str()),
                    "{label} should keep keyword-like select alias `{alias}` attached to the select item, got:\n{formatted}"
                );
                assert!(
                    formatted.contains("2 keep_alias"),
                    "{label} should keep the sibling select item after keyword-like alias `{alias}`, got:\n{formatted}"
                );
                assert!(
                    formatted.contains("FROM DUAL;"),
                    "{label} should still realign the FROM clause after keyword-like alias `{alias}`, got:\n{formatted}"
                );
                assert!(
                    !formatted.contains(format!("\n{}\n", alias.to_ascii_uppercase()).as_str()),
                    "{label} should not split keyword-like select alias `{alias}` into a clause line, got:\n{formatted}"
                );
            }
        }
    }

    #[test]
    fn format_sql_basic_breaks_pivot_and_unpivot_blocks() {
        let pivot_source =
            "select * from sales pivot (sum(amount) for category in ('A' as a, 'B' as b));";
        let pivot_formatted = SqlEditorWidget::format_sql_basic(pivot_source);
        assert!(
            pivot_formatted.contains(
                "PIVOT (
"
            ),
            "PIVOT block should be treated as subquery-like block for formatting, got:
{}",
            pivot_formatted
        );

        let unpivot_source =
            "select * from t unpivot (comp_value for comp_type in (salary as 'SALARY', bonus as 'BONUS'));";
        let unpivot_formatted = SqlEditorWidget::format_sql_basic(unpivot_source);
        assert!(
            unpivot_formatted.contains(
                "UNPIVOT (
"
            ),
            "UNPIVOT block should be treated as subquery-like block for formatting, got:
{}",
            unpivot_formatted
        );
    }

    #[test]
    fn format_for_auto_formatting_keeps_from_aligned_before_unpivot_after_multiline_select_list() {
        let source = r#"CREATE OR REPLACE PROCEDURE test_open_with_proc IS
    p_rc SYS_REFCURSOR;
BEGIN
    OPEN p_rc FOR
        WITH src AS (
            SELECT deptno,
                job,
                sal
            FROM emp
        ),
        pivoted AS (
            SELECT *
            FROM src PIVOT (
                SUM (sal) AS sum_sal FOR
                deptno IN (
                    10 AS D10, 20 AS D20, 30 AS D30)
            )
        )
        SELECT job,
            dept_tag,
            sal_amt
        FROM pivoted UNPIVOT (
            sal_amt
            FOR dept_tag IN (
                D10 AS '10', D20 AS '20', D30 AS '30')
        )
        WHERE sal_amt IS NOT NULL
        ORDER BY job,
            dept_tag;
END;"#;

        let formatted = SqlEditorWidget::format_for_auto_formatting(source, false);

        assert!(
            formatted.contains(
                "        SELECT job,\n            dept_tag,\n            sal_amt\n        FROM pivoted\n        UNPIVOT ("
            ),
            "FROM before UNPIVOT should realign to the SELECT base depth and UNPIVOT should start on its own line, got:\n{}",
            formatted
        );
        assert!(
            !formatted.contains(
                "        SELECT job,\n            dept_tag,\n            sal_amt\n            FROM pivoted"
            ),
            "FROM before UNPIVOT must not remain indented like a select-list continuation, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_for_auto_formatting_keeps_already_aligned_from_before_unpivot_stable() {
        let expected = r#"CREATE OR REPLACE PROCEDURE test_open_with_proc IS
    p_rc SYS_REFCURSOR;
BEGIN
    OPEN p_rc FOR
        WITH src AS (
            SELECT deptno,
                job,
                sal
            FROM emp
        ),
        pivoted AS (
            SELECT *
            FROM src
            PIVOT (
                SUM (sal) AS sum_sal
                FOR deptno IN (10 AS D10, 20 AS D20, 30 AS D30)
            )
        )
        SELECT job,
            dept_tag,
            sal_amt
        FROM pivoted
        UNPIVOT (
            sal_amt
            FOR dept_tag IN (D10 AS '10', D20 AS '20', D30 AS '30')
        )
        WHERE sal_amt IS NOT NULL
        ORDER BY job,
            dept_tag;
END;"#;

        let reformatted = SqlEditorWidget::format_for_auto_formatting(expected, false);

        assert_eq!(
            reformatted, expected,
            "already aligned FROM before UNPIVOT should remain stable"
        );
    }

    #[test]
    fn format_for_auto_formatting_keeps_json_table_comment_attached_and_select_items_aligned() {
        let source = r#"CREATE OR REPLACE PROCEDURE test_open_with_proc IS
    p_rc SYS_REFCURSOR;
BEGIN
       OPEN p_rc FOR
        WITH jdocs AS (
            SELECT id,
                payload
            FROM json_docs
            WHERE /* AL: 활성 문서만 */
            active_flag = 1
        )
        SELECT jd.id,

            /* AM: JSON 파싱 결과 */
            jt.order_id,
               jt.cust_name,
               jt.tier,
               it.sku,
               it.qty,
               it.price,
               (it.qty * it.price) AS line_amt
        FROM jdocs jd
        CROSS JOIN JSON_TABLE (jd.payload, 
            /* AN: root path */
            '$' COLUMNS (
                -- [AO] 최상위 컬럼
                order_id NUMBER PATH '$.order_id', cust_name VARCHAR2 (100) PATH '$.customer.name', tier VARCHAR2 (20) PATH '$.customer.tier', NESTED PATH '$.items[*]' COLUMNS (
                    /* AP: 아이템 컬럼 */
                    sku VARCHAR2 (30) PATH '$.sku', qty NUMBER PATH '$.qty', price NUMBER PATH '$.price') -- [AQ] nested columns 끝
            )        -- [AR] outer columns 끝
        )         jt
        CROSS APPLY (
            -- [AS] item alias
            SELECT jt.sku,
                jt.qty,
                jt.price
            FROM DUAL
        ) it
        ORDER BY jd.id,
            it.sku;
END;"#;

        let formatted = SqlEditorWidget::format_for_auto_formatting(source, false);

        assert!(
            formatted.contains(
                "        SELECT jd.id,\n            /* AM: JSON 파싱 결과 */\n            jt.order_id,\n            jt.cust_name,\n            jt.tier,"
            ),
            "JSON_TABLE select-list comment should stay attached to the preceding SELECT item block and the following items should share one depth, got:\n{}",
            formatted
        );
        assert!(
            !formatted.contains("SELECT jd.id,\n\n            /* AM: JSON 파싱 결과 */"),
            "formatter should not insert a blank line before the JSON_TABLE select-list comment, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_for_auto_formatting_breaks_json_table_columns_clause_into_aligned_items() {
        let source = r#"CREATE OR REPLACE PROCEDURE test_open_with_proc IS
    p_rc SYS_REFCURSOR;
BEGIN
     OPEN p_rc FOR
        WITH jdocs AS (
            SELECT id,
                payload
            FROM json_docs
            WHERE /* AL: 활성 문서만 */
            active_flag = 1
        )
        SELECT jd.id,
            /* AM: JSON 파싱 결과 */
            jt.order_id,
            jt.cust_name,
            jt.tier,
            it.sku,
            it.qty,
            it.price,
            (it.qty * it.price) AS line_amt
        FROM jdocs jd
        CROSS JOIN JSON_TABLE (jd.payload, 
            /* AN: root path */
            '$' COLUMNS (
                -- [AO] 최상위 컬럼
                order_id NUMBER PATH '$.order_id', cust_name VARCHAR2 (100) PATH '$.customer.name', tier VARCHAR2 (20) PATH '$.customer.tier', NESTED PATH '$.items[*]' COLUMNS (
                    /* AP: 아이템 컬럼 */
                    sku VARCHAR2 (30) PATH '$.sku', qty NUMBER PATH '$.qty', price NUMBER PATH '$.price') -- [AQ] nested columns 끝
            )        -- [AR] outer columns 끝
        )         jt
        CROSS APPLY (
            -- [AS] item alias
            SELECT jt.sku,
                jt.qty,
                jt.price
            FROM DUAL
        ) it
        ORDER BY jd.id,
            it.sku;
END;"#;

        let formatted = SqlEditorWidget::format_for_auto_formatting(source, false);

        assert!(
            formatted.contains(
                "        CROSS JOIN JSON_TABLE (jd.payload,\n            /* AN: root path */\n            '$' COLUMNS (\n                -- [AO] 최상위 컬럼\n                order_id NUMBER PATH '$.order_id',\n                cust_name VARCHAR2 (100) PATH '$.customer.name',\n                tier VARCHAR2 (20) PATH '$.customer.tier',\n                NESTED PATH '$.items[*]' COLUMNS (\n                    /* AP: 아이템 컬럼 */\n                    sku VARCHAR2 (30) PATH '$.sku',\n                    qty NUMBER PATH '$.qty',\n                    price NUMBER PATH '$.price'\n                ) -- [AQ] nested columns 끝\n            ) -- [AR] outer columns 끝\n        ) jt"
            ),
            "JSON_TABLE COLUMNS clause should reuse structured column-list layout, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_sql_basic_insert_all_into_indented() {
        let source = "insert all into t1 (a) values (1) into t2 (b) values (2) select * from dual;";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        let lines: Vec<&str> = formatted.lines().collect();
        let indent = |line: &str| line.len().saturating_sub(line.trim_start().len());
        let insert_idx = lines
            .iter()
            .position(|line| line.trim_start() == "INSERT ALL")
            .unwrap_or(0);
        let into_1_idx = lines
            .iter()
            .position(|line| line.trim_start().starts_with("INTO t1"))
            .unwrap_or(0);
        let values_1_idx = lines
            .iter()
            .position(|line| line.trim_start() == "VALUES (1)")
            .unwrap_or(0);
        let into_2_idx = lines
            .iter()
            .position(|line| line.trim_start().starts_with("INTO t2"))
            .unwrap_or(0);
        let values_2_idx = lines
            .iter()
            .position(|line| line.trim_start() == "VALUES (2)")
            .unwrap_or(0);
        let select_idx = lines
            .iter()
            .rposition(|line| line.trim_start() == "SELECT *")
            .unwrap_or(0);

        assert_eq!(
            indent(lines[into_1_idx]),
            indent(lines[insert_idx]).saturating_add(4),
            "INSERT ALL INTO should be exactly one structural level deeper than INSERT ALL, got:\n{}",
            formatted
        );
        assert_eq!(
            indent(lines[values_1_idx]),
            indent(lines[into_1_idx]),
            "INSERT ALL VALUES should align with its sibling INTO branch depth, got:\n{}",
            formatted
        );
        assert_eq!(
            indent(lines[into_2_idx]),
            indent(lines[into_1_idx]),
            "INSERT ALL INTO branches should share the same structural depth, got:\n{}",
            formatted
        );
        assert_eq!(
            indent(lines[values_2_idx]),
            indent(lines[into_1_idx]),
            "INSERT ALL VALUES branches should share the same structural depth, got:\n{}",
            formatted
        );
        assert_eq!(
            indent(lines[select_idx]),
            indent(lines[insert_idx]),
            "INSERT ALL driving SELECT should return to the query head depth, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_sql_basic_insert_all_case_inside_values_keeps_expression_depth() {
        let source = r#"INSERT ALL
    WHEN total_amount > 10000 THEN
        INTO high_value_orders (order_id, customer_id, amount, order_date)
        VALUES (oid, cid, total_amount, odate)
        INTO vip_notifications (customer_id, message, created)
        VALUES (cid, 'High value order: ' || TO_CHAR (total_amount, 'FM$999,999.00'), SYSDATE)
    WHEN total_amount BETWEEN 1000 AND 10000 THEN
        INTO medium_value_orders (order_id, customer_id, amount)
        VALUES (oid, cid, total_amount)
    WHEN category = 'ELECTRONICS' THEN
        INTO electronics_orders (order_id, amount, warranty_end)
        VALUES (oid, total_amount, ADD_MONTHS (odate,
    CASE
        WHEN total_amount > 5000 THEN 24
            ELSE 12
    END
    ))
    ELSE
        INTO standard_orders (order_id, customer_id, amount)
        VALUES (oid, cid, total_amount)
SELECT o.order_id AS oid,
    o.customer_id AS cid,
    o.total_amount,
    o.order_date AS odate,
    p.category
FROM orders o
JOIN products p
    ON o.product_id = p.product_id
WHERE o.order_date >= TRUNC (SYSDATE, 'MM');"#;

        let formatted = SqlEditorWidget::format_sql_basic(source);
        let lines: Vec<&str> = formatted.lines().collect();
        let leading_spaces = |line: &str| line.len().saturating_sub(line.trim_start().len());
        let values_idx = lines
            .iter()
            .position(|line| {
                line.trim_start()
                    .starts_with("VALUES (oid, total_amount, ADD_MONTHS (odate,")
            })
            .unwrap_or(0);
        let case_idx = lines
            .iter()
            .enumerate()
            .skip(values_idx.saturating_add(1))
            .find(|(_, line)| line.trim_start() == "CASE")
            .map(|(idx, _)| idx)
            .unwrap_or(0);
        let when_idx = lines
            .iter()
            .enumerate()
            .skip(case_idx.saturating_add(1))
            .find(|(_, line)| line.trim_start() == "WHEN total_amount > 5000 THEN 24")
            .map(|(idx, _)| idx)
            .unwrap_or(0);
        let else_idx = lines
            .iter()
            .enumerate()
            .skip(when_idx.saturating_add(1))
            .find(|(_, line)| line.trim_start() == "ELSE 12")
            .map(|(idx, _)| idx)
            .unwrap_or(0);
        let end_idx = lines
            .iter()
            .enumerate()
            .skip(else_idx.saturating_add(1))
            .find(|(_, line)| line.trim_start() == "END")
            .map(|(idx, _)| idx)
            .unwrap_or(0);
        let close_idx = lines
            .iter()
            .enumerate()
            .skip(end_idx.saturating_add(1))
            .find(|(_, line)| line.trim_start() == "))")
            .map(|(idx, _)| idx)
            .unwrap_or(0);

        assert!(
            leading_spaces(lines[case_idx]) > leading_spaces(lines[values_idx]),
            "CASE inside INSERT ALL VALUES should indent deeper than VALUES, got:\n{}",
            formatted
        );
        assert!(
            leading_spaces(lines[when_idx]) > leading_spaces(lines[case_idx]),
            "WHEN inside INSERT ALL VALUES CASE should indent deeper than CASE, got:\n{}",
            formatted
        );
        assert_eq!(
            leading_spaces(lines[else_idx]),
            leading_spaces(lines[when_idx]),
            "ELSE inside INSERT ALL VALUES CASE should align with WHEN, got:\n{}",
            formatted
        );
        assert_eq!(
            leading_spaces(lines[end_idx]),
            leading_spaces(lines[case_idx]),
            "END inside INSERT ALL VALUES CASE should align with CASE, got:\n{}",
            formatted
        );
        assert_eq!(
            leading_spaces(lines[close_idx]),
            leading_spaces(lines[values_idx]),
            ")) after INSERT ALL VALUES CASE should realign with VALUES, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_sql_basic_package_body_open_cursor_nested_subquery_keeps_expected_query_base_depth() {
        let input = r#"create package body a as
    procedure b (c in number) as
    begin
        open cv for
            select 1
            from e
            where f in (
                    select 1
                    from (
                            select g
                            from dual
                        )
                );
    end b;
end a;"#;
        let expected = r#"create package body a as
    procedure b (c in number) as
    begin
        open cv for
            select 1
            from e
            where f in (
                    select 1
                    from (
                            select g
                            from dual
                        )
                );
    end b;
end a;"#;

        let formatted = SqlEditorWidget::format_sql_basic(input);
        assert_eq!(
            formatted.trim().to_ascii_lowercase(),
            expected.trim().to_ascii_lowercase(),
            "package body OPEN cursor nested subquery should keep expected query base depth (case-insensitive), got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_sql_basic_end_case_in_dml_aligns_with_case_keyword() {
        let source = r#"SELECT
    col1,
    CASE
        WHEN col2 = 1 THEN 'a'
        ELSE 'b'
    END CASE AS result
FROM t1;"#;

        let formatted = SqlEditorWidget::format_sql_basic(source);
        let lines: Vec<&str> = formatted.lines().collect();
        let leading_spaces = |line: &str| line.len().saturating_sub(line.trim_start().len());

        let case_idx = lines
            .iter()
            .position(|line| line.trim_start() == "CASE")
            .expect("CASE line should exist");
        let end_case_idx = lines
            .iter()
            .position(|line| line.trim_start().starts_with("END CASE"))
            .expect("END CASE line should exist");

        assert_eq!(
            leading_spaces(lines[case_idx]),
            leading_spaces(lines[end_case_idx]),
            "END CASE should align with CASE keyword, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_sql_basic_nested_case_end_case_in_dml_preserves_depth() {
        let source = r#"SELECT
    CASE
        WHEN a = 1 THEN
            CASE
                WHEN b = 1 THEN 'x'
                ELSE 'y'
            END CASE
        ELSE 'z'
    END AS result
FROM t1;"#;

        let formatted = SqlEditorWidget::format_sql_basic(source);
        let lines: Vec<&str> = formatted.lines().collect();
        let leading_spaces = |line: &str| line.len().saturating_sub(line.trim_start().len());

        let outer_case_idx = lines
            .iter()
            .position(|line| line.trim_start() == "CASE")
            .expect("outer CASE should exist");
        let inner_case_idx = lines
            .iter()
            .enumerate()
            .skip(outer_case_idx + 1)
            .find(|(_, line)| line.trim_start() == "CASE")
            .map(|(idx, _)| idx)
            .expect("inner CASE should exist");
        let end_case_idx = lines
            .iter()
            .position(|line| line.trim_start().starts_with("END CASE"))
            .expect("END CASE should exist");
        let outer_else_idx = lines
            .iter()
            .enumerate()
            .skip(end_case_idx + 1)
            .find(|(_, line)| line.trim_start().starts_with("ELSE"))
            .map(|(idx, _)| idx)
            .expect("outer ELSE should exist");

        assert_eq!(
            leading_spaces(lines[inner_case_idx]),
            leading_spaces(lines[end_case_idx]),
            "END CASE should align with inner CASE, got:\n{}",
            formatted
        );
        assert!(
            leading_spaces(lines[inner_case_idx]) > leading_spaces(lines[outer_case_idx]),
            "Inner CASE should indent deeper than outer CASE, got:\n{}",
            formatted
        );
        assert_eq!(
            leading_spaces(lines[outer_else_idx]),
            leading_spaces(lines[outer_case_idx]).saturating_add(4),
            "Outer ELSE should indent one level deeper than outer CASE, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_for_auto_formatting_nested_case_after_then_uses_branch_body_depth() {
        let source = r#"SELECT CASE WHEN score > avg_score THEN CASE WHEN bonus >= 300 THEN 'TOP_WITH_BONUS' ELSE 'TOP_NO_BIG_BONUS' END ELSE CASE WHEN grade IN ('A', 'B') THEN 'MID_GOOD_GRADE' ELSE 'MID_OTHER' END END AS emp_class FROM dual;"#;

        let formatted = SqlEditorWidget::format_for_auto_formatting(source, false);
        let lines: Vec<&str> = formatted.lines().collect();
        let leading_spaces = |line: &str| line.len().saturating_sub(line.trim_start().len());

        let outer_when_idx = lines
            .iter()
            .position(|line| line.trim_start().starts_with("WHEN score > avg_score THEN"))
            .expect("outer WHEN should exist");
        let first_inner_case_idx = lines
            .iter()
            .enumerate()
            .skip(outer_when_idx + 1)
            .find(|(_, line)| line.trim_start() == "CASE")
            .map(|(idx, _)| idx)
            .expect("inner CASE after THEN should exist");
        let outer_else_idx = lines
            .iter()
            .enumerate()
            .skip(first_inner_case_idx + 1)
            .find(|(_, line)| line.trim_start() == "ELSE")
            .map(|(idx, _)| idx)
            .expect("outer ELSE should exist");
        let second_inner_case_idx = lines
            .iter()
            .enumerate()
            .skip(outer_else_idx + 1)
            .find(|(_, line)| line.trim_start() == "CASE")
            .map(|(idx, _)| idx)
            .expect("inner CASE after ELSE should exist");

        assert_eq!(
            leading_spaces(lines[first_inner_case_idx]),
            leading_spaces(lines[outer_when_idx]).saturating_add(4),
            "CASE after THEN should indent one level deeper than the WHEN branch header, got:\n{}",
            formatted
        );
        assert_eq!(
            leading_spaces(lines[second_inner_case_idx]),
            leading_spaces(lines[outer_else_idx]).saturating_add(4),
            "CASE after ELSE should indent one level deeper than the ELSE branch header, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_for_auto_formatting_order_siblings_by_case_uses_clause_body_depth() {
        let source = r#"SELECT CONNECT_BY_ROOT o.node_name AS root_name,
    o.node_id,
    o.parent_id,
    o.node_name,
    LEVEL AS lvl,
    SYS_CONNECT_BY_PATH (o.node_name, ' > ') AS node_path,
    CONNECT_BY_ISLEAF AS is_leaf,
    (
        SELECT SUM (x.node_val)
        FROM org_tree x
        START WITH x.node_id = o.node_id
        CONNECT BY PRIOR x.node_id = x.parent_id
    ) AS subtree_sum,
    CASE
        WHEN CONNECT_BY_ISLEAF = 1 THEN (
            SELECT MAX (y.node_val)
            FROM org_tree y
            WHERE y.parent_id = o.parent_id
        )
        ELSE (
            SELECT COUNT (*)
            FROM org_tree c
            WHERE c.parent_id = o.node_id
        )
    END AS compare_metric
FROM org_tree o
START WITH o.parent_id IS NULL
CONNECT BY PRIOR o.node_id = o.parent_id
ORDER SIBLINGS BY
CASE
    WHEN o.parent_id IS NULL THEN 0
    ELSE 1
END,
    o.node_name;"#;

        let formatted = SqlEditorWidget::format_for_auto_formatting(source, false);
        let lines: Vec<&str> = formatted.lines().collect();

        let order_idx = find_line_starting_with(&lines, "ORDER SIBLINGS BY")
            .expect("ORDER SIBLINGS BY line should exist");
        let case_idx = lines
            .iter()
            .enumerate()
            .skip(order_idx.saturating_add(1))
            .find(|(_, line)| line.trim_start() == "CASE")
            .map(|(idx, _)| idx)
            .expect("ORDER SIBLINGS BY CASE line should exist");
        let when_idx = lines
            .iter()
            .enumerate()
            .skip(case_idx.saturating_add(1))
            .find(|(_, line)| {
                line.trim_start()
                    .starts_with("WHEN o.parent_id IS NULL THEN")
            })
            .map(|(idx, _)| idx)
            .expect("ORDER SIBLINGS BY CASE WHEN line should exist");
        let end_idx = lines
            .iter()
            .enumerate()
            .skip(when_idx.saturating_add(1))
            .find(|(_, line)| line.trim_start() == "END,")
            .map(|(idx, _)| idx)
            .expect("ORDER SIBLINGS BY CASE END line should exist");

        assert_eq!(
            leading_spaces(lines[case_idx]),
            leading_spaces(lines[order_idx]).saturating_add(4),
            "CASE after ORDER SIBLINGS BY should indent one level deeper than the clause header, got:\n{}",
            formatted
        );
        assert_eq!(
            leading_spaces(lines[when_idx]),
            leading_spaces(lines[case_idx]).saturating_add(4),
            "WHEN inside ORDER SIBLINGS BY CASE should indent one level deeper than CASE, got:\n{}",
            formatted
        );
        assert_eq!(
            leading_spaces(lines[end_idx]),
            leading_spaces(lines[case_idx]),
            "END in ORDER SIBLINGS BY CASE should align with CASE, got:\n{}",
            formatted
        );
        assert_eq!(
            SqlEditorWidget::format_for_auto_formatting(&formatted, false),
            formatted,
            "ORDER SIBLINGS BY CASE formatting should stay stable after reformatting"
        );
    }

    #[test]
    fn format_sql_basic_nested_parenthesized_case_condition_keeps_each_close_paren_on_its_owner() {
        let source = r#"BEGIN
    IF (
        CASE
            WHEN flag = 'Y' THEN (
                CASE
                    WHEN nested_flag = 'Y' THEN 1
                    ELSE 0
                END
            )
            ELSE 0
        END
    ) = 1 THEN
        NULL;
    END IF;
END;"#;

        let formatted = SqlEditorWidget::format_sql_basic(source);
        let lines: Vec<&str> = formatted.lines().collect();

        let if_idx = find_line_starting_with(&lines, "IF (").expect("IF line");
        let outer_when_idx =
            find_line_starting_with(&lines, "WHEN flag = 'Y' THEN").expect("outer WHEN line");
        let inner_case_idx = find_line_starting_with(&lines, "CASE")
            .and_then(|idx| (idx > if_idx).then_some(idx + 1))
            .and_then(|start| {
                lines
                    .iter()
                    .enumerate()
                    .skip(start)
                    .find(|(_, line)| line.trim_start() == "CASE")
                    .map(|(idx, _)| idx)
            })
            .expect("inner CASE line");
        let inner_when_idx = lines
            .iter()
            .enumerate()
            .skip(inner_case_idx + 1)
            .find(|(_, line)| line.trim_start().starts_with("WHEN nested_flag = 'Y' THEN"))
            .map(|(idx, _)| idx)
            .expect("inner WHEN line");
        let inner_close_idx = lines
            .iter()
            .enumerate()
            .skip(inner_when_idx + 1)
            .find(|(_, line)| line.trim() == ")")
            .map(|(idx, _)| idx)
            .expect("inner close paren line");
        let outer_close_idx = lines
            .iter()
            .position(|line| line.trim_start().starts_with(") = 1 THEN"))
            .expect("outer close paren line");

        assert!(
            leading_spaces(lines[inner_case_idx]) > leading_spaces(lines[outer_when_idx]),
            "inner CASE should indent deeper than the outer WHEN owner, got:\n{}",
            formatted
        );
        assert!(
            leading_spaces(lines[inner_when_idx]) > leading_spaces(lines[inner_case_idx]),
            "inner WHEN should indent deeper than the nested CASE, got:\n{}",
            formatted
        );
        assert_eq!(
            leading_spaces(lines[inner_close_idx]),
            leading_spaces(lines[outer_when_idx]),
            "inner close paren should realign to the outer WHEN owner line, got:\n{}",
            formatted
        );
        assert_eq!(
            leading_spaces(lines[outer_close_idx]),
            leading_spaces(lines[if_idx]),
            "outer close paren should realign to the IF owner line, got:\n{}",
            formatted
        );
        assert_eq!(SqlEditorWidget::format_sql_basic(&formatted), formatted);
    }

    #[test]
    fn format_sql_basic_nested_parenthesized_case_after_branch_wrapper_keeps_branch_depth() {
        let source = r#"BEGIN
    IF (
        CASE
            WHEN flag = 'Y' THEN
                (
                    -- gap
                    CASE
                        WHEN nested_flag = 'Y' THEN 1
                        ELSE 0
                    END
                )
            ELSE 0
        END
    ) = 1 THEN
        NULL;
    END IF;
END;"#;

        let formatted = SqlEditorWidget::format_sql_basic(source);
        let lines: Vec<&str> = formatted.lines().collect();

        let outer_when_idx =
            find_line_starting_with(&lines, "WHEN flag = 'Y' THEN").expect("outer WHEN line");
        let wrapper_open_idx = lines
            .iter()
            .enumerate()
            .skip(outer_when_idx + 1)
            .find(|(_, line)| line.trim() == "(")
            .map(|(idx, _)| idx)
            .expect("wrapper open paren line");
        let gap_comment_idx = lines
            .iter()
            .enumerate()
            .skip(wrapper_open_idx + 1)
            .find(|(_, line)| line.trim_start() == "-- gap")
            .map(|(idx, _)| idx)
            .expect("wrapper gap comment line");
        let inner_case_idx = lines
            .iter()
            .enumerate()
            .skip(gap_comment_idx + 1)
            .find(|(_, line)| line.trim_start() == "CASE")
            .map(|(idx, _)| idx)
            .expect("inner CASE line");
        let inner_close_idx = lines
            .iter()
            .enumerate()
            .skip(inner_case_idx + 1)
            .find(|(_, line)| line.trim() == ")")
            .map(|(idx, _)| idx)
            .expect("wrapper close paren line");

        assert_eq!(
            leading_spaces(lines[wrapper_open_idx]),
            leading_spaces(lines[outer_when_idx]).saturating_add(4),
            "branch wrapper paren should consume the pending branch-body depth exactly once, got:\n{}",
            formatted
        );
        assert_eq!(
            leading_spaces(lines[gap_comment_idx]),
            leading_spaces(lines[wrapper_open_idx]).saturating_add(4),
            "comment inside the branch wrapper should stay aligned with the wrapper body depth, got:\n{}",
            formatted
        );
        assert_eq!(
            leading_spaces(lines[inner_case_idx]),
            leading_spaces(lines[wrapper_open_idx]).saturating_add(4),
            "CASE after branch wrapper should stay one level deeper than the wrapper owner, got:\n{}",
            formatted
        );
        assert_eq!(
            leading_spaces(lines[inner_close_idx]),
            leading_spaces(lines[outer_when_idx]),
            "wrapper close paren should realign with the outer WHEN owner depth after the nested CASE closes, got:\n{}",
            formatted
        );
        assert_eq!(SqlEditorWidget::format_sql_basic(&formatted), formatted);
    }

    #[test]
    fn format_sql_basic_nested_parenthesized_dml_case_branches_keep_stack_scoped_indents() {
        let source = r#"SELECT CASE
WHEN a = 1 THEN (
CASE
WHEN b = 2 THEN (
CASE
WHEN c = 3 THEN 1
ELSE 0
END
)
ELSE 2
END
)
ELSE 3
END AS result
FROM dual;"#;

        let formatted = SqlEditorWidget::format_sql_basic(source);
        let lines: Vec<&str> = formatted.lines().collect();

        let outer_when_idx =
            find_line_starting_with(&lines, "WHEN a = 1 THEN").expect("outer WHEN line");
        let first_open_idx = lines
            .iter()
            .enumerate()
            .skip(outer_when_idx + 1)
            .find(|(_, line)| line.trim() == "(")
            .map(|(idx, _)| idx)
            .expect("first open paren line");
        let middle_case_idx = lines
            .iter()
            .enumerate()
            .skip(first_open_idx + 1)
            .find(|(_, line)| line.trim_start() == "CASE")
            .map(|(idx, _)| idx)
            .expect("middle CASE line");
        let inner_when_idx = lines
            .iter()
            .enumerate()
            .skip(middle_case_idx + 1)
            .find(|(_, line)| line.trim_start().starts_with("WHEN b = 2 THEN"))
            .map(|(idx, _)| idx)
            .expect("inner WHEN line");
        let second_open_idx = lines
            .iter()
            .enumerate()
            .skip(inner_when_idx + 1)
            .find(|(_, line)| line.trim() == "(")
            .map(|(idx, _)| idx)
            .expect("second open paren line");
        let deepest_case_idx = lines
            .iter()
            .enumerate()
            .skip(second_open_idx + 1)
            .find(|(_, line)| line.trim_start() == "CASE")
            .map(|(idx, _)| idx)
            .expect("deepest CASE line");
        let inner_close_idx = lines
            .iter()
            .enumerate()
            .skip(deepest_case_idx + 1)
            .find(|(_, line)| line.trim() == ")")
            .map(|(idx, _)| idx)
            .expect("inner close paren line");
        let outer_close_idx = lines
            .iter()
            .enumerate()
            .skip(inner_close_idx + 1)
            .find(|(_, line)| line.trim() == ")")
            .map(|(idx, _)| idx)
            .expect("outer close paren line");

        assert!(
            leading_spaces(lines[middle_case_idx]) > leading_spaces(lines[first_open_idx]),
            "CASE after the first standalone paren should indent deeper than the wrapper line, got:\n{}",
            formatted
        );
        assert!(
            leading_spaces(lines[deepest_case_idx]) > leading_spaces(lines[second_open_idx]),
            "deepest CASE should indent deeper than the nested wrapper line, got:\n{}",
            formatted
        );
        assert_eq!(
            leading_spaces(lines[inner_close_idx]),
            leading_spaces(lines[inner_when_idx]),
            "inner close paren should realign to its WHEN owner line, got:\n{}",
            formatted
        );
        assert_eq!(
            leading_spaces(lines[outer_close_idx]),
            leading_spaces(lines[outer_when_idx]),
            "outer close paren should realign to the outer WHEN owner line, got:\n{}",
            formatted
        );
        assert_eq!(SqlEditorWidget::format_sql_basic(&formatted), formatted);
    }

    #[test]
    fn format_sql_basic_case_when_after_clause_starter_opens_frame() {
        let source = r#"SELECT
    CASE
        WHEN col1 = 1 THEN 'a'
        WHEN col1 = 2 THEN 'b'
        ELSE 'c'
    END AS result,
    col2
FROM t1;"#;

        let formatted = SqlEditorWidget::format_sql_basic(source);
        let lines: Vec<&str> = formatted.lines().collect();
        let leading_spaces = |line: &str| line.len().saturating_sub(line.trim_start().len());

        let case_idx = lines
            .iter()
            .position(|line| line.trim_start() == "CASE")
            .expect("CASE should exist");
        let when1_idx = lines
            .iter()
            .enumerate()
            .skip(case_idx + 1)
            .find(|(_, line)| line.trim_start().starts_with("WHEN col1 = 1"))
            .map(|(idx, _)| idx)
            .expect("first WHEN should exist");
        let when2_idx = lines
            .iter()
            .enumerate()
            .skip(when1_idx + 1)
            .find(|(_, line)| line.trim_start().starts_with("WHEN col1 = 2"))
            .map(|(idx, _)| idx)
            .expect("second WHEN should exist");
        let else_idx = lines
            .iter()
            .enumerate()
            .skip(when2_idx + 1)
            .find(|(_, line)| line.trim_start().starts_with("ELSE"))
            .map(|(idx, _)| idx)
            .expect("ELSE should exist");
        let end_idx = lines
            .iter()
            .enumerate()
            .skip(else_idx + 1)
            .find(|(_, line)| line.trim_start().starts_with("END"))
            .map(|(idx, _)| idx)
            .expect("END should exist");

        assert!(
            leading_spaces(lines[when1_idx]) > leading_spaces(lines[case_idx]),
            "WHEN should indent deeper than CASE, got:\n{}",
            formatted
        );
        assert_eq!(
            leading_spaces(lines[when1_idx]),
            leading_spaces(lines[when2_idx]),
            "All WHEN branches should align, got:\n{}",
            formatted
        );
        assert_eq!(
            leading_spaces(lines[when1_idx]),
            leading_spaces(lines[else_idx]),
            "ELSE should align with WHEN, got:\n{}",
            formatted
        );
        assert_eq!(
            leading_spaces(lines[end_idx]),
            leading_spaces(lines[case_idx]),
            "END should align with CASE, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_sql_basic_mid_line_case_after_comma_opens_frame() {
        // Stage 1 keeps CASE on the same line as the preceding comma item,
        // so phase 2 must open the CASE frame from that owner line itself.
        let source = "SELECT col1, CASE\nWHEN col2 = 1 THEN 'a'\nELSE 'b'\nEND AS result\nFROM t1;";

        let formatted = SqlEditorWidget::format_sql_basic(source);
        let lines: Vec<&str> = formatted.lines().collect();
        let leading_spaces = |line: &str| line.len().saturating_sub(line.trim_start().len());

        let when_idx = lines
            .iter()
            .position(|line| line.trim_start().starts_with("WHEN"))
            .expect("WHEN should exist");
        let else_idx = lines
            .iter()
            .position(|line| line.trim_start().starts_with("ELSE"))
            .expect("ELSE should exist");
        let end_idx = lines
            .iter()
            .position(|line| line.trim_start().starts_with("END"))
            .expect("END should exist");

        assert_eq!(
            leading_spaces(lines[when_idx]),
            leading_spaces(lines[else_idx]),
            "ELSE should align with WHEN when CASE opened mid-line, got:\n{}",
            formatted
        );
        assert!(
            leading_spaces(lines[when_idx]) > leading_spaces(lines[end_idx]),
            "WHEN should indent deeper than END when CASE opened mid-line, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_sql_basic_mid_line_case_in_update_set_opens_frame() {
        let source = "UPDATE t1\nSET col1 = CASE\nWHEN col2 = 1 THEN 'a'\nELSE 'b'\nEND;";

        let formatted = SqlEditorWidget::format_sql_basic(source);
        let lines: Vec<&str> = formatted.lines().collect();
        let leading_spaces = |line: &str| line.len().saturating_sub(line.trim_start().len());

        let when_idx = lines
            .iter()
            .position(|line| line.trim_start().starts_with("WHEN"))
            .expect("WHEN should exist");
        let else_idx = lines
            .iter()
            .position(|line| line.trim_start().starts_with("ELSE"))
            .expect("ELSE should exist");
        let end_idx = lines
            .iter()
            .position(|line| line.trim_start().starts_with("END"))
            .expect("END should exist");

        assert_eq!(
            leading_spaces(lines[when_idx]),
            leading_spaces(lines[else_idx]),
            "ELSE should align with WHEN in UPDATE SET CASE, got:\n{}",
            formatted
        );
        assert!(
            leading_spaces(lines[when_idx]) > leading_spaces(lines[end_idx]),
            "WHEN should indent deeper than END in UPDATE SET CASE, got:\n{}",
            formatted
        );
    }

    #[test]
    fn line_has_trailing_unclosed_case_detects_mid_line_case() {
        assert!(SqlEditorWidget::line_has_trailing_unclosed_case(
            "SELECT col1, CASE"
        ));
        assert!(SqlEditorWidget::line_has_trailing_unclosed_case(
            "SET col1 = CASE"
        ));
        assert!(SqlEditorWidget::line_has_trailing_unclosed_case(
            "NVL(col, CASE"
        ));
    }

    #[test]
    fn line_has_trailing_unclosed_case_ignores_closed_case() {
        assert!(!SqlEditorWidget::line_has_trailing_unclosed_case(
            "CASE WHEN a = 1 THEN 'x' ELSE 'y' END"
        ));
        assert!(!SqlEditorWidget::line_has_trailing_unclosed_case(
            "SELECT CASE WHEN a THEN b END AS c"
        ));
    }

    #[test]
    fn line_has_trailing_unclosed_case_returns_false_for_no_case() {
        assert!(!SqlEditorWidget::line_has_trailing_unclosed_case(
            "SELECT col1, col2"
        ));
        assert!(!SqlEditorWidget::line_has_trailing_unclosed_case(
            "WHERE col1 = 1"
        ));
    }

    #[test]
    fn nested_paren_and_or_drops_depth_after_close_paren() {
        let input = r#"SELECT *
FROM emp
WHERE (
    (
        col1 = 1
        AND col2 = 2
    )
    OR (
        col3 = 3
        AND col4 = 4
    )
)
AND (
    (
        col5 = 5
        OR col6 = 6
    )
    AND col7 = 7
);"#;
        let formatted = SqlEditorWidget::format_sql_basic(input);
        let lines: Vec<&str> = formatted.lines().collect();
        let indent = |line: &str| line.len().saturating_sub(line.trim_start().len());

        let and_outer = lines
            .iter()
            .position(|line| line.trim_start().starts_with("AND ((col5"))
            .expect("should contain outer AND ((col5");
        let where_idx = lines
            .iter()
            .position(|line| line.trim_start().starts_with("WHERE"))
            .expect("should contain WHERE");
        let condition_base = indent(lines[where_idx]) + 4;

        assert_eq!(
            indent(lines[and_outer]),
            condition_base,
            "top-level AND after closed paren group should return to condition base depth, got:\n{}",
            formatted
        );

        let reformatted = SqlEditorWidget::format_sql_basic(&formatted);
        assert_eq!(
            reformatted, formatted,
            "formatting should be stable for deeply nested parenthesized conditions"
        );
    }

    #[test]
    fn mixed_nested_paren_and_or_keeps_correct_depths() {
        let input = r#"SELECT *
FROM emp
WHERE status = 'A'
AND (
    dept_id = 10
    OR (
        dept_id = 20
        AND role = 'MGR'
    )
    OR dept_id = 30
)
AND active = 1;"#;
        let formatted = SqlEditorWidget::format_sql_basic(input);
        let lines: Vec<&str> = formatted.lines().collect();
        let indent = |line: &str| line.len().saturating_sub(line.trim_start().len());

        let and_open = lines
            .iter()
            .position(|line| line.trim_start().starts_with("AND (dept_id = 10"))
            .expect("should contain AND (dept_id = 10");
        let or_inner = lines
            .iter()
            .position(|line| line.trim_start().starts_with("OR (dept_id = 20"))
            .expect("should contain OR (dept_id = 20");
        let or_flat = lines
            .iter()
            .position(|line| line.trim_start().starts_with("OR dept_id = 30"))
            .expect("should contain OR dept_id = 30");
        let and_final = lines
            .iter()
            .position(|line| line.trim_start().starts_with("AND active"))
            .expect("should contain AND active");

        assert_eq!(
            indent(lines[or_inner]),
            indent(lines[or_flat]),
            "OR inside same paren group should align, got:\n{}",
            formatted
        );
        assert!(
            indent(lines[or_inner]) > indent(lines[and_open]),
            "OR inside AND's paren should be deeper than AND, got:\n{}",
            formatted
        );
        assert_eq!(
            indent(lines[and_final]),
            indent(lines[and_open]),
            "AND after closed paren should return to same depth as opening AND, got:\n{}",
            formatted
        );

        let reformatted = SqlEditorWidget::format_sql_basic(&formatted);
        assert_eq!(
            reformatted, formatted,
            "formatting should be stable for mixed nested conditions"
        );
    }

    #[test]
    fn triple_nested_paren_conditions_keep_progressive_depth() {
        let input = r#"SELECT *
FROM emp
WHERE (
    a = 1
    AND (
        b = 2
        OR (
            c = 3
            AND d = 4
        )
    )
);"#;
        let formatted = SqlEditorWidget::format_sql_basic(input);
        let lines: Vec<&str> = formatted.lines().collect();
        let indent = |line: &str| line.len().saturating_sub(line.trim_start().len());

        let and_inner = lines
            .iter()
            .position(|line| line.trim_start().starts_with("AND (b"))
            .or_else(|| {
                lines
                    .iter()
                    .position(|line| line.trim_start().starts_with("AND ("))
            })
            .expect("should contain AND (b or AND (");
        let or_deepest = lines
            .iter()
            .position(|line| {
                line.trim_start().starts_with("OR (c") || line.trim_start().starts_with("OR (")
            })
            .expect("should contain OR (c or OR (");

        assert!(
            indent(lines[or_deepest]) > indent(lines[and_inner]),
            "deeper nested OR should have greater indent than AND, got:\n{}",
            formatted
        );

        let reformatted = SqlEditorWidget::format_sql_basic(&formatted);
        assert_eq!(
            reformatted, formatted,
            "formatting should be stable for triple nested conditions"
        );
    }

    #[test]
    fn general_paren_body_and_condition_lines_follow_structural_depth_only() {
        let input = r#"SELECT *
FROM emp
WHERE
(
    -- candidate row
            deptno = 10
                    AND status = 'A'
                    OR job = 'CLERK'
)
AND active = 1;"#;
        let formatted = SqlEditorWidget::format_sql_basic(input);
        let lines: Vec<&str> = formatted.lines().collect();
        let indent = |line: &str| line.len().saturating_sub(line.trim_start().len());

        let where_idx = lines
            .iter()
            .position(|line| line.trim_start().starts_with("WHERE"))
            .expect("should contain WHERE line");
        let comment_idx = lines
            .iter()
            .position(|line| line.trim_start() == "-- candidate row")
            .expect("should contain candidate comment");
        let deptno_idx = lines
            .iter()
            .position(|line| line.trim_start().starts_with("deptno = 10"))
            .expect("should contain deptno = 10");
        let and_idx = lines
            .iter()
            .position(|line| line.trim_start().starts_with("AND status = 'A'"))
            .expect("should contain AND status = 'A'");
        let or_idx = lines
            .iter()
            .position(|line| line.trim_start().starts_with("OR job = 'CLERK'"))
            .expect("should contain OR job = 'CLERK'");
        let outer_and_idx = lines
            .iter()
            .position(|line| line.trim_start().starts_with("AND active = 1;"))
            .expect("should contain AND active = 1;");

        assert_eq!(
            indent(lines[comment_idx]),
            indent(lines[where_idx]).saturating_add(4),
            "comment inside the parenthesized body should stay one level deeper than the owner line, got:\n{}",
            formatted
        );
        assert_eq!(
            indent(lines[deptno_idx]),
            indent(lines[comment_idx]),
            "plain body line inside the same general paren depth should align with the body comment depth, got:\n{}",
            formatted
        );
        assert_eq!(
            indent(lines[and_idx]),
            indent(lines[deptno_idx]).saturating_add(4),
            "AND inside one active general paren should be exactly one more level than the body line, got:\n{}",
            formatted
        );
        assert_eq!(
            indent(lines[or_idx]),
            indent(lines[and_idx]),
            "condition keywords inside the same general paren depth should align, got:\n{}",
            formatted
        );
        assert_eq!(
            indent(lines[outer_and_idx]),
            indent(lines[where_idx]).saturating_add(4),
            "AND after the closed general paren group should return to the condition continuation depth, got:\n{}",
            formatted
        );

        let reformatted = SqlEditorWidget::format_sql_basic(&formatted);
        assert_eq!(
            reformatted, formatted,
            "formatting should stay stable after normalizing general paren body depth"
        );
    }

    #[test]
    fn join_on_nested_paren_and_or_keeps_correct_depths() {
        let input = r#"SELECT *
FROM emp e
JOIN dept d
ON (e.dept_id = d.dept_id
    AND e.loc_id = d.loc_id)
OR (e.alt_dept_id = d.dept_id
    AND e.alt_loc_id = d.loc_id)
JOIN region r
ON r.region_id = d.region_id;"#;
        let formatted = SqlEditorWidget::format_sql_basic(input);
        let lines: Vec<&str> = formatted.lines().collect();
        let indent = |line: &str| line.len().saturating_sub(line.trim_start().len());

        let on_idx = lines
            .iter()
            .position(|line| line.trim_start().starts_with("ON (e.dept_id"))
            .expect("should contain ON (e.dept_id");
        let or_idx = lines
            .iter()
            .position(|line| line.trim_start().starts_with("OR (e.alt_dept_id"))
            .expect("should contain OR (e.alt_dept_id");
        let join_region = lines
            .iter()
            .position(|line| line.trim_start().starts_with("JOIN region"))
            .expect("should contain JOIN region");

        assert_eq!(
            indent(lines[or_idx]),
            indent(lines[on_idx]).saturating_add(4),
            "OR after closed paren group in JOIN ON should return to the JOIN condition continuation depth, got:\n{}",
            formatted
        );
        assert!(
            indent(lines[join_region]) < indent(lines[on_idx]),
            "next JOIN should be at base depth, less than ON, got:\n{}",
            formatted
        );

        let reformatted = SqlEditorWidget::format_sql_basic(&formatted);
        assert_eq!(
            reformatted, formatted,
            "formatting should be stable for JOIN ON with nested paren conditions"
        );
    }

    #[test]
    fn join_on_double_wrapper_parens_get_progressive_depth() {
        let input = r#"SELECT *
FROM a
JOIN b
ON ((1 = 1
    AND 2 = 2))
AND 3 = 3;"#;
        let formatted = SqlEditorWidget::format_sql_basic(input);
        let lines: Vec<&str> = formatted.lines().collect();
        let indent = |line: &str| line.len().saturating_sub(line.trim_start().len());

        let on_idx = lines
            .iter()
            .position(|line| line.trim_start().starts_with("ON ((1 = 1"))
            .expect("should contain ON ((1 = 1");
        let and_inner = lines
            .iter()
            .position(|line| line.trim_start().starts_with("AND 2 = 2"))
            .expect("should contain AND 2 = 2");
        let and_outer = lines
            .iter()
            .position(|line| line.trim_start().starts_with("AND 3 = 3"))
            .expect("should contain AND 3 = 3");

        assert_eq!(
            indent(lines[and_inner]),
            indent(lines[on_idx]).saturating_add(12),
            "AND inside ON (( should add one continuation level plus one level per open paren, got:\n{}",
            formatted
        );
        assert_eq!(
            indent(lines[and_outer]),
            indent(lines[on_idx]).saturating_add(4),
            "AND after the closed ON paren group should return to the ON continuation depth, got:\n{}",
            formatted
        );

        let reformatted = SqlEditorWidget::format_sql_basic(&formatted);
        assert_eq!(
            reformatted, formatted,
            "formatting should be stable for JOIN ON with repeated wrapper parens"
        );
    }

    #[test]
    fn join_on_inner_close_then_and_keeps_outer_open_paren_depth() {
        let input = r#"SELECT *
FROM a
JOIN b
ON (
    (
        a.id = b.id
    )
    AND a.flag = b.flag
);"#;
        let formatted = SqlEditorWidget::format_sql_basic(input);
        let lines: Vec<&str> = formatted.lines().collect();
        let indent = |line: &str| line.len().saturating_sub(line.trim_start().len());

        let on_idx = lines
            .iter()
            .position(|line| line.trim_start().starts_with("ON"))
            .expect("should contain ON");
        let and_idx = lines
            .iter()
            .position(|line| line.trim_start().starts_with("AND a.flag"))
            .expect("should contain AND a.flag");
        let prev_idx = and_idx
            .checked_sub(1)
            .expect("AND line should have a previous line");

        assert_eq!(
            indent(lines[and_idx]),
            indent(lines[on_idx]).saturating_add(8),
            "AND after an inner close should keep the remaining outer ON paren depth, got:\n{}",
            formatted
        );
        assert!(
            lines[prev_idx].contains(')'),
            "the line before AND should still contain the inner close paren, got:\n{}",
            formatted
        );
        assert!(
            indent(lines[and_idx]) >= indent(lines[prev_idx]),
            "AND should remain at the outer ON paren depth after the inner close, got:\n{}",
            formatted
        );
    }

    #[test]
    fn where_inner_close_then_and_keeps_outer_open_paren_depth() {
        let input = r#"SELECT *
FROM emp
WHERE (
    (
        deptno = 10
    )
    AND status = 'A'
);"#;
        let formatted = SqlEditorWidget::format_sql_basic(input);
        let lines: Vec<&str> = formatted.lines().collect();
        let indent = |line: &str| line.len().saturating_sub(line.trim_start().len());

        let where_idx = lines
            .iter()
            .position(|line| line.trim_start().starts_with("WHERE"))
            .expect("should contain WHERE");
        let and_idx = lines
            .iter()
            .position(|line| line.trim_start().starts_with("AND status = 'A'"))
            .expect("should contain AND status = 'A'");

        assert_eq!(
            indent(lines[and_idx]),
            indent(lines[where_idx]).saturating_add(8),
            "AND after an inner close should keep the remaining outer WHERE paren depth, got:\n{}",
            formatted
        );
    }

    #[test]
    fn split_where_standalone_open_paren_keeps_body_one_level_deeper_than_wrapper() {
        let input = r#"SELECT *
FROM emp
WHERE
(
deptno = 10
AND status = 'A'
)
AND active = 1;"#;
        let formatted = SqlEditorWidget::format_sql_basic(input);
        let lines: Vec<&str> = formatted.lines().collect();
        let indent = |line: &str| line.len().saturating_sub(line.trim_start().len());

        let where_idx = lines
            .iter()
            .position(|line| line.trim_start().starts_with("WHERE"))
            .expect("should contain WHERE");
        let and_inner_idx = lines
            .iter()
            .enumerate()
            .skip(where_idx + 1)
            .find(|(_, line)| line.trim_start().starts_with("AND status = 'A'"))
            .map(|(idx, _)| idx)
            .expect("should contain inner AND");
        let and_outer_idx = lines
            .iter()
            .enumerate()
            .skip(and_inner_idx + 1)
            .find(|(_, line)| line.trim_start().starts_with("AND active = 1;"))
            .map(|(idx, _)| idx)
            .expect("should contain outer AND");

        assert!(
            indent(lines[and_inner_idx]) > indent(lines[where_idx]),
            "AND inside the parenthesized WHERE group should be deeper than the WHERE owner line, got:\n{}",
            formatted
        );
        assert_eq!(
            indent(lines[and_outer_idx]),
            indent(lines[where_idx]).saturating_add(4),
            "AND after the split WHERE paren group should return to the condition continuation depth, got:\n{}",
            formatted
        );
    }

    #[test]
    fn split_join_on_standalone_open_paren_keeps_body_one_level_deeper_than_wrapper() {
        let input = r#"SELECT *
FROM emp e
JOIN dept d
ON
(
e.deptno = d.deptno
AND e.status = d.status
)
AND e.active = d.active;"#;
        let formatted = SqlEditorWidget::format_sql_basic(input);
        let lines: Vec<&str> = formatted.lines().collect();
        let indent = |line: &str| line.len().saturating_sub(line.trim_start().len());

        let on_idx = lines
            .iter()
            .position(|line| line.trim_start().starts_with("ON"))
            .expect("should contain ON");
        let and_inner_idx = lines
            .iter()
            .enumerate()
            .skip(on_idx + 1)
            .find(|(_, line)| line.trim_start().starts_with("AND e.status = d.status"))
            .map(|(idx, _)| idx)
            .expect("should contain inner AND");
        let and_outer_idx = lines
            .iter()
            .enumerate()
            .skip(and_inner_idx + 1)
            .find(|(_, line)| line.trim_start().starts_with("AND e.active = d.active;"))
            .map(|(idx, _)| idx)
            .expect("should contain outer AND");

        assert!(
            indent(lines[and_inner_idx]) > indent(lines[on_idx]),
            "AND inside the parenthesized JOIN ON group should be deeper than the ON owner line, got:\n{}",
            formatted
        );
        assert_eq!(
            indent(lines[and_outer_idx]),
            indent(lines[on_idx]).saturating_add(4),
            "AND after the split JOIN ON paren group should return to the ON continuation depth, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_sql_basic_keeps_join_on_nested_paren_groups_deeper_than_on_with_inline_comments() {
        let source = r#"SELECT *
FROM emp e
JOIN dept d
ON (e.dept_id = d.dept_id -- branch one
AND e.loc_id = d.loc_id) -- close one
OR (e.alt_dept_id = d.dept_id -- branch two
AND e.alt_loc_id = d.loc_id) -- close two
JOIN region r
ON r.region_id = d.region_id;"#;

        let formatted = SqlEditorWidget::format_sql_basic(source);
        let lines: Vec<&str> = formatted.lines().collect();

        let on_idx =
            find_line_starting_with(&lines, "ON (e.dept_id = d.dept_id").expect("first ON line");
        let and_first_idx =
            find_line_starting_with(&lines, "AND e.loc_id = d.loc_id)").expect("first AND line");
        let or_idx = find_line_starting_with(&lines, "OR (e.alt_dept_id = d.dept_id")
            .expect("OR branch line");
        let and_second_idx = find_line_starting_with(&lines, "AND e.alt_loc_id = d.loc_id)")
            .expect("second AND line");
        let join_region_idx =
            find_line_starting_with(&lines, "JOIN region r").expect("next JOIN line");

        assert_eq!(
            leading_spaces(lines[and_first_idx]),
            leading_spaces(lines[on_idx]).saturating_add(8),
            "full formatter should keep AND inside JOIN ON paren groups two levels deeper than ON, got:\n{}",
            formatted
        );
        assert_eq!(
            leading_spaces(lines[or_idx]),
            leading_spaces(lines[on_idx]).saturating_add(4),
            "full formatter should return OR to the ON continuation depth after the closed paren, got:\n{}",
            formatted
        );
        assert_eq!(
            leading_spaces(lines[and_second_idx]),
            leading_spaces(lines[on_idx]).saturating_add(8),
            "full formatter should keep the second nested AND aligned with the first nested AND, got:\n{}",
            formatted
        );
        assert_eq!(
            leading_spaces(lines[join_region_idx]).saturating_add(4),
            leading_spaces(lines[on_idx]),
            "full formatter should return the next JOIN to the query base depth, got:\n{}",
            formatted
        );
    }

    #[test]
    fn having_nested_paren_and_or_keeps_correct_depths() {
        let input = r#"SELECT dept_id, COUNT(*)
FROM emp
GROUP BY dept_id
HAVING (COUNT(*) > 5
    AND SUM(salary) > 100000)
OR (COUNT(*) > 10
    AND AVG(salary) > 50000);"#;
        let formatted = SqlEditorWidget::format_sql_basic(input);
        let lines: Vec<&str> = formatted.lines().collect();
        let indent = |line: &str| line.len().saturating_sub(line.trim_start().len());

        let having_idx = lines
            .iter()
            .position(|line| line.trim_start().starts_with("HAVING"))
            .expect("should contain HAVING");
        let and_first = lines
            .iter()
            .position(|line| line.trim_start().starts_with("AND SUM"))
            .expect("should contain AND SUM");
        let or_idx = lines
            .iter()
            .position(|line| line.trim_start().starts_with("OR (COUNT"))
            .expect("should contain OR (COUNT");

        assert!(
            indent(lines[and_first]) > indent(lines[having_idx]),
            "AND inside HAVING paren should be deeper than HAVING, got:\n{}",
            formatted
        );
        assert!(
            indent(lines[and_first]) > indent(lines[or_idx]),
            "AND inside paren should be deeper than OR outside paren, got:\n{}",
            formatted
        );
        assert!(
            indent(lines[or_idx]) > indent(lines[having_idx]),
            "OR at condition level should be deeper than HAVING, got:\n{}",
            formatted
        );

        let reformatted = SqlEditorWidget::format_sql_basic(&formatted);
        assert_eq!(
            reformatted, formatted,
            "formatting should be stable for HAVING with nested paren conditions"
        );
    }

    #[test]
    fn case_when_nested_paren_and_or_keeps_correct_depths() {
        let input = r#"SELECT
    CASE
        WHEN (status = 'A'
            AND (dept_id = 10
                OR dept_id = 20))
            OR priority = 'HIGH' THEN 'YES'
        ELSE 'NO'
    END AS result
FROM emp;"#;
        let formatted = SqlEditorWidget::format_sql_basic(input);
        let lines: Vec<&str> = formatted.lines().collect();
        let indent = |line: &str| line.len().saturating_sub(line.trim_start().len());

        let when_idx = lines
            .iter()
            .position(|line| line.trim_start().starts_with("WHEN (status"))
            .expect("should contain WHEN (status");
        let and_nested = lines
            .iter()
            .position(|line| line.trim_start().starts_with("AND (dept_id = 10"))
            .expect("should contain AND (dept_id = 10");
        let or_outer = lines
            .iter()
            .position(|line| line.trim_start().starts_with("OR priority"))
            .expect("should contain OR priority");

        assert!(
            indent(lines[and_nested]) > indent(lines[when_idx]),
            "AND inside WHEN paren should be deeper than WHEN, got:\n{}",
            formatted
        );
        assert!(
            indent(lines[or_outer]) <= indent(lines[and_nested]),
            "OR after closed nested parens should be at or shallower than AND inside paren, got:\n{}",
            formatted
        );

        let reformatted = SqlEditorWidget::format_sql_basic(&formatted);
        assert_eq!(
            reformatted, formatted,
            "formatting should be stable for CASE WHEN with nested paren conditions"
        );
    }

    #[test]
    fn exists_subquery_nested_paren_and_or_keeps_correct_depths() {
        let input = r#"SELECT *
FROM dept d
WHERE EXISTS (
    SELECT 1
    FROM emp e
    WHERE e.dept_id = d.dept_id
    AND (e.status = 'A'
        OR (e.status = 'B'
            AND e.hire_date > DATE '2020-01-01'))
)
AND d.active = 1;"#;
        let formatted = SqlEditorWidget::format_sql_basic(input);
        let lines: Vec<&str> = formatted.lines().collect();
        let indent = |line: &str| line.len().saturating_sub(line.trim_start().len());

        let outer_where = lines
            .iter()
            .position(|line| line.trim_start().starts_with("WHERE EXISTS"))
            .expect("should contain WHERE EXISTS");
        let and_outer = lines
            .iter()
            .position(|line| line.trim_start().starts_with("AND d.active"))
            .expect("should contain AND d.active");
        let outer_condition_base = indent(lines[outer_where]) + 4;

        assert_eq!(
            indent(lines[and_outer]),
            outer_condition_base,
            "AND after EXISTS subquery should return to outer condition depth, got:\n{}",
            formatted
        );

        let inner_and = lines
            .iter()
            .position(|line| line.trim_start().starts_with("AND (e.status"))
            .expect("should contain AND (e.status");
        let inner_or = lines
            .iter()
            .position(|line| line.trim_start().starts_with("OR (e.status"))
            .expect("should contain OR (e.status");

        assert!(
            indent(lines[inner_or]) > indent(lines[inner_and]),
            "OR inside AND's paren should be deeper than AND in subquery, got:\n{}",
            formatted
        );

        let reformatted = SqlEditorWidget::format_sql_basic(&formatted);
        assert_eq!(
            reformatted, formatted,
            "formatting should be stable for EXISTS subquery with nested paren conditions"
        );
    }

    #[test]
    fn merge_on_nested_paren_and_or_keeps_correct_depths() {
        let input = r#"MERGE INTO target t
USING source s
ON (t.id = s.id
    AND (t.type = s.type
        OR t.alt_type = s.type))
WHEN MATCHED THEN
    UPDATE SET t.val = s.val
WHEN NOT MATCHED THEN
    INSERT (id, val) VALUES (s.id, s.val);"#;
        let formatted = SqlEditorWidget::format_sql_basic(input);
        let lines: Vec<&str> = formatted.lines().collect();
        let indent = |line: &str| line.len().saturating_sub(line.trim_start().len());

        let on_idx = lines
            .iter()
            .position(|line| line.trim_start().starts_with("ON (t.id"))
            .expect("should contain ON (t.id");
        let and_idx = lines
            .iter()
            .position(|line| line.trim_start().starts_with("AND (t.TYPE"))
            .or_else(|| {
                lines
                    .iter()
                    .position(|line| line.trim_start().starts_with("AND (t.type"))
            })
            .expect("should contain AND (t.type");
        let or_idx = lines
            .iter()
            .position(|line| line.trim_start().starts_with("OR t.alt_type"))
            .expect("should contain OR t.alt_type");
        let when_matched = lines
            .iter()
            .position(|line| line.trim_start().starts_with("WHEN MATCHED"))
            .expect("should contain WHEN MATCHED");

        assert!(
            indent(lines[and_idx]) > indent(lines[on_idx]),
            "AND inside MERGE ON paren should be deeper than ON, got:\n{}",
            formatted
        );
        assert!(
            indent(lines[or_idx]) > indent(lines[and_idx]),
            "OR inside nested paren should be deeper than AND, got:\n{}",
            formatted
        );
        assert!(
            indent(lines[when_matched]) <= indent(lines[on_idx]),
            "WHEN MATCHED should be at base depth after ON clause, got:\n{}",
            formatted
        );

        let reformatted = SqlEditorWidget::format_sql_basic(&formatted);
        assert_eq!(
            reformatted, formatted,
            "formatting should be stable for MERGE ON with nested paren conditions"
        );
    }

    #[test]
    fn plsql_if_nested_paren_and_or_keeps_correct_depths() {
        let input = r#"BEGIN
    IF (v_status = 'A'
        AND (v_dept = 10
            OR v_dept = 20))
        OR v_override = 'Y' THEN
        NULL;
    END IF;
END;"#;
        let formatted = SqlEditorWidget::format_sql_basic(input);
        let lines: Vec<&str> = formatted.lines().collect();
        let indent = |line: &str| line.len().saturating_sub(line.trim_start().len());

        let if_idx = lines
            .iter()
            .position(|line| line.trim_start().starts_with("IF (v_status"))
            .expect("should contain IF (v_status");
        let and_nested = lines
            .iter()
            .position(|line| line.trim_start().starts_with("AND (v_dept = 10"))
            .expect("should contain AND (v_dept = 10");
        let or_outer = lines
            .iter()
            .position(|line| line.trim_start().starts_with("OR v_override"))
            .expect("should contain OR v_override");
        let end_if = lines
            .iter()
            .position(|line| line.trim_start() == "END IF;")
            .expect("should contain END IF");

        assert!(
            indent(lines[and_nested]) > indent(lines[if_idx]),
            "AND inside IF paren should be deeper than IF, got:\n{}",
            formatted
        );
        assert_eq!(
            indent(lines[or_outer]),
            indent(lines[and_nested]),
            "OR after closed nested parens should be at condition continuation depth, got:\n{}",
            formatted
        );
        assert_eq!(
            indent(lines[end_if]),
            indent(lines[if_idx]),
            "END IF should align with IF, got:\n{}",
            formatted
        );

        let reformatted = SqlEditorWidget::format_sql_basic(&formatted);
        assert_eq!(
            reformatted, formatted,
            "formatting should be stable for PL/SQL IF with nested paren conditions"
        );
    }

    #[test]
    fn plsql_if_same_line_close_and_condition_keyword_keeps_condition_depth() {
        let input = r#"BEGIN
    IF (
        v_ready = 'Y'
    ) AND v_dept = 10 THEN
        NULL;
    END IF;
END;"#;
        let formatted = SqlEditorWidget::format_sql_basic(input);
        let lines: Vec<&str> = formatted.lines().collect();
        let indent = |line: &str| line.len().saturating_sub(line.trim_start().len());

        let if_idx = lines
            .iter()
            .position(|line| line.trim_start().starts_with("IF ("))
            .expect("should contain IF line");
        let close_and_idx = lines
            .iter()
            .position(|line| line.trim_start() == "AND v_dept = 10 THEN")
            .expect("should contain IF continuation line");
        let null_idx = lines
            .iter()
            .position(|line| line.trim_start() == "NULL;")
            .expect("should contain THEN body line");
        let end_if_idx = lines
            .iter()
            .position(|line| line.trim_start() == "END IF;")
            .expect("should contain END IF line");

        assert_eq!(
            indent(lines[close_and_idx]),
            indent(lines[if_idx]).saturating_add(4),
            "IF condition continuation should stay one level deeper than the IF header after consuming the close"
        );
        assert_eq!(
            indent(lines[null_idx]),
            indent(lines[close_and_idx]).saturating_add(4),
            "THEN body should open one level deeper than the split continuation line"
        );
        assert_eq!(
            indent(lines[end_if_idx]),
            indent(lines[if_idx]),
            "END IF should align with IF after same-line close + continuation"
        );

        let reformatted = SqlEditorWidget::format_sql_basic(&formatted);
        assert_eq!(
            reformatted, formatted,
            "formatting should stay stable for same-line IF close + condition continuation"
        );
    }

    #[test]
    fn plsql_if_multiline_comment_close_and_condition_keyword_keeps_condition_depth() {
        let input = r#"BEGIN
    IF (
        v_ready = 'Y'
        /* close condition
        */ ) AND v_dept = 10 THEN
        NULL;
    END IF;
END;"#;
        let formatted = SqlEditorWidget::format_sql_basic(input);
        let lines: Vec<&str> = formatted.lines().collect();
        let indent = |line: &str| line.len().saturating_sub(line.trim_start().len());

        let if_idx = lines
            .iter()
            .position(|line| line.trim_start().starts_with("IF ("))
            .expect("should contain IF line");
        let close_and_idx = lines
            .iter()
            .position(|line| line.trim_start() == "AND v_dept = 10 THEN")
            .expect("should contain IF continuation line");
        let null_idx = lines
            .iter()
            .position(|line| line.trim_start() == "NULL;")
            .expect("should contain THEN body line");
        let end_if_idx = lines
            .iter()
            .position(|line| line.trim_start() == "END IF;")
            .expect("should contain END IF line");

        assert_eq!(
            indent(lines[close_and_idx]),
            indent(lines[if_idx]).saturating_add(4),
            "IF continuation after a multiline block-comment tail should stay one level deeper than IF, got:\n{}",
            formatted
        );
        assert_eq!(
            indent(lines[null_idx]),
            indent(lines[close_and_idx]).saturating_add(4),
            "THEN body should remain one level deeper than the split continuation line, got:\n{}",
            formatted
        );
        assert_eq!(
            indent(lines[end_if_idx]),
            indent(lines[if_idx]),
            "END IF should align with IF after multiline-comment close + continuation, got:\n{}",
            formatted
        );

        let reformatted = SqlEditorWidget::format_sql_basic(&formatted);
        assert_eq!(
            reformatted, formatted,
            "formatting should stay stable for multiline-comment close + condition continuation"
        );
    }

    #[test]
    fn plsql_if_multiline_qquote_tail_close_and_condition_keyword_keeps_condition_depth() {
        let input = r#"BEGIN
    IF (
        (
            q'[
ready
]' ) AND v_dept = 10
    ) THEN
        NULL;
    END IF;
END;"#;
        let formatted = SqlEditorWidget::format_sql_basic(input);
        let lines: Vec<&str> = formatted.lines().collect();
        let indent = |line: &str| line.len().saturating_sub(line.trim_start().len());

        let if_idx = lines
            .iter()
            .position(|line| line.trim_start().starts_with("IF ("))
            .expect("should contain IF line");
        let close_and_idx = lines
            .iter()
            .position(|line| line.trim_start().contains("AND v_dept = 10"))
            .unwrap_or_else(|| {
                panic!("should contain IF continuation line, got:\n{formatted}");
            });
        let null_idx = lines
            .iter()
            .position(|line| line.trim_start() == "NULL;")
            .expect("should contain THEN body line");
        let end_if_idx = lines
            .iter()
            .position(|line| line.trim_start() == "END IF;")
            .expect("should contain END IF line");

        assert_eq!(
            indent(lines[close_and_idx]),
            indent(lines[if_idx]).saturating_add(4),
            "IF continuation after multiline q-quote tail close should stay one level deeper than IF, got:\n{}",
            formatted
        );
        assert_eq!(
            indent(lines[null_idx]),
            indent(lines[close_and_idx]).saturating_add(4),
            "THEN body should remain one level deeper than the split continuation line, got:\n{}",
            formatted
        );
        assert_eq!(
            indent(lines[end_if_idx]),
            indent(lines[if_idx]),
            "END IF should align with IF after multiline q-quote tail close + continuation, got:\n{}",
            formatted
        );

        let reformatted = SqlEditorWidget::format_sql_basic(&formatted);
        assert_eq!(
            reformatted, formatted,
            "formatting should stay stable for multiline q-quote tail close + condition continuation"
        );
    }

    #[test]
    fn format_sql_basic_identifier_suffix_end_before_close_paren_stays_inline() {
        let input = r#"SELECT fn_legend(LEGEND) AS value_col
FROM dual;"#;

        let formatted = SqlEditorWidget::format_sql_basic(input);
        let lines: Vec<&str> = formatted.lines().collect();
        let legend_line_idx = lines
            .iter()
            .position(|line| line.contains("LEGEND"))
            .unwrap_or_else(|| {
                panic!("formatted output should contain LEGEND operand, got:\n{formatted}")
            });
        let legend_line = lines[legend_line_idx];
        let next_trimmed = lines
            .get(legend_line_idx.saturating_add(1))
            .map(|line| line.trim());

        assert!(
            legend_line.contains(')'),
            "identifier ending with `END` must keep close paren on the same line, got:\n{formatted}"
        );
        assert_ne!(
            next_trimmed,
            Some(")"),
            "identifier ending with `END` must not trigger case-close newline logic, got:\n{formatted}"
        );

        let reformatted = SqlEditorWidget::format_sql_basic(&formatted);
        assert_eq!(
            reformatted, formatted,
            "identifier suffix `END` close-paren formatting should remain idempotent"
        );
    }

    #[test]
    fn where_in_subquery_nested_paren_and_or_keeps_correct_depths() {
        let input = r#"SELECT *
FROM emp
WHERE dept_id IN (
    SELECT dept_id
    FROM dept
    WHERE (region = 'EAST'
        AND active = 1)
    OR (region = 'WEST'
        AND priority = 'HIGH')
)
AND status = 'A';"#;
        let formatted = SqlEditorWidget::format_sql_basic(input);
        let lines: Vec<&str> = formatted.lines().collect();
        let indent = |line: &str| line.len().saturating_sub(line.trim_start().len());

        let outer_where = lines
            .iter()
            .position(|line| line.trim_start().starts_with("WHERE dept_id IN"))
            .expect("should contain WHERE dept_id IN");
        let and_final = lines
            .iter()
            .position(|line| line.trim_start().starts_with("AND status"))
            .expect("should contain AND status");
        let outer_condition_base = indent(lines[outer_where]) + 4;

        assert_eq!(
            indent(lines[and_final]),
            outer_condition_base,
            "AND after IN subquery should return to outer condition depth, got:\n{}",
            formatted
        );

        let inner_or = lines
            .iter()
            .position(|line| {
                line.trim_start().starts_with("OR (region = 'WEST'")
                    || line.trim_start().starts_with("OR (region")
            })
            .expect("should contain OR in inner query");
        let inner_and_first = lines
            .iter()
            .position(|line| line.trim_start().starts_with("AND active"))
            .expect("should contain AND active");

        assert!(
            indent(lines[inner_and_first]) > indent(lines[inner_or]),
            "AND inside paren should be deeper than OR outside paren in subquery, got:\n{}",
            formatted
        );

        let reformatted = SqlEditorWidget::format_sql_basic(&formatted);
        assert_eq!(
            reformatted, formatted,
            "formatting should be stable for WHERE IN subquery with nested paren conditions"
        );
    }

    #[test]
    fn comments_inside_nested_paren_and_or_preserve_depth() {
        let input = r#"SELECT *
FROM emp
WHERE (
    dept_id = 10
    -- check status
    AND status = 'A'
)
OR (
    dept_id = 20
    /* alt condition */
    AND status = 'B'
);"#;
        let formatted = SqlEditorWidget::format_sql_basic(input);
        let lines: Vec<&str> = formatted.lines().collect();
        let indent = |line: &str| line.len().saturating_sub(line.trim_start().len());

        let and_first = lines
            .iter()
            .position(|line| line.trim_start().starts_with("AND status = 'A'"))
            .expect("should contain AND status = 'A'");
        let or_idx = lines
            .iter()
            .position(|line| line.trim_start().starts_with("OR (dept_id = 20"))
            .expect("should contain OR (dept_id = 20");
        let comment_line = lines
            .iter()
            .position(|line| line.trim_start().starts_with("-- check status"))
            .expect("should contain -- check status");

        assert_eq!(
            indent(lines[comment_line]),
            indent(lines[and_first]),
            "line comment should align with following AND inside paren, got:\n{}",
            formatted
        );
        assert!(
            indent(lines[and_first]) > indent(lines[or_idx]),
            "AND inside paren should be deeper than OR outside paren, got:\n{}",
            formatted
        );

        let reformatted = SqlEditorWidget::format_sql_basic(&formatted);
        assert_eq!(
            reformatted, formatted,
            "formatting should be stable for comments inside nested paren conditions"
        );
    }

    #[test]
    fn double_consecutive_open_parens_get_progressive_depth() {
        let input = "SELECT * FROM emp WHERE ((col1 = 1 AND col2 = 2) AND col3 = 3);";
        let formatted = SqlEditorWidget::format_sql_basic(input);
        let lines: Vec<&str> = formatted.lines().collect();
        let indent = |line: &str| line.len().saturating_sub(line.trim_start().len());

        let and_inner = lines
            .iter()
            .position(|line| line.trim_start().starts_with("AND col2"))
            .expect("should contain AND col2");
        let and_outer = lines
            .iter()
            .position(|line| line.trim_start().starts_with("AND col3"))
            .expect("should contain AND col3");

        assert_eq!(
            indent(lines[and_inner]),
            12,
            "AND inside (( should be at depth 3 (12 spaces): WHERE base(1) + 2 parens, got:\n{}",
            formatted
        );
        assert_eq!(
            indent(lines[and_outer]),
            8,
            "AND inside ( should be at depth 2 (8 spaces): WHERE base(1) + 1 paren, got:\n{}",
            formatted
        );

        let reformatted = SqlEditorWidget::format_sql_basic(&formatted);
        assert_eq!(
            reformatted, formatted,
            "formatting should be stable for (( double parens"
        );
    }

    #[test]
    fn triple_consecutive_open_parens_get_progressive_depth() {
        let input =
            "SELECT * FROM emp WHERE (((col1 = 1 AND col2 = 2) AND col3 = 3) AND col4 = 4);";
        let formatted = SqlEditorWidget::format_sql_basic(input);
        let lines: Vec<&str> = formatted.lines().collect();
        let indent = |line: &str| line.len().saturating_sub(line.trim_start().len());

        let and_innermost = lines
            .iter()
            .position(|line| line.trim_start().starts_with("AND col2"))
            .expect("should contain AND col2");
        let and_middle = lines
            .iter()
            .position(|line| line.trim_start().starts_with("AND col3"))
            .expect("should contain AND col3");
        let and_outer = lines
            .iter()
            .position(|line| line.trim_start().starts_with("AND col4"))
            .expect("should contain AND col4");

        assert_eq!(
            indent(lines[and_innermost]),
            16,
            "AND inside ((( should be at depth 4 (16 spaces): WHERE base(1) + 3 parens, got:\n{}",
            formatted
        );
        assert_eq!(
            indent(lines[and_middle]),
            12,
            "AND inside (( should be at depth 3 (12 spaces): WHERE base(1) + 2 parens, got:\n{}",
            formatted
        );
        assert_eq!(
            indent(lines[and_outer]),
            8,
            "AND inside ( should be at depth 2 (8 spaces): WHERE base(1) + 1 paren, got:\n{}",
            formatted
        );

        let reformatted = SqlEditorWidget::format_sql_basic(&formatted);
        assert_eq!(
            reformatted, formatted,
            "formatting should be stable for ((( triple parens"
        );
    }

    #[test]
    fn triple_consecutive_parens_with_or_get_progressive_depth() {
        let input = r#"SELECT *
FROM emp
WHERE (((status = 'A' OR status = 'B')
    AND dept_id = 10)
    OR region = 'WEST');"#;
        let formatted = SqlEditorWidget::format_sql_basic(input);
        let lines: Vec<&str> = formatted.lines().collect();
        let indent = |line: &str| line.len().saturating_sub(line.trim_start().len());

        let or_inner = lines
            .iter()
            .position(|line| line.trim_start().starts_with("OR status = 'B'"))
            .expect("should contain OR status = 'B'");
        let and_middle = lines
            .iter()
            .position(|line| line.trim_start().starts_with("AND dept_id"))
            .expect("should contain AND dept_id");
        let or_outer = lines
            .iter()
            .position(|line| line.trim_start().starts_with("OR region"))
            .expect("should contain OR region");

        assert_eq!(
            indent(lines[or_inner]),
            16,
            "OR inside ((( should be at depth 4 (16 spaces): WHERE base(1) + 3 parens, got:\n{}",
            formatted
        );
        assert_eq!(
            indent(lines[and_middle]),
            12,
            "AND inside (( should be at depth 3 (12 spaces): WHERE base(1) + 2 parens, got:\n{}",
            formatted
        );
        assert_eq!(
            indent(lines[or_outer]),
            8,
            "OR inside ( should be at depth 2 (8 spaces): WHERE base(1) + 1 paren, got:\n{}",
            formatted
        );

        let reformatted = SqlEditorWidget::format_sql_basic(&formatted);
        assert_eq!(
            reformatted, formatted,
            "formatting should be stable for ((( with OR at various depths"
        );
    }

    #[test]
    fn func_call_parens_do_not_get_progressive_depth() {
        // func(func2( should NOT get progressive depth — only consecutive ((( should
        let input = "SELECT TO_CHAR(NVL(col1, 0), 'FM9999') FROM emp;";
        let formatted = SqlEditorWidget::format_sql_basic(input);
        let reformatted = SqlEditorWidget::format_sql_basic(&formatted);
        assert_eq!(
            reformatted, formatted,
            "function call nesting should be stable:\n{}",
            formatted
        );
    }

    #[test]
    fn format_sql_basic_open_for_select_after_block_comment_in_package_body() {
        let sql = "create package body a as\nprocedure b (c in number) as\nbegin\n/* */\nopen cv for\nselect 1\nfrom dual;\nend b;\nend;";
        let formatted = SqlEditorWidget::format_sql_basic(sql);
        assert!(
            formatted.contains("OPEN cv FOR\n            SELECT 1\n            FROM DUAL;"),
            "SELECT after OPEN FOR should be indented exactly 1 level deeper than OPEN, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_sql_basic_open_for_select_after_block_comment_in_begin() {
        let sql = "begin\n/* */\nopen cv for\nselect 1\nfrom dual;\nend;";
        let formatted = SqlEditorWidget::format_sql_basic(sql);
        assert!(
            formatted.contains("OPEN cv FOR\n        SELECT 1\n        FROM DUAL;"),
            "SELECT after OPEN FOR should be indented exactly 1 level deeper than OPEN, got:\n{}",
            formatted
        );
    }

    // ---- JOIN ON indentation: AND/OR after ON should be one level deeper ----

    #[test]
    fn join_on_and_indented_deeper_than_on() {
        let input = "select * from a join b on 1 = 1 and 2 = 2;";
        let formatted = SqlEditorWidget::format_sql_basic(input);
        let lines: Vec<&str> = formatted.lines().collect();
        let indent = |line: &str| line.len().saturating_sub(line.trim_start().len());

        let on_idx = lines
            .iter()
            .position(|l| l.trim_start().starts_with("ON "))
            .expect("ON line");
        let and_idx = lines
            .iter()
            .position(|l| l.trim_start().starts_with("AND "))
            .expect("AND line");

        assert_eq!(
            indent(lines[and_idx]),
            indent(lines[on_idx]) + 4,
            "AND after ON should be indented one level deeper than ON, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_sql_basic_create_view_left_join_on_stays_one_frame_deeper_than_join() {
        let source = r#"CREATE OR REPLACE VIEW v_item_rollup AS
    SELECT wi.item_id,
        wi.project_id,
        wi.sprint_id,
        wi.assignee_user_id,
        wi.item_code,
        wi.status,
        wi.points,
        COUNT(we.event_id) AS event_count,
        ROUND(COALESCE(SUM(we.delta_hours), 0), 2) AS total_hours,
        MAX(we.event_at) AS last_event_at
    FROM work_item wi
    LEFT JOIN work_event we
    ON we.item_id = wi.item_id
    GROUP BY wi.item_id,
        wi.project_id,
        wi.sprint_id,
        wi.assignee_user_id,
        wi.item_code,
        wi.status,
        wi.points;"#;

        let formatted = SqlEditorWidget::format_sql_basic(source);
        let lines: Vec<&str> = formatted.lines().collect();
        let indent = |line: &str| line.len().saturating_sub(line.trim_start().len());

        let join_idx = find_line_starting_with(&lines, "LEFT JOIN work_event we")
            .expect("LEFT JOIN work_event we line");
        let on_idx =
            find_line_starting_with(&lines, "ON we.item_id = wi.item_id").expect("ON line");
        let group_by_idx =
            find_line_starting_with(&lines, "GROUP BY wi.item_id,").expect("GROUP BY line");

        assert_eq!(
            indent(lines[on_idx]),
            indent(lines[join_idx]).saturating_add(4),
            "ON line should stay one frame deeper than LEFT JOIN in CREATE VIEW query body, got:\n{formatted}"
        );
        assert_eq!(
            indent(lines[group_by_idx]),
            indent(lines[join_idx]),
            "GROUP BY should realign to the FROM/JOIN frame after ON condition closes, got:\n{formatted}"
        );
    }

    #[test]
    fn format_for_auto_formatting_create_view_left_join_on_stays_one_frame_deeper_than_join() {
        let source = r#"CREATE OR REPLACE VIEW v_item_rollup AS
    SELECT wi.item_id,
        wi.project_id,
        wi.sprint_id,
        wi.assignee_user_id,
        wi.item_code,
        wi.status,
        wi.points,
        COUNT(we.event_id) AS event_count,
        ROUND(COALESCE(SUM(we.delta_hours), 0), 2) AS total_hours,
        MAX(we.event_at) AS last_event_at
    FROM work_item wi
    LEFT JOIN work_event we
    ON we.item_id = wi.item_id
    GROUP BY wi.item_id,
        wi.project_id,
        wi.sprint_id,
        wi.assignee_user_id,
        wi.item_code,
        wi.status,
        wi.points;"#;

        let formatted =
            SqlEditorWidget::format_for_auto_formatting_with_db_type(source, false, None);
        let lines: Vec<&str> = formatted.lines().collect();
        let indent = |line: &str| line.len().saturating_sub(line.trim_start().len());

        let join_idx = find_line_starting_with(&lines, "LEFT JOIN work_event we")
            .expect("LEFT JOIN work_event we line");
        let on_idx =
            find_line_starting_with(&lines, "ON we.item_id = wi.item_id").expect("ON line");
        let group_by_idx =
            find_line_starting_with(&lines, "GROUP BY wi.item_id,").expect("GROUP BY line");

        assert_eq!(
            indent(lines[on_idx]),
            indent(lines[join_idx]).saturating_add(4),
            "auto-format ON line should stay one frame deeper than LEFT JOIN in CREATE VIEW query body, got:\n{formatted}"
        );
        assert_eq!(
            indent(lines[group_by_idx]),
            indent(lines[join_idx]),
            "auto-format GROUP BY should realign to the FROM/JOIN frame after ON condition closes, got:\n{formatted}"
        );
    }

    #[test]
    fn join_on_multiple_and_or_indented_deeper_than_on() {
        let input = "select * from a join b on a.id = b.id and a.x = b.x or a.y = b.y;";
        let formatted = SqlEditorWidget::format_sql_basic(input);
        let lines: Vec<&str> = formatted.lines().collect();
        let indent = |line: &str| line.len().saturating_sub(line.trim_start().len());

        let on_idx = lines
            .iter()
            .position(|l| l.trim_start().starts_with("ON "))
            .expect("ON line");
        let and_idx = lines
            .iter()
            .position(|l| l.trim_start().starts_with("AND "))
            .expect("AND line");
        let or_idx = lines
            .iter()
            .position(|l| l.trim_start().starts_with("OR "))
            .expect("OR line");

        let on_indent = indent(lines[on_idx]);
        assert_eq!(
            indent(lines[and_idx]),
            on_indent + 4,
            "AND after ON should be one level deeper, got:\n{}",
            formatted
        );
        assert_eq!(
            indent(lines[or_idx]),
            indent(lines[and_idx]),
            "OR should be at same depth as AND, got:\n{}",
            formatted
        );
    }

    #[test]
    fn join_on_condition_keywords_without_space_are_aligned_as_conditions() {
        let input = "select * from a join b on a.id = b.id and(a.flag = 'Y') or(b.flag = 'N');";
        let formatted = SqlEditorWidget::format_sql_basic(input);
        let lines: Vec<&str> = formatted.lines().collect();
        let indent = |line: &str| line.len().saturating_sub(line.trim_start().len());

        let on_idx = lines
            .iter()
            .position(|l| l.trim_start().starts_with("ON "))
            .expect("ON line");
        let and_idx = lines
            .iter()
            .position(|l| {
                crate::sql_text::starts_with_keyword_token(
                    &l.trim_start().to_ascii_uppercase(),
                    "AND",
                )
            })
            .expect("AND line");
        let or_idx = lines
            .iter()
            .position(|l| {
                crate::sql_text::starts_with_keyword_token(
                    &l.trim_start().to_ascii_uppercase(),
                    "OR",
                )
            })
            .expect("OR line");

        assert_eq!(
            indent(lines[and_idx]),
            indent(lines[on_idx]) + 4,
            "AND(...) should be treated as JOIN condition continuation, got:\n{}",
            formatted
        );
        assert_eq!(
            indent(lines[or_idx]),
            indent(lines[and_idx]),
            "OR(...) should align with sibling condition keyword, got:\n{}",
            formatted
        );
    }

    #[test]
    fn join_on_and_deeper_with_multiple_joins() {
        let input = r#"select *
from a
join b on a.id = b.id and a.x = b.x
join c on b.id = c.id and b.y = c.y;"#;
        let formatted = SqlEditorWidget::format_sql_basic(input);
        let lines: Vec<&str> = formatted.lines().collect();
        let indent = |line: &str| line.len().saturating_sub(line.trim_start().len());

        let on_lines: Vec<usize> = lines
            .iter()
            .enumerate()
            .filter(|(_, l)| l.trim_start().starts_with("ON "))
            .map(|(i, _)| i)
            .collect();
        let and_lines: Vec<usize> = lines
            .iter()
            .enumerate()
            .filter(|(_, l)| l.trim_start().starts_with("AND "))
            .map(|(i, _)| i)
            .collect();

        assert_eq!(
            on_lines.len(),
            2,
            "should have two ON lines, got:\n{}",
            formatted
        );
        assert_eq!(
            and_lines.len(),
            2,
            "should have two AND lines, got:\n{}",
            formatted
        );

        for (on_idx, and_idx) in on_lines.iter().zip(and_lines.iter()) {
            assert_eq!(
                indent(lines[*and_idx]),
                indent(lines[*on_idx]) + 4,
                "AND after ON should be one level deeper for each join, got:\n{}",
                formatted
            );
        }
    }

    #[test]
    fn join_on_and_deeper_in_subquery() {
        let input = r#"select * from (
select * from a
join b on a.id = b.id and a.x = b.x
) sub;"#;
        let formatted = SqlEditorWidget::format_sql_basic(input);
        let lines: Vec<&str> = formatted.lines().collect();
        let indent = |line: &str| line.len().saturating_sub(line.trim_start().len());

        let on_idx = lines
            .iter()
            .position(|l| l.trim_start().starts_with("ON "))
            .expect("ON line");
        let and_idx = lines
            .iter()
            .position(|l| l.trim_start().starts_with("AND "))
            .expect("AND line");

        assert_eq!(
            indent(lines[and_idx]),
            indent(lines[on_idx]) + 4,
            "AND after ON should be one level deeper even in subquery, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_sql_basic_keeps_incomplete_nested_join_on_one_level_deeper_than_join() {
        let input = r#"select 1
from (a
    join (
        select 1
        from a
        inner join a
    on 1 = 1"#;
        let formatted = SqlEditorWidget::format_sql_basic(input);
        let lines: Vec<&str> = formatted.lines().collect();
        let indent = |line: &str| line.len().saturating_sub(line.trim_start().len());

        let inner_join_idx = lines
            .iter()
            .position(|line| line.trim_start().eq_ignore_ascii_case("INNER JOIN a"))
            .expect("should contain INNER JOIN a");
        let on_idx = lines
            .iter()
            .position(|line| line.trim_start().starts_with("ON "))
            .expect("should contain ON");

        assert_eq!(
            indent(lines[on_idx]),
            indent(lines[inner_join_idx]).saturating_add(4),
            "Incomplete nested JOIN ON should stay one level deeper than INNER JOIN, got:\n{}",
            formatted
        );
    }

    #[test]
    fn join_on_and_deeper_is_idempotent() {
        let input = "select * from a join b on 1 = 1 and 2 = 2;";
        let formatted = SqlEditorWidget::format_sql_basic(input);
        let reformatted = SqlEditorWidget::format_sql_basic(&formatted);
        assert_eq!(
            reformatted, formatted,
            "formatting should be idempotent for JOIN ON with AND"
        );
    }

    #[test]
    fn left_join_on_and_deeper_than_on() {
        let input = "select * from a left join b on a.id = b.id and a.x = b.x;";
        let formatted = SqlEditorWidget::format_sql_basic(input);
        let lines: Vec<&str> = formatted.lines().collect();
        let indent = |line: &str| line.len().saturating_sub(line.trim_start().len());

        let on_idx = lines
            .iter()
            .position(|l| l.trim_start().starts_with("ON "))
            .expect("ON line");
        let and_idx = lines
            .iter()
            .position(|l| l.trim_start().starts_with("AND "))
            .expect("AND line");

        assert_eq!(
            indent(lines[and_idx]),
            indent(lines[on_idx]) + 4,
            "AND after ON in LEFT JOIN should be one level deeper, got:\n{}",
            formatted
        );
    }

    #[test]
    fn full_outer_join_on_and_deeper_than_on() {
        let input = "select * from a full outer join b on a.id = b.id and a.x = b.x;";
        let formatted = SqlEditorWidget::format_sql_basic(input);
        let lines: Vec<&str> = formatted.lines().collect();
        let indent = |line: &str| line.len().saturating_sub(line.trim_start().len());

        let on_idx = lines
            .iter()
            .position(|l| l.trim_start().starts_with("ON "))
            .expect("ON line");
        let and_idx = lines
            .iter()
            .position(|l| l.trim_start().starts_with("AND "))
            .expect("AND line");

        assert_eq!(
            indent(lines[and_idx]),
            indent(lines[on_idx]) + 4,
            "AND after ON in FULL OUTER JOIN should be one level deeper, got:\n{}",
            formatted
        );
    }

    #[test]
    fn join_on_with_where_and_both_correctly_indented() {
        let input =
            "select * from a join b on a.id = b.id and a.x = b.x where a.y = 1 and a.z = 2;";
        let formatted = SqlEditorWidget::format_sql_basic(input);
        let lines: Vec<&str> = formatted.lines().collect();
        let indent = |line: &str| line.len().saturating_sub(line.trim_start().len());

        let on_idx = lines
            .iter()
            .position(|l| l.trim_start().starts_with("ON "))
            .expect("ON line");
        let where_idx = lines
            .iter()
            .position(|l| l.trim_start().starts_with("WHERE "))
            .expect("WHERE line");
        let and_lines: Vec<usize> = lines
            .iter()
            .enumerate()
            .filter(|(_, l)| l.trim_start().starts_with("AND "))
            .map(|(i, _)| i)
            .collect();
        assert_eq!(
            and_lines.len(),
            2,
            "should have two AND lines, got:\n{}",
            formatted
        );

        // First AND after ON
        assert_eq!(
            indent(lines[and_lines[0]]),
            indent(lines[on_idx]) + 4,
            "AND after ON should be deeper than ON, got:\n{}",
            formatted
        );
        // Second AND after WHERE - indented as continuation
        assert!(
            indent(lines[and_lines[1]]) >= indent(lines[where_idx]),
            "AND after WHERE should be at or deeper than WHERE depth, got:\n{}",
            formatted
        );
    }

    #[test]
    fn join_on_and_edge_case_survey() {
        let indent = |line: &str| line.len().saturating_sub(line.trim_start().len());

        // Helper: format + check idempotent + return formatted
        let format_check = |label: &str, input: &str| -> String {
            let formatted = SqlEditorWidget::format_sql_basic(input);
            let reformatted = SqlEditorWidget::format_sql_basic(&formatted);
            assert_eq!(
                reformatted, formatted,
                "[{}] formatting should be idempotent.\nInput:\n{}\nFormatted:\n{}\nReformatted:\n{}",
                label, input, formatted, reformatted
            );
            formatted
        };

        // 1. ON with OR then AND (operator precedence edge case)
        let formatted = format_check(
            "OR-then-AND",
            "select * from a join b on a.id = b.id or a.x = b.x and a.y = b.y;",
        );
        {
            let lines: Vec<&str> = formatted.lines().collect();
            let on_idx = lines
                .iter()
                .position(|l| l.trim_start().starts_with("ON "))
                .unwrap();
            let or_idx = lines
                .iter()
                .position(|l| l.trim_start().starts_with("OR "))
                .unwrap();
            let and_idx = lines
                .iter()
                .position(|l| l.trim_start().starts_with("AND "))
                .unwrap();
            let on_ind = indent(lines[on_idx]);
            assert_eq!(
                indent(lines[or_idx]),
                on_ind + 4,
                "[OR-then-AND] OR should be deeper than ON:\n{}",
                formatted
            );
            assert_eq!(
                indent(lines[and_idx]),
                indent(lines[or_idx]),
                "[OR-then-AND] AND should be same as OR:\n{}",
                formatted
            );
        }

        // 2. Many ANDs (3+)
        let formatted = format_check(
            "Many-ANDs",
            "select * from a join b on a.id = b.id and a.x = b.x and a.y = b.y and a.z = b.z;",
        );
        {
            let lines: Vec<&str> = formatted.lines().collect();
            let on_idx = lines
                .iter()
                .position(|l| l.trim_start().starts_with("ON "))
                .unwrap();
            let and_lines: Vec<usize> = lines
                .iter()
                .enumerate()
                .filter(|(_, l)| l.trim_start().starts_with("AND "))
                .map(|(i, _)| i)
                .collect();
            assert_eq!(
                and_lines.len(),
                3,
                "should have 3 AND lines:\n{}",
                formatted
            );
            for &ai in &and_lines {
                assert_eq!(
                    indent(lines[ai]),
                    indent(lines[on_idx]) + 4,
                    "all ANDs should be at same depth (ON + 4):\n{}",
                    formatted
                );
            }
        }

        // 3. CROSS JOIN (no ON) then regular JOIN ON AND
        let formatted = format_check(
            "CROSS-then-JOIN",
            "select * from a cross join b join c on a.id = c.id and b.id = c.bid;",
        );
        {
            let lines: Vec<&str> = formatted.lines().collect();
            let on_idx = lines
                .iter()
                .position(|l| l.trim_start().starts_with("ON "))
                .unwrap();
            let and_idx = lines
                .iter()
                .position(|l| l.trim_start().starts_with("AND "))
                .unwrap();
            assert_eq!(
                indent(lines[and_idx]),
                indent(lines[on_idx]) + 4,
                "AND after ON should be deeper even after CROSS JOIN:\n{}",
                formatted
            );
        }

        // 4. LEFT OUTER JOIN
        let formatted = format_check(
            "LEFT-OUTER",
            "select * from a left outer join b on a.id = b.id and a.x = b.x;",
        );
        {
            let lines: Vec<&str> = formatted.lines().collect();
            let on_idx = lines
                .iter()
                .position(|l| l.trim_start().starts_with("ON "))
                .unwrap();
            let and_idx = lines
                .iter()
                .position(|l| l.trim_start().starts_with("AND "))
                .unwrap();
            assert_eq!(
                indent(lines[and_idx]),
                indent(lines[on_idx]) + 4,
                "AND after ON in LEFT OUTER JOIN:\n{}",
                formatted
            );
        }

        // 5. USING clause then JOIN ON AND
        let formatted = format_check(
            "USING-then-JOIN-ON",
            "select * from a join b using (id) join c on b.name = c.name and b.x = c.x;",
        );
        {
            let lines: Vec<&str> = formatted.lines().collect();
            let on_idx = lines
                .iter()
                .position(|l| l.trim_start().starts_with("ON "))
                .unwrap();
            let and_idx = lines
                .iter()
                .position(|l| l.trim_start().starts_with("AND "))
                .unwrap();
            assert_eq!(
                indent(lines[and_idx]),
                indent(lines[on_idx]) + 4,
                "AND after ON when previous JOIN used USING:\n{}",
                formatted
            );
        }

        // 6. Function calls in ON condition
        let formatted = format_check("Function-in-ON",
            "select * from a join b on upper(a.name) = upper(b.name) and nvl(a.id, 0) = nvl(b.id, 0);");
        {
            let lines: Vec<&str> = formatted.lines().collect();
            let on_idx = lines
                .iter()
                .position(|l| l.trim_start().starts_with("ON "))
                .unwrap();
            let and_idx = lines
                .iter()
                .position(|l| l.trim_start().starts_with("AND "))
                .unwrap();
            assert_eq!(
                indent(lines[and_idx]),
                indent(lines[on_idx]) + 4,
                "AND after ON with function calls:\n{}",
                formatted
            );
        }

        // 7. CTE with JOIN ON AND
        let formatted = format_check("CTE-with-JOIN",
            "with cte as (\nselect * from a join b on a.id = b.id and a.x = b.x\n)\nselect * from cte;");
        {
            let lines: Vec<&str> = formatted.lines().collect();
            let on_idx = lines
                .iter()
                .position(|l| l.trim_start().starts_with("ON "))
                .unwrap();
            let and_idx = lines
                .iter()
                .position(|l| l.trim_start().starts_with("AND "))
                .unwrap();
            assert_eq!(
                indent(lines[and_idx]),
                indent(lines[on_idx]) + 4,
                "AND after ON inside CTE:\n{}",
                formatted
            );
        }

        // 8. Already-formatted input (stability)
        let _formatted = format_check("Already-formatted",
            "SELECT *\nFROM a\nJOIN b\n    ON a.id = b.id\n        AND a.x = b.x\n        OR a.y = b.y\nJOIN c\n    ON b.id = c.id\n        AND b.x = c.x;");

        // 9. Deep subquery nesting
        let formatted = format_check("Deep-subquery",
            "select * from (\nselect * from (\nselect * from a join b on a.id = b.id and a.x = b.x\n) inner_sub\n) outer_sub;");
        {
            let lines: Vec<&str> = formatted.lines().collect();
            let on_idx = lines
                .iter()
                .position(|l| l.trim_start().starts_with("ON "))
                .unwrap();
            let and_idx = lines
                .iter()
                .position(|l| l.trim_start().starts_with("AND "))
                .unwrap();
            assert_eq!(
                indent(lines[and_idx]),
                indent(lines[on_idx]) + 4,
                "AND after ON in deeply nested subquery:\n{}",
                formatted
            );
        }

        // 10. JOIN ON with subquery in the condition
        let formatted = format_check("Subquery-in-ON",
            "select * from a join b on a.id = b.id and a.type in (select type from types) and a.x = b.x;");
        {
            let lines: Vec<&str> = formatted.lines().collect();
            let on_idx = lines
                .iter()
                .position(|l| l.trim_start().starts_with("ON "))
                .unwrap();
            let and_lines: Vec<usize> = lines
                .iter()
                .enumerate()
                .filter(|(_, l)| l.trim_start().starts_with("AND "))
                .map(|(i, _)| i)
                .collect();
            assert!(
                and_lines.len() >= 2,
                "should have at least 2 AND lines:\n{}",
                formatted
            );
            for &ai in &and_lines {
                assert_eq!(
                    indent(lines[ai]),
                    indent(lines[on_idx]) + 4,
                    "AND after ON with subquery in condition:\n{}",
                    formatted
                );
            }
        }

        // 11. WHERE with subquery then AND (should NOT be affected by join fix)
        let formatted = format_check(
            "WHERE-subquery-AND",
            "select * from a where a.id in (select id from b) and a.x = 1;",
        );
        {
            let lines: Vec<&str> = formatted.lines().collect();
            let where_idx = lines
                .iter()
                .position(|l| l.trim_start().starts_with("WHERE "))
                .unwrap();
            let and_idx = lines
                .iter()
                .position(|l| l.trim_start().starts_with("AND "))
                .unwrap();
            // WHERE AND should be at or deeper than WHERE level
            assert!(
                indent(lines[and_idx]) >= indent(lines[where_idx]),
                "AND after WHERE with subquery should be at or deeper than WHERE:\n{}",
                formatted
            );
        }

        // 12. JOIN ON with EXISTS subquery then AND
        let formatted = format_check("EXISTS-in-ON",
            "select * from a join b on a.id = b.id and exists (select 1 from c where c.id = a.id) and a.x = b.x;");
        {
            let lines: Vec<&str> = formatted.lines().collect();
            let on_idx = lines
                .iter()
                .position(|l| l.trim_start().starts_with("ON "))
                .unwrap();
            let and_lines: Vec<usize> = lines
                .iter()
                .enumerate()
                .filter(|(_, l)| l.trim_start().starts_with("AND "))
                .map(|(i, _)| i)
                .collect();
            assert!(
                and_lines.len() >= 2,
                "should have at least 2 AND lines:\n{}",
                formatted
            );
            for &ai in &and_lines {
                assert_eq!(
                    indent(lines[ai]),
                    indent(lines[on_idx]) + 4,
                    "AND after ON with EXISTS subquery:\n{}",
                    formatted
                );
            }
        }

        // 12b. JOIN ON with nested JOIN ON inside subquery then outer AND
        let formatted = format_check(
            "Nested-JOIN-in-ON",
            "select * from a join b on a.id = b.id and exists (select 1 from c join d on c.id = d.c_id and d.flag = 'Y' where c.id = a.id) and a.x = b.x;",
        );
        {
            let lines: Vec<&str> = formatted.lines().collect();
            let indent = |line: &str| line.len().saturating_sub(line.trim_start().len());
            let outer_on_idx = lines
                .iter()
                .position(|line| line.trim_start().starts_with("ON a.id = b.id"))
                .expect("outer ON line");
            let inner_on_idx = lines
                .iter()
                .position(|line| line.trim_start().starts_with("ON c.id = d.c_id"))
                .expect("inner ON line");
            let inner_and_idx = lines
                .iter()
                .position(|line| line.trim_start().starts_with("AND d.flag = 'Y'"))
                .expect("inner AND line");
            let outer_and_idx = lines
                .iter()
                .rposition(|line| line.trim_start().starts_with("AND a.x = b.x"))
                .expect("outer AND line");

            assert_eq!(
                indent(lines[outer_and_idx]),
                indent(lines[outer_on_idx]) + 4,
                "Outer AND after nested JOIN ON subquery should return to the outer ON depth, got:\n{}",
                formatted
            );
            assert_eq!(
                indent(lines[inner_and_idx]),
                indent(lines[inner_on_idx]) + 4,
                "Inner AND inside nested JOIN ON should stay aligned to the inner ON depth, got:\n{}",
                formatted
            );
            assert!(
                indent(lines[inner_and_idx]) > indent(lines[outer_and_idx]),
                "Nested JOIN ON continuation should remain deeper than the outer JOIN ON continuation, got:\n{}",
                formatted
            );
        }

        // 13. JOIN ON subquery then WHERE (state should be cleared for WHERE)
        let formatted = format_check("ON-subquery-then-WHERE",
            "select * from a join b on a.id = b.id and a.type in (select type from types) where a.x = 1 and a.y = 2;");
        {
            let lines: Vec<&str> = formatted.lines().collect();
            let where_idx = lines
                .iter()
                .position(|l| l.trim_start().starts_with("WHERE "))
                .unwrap();
            let where_and_idx = lines
                .iter()
                .enumerate()
                .filter(|(i, l)| *i > where_idx && l.trim_start().starts_with("AND "))
                .map(|(i, _)| i)
                .next()
                .expect("should have AND after WHERE");
            // WHERE AND should NOT be at join ON AND depth
            assert!(
                indent(lines[where_and_idx]) <= indent(lines[where_idx]) + 4,
                "AND after WHERE should be at WHERE continuation depth, not JOIN ON depth:\n{}",
                formatted
            );
        }
    }

    // ── NATURAL LEFT/RIGHT JOIN stays on one line ──

    #[test]
    fn format_sql_natural_left_join_stays_together() {
        let source = "SELECT * FROM emp e NATURAL LEFT JOIN dept d JOIN bonus b USING (deptno);";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        assert!(
            formatted.contains("NATURAL LEFT JOIN"),
            "NATURAL LEFT JOIN should stay on one line, got:\n{}",
            formatted
        );
        // Idempotent
        let formatted2 = SqlEditorWidget::format_sql_basic(&formatted);
        assert_eq!(
            formatted, formatted2,
            "NATURAL LEFT JOIN formatting should be idempotent"
        );
    }

    #[test]
    fn format_sql_natural_right_join_stays_together() {
        let source = "SELECT * FROM t1 NATURAL RIGHT JOIN t2;";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        assert!(
            formatted.contains("NATURAL RIGHT JOIN"),
            "NATURAL RIGHT JOIN should stay on one line, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_sql_natural_full_join_stays_together() {
        let source = "SELECT * FROM t1 NATURAL FULL JOIN t2;";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        assert!(
            formatted.contains("NATURAL FULL JOIN"),
            "NATURAL FULL JOIN should stay on one line, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_sql_natural_inner_join_stays_together() {
        let source = "SELECT * FROM t1 NATURAL INNER JOIN t2;";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        assert!(
            formatted.contains("NATURAL INNER JOIN"),
            "NATURAL INNER JOIN should stay on one line, got:\n{}",
            formatted
        );
    }

    // ── Trigger REFERENCING / FOR EACH ROW / WHEN ──

    #[test]
    fn format_sql_trigger_referencing_and_when_alignment() {
        let source = "CREATE OR REPLACE TRIGGER trg_emp_biu BEFORE INSERT OR UPDATE OF sal, comm ON emp REFERENCING NEW AS n OLD AS o FOR EACH ROW WHEN (n.sal > 0) BEGIN :n.comm := NVL(:n.comm, 0); END;";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        // Comma in UPDATE OF column list should stay inline inside the trigger event clause.
        assert!(
            formatted.contains("UPDATE OF sal, comm"),
            "UPDATE OF column list commas should stay inline in trigger event clause, got:\n{}",
            formatted
        );
        // ON should start a new trigger header line.
        assert!(
            formatted.contains("\n    ON emp"),
            "Trigger ON clause should start on its own line at trigger header indent, got:\n{}",
            formatted
        );
        // REFERENCING should start a new line at trigger header indent
        assert!(
            formatted.contains("\n    REFERENCING"),
            "REFERENCING should start a new line at trigger header indent, got:\n{}",
            formatted
        );
        // REFERENCING clause should stay on one line
        assert!(
            formatted.contains("REFERENCING NEW AS n OLD AS o"),
            "REFERENCING NEW AS n OLD AS o should stay on one line, got:\n{}",
            formatted
        );
        // FOR EACH ROW on its own line
        assert!(
            formatted.contains("\n    FOR EACH ROW"),
            "FOR EACH ROW should be on its own line at trigger header indent, got:\n{}",
            formatted
        );
        // WHEN on its own line at trigger header indent
        assert!(
            formatted.contains("\n    WHEN (n.sal > 0)"),
            "WHEN clause should be on its own line at trigger header indent, got:\n{}",
            formatted
        );
        // BEGIN at base indent
        assert!(
            formatted.contains("\nBEGIN"),
            "BEGIN should be at base indent after trigger WHEN, got:\n{}",
            formatted
        );
        // Idempotent
        let formatted2 = SqlEditorWidget::format_sql_basic(&formatted);
        assert_eq!(
            formatted, formatted2,
            "Trigger REFERENCING/WHEN formatting should be idempotent"
        );
    }

    #[test]
    fn format_sql_trigger_update_of_multi_column_comma_stays_inline() {
        let source = "CREATE OR REPLACE TRIGGER trg_test AFTER UPDATE OF col1, col2, col3 ON my_table FOR EACH ROW BEGIN NULL; END;";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        assert!(
            formatted.contains("UPDATE OF col1, col2, col3"),
            "Multiple columns in trigger UPDATE OF should stay inline, got:\n{}",
            formatted
        );
        assert!(
            formatted.contains("\n    ON my_table"),
            "Trigger ON clause should move to its own line after UPDATE OF, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_sql_trigger_before_insert_places_on_on_separate_header_line() {
        let source = "CREATE OR REPLACE TRIGGER oqt_trg_test_bi BEFORE INSERT ON oqt_t_test FOR EACH ROW DECLARE v_tag VARCHAR2(30) := 'TRG'; BEGIN NULL; END;";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        assert!(
            formatted.contains("\n    BEFORE INSERT\n    ON oqt_t_test\n    FOR EACH ROW"),
            "Trigger header should place ON on its own line between timing/event and FOR EACH ROW, got:\n{}",
            formatted
        );
        assert!(
            formatted.contains("\nDECLARE\n    v_tag VARCHAR2") && formatted.contains(":= 'TRG';"),
            "DECLARE section should stay below the trigger header, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_sql_match_recognize_one_row_per_match_on_own_line() {
        let source = "SELECT * FROM sales MATCH_RECOGNIZE (PARTITION BY customer_id ORDER BY sale_date MEASURES FIRST(A.sale_date) AS first_dt, LAST(B.sale_date) AS last_dt ONE ROW PER MATCH PATTERN (A B+) DEFINE A AS amount < 100, B AS amount >= 100);";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        assert!(
            formatted.contains("\n    ONE ROW PER MATCH"),
            "ONE ROW PER MATCH should be on its own line inside MATCH_RECOGNIZE, got:\n{}",
            formatted
        );
        assert!(
            formatted.contains("\n    PATTERN"),
            "PATTERN should be on its own line inside MATCH_RECOGNIZE, got:\n{}",
            formatted
        );
        // Idempotent
        let formatted2 = SqlEditorWidget::format_sql_basic(&formatted);
        assert_eq!(
            formatted, formatted2,
            "MATCH_RECOGNIZE ONE ROW PER MATCH formatting should be idempotent"
        );
    }

    #[test]
    fn format_sql_match_recognize_all_rows_per_match_on_own_line() {
        let source = "SELECT * FROM sales MATCH_RECOGNIZE (PARTITION BY cust_id ORDER BY sale_date MEASURES MATCH_NUMBER() AS mno ALL ROWS PER MATCH PATTERN (A B+) DEFINE A AS amount < 50, B AS amount >= 50);";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        assert!(
            formatted.contains("\n    ALL ROWS PER MATCH"),
            "ALL ROWS PER MATCH should be on its own line inside MATCH_RECOGNIZE, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_sql_match_recognize_after_match_skip_and_subset_on_own_lines() {
        let source = "SELECT * FROM sales MATCH_RECOGNIZE (PARTITION BY cust_id ORDER BY sale_date MEASURES MATCH_NUMBER() AS mno AFTER MATCH SKIP TO NEXT ROW SUBSET ab = (A, B) PATTERN (A B+) DEFINE A AS amount < 50, B AS amount >= 50);";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        assert!(
            formatted.contains(
                "MATCH_NUMBER () AS mno
    AFTER MATCH SKIP TO NEXT ROW"
            ),
            "AFTER MATCH SKIP should be on its own line inside MATCH_RECOGNIZE, got:\n{}",
            formatted
        );
        assert!(
            formatted.contains(
                "AFTER MATCH SKIP TO NEXT ROW
    SUBSET ab = (A, B)
    PATTERN"
            ),
            "SUBSET should be on its own line inside MATCH_RECOGNIZE, got:\n{}",
            formatted
        );
        assert_eq!(
            SqlEditorWidget::format_sql_basic(&formatted),
            formatted,
            "MATCH_RECOGNIZE AFTER MATCH SKIP/SUBSET formatting should be idempotent"
        );
    }

    #[test]
    fn format_sql_match_recognize_unmatched_and_empty_match_headers_on_own_lines() {
        let source = "SELECT * FROM sales MATCH_RECOGNIZE (PARTITION BY cust_id ORDER BY sale_date MEASURES MATCH_NUMBER() AS mno ALL ROWS PER MATCH WITH UNMATCHED ROWS OMIT EMPTY MATCHES PATTERN (A B+) DEFINE A AS amount < 50, B AS amount >= 50);";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        assert!(
            formatted.contains(
                "ALL ROWS PER MATCH
    WITH UNMATCHED ROWS"
            ),
            "WITH UNMATCHED ROWS should start on its own line inside MATCH_RECOGNIZE, got:\n{}",
            formatted
        );
        assert!(
            formatted.contains(
                "WITH UNMATCHED ROWS
    OMIT EMPTY MATCHES"
            ),
            "OMIT EMPTY MATCHES should start on its own line inside MATCH_RECOGNIZE, got:\n{}",
            formatted
        );
        assert!(
            formatted.contains(
                "OMIT EMPTY MATCHES
    PATTERN"
            ),
            "PATTERN should remain on its own line after MATCH_RECOGNIZE output modifiers, got:\n{}",
            formatted
        );
        assert_eq!(
            SqlEditorWidget::format_sql_basic(&formatted),
            formatted,
            "MATCH_RECOGNIZE unmatched/empty-match formatting should be idempotent"
        );
    }

    #[test]
    fn format_sql_analytic_over_clause_breaks_subclauses() {
        let source = "SELECT empno, SUM(sal) OVER (PARTITION BY deptno ORDER BY hiredate ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW) AS running_sal FROM emp;";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        let expected = r#"SELECT empno,
    SUM (sal) OVER (
        PARTITION BY deptno
        ORDER BY hiredate
        ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW
    ) AS running_sal
FROM emp;"#;
        assert_eq!(formatted, expected);
        // Idempotent
        let formatted2 = SqlEditorWidget::format_sql_basic(&formatted);
        assert_eq!(
            formatted, formatted2,
            "OVER clause formatting should be idempotent"
        );
    }

    #[test]
    fn format_sql_analytic_over_with_range_and_groups() {
        let source = "SELECT id, AVG(val) OVER (ORDER BY id RANGE BETWEEN INTERVAL '7' DAY PRECEDING AND CURRENT ROW) AS avg7d, COUNT(*) OVER (PARTITION BY grp ORDER BY id GROUPS BETWEEN 1 PRECEDING AND 1 FOLLOWING) AS cnt3 FROM t;";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        assert!(
            formatted.contains("RANGE BETWEEN"),
            "RANGE BETWEEN should appear in formatted output, got:\n{}",
            formatted
        );
        assert!(
            formatted.contains("\n        RANGE BETWEEN"),
            "RANGE should be on its own indented line inside OVER, got:\n{}",
            formatted
        );
        assert!(
            formatted.contains("\n        GROUPS BETWEEN"),
            "GROUPS should be on its own indented line inside OVER, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_sql_analytic_over_partition_only() {
        let source = "SELECT deptno, SUM(sal) OVER (PARTITION BY deptno) AS dept_total FROM emp;";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        assert!(
            formatted.contains("OVER (\n        PARTITION BY deptno\n    )"),
            "Single PARTITION BY in OVER should be on its own line, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_sql_analytic_and_window_exclude_breaks_to_owner_relative_lines() {
        let analytic_source =
            "SELECT SUM(sal) OVER (PARTITION BY deptno EXCLUDE CURRENT ROW) AS running_sal FROM emp;";
        let analytic_formatted = SqlEditorWidget::format_sql_basic(analytic_source);
        assert!(
            analytic_formatted.contains(
                "OVER (\n    PARTITION BY deptno\n    EXCLUDE CURRENT ROW\n) AS running_sal"
            ),
            "EXCLUDE inside OVER should break onto the shared owner-relative depth, got:\n{}",
            analytic_formatted
        );

        let window_source = "SELECT SUM(e.sal) OVER w_dept AS dept_sum FROM emp e WINDOW w_dept AS (PARTITION BY e.deptno EXCLUDE CURRENT ROW) QUALIFY ROW_NUMBER() OVER w_dept = 1;";
        let window_formatted = SqlEditorWidget::format_sql_basic(window_source);
        let lines: Vec<&str> = window_formatted.lines().collect();
        let window_idx = find_line_starting_with(&lines, "WINDOW").expect("WINDOW clause line");
        let named_idx = find_line_starting_with(&lines, "w_dept AS (").expect("named WINDOW line");
        let partition_idx =
            find_line_starting_with(&lines, "PARTITION BY e.deptno").expect("WINDOW PARTITION BY");
        let exclude_idx = find_line_starting_with(&lines, "EXCLUDE CURRENT ROW")
            .expect("WINDOW EXCLUDE clause line");

        assert_eq!(
            leading_spaces(lines[exclude_idx]),
            leading_spaces(lines[partition_idx]),
            "WINDOW EXCLUDE should share the WINDOW body depth after line breaking, got:\n{}",
            window_formatted
        );
        assert_eq!(
            leading_spaces(lines[named_idx]),
            leading_spaces(lines[window_idx]).saturating_add(4),
            "named WINDOW definition should stay one clause-body level deeper than bare WINDOW, got:\n{}",
            window_formatted
        );
        assert!(
            leading_spaces(lines[exclude_idx]) > leading_spaces(lines[named_idx]),
            "WINDOW EXCLUDE should stay indented under WINDOW, got:\n{}",
            window_formatted
        );
        assert_eq!(
            SqlEditorWidget::format_sql_basic(&window_formatted),
            window_formatted,
            "WINDOW EXCLUDE formatting should be idempotent"
        );
    }

    #[test]
    fn format_sql_analytic_over_restores_outer_window_depth_after_nested_over() {
        let source = "SELECT SUM(sal) OVER (PARTITION BY deptno ORDER BY (SELECT MAX(inner_val) OVER (PARTITION BY grp ORDER BY inner_val) FROM dual) ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW) AS running_sal FROM emp;";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        let lines: Vec<&str> = formatted.lines().collect();
        let indent = |line: &str| line.len().saturating_sub(line.trim_start().len());

        let outer_partition_idx = lines
            .iter()
            .position(|line| line.trim_start().starts_with("PARTITION BY deptno"))
            .expect("outer PARTITION BY line");
        let inner_partition_idx = lines
            .iter()
            .position(|line| line.trim_start().starts_with("PARTITION BY grp"))
            .expect("inner PARTITION BY line");
        let outer_rows_idx = lines
            .iter()
            .position(|line| line.trim_start().starts_with("ROWS BETWEEN"))
            .expect("outer ROWS BETWEEN line");

        assert_eq!(
            indent(lines[outer_rows_idx]),
            indent(lines[outer_partition_idx]),
            "Outer ROWS BETWEEN should stay aligned with the outer OVER subclauses after nested OVER closes, got:\n{}",
            formatted
        );
        assert!(
            indent(lines[inner_partition_idx]) > indent(lines[outer_partition_idx]),
            "Nested OVER subclauses should stay deeper than the outer OVER subclauses, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_sql_window_clause_and_qualify_keep_clause_depths_stable() {
        let source = "SELECT e.deptno, e.empno, SUM(e.sal) OVER w_dept AS dept_sum FROM emp e WINDOW w_dept AS (PARTITION BY e.deptno ORDER BY e.sal DESC, e.empno) QUALIFY ROW_NUMBER() OVER w_dept = 1;";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        let lines: Vec<&str> = formatted.lines().collect();

        let window_idx = find_line_starting_with(&lines, "WINDOW").expect("WINDOW clause line");
        let named_idx = find_line_starting_with(&lines, "w_dept AS (").expect("named WINDOW line");
        let partition_idx =
            find_line_starting_with(&lines, "PARTITION BY e.deptno").expect("WINDOW PARTITION BY");
        let order_idx =
            find_line_starting_with(&lines, "ORDER BY e.sal DESC").expect("WINDOW ORDER BY");
        let qualify_idx =
            find_line_starting_with(&lines, "QUALIFY ROW_NUMBER").expect("QUALIFY clause line");
        let close_idx = lines
            .iter()
            .enumerate()
            .skip(order_idx.saturating_add(1))
            .find(|(idx, line)| *idx < qualify_idx && line.trim() == ")")
            .map(|(idx, _)| idx)
            .expect("WINDOW closing parenthesis");

        assert!(
            formatted.contains("\nWINDOW\n    w_dept AS ("),
            "WINDOW clause should start on its own clause line, got:\n{}",
            formatted
        );
        assert_eq!(
            leading_spaces(lines[named_idx]),
            leading_spaces(lines[window_idx]).saturating_add(4),
            "named WINDOW definition should stay one level deeper than bare WINDOW, got:\n{}",
            formatted
        );
        assert!(
            leading_spaces(lines[partition_idx]) > leading_spaces(lines[named_idx]),
            "WINDOW PARTITION BY should be indented under WINDOW, got:\n{}",
            formatted
        );
        assert_eq!(
            leading_spaces(lines[partition_idx]),
            leading_spaces(lines[order_idx]),
            "WINDOW PARTITION BY / ORDER BY should share the same continuation depth, got:\n{}",
            formatted
        );
        assert_eq!(
            leading_spaces(lines[close_idx]),
            leading_spaces(lines[named_idx]),
            "WINDOW closing parenthesis should realign with the named WINDOW definition, got:\n{}",
            formatted
        );
        assert_eq!(
            leading_spaces(lines[qualify_idx]),
            leading_spaces(lines[window_idx]),
            "QUALIFY should align with top-level clause anchors, got:\n{}",
            formatted
        );

        let formatted_again = SqlEditorWidget::format_sql_basic(&formatted);
        assert_eq!(
            formatted, formatted_again,
            "WINDOW / QUALIFY formatting should be idempotent"
        );
    }

    #[test]
    fn format_sql_window_clause_indents_named_window_siblings_under_bare_window_header() {
        let source = "SELECT ob.*, ROW_NUMBER() OVER w_emp AS rn_in_emp, DENSE_RANK() OVER w_global AS global_rank, SUM(ob.total_usd) OVER w_emp_running AS running_emp_total FROM order_base AS ob WINDOW w_emp AS (PARTITION BY ob.emp_id ORDER BY ob.created_at, ob.order_id), w_emp_running AS (PARTITION BY ob.emp_id ORDER BY ob.created_at, ob.order_id ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW), w_global AS (ORDER BY ob.total_usd DESC);";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        let window_fragment = r#"WINDOW
    w_emp AS (
        PARTITION BY ob.emp_id
        ORDER BY ob.created_at,
        ob.order_id
    ),
    w_emp_running AS (
        PARTITION BY ob.emp_id
        ORDER BY ob.created_at,
        ob.order_id
        ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW
    ),
    w_global AS (
        ORDER BY ob.total_usd DESC
    );"#;

        assert!(
            formatted.contains(window_fragment),
            "named WINDOW siblings should format as clause-body items under a bare WINDOW header, got:\n{}",
            formatted
        );
        assert_eq!(SqlEditorWidget::format_sql_basic(&formatted), formatted);
    }

    #[test]
    fn format_sql_nested_window_clause_restores_outer_query_depth() {
        let source = "SELECT * FROM (SELECT e.deptno, SUM(e.sal) OVER w_dept AS dept_sum FROM emp e WINDOW w_dept AS (PARTITION BY e.deptno ORDER BY e.sal DESC, e.empno) QUALIFY ROW_NUMBER() OVER w_dept = 1) ranked WHERE ranked.dept_sum > 0;";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        let lines: Vec<&str> = formatted.lines().collect();
        let indent = |line: &str| line.len().saturating_sub(line.trim_start().len());

        let from_paren_idx =
            find_line_starting_with(&lines, "FROM (").expect("nested query owner line");
        let inner_select_idx =
            find_line_starting_with(&lines, "SELECT e.deptno").expect("nested SELECT line");
        let window_idx = find_line_starting_with(&lines, "WINDOW").expect("nested WINDOW line");
        let named_idx =
            find_line_starting_with(&lines, "w_dept AS (").expect("nested named WINDOW line");
        let partition_idx =
            find_line_starting_with(&lines, "PARTITION BY e.deptno").expect("nested PARTITION BY");
        let order_idx =
            find_line_starting_with(&lines, "ORDER BY e.sal DESC").expect("nested ORDER BY");
        let qualify_idx =
            find_line_starting_with(&lines, "QUALIFY ROW_NUMBER").expect("nested QUALIFY line");
        let close_idx = lines
            .iter()
            .enumerate()
            .find(|(_, line)| line.trim_start() == ") ranked")
            .map(|(idx, _)| idx)
            .expect("nested query closing line");
        let outer_where_idx = lines
            .iter()
            .rposition(|line| line.trim_start().starts_with("WHERE ranked.dept_sum"))
            .expect("outer WHERE line");

        assert!(
            indent(lines[inner_select_idx]) > indent(lines[from_paren_idx]),
            "nested SELECT should stay deeper than the FROM ( owner, got:\n{}",
            formatted
        );
        assert_eq!(
            indent(lines[window_idx]),
            indent(lines[inner_select_idx]),
            "nested WINDOW should stay aligned with the nested query clause depth, got:\n{}",
            formatted
        );
        assert_eq!(
            indent(lines[named_idx]),
            indent(lines[window_idx]).saturating_add(4),
            "nested named WINDOW definition should stay one level deeper than bare WINDOW, got:\n{}",
            formatted
        );
        assert_eq!(
            indent(lines[partition_idx]),
            indent(lines[named_idx]).saturating_add(4),
            "nested WINDOW body should be one indentation level deeper than its named owner, got:\n{}",
            formatted
        );
        assert_eq!(
            indent(lines[partition_idx]),
            indent(lines[order_idx]),
            "nested WINDOW PARTITION BY / ORDER BY should share the same continuation depth, got:\n{}",
            formatted
        );
        assert_eq!(
            indent(lines[qualify_idx]),
            indent(lines[window_idx]),
            "nested QUALIFY should realign with the nested WINDOW owner depth after the body closes, got:\n{}",
            formatted
        );
        assert_eq!(
            indent(lines[close_idx]),
            indent(lines[inner_select_idx]).saturating_sub(4),
            "nested query closing line should step out of the inner query body by one level, got:\n{}",
            formatted
        );
        assert_eq!(
            indent(lines[outer_where_idx]),
            0,
            "outer WHERE should return to the outer query base after the nested WINDOW query closes, got:\n{}",
            formatted
        );
        assert_eq!(
            SqlEditorWidget::format_sql_basic(&formatted),
            formatted,
            "nested WINDOW formatting should remain idempotent"
        );
    }

    // ── Bug regression: ORDER BY ... DESC split ──

    #[test]
    fn format_sql_order_by_desc_not_split_as_describe_command() {
        // DESC on its own line must NOT be misidentified as SQL*Plus DESCRIBE.
        let source = "SELECT empno, sal\nFROM emp\nORDER BY sal\nDESC;";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        assert!(
            formatted.contains("ORDER BY sal DESC"),
            "ORDER BY sal DESC should stay together, got:\n{}",
            formatted
        );
        // Idempotence
        let formatted_again = SqlEditorWidget::format_sql_basic(&formatted);
        assert_eq!(
            formatted, formatted_again,
            "Formatting ORDER BY DESC should be idempotent, got:\n{}",
            formatted_again
        );
    }

    #[test]
    fn format_sql_order_by_desc_with_slash_terminator() {
        // DESC on its own line with / terminator must not be split off.
        let source = "SELECT empno, sal\nFROM emp\nORDER BY sal\nDESC\n/";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        assert!(
            formatted.contains("ORDER BY sal DESC"),
            "ORDER BY sal DESC with slash terminator should stay together, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_sql_order_by_desc_nulls_last_multiline() {
        let source = "SELECT empno, sal\nFROM emp\nORDER BY sal\nDESC\nNULLS LAST;";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        assert!(
            formatted.contains("ORDER BY sal DESC NULLS LAST"),
            "ORDER BY sal DESC NULLS LAST should stay together, got:\n{}",
            formatted
        );
    }

    // ── Bug regression: MATCH_RECOGNIZE DEFINE with content ──

    #[test]
    fn format_sql_match_recognize_define_with_content_not_split() {
        let source = r#"SELECT *
FROM ticks
MATCH_RECOGNIZE (
    PARTITION BY symbol
    ORDER BY ts
    PATTERN (A B+)
    DEFINE B AS B.price > PREV(B.price)
);"#;
        let formatted = SqlEditorWidget::format_sql_basic(source);
        // DEFINE must be kept as SQL, not rewritten as SQL*Plus DEFINE assignment
        assert!(
            !formatted.contains("DEFINE B = "),
            "DEFINE should not be rewritten as SQL*Plus DEFINE assignment, got:\n{}",
            formatted
        );
        // The DEFINE line must remain in the statement (formatter may add spaces around parens)
        assert!(
            formatted.contains("DEFINE B AS B.price > PREV"),
            "DEFINE inside MATCH_RECOGNIZE should be kept as SQL, got:\n{}",
            formatted
        );
        let formatted_again = SqlEditorWidget::format_sql_basic(&formatted);
        assert_eq!(
            formatted, formatted_again,
            "MATCH_RECOGNIZE formatting should be idempotent, got:\n{}",
            formatted_again
        );
    }

    // ── Bug regression: FOR UPDATE ──

    #[test]
    fn format_sql_for_update_not_split() {
        let source = "SELECT * FROM emp FOR UPDATE OF sal SKIP LOCKED;";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        assert!(
            formatted.contains("FOR UPDATE OF sal SKIP LOCKED"),
            "FOR UPDATE should stay on one line, got:\n{}",
            formatted
        );
        assert!(
            !formatted.contains("FOR\nUPDATE") && !formatted.contains("FOR\n    UPDATE"),
            "FOR and UPDATE should not be split across lines, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_sql_for_update_multiline_input() {
        // Even when input has FOR and UPDATE on separate lines, output should join them.
        let source = "SELECT *\nFROM emp\nFOR\nUPDATE OF sal SKIP LOCKED;";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        assert!(
            formatted.contains("FOR UPDATE"),
            "FOR UPDATE should be on same line, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_sql_for_update_inline_comment_keeps_lock_body_on_shared_clause_depth() {
        let source = "SELECT *\nFROM emp\nFOR UPDATE -- lock mode\nSKIP LOCKED;";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        let lines: Vec<&str> = formatted.lines().collect();
        let indent = |line: &str| line.len().saturating_sub(line.trim_start().len());

        let for_update_idx = lines
            .iter()
            .position(|line| line.trim_start().starts_with("FOR UPDATE --"))
            .unwrap_or(0);
        let skip_locked_idx = lines
            .iter()
            .position(|line| line.trim_start() == "SKIP LOCKED;")
            .unwrap_or(0);

        assert_eq!(
            indent(lines[skip_locked_idx]),
            indent(lines[for_update_idx]).saturating_add(4),
            "line after `FOR UPDATE -- ...` should indent one structural level deeper than the FOR UPDATE owner line, got:\n{}",
            formatted
        );
        assert_eq!(
            SqlEditorWidget::format_sql_basic(&formatted),
            formatted,
            "FOR UPDATE inline-comment body layout should remain stable"
        );
    }

    #[test]
    fn format_sql_mixed_close_for_update_inline_comment_keeps_lock_body_on_structural_tail_depth() {
        let source = "SELECT *\nFROM emp\nWHERE deptno IN (\n    SELECT deptno\n    FROM dept\n) FOR UPDATE -- lock mode\nSKIP LOCKED;";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        let lines: Vec<&str> = formatted.lines().collect();
        let indent = |line: &str| line.len().saturating_sub(line.trim_start().len());

        let select_idx = lines
            .iter()
            .position(|line| line.trim_start() == "SELECT *")
            .unwrap_or(0);
        let for_update_idx = lines
            .iter()
            .position(|line| line.trim_start().starts_with(") FOR UPDATE --"))
            .unwrap_or(0);
        let skip_locked_idx = lines
            .iter()
            .position(|line| line.trim_start() == "SKIP LOCKED;")
            .unwrap_or(0);

        assert_eq!(
            indent(lines[for_update_idx]),
            indent(lines[select_idx]),
            "mixed leading-close `) FOR UPDATE -- ...` should realign to the outer query base depth, got:\n{}",
            formatted
        );
        assert_eq!(
            indent(lines[skip_locked_idx]),
            indent(lines[for_update_idx]).saturating_add(4),
            "SKIP LOCKED after mixed leading-close `) FOR UPDATE -- ...` should use the structural FOR UPDATE body depth, got:\n{}",
            formatted
        );
        assert_eq!(
            SqlEditorWidget::format_sql_basic(&formatted),
            formatted,
            "mixed leading-close FOR UPDATE inline-comment body layout should remain stable"
        );
    }

    // ── Bug regression: MERGE USING alignment ──

    #[test]
    fn format_sql_merge_using_gets_clause_break() {
        let source = "MERGE INTO tgt t USING src s ON (t.id = s.id) WHEN MATCHED THEN UPDATE SET t.val = s.val WHEN NOT MATCHED THEN INSERT (id, val) VALUES (s.id, s.val);";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        assert!(
            formatted.contains("\nUSING src s"),
            "USING should start on a new line like a clause keyword, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_sql_join_using_gets_condition_break() {
        let source =
            "SELECT e.empno, d.dname FROM emp e JOIN dept d USING (deptno) WHERE e.empno > 0;";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        assert!(
            formatted.contains("\n    USING (deptno)") || formatted.contains("\nUSING (deptno)"),
            "JOIN USING should get alignment similar to ON, got:\n{}",
            formatted
        );
    }

    // ── Bug regression: CROSS APPLY / OUTER APPLY ──

    #[test]
    fn format_sql_cross_apply_gets_join_like_break() {
        let source = "SELECT e.empno, x.cnt FROM emp e CROSS APPLY (SELECT COUNT(*) AS cnt FROM bonus b WHERE b.empno = e.empno) x;";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        assert!(
            formatted.contains("\nCROSS APPLY ("),
            "CROSS APPLY should start on a new line like a JOIN, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_sql_outer_apply_gets_join_like_break() {
        let source = "SELECT e.empno, x.cnt FROM emp e OUTER APPLY (SELECT COUNT(*) AS cnt FROM bonus b WHERE b.empno = e.empno) x;";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        assert!(
            formatted.contains("\nOUTER APPLY ("),
            "OUTER APPLY should start on a new line like a JOIN, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_sql_lateral_derived_table_uses_from_item_query_depth() {
        let source = "SELECT d.deptno, x.max_sal FROM dept d, LATERAL (SELECT MAX(e.sal) AS max_sal FROM emp e WHERE e.deptno = d.deptno) x;";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        let lines: Vec<&str> = formatted.lines().collect();

        let from_idx = find_line_starting_with(&lines, "FROM dept d,").expect("outer FROM line");
        let lateral_idx = find_line_starting_with(&lines, "LATERAL (").expect("LATERAL owner line");
        let select_idx =
            find_line_starting_with(&lines, "SELECT MAX").expect("LATERAL inner SELECT line");
        let inner_from_idx =
            find_line_starting_with(&lines, "FROM emp e").expect("LATERAL inner FROM line");
        let close_idx = lines
            .iter()
            .enumerate()
            .skip(inner_from_idx.saturating_add(1))
            .find(|(_, line)| line.trim_start().starts_with(") x"))
            .map(|(idx, _)| idx)
            .expect("LATERAL closing line");

        assert!(
            leading_spaces(lines[lateral_idx]) > leading_spaces(lines[from_idx]),
            "LATERAL derived table should indent under the FROM item list, got:\n{}",
            formatted
        );
        assert!(
            leading_spaces(lines[select_idx]) > leading_spaces(lines[lateral_idx]),
            "SELECT inside LATERAL should indent deeper than the owner line, got:\n{}",
            formatted
        );
        assert_eq!(
            leading_spaces(lines[select_idx]),
            leading_spaces(lines[inner_from_idx]),
            "Nested query clauses inside LATERAL should share one query depth, got:\n{}",
            formatted
        );
        assert_eq!(
            leading_spaces(lines[close_idx]),
            leading_spaces(lines[lateral_idx]),
            "LATERAL closing parenthesis should realign with the owner line, got:\n{}",
            formatted
        );
    }

    // ── Bug regression: PIVOT/UNPIVOT alignment ──

    #[test]
    fn format_sql_pivot_stays_intact() {
        let source = r#"SELECT * FROM (SELECT deptno, job, sal FROM emp) PIVOT (SUM(sal) FOR job IN ('CLERK' AS clerk, 'MANAGER' AS manager));"#;
        let formatted = SqlEditorWidget::format_sql_basic(source);
        // PIVOT block must be present and correctly formatted
        assert!(
            formatted.contains("PIVOT ("),
            "PIVOT keyword and opening paren should be present, got:\n{}",
            formatted
        );
        assert!(
            formatted.contains("SUM (sal)") || formatted.contains("SUM(sal)"),
            "PIVOT aggregate should be present, got:\n{}",
            formatted
        );
        let formatted_again = SqlEditorWidget::format_sql_basic(&formatted);
        assert_eq!(
            formatted, formatted_again,
            "PIVOT formatting should be idempotent, got:\n{}",
            formatted_again
        );
    }

    // ── Bug regression: FETCH FIRST ... WITH TIES ──

    #[test]
    fn format_sql_fetch_first_with_ties_not_split() {
        let source = "SELECT * FROM emp ORDER BY sal DESC FETCH FIRST 3 ROWS WITH TIES;";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        // WITH TIES must not become a separate WITH clause
        assert!(
            !formatted.contains("\nWITH TIES"),
            "WITH TIES should not be split to a new line as a WITH clause, got:\n{}",
            formatted
        );
        assert!(
            formatted.contains("ROWS WITH TIES"),
            "WITH TIES should stay on the FETCH line, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_sql_offset_and_fetch_comments_keep_clause_continuation_indent() {
        let source = "SELECT e.empno, e.ename FROM emp e ORDER BY e.empno OFFSET -- skip first page\n10 ROWS FETCH -- page size\nFIRST 5 ROWS ONLY;";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        let lines: Vec<&str> = formatted.lines().collect();

        let offset_idx = find_line_starting_with(&lines, "OFFSET").expect("OFFSET clause line");
        let offset_rows_idx =
            find_line_starting_with(&lines, "10 ROWS").expect("OFFSET operand line");
        let fetch_idx = find_line_starting_with(&lines, "FETCH").expect("FETCH clause line");
        let fetch_rows_idx =
            find_line_starting_with(&lines, "FIRST 5 ROWS ONLY;").expect("FETCH operand line");

        assert!(
            lines[offset_idx].contains("-- skip first page")
                || lines
                    .iter()
                    .any(|line| line.trim_start().starts_with("-- skip first page")),
            "OFFSET comment should be preserved in formatted output, got:\n{}",
            formatted
        );
        assert!(
            leading_spaces(lines[offset_rows_idx]) > leading_spaces(lines[offset_idx]),
            "OFFSET operand should stay deeper than the OFFSET clause line, got:\n{}",
            formatted
        );
        assert!(
            lines[fetch_idx].contains("-- page size")
                || lines
                    .iter()
                    .any(|line| line.trim_start().starts_with("-- page size")),
            "FETCH comment should be preserved in formatted output, got:\n{}",
            formatted
        );
        assert!(
            leading_spaces(lines[fetch_rows_idx]) > leading_spaces(lines[fetch_idx]),
            "FETCH operand should stay deeper than the FETCH clause line, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_sql_basic_limit_comment_continuation_keeps_operand_indent() {
        let source = "SELECT e.empno FROM emp e ORDER BY e.empno LIMIT -- page size\n10;";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        let lines: Vec<&str> = formatted.lines().collect();

        let limit_idx = find_line_starting_with(&lines, "LIMIT").expect("LIMIT clause line");
        let operand_idx = find_line_starting_with(&lines, "10;").expect("LIMIT operand line");

        assert!(
            lines[limit_idx].contains("-- page size")
                || lines
                    .iter()
                    .any(|line| line.trim_start().starts_with("-- page size")),
            "LIMIT comment should be preserved in formatted output, got:\n{}",
            formatted
        );
        assert!(
            leading_spaces(lines[operand_idx]) > leading_spaces(lines[limit_idx]),
            "LIMIT operand should stay deeper than the LIMIT clause line, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_sql_basic_values_comment_continuation_keeps_tuple_indent() {
        let source = "INSERT INTO t_log (id, msg)\nVALUES -- tuple payload\n(1, 'x');";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        let lines: Vec<&str> = formatted.lines().collect();

        let values_idx = find_line_starting_with(&lines, "VALUES").expect("VALUES clause line");
        let tuple_idx = find_line_starting_with(&lines, "(1, 'x');").expect("VALUES tuple line");

        assert!(
            lines[values_idx].contains("-- tuple payload")
                || lines
                    .iter()
                    .any(|line| line.trim_start().starts_with("-- tuple payload")),
            "VALUES comment should be preserved in formatted output, got:\n{}",
            formatted
        );
        assert!(
            leading_spaces(lines[tuple_idx]) > leading_spaces(lines[values_idx]),
            "VALUES tuple should stay deeper than the VALUES clause line, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_sql_versions_between_comment_continuation_stays_attached_to_from_clause() {
        let source = "SELECT versions_starttime, versions_endtime, employee_id, salary FROM employees VERSIONS BETWEEN TIMESTAMP -- keep temporal boundary\nSYSTIMESTAMP - INTERVAL '7' DAY AND SYSTIMESTAMP WHERE employee_id = 100;";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        let lines: Vec<&str> = formatted.lines().collect();

        let from_idx = find_line_starting_with(&lines, "FROM employees VERSIONS BETWEEN TIMESTAMP")
            .expect("VERSIONS BETWEEN owner line");
        let boundary_idx =
            find_line_starting_with(&lines, "SYSTIMESTAMP - INTERVAL '7' DAY AND SYSTIMESTAMP")
                .expect("VERSIONS BETWEEN boundary line");
        let where_idx = find_line_starting_with(&lines, "WHERE employee_id = 100;")
            .expect("WHERE line after VERSIONS BETWEEN");

        assert!(
            lines[from_idx].contains("-- keep temporal boundary")
                || lines
                    .iter()
                    .any(|line| line.trim_start().starts_with("-- keep temporal boundary")),
            "Temporal clause comment should be preserved in formatted output, got:\n{}",
            formatted
        );
        assert!(
            leading_spaces(lines[boundary_idx]) > leading_spaces(lines[from_idx]),
            "VERSIONS BETWEEN boundary should stay deeper than the FROM owner line, got:\n{}",
            formatted
        );
        assert_eq!(
            leading_spaces(lines[where_idx]),
            leading_spaces(lines[from_idx]),
            "WHERE should realign with the FROM clause after temporal continuation, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_sql_log_errors_into_and_reject_limit_stay_inline() {
        let source = "INSERT INTO target_emp (empno, ename) SELECT empno, ename FROM staging_emp LOG ERRORS INTO err$_target_emp ('LOAD1') REJECT LIMIT UNLIMITED;";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        assert!(
            formatted.contains("LOG ERRORS INTO err$_target_emp ('LOAD1')"),
            "LOG ERRORS INTO should stay on one clause line, got:\n{}",
            formatted
        );
        assert!(
            formatted.contains("REJECT LIMIT UNLIMITED"),
            "REJECT LIMIT should stay on one clause line, got:\n{}",
            formatted
        );
        assert!(
            !formatted.contains("\nINTO err$_target_emp")
                && !formatted.contains("\nLIMIT UNLIMITED"),
            "LOG ERRORS INTO / REJECT LIMIT must not be split by clause keywords, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_sql_model_rules_update_stays_on_rules_line() {
        let source = "SELECT * FROM sales MODEL PARTITION BY (deptno) DIMENSION BY (month_key) MEASURES (amt) RULES UPDATE (amt[1] = amt[CV(month_key)] * 1.1);";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        assert!(
            formatted.contains("\n    RULES UPDATE ("),
            "MODEL RULES UPDATE should stay in the same subclause header, got:\n{}",
            formatted
        );
        assert!(
            !formatted.contains("\nUPDATE ("),
            "MODEL RULES UPDATE must not split UPDATE as a top-level clause, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_sql_basic_match_recognize_measures_comment_continuation_keeps_subclause_indent() {
        let source = "SELECT *\nFROM sales\nMATCH_RECOGNIZE (\n    MEASURES -- derived columns\n    FIRST(A.sale_date) AS first_dt\n    PATTERN (A+)\n    DEFINE A AS amount < 100\n);";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        let lines: Vec<&str> = formatted.lines().collect();

        let measures_idx =
            find_line_starting_with(&lines, "MEASURES").expect("MATCH_RECOGNIZE MEASURES line");
        let expr_idx =
            find_line_starting_with(&lines, "FIRST").expect("MATCH_RECOGNIZE MEASURES body line");

        assert!(
            lines[measures_idx].contains("-- derived columns")
                || lines
                    .iter()
                    .any(|line| line.trim_start().starts_with("-- derived columns")),
            "MEASURES comment should be preserved in formatted output, got:\n{}",
            formatted
        );
        assert!(
            leading_spaces(lines[expr_idx]) > leading_spaces(lines[measures_idx]),
            "MATCH_RECOGNIZE MEASURES body should stay deeper than the header line, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_sql_basic_model_measures_or_rules_comment_continuation_keeps_subclause_indent() {
        let source = "SELECT *\nFROM sales\nMODEL\n    PARTITION BY (deptno)\n    DIMENSION BY (month_key)\n    MEASURES -- model metrics\n    (amt)\n    RULES UPDATE (amt[1] = amt[CV(month_key)] * 1.1);";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        let lines: Vec<&str> = formatted.lines().collect();

        let measures_idx =
            find_line_starting_with(&lines, "MEASURES").expect("MODEL MEASURES line");
        let measures_body_idx =
            find_line_starting_with(&lines, "(amt)").expect("MODEL MEASURES body line");

        assert!(
            lines[measures_idx].contains("-- model metrics")
                || lines
                    .iter()
                    .any(|line| line.trim_start().starts_with("-- model metrics")),
            "MODEL MEASURES comment should be preserved in formatted output, got:\n{}",
            formatted
        );
        assert!(
            leading_spaces(lines[measures_body_idx]) > leading_spaces(lines[measures_idx]),
            "MODEL MEASURES body should stay deeper than the header line, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_sql_basic_model_reference_comment_continuation_keeps_owner_depth() {
        let source = "SELECT *\nFROM sales\nMODEL\n    PARTITION BY (deptno)\n    DIMENSION BY (month_key)\n    REFERENCE -- model ref\n    ref_limits ON (\n        SELECT limit_amt\n        FROM limits l\n        WHERE l.deptno = sales.deptno\n    )\n    MEASURES (amount)\n    RULES UPDATE (amount[ANY] = amount[CV()] + 1)\nORDER BY deptno;";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        let lines: Vec<&str> = formatted.lines().collect();

        let reference_idx =
            find_line_starting_with(&lines, "REFERENCE -- model ref").expect("REFERENCE line");
        let reference_on_idx =
            find_line_starting_with(&lines, "ref_limits ON (").expect("REFERENCE ON line");
        let child_select_idx =
            find_line_starting_with(&lines, "SELECT limit_amt").expect("child SELECT line");

        assert_eq!(
            leading_spaces(lines[reference_on_idx]),
            leading_spaces(lines[reference_idx]),
            "split MODEL REFERENCE owner text should stay aligned with the exact bare REFERENCE line, got:\n{}",
            formatted
        );
        assert!(
            leading_spaces(lines[child_select_idx]) > leading_spaces(lines[reference_on_idx]),
            "nested SELECT under comment-split MODEL REFERENCE should stay deeper than the owner line, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_sql_basic_xmltable_columns_comment_continuation_keeps_column_indent() {
        let source = "SELECT *\nFROM XMLTABLE(\n    '/rows/row'\n    PASSING x.payload\n    COLUMNS -- projected columns\n    id NUMBER PATH '@id',\n    name VARCHAR2(30) PATH 'name'\n) xt;";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        let lines: Vec<&str> = formatted.lines().collect();

        let columns_idx = lines
            .iter()
            .position(|line| line.contains("COLUMNS -- projected columns"))
            .unwrap_or_else(|| panic!("XMLTABLE COLUMNS header line, got:\n{}", formatted));
        let id_idx = find_line_starting_with(&lines, "id NUMBER PATH '@id'")
            .expect("XMLTABLE first column line");

        assert!(
            lines[columns_idx].contains("-- projected columns")
                || lines
                    .iter()
                    .any(|line| line.trim_start().starts_with("-- projected columns")),
            "XMLTABLE COLUMNS comment should be preserved in formatted output, got:\n{}",
            formatted
        );
        assert!(
            leading_spaces(lines[id_idx]) > leading_spaces(lines[columns_idx]),
            "XMLTABLE column line should stay deeper than the COLUMNS header, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_sql_forall_values_of_stays_in_header() {
        let source = "DECLARE TYPE idx_tab IS TABLE OF PLS_INTEGER; l_idx idx_tab := idx_tab(1, 3, 5); BEGIN FORALL i IN VALUES OF l_idx INSERT INTO t_log (id, msg) VALUES (i, 'x'); END; /";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        assert!(
            formatted.contains("FORALL i IN VALUES OF l_idx"),
            "FORALL header should keep VALUES OF inline, got:\n{}",
            formatted
        );
        assert!(
            !formatted.contains("IN\n        VALUES OF")
                && !formatted.contains("IN\n    VALUES OF"),
            "VALUES OF must not be split out of the FORALL header, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_sql_multiset_except_stays_inline() {
        let source = "DECLARE TYPE num_nt IS TABLE OF NUMBER; nt_a num_nt := num_nt(1, 2, 3); nt_b num_nt := num_nt(2, 3); v_diff num_nt; BEGIN v_diff := nt_a MULTISET EXCEPT DISTINCT nt_b; END; /";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        assert!(
            formatted.contains("MULTISET EXCEPT DISTINCT nt_b"),
            "MULTISET EXCEPT should remain an expression operator, got:\n{}",
            formatted
        );
        assert!(
            !formatted.contains("MULTISET\n") && !formatted.contains("\nEXCEPT DISTINCT nt_b"),
            "MULTISET EXCEPT must not be treated as a set-operator clause break, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_sql_multiset_union_stays_inline() {
        let source = "DECLARE TYPE num_nt IS TABLE OF NUMBER; nt_a num_nt := num_nt(1, 2); nt_b num_nt := num_nt(3, 4); v_all num_nt; BEGIN v_all := nt_a MULTISET UNION DISTINCT nt_b; END; /";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        assert!(
            formatted.contains("MULTISET UNION DISTINCT nt_b"),
            "MULTISET UNION should remain an expression operator, got:\n{}",
            formatted
        );
        assert!(
            !formatted.contains("MULTISET\n") && !formatted.contains("\nUNION DISTINCT nt_b"),
            "MULTISET UNION must not be treated as a top-level set operator, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_sql_json_object_with_unique_keys_stays_inside_function() {
        let source = "SELECT JSON_OBJECT('id' VALUE e.empno, 'name' VALUE e.ename WITH UNIQUE KEYS) AS j FROM emp e;";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        assert!(
            formatted.contains("WITH UNIQUE KEYS"),
            "JSON_OBJECT WITH UNIQUE KEYS should remain inside the function call, got:\n{}",
            formatted
        );
        assert!(
            !formatted.contains("\nWITH UNIQUE KEYS"),
            "JSON_OBJECT WITH UNIQUE KEYS must not be treated as a new WITH clause, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_sql_json_query_with_wrapper_stays_inside_function() {
        let source = "SELECT JSON_QUERY(e.payload, '$.items[*]' WITH WRAPPER) AS items_json FROM emp_json e;";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        assert!(
            formatted.contains("WITH WRAPPER"),
            "JSON_QUERY WITH WRAPPER should remain inside the function call, got:\n{}",
            formatted
        );
        assert!(
            !formatted.contains("\nWITH WRAPPER"),
            "JSON_QUERY WITH WRAPPER must not be treated as a new WITH clause, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_for_auto_formatting_keeps_multiline_json_query_with_inside_function_parens() {
        let source = r#"SELECT
    JSON_QUERY (
        e.payload, '$.items[*]' -- json path
        WITH WRAPPER
    ) AS items_json,
    e.empno
FROM emp_json e;"#;
        let formatted = SqlEditorWidget::format_for_auto_formatting(source, false);
        let lines: Vec<&str> = formatted.lines().collect();
        let indent = |line: &str| line.len().saturating_sub(line.trim_start().len());

        let function_with_idx = lines
            .iter()
            .position(|line| line.trim_start().starts_with("WITH WRAPPER"))
            .unwrap_or_else(|| panic!("JSON_QUERY WITH option line, got:\n{}", formatted));
        let sibling_idx = lines
            .iter()
            .position(|line| line.trim_start() == "e.empno")
            .unwrap_or_else(|| panic!("SELECT-list sibling after JSON_QUERY, got:\n{}", formatted));
        let query_from_idx = lines
            .iter()
            .position(|line| line.trim_start().eq_ignore_ascii_case("FROM emp_json e;"))
            .unwrap_or_else(|| panic!("outer query FROM clause, got:\n{}", formatted));

        assert!(
            indent(lines[function_with_idx]) > indent(lines[query_from_idx]),
            "function-local WITH should not be promoted to a query WITH frame, got:\n{}",
            formatted
        );
        assert_eq!(
            indent(lines[sibling_idx]),
            indent(lines[query_from_idx]).saturating_add(4),
            "SELECT-list sibling after multiline JSON_QUERY should return to list depth, got:\n{}",
            formatted
        );
        assert_eq!(
            SqlEditorWidget::format_for_auto_formatting(&formatted, false),
            formatted,
            "multiline JSON_QUERY auto-formatting should be idempotent"
        );
    }

    #[test]
    fn format_for_auto_formatting_keeps_nested_subquery_json_query_with_inside_function_parens() {
        let source = r#"SELECT
    (
        SELECT JSON_QUERY (
            x.payload, '$.items[*]' -- json path
            WITH WRAPPER
        )
        FROM emp_json x
    ) AS items_json,
    e.empno
FROM emp_json e;"#;
        let formatted = SqlEditorWidget::format_for_auto_formatting(source, false);
        let lines: Vec<&str> = formatted.lines().collect();
        let indent = |line: &str| line.len().saturating_sub(line.trim_start().len());

        let with_idx = lines
            .iter()
            .position(|line| line.trim_start().starts_with("WITH WRAPPER"))
            .unwrap_or_else(|| panic!("nested JSON_QUERY WITH option line, got:\n{}", formatted));
        let subquery_from_idx = lines
            .iter()
            .position(|line| line.trim_start().eq_ignore_ascii_case("FROM emp_json x"))
            .unwrap_or_else(|| panic!("nested subquery FROM clause, got:\n{}", formatted));
        let outer_sibling_idx = lines
            .iter()
            .position(|line| line.trim_start() == "e.empno")
            .unwrap_or_else(|| {
                panic!(
                    "outer SELECT-list sibling after nested JSON_QUERY, got:\n{}",
                    formatted
                )
            });
        let outer_from_idx = lines
            .iter()
            .position(|line| line.trim_start().eq_ignore_ascii_case("FROM emp_json e;"))
            .unwrap_or_else(|| panic!("outer query FROM clause, got:\n{}", formatted));

        assert!(
            indent(lines[with_idx]) > indent(lines[subquery_from_idx]),
            "function-local WITH inside a nested subquery function call should stay deeper than the child query FROM clause, got:\n{}",
            formatted
        );
        assert_eq!(
            indent(lines[outer_sibling_idx]),
            indent(lines[outer_from_idx]).saturating_add(4),
            "outer SELECT-list sibling after nested JSON_QUERY should return to the canonical list depth, got:\n{}",
            formatted
        );
        assert_eq!(
            SqlEditorWidget::format_for_auto_formatting(&formatted, false),
            formatted,
            "nested JSON_QUERY WITH auto-formatting should be idempotent"
        );
    }

    #[test]
    fn format_sql_json_transform_set_and_insert_stay_inside_function() {
        let source = "SELECT JSON_TRANSFORM(e.payload, SET '$.status' = 'DONE', INSERT '$.audit.user' = USER) AS payload2 FROM emp_json e;";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        assert!(
            formatted.contains("SET '$.status' = 'DONE'"),
            "JSON_TRANSFORM SET operation should remain inside the function call, got:\n{}",
            formatted
        );
        assert!(
            formatted.contains("INSERT '$.audit.user' = USER"),
            "JSON_TRANSFORM INSERT operation should remain inside the function call, got:\n{}",
            formatted
        );
        assert!(
            !formatted.contains("\nSET '$.status' = 'DONE'\nFROM")
                && !formatted.contains("\nINSERT '$.audit.user' = USER\nFROM"),
            "JSON_TRANSFORM operations must not escape the function argument context, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_sql_with_package_declaration_stays_structured() {
        let source =
            "WITH PACKAGE pkg_demo AS FUNCTION f RETURN NUMBER; END pkg_demo; SELECT 1 FROM dual;";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        assert!(
            formatted.contains("WITH\n    PACKAGE pkg_demo AS"),
            "WITH PACKAGE should start a structured declaration block, got:\n{}",
            formatted
        );
        assert!(
            formatted.contains("\n        FUNCTION f RETURN NUMBER;"),
            "WITH PACKAGE members should be indented under the declaration, got:\n{}",
            formatted
        );
        assert!(
            formatted.contains("\n    END pkg_demo;\nSELECT 1"),
            "WITH PACKAGE should close before the main query at the correct depth, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_sql_with_type_declaration_stays_attached_to_main_query() {
        let source = "WITH TYPE t_num IS TABLE OF NUMBER; SELECT * FROM TABLE(t_num(1, 2, 3));";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        assert!(
            formatted.contains("WITH\n    TYPE t_num IS TABLE OF NUMBER;\nSELECT *"),
            "WITH TYPE declaration should stay attached to the main query, got:\n{}",
            formatted
        );
        assert!(
            formatted.contains("FROM TABLE (t_num (1, 2, 3))")
                || formatted.contains("FROM TABLE(t_num(1, 2, 3))"),
            "Main query after WITH TYPE should remain intact, got:\n{}",
            formatted
        );
    }

    // ── Bug regression: WITH CTE + UPDATE/MERGE/DELETE/INSERT comma indent ──

    #[test]
    fn format_sql_with_update_cte_keeps_set_indent() {
        let source = "WITH src AS (\nSELECT 1 AS c1, 2 AS c2 FROM dual\n)\nUPDATE tgt t\nSET t.c1 = (SELECT c1 FROM src),\nt.c2 = (SELECT c2 FROM src),\nt.c3 = 3\nWHERE EXISTS (SELECT 1 FROM src);";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        // After the CTE, the UPDATE SET commas should indent under SET, not at root depth
        let lines: Vec<&str> = formatted.lines().collect();
        let leading_spaces = |line: &str| line.len().saturating_sub(line.trim_start().len());
        let set_line = lines
            .iter()
            .find(|l| l.trim_start().starts_with("SET"))
            .unwrap();
        let c2_line = lines
            .iter()
            .find(|l| l.trim_start().starts_with("t.c2"))
            .unwrap();
        let c3_line = lines
            .iter()
            .find(|l| l.trim_start().starts_with("t.c3"))
            .unwrap();
        let set_indent = leading_spaces(set_line);
        let c2_indent = leading_spaces(c2_line);
        let c3_indent = leading_spaces(c3_line);
        assert!(
            c2_indent > 0,
            "t.c2 should be indented (not at root depth), got:\n{}",
            formatted
        );
        assert_eq!(
            c2_indent, c3_indent,
            "SET list items should have consistent indent, got:\n{}",
            formatted
        );
        assert!(
            c2_indent >= set_indent,
            "SET list items should be at or deeper than SET clause, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_sql_with_merge_cte_keeps_set_indent() {
        let source = "WITH src AS (\nSELECT 1 AS id, 2 AS val FROM dual\n)\nMERGE INTO tgt t\nUSING src s\nON (t.id = s.id)\nWHEN MATCHED THEN\nUPDATE SET t.val = s.val, t.extra = 0\nWHEN NOT MATCHED THEN\nINSERT (id, val) VALUES (s.id, s.val);";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        // Commas in UPDATE SET inside MERGE after WITH should not be treated as CTE separators
        assert!(
            !formatted.contains("\nt.extra"),
            "SET list commas in WITH+MERGE should not drop to root indent, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_for_auto_formatting_keeps_first_select_child_query_one_level_deeper_than_item() {
        let source = "SELECT (SELECT MAX(s.amount) FROM qt_sales s WHERE s.emp_id = e.emp_id) AS max_amount, e.emp_id FROM qt_emp e;";
        let formatted = SqlEditorWidget::format_for_auto_formatting(source, false);
        let lines: Vec<&str> = formatted.lines().collect();
        let indent = |line: &str| line.len().saturating_sub(line.trim_start().len());
        let item_idx = find_line_starting_with(&lines, "(")
            .unwrap_or_else(|| panic!("outer SELECT item opener, got:\n{}", formatted));
        let child_select_idx = find_line_starting_with(&lines, "SELECT MAX (s.amount)")
            .unwrap_or_else(|| panic!("nested SELECT line, got:\n{}", formatted));
        let close_idx = find_line_starting_with(&lines, ") AS max_amount,")
            .unwrap_or_else(|| panic!("outer SELECT item close, got:\n{}", formatted));

        assert_eq!(
            indent(lines[child_select_idx]),
            indent(lines[item_idx]).saturating_add(4),
            "First SELECT item child query should be one level deeper than the item opener, got:\n{}",
            formatted
        );
        assert_eq!(
            indent(lines[close_idx]),
            indent(lines[item_idx]),
            "First SELECT item close should return to the item depth, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_for_auto_formatting_keeps_first_from_child_query_one_level_deeper_than_item() {
        let source = "SELECT src.emp_id FROM (SELECT e.emp_id FROM qt_emp e WHERE e.active_yn = 'Y') src WHERE src.emp_id > 0;";
        let formatted = SqlEditorWidget::format_for_auto_formatting(source, false);
        let lines: Vec<&str> = formatted.lines().collect();
        let indent = |line: &str| line.len().saturating_sub(line.trim_start().len());
        let from_idx = find_line_starting_with(&lines, "FROM (")
            .or_else(|| find_line_starting_with(&lines, "FROM"))
            .unwrap_or_else(|| panic!("FROM line, got:\n{}", formatted));
        let child_select_idx = find_line_starting_with(&lines, "SELECT e.emp_id")
            .unwrap_or_else(|| panic!("nested FROM child SELECT line, got:\n{}", formatted));
        let close_idx = find_line_starting_with(&lines, ") src")
            .unwrap_or_else(|| panic!("FROM child close line, got:\n{}", formatted));

        assert!(
            indent(lines[child_select_idx]) > indent(lines[from_idx]),
            "First FROM item child query should be deeper than the FROM owner line, got:\n{}",
            formatted
        );
        assert!(
            indent(lines[close_idx]) >= indent(lines[from_idx]),
            "First FROM item close should not escape above the FROM owner depth, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_for_auto_formatting_keeps_first_multiline_set_item_off_root_depth() {
        let source = "UPDATE qt_emp e\nSET\ne.salary = e.salary + (\nSELECT NVL (ROUND (AVG (s.amount) / 100, 2), 0)\nFROM qt_sales s\nWHERE s.emp_id = e.emp_id\n),\ne.note = NVL (e.note, EMPTY_CLOB ()) || CHR (10) || 'UPDATED@' || TO_CHAR (SYSTIMESTAMP, 'YYYY-MM-DD HH24:MI:SS.FF3')\nWHERE e.active_yn = 'Y'\nAND EXISTS (\nSELECT 1\nFROM qt_sales s\nWHERE s.emp_id = e.emp_id\nAND s.status IN ('NEW', 'PAID')\n);";
        let formatted = SqlEditorWidget::format_for_auto_formatting(source, false);
        let lines: Vec<&str> = formatted.lines().collect();
        let indent = |line: &str| line.len().saturating_sub(line.trim_start().len());
        let set_idx = find_line_starting_with(&lines, "SET").expect("SET line");
        let salary_idx = find_line_starting_with(&lines, "SET e.salary = e.salary + (")
            .or_else(|| find_line_starting_with(&lines, "e.salary = e.salary + ("))
            .unwrap_or_else(|| panic!("first multiline SET item, got:\n{}", formatted));
        let note_idx = find_line_starting_with(
            &lines,
            "e.note = NVL (e.note, EMPTY_CLOB ()) || CHR (10) || 'UPDATED@' || TO_CHAR (SYSTIMESTAMP, 'YYYY-MM-DD HH24:MI:SS.FF3')",
        )
        .expect("second SET item");
        let child_select_idx =
            find_line_starting_with(&lines, "SELECT NVL (ROUND (AVG (s.amount) / 100, 2), 0)")
                .unwrap_or_else(|| panic!("SET child SELECT line, got:\n{}", formatted));
        let child_close_idx = find_line_starting_with(&lines, "),")
            .unwrap_or_else(|| panic!("SET child close line, got:\n{}", formatted));
        let exists_select_idx =
            find_line_starting_with(&lines, "SELECT 1").expect("EXISTS child SELECT line");

        assert!(
            indent(lines[child_select_idx]) > indent(lines[salary_idx]),
            "First multiline SET item child query should be deeper than the SET item line, got:\n{}",
            formatted
        );
        assert!(
            indent(lines[child_close_idx]) == indent(lines[note_idx]),
            "First multiline SET item close should return to the SET list depth, got:\n{}",
            formatted
        );
        assert!(
            indent(lines[note_idx]) > indent(lines[set_idx]),
            "SET sibling items should stay one level deeper than the SET clause, got:\n{}",
            formatted
        );
        assert!(
            indent(lines[exists_select_idx]) > indent(lines[note_idx]),
            "Nested EXISTS query should stay deeper than the SET list items, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_sql_with_delete_cte_keeps_where_indent() {
        let source = "WITH old AS (\nSELECT id FROM archive WHERE ts < SYSDATE - 30\n)\nDELETE FROM main_tbl\nWHERE id IN (SELECT id FROM old);";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        assert!(
            formatted.contains("DELETE FROM main_tbl"),
            "DELETE FROM should stay on the same line, got:\n{}",
            formatted
        );
        // WITH + DELETE: the CTE state must close so WHERE gets proper clause indent
        assert!(
            formatted.contains("\nWHERE"),
            "WHERE should start on its own line, got:\n{}",
            formatted
        );
        // Idempotency check
        let formatted_again = SqlEditorWidget::format_sql_basic(&formatted);
        assert_eq!(
            formatted, formatted_again,
            "WITH+DELETE formatting should be idempotent, got:\n{}",
            formatted_again
        );
    }

    #[test]
    fn format_sql_basic_keeps_delete_from_inline() {
        let source = "DELETE FROM emp WHERE deptno = 10;";
        let formatted = SqlEditorWidget::format_sql_basic(source);

        assert!(
            formatted.contains("DELETE FROM emp"),
            "DELETE FROM should stay on the same line, got:\n{}",
            formatted
        );
        assert!(
            !formatted.contains("DELETE\nFROM emp"),
            "DELETE and FROM should not be split across lines, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_sql_with_insert_cte_keeps_values_indent() {
        let source = "WITH src AS (\nSELECT 1 AS c1, 2 AS c2 FROM dual\n)\nINSERT INTO tgt (c1, c2)\nSELECT c1, c2 FROM src;";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        // WITH + INSERT ... SELECT: the CTE should close properly
        assert!(
            formatted.contains("INSERT INTO tgt"),
            "INSERT after CTE should have proper layout, got:\n{}",
            formatted
        );
        // Idempotency check
        let formatted_again = SqlEditorWidget::format_sql_basic(&formatted);
        assert_eq!(
            formatted, formatted_again,
            "WITH+INSERT formatting should be idempotent, got:\n{}",
            formatted_again
        );
    }

    // ── Bug regression: split_format_items DESC is not DESCRIBE ──

    #[test]
    fn split_format_items_desc_in_order_by_is_not_describe() {
        let source = "SELECT e.empno, e.sal\nFROM emp e\nORDER BY\ne.sal\nDESC;";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        // DESC must remain part of the SELECT statement, not split off as DESCRIBE
        assert!(
            formatted.contains("DESC;") || formatted.contains("DESC\n"),
            "DESC should remain in the statement, got:\n{}",
            formatted
        );
        assert!(
            !formatted.contains("DESCRIBE"),
            "DESC in ORDER BY must not be treated as DESCRIBE command, got:\n{}",
            formatted
        );
    }

    // ── Bug regression: MATCH_RECOGNIZE DEFINE with arguments ──

    #[test]
    fn split_format_items_match_recognize_define_with_args_is_sql() {
        let source = "SELECT *\nFROM ticks\nMATCH_RECOGNIZE (\nPARTITION BY symbol\nORDER BY ts\nPATTERN (A+)\nDEFINE A AS price > PREV(price)\n);";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        // DEFINE A AS ... inside MATCH_RECOGNIZE must not be mistaken for SQL*Plus DEFINE
        assert!(
            formatted.contains("DEFINE"),
            "DEFINE should remain in the statement, got:\n{}",
            formatted
        );
        assert!(
            formatted.contains("MATCH_RECOGNIZE"),
            "MATCH_RECOGNIZE should be present, got:\n{}",
            formatted
        );
        // Should be a single formatted statement, not split
        let items = crate::db::QueryExecutor::split_format_items(source);
        let stmt_count = items
            .iter()
            .filter(|item| matches!(item, crate::db::FormatItem::Statement(_)))
            .count();
        assert_eq!(
            stmt_count, 1,
            "MATCH_RECOGNIZE with DEFINE should be one statement, got {} statements from: {:?}",
            stmt_count, items
        );
    }

    // ── Bug candidate 1: CROSS APPLY / OUTER APPLY with subquery body alignment ──

    #[test]
    fn format_sql_cross_apply_multi_subquery_alignment() {
        let source = "SELECT e.empno, x.max_sal, y.deptno FROM emp e CROSS APPLY (SELECT MAX(b.sal) AS max_sal FROM emp_bonus b WHERE b.empno = e.empno) x OUTER APPLY (SELECT d.deptno FROM dept d WHERE d.deptno = e.deptno) y;";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        let lines: Vec<&str> = formatted.lines().collect();

        assert!(
            formatted.contains("\nCROSS APPLY ("),
            "CROSS APPLY should start on its own line, got:\n{}",
            formatted
        );
        assert!(
            formatted.contains("\nOUTER APPLY ("),
            "OUTER APPLY should start on its own line, got:\n{}",
            formatted
        );

        // Both APPLY keywords should be at the same indent level (base join level)
        let cross_idx = lines
            .iter()
            .position(|l| l.trim_start().starts_with("CROSS APPLY"))
            .unwrap();
        let outer_idx = lines
            .iter()
            .position(|l| l.trim_start().starts_with("OUTER APPLY"))
            .unwrap();
        assert_eq!(
            leading_spaces(lines[cross_idx]),
            leading_spaces(lines[outer_idx]),
            "CROSS APPLY and OUTER APPLY should be at the same indent level, got:\n{}",
            formatted
        );

        // Idempotence check
        let formatted_again = SqlEditorWidget::format_sql_basic(&formatted);
        assert_eq!(
            formatted, formatted_again,
            "CROSS/OUTER APPLY formatting should be idempotent, got:\n{}",
            formatted_again
        );
    }

    // ── Bug candidate 2: MATCH_RECOGNIZE inline DEFINE with arguments ──

    #[test]
    fn split_format_items_match_recognize_define_a_as_is_not_tool_command() {
        let source = "SELECT *\nFROM ticks\nMATCH_RECOGNIZE (\n  ORDER BY ts\n  MEASURES A.price AS a_price\n  PATTERN (A B)\n  DEFINE A AS A.price < PREV(A.price),\n         B AS B.price > PREV(B.price)\n);";
        let items = crate::db::QueryExecutor::split_format_items(source);
        let stmt_count = items
            .iter()
            .filter(|item| matches!(item, crate::db::FormatItem::Statement(_)))
            .count();
        let tool_count = items
            .iter()
            .filter(|item| matches!(item, crate::db::FormatItem::ToolCommand(_)))
            .count();
        assert_eq!(
            stmt_count, 1,
            "MATCH_RECOGNIZE with DEFINE A AS should be one statement, got {} statements and {} tool commands from: {:?}",
            stmt_count, tool_count, items
        );
        assert_eq!(
            tool_count, 0,
            "DEFINE A AS inside MATCH_RECOGNIZE should not be parsed as tool command, got {} tool commands from: {:?}",
            tool_count, items
        );
    }

    #[test]
    fn format_sql_match_recognize_define_multi_variable_stays_intact() {
        let source = "SELECT * FROM ticks MATCH_RECOGNIZE (ORDER BY ts MEASURES A.price AS a_price PATTERN (A B) DEFINE A AS A.price < PREV(A.price), B AS B.price > PREV(B.price));";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        assert!(
            formatted.contains("DEFINE"),
            "DEFINE should remain in the formatted output, got:\n{}",
            formatted
        );
        assert!(
            formatted.contains("MATCH_RECOGNIZE"),
            "MATCH_RECOGNIZE should remain in the formatted output, got:\n{}",
            formatted
        );
        // Idempotence
        let formatted_again = SqlEditorWidget::format_sql_basic(&formatted);
        assert_eq!(
            formatted, formatted_again,
            "MATCH_RECOGNIZE DEFINE formatting should be idempotent, got:\n{}",
            formatted_again
        );
    }

    // ── Bug candidate 3: JSON_VALUE RETURNING ... ON ERROR ──

    #[test]
    fn format_sql_json_value_returning_on_error_stays_inline() {
        let source = "SELECT JSON_VALUE(payload, '$.name' RETURNING VARCHAR2(30) NULL ON ERROR) AS name_txt FROM event_log;";
        let formatted = SqlEditorWidget::format_sql_basic(source);

        // RETURNING and ON ERROR should stay inside the function parentheses
        // and not cause unwanted line breaks
        assert!(
            !formatted.contains("\n    ON ERROR"),
            "ON ERROR inside JSON_VALUE should not get its own clause line, got:\n{}",
            formatted
        );
        // The entire function call should be within one paren group
        assert!(
            formatted.contains("NULL ON ERROR)"),
            "NULL ON ERROR should stay inline inside JSON_VALUE parens, got:\n{}",
            formatted
        );

        // Idempotence
        let formatted_again = SqlEditorWidget::format_sql_basic(&formatted);
        assert_eq!(
            formatted, formatted_again,
            "JSON_VALUE RETURNING ON ERROR formatting should be idempotent, got:\n{}",
            formatted_again
        );
    }

    #[test]
    fn format_for_auto_formatting_keeps_multiline_json_value_on_empty_and_error_inside_function_parens(
    ) {
        let source = r#"SELECT
    JSON_VALUE (
        e.payload, '$.name' -- json path
        RETURNING VARCHAR2 (30)
        NULL ON EMPTY -- missing path
        NULL ON ERROR
    ) AS name_txt,
    e.empno
FROM event_log e;"#;
        let formatted = SqlEditorWidget::format_for_auto_formatting(source, false);
        let lines: Vec<&str> = formatted.lines().collect();
        let indent = |line: &str| line.len().saturating_sub(line.trim_start().len());

        let function_returning_idx = lines
            .iter()
            .position(|line| line.trim_start().starts_with("RETURNING VARCHAR2 (30)"))
            .unwrap_or_else(|| panic!("JSON_VALUE RETURNING option line, got:\n{}", formatted));
        let function_on_error_idx = lines
            .iter()
            .position(|line| line.trim_start().starts_with("NULL ON ERROR"))
            .unwrap_or_else(|| panic!("JSON_VALUE ON ERROR option line, got:\n{}", formatted));
        let sibling_idx = lines
            .iter()
            .position(|line| line.trim_start() == "e.empno")
            .unwrap_or_else(|| panic!("SELECT-list sibling after JSON_VALUE, got:\n{}", formatted));
        let query_from_idx = lines
            .iter()
            .position(|line| line.trim_start().eq_ignore_ascii_case("FROM event_log e;"))
            .unwrap_or_else(|| panic!("outer query FROM clause, got:\n{}", formatted));

        assert!(
            formatted.contains("NULL ON EMPTY"),
            "JSON_VALUE ON EMPTY option should remain inside the function call, got:\n{}",
            formatted
        );
        for option_idx in [function_returning_idx, function_on_error_idx] {
            assert!(
                indent(lines[option_idx]) > indent(lines[query_from_idx]),
                "function-local JSON_VALUE option line should not be promoted to outer clause depth, got:\n{}",
                formatted
            );
        }
        assert_eq!(
            indent(lines[sibling_idx]),
            indent(lines[query_from_idx]).saturating_add(4),
            "SELECT-list sibling after multiline JSON_VALUE options should return to list depth, got:\n{}",
            formatted
        );
        assert_eq!(
            SqlEditorWidget::format_for_auto_formatting(&formatted, false),
            formatted,
            "multiline JSON_VALUE option auto-formatting should be idempotent"
        );
    }

    #[test]
    fn format_for_auto_formatting_keeps_json_value_on_error_prefix_lines_inside_function_parens() {
        let source = r#"SELECT
    JSON_VALUE (
        e.payload, '$.name'
        RETURNING VARCHAR2 (30) -- type
        ON ERROR NULL -- policy
        ON EMPTY NULL -- fallback
    ) AS name_txt,
    e.empno
FROM event_log e;"#;
        let formatted = SqlEditorWidget::format_for_auto_formatting(source, false);
        let lines: Vec<&str> = formatted.lines().collect();
        let indent = |line: &str| line.len().saturating_sub(line.trim_start().len());
        let find_line_starting_with = |prefix: &str| -> usize {
            lines
                .iter()
                .position(|line| line.trim_start().starts_with(prefix))
                .unwrap_or_else(|| panic!("missing line starting with: {prefix}\n{formatted}"))
        };

        let on_error_idx = find_line_starting_with("ON ERROR NULL -- policy");
        let on_empty_idx = find_line_starting_with("ON EMPTY NULL -- fallback");
        let sibling_idx = find_line_starting_with("e.empno");
        let from_idx = find_line_starting_with("FROM event_log e;");

        for option_idx in [on_error_idx, on_empty_idx] {
            assert!(
                indent(lines[option_idx]) > indent(lines[from_idx]),
                "JSON_VALUE ON ERROR/ON EMPTY option line should stay inside function parens, got:\n{}",
                formatted
            );
        }
        assert_eq!(
            indent(lines[sibling_idx]),
            indent(lines[from_idx]).saturating_add(4),
            "SELECT-list sibling after ON-prefixed JSON_VALUE options should return to canonical list depth, got:\n{}",
            formatted
        );
        assert_eq!(
            SqlEditorWidget::format_for_auto_formatting(&formatted, false),
            formatted,
            "ON-prefixed JSON_VALUE option auto-formatting should be idempotent"
        );
    }

    #[test]
    fn format_for_auto_formatting_keeps_mixed_close_json_value_on_error_inside_function_parens() {
        let source = r#"SELECT
    JSON_VALUE (
        e.payload,
        '$.name'
        RETURNING VARCHAR2 (
            30
        ) ON ERROR NULL -- policy
        ON EMPTY NULL -- fallback
    ) AS name_txt,
    e.empno
FROM event_log e;"#;
        let formatted = SqlEditorWidget::format_for_auto_formatting(source, false);
        let lines: Vec<&str> = formatted.lines().collect();
        let indent = |line: &str| line.len().saturating_sub(line.trim_start().len());
        let find_line_starting_with = |prefix: &str| -> usize {
            lines
                .iter()
                .position(|line| line.trim_start().starts_with(prefix))
                .unwrap_or_else(|| panic!("missing line starting with `{}`\n{}", prefix, formatted))
        };
        let find_line_containing = |needle: &str| -> usize {
            lines
                .iter()
                .position(|line| line.contains(needle))
                .unwrap_or_else(|| panic!("missing line containing `{needle}`\n{formatted}"))
        };

        let mixed_on_error_idx = find_line_containing("ON ERROR NULL -- policy");
        let on_empty_idx = find_line_starting_with("ON EMPTY NULL -- fallback");
        let sibling_idx = find_line_starting_with("e.empno");
        let from_idx = find_line_starting_with("FROM event_log e;");

        let mixed_on_error_trimmed = lines[mixed_on_error_idx].trim_start();
        let mixed_on_error_is_dedicated_line = mixed_on_error_trimmed
            .starts_with(") ON ERROR NULL -- policy")
            || mixed_on_error_trimmed.starts_with("ON ERROR NULL -- policy");
        if mixed_on_error_is_dedicated_line {
            assert_eq!(
                indent(lines[on_empty_idx]),
                indent(lines[mixed_on_error_idx]),
                "mixed-close JSON_VALUE option lines should stay on the shared function-local depth, got:\n{}",
                formatted
            );
        } else {
            assert!(
                lines[mixed_on_error_idx].contains("JSON_VALUE")
                    || lines[mixed_on_error_idx].contains("RETURNING")
                    || lines[mixed_on_error_idx].contains(") ON ERROR"),
                "mixed-close JSON_VALUE ON ERROR must remain in function-local expression context, got:\n{}",
                formatted
            );
        }
        assert!(
            indent(lines[on_empty_idx]) > indent(lines[from_idx]),
            "mixed-close JSON_VALUE ON EMPTY option should stay inside function parens, got:\n{}",
            formatted
        );
        assert_eq!(
            indent(lines[sibling_idx]),
            indent(lines[from_idx]).saturating_add(4),
            "SELECT-list sibling after mixed-close JSON_VALUE options should return to canonical list depth, got:\n{}",
            formatted
        );
        assert_eq!(
            SqlEditorWidget::format_for_auto_formatting(&formatted, false),
            formatted,
            "mixed-close JSON_VALUE option auto-formatting should be idempotent"
        );
    }

    #[test]
    fn format_for_auto_formatting_keeps_mixed_close_extract_from_inside_function_parens() {
        let source = r#"SELECT
    EXTRACT (
        (YEAR)
        FROM e.hire_date
    ) AS hire_year,
    e.empno
FROM emp e;"#;
        let formatted = SqlEditorWidget::format_for_auto_formatting(source, false);
        let lines: Vec<&str> = formatted.lines().collect();
        let indent = |line: &str| line.len().saturating_sub(line.trim_start().len());
        let outer_from_idx = lines
            .iter()
            .position(|line| line.trim_start().eq_ignore_ascii_case("FROM emp e;"))
            .unwrap_or_else(|| panic!("outer query FROM clause, got:\n{}", formatted));
        let function_from_idx = lines
            .iter()
            .position(|line| line.contains("FROM e.hire_date"))
            .unwrap_or_else(|| panic!("function-local EXTRACT FROM line, got:\n{}", formatted));

        if lines[function_from_idx]
            .trim_start()
            .starts_with("FROM e.hire_date")
        {
            assert!(
                indent(lines[function_from_idx]) > indent(lines[outer_from_idx]),
                "mixed-close EXTRACT FROM line should stay inside function parens, got:\n{}",
                formatted
            );
        }
        assert_eq!(
            SqlEditorWidget::format_for_auto_formatting(&formatted, false),
            formatted,
            "mixed-close EXTRACT FROM auto-formatting should be idempotent"
        );
    }

    #[test]
    fn format_for_auto_formatting_keeps_mixed_close_json_query_with_wrapper_inside_function_parens()
    {
        let source = r#"SELECT
    JSON_QUERY (
        e.payload,
        '$.items[*]'
        RETURNING VARCHAR2 (
            100
        ) WITH WRAPPER
    ) AS items_json,
    e.empno
FROM emp_json e;"#;
        let formatted = SqlEditorWidget::format_for_auto_formatting(source, false);
        let lines: Vec<&str> = formatted.lines().collect();
        let indent = |line: &str| line.len().saturating_sub(line.trim_start().len());
        let outer_from_idx = lines
            .iter()
            .position(|line| line.trim_start().eq_ignore_ascii_case("FROM emp_json e;"))
            .unwrap_or_else(|| panic!("outer query FROM clause, got:\n{}", formatted));
        let wrapper_idx = lines
            .iter()
            .position(|line| line.contains("WITH WRAPPER"))
            .unwrap_or_else(|| panic!("function-local WITH WRAPPER line, got:\n{}", formatted));

        if lines[wrapper_idx].trim_start().starts_with("WITH WRAPPER")
            || lines[wrapper_idx]
                .trim_start()
                .starts_with(") WITH WRAPPER")
        {
            assert!(
                indent(lines[wrapper_idx]) > indent(lines[outer_from_idx]),
                "mixed-close JSON_QUERY WITH WRAPPER should stay inside function parens, got:\n{}",
                formatted
            );
        }
        assert_eq!(
            SqlEditorWidget::format_for_auto_formatting(&formatted, false),
            formatted,
            "mixed-close JSON_QUERY WITH WRAPPER auto-formatting should be idempotent"
        );
    }

    #[test]
    fn format_for_auto_formatting_keeps_function_local_from_under_wrapped_case_frame() {
        let source = r#"SELECT
    EXTRACT (
        (CASE
            WHEN e.flag = 'Y' THEN YEAR
            ELSE MONTH
        END) -- wrapped case result
        FROM e.hire_date -- function-local from
    ) AS part_val,
    e.empno
FROM emp e;"#;
        let formatted = SqlEditorWidget::format_for_auto_formatting(source, false);
        let lines: Vec<&str> = formatted.lines().collect();
        let indent = |line: &str| line.len().saturating_sub(line.trim_start().len());
        let find_line_starting_with = |prefix: &str| -> usize {
            lines
                .iter()
                .position(|line| line.trim_start().starts_with(prefix))
                .unwrap_or_else(|| panic!("missing line starting with: {prefix}\n{formatted}"))
        };

        let function_from_idx = find_line_starting_with("FROM e.hire_date -- function-local from");
        let outer_from_idx = find_line_starting_with("FROM emp e;");
        let sibling_idx = find_line_starting_with("e.empno");

        assert!(
            indent(lines[function_from_idx]) > indent(lines[outer_from_idx]),
            "EXTRACT function-local FROM under wrapped CASE frame should stay inside the function parens, got:\n{}",
            formatted
        );
        assert_eq!(
            indent(lines[sibling_idx]),
            indent(lines[outer_from_idx]).saturating_add(4),
            "SELECT-list sibling after wrapped CASE EXTRACT should return to canonical list depth, got:\n{}",
            formatted
        );
        assert_eq!(
            SqlEditorWidget::format_for_auto_formatting(&formatted, false),
            formatted,
            "wrapped CASE EXTRACT auto-formatting should be idempotent"
        );
    }

    #[test]
    fn format_for_auto_formatting_keeps_json_exists_true_false_on_error_inside_function_parens() {
        let source = r#"SELECT
    JSON_EXISTS (
        e.payload,
        '$.items[*]'
        TRUE ON ERROR
        FALSE ON EMPTY
    ) AS has_items,
    e.empno
FROM emp_json e;"#;
        let formatted = SqlEditorWidget::format_for_auto_formatting(source, false);
        let lines: Vec<&str> = formatted.lines().collect();
        let indent = |line: &str| line.len().saturating_sub(line.trim_start().len());
        let find_line_starting_with = |prefix: &str| -> usize {
            lines
                .iter()
                .position(|line| line.trim_start().starts_with(prefix))
                .unwrap_or_else(|| panic!("missing line starting with: {prefix}\n{formatted}"))
        };

        assert!(
            formatted.contains("TRUE ON ERROR"),
            "JSON_EXISTS TRUE ON ERROR should stay inside the function call, got:\n{formatted}"
        );
        assert!(
            formatted.contains("FALSE ON EMPTY"),
            "JSON_EXISTS FALSE ON EMPTY should stay inside the function call, got:\n{formatted}"
        );
        assert!(
            !formatted.contains("\nTRUE ON ERROR"),
            "JSON_EXISTS TRUE ON ERROR should not be promoted to a top-level clause line, got:\n{formatted}"
        );
        assert!(
            !formatted.contains("\nFALSE ON EMPTY"),
            "JSON_EXISTS FALSE ON EMPTY should not be promoted to a top-level clause line, got:\n{formatted}"
        );

        let sibling_idx = find_line_starting_with("e.empno");
        let from_idx = find_line_starting_with("FROM emp_json e;");

        assert_eq!(
            indent(lines[sibling_idx]),
            indent(lines[from_idx]).saturating_add(4),
            "SELECT-list sibling after JSON_EXISTS TRUE/FALSE ON ERROR/ON EMPTY should return to canonical list depth, got:\n{formatted}"
        );
        assert_eq!(
            SqlEditorWidget::format_for_auto_formatting(&formatted, false),
            formatted,
            "JSON_EXISTS TRUE/FALSE ON ERROR/ON EMPTY auto-formatting should be idempotent"
        );
    }

    #[test]
    fn format_for_auto_formatting_keeps_mixed_close_json_exists_true_false_inside_function_parens()
    {
        let source = r#"SELECT
    JSON_EXISTS (
        e.payload,
        '$.items[*]'
        PASSING JSON_OBJECT (
            KEY 'k' VALUE e.empno
        ) TRUE ON ERROR -- policy
        FALSE ON EMPTY -- fallback
    ) AS has_items,
    e.empno
FROM emp_json e;"#;
        let formatted = SqlEditorWidget::format_for_auto_formatting(source, false);
        let lines: Vec<&str> = formatted.lines().collect();
        let indent = |line: &str| line.len().saturating_sub(line.trim_start().len());
        let find_line_starting_with = |prefix: &str| -> usize {
            lines
                .iter()
                .position(|line| line.trim_start().starts_with(prefix))
                .unwrap_or_else(|| panic!("missing line starting with: {prefix}\n{formatted}"))
        };
        let find_line_containing = |needle: &str| -> usize {
            lines
                .iter()
                .position(|line| line.contains(needle))
                .unwrap_or_else(|| panic!("missing line containing `{needle}`\n{formatted}"))
        };

        let mixed_on_error_idx = find_line_containing("TRUE ON ERROR -- policy");
        let on_empty_idx = find_line_starting_with("FALSE ON EMPTY -- fallback");
        let sibling_idx = find_line_starting_with("e.empno");
        let from_idx = find_line_starting_with("FROM emp_json e;");

        let mixed_on_error_trimmed = lines[mixed_on_error_idx].trim_start();
        let mixed_on_error_is_dedicated_line = mixed_on_error_trimmed
            .starts_with(") TRUE ON ERROR -- policy")
            || mixed_on_error_trimmed.starts_with("TRUE ON ERROR -- policy");
        if mixed_on_error_is_dedicated_line {
            assert_eq!(
                indent(lines[on_empty_idx]),
                indent(lines[mixed_on_error_idx]),
                "mixed-close JSON_EXISTS TRUE/FALSE option lines should stay on the shared function-local depth, got:\n{}",
                formatted
            );
        } else {
            assert!(
                lines[mixed_on_error_idx].contains("JSON_OBJECT")
                    || lines[mixed_on_error_idx].contains(") TRUE ON ERROR"),
                "mixed-close JSON_EXISTS TRUE ON ERROR must remain inside function-local expression context, got:\n{}",
                formatted
            );
        }
        assert!(
            indent(lines[on_empty_idx]) > indent(lines[from_idx]),
            "mixed-close JSON_EXISTS FALSE ON EMPTY should stay inside function parens, got:\n{}",
            formatted
        );
        assert_eq!(
            indent(lines[sibling_idx]),
            indent(lines[from_idx]).saturating_add(4),
            "SELECT-list sibling after mixed-close JSON_EXISTS options should return to canonical list depth, got:\n{}",
            formatted
        );
        assert_eq!(
            SqlEditorWidget::format_for_auto_formatting(&formatted, false),
            formatted,
            "mixed-close JSON_EXISTS TRUE/FALSE auto-formatting should be idempotent"
        );
    }

    #[test]
    fn format_for_auto_formatting_keeps_nested_subquery_json_value_on_error_inside_function_parens()
    {
        let source = r#"SELECT
    (
        SELECT JSON_VALUE (
            x.payload, '$.name' -- json path
            RETURNING VARCHAR2 (30)
            NULL ON ERROR
        )
        FROM event_log x
    ) AS name_txt,
    e.empno
FROM event_log e;"#;
        let formatted = SqlEditorWidget::format_for_auto_formatting(source, false);
        let lines: Vec<&str> = formatted.lines().collect();
        let indent = |line: &str| line.len().saturating_sub(line.trim_start().len());

        let returning_idx = lines
            .iter()
            .position(|line| line.trim_start().starts_with("RETURNING VARCHAR2 (30)"))
            .unwrap_or_else(|| {
                panic!(
                    "nested JSON_VALUE RETURNING option line, got:\n{}",
                    formatted
                )
            });
        let on_error_idx = lines
            .iter()
            .position(|line| line.contains("ON ERROR"))
            .unwrap_or_else(|| {
                panic!(
                    "nested JSON_VALUE ON ERROR option line, got:\n{}",
                    formatted
                )
            });
        let subquery_from_idx = lines
            .iter()
            .position(|line| line.trim_start().eq_ignore_ascii_case("FROM event_log x"))
            .unwrap_or_else(|| panic!("nested subquery FROM clause, got:\n{}", formatted));
        let outer_sibling_idx = lines
            .iter()
            .position(|line| line.trim_start() == "e.empno")
            .unwrap_or_else(|| {
                panic!(
                    "outer SELECT-list sibling after nested JSON_VALUE, got:\n{}",
                    formatted
                )
            });
        let outer_from_idx = lines
            .iter()
            .position(|line| line.trim_start().eq_ignore_ascii_case("FROM event_log e;"))
            .unwrap_or_else(|| panic!("outer query FROM clause, got:\n{}", formatted));

        for option_idx in [returning_idx, on_error_idx] {
            assert!(
                indent(lines[option_idx]) > indent(lines[subquery_from_idx]),
                "function-local JSON_VALUE option inside a nested subquery should stay deeper than the child query FROM clause, got:\n{}",
                formatted
            );
        }
        assert_eq!(
            indent(lines[outer_sibling_idx]),
            indent(lines[outer_from_idx]).saturating_add(4),
            "outer SELECT-list sibling after nested JSON_VALUE should return to the canonical list depth, got:\n{}",
            formatted
        );
        assert_eq!(
            SqlEditorWidget::format_for_auto_formatting(&formatted, false),
            formatted,
            "nested JSON_VALUE option auto-formatting should be idempotent"
        );
    }

    #[test]
    fn format_for_auto_formatting_keeps_nested_subquery_json_transform_operations_inside_function_parens(
    ) {
        let source = r#"SELECT
    (
        SELECT JSON_TRANSFORM (
            x.payload, -- source json
            INSERT '$.audit.user' = USER, -- audit stamp
            SET '$.status' = 'DONE'
        )
        FROM emp_json x
    ) AS payload2,
    e.empno
FROM emp_json e;"#;
        let formatted = SqlEditorWidget::format_for_auto_formatting(source, false);
        let lines: Vec<&str> = formatted.lines().collect();
        let indent = |line: &str| line.len().saturating_sub(line.trim_start().len());

        let function_insert_idx = lines
            .iter()
            .position(|line| {
                line.trim_start()
                    .starts_with("INSERT '$.audit.user' = USER")
            })
            .unwrap_or_else(|| {
                panic!(
                    "nested JSON_TRANSFORM INSERT option line, got:\n{}",
                    formatted
                )
            });
        let function_set_idx = lines
            .iter()
            .position(|line| line.trim_start().starts_with("SET '$.status' = 'DONE'"))
            .unwrap_or_else(|| {
                panic!("nested JSON_TRANSFORM SET option line, got:\n{}", formatted)
            });
        let subquery_from_idx = lines
            .iter()
            .position(|line| line.trim_start().eq_ignore_ascii_case("FROM emp_json x"))
            .unwrap_or_else(|| panic!("nested subquery FROM clause, got:\n{}", formatted));
        let outer_sibling_idx = lines
            .iter()
            .position(|line| line.trim_start() == "e.empno")
            .unwrap_or_else(|| {
                panic!(
                    "outer SELECT-list sibling after nested JSON_TRANSFORM, got:\n{}",
                    formatted
                )
            });
        let outer_from_idx = lines
            .iter()
            .position(|line| line.trim_start().eq_ignore_ascii_case("FROM emp_json e;"))
            .unwrap_or_else(|| panic!("outer query FROM clause, got:\n{}", formatted));

        for option_idx in [function_insert_idx, function_set_idx] {
            assert!(
                indent(lines[option_idx]) > indent(lines[subquery_from_idx]),
                "function-local JSON_TRANSFORM operation inside a nested subquery should stay deeper than the child query FROM clause, got:\n{}",
                formatted
            );
        }
        assert_eq!(
            indent(lines[outer_sibling_idx]),
            indent(lines[outer_from_idx]).saturating_add(4),
            "outer SELECT-list sibling after nested JSON_TRANSFORM should return to the canonical list depth, got:\n{}",
            formatted
        );
        assert_eq!(
            SqlEditorWidget::format_for_auto_formatting(&formatted, false),
            formatted,
            "nested JSON_TRANSFORM option auto-formatting should be idempotent"
        );
    }

    #[test]
    fn format_for_auto_formatting_keeps_multiline_json_transform_operations_inside_function_parens()
    {
        let source = r#"SELECT
    JSON_TRANSFORM (
        e.payload, -- source json
        INSERT '$.audit.user' = USER, -- audit stamp
        SET '$.status' = 'DONE'
    ) AS payload2,
    e.empno
FROM emp_json e;"#;
        let formatted = SqlEditorWidget::format_for_auto_formatting(source, false);
        let lines: Vec<&str> = formatted.lines().collect();
        let indent = |line: &str| line.len().saturating_sub(line.trim_start().len());

        let function_set_idx = lines
            .iter()
            .position(|line| line.trim_start().starts_with("SET '$.status' = 'DONE',"))
            .or_else(|| {
                lines
                    .iter()
                    .position(|line| line.trim_start().starts_with("SET '$.status' = 'DONE'"))
            })
            .unwrap_or_else(|| panic!("JSON_TRANSFORM SET option line, got:\n{}", formatted));
        let function_insert_idx = lines
            .iter()
            .position(|line| {
                line.trim_start()
                    .starts_with("INSERT '$.audit.user' = USER,")
            })
            .or_else(|| {
                lines.iter().position(|line| {
                    line.trim_start()
                        .starts_with("INSERT '$.audit.user' = USER")
                })
            })
            .unwrap_or_else(|| panic!("JSON_TRANSFORM INSERT option line, got:\n{}", formatted));
        let sibling_idx = lines
            .iter()
            .position(|line| line.trim_start() == "e.empno")
            .unwrap_or_else(|| {
                panic!(
                    "SELECT-list sibling after JSON_TRANSFORM, got:\n{}",
                    formatted
                )
            });
        let query_from_idx = lines
            .iter()
            .position(|line| line.trim_start().eq_ignore_ascii_case("FROM emp_json e;"))
            .unwrap_or_else(|| panic!("outer query FROM clause, got:\n{}", formatted));

        for option_idx in [function_set_idx, function_insert_idx] {
            assert!(
                indent(lines[option_idx]) > indent(lines[query_from_idx]),
                "function-local JSON_TRANSFORM operation line should not be promoted to outer clause depth, got:\n{}",
                formatted
            );
        }
        assert_eq!(
            indent(lines[sibling_idx]),
            indent(lines[query_from_idx]).saturating_add(4),
            "SELECT-list sibling after multiline JSON_TRANSFORM should return to list depth, got:\n{}",
            formatted
        );
        assert_eq!(
            SqlEditorWidget::format_for_auto_formatting(&formatted, false),
            formatted,
            "multiline JSON_TRANSFORM auto-formatting should be idempotent"
        );
    }

    // ── Bug candidate 4: Recursive CTE + SEARCH / CYCLE ──

    #[test]
    fn format_sql_recursive_cte_search_cycle_alignment() {
        let source = "WITH r (n) AS (SELECT 1 FROM dual UNION ALL SELECT n + 1 FROM r WHERE n < 3) SEARCH DEPTH FIRST BY n SET ord CYCLE n SET is_cycle TO 'Y' DEFAULT 'N' SELECT n, ord, is_cycle FROM r ORDER BY ord;";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        let lines: Vec<&str> = formatted.lines().collect();

        // SEARCH and CYCLE should each get their own line
        let search_idx = lines
            .iter()
            .position(|l| l.trim_start().starts_with("SEARCH"));
        let cycle_idx = lines
            .iter()
            .position(|l| l.trim_start().starts_with("CYCLE"));

        assert!(
            search_idx.is_some(),
            "SEARCH should be on its own line, got:\n{}",
            formatted
        );
        assert!(
            cycle_idx.is_some(),
            "CYCLE should be on its own line, got:\n{}",
            formatted
        );

        let search_idx = search_idx.unwrap();
        let cycle_idx = cycle_idx.unwrap();

        // SEARCH and CYCLE should be at the same indent level
        assert_eq!(
            leading_spaces(lines[search_idx]),
            leading_spaces(lines[cycle_idx]),
            "SEARCH and CYCLE should be at the same indent level, got:\n{}",
            formatted
        );

        // Final SELECT should be at top level (WITH depth 0)
        // The SELECT may have items on next lines, so find the SELECT line after CYCLE
        let select_idx = lines
            .iter()
            .enumerate()
            .skip(cycle_idx + 1)
            .find(|(_, l)| l.trim_start().starts_with("SELECT"))
            .map(|(i, _)| i);
        assert!(
            select_idx.is_some(),
            "Final SELECT should be on its own line after CYCLE, got:\n{}",
            formatted
        );
        assert_eq!(
            leading_spaces(lines[select_idx.unwrap()]),
            0,
            "Final SELECT after SEARCH/CYCLE should be at indent 0, got:\n{}",
            formatted
        );

        // Idempotence
        let formatted_again = SqlEditorWidget::format_sql_basic(&formatted);
        assert_eq!(
            formatted, formatted_again,
            "Recursive CTE SEARCH/CYCLE formatting should be idempotent, got:\n{}",
            formatted_again
        );
    }

    #[test]
    fn format_sql_nested_recursive_cte_search_cycle_scopes_outer_and_inner_clauses() {
        let source = "WITH outer_r (id, parent_id) AS (SELECT id, parent_id FROM (WITH inner_r (id) AS (SELECT 1 FROM dual UNION ALL SELECT id + 1 FROM inner_r WHERE id < 2) SEARCH DEPTH FIRST BY id SET inner_ord CYCLE id SET inner_cycle TO 'Y' DEFAULT 'N' SELECT id, 0 AS parent_id FROM inner_r) nested_src UNION ALL SELECT id + 1, parent_id FROM outer_r WHERE id < 3) SEARCH DEPTH FIRST BY id SET outer_ord CYCLE id SET outer_cycle TO 'Y' DEFAULT 'N' SELECT id, outer_ord, outer_cycle FROM outer_r ORDER BY outer_ord;";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        let lines: Vec<&str> = formatted.lines().collect();

        let inner_search_idx = lines
            .iter()
            .position(|line| {
                line.trim_start()
                    .starts_with("SEARCH DEPTH FIRST BY id SET inner_ord")
            })
            .expect("inner SEARCH line");
        let inner_cycle_idx = lines
            .iter()
            .position(|line| {
                line.trim_start()
                    .starts_with("CYCLE id SET inner_cycle TO 'Y' DEFAULT 'N'")
            })
            .expect("inner CYCLE line");
        let outer_search_idx = lines
            .iter()
            .rposition(|line| {
                line.trim_start()
                    .starts_with("SEARCH DEPTH FIRST BY id SET outer_ord")
            })
            .expect("outer SEARCH line");
        let outer_cycle_idx = lines
            .iter()
            .rposition(|line| {
                line.trim_start()
                    .starts_with("CYCLE id SET outer_cycle TO 'Y' DEFAULT 'N'")
            })
            .expect("outer CYCLE line");
        let outer_select_idx = lines
            .iter()
            .enumerate()
            .skip(outer_cycle_idx.saturating_add(1))
            .find(|(_, line)| line.trim_start().starts_with("SELECT"))
            .map(|(idx, _)| idx)
            .expect("outer SELECT line");

        assert_eq!(
            leading_spaces(lines[inner_search_idx]),
            leading_spaces(lines[inner_cycle_idx]),
            "Inner SEARCH/CYCLE should stay aligned to the inner recursive WITH depth, got:\n{}",
            formatted
        );
        assert_eq!(
            leading_spaces(lines[outer_search_idx]),
            leading_spaces(lines[outer_cycle_idx]),
            "Outer SEARCH/CYCLE should stay aligned to the outer recursive WITH depth, got:\n{}",
            formatted
        );
        assert!(
            leading_spaces(lines[inner_search_idx]) > leading_spaces(lines[outer_search_idx]),
            "Nested SEARCH/CYCLE should remain deeper than the outer recursive WITH clauses, got:\n{}",
            formatted
        );
        assert_eq!(
            leading_spaces(lines[outer_select_idx]),
            0,
            "Outer main SELECT should return to top-level depth after nested SEARCH/CYCLE clauses, got:\n{}",
            formatted
        );
    }

    // ── Bug candidate 5: MODEL clause subclauses ──

    #[test]
    fn format_sql_model_subclauses_consistent_indent() {
        let source = "SELECT country, year, amount FROM sales MODEL PARTITION BY (country) DIMENSION BY (year) MEASURES (amount) RULES UPSERT SEQUENTIAL ORDER (amount[2026] = amount[2025] * 1.1);";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        let lines: Vec<&str> = formatted.lines().collect();

        let model_idx = lines
            .iter()
            .position(|l| l.trim_start().starts_with("MODEL"))
            .unwrap();
        let partition_idx = lines
            .iter()
            .position(|l| l.trim_start().starts_with("PARTITION BY"))
            .unwrap();
        let dimension_idx = lines
            .iter()
            .position(|l| l.trim_start().starts_with("DIMENSION BY"))
            .unwrap();
        let measures_idx = lines
            .iter()
            .position(|l| l.trim_start().starts_with("MEASURES"))
            .unwrap();
        let rules_idx = lines
            .iter()
            .position(|l| l.trim_start().starts_with("RULES"))
            .unwrap();

        // All subclauses should share the same indent level
        assert_eq!(
            leading_spaces(lines[partition_idx]),
            leading_spaces(lines[dimension_idx]),
            "PARTITION BY and DIMENSION BY should be at the same indent, got:\n{}",
            formatted
        );
        assert_eq!(
            leading_spaces(lines[dimension_idx]),
            leading_spaces(lines[measures_idx]),
            "DIMENSION BY and MEASURES should be at the same indent, got:\n{}",
            formatted
        );
        assert_eq!(
            leading_spaces(lines[measures_idx]),
            leading_spaces(lines[rules_idx]),
            "MEASURES and RULES should be at the same indent, got:\n{}",
            formatted
        );

        // Subclauses should be deeper than MODEL
        assert!(
            leading_spaces(lines[partition_idx]) > leading_spaces(lines[model_idx]),
            "MODEL subclauses should be indented deeper than MODEL, got:\n{}",
            formatted
        );

        // Idempotence
        let formatted_again = SqlEditorWidget::format_sql_basic(&formatted);
        assert_eq!(
            formatted, formatted_again,
            "MODEL formatting should be idempotent, got:\n{}",
            formatted_again
        );
    }

    // ── Bug candidate 6: PIVOT / UNPIVOT ──

    #[test]
    fn format_sql_pivot_for_in_alignment() {
        let source = "SELECT * FROM (SELECT deptno, job, sal FROM emp) PIVOT (SUM(sal) FOR job IN ('CLERK' AS clerk, 'MANAGER' AS manager));";
        let formatted = SqlEditorWidget::format_sql_basic(source);

        // PIVOT should get its own clause line
        assert!(
            formatted.contains("\nPIVOT (") || formatted.contains("PIVOT ("),
            "PIVOT should be present and formatted, got:\n{}",
            formatted
        );

        // Idempotence
        let formatted_again = SqlEditorWidget::format_sql_basic(&formatted);
        assert_eq!(
            formatted, formatted_again,
            "PIVOT formatting should be idempotent, got:\n{}",
            formatted_again
        );
    }

    #[test]
    fn format_sql_pivot_xml_body_stays_multiline_and_idempotent() {
        let source = "SELECT * FROM (SELECT deptno, job, sal FROM emp) PIVOT XML (SUM(sal) AS sum_sal FOR job IN ('CLERK' AS clerk, 'MANAGER' AS manager));";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        let lines: Vec<&str> = formatted.lines().collect();
        let indent = |line: &str| line.len().saturating_sub(line.trim_start().len());
        let pivot_idx = lines
            .iter()
            .position(|line| line.trim_start() == "PIVOT XML (")
            .unwrap_or(0);
        let sum_idx = lines
            .iter()
            .position(|line| line.trim_start() == "SUM (sal) AS sum_sal")
            .unwrap_or(0);
        let for_idx = lines
            .iter()
            .position(|line| line.trim_start().starts_with("FOR job IN"))
            .unwrap_or(0);

        assert_eq!(
            indent(lines[sum_idx]),
            indent(lines[pivot_idx]).saturating_add(4),
            "PIVOT XML body should be one level deeper than the owner line, got:\n{}",
            formatted
        );
        assert_eq!(
            indent(lines[for_idx]),
            indent(lines[sum_idx]),
            "PIVOT XML FOR line should stay aligned with the aggregate line, got:\n{}",
            formatted
        );
        assert_eq!(
            SqlEditorWidget::format_sql_basic(&formatted),
            formatted,
            "PIVOT XML formatting should remain idempotent"
        );
    }

    #[test]
    fn format_sql_unpivot_include_nulls_body_stays_multiline_and_idempotent() {
        let source = "SELECT * FROM pivoted UNPIVOT INCLUDE NULLS (sal_amt FOR dept_tag IN (d10 AS '10', d20 AS '20'));";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        let lines: Vec<&str> = formatted.lines().collect();
        let indent = |line: &str| line.len().saturating_sub(line.trim_start().len());
        let unpivot_idx = lines
            .iter()
            .position(|line| line.trim_start() == "UNPIVOT INCLUDE NULLS (")
            .unwrap_or(0);
        let value_idx = lines
            .iter()
            .position(|line| line.trim_start() == "sal_amt")
            .unwrap_or(0);
        let for_idx = lines
            .iter()
            .position(|line| line.trim_start().starts_with("FOR dept_tag IN"))
            .unwrap_or(0);

        assert_eq!(
            indent(lines[value_idx]),
            indent(lines[unpivot_idx]).saturating_add(4),
            "UNPIVOT INCLUDE NULLS body should be one level deeper than the owner line, got:\n{}",
            formatted
        );
        assert_eq!(
            indent(lines[for_idx]),
            indent(lines[value_idx]),
            "UNPIVOT INCLUDE NULLS FOR line should stay aligned with the value line, got:\n{}",
            formatted
        );
        assert_eq!(
            SqlEditorWidget::format_sql_basic(&formatted),
            formatted,
            "UNPIVOT INCLUDE NULLS formatting should remain idempotent"
        );
    }

    // ── Bug candidate 7: WITH FUNCTION / WITH PROCEDURE ──

    #[test]
    fn format_sql_with_function_body_alignment() {
        let source = "WITH\n  FUNCTION f(p NUMBER) RETURN NUMBER IS\n  BEGIN\n    RETURN p * 2;\n  END;\nSELECT f(10) AS v\nFROM dual;";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        let lines: Vec<&str> = formatted.lines().collect();

        // WITH FUNCTION should be present
        assert!(
            formatted.contains("WITH")
                && formatted.contains("FUNCTION")
                && formatted.contains("BEGIN")
                && formatted.contains("END"),
            "WITH FUNCTION block should be fully present, got:\n{}",
            formatted
        );

        // Final SELECT should be at top level
        let select_idx = lines.iter().position(|l| {
            let t = l.trim_start();
            t.starts_with("SELECT f(") || t.starts_with("SELECT f (")
        });
        assert!(
            select_idx.is_some(),
            "Final SELECT should be on its own line, got:\n{}",
            formatted
        );
        assert_eq!(
            leading_spaces(lines[select_idx.unwrap()]),
            0,
            "Final SELECT after WITH FUNCTION should be at indent 0, got:\n{}",
            formatted
        );

        // Idempotence
        let formatted_again = SqlEditorWidget::format_sql_basic(&formatted);
        assert_eq!(
            formatted, formatted_again,
            "WITH FUNCTION formatting should be idempotent, got:\n{}",
            formatted_again
        );
    }

    #[test]
    fn format_sql_with_function_body_does_not_leak_plsql_context_into_for_update() {
        let source = "WITH FUNCTION f RETURN NUMBER IS BEGIN RETURN 1; END; SELECT d.dummy FROM dual d FOR UPDATE OF d.dummy NOWAIT;";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        let lines: Vec<&str> = formatted.lines().collect();

        let select_idx = lines
            .iter()
            .position(|line| line.trim_start().starts_with("SELECT d.dummy"))
            .expect("main SELECT line");
        let for_update_idx = lines
            .iter()
            .position(|line| {
                line.trim_start()
                    .starts_with("FOR UPDATE OF d.dummy NOWAIT")
            })
            .expect("FOR UPDATE line");

        assert_eq!(
            leading_spaces(lines[select_idx]),
            0,
            "Main SELECT after WITH FUNCTION should return to top-level depth, got:\n{}",
            formatted
        );
        assert_eq!(
            leading_spaces(lines[for_update_idx]),
            0,
            "FOR UPDATE after WITH FUNCTION should stay at SQL clause depth instead of inheriting PL/SQL block depth, got:\n{}",
            formatted
        );
        assert!(
            formatted.contains("FOR UPDATE OF d.dummy NOWAIT"),
            "FOR UPDATE OF ... NOWAIT should stay inline after WITH FUNCTION, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_sql_with_function_local_function_body_stays_attached_to_main_query() {
        let source = "WITH\n  FUNCTION outer_f RETURN NUMBER IS\n    FUNCTION inner_f RETURN NUMBER IS\n    BEGIN\n      RETURN 1;\n    END inner_f;\n  BEGIN\n    RETURN inner_f();\n  END outer_f;\nSELECT outer_f() AS v FROM dual;";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        let lines: Vec<&str> = formatted.lines().collect();
        let leading_spaces = |line: &str| line.len().saturating_sub(line.trim_start().len());

        let end_inner_idx = lines
            .iter()
            .position(|line| line.trim_start().starts_with("END inner_f;"))
            .expect("local function END line");
        let outer_begin_idx = lines
            .iter()
            .enumerate()
            .skip(end_inner_idx.saturating_add(1))
            .find(|(_, line)| line.trim_start() == "BEGIN")
            .map(|(idx, _)| idx)
            .expect("outer function BEGIN line");
        let end_outer_idx = lines
            .iter()
            .position(|line| line.trim_start().starts_with("END outer_f;"))
            .expect("outer function END line");
        let select_idx = lines
            .iter()
            .position(|line| line.trim_start().starts_with("SELECT outer_f"))
            .expect("main SELECT line");

        assert!(
            end_inner_idx < outer_begin_idx && outer_begin_idx < end_outer_idx,
            "nested local function END must not close the outer WITH FUNCTION body early, got:\n{}",
            formatted
        );
        assert!(
            end_outer_idx < select_idx,
            "main SELECT must stay after the outer WITH FUNCTION END, got:\n{}",
            formatted
        );
        assert_eq!(
            leading_spaces(lines[outer_begin_idx]),
            4,
            "outer function BEGIN should remain one level under WITH, got:\n{}",
            formatted
        );
        assert_eq!(
            leading_spaces(lines[select_idx]),
            0,
            "main SELECT after nested local function should return to top-level depth, got:\n{}",
            formatted
        );

        let formatted_again = SqlEditorWidget::format_sql_basic(&formatted);
        assert_eq!(
            formatted, formatted_again,
            "nested local function formatting should be idempotent, got:\n{}",
            formatted_again
        );
    }

    #[test]
    fn format_sql_with_function_multi_parameter_header_stays_attached_to_main_query() {
        let source = "WITH FUNCTION add_two (p_left NUMBER, p_right NUMBER) RETURN NUMBER IS BEGIN RETURN p_left + p_right; END; SELECT add_two(1, 2) AS v FROM dual;";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        let lines: Vec<&str> = formatted.lines().collect();
        let select_idx = lines
            .iter()
            .position(|line| line.trim_start().starts_with("SELECT add_two"))
            .expect("main SELECT line");

        assert!(
            formatted.contains("FUNCTION add_two")
                && formatted.contains("p_left NUMBER")
                && formatted.contains("p_right NUMBER"),
            "WITH FUNCTION header with multi-parameter list should stay intact, got:\n{}",
            formatted
        );
        assert_eq!(
            lines[select_idx]
                .len()
                .saturating_sub(lines[select_idx].trim_start().len()),
            0,
            "main SELECT after multi-parameter WITH FUNCTION should return to top-level depth, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_sql_with_package_member_function_body_stays_attached_to_main_query() {
        let source = "WITH\n  PACKAGE pkg_demo AS\n    FUNCTION f RETURN NUMBER IS\n    BEGIN\n      RETURN 1;\n    END f;\n  END pkg_demo;\nSELECT pkg_demo.f() AS v FROM dual;";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        let lines: Vec<&str> = formatted.lines().collect();
        let leading_spaces = |line: &str| line.len().saturating_sub(line.trim_start().len());

        let end_member_idx = lines
            .iter()
            .position(|line| line.trim_start().starts_with("END f;"))
            .expect("member function END line");
        let end_package_idx = lines
            .iter()
            .position(|line| line.trim_start().starts_with("END pkg_demo;"))
            .expect("package END line");
        let select_idx = lines
            .iter()
            .position(|line| line.trim_start().starts_with("SELECT pkg_demo.f"))
            .expect("main SELECT line");

        assert!(
            end_member_idx < end_package_idx,
            "member routine END must stay inside the WITH PACKAGE body, got:\n{}",
            formatted
        );
        assert!(
            end_package_idx < select_idx,
            "main SELECT must stay after END pkg_demo, got:\n{}",
            formatted
        );
        assert_eq!(
            leading_spaces(lines[end_package_idx]),
            4,
            "END pkg_demo should stay indented under WITH, got:\n{}",
            formatted
        );
        assert_eq!(
            leading_spaces(lines[select_idx]),
            0,
            "main SELECT after WITH PACKAGE member body should return to top-level depth, got:\n{}",
            formatted
        );

        let formatted_again = SqlEditorWidget::format_sql_basic(&formatted);
        assert_eq!(
            formatted, formatted_again,
            "WITH PACKAGE member-body formatting should be idempotent, got:\n{}",
            formatted_again
        );
    }

    // ── Bug analysis test cases: statement split and indent quality ──

    // Bug candidate 1: MATCH_RECOGNIZE DEFINE A AS ... (statement split)
    #[test]
    fn format_sql_basic_match_recognize_define_multiline_stays_single_statement() {
        let source = "SELECT *\nFROM sales\nMATCH_RECOGNIZE (\n  PARTITION BY customer_id\n  ORDER BY sale_date\n  MEASURES CLASSIFIER() AS cls\n  PATTERN (A B+)\n  DEFINE A AS amount < 100,\n         B AS amount >= 100\n);";
        let items = crate::db::QueryExecutor::split_format_items(source);
        let stmt_count = items
            .iter()
            .filter(|item| matches!(item, crate::db::FormatItem::Statement(_)))
            .count();
        let tool_count = items
            .iter()
            .filter(|item| matches!(item, crate::db::FormatItem::ToolCommand(_)))
            .count();
        assert_eq!(
            stmt_count, 1,
            "MATCH_RECOGNIZE with multiline DEFINE should be one statement, got {} statements and {} tool commands from: {:?}",
            stmt_count, tool_count, items
        );
        assert_eq!(
            tool_count, 0,
            "DEFINE inside MATCH_RECOGNIZE must not be parsed as tool command, got: {:?}",
            items
        );

        let formatted = SqlEditorWidget::format_sql_basic(source);
        assert!(
            formatted.contains("DEFINE"),
            "DEFINE should remain in the formatted output, got:\n{}",
            formatted
        );
        // Idempotence
        let formatted_again = SqlEditorWidget::format_sql_basic(&formatted);
        assert_eq!(
            formatted, formatted_again,
            "MATCH_RECOGNIZE DEFINE formatting should be idempotent.\nFirst:\n{}\nSecond:\n{}",
            formatted, formatted_again
        );
    }

    // Bug candidate 2: ALTER SYSTEM SET (statement split)
    #[test]
    fn format_sql_basic_alter_system_set_stays_single_statement() {
        let source = "ALTER SYSTEM\nSET optimizer_mode = ALL_ROWS;";
        let items = crate::db::QueryExecutor::split_format_items(source);
        let stmt_count = items
            .iter()
            .filter(|item| matches!(item, crate::db::FormatItem::Statement(_)))
            .count();
        let tool_count = items
            .iter()
            .filter(|item| matches!(item, crate::db::FormatItem::ToolCommand(_)))
            .count();
        assert_eq!(
            stmt_count, 1,
            "ALTER SYSTEM SET should be one statement, got {} statements and {} tool commands from: {:?}",
            stmt_count, tool_count, items
        );
        assert_eq!(
            tool_count, 0,
            "SET inside ALTER SYSTEM must not be parsed as tool command, got: {:?}",
            items
        );

        let formatted = SqlEditorWidget::format_sql_basic(source);
        assert!(
            formatted.contains("ALTER SYSTEM"),
            "ALTER SYSTEM should be in output, got:\n{}",
            formatted
        );
        assert!(
            formatted.contains("SET"),
            "SET should be in output, got:\n{}",
            formatted
        );
        // Idempotence
        let formatted_again = SqlEditorWidget::format_sql_basic(&formatted);
        assert_eq!(
            formatted, formatted_again,
            "ALTER SYSTEM SET formatting should be idempotent.\nFirst:\n{}\nSecond:\n{}",
            formatted, formatted_again
        );
    }

    // Bug candidate 2b: ALTER SYSTEM SET with SQL*Plus-overlapping parameter names
    #[test]
    fn split_format_items_alter_system_set_long_not_tool_command() {
        // "SET LONG 100" would match SQL*Plus SET LONG, but should stay as part of ALTER SYSTEM
        let source = "ALTER SYSTEM\nSET LONG 100;";
        let items = crate::db::QueryExecutor::split_format_items(source);
        let stmt_count = items
            .iter()
            .filter(|item| matches!(item, crate::db::FormatItem::Statement(_)))
            .count();
        let tool_count = items
            .iter()
            .filter(|item| matches!(item, crate::db::FormatItem::ToolCommand(_)))
            .count();
        assert_eq!(
            stmt_count, 1,
            "ALTER SYSTEM SET LONG should be one statement, got {} stmts + {} tool cmds: {:?}",
            stmt_count, tool_count, items
        );
        assert_eq!(
            tool_count, 0,
            "SET LONG inside ALTER SYSTEM must not be a tool command, got: {:?}",
            items
        );
    }

    // Bug candidate 3: MERGE branch alignment
    #[test]
    fn format_sql_basic_merge_using_and_when_branches_alignment() {
        let source = "MERGE INTO tgt t USING src s ON (t.id = s.id) WHEN MATCHED THEN UPDATE SET t.val = s.val, t.updated_at = SYSTIMESTAMP WHEN NOT MATCHED THEN INSERT (id, val, updated_at) VALUES (s.id, s.val, SYSTIMESTAMP);";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        let lines: Vec<&str> = formatted.lines().collect();
        let leading = |line: &str| line.len().saturating_sub(line.trim_start().len());
        let find_line = |prefix: &str| -> Option<&str> {
            lines
                .iter()
                .copied()
                .find(|l| l.trim_start().starts_with(prefix))
        };

        // USING should be present and at base depth (same as MERGE)
        let merge_line = find_line("MERGE INTO").unwrap_or("");
        let using_line = find_line("USING");
        let on_line = find_line("ON (").unwrap_or("");
        assert!(
            !merge_line.is_empty() && using_line.is_some() && !on_line.is_empty(),
            "MERGE/USING/ON lines should all be present, got:\n{}",
            formatted
        );
        assert_eq!(
            leading(using_line.unwrap_or("")),
            leading(merge_line),
            "USING should stay on the MERGE base depth, got:\n{}",
            formatted
        );
        assert_eq!(
            leading(on_line),
            leading(merge_line).saturating_add(4),
            "ON should stay one level deeper than the MERGE base depth, got:\n{}",
            formatted
        );

        // Both WHEN branches at same depth
        let when_matched = find_line("WHEN MATCHED THEN").unwrap_or("");
        let when_not_matched = find_line("WHEN NOT MATCHED THEN").unwrap_or("");
        assert!(
            !when_matched.is_empty() && !when_not_matched.is_empty(),
            "Both WHEN branches should be present, got:\n{}",
            formatted
        );
        assert_eq!(
            leading(when_matched),
            leading(when_not_matched),
            "WHEN MATCHED and WHEN NOT MATCHED should be at same depth, got:\n{}",
            formatted
        );
        assert_eq!(
            leading(when_matched),
            leading(merge_line),
            "WHEN branches should return to the MERGE base depth after ON, got:\n{}",
            formatted
        );

        // UPDATE SET and INSERT should be indented under their WHEN
        let update_line = find_line("UPDATE SET");
        let insert_line = find_line("INSERT (");
        assert!(
            update_line.is_some() && insert_line.is_some(),
            "UPDATE SET and INSERT should be present, got:\n{}",
            formatted
        );
        assert!(
            leading(update_line.unwrap()) > leading(when_matched),
            "UPDATE SET should be deeper than WHEN MATCHED, got:\n{}",
            formatted
        );

        // Idempotence
        let formatted_again = SqlEditorWidget::format_sql_basic(&formatted);
        assert_eq!(
            formatted, formatted_again,
            "MERGE formatting should be idempotent.\nFirst:\n{}\nSecond:\n{}",
            formatted, formatted_again
        );
    }

    #[test]
    fn format_sql_basic_merge_update_set_continuation_uses_branch_body_depth() {
        let source = r#"MERGE INTO qt_fmt_emp t
USING (
        WITH recent_sales AS (
            SELECT
                s.emp_id,
                SUM ((s.qty * s.unit_price) - s.discount_amt + s.tax_amt) AS recent_amt
            FROM qt_fmt_sales s
            WHERE s.sale_date >= DATE '2024-03-01'
            GROUP BY s.emp_id
        ),
    scored AS (
            SELECT
                e.emp_id,
                e.note_text,
                rs.recent_amt,
                CASE
                    WHEN NVL (rs.recent_amt, 0) >= 50000 THEN ' | HOT'
                    WHEN NVL (rs.recent_amt, 0) >= 20000 THEN ' | WARM'
                    ELSE ' | COLD'
                END AS score_tag
            FROM qt_fmt_emp e
            LEFT JOIN recent_sales rs
                ON rs.emp_id = e.emp_id
            WHERE e.dept_id = 30
        )
        SELECT
            s.emp_id,
            SUBSTR (NVL (s.note_text, 'sales-profile') || s.score_tag || ' | recent=' || TO_CHAR (NVL (s.recent_amt, 0)), 1, 4000) AS new_note_text
        FROM scored s
    ) src
    ON (t.emp_id = src.emp_id)
WHEN MATCHED THEN
    UPDATE SET t.note_text = src.new_note_text,
        t.updated_at = SYSTIMESTAMP;"#;
        let formatted = SqlEditorWidget::format_sql_basic(source);
        let lines: Vec<&str> = formatted.lines().collect();
        let leading = |line: &str| line.len().saturating_sub(line.trim_start().len());
        let find_line = |prefix: &str| -> Option<&str> {
            lines
                .iter()
                .copied()
                .find(|line| line.trim_start().starts_with(prefix))
        };

        let when_matched = find_line("WHEN MATCHED THEN").unwrap_or("");
        let update_line = find_line("UPDATE SET t.note_text = src.new_note_text,").unwrap_or("");
        let updated_at_line = find_line("t.updated_at = SYSTIMESTAMP;").unwrap_or("");
        let with_line = find_line("WITH recent_sales AS (").unwrap_or("");
        let scored_line = find_line("scored AS (").unwrap_or("");

        assert!(
            !when_matched.is_empty()
                && !update_line.is_empty()
                && !updated_at_line.is_empty()
                && !with_line.is_empty()
                && !scored_line.is_empty(),
            "expected MERGE branch lines to be present, got:\n{}",
            formatted
        );
        assert_eq!(
            leading(scored_line),
            leading(with_line),
            "nested USING CTE siblings should align on the same WITH base depth, got:\n{}",
            formatted
        );
        assert_eq!(
            leading(update_line),
            leading(when_matched).saturating_add(4),
            "UPDATE SET should stay one structural level deeper than WHEN MATCHED, got:\n{}",
            formatted
        );
        assert_eq!(
            leading(updated_at_line),
            leading(update_line).saturating_add(4),
            "MERGE UPDATE SET continuation should use the branch body list depth, got:\n{}",
            formatted
        );
    }

    // Bug candidate 4: JOIN ... USING (...) alignment
    #[test]
    fn format_sql_basic_join_using_same_depth_as_join_on() {
        let source = "SELECT * FROM a JOIN b USING (id, tenant_id) LEFT OUTER JOIN c USING (id, tenant_id) JOIN d ON d.id = a.id AND d.tenant_id = a.tenant_id;";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        let lines: Vec<&str> = formatted.lines().collect();
        let leading = |line: &str| line.len().saturating_sub(line.trim_start().len());

        // All JOIN keywords should be at the same indent
        let join_lines: Vec<&&str> = lines
            .iter()
            .filter(|l| {
                let t = l.trim_start();
                t.starts_with("JOIN ")
                    || t.starts_with("LEFT OUTER JOIN ")
                    || t.starts_with("LEFT JOIN ")
                    || t.starts_with("RIGHT JOIN ")
                    || t.starts_with("INNER JOIN ")
            })
            .collect();

        assert!(
            join_lines.len() >= 3,
            "Should have at least 3 JOIN lines, got {} in:\n{}",
            join_lines.len(),
            formatted
        );

        let first_indent = leading(join_lines[0]);
        for join_line in &join_lines {
            assert_eq!(
                leading(join_line),
                first_indent,
                "All JOINs should be at the same indent level, got:\n{}",
                formatted
            );
        }

        // Idempotence
        let formatted_again = SqlEditorWidget::format_sql_basic(&formatted);
        assert_eq!(
            formatted, formatted_again,
            "JOIN USING formatting should be idempotent.\nFirst:\n{}\nSecond:\n{}",
            formatted, formatted_again
        );
    }

    // Bug candidate 5: Window function OVER (...) internal alignment
    #[test]
    fn format_sql_basic_over_partition_by_internal_indent() {
        let source = "SELECT deptno, ROW_NUMBER() OVER (PARTITION BY deptno ORDER BY sal DESC, empno) AS rn, SUM(sal) OVER (PARTITION BY deptno ORDER BY hiredate ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW) AS run_sal FROM emp;";
        let formatted = SqlEditorWidget::format_sql_basic(source);

        // Basic structural checks: OVER, PARTITION BY, ORDER BY should be present
        assert!(
            formatted.contains("OVER"),
            "OVER should be in output, got:\n{}",
            formatted
        );
        assert!(
            formatted.contains("PARTITION BY"),
            "PARTITION BY should be in output, got:\n{}",
            formatted
        );

        // Idempotence
        let formatted_again = SqlEditorWidget::format_sql_basic(&formatted);
        assert_eq!(
            formatted, formatted_again,
            "Window function OVER formatting should be idempotent.\nFirst:\n{}\nSecond:\n{}",
            formatted, formatted_again
        );
    }

    // Bug candidate 6: MODEL internal subclauses alignment
    #[test]
    fn format_sql_basic_model_subclauses_alignment() {
        let source = "SELECT * FROM sales MODEL PARTITION BY (country) DIMENSION BY (prod_id) MEASURES (amount) RULES UPDATE (amount[ANY] = amount[CV()] * 1.1);";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        let lines: Vec<&str> = formatted.lines().collect();
        let leading = |line: &str| line.len().saturating_sub(line.trim_start().len());
        let find_line = |prefix: &str| -> Option<&str> {
            lines
                .iter()
                .copied()
                .find(|l| l.trim_start().starts_with(prefix))
        };

        // MODEL subclauses should each be on their own line
        let partition_line = find_line("PARTITION BY");
        let dimension_line = find_line("DIMENSION BY");
        let measures_line = find_line("MEASURES");
        let rules_line = find_line("RULES");

        assert!(
            partition_line.is_some()
                && dimension_line.is_some()
                && measures_line.is_some()
                && rules_line.is_some(),
            "MODEL subclauses should each appear on their own line, got:\n{}",
            formatted
        );

        // All subclauses should be at the same depth (indented under MODEL)
        let p = leading(partition_line.unwrap());
        let d = leading(dimension_line.unwrap());
        let m = leading(measures_line.unwrap());
        let r = leading(rules_line.unwrap());
        assert_eq!(
            p, d,
            "PARTITION BY and DIMENSION BY should be at same depth, got:\n{}",
            formatted
        );
        assert_eq!(
            d, m,
            "DIMENSION BY and MEASURES should be at same depth, got:\n{}",
            formatted
        );
        assert_eq!(
            m, r,
            "MEASURES and RULES should be at same depth, got:\n{}",
            formatted
        );

        // Idempotence
        let formatted_again = SqlEditorWidget::format_sql_basic(&formatted);
        assert_eq!(
            formatted, formatted_again,
            "MODEL subclauses formatting should be idempotent.\nFirst:\n{}\nSecond:\n{}",
            formatted, formatted_again
        );
    }

    // ── Regression tests: PIVOT/UNPIVOT and related formatting ──

    #[test]
    fn format_sql_basic_for_update_skip_locked_stays_on_one_line() {
        let source = "select * from emp e where e.deptno = 10 for update skip locked;";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        assert!(
            formatted.contains("FOR UPDATE SKIP LOCKED"),
            "FOR UPDATE SKIP LOCKED should stay on one line, got:\n{}",
            formatted
        );
        let formatted_again = SqlEditorWidget::format_sql_basic(&formatted);
        assert_eq!(
            formatted, formatted_again,
            "FOR UPDATE SKIP LOCKED formatting should be idempotent"
        );
    }

    #[test]
    fn format_sql_basic_for_update_of_nowait_stays_on_one_line() {
        let source =
            "select e.empno, e.ename from emp e where e.deptno = 10 for update of e.sal nowait;";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        assert!(
            formatted.contains("FOR UPDATE OF e.sal NOWAIT"),
            "FOR UPDATE OF ... NOWAIT should stay on one line, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_sql_basic_merge_using_clause_alignment() {
        let source = "merge into tgt t using src s on (t.id = s.id) when matched then update set t.val = s.val when not matched then insert (id, val) values (s.id, s.val);";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        assert!(
            formatted.contains("USING src s"),
            "USING should appear as a clause, got:\n{}",
            formatted
        );
        let formatted_again = SqlEditorWidget::format_sql_basic(&formatted);
        assert_eq!(
            formatted, formatted_again,
            "MERGE USING formatting should be idempotent"
        );
    }

    #[test]
    fn format_sql_basic_recursive_cte_search_cycle_inline() {
        let source = "with r as (select 1 as id, cast(null as number) as parent_id from dual union all select t.id, t.parent_id from tree t join r on t.parent_id = r.id) search depth first by id set ord cycle id set is_cycle to 'Y' default 'N' select * from r;";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        assert!(
            formatted.contains("\nSEARCH DEPTH FIRST BY id SET ord"),
            "SEARCH should start on its own line with SET inline, got:\n{}",
            formatted
        );
        assert!(
            formatted.contains("\nCYCLE id SET is_cycle TO 'Y' DEFAULT 'N'"),
            "CYCLE should start on its own line with SET inline, got:\n{}",
            formatted
        );
        let formatted_again = SqlEditorWidget::format_sql_basic(&formatted);
        assert_eq!(
            formatted, formatted_again,
            "SEARCH/CYCLE formatting should be idempotent"
        );
    }

    #[test]
    fn format_sql_basic_match_recognize_subclauses_on_own_lines() {
        let source = "select * from sales match_recognize (partition by customer_id order by sale_dt measures a.sale_dt as start_dt, last(b.sale_dt) as end_dt pattern (a b+) define b as b.amount > prev(b.amount));";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        let lines: Vec<&str> = formatted.lines().collect();
        assert!(
            lines.iter().any(|l| l.trim().starts_with("MEASURES")),
            "MEASURES should start on its own line, got:\n{}",
            formatted
        );
        assert!(
            lines.iter().any(|l| l.trim().starts_with("PATTERN")),
            "PATTERN should start on its own line, got:\n{}",
            formatted
        );
        assert!(
            lines.iter().any(|l| l.trim().starts_with("DEFINE")),
            "DEFINE should start on its own line, got:\n{}",
            formatted
        );
        let formatted_again = SqlEditorWidget::format_sql_basic(&formatted);
        assert_eq!(
            formatted, formatted_again,
            "MATCH_RECOGNIZE formatting should be idempotent"
        );
    }

    #[test]
    fn format_sql_basic_pivot_on_own_line() {
        let source = "select * from sales pivot (sum(amount) as amt for quarter in ('Q1' as q1, 'Q2' as q2));";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        // PIVOT should be separated from FROM on its own line
        let lines: Vec<&str> = formatted.lines().collect();
        assert!(
            lines.iter().any(|l| l.trim().starts_with("PIVOT")),
            "PIVOT should start on its own line, got:\n{}",
            formatted
        );
        // FOR inside PIVOT should NOT be treated as PL/SQL loop
        assert!(
            formatted.contains("FOR quarter"),
            "FOR inside PIVOT should keep quarter on same line, got:\n{}",
            formatted
        );
        let formatted_again = SqlEditorWidget::format_sql_basic(&formatted);
        assert_eq!(
            formatted, formatted_again,
            "PIVOT formatting should be idempotent"
        );
    }

    #[test]
    fn format_sql_basic_unpivot_on_own_line() {
        let source = "select * from t unpivot (comp_value for comp_type in (salary as 'SALARY', bonus as 'BONUS'));";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        let lines: Vec<&str> = formatted.lines().collect();
        assert!(
            lines.iter().any(|l| l.trim().starts_with("UNPIVOT")),
            "UNPIVOT should start on its own line, got:\n{}",
            formatted
        );
        let formatted_again = SqlEditorWidget::format_sql_basic(&formatted);
        assert_eq!(
            formatted, formatted_again,
            "UNPIVOT formatting should be idempotent"
        );
    }

    #[test]
    fn format_sql_basic_pivot_in_cte_same_depth_as_from() {
        let source = "with p as (select * from src pivot (sum(amt) for qtr in ('Q1' as q1, 'Q2' as q2))) select * from p;";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        // PIVOT should be at same indent level as FROM inside CTE
        assert!(
            formatted.contains("    FROM src\n    PIVOT ("),
            "PIVOT should be at the same indent level as FROM inside CTE, got:\n{}",
            formatted
        );
        let formatted_again = SqlEditorWidget::format_sql_basic(&formatted);
        assert_eq!(
            formatted, formatted_again,
            "CTE PIVOT formatting should be idempotent"
        );
    }

    #[test]
    fn format_sql_basic_nested_declare_routines_stay_structured() {
        let source = "DECLARE PROCEDURE p1 IS PROCEDURE p2 IS PROCEDURE p3 IS BEGIN NULL; END; BEGIN p3; END; BEGIN p2; END; BEGIN p1; END;";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        let expected = [
            "DECLARE",
            "    PROCEDURE p1 IS",
            "        PROCEDURE p2 IS",
            "            PROCEDURE p3 IS",
            "            BEGIN",
            "                NULL;",
            "            END;",
            "        BEGIN",
            "            p3;",
            "        END;",
            "    BEGIN",
            "        p2;",
            "    END;",
            "BEGIN",
            "    p1;",
            "END;",
        ]
        .join("\n");

        assert_eq!(formatted, expected);
    }

    #[test]
    fn format_sql_basic_nested_with_multiple_ctes_keeps_inner_main_query_on_with_depth() {
        let source = "WITH outer_1 AS (WITH inner_1 AS (SELECT 1 AS id FROM dual), inner_2 AS (SELECT id FROM inner_1) SELECT id FROM inner_2), outer_2 AS (SELECT id FROM outer_1) SELECT id FROM outer_2;";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        let expected = [
            "WITH outer_1 AS (",
            "    WITH inner_1 AS (",
            "        SELECT 1 AS id",
            "        FROM DUAL",
            "    ),",
            "    inner_2 AS (",
            "        SELECT id",
            "        FROM inner_1",
            "    )",
            "    SELECT id",
            "    FROM inner_2",
            "),",
            "outer_2 AS (",
            "    SELECT id",
            "    FROM outer_1",
            ")",
            "SELECT id",
            "FROM outer_2;",
        ]
        .join("\n");

        assert_eq!(formatted, expected);
        assert_eq!(SqlEditorWidget::format_sql_basic(&formatted), expected);
    }

    #[test]
    fn format_sql_basic_triple_wrapper_scalar_subquery_uses_first_non_comment_token_for_head() {
        let source = "SELECT (((SELECT 1 FROM dual))) AS v FROM dual;";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        let expected = [
            "SELECT",
            "    (",
            "        (",
            "            (",
            "                SELECT 1",
            "                FROM DUAL",
            "            )",
            "        )",
            "    ) AS v",
            "FROM DUAL;",
        ]
        .join("\n");

        assert_eq!(formatted, expected);
        assert_eq!(SqlEditorWidget::format_sql_basic(&formatted), expected);
    }

    #[test]
    fn format_sql_basic_returning_nested_case_keeps_dml_case_structure_stable() {
        let source = "BEGIN UPDATE t SET v = 1 WHERE id = 1 RETURNING CASE WHEN a = 1 THEN CASE WHEN b = 2 THEN CASE WHEN c = 3 THEN 100 ELSE 200 END ELSE 300 END ELSE 400 END INTO v_out; END;";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        let expected = [
            "BEGIN",
            "    UPDATE t",
            "    SET v = 1",
            "    WHERE id = 1",
            "    RETURNING",
            "        CASE",
            "            WHEN a = 1 THEN",
            "                CASE",
            "                    WHEN b = 2 THEN",
            "                        CASE",
            "                            WHEN c = 3 THEN",
            "                                100",
            "                            ELSE",
            "                                200",
            "                        END",
            "                    ELSE",
            "                        300",
            "                END",
            "            ELSE",
            "                400",
            "        END INTO v_out;",
            "END;",
        ]
        .join("\n");

        assert_eq!(formatted, expected);
        assert_eq!(SqlEditorWidget::format_sql_basic(&formatted), expected);
    }

    #[test]
    fn format_sql_basic_triple_wrapper_nested_case_uses_deepest_general_paren_frame() {
        let source = "SELECT (((CASE WHEN a = 1 THEN CASE WHEN b = 2 THEN CASE WHEN c = 3 THEN 1 ELSE 2 END ELSE 3 END ELSE 4 END))) AS v FROM dual;";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        let expected = [
            "SELECT",
            "    (",
            "        (",
            "            (",
            "                CASE",
            "                    WHEN a = 1 THEN",
            "                        CASE",
            "                            WHEN b = 2 THEN",
            "                                CASE",
            "                                    WHEN c = 3 THEN 1",
            "                                    ELSE 2",
            "                                END",
            "                            ELSE 3",
            "                        END",
            "                    ELSE 4",
            "                END",
            "            )",
            "        )",
            "    ) AS v",
            "FROM DUAL;",
        ]
        .join("\n");

        assert_eq!(formatted, expected);
        assert_eq!(SqlEditorWidget::format_sql_basic(&formatted), expected);
    }

    #[test]
    fn format_sql_basic_nested_with_scalar_subquery_restores_outer_select_list_depth() {
        let source = "SELECT e.empno, (WITH dept_name_q AS (SELECT d.dname FROM dept d WHERE d.deptno = e.deptno) SELECT dname FROM dept_name_q) AS dept_name, e.sal FROM emp e;";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        let expected = [
            "SELECT e.empno,",
            "    (",
            "        WITH dept_name_q AS (",
            "            SELECT d.dname",
            "            FROM dept d",
            "            WHERE d.deptno = e.deptno",
            "        )",
            "        SELECT dname",
            "        FROM dept_name_q",
            "    ) AS dept_name,",
            "    e.sal",
            "FROM emp e;",
        ]
        .join("\n");

        assert_eq!(formatted, expected);
        assert_eq!(SqlEditorWidget::format_sql_basic(&formatted), expected);
    }

    #[test]
    fn format_sql_basic_restores_select_item_depth_after_scalar_subquery_before_over_clause() {
        let source = r#"CREATE OR REPLACE VIEW qt_fmt_emp_v AS
    WITH dept_path (dept_id, parent_dept_id, dept_path_txt, lvl) AS (
        SELECT
            d.dept_id,
            d.parent_dept_id,
            TO_CHAR (d.dept_name) AS dept_path_txt,
            1 AS lvl
        FROM qt_fmt_dept d
        WHERE d.parent_dept_id IS NULL
        UNION ALL
        SELECT
            c.dept_id,
            c.parent_dept_id,
            p.dept_path_txt || ' > ' || c.dept_name,
            p.lvl + 1
        FROM qt_fmt_dept c
        JOIN dept_path p
            ON p.dept_id = c.parent_dept_id
    ),
    skill_rows AS (
        SELECT
            e.emp_id,
            jt.skill,
            jt.grade,
            jt.remote_yn
        FROM qt_fmt_emp e,
            JSON_TABLE (e.json_doc, '$' COLUMNS (
                grade VARCHAR2 (10) PATH '$.meta.grade',
                remote_yn VARCHAR2 (5) PATH '$.meta.remote',
                NESTED PATH '$.skills[*]' COLUMNS (
                    skill VARCHAR2 (100) PATH '$'
                )
            )) jt
    ),
    xml_rows AS (
        SELECT
            e.emp_id,
            x.flag_txt
        FROM qt_fmt_emp e,
            XMLTABLE ('/emp/flags/flag' PASSING e.xml_doc COLUMNS flag_txt VARCHAR2 (100) PATH '.') x
    )
    SELECT
        e.emp_id,
        e.emp_name,
        e.login_name,
        e.email_addr,
        e.salary,
        e.bonus_pct,
        e.hire_dt,
        e.status_cd,
        e.job_title,
        d.dept_id,
        d.dept_code,
        d.dept_name,
        dp.dept_path_txt,
        (
            SELECT LISTAGG (sr.skill, ', ') WITHIN GROUP (ORDER BY sr.skill)
            FROM skill_rows sr
            WHERE sr.emp_id = e.emp_id
        ) AS skill_list,
        (
            SELECT LISTAGG (xr.flag_txt, ', ') WITHIN GROUP (ORDER BY xr.flag_txt)
            FROM xml_rows xr
            WHERE xr.emp_id = e.emp_id
        ) AS flag_list,
                (
                    SELECT SUM (b.amount)
                    FROM qt_fmt_bonus b
                    WHERE b.emp_id = e.emp_id
                        AND b.bonus_year = 2024
                ) AS bonus_2024,
                        ROW_NUMBER () OVER (
                            PARTITION BY e.dept_id
                            ORDER BY e.salary DESC NULLS LAST,
                            e.emp_id
                        ) AS rn_in_dept,
        DENSE_RANK () OVER (
            ORDER BY e.salary DESC NULLS LAST
        ) AS salary_rank_all,
        AVG (e.salary) OVER (
            PARTITION BY e.dept_id
        ) AS dept_avg_salary,
        SUM (
            CASE
                WHEN e.status_cd = 'ACTIVE' THEN NVL (e.salary, 0)
                ELSE 0
            END
        ) OVER (
            PARTITION BY e.dept_id
        ) AS active_salary_sum
    FROM qt_fmt_emp e
    JOIN qt_fmt_dept d
        ON d.dept_id = e.dept_id
    LEFT JOIN dept_path dp
        ON dp.dept_id = d.dept_id;"#;

        let formatted = SqlEditorWidget::format_sql_basic(source);
        let lines: Vec<&str> = formatted.lines().collect();
        let indent = |line: &str| line.len().saturating_sub(line.trim_start().len());

        let bonus_idx = find_line_starting_with(&lines, ") AS bonus_2024,").unwrap_or(0);
        let rn_idx = find_line_starting_with(&lines, "ROW_NUMBER () OVER (").unwrap_or(0);
        let partition_idx = find_line_starting_with(&lines, "PARTITION BY e.dept_id").unwrap_or(0);
        let order_idx =
            find_line_starting_with(&lines, "ORDER BY e.salary DESC NULLS LAST,").unwrap_or(0);
        let rank_idx = find_line_starting_with(&lines, "DENSE_RANK () OVER (").unwrap_or(0);
        let close_idx = lines
            .iter()
            .enumerate()
            .skip(order_idx.saturating_add(1))
            .find(|(_, line)| line.trim_start() == ") AS rn_in_dept,")
            .map(|(idx, _)| idx)
            .unwrap_or(0);

        assert_eq!(
            indent(lines[bonus_idx]),
            indent(lines[rank_idx]),
            "scalar subquery select item should realign with sibling SELECT items, got:\n{}",
            formatted
        );
        assert_eq!(
            indent(lines[rn_idx]),
            indent(lines[rank_idx]),
            "analytic item after scalar subquery should return to the SELECT item depth, got:\n{}",
            formatted
        );
        assert_eq!(
            indent(lines[partition_idx]),
            indent(lines[rn_idx]).saturating_add(4),
            "OVER PARTITION BY should stay one level deeper than the analytic owner, got:\n{}",
            formatted
        );
        assert_eq!(
            indent(lines[order_idx]),
            indent(lines[partition_idx]),
            "OVER ORDER BY should stay aligned with the analytic owner-relative body depth, got:\n{}",
            formatted
        );
        assert_eq!(
            indent(lines[close_idx]),
            indent(lines[rn_idx]),
            "OVER closing line should realign with the analytic owner depth, got:\n{}",
            formatted
        );
        assert_eq!(
            indent(lines[rank_idx]),
            indent(lines[bonus_idx]),
            "sibling analytic items should share the same SELECT item depth, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_sql_basic_insert_all_nested_subquery_keeps_outer_into_scope() {
        let source =
            "INSERT ALL INTO t1 (id) VALUES ((SELECT 1 FROM dual)) INTO t2 (id) VALUES (2) SELECT 1 FROM dual;";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        let lines: Vec<&str> = formatted.lines().collect();
        let leading_spaces = |line: &str| line.len().saturating_sub(line.trim_start().len());
        let into_1_idx = lines
            .iter()
            .position(|line| line.trim_start().starts_with("INTO t1"))
            .unwrap_or(0);
        let into_2_idx = lines
            .iter()
            .position(|line| line.trim_start().starts_with("INTO t2"))
            .unwrap_or(0);
        let select_indices: Vec<usize> = lines
            .iter()
            .enumerate()
            .filter_map(|(idx, line)| line.trim_start().starts_with("SELECT 1").then_some(idx))
            .collect();

        assert_eq!(
            leading_spaces(lines[into_1_idx]),
            leading_spaces(lines[into_2_idx]),
            "Nested SELECT inside INSERT ALL VALUES must not terminate the outer INTO scope, got:\n{}",
            formatted
        );
        assert!(
            leading_spaces(lines[into_1_idx]) > 0,
            "INSERT ALL INTO lines should stay indented under INSERT ALL, got:\n{}",
            formatted
        );
        assert!(
            select_indices.len() >= 2,
            "Expected both nested and final SELECT lines, got:\n{}",
            formatted
        );
        assert!(
            leading_spaces(lines[select_indices[0]]) > leading_spaces(lines[into_1_idx]),
            "Nested SELECT inside INSERT ALL VALUES should stay deeper than INTO, got:\n{}",
            formatted
        );
        assert_eq!(
            leading_spaces(lines[*select_indices.last().unwrap_or(&0)]),
            0,
            "Final SELECT after INSERT ALL branches should return to top-level depth, got:\n{}",
            formatted
        );
        assert_eq!(SqlEditorWidget::format_sql_basic(&formatted), formatted);
    }

    #[test]
    fn format_sql_basic_nested_between_keeps_outer_between_and_inline() {
        let source = "SELECT * FROM orders WHERE total_amount BETWEEN CASE WHEN discount BETWEEN 1 AND 2 THEN 10 ELSE 20 END AND 100 AND status = 'OPEN';";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        let lines: Vec<&str> = formatted.lines().collect();

        assert!(
            lines.iter().all(|line| !line.trim_start().starts_with("AND 100")),
            "Outer BETWEEN terminator AND should not be treated as a new condition line after nested BETWEEN, got:\n{}",
            formatted
        );
        assert!(
            lines
                .iter()
                .any(|line| { line.trim_start().starts_with("AND status = 'OPEN';") }),
            "The real condition AND after BETWEEN should still start its own line, got:\n{}",
            formatted
        );
        assert!(
            formatted.contains("discount BETWEEN 1 AND 2"),
            "Nested BETWEEN expression should remain intact, got:\n{}",
            formatted
        );
        assert_eq!(SqlEditorWidget::format_sql_basic(&formatted), formatted);
    }

    #[test]
    fn format_sql_basic_model_nested_subquery_restores_outer_model_scope() {
        let source = "SELECT deptno, amount FROM sales MODEL PARTITION BY (deptno) DIMENSION BY (month_key) MEASURES ((SELECT limit_amt FROM limits l WHERE l.deptno = sales.deptno) cap, amount) RULES UPDATE (amount[ANY] = cap[CV()] * 1.1) ORDER BY deptno;";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        let lines: Vec<&str> = formatted.lines().collect();
        let leading_spaces = |line: &str| line.len().saturating_sub(line.trim_start().len());
        let measures_idx = lines
            .iter()
            .position(|line| line.trim_start().starts_with("MEASURES ("))
            .unwrap_or(0);
        let inner_select_idx = lines
            .iter()
            .enumerate()
            .skip(measures_idx.saturating_add(1))
            .find(|(_, line)| line.trim_start().starts_with("SELECT limit_amt"))
            .map(|(idx, _)| idx)
            .unwrap_or(0);
        let rules_idx = lines
            .iter()
            .position(|line| line.trim_start().starts_with("RULES UPDATE ("))
            .unwrap_or(0);
        let order_idx = lines
            .iter()
            .position(|line| line.trim_start().starts_with("ORDER BY deptno;"))
            .unwrap_or(0);

        assert!(
            leading_spaces(lines[inner_select_idx]) > leading_spaces(lines[measures_idx]),
            "Nested SELECT inside MODEL MEASURES should stay deeper than the MEASURES header, got:\n{}",
            formatted
        );
        assert_eq!(
            leading_spaces(lines[rules_idx]),
            leading_spaces(lines[measures_idx]),
            "RULES should realign to the outer MODEL subclause depth after nested subquery closes, got:\n{}",
            formatted
        );
        assert_eq!(
            leading_spaces(lines[order_idx]),
            0,
            "ORDER BY after MODEL should return to the top-level clause depth, got:\n{}",
            formatted
        );
        assert_eq!(SqlEditorWidget::format_sql_basic(&formatted), formatted);
    }

    #[test]
    fn format_sql_basic_model_extended_subclauses_restore_outer_model_scope() {
        let source = "SELECT deptno, amount FROM sales MODEL PARTITION BY (deptno) DIMENSION BY (month_key) MEASURES ((SELECT limit_amt FROM limits l WHERE l.deptno = sales.deptno) cap, amount) IGNORE NAV RETURN UPDATED ROWS RULES UPDATE (amount[ANY] = cap[CV()] * 1.1) ORDER BY deptno;";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        let lines: Vec<&str> = formatted.lines().collect();
        let leading_spaces = |line: &str| line.len().saturating_sub(line.trim_start().len());
        let measures_idx = find_line_starting_with(&lines, "MEASURES (").expect("MEASURES line");
        let inner_select_idx = lines
            .iter()
            .position(|line| line.contains("SELECT limit_amt"))
            .expect("inner SELECT line");
        let ignore_idx = find_line_starting_with(&lines, "IGNORE NAV").expect("IGNORE NAV line");
        let return_idx = find_line_starting_with(&lines, "RETURN UPDATED ROWS")
            .expect("RETURN UPDATED ROWS line");
        let rules_idx =
            find_line_starting_with(&lines, "RULES UPDATE (").expect("RULES UPDATE line");
        let order_idx = find_line_starting_with(&lines, "ORDER BY deptno;").expect("ORDER BY line");

        assert!(
            leading_spaces(lines[inner_select_idx]) > leading_spaces(lines[measures_idx]),
            "Nested SELECT inside MODEL MEASURES should stay deeper than the MEASURES header, got:\n{}",
            formatted
        );
        assert_eq!(
            leading_spaces(lines[ignore_idx]),
            leading_spaces(lines[measures_idx]),
            "IGNORE NAV should realign to the MODEL owner-relative subclause depth after nested subquery closes, got:\n{}",
            formatted
        );
        assert_eq!(
            leading_spaces(lines[return_idx]),
            leading_spaces(lines[ignore_idx]),
            "RETURN UPDATED ROWS should share the MODEL subclause depth, got:\n{}",
            formatted
        );
        assert_eq!(
            leading_spaces(lines[rules_idx]),
            leading_spaces(lines[ignore_idx]),
            "RULES should stay aligned with the extended MODEL subclauses, got:\n{}",
            formatted
        );
        assert_eq!(
            leading_spaces(lines[order_idx]),
            0,
            "ORDER BY after MODEL should return to the top-level clause depth, got:\n{}",
            formatted
        );
        assert_eq!(SqlEditorWidget::format_sql_basic(&formatted), formatted);
    }

    #[test]
    fn format_sql_basic_model_reference_nested_subquery_restores_outer_model_scope() {
        let source = "SELECT deptno, amount FROM sales MODEL REFERENCE ref_limits ON (SELECT limit_amt FROM limits l WHERE l.deptno = sales.deptno) DIMENSION BY (month_key) MEASURES (amount) RULES UPDATE (amount[ANY] = amount[CV()] + 1) ORDER BY deptno;";
        let formatted = SqlEditorWidget::format_sql_basic(source);
        let lines: Vec<&str> = formatted.lines().collect();
        let leading_spaces = |line: &str| line.len().saturating_sub(line.trim_start().len());
        let reference_idx = lines
            .iter()
            .position(|line| line.trim_start().starts_with("REFERENCE ref_limits ON ("))
            .unwrap_or(0);
        let inner_select_idx = lines
            .iter()
            .enumerate()
            .skip(reference_idx.saturating_add(1))
            .find(|(_, line)| line.trim_start().starts_with("SELECT limit_amt"))
            .map(|(idx, _)| idx)
            .unwrap_or(0);
        let dimension_idx = lines
            .iter()
            .position(|line| line.trim_start().starts_with("DIMENSION BY (month_key)"))
            .unwrap_or(0);
        let rules_idx = lines
            .iter()
            .position(|line| line.trim_start().starts_with("RULES UPDATE ("))
            .unwrap_or(0);
        let order_idx = lines
            .iter()
            .position(|line| line.trim_start().starts_with("ORDER BY deptno;"))
            .unwrap_or(0);

        assert!(
            leading_spaces(lines[inner_select_idx]) > leading_spaces(lines[reference_idx]),
            "Nested SELECT inside MODEL REFERENCE ON should stay deeper than the REFERENCE header, got:\n{}",
            formatted
        );
        assert_eq!(
            leading_spaces(lines[dimension_idx]),
            leading_spaces(lines[reference_idx]),
            "DIMENSION BY should realign to the MODEL REFERENCE owner depth after nested subquery closes, got:\n{}",
            formatted
        );
        assert_eq!(
            leading_spaces(lines[rules_idx]),
            leading_spaces(lines[reference_idx]),
            "RULES should stay aligned with MODEL REFERENCE subclauses, got:\n{}",
            formatted
        );
        assert_eq!(
            leading_spaces(lines[order_idx]),
            0,
            "ORDER BY after MODEL REFERENCE should return to the top-level clause depth, got:\n{}",
            formatted
        );
        assert_eq!(SqlEditorWidget::format_sql_basic(&formatted), formatted);
    }

    #[test]
    fn format_sql_basic_skips_blank_line_before_comment_glued_case_item() {
        let source = r#"SELECT
    col1,

    /* gap */ CASE
        WHEN flag = 'Y' THEN 1
        ELSE 0
    END AS val
FROM dual;"#;

        let formatted = SqlEditorWidget::format_sql_basic(source);
        assert!(
            !formatted.contains("col1,\n\n    /* gap */ CASE"),
            "blank line before a comment-glued CASE item should be removed when the previous item ends with a comma, got:\n{}",
            formatted
        );
    }

    #[test]
    fn comment_header_continuation_kind_reuses_exact_bare_structural_taxonomy_and_skips_named_owners(
    ) {
        let continuation_kind =
            |line: &str| -> Option<crate::sql_text::FormatInlineCommentHeaderContinuationKind> {
                let tokens = SqlEditorWidget::tokenize_sql(line);
                let comment_idx = tokens
                    .iter()
                    .position(|token| matches!(token, SqlToken::Comment(_)))
                    .unwrap_or(0);
                SqlEditorWidget::comment_header_continuation_kind(&tokens, comment_idx)
            };

        for line in [
            "WITHIN GROUP -- analytic owner",
            "KEEP -- analytic owner",
            "MATCH_RECOGNIZE -- row pattern",
            "PIVOT -- rotated rows",
            "UNPIVOT -- rotated rows",
            "FROM TABLE -- collection rows",
            "USING TABLE -- collection rows",
            "JOIN TABLE -- collection rows",
            "LEFT JOIN TABLE -- collection rows",
            "LEFT OUTER JOIN TABLE -- collection rows",
            "RIGHT JOIN TABLE -- collection rows",
            "RIGHT OUTER JOIN TABLE -- collection rows",
            "FULL JOIN TABLE -- collection rows",
            "FULL OUTER JOIN TABLE -- collection rows",
            "INNER JOIN TABLE -- collection rows",
            "CROSS JOIN TABLE -- collection rows",
            "NATURAL JOIN TABLE -- collection rows",
            "NATURAL LEFT JOIN TABLE -- collection rows",
            "NATURAL LEFT OUTER JOIN TABLE -- collection rows",
            "NATURAL RIGHT JOIN TABLE -- collection rows",
            "NATURAL RIGHT OUTER JOIN TABLE -- collection rows",
            "NATURAL FULL JOIN TABLE -- collection rows",
            "NATURAL FULL OUTER JOIN TABLE -- collection rows",
        ] {
            assert_eq!(
                continuation_kind(line),
                Some(crate::sql_text::FormatInlineCommentHeaderContinuationKind::SameDepth),
                "expected `{line}` to reuse exact bare owner/header continuation depth"
            );
        }

        assert_eq!(
            continuation_kind("JOIN -- joined rows"),
            Some(crate::sql_text::FormatInlineCommentHeaderContinuationKind::OneDeeperThanCurrentLine)
        );
        assert_eq!(
            continuation_kind("CALL -- invoke routine"),
            Some(
                crate::sql_text::FormatInlineCommentHeaderContinuationKind::OneDeeperThanCurrentLine
            )
        );
        assert_eq!(
            continuation_kind("INNER JOIN -- joined rows"),
            Some(
                crate::sql_text::FormatInlineCommentHeaderContinuationKind::OneDeeperThanQueryBase
            )
        );
        assert_eq!(
            continuation_kind("NATURAL LEFT JOIN -- joined rows"),
            Some(
                crate::sql_text::FormatInlineCommentHeaderContinuationKind::OneDeeperThanQueryBase
            )
        );
        assert_eq!(
            continuation_kind("REFERENCE -- model ref"),
            Some(crate::sql_text::FormatInlineCommentHeaderContinuationKind::SameDepth)
        );
        assert_eq!(
            continuation_kind("WINDOW -- named window"),
            Some(
                crate::sql_text::FormatInlineCommentHeaderContinuationKind::OneDeeperThanCurrentLine
            )
        );
        assert_eq!(
            continuation_kind("MATCH_RECOGNIZE -- row pattern"),
            Some(crate::sql_text::FormatInlineCommentHeaderContinuationKind::SameDepth)
        );
        assert_eq!(
            continuation_kind("PIVOT -- rotated rows"),
            Some(crate::sql_text::FormatInlineCommentHeaderContinuationKind::SameDepth)
        );
        assert_eq!(
            continuation_kind("UNPIVOT -- rotated rows"),
            Some(crate::sql_text::FormatInlineCommentHeaderContinuationKind::SameDepth)
        );
        assert_eq!(
            continuation_kind("FROM TABLE -- collection rows"),
            Some(crate::sql_text::FormatInlineCommentHeaderContinuationKind::SameDepth)
        );
        assert_eq!(
            continuation_kind("LEFT JOIN TABLE -- collection rows"),
            Some(crate::sql_text::FormatInlineCommentHeaderContinuationKind::SameDepth)
        );
        assert_eq!(
            continuation_kind("EXISTS -- deferred wrapper"),
            Some(crate::sql_text::FormatInlineCommentHeaderContinuationKind::SameDepth)
        );
        assert_eq!(
            continuation_kind("NOT EXISTS -- deferred wrapper"),
            Some(crate::sql_text::FormatInlineCommentHeaderContinuationKind::SameDepth)
        );
        assert_eq!(
            continuation_kind("IN -- deferred wrapper"),
            Some(crate::sql_text::FormatInlineCommentHeaderContinuationKind::SameDepth)
        );
        assert_eq!(
            continuation_kind("NOT IN -- deferred wrapper"),
            Some(crate::sql_text::FormatInlineCommentHeaderContinuationKind::SameDepth)
        );
        assert_eq!(
            continuation_kind("ANY -- deferred wrapper"),
            Some(crate::sql_text::FormatInlineCommentHeaderContinuationKind::SameDepth)
        );
        assert_eq!(
            continuation_kind("SOME -- deferred wrapper"),
            Some(crate::sql_text::FormatInlineCommentHeaderContinuationKind::SameDepth)
        );
        assert_eq!(
            continuation_kind("ALL -- deferred wrapper"),
            Some(crate::sql_text::FormatInlineCommentHeaderContinuationKind::SameDepth)
        );
    }

    #[test]
    fn comment_header_continuation_kind_keeps_rhs_operator_precedence_for_non_owner_lines() {
        let continuation_kind =
            |line: &str| -> Option<crate::sql_text::FormatInlineCommentHeaderContinuationKind> {
                let tokens = SqlEditorWidget::tokenize_sql(line);
                let comment_idx = tokens
                    .iter()
                    .position(|token| matches!(token, SqlToken::Comment(_)))
                    .unwrap_or(0);
                SqlEditorWidget::comment_header_continuation_kind(&tokens, comment_idx)
            };

        for line in [
            "AND e.member_col MEMBER OF -- type filter",
            "AND e.num_nt SUBMULTISET OF -- subset filter",
            "AND e.ename LIKE4 -- pattern",
            "AND e.ename LIKE 'A%' ESCAPE -- esc",
            "v_total := -- assign",
            "pkg_lock.request => -- named arg",
        ] {
            assert_eq!(
                continuation_kind(line),
                Some(crate::sql_text::FormatInlineCommentHeaderContinuationKind::OneDeeperThanCurrentLine),
                "expected `{line}` to keep RHS operator continuation depth"
            );
        }
    }

    #[test]
    fn resolve_comment_header_continuation_indent_reuses_shared_anchor_resolver() {
        assert_eq!(
            SqlEditorWidget::resolve_comment_header_continuation_indent(
                Some(crate::sql_text::FormatInlineCommentHeaderContinuationKind::SameDepth),
                5,
                2,
                5,
                9,
            ),
            5
        );
        assert_eq!(
            SqlEditorWidget::resolve_comment_header_continuation_indent(
                Some(
                    crate::sql_text::FormatInlineCommentHeaderContinuationKind::OneDeeperThanQueryBase,
                ),
                5,
                2,
                5,
                9,
            ),
            3
        );
        assert_eq!(
            SqlEditorWidget::resolve_comment_header_continuation_indent(
                Some(
                    crate::sql_text::FormatInlineCommentHeaderContinuationKind::OneDeeperThanCurrentLine,
                ),
                5,
                2,
                5,
                9,
            ),
            6
        );
        assert_eq!(
            SqlEditorWidget::resolve_comment_header_continuation_indent(None, 5, 2, 5, 9),
            9
        );
    }

    #[test]
    fn comment_header_continuation_kind_prioritizes_structural_header_family_over_trailing_operator_for_header_lines(
    ) {
        let continuation_kind =
            |line: &str| -> Option<crate::sql_text::FormatInlineCommentHeaderContinuationKind> {
                let tokens = SqlEditorWidget::tokenize_sql(line);
                let comment_idx = tokens
                    .iter()
                    .position(|token| matches!(token, SqlToken::Comment(_)))
                    .unwrap_or(0);
                SqlEditorWidget::comment_header_continuation_kind(&tokens, comment_idx)
            };

        for (line, expected) in [
            (
                "WHERE e.emp_id = -- keep",
                Some(
                    crate::sql_text::FormatInlineCommentHeaderContinuationKind::OneDeeperThanQueryBase,
                ),
            ),
            (
                "WHERE e.member_col MEMBER OF -- keep",
                Some(
                    crate::sql_text::FormatInlineCommentHeaderContinuationKind::OneDeeperThanQueryBase,
                ),
            ),
            (
                "ON e.emp_id = -- keep",
                Some(
                    crate::sql_text::FormatInlineCommentHeaderContinuationKind::OneDeeperThanQueryBase,
                ),
            ),
            (
                "AND e.status_cd = -- keep",
                Some(
                    crate::sql_text::FormatInlineCommentHeaderContinuationKind::OneDeeperThanCurrentLine,
                ),
            ),
            (
                "v_total := -- keep",
                Some(
                    crate::sql_text::FormatInlineCommentHeaderContinuationKind::OneDeeperThanCurrentLine,
                ),
            ),
        ] {
            assert_eq!(
                continuation_kind(line),
                expected,
                "expected `{line}` to prefer structural header carry before pure RHS fallback",
            );
        }
    }

    #[test]
    fn format_sql_basic_keeps_inline_comment_split_not_exists_owner_depth_across_standalone_open_paren(
    ) {
        let source = r#"SELECT d.deptno
FROM dept d
WHERE
NOT EXISTS -- owner
(
    SELECT 1
    FROM emp e
    WHERE e.deptno = d.deptno
)
AND d.active = 'Y';"#;
        let formatted = SqlEditorWidget::format_sql_basic(source);
        let lines: Vec<&str> = formatted.lines().collect();
        let indent = |line: &str| line.len().saturating_sub(line.trim_start().len());

        let owner_idx = lines
            .iter()
            .position(|line| line.trim_start() == "NOT EXISTS -- owner")
            .unwrap_or(0);
        let open_idx = lines
            .iter()
            .position(|line| line.trim() == "(")
            .unwrap_or(0);
        let select_idx = lines
            .iter()
            .position(|line| line.trim_start() == "SELECT 1")
            .unwrap_or(0);
        let close_idx = lines
            .iter()
            .position(|line| line.trim() == ")")
            .unwrap_or(0);

        assert_eq!(
            indent(lines[open_idx]),
            indent(lines[owner_idx]),
            "standalone open paren after inline-comment split NOT EXISTS should stay on owner depth, got:\n{}",
            formatted
        );
        assert_eq!(
            indent(lines[select_idx]),
            indent(lines[owner_idx]).saturating_add(4),
            "child SELECT under inline-comment split NOT EXISTS should stay one level deeper than owner, got:\n{}",
            formatted
        );
        assert_eq!(
            indent(lines[close_idx]),
            indent(lines[owner_idx]),
            "inline-comment split NOT EXISTS closing paren should realign with owner depth, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_sql_basic_join_inline_comment_keeps_next_table_one_level_deeper_than_join() {
        let source = r#"SELECT e.empno
FROM emp e
JOIN -- joined rows
dept d
ON d.deptno = e.deptno;"#;

        let formatted = SqlEditorWidget::format_sql_basic(source);
        let lines: Vec<&str> = formatted.lines().collect();
        let join_idx = find_line_starting_with(&lines, "JOIN -- joined rows").unwrap_or(0);
        let table_idx = find_line_starting_with(&lines, "dept d").unwrap_or(0);

        assert_eq!(
            leading_spaces(lines[table_idx]),
            leading_spaces(lines[join_idx]).saturating_add(4),
            "comment-split JOIN item should stay one level deeper than the JOIN header, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_sql_basic_keep_inline_comment_keeps_wrapper_on_exact_owner_depth() {
        let source = r#"SELECT *
FROM (
    SELECT
        MAX (e.sal) KEEP -- owner
        (
            DENSE_RANK LAST ORDER BY (
                SELECT MAX (b.sal)
                FROM bonus b
                WHERE b.empno = e.empno
            ),
            e.empno
        ) AS top_sal
    FROM emp e
) ranked
WHERE ranked.top_sal > 0;"#;

        let formatted = SqlEditorWidget::format_sql_basic(source);
        let lines: Vec<&str> = formatted.lines().collect();
        let keep_idx =
            find_line_starting_with(&lines, "SELECT MAX (e.sal) KEEP -- owner").unwrap_or(0);
        let open_idx = lines
            .iter()
            .enumerate()
            .skip(keep_idx.saturating_add(1))
            .find(|(_, line)| line.trim_start() == "(")
            .map(|(idx, _)| idx)
            .unwrap_or(0);
        let dense_rank_idx = lines
            .iter()
            .enumerate()
            .skip(open_idx.saturating_add(1))
            .find(|(_, line)| line.trim_start().starts_with("DENSE_RANK"))
            .map(|(idx, _)| idx)
            .unwrap_or(0);
        let close_idx = lines
            .iter()
            .enumerate()
            .skip(dense_rank_idx.saturating_add(1))
            .find(|(_, line)| line.trim_start().starts_with(") AS top_sal"))
            .map(|(idx, _)| idx)
            .unwrap_or(0);

        assert_eq!(
            leading_spaces(lines[open_idx]),
            leading_spaces(lines[keep_idx]),
            "standalone open paren after inline-comment split KEEP should stay on the owner depth, got:\n{}",
            formatted
        );
        assert_eq!(
            leading_spaces(lines[dense_rank_idx]),
            leading_spaces(lines[keep_idx]).saturating_add(4),
            "KEEP body header should stay one level deeper than the exact owner line, got:\n{}",
            formatted
        );
        assert_eq!(
            leading_spaces(lines[close_idx]),
            leading_spaces(lines[keep_idx]),
            "KEEP close line should realign with the owner depth, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_sql_basic_right_outer_join_table_inline_comment_keeps_exact_owner_depth() {
        let source = r#"SELECT *
FROM dept d
WHERE EXISTS (
    SELECT 1
    FROM emp e
    RIGHT OUTER JOIN TABLE -- owner
    (
        SELECT b.empno
        FROM bonus b
        WHERE b.empno = e.empno
    ) bonus_rows
    ON bonus_rows.empno = e.empno
)
AND d.status = 'A';"#;

        let formatted = SqlEditorWidget::format_sql_basic(source);
        let lines: Vec<&str> = formatted.lines().collect();
        let owner_idx =
            find_line_starting_with(&lines, "RIGHT OUTER JOIN TABLE -- owner").unwrap_or(0);
        let open_idx = lines
            .iter()
            .enumerate()
            .skip(owner_idx.saturating_add(1))
            .find(|(_, line)| line.trim_start() == "(")
            .map(|(idx, _)| idx)
            .unwrap_or(0);
        let select_idx = lines
            .iter()
            .enumerate()
            .skip(open_idx.saturating_add(1))
            .find(|(_, line)| line.trim_start().starts_with("SELECT b.empno"))
            .map(|(idx, _)| idx)
            .unwrap_or(0);
        let close_idx = lines
            .iter()
            .enumerate()
            .skip(select_idx.saturating_add(1))
            .find(|(_, line)| line.trim_start().starts_with(") bonus_rows"))
            .map(|(idx, _)| idx)
            .unwrap_or(0);

        assert_eq!(
            leading_spaces(lines[open_idx]),
            leading_spaces(lines[owner_idx]),
            "standalone open paren after inline-comment split JOIN TABLE owner should stay on the owner depth, got:\n{}",
            formatted
        );
        assert_eq!(
            leading_spaces(lines[select_idx]),
            leading_spaces(lines[owner_idx]).saturating_add(4),
            "child SELECT under inline-comment split JOIN TABLE owner should stay one level deeper than the owner, got:\n{}",
            formatted
        );
        assert_eq!(
            leading_spaces(lines[close_idx]),
            leading_spaces(lines[owner_idx]),
            "JOIN TABLE close line should realign with the owner depth, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_sql_basic_plsql_case_after_then_uses_branch_body_depth() {
        let source = r#"BEGIN
IF l_ready THEN
CASE
WHEN l_mode = 'A' THEN
NULL;
ELSE
NULL;
END CASE;
END IF;
END;"#;

        let formatted = SqlEditorWidget::format_sql_basic(source);
        let lines: Vec<&str> = formatted.lines().collect();
        let indent = |line: &str| line.len().saturating_sub(line.trim_start().len());

        let if_idx = find_line_starting_with(&lines, "IF l_ready THEN").expect("IF line");
        let case_idx = lines
            .iter()
            .enumerate()
            .skip(if_idx + 1)
            .find(|(_, line)| line.trim_start() == "CASE")
            .map(|(idx, _)| idx)
            .expect("CASE line");
        let when_idx = lines
            .iter()
            .enumerate()
            .skip(case_idx + 1)
            .find(|(_, line)| line.trim_start().starts_with("WHEN l_mode = 'A' THEN"))
            .map(|(idx, _)| idx)
            .expect("WHEN line");
        let end_case_idx = lines
            .iter()
            .enumerate()
            .skip(when_idx + 1)
            .find(|(_, line)| line.trim_start() == "END CASE;")
            .map(|(idx, _)| idx)
            .expect("END CASE line");

        assert_eq!(
            indent(lines[case_idx]),
            indent(lines[if_idx]).saturating_add(4),
            "PL/SQL CASE after THEN should indent one level deeper than the IF header, got:\n{}",
            formatted
        );
        assert_eq!(
            indent(lines[when_idx]),
            indent(lines[case_idx]).saturating_add(4),
            "PL/SQL CASE branch should indent one level deeper than CASE, got:\n{}",
            formatted
        );
        assert_eq!(
            indent(lines[end_case_idx]),
            indent(lines[case_idx]),
            "PL/SQL END CASE should align with CASE inside an IF branch, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_sql_basic_exception_open_cursor_nested_subquery_uses_handler_base_depth() {
        let source = r#"BEGIN
NULL;
EXCEPTION
WHEN OTHERS THEN
OPEN c_emp FOR
SELECT empno
FROM (
SELECT empno
FROM emp
);
END;"#;

        let formatted = SqlEditorWidget::format_sql_basic(source);
        let lines: Vec<&str> = formatted.lines().collect();
        let indent = |line: &str| line.len().saturating_sub(line.trim_start().len());

        let when_idx =
            find_line_starting_with(&lines, "WHEN OTHERS THEN").expect("WHEN OTHERS line");
        let open_idx = find_line_starting_with(&lines, "OPEN c_emp FOR").expect("OPEN cursor line");
        let select_idx = lines
            .iter()
            .enumerate()
            .skip(open_idx + 1)
            .find(|(_, line)| line.trim_start() == "SELECT empno")
            .map(|(idx, _)| idx)
            .expect("SELECT line");
        let from_idx = lines
            .iter()
            .enumerate()
            .skip(select_idx + 1)
            .find(|(_, line)| line.trim_start() == "FROM (")
            .map(|(idx, _)| idx)
            .expect("FROM line");
        let nested_select_idx = lines
            .iter()
            .enumerate()
            .skip(from_idx + 1)
            .find(|(_, line)| line.trim_start() == "SELECT empno")
            .map(|(idx, _)| idx)
            .expect("nested SELECT line");
        let close_idx = lines
            .iter()
            .enumerate()
            .skip(nested_select_idx + 1)
            .find(|(_, line)| line.trim_start() == ");")
            .map(|(idx, _)| idx)
            .expect("close paren line");

        assert_eq!(
            indent(lines[open_idx]),
            indent(lines[when_idx]).saturating_add(4),
            "OPEN cursor in EXCEPTION handler should use the handler body depth, got:\n{}",
            formatted
        );
        assert_eq!(
            indent(lines[select_idx]),
            indent(lines[open_idx]).saturating_add(4),
            "OPEN cursor SELECT should indent one level deeper than OPEN, got:\n{}",
            formatted
        );
        assert_eq!(
            indent(lines[nested_select_idx]),
            indent(lines[from_idx]).saturating_add(8),
            "Nested SELECT under EXCEPTION handler should preserve the formatter's nested query-base step under FROM, got:\n{}",
            formatted
        );
        assert_eq!(
            indent(lines[close_idx]),
            indent(lines[from_idx]).saturating_add(4),
            "Nested close paren under EXCEPTION handler should align with the child query close depth under FROM, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_sql_basic_repeat_open_cursor_nested_subquery_uses_repeat_body_depth() {
        let source = r#"BEGIN
REPEAT
OPEN c_emp FOR
SELECT empno
FROM (
SELECT empno
FROM emp
);
END REPEAT;
END;"#;

        let formatted = SqlEditorWidget::format_sql_basic(source);
        let lines: Vec<&str> = formatted.lines().collect();
        let indent = |line: &str| line.len().saturating_sub(line.trim_start().len());

        let repeat_idx = find_line_starting_with(&lines, "REPEAT").expect("REPEAT line");
        let open_idx = find_line_starting_with(&lines, "OPEN c_emp FOR")
            .unwrap_or_else(|| panic!("OPEN cursor line, got:\n{}", formatted));
        let select_idx = lines
            .iter()
            .enumerate()
            .skip(open_idx + 1)
            .find(|(_, line)| line.trim_start() == "SELECT empno")
            .map(|(idx, _)| idx)
            .unwrap_or_else(|| panic!("SELECT line, got:\n{}", formatted));
        let from_idx = lines
            .iter()
            .enumerate()
            .skip(select_idx + 1)
            .find(|(_, line)| line.trim_start() == "FROM (")
            .map(|(idx, _)| idx)
            .unwrap_or_else(|| panic!("FROM line, got:\n{}", formatted));
        let nested_select_idx = lines
            .iter()
            .enumerate()
            .skip(from_idx + 1)
            .find(|(_, line)| line.trim_start() == "SELECT empno")
            .map(|(idx, _)| idx)
            .unwrap_or_else(|| panic!("nested SELECT line, got:\n{}", formatted));
        let end_repeat_idx = find_line_starting_with(&lines, "END REPEAT;")
            .unwrap_or_else(|| panic!("END REPEAT line, got:\n{}", formatted));

        assert_eq!(
            indent(lines[open_idx]),
            indent(lines[repeat_idx]).saturating_add(4),
            "OPEN cursor in REPEAT body should use the REPEAT body depth, got:\n{}",
            formatted
        );
        assert_eq!(
            indent(lines[select_idx]),
            indent(lines[open_idx]).saturating_add(4),
            "REPEAT body SELECT should indent one level deeper than OPEN, got:\n{}",
            formatted
        );
        assert_eq!(
            indent(lines[nested_select_idx]),
            indent(lines[from_idx]).saturating_add(8),
            "Nested SELECT in REPEAT body should preserve the formatter's nested query-base step under FROM, got:\n{}",
            formatted
        );
        assert_eq!(
            indent(lines[end_repeat_idx]),
            indent(lines[repeat_idx]),
            "END REPEAT should align with REPEAT after nested query formatting, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_sql_basic_nested_begin_open_cursor_nested_subquery_uses_begin_body_depth() {
        let source = r#"BEGIN
IF l_ready THEN
BEGIN
OPEN c_emp FOR
SELECT empno
FROM emp;
END;
END IF;
END;"#;

        let formatted = SqlEditorWidget::format_sql_basic(source);
        let lines: Vec<&str> = formatted.lines().collect();
        let indent = |line: &str| line.len().saturating_sub(line.trim_start().len());

        let if_idx = find_line_starting_with(&lines, "IF l_ready THEN").expect("IF line");
        let begin_idx = lines
            .iter()
            .enumerate()
            .skip(if_idx + 1)
            .find(|(_, line)| line.trim_start() == "BEGIN")
            .map(|(idx, _)| idx)
            .expect("nested BEGIN line");
        let open_idx = find_line_starting_with(&lines, "OPEN c_emp FOR").expect("OPEN cursor line");
        let select_idx = lines
            .iter()
            .enumerate()
            .skip(open_idx + 1)
            .find(|(_, line)| line.trim_start() == "SELECT empno")
            .map(|(idx, _)| idx)
            .expect("SELECT line");
        let end_idx = lines
            .iter()
            .enumerate()
            .skip(select_idx + 1)
            .find(|(_, line)| line.trim_start() == "END;")
            .map(|(idx, _)| idx)
            .expect("END line");

        assert_eq!(
            indent(lines[begin_idx]),
            indent(lines[if_idx]).saturating_add(4),
            "nested BEGIN should indent one level deeper than IF, got:\n{}",
            formatted
        );
        assert_eq!(
            indent(lines[open_idx]),
            indent(lines[begin_idx]).saturating_add(4),
            "OPEN cursor after nested BEGIN should use the BEGIN body depth, got:\n{}",
            formatted
        );
        assert_eq!(
            indent(lines[select_idx]),
            indent(lines[open_idx]).saturating_add(4),
            "SELECT after OPEN cursor should indent one level deeper than OPEN, got:\n{}",
            formatted
        );
        assert_eq!(
            indent(lines[end_idx]),
            indent(lines[begin_idx]),
            "nested END should align with BEGIN after OPEN cursor query formatting, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_sql_basic_split_open_cursor_for_nested_subquery_uses_completed_owner_depth() {
        let source = r#"BEGIN
EXCEPTION
WHEN OTHERS THEN
OPEN c_emp
FOR
SELECT empno
FROM (
SELECT empno
FROM emp
);
END;"#;

        let formatted = SqlEditorWidget::format_sql_basic(source);
        let lines: Vec<&str> = formatted.lines().collect();
        let indent = |line: &str| line.len().saturating_sub(line.trim_start().len());

        let when_idx =
            find_line_starting_with(&lines, "WHEN OTHERS THEN").expect("WHEN OTHERS line");
        let open_idx = find_line_starting_with(&lines, "OPEN c_emp FOR").expect("OPEN cursor line");
        let select_idx = lines
            .iter()
            .enumerate()
            .skip(open_idx + 1)
            .find(|(_, line)| line.trim_start() == "SELECT empno")
            .map(|(idx, _)| idx)
            .expect("SELECT line");

        assert_eq!(
            indent(lines[open_idx]),
            indent(lines[when_idx]).saturating_add(4),
            "split OPEN ... FOR should collapse to the handler body depth, got:\n{}",
            formatted
        );
        assert_eq!(
            indent(lines[select_idx]),
            indent(lines[open_idx]).saturating_add(4),
            "SELECT after split OPEN ... FOR should still indent one level deeper than OPEN, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_for_auto_formatting_keeps_split_materialized_view_header_on_owner_depth() {
        let source = r#"CREATE MATERIALIZED VIEW mv_sales_dashboard BUILD DEFERRED REFRESH FAST
ON DEMAND ENABLE QUERY REWRITE AS
WITH date_dim AS (
    SELECT
        cal_date
    FROM dual
)
SELECT
    cal_date
FROM date_dim;"#;

        let formatted = SqlEditorWidget::format_for_auto_formatting(source, false);
        let expected = r#"CREATE MATERIALIZED VIEW mv_sales_dashboard BUILD DEFERRED REFRESH FAST
ON DEMAND ENABLE QUERY REWRITE AS
    WITH date_dim AS (
        SELECT cal_date
        FROM DUAL
    )
    SELECT cal_date
    FROM date_dim;"#;

        assert_eq!(
            formatted, expected,
            "split materialized-view headers should keep BUILD/ON fragments on the owner depth and indent the owned WITH query one level deeper"
        );
    }

    #[test]
    fn line_starts_structural_operator_rhs_uses_shared_owner_boundary_taxonomy() {
        assert!(SqlEditorWidget::line_starts_structural_operator_rhs(
            "CURSOR c_emp",
            None,
        ));
        assert!(SqlEditorWidget::line_starts_structural_operator_rhs(
            "WINDOW w_dept AS",
            None,
        ));
        assert!(SqlEditorWidget::line_starts_structural_operator_rhs(
            "SEARCH DEPTH FIRST BY deptno SET ord",
            None,
        ));
        assert!(SqlEditorWidget::line_starts_structural_operator_rhs(
            "(",
            Some("SELECT empno"),
        ));
        assert!(!SqlEditorWidget::line_starts_structural_operator_rhs(
            "MULTISET",
            Some("SELECT bonus"),
        ));
    }

    #[test]
    fn format_sql_basic_preserves_mysql_hash_comment_and_end_delimiter() {
        let source = r#"DELIMITER $$
CREATE PROCEDURE demo_proc()
BEGIN
# line comment torture: ; ; ; DELIMITER $$ should not matter here.....
SELECT 1;
END$$
DELIMITER ;
SELECT 2;
"#;

        let formatted = SqlEditorWidget::format_sql_basic(source);

        assert!(
            formatted
                .contains("# line comment torture: ; ; ; DELIMITER $$ should not matter here....."),
            "MySQL hash comment text should be preserved, got:\n{}",
            formatted
        );
        assert!(
            formatted.contains("END$$"),
            "custom delimiter should stay attached to the routine terminator, got:\n{}",
            formatted
        );
        assert_eq!(SqlEditorWidget::format_sql_basic(&formatted), formatted);
    }

    #[test]
    fn format_sql_basic_keeps_mysql_custom_delimiter_before_inline_hash_comment() {
        let source = r#"DELIMITER $$
CREATE PROCEDURE demo_proc()
BEGIN
SELECT 1;
END$$ # routine end marker
DELIMITER ;
"#;

        let formatted = SqlEditorWidget::format_sql_basic(source);

        assert!(
            formatted.contains("END$$ # routine end marker"),
            "custom delimiter should remain before the inline hash comment, got:\n{}",
            formatted
        );
    }

    #[test]
    fn format_sql_basic_keeps_mysql_executable_comment_as_separate_statement() {
        let source = r#"/*!80000 SET @versioned_comment_executed = 1 */
# line comment torture: ; ; ; DELIMITER $$ should not matter here
CREATE TABLE demo (
    id INT
);"#;

        let formatted = SqlEditorWidget::format_sql_basic(source);
        let items = crate::db::QueryExecutor::split_script_items(&formatted);
        let statements: Vec<&str> = items
            .iter()
            .filter_map(|item| match item {
                crate::db::ScriptItem::Statement(statement) => Some(statement.as_str()),
                _ => None,
            })
            .collect();

        assert!(
            formatted.contains("/*!80000 SET @versioned_comment_executed = 1 */;"),
            "mysql executable comment should keep its own semicolon terminator, got:\n{}",
            formatted
        );
        assert!(
            statements
                .iter()
                .any(|stmt| stmt.starts_with("/*!80000 SET @versioned_comment_executed = 1 */")),
            "mysql executable comment should remain independently executable: {statements:?}"
        );
        assert!(
            statements
                .iter()
                .any(|stmt| stmt.starts_with("CREATE TABLE demo")),
            "following CREATE TABLE should remain a separate execution unit: {statements:?}"
        );
    }

    #[test]
    fn format_sql_basic_keeps_mariadb_executable_comment_as_separate_statement() {
        let source = r#"/*M!100100 SET @feature_flag = 1 */
CREATE TABLE demo (
    id INT
);"#;

        let formatted = SqlEditorWidget::format_sql_basic(source);
        let items = crate::db::QueryExecutor::split_script_items(&formatted);
        let statements: Vec<&str> = items
            .iter()
            .filter_map(|item| match item {
                crate::db::ScriptItem::Statement(statement) => Some(statement.as_str()),
                _ => None,
            })
            .collect();

        assert!(
            formatted.contains("/*M!100100 SET @feature_flag = 1 */;"),
            "mariadb executable comment should keep its own semicolon terminator, got:\n{}",
            formatted
        );
        assert!(
            statements
                .iter()
                .any(|stmt| stmt.starts_with("/*M!100100 SET @feature_flag = 1 */")),
            "mariadb executable comment should remain independently executable: {statements:?}"
        );
        assert!(
            statements
                .iter()
                .any(|stmt| stmt.starts_with("CREATE TABLE demo")),
            "following CREATE TABLE should remain a separate execution unit: {statements:?}"
        );
    }

    #[test]
    fn format_sql_basic_auto_detects_mariadb_begin_not_atomic_block() {
        let source = r#"BEGIN NOT ATOMIC
DECLARE v_count INT DEFAULT 0;
SET v_count = v_count + 1;
END;
SELECT 1;"#;

        let formatted = SqlEditorWidget::format_sql_basic(source);
        let items = crate::db::QueryExecutor::split_script_items(&formatted);
        let statements: Vec<&str> = items
            .iter()
            .filter_map(|item| match item {
                crate::db::ScriptItem::Statement(statement) => Some(statement.as_str()),
                _ => None,
            })
            .collect();

        assert!(
            formatted.contains("BEGIN NOT ATOMIC")
                && formatted.contains("DECLARE v_count INT DEFAULT 0;")
                && formatted.contains("SET v_count = v_count + 1;")
                && formatted.contains("END;"),
            "formatter should keep the anonymous MariaDB compound block intact, got:\n{}",
            formatted
        );
        assert_eq!(
            statements.len(),
            2,
            "formatter output should still split into the anonymous block and trailing SELECT: {statements:?}"
        );
        assert!(
            statements[0].starts_with("BEGIN NOT ATOMIC") && statements[0].ends_with("END"),
            "first formatted statement should stay the anonymous MariaDB block: {}",
            statements[0]
        );
        assert_eq!(statements[1], "SELECT 1");
        assert_eq!(SqlEditorWidget::format_sql_basic(&formatted), formatted);
    }

    #[test]
    fn format_sql_basic_for_mysql_db_type_keeps_double_dash_arithmetic_as_expression() {
        let source = "SELECT 5--2;\nSELECT 9;";

        let formatted = SqlEditorWidget::format_sql_basic_for_db_type(
            source,
            crate::db::connection::DatabaseType::MySQL,
        );
        let items = crate::db::QueryExecutor::split_script_items_for_db_type(
            &formatted,
            Some(crate::db::connection::DatabaseType::MySQL),
        );
        let statements: Vec<&str> = items
            .iter()
            .filter_map(|item| match item {
                crate::db::ScriptItem::Statement(statement) => Some(statement.as_str()),
                _ => None,
            })
            .collect();

        assert!(
            formatted.contains("SELECT 5 - - 2;"),
            "formatter should keep the arithmetic expression executable instead of turning it into a comment: {formatted}"
        );
        assert_eq!(
            statements,
            vec!["SELECT 5 - - 2", "SELECT 9"],
            "MySQL formatter must keep `--2` arithmetic and the following statement separate: {statements:?}"
        );
    }

    #[test]
    fn format_sql_basic_for_mysql_db_type_handles_mysql_compound_rules_without_markers() {
        let source = r#"CREATE PROCEDURE demo_proc()
BEGIN
DECLARE EXIT HANDLER FOR SQLEXCEPTION
BEGIN
RESIGNAL;
END;
END;"#;

        let formatted = SqlEditorWidget::format_sql_basic_for_db_type(
            source,
            crate::db::connection::DatabaseType::MySQL,
        );

        assert!(
            formatted.contains("DECLARE EXIT HANDLER FOR SQLEXCEPTION"),
            "MySQL db type should preserve handler FOR clause without extra line breaks, got:\n{}",
            formatted
        );
        assert_eq!(
            SqlEditorWidget::format_sql_basic_for_db_type(
                &formatted,
                crate::db::connection::DatabaseType::MySQL,
            ),
            formatted
        );
    }

    #[test]
    fn format_sql_basic_for_mysql_db_type_keeps_test1_function_parens_tight() {
        let source = include_str!("../../../test_mysql/test1.txt");
        let formatted = SqlEditorWidget::format_sql_basic_for_db_type(
            source,
            crate::db::connection::DatabaseType::MySQL,
        );

        let forbidden_snippets = [
            "fn_currency_rate (",
            "sp_assert (",
            "sp_run_final_boss ()",
            "UPPER (",
            "TRIM (",
            "CURRENT_TIMESTAMP (",
            "JSON_UNQUOTE (",
            "JSON_EXTRACT (",
        ];

        for snippet in forbidden_snippets {
            assert!(
                !formatted.contains(snippet),
                "formatted test1 output is introducing a MySQL/MariaDB function gap around `{snippet}`:\n{formatted}"
            );
        }
    }

    #[test]
    fn format_sql_basic_for_mysql_db_type_keeps_test1_mysql_identifier_case_and_values_function_calls(
    ) {
        let source = include_str!("../../../test_mysql/test1.txt");
        let formatted = SqlEditorWidget::format_sql_basic_for_db_type(
            source,
            crate::db::connection::DatabaseType::MySQL,
        );

        let required_snippets = [
            "JSON_EXTRACT(profile, '$.locale')",
            "JSON_EXTRACT(profile, '$.skills')",
            "VALUES(dept_name)",
            "VALUES(sort_no)",
            "VALUES(`group`)",
            "VALUES(summary_value_decimal)",
            "VALUES(summary_value_text)",
            "VALUES(summary_json)",
        ];

        for snippet in required_snippets {
            assert!(
                formatted.contains(snippet),
                "formatted test1 output should keep MySQL identifier/function rendering for `{snippet}`:\n{formatted}"
            );
        }

        let forbidden_snippets = [
            "JSON_EXTRACT(PROFILE, '$.locale')",
            "JSON_EXTRACT(PROFILE, '$.skills')",
            "VALUES (dept_name)",
            "VALUES (sort_no)",
            "VALUES (`group`)",
            "VALUES (summary_value_decimal)",
            "VALUES (summary_value_text)",
            "VALUES (summary_json)",
        ];

        for snippet in forbidden_snippets {
            assert!(
                !formatted.contains(snippet),
                "formatted test1 output should not regress to `{snippet}`:\n{formatted}"
            );
        }
    }

    #[test]
    fn format_sql_basic_for_mysql_db_type_keeps_on_duplicate_multiline_call_siblings_on_parent_frame_depth(
    ) {
        let source = r#"INSERT INTO dept (dept_id, parent_dept_id, dept_code, dept_name, sort_no)
VALUES (20, 10, 'SALES', 'Sales-upsert-check', 2)
ON DUPLICATE KEY UPDATE dept_name = CONCAT(
VALUES(dept_name),
' / touched'
),
sort_no = VALUES(sort_no);"#;

        let formatted = SqlEditorWidget::format_sql_basic_for_db_type(
            source,
            crate::db::connection::DatabaseType::MySQL,
        );
        let lines: Vec<&str> = formatted.lines().collect();
        let on_duplicate_idx =
            find_line_starting_with(&lines, "ON DUPLICATE KEY UPDATE dept_name = CONCAT(")
                .expect("ON DUPLICATE KEY UPDATE owner");
        let values_idx =
            find_line_starting_with(&lines, "VALUES(dept_name),").expect("VALUES() sibling");
        let literal_idx = find_line_starting_with(&lines, "' / touched'").expect("literal sibling");
        let close_idx = lines
            .iter()
            .enumerate()
            .skip(literal_idx.saturating_add(1))
            .find(|(_, line)| line.trim_start() == "),")
            .map(|(idx, _)| idx)
            .expect("CONCAT close");

        assert!(
            leading_spaces(lines[values_idx]) > leading_spaces(lines[on_duplicate_idx]),
            "VALUES() argument should stay nested under the ON DUPLICATE call owner, got:\n{formatted}"
        );
        assert_eq!(
            leading_spaces(lines[literal_idx]),
            leading_spaces(lines[values_idx]),
            "wrapped call siblings inside ON DUPLICATE KEY UPDATE should reuse the parent frame body depth, got:\n{formatted}"
        );
        assert_eq!(
            leading_spaces(lines[close_idx]),
            leading_spaces(lines[on_duplicate_idx]),
            "wrapped call close should realign with the ON DUPLICATE owner depth, got:\n{formatted}"
        );
        assert_eq!(
            SqlEditorWidget::format_sql_basic_for_db_type(
                &formatted,
                crate::db::connection::DatabaseType::MySQL,
            ),
            formatted
        );
    }

    #[test]
    fn format_sql_basic_for_mysql_db_type_indents_on_duplicate_key_update_assignment_siblings() {
        let source = r#"INSERT INTO dept (dept_id, dept_name, sort_no, updated_at)
VALUES (20, 'SALES', 2, NOW())
ON DUPLICATE KEY UPDATE dept_name = VALUES(dept_name),
sort_no = VALUES(sort_no),
updated_at = NOW();"#;

        let formatted = SqlEditorWidget::format_sql_basic_for_db_type(
            source,
            crate::db::connection::DatabaseType::MySQL,
        );
        let lines: Vec<&str> = formatted.lines().collect();
        let on_duplicate_idx = find_line_starting_with(
            &lines,
            "ON DUPLICATE KEY UPDATE dept_name = VALUES(dept_name),",
        )
        .expect("ON DUPLICATE KEY UPDATE owner");
        let sort_no_idx =
            find_line_starting_with(&lines, "sort_no = VALUES(sort_no),").expect("sort_no sibling");
        let updated_at_idx =
            find_line_starting_with(&lines, "updated_at = NOW();").expect("updated_at sibling");

        assert_eq!(
            leading_spaces(lines[sort_no_idx]),
            leading_spaces(lines[on_duplicate_idx]).saturating_add(4),
            "ON DUPLICATE KEY UPDATE assignment siblings should indent one level deeper than the owner, got:\n{formatted}"
        );
        assert_eq!(
            leading_spaces(lines[updated_at_idx]),
            leading_spaces(lines[sort_no_idx]),
            "ON DUPLICATE KEY UPDATE sibling assignments should stay on the same list depth, got:\n{formatted}"
        );
        assert_eq!(
            SqlEditorWidget::format_sql_basic_for_db_type(
                &formatted,
                crate::db::connection::DatabaseType::MySQL,
            ),
            formatted
        );
    }

    #[test]
    fn format_sql_basic_for_mysql_db_type_reapplies_frame_depth_after_nested_subquery_close_comma()
    {
        let source = r#"SELECT ROUND(
(
SELECT AVG (s2.score)
FROM emp_score s2
WHERE s2.empno = s.empno
),
2
),
s.ename
FROM emp_score s;"#;

        let formatted = SqlEditorWidget::format_sql_basic_for_db_type(
            source,
            crate::db::connection::DatabaseType::MySQL,
        );
        let lines: Vec<&str> = formatted.lines().collect();
        let select_idx = find_line_starting_with(&lines, "SELECT").expect("SELECT line");
        let round_idx = find_line_starting_with(&lines, "ROUND(")
            .or_else(|| find_line_starting_with(&lines, "SELECT ROUND("))
            .expect("ROUND owner");
        let sibling_idx = find_line_starting_with(&lines, "s.ename").expect("SELECT sibling");
        let outer_close_idx = lines
            .iter()
            .enumerate()
            .take(sibling_idx)
            .rfind(|(_, line)| line.trim_start() == "),")
            .map(|(idx, _)| idx)
            .expect("ROUND close-comma");
        let from_idx = find_line_starting_with(&lines, "FROM emp_score s;").expect("FROM line");

        assert_eq!(
            leading_spaces(lines[outer_close_idx]),
            leading_spaces(lines[round_idx]),
            "ROUND close-comma should return to the owner depth, got:\n{formatted}"
        );
        assert_eq!(
            leading_spaces(lines[sibling_idx]),
            leading_spaces(lines[select_idx]).saturating_add(4),
            "SELECT sibling after a nested close-comma must realign to the SELECT-list frame depth, got:\n{formatted}"
        );
        assert!(
            leading_spaces(lines[from_idx]) < leading_spaces(lines[sibling_idx]),
            "FROM should clear the carried SELECT list depth after the nested ROUND item closes, got:\n{formatted}"
        );
        assert_eq!(
            SqlEditorWidget::format_sql_basic_for_db_type(
                &formatted,
                crate::db::connection::DatabaseType::MySQL,
            ),
            formatted,
            "MySQL nested close-comma realignment should stay idempotent once frame-based depth wins"
        );
    }

    #[test]
    fn mysql_formatter_keeps_sibling_depth_after_nested_close_comma() {
        let source = r#"SELECT ROUND(
(
SELECT AVG (s2.score)
FROM emp_score s2
WHERE s2.empno = s.empno
),
2
),
            s.ename
FROM emp_score s;"#;

        let normalized = SqlEditorWidget::format_sql_basic_for_db_type(
            source,
            crate::db::connection::DatabaseType::MySQL,
        );
        let lines: Vec<&str> = normalized.lines().collect();
        let select_idx = find_line_starting_with(&lines, "SELECT").expect("SELECT line");
        let _round_idx = find_line_starting_with(&lines, "ROUND(")
            .or_else(|| find_line_starting_with(&lines, "SELECT ROUND("))
            .expect("ROUND owner");
        let sibling_idx = find_line_starting_with(&lines, "s.ename").expect("SELECT sibling");

        assert_eq!(
            leading_spaces(lines[sibling_idx]),
            leading_spaces(lines[select_idx]).saturating_add(4),
            "MySQL formatter should keep sibling depth after nested close-comma, got:\n{normalized}"
        );
    }

    #[test]
    fn mysql_formatter_keeps_select_list_sibling_depth_after_nested_subquery_close() {
        let source = r#"SELECT JSON_OBJECT(
'avg_score',
(
SELECT AVG (s2.score)
FROM emp_score s2
WHERE s2.empno = s.empno
),
'employee_name',
        s.ename
)
FROM emp_score s;"#;

        let normalized = SqlEditorWidget::format_sql_basic_for_db_type(
            source,
            crate::db::connection::DatabaseType::MySQL,
        );
        let lines: Vec<&str> = normalized.lines().collect();
        let key_idx = find_line_starting_with(&lines, "'employee_name',");
        let value_idx = find_line_starting_with(&lines, "s.ename");

        if let (Some(key_idx), Some(value_idx)) = (key_idx, value_idx) {
            assert_eq!(
                leading_spaces(lines[value_idx]),
                leading_spaces(lines[key_idx]),
                "MySQL formatter should keep select-list sibling depth after nested subquery close, got:\n{normalized}"
            );
        } else {
            assert!(
                normalized.contains("'employee_name', s.ename"),
                "MySQL formatter should keep the select-list sibling pair adjacent even when rendered inline, got:\n{normalized}"
            );
        }
    }

    #[test]
    fn format_sql_basic_for_mysql_db_type_keeps_test2_function_parens_tight() {
        let source = include_str!("../../../test_mysql/test2.txt");
        let formatted = SqlEditorWidget::format_sql_basic_for_db_type(
            source,
            crate::db::connection::DatabaseType::MySQL,
        );

        let forbidden_snippets = [
            "fn_priority_weight (",
            "sp_check (",
            "sp_touch_rank (",
            "sp_status_counts (",
            "LOWER (",
            "TRIM (",
            "SUM (",
        ];

        for snippet in forbidden_snippets {
            assert!(
                !formatted.contains(snippet),
                "formatted test2 output is introducing a MySQL/MariaDB function gap around `{snippet}`:\n{formatted}"
            );
        }
    }

    #[test]
    fn format_sql_basic_for_mysql_db_type_keeps_test2_values_function_calls_tight() {
        let source = include_str!("../../../test_mysql/test2.txt");
        let formatted = SqlEditorWidget::format_sql_basic_for_db_type(
            source,
            crate::db::connection::DatabaseType::MySQL,
        );

        let required_snippets = [
            "VALUES(node_name)",
            "VALUES(sort_no)",
            "VALUES(`group`)",
            "VALUES(result_num)",
            "VALUES(result_text)",
            "VALUES(result_json)",
        ];

        for snippet in required_snippets {
            assert!(
                formatted.contains(snippet),
                "formatted test2 output should keep MySQL VALUES() rendering for `{snippet}`:\n{formatted}"
            );
        }

        let forbidden_snippets = [
            "VALUES (node_name)",
            "VALUES (sort_no)",
            "VALUES (`group`)",
            "VALUES (result_num)",
            "VALUES (result_text)",
            "VALUES (result_json)",
        ];

        for snippet in forbidden_snippets {
            assert!(
                !formatted.contains(snippet),
                "formatted test2 output should not regress to `{snippet}`:\n{formatted}"
            );
        }
    }

    #[test]
    fn format_sql_basic_for_mysql_db_type_keeps_test3_function_parens_tight() {
        let source = include_str!("../../../test_mysql/test3.txt");
        let formatted = SqlEditorWidget::format_sql_basic_for_db_type(
            source,
            crate::db::connection::DatabaseType::MySQL,
        );

        let forbidden_snippets = [
            "fn_priority_factor (",
            "sp_assert (",
            "sp_shift_rank (",
            "sp_collect_status_counts (",
            "LOWER (",
            "TRIM (",
            "COALESCE (",
            "CAST (",
            "JSON_LENGTH (",
            "JSON_EXTRACT (",
        ];

        for snippet in forbidden_snippets {
            assert!(
                !formatted.contains(snippet),
                "formatted test3 output is introducing a MySQL/MariaDB function gap around `{snippet}`:\n{formatted}"
            );
        }
    }

    #[test]
    fn format_sql_basic_for_mysql_db_type_keeps_test3_values_function_calls_tight() {
        let source = include_str!("../../../test_mysql/test3.txt");
        let formatted = SqlEditorWidget::format_sql_basic_for_db_type(
            source,
            crate::db::connection::DatabaseType::MySQL,
        );

        let required_snippets = [
            "VALUES(node_name)",
            "VALUES(sort_no)",
            "VALUES(`group`)",
            "VALUES(`rank`)",
            "VALUES(summary_num)",
            "VALUES(summary_text)",
            "VALUES(summary_json)",
        ];

        for snippet in required_snippets {
            assert!(
                formatted.contains(snippet),
                "formatted test3 output should keep MySQL VALUES() rendering for `{snippet}`:\n{formatted}"
            );
        }

        let forbidden_snippets = [
            "VALUES (node_name)",
            "VALUES (sort_no)",
            "VALUES (`group`)",
            "VALUES (`rank`)",
            "VALUES (summary_num)",
            "VALUES (summary_text)",
            "VALUES (summary_json)",
        ];

        for snippet in forbidden_snippets {
            assert!(
                !formatted.contains(snippet),
                "formatted test3 output should not regress to `{snippet}`:\n{formatted}"
            );
        }
    }

    #[test]
    fn format_sql_basic_for_mysql_db_type_keeps_mariadb_function_headers_split() {
        let cases = [
            (
                include_str!("../../../test_mysql/test1.txt"),
                "CREATE FUNCTION fn_currency_rate(p_currency_code CHAR(3))",
            ),
            (
                include_str!("../../../test_mysql/test2.txt"),
                "CREATE FUNCTION fn_priority_weight(p_priority INT)",
            ),
            (
                include_str!("../../../test_mysql/test3.txt"),
                "CREATE FUNCTION fn_priority_factor(p_priority INT)",
            ),
            (
                include_str!("../../../test_mariadb/test6.txt"),
                "CREATE FUNCTION fn_month_key(p_date DATE)",
            ),
            (
                include_str!("../../../test_mariadb/test6.txt"),
                "CREATE FUNCTION fn_order_risk(p_order_id INT)",
            ),
        ];

        for (source, snippet) in cases {
            let formatted = SqlEditorWidget::format_sql_basic_for_db_type(
                source,
                crate::db::connection::DatabaseType::MySQL,
            );

            assert!(
                formatted.contains(snippet),
                "formatted MySQL/MariaDB function header should preserve function signature text, got:\n{formatted}"
            );
            assert!(
                formatted.contains("RETURNS"),
                "formatted MySQL/MariaDB function should keep RETURNS clause text, got:\n{formatted}"
            );
            assert_eq!(
                SqlEditorWidget::format_sql_basic_for_db_type(
                    &formatted,
                    crate::db::connection::DatabaseType::MySQL,
                ),
                formatted
            );
        }
    }

    #[test]
    fn format_sql_basic_for_mysql_db_type_keeps_test1_declare_cursor_and_handlers_nested() {
        let source = include_str!("../../../test_mysql/test1.txt");
        let formatted = SqlEditorWidget::format_sql_basic_for_db_type(
            source,
            crate::db::connection::DatabaseType::MySQL,
        );
        let lines: Vec<&str> = formatted.lines().collect();

        let cursor_idx =
            find_line_starting_with(&lines, "DECLARE cur_emp CURSOR FOR").expect("cursor header");
        let cursor_select_idx = lines
            .iter()
            .enumerate()
            .skip(cursor_idx + 1)
            .find(|(_, line)| line.trim_start() == "SELECT emp_id,")
            .map(|(idx, _)| idx)
            .expect("cursor SELECT");
        let cursor_from_idx = lines
            .iter()
            .enumerate()
            .skip(cursor_select_idx + 1)
            .find(|(_, line)| line.trim_start() == "FROM emp")
            .map(|(idx, _)| idx)
            .expect("cursor FROM");
        let cursor_order_idx = lines
            .iter()
            .enumerate()
            .skip(cursor_from_idx + 1)
            .find(|(_, line)| line.trim_start() == "ORDER BY emp_id;")
            .map(|(idx, _)| idx)
            .expect("cursor ORDER BY");
        let not_found_idx =
            find_line_starting_with(&lines, "DECLARE CONTINUE HANDLER FOR NOT FOUND")
                .expect("NOT FOUND handler");
        let not_found_set_idx = lines
            .iter()
            .enumerate()
            .skip(not_found_idx + 1)
            .find(|(_, line)| line.trim_start() == "SET v_done = 1;")
            .map(|(idx, _)| idx)
            .expect("NOT FOUND SET");
        let duplicate_idx = find_line_starting_with(&lines, "DECLARE CONTINUE HANDLER FOR 1062")
            .expect("duplicate handler");
        let duplicate_begin_idx = lines
            .iter()
            .enumerate()
            .skip(duplicate_idx + 1)
            .find(|(_, line)| line.trim_start() == "BEGIN")
            .map(|(idx, _)| idx)
            .expect("duplicate handler BEGIN");
        let duplicate_diag_idx = lines
            .iter()
            .enumerate()
            .skip(duplicate_begin_idx + 1)
            .find(|(_, line)| line.trim_start().starts_with("GET DIAGNOSTICS CONDITION 1"))
            .map(|(idx, _)| idx)
            .expect("duplicate handler diagnostics");
        let duplicate_end_idx = lines
            .iter()
            .enumerate()
            .skip(duplicate_diag_idx + 1)
            .find(|(_, line)| line.trim_start() == "END;")
            .map(|(idx, _)| idx)
            .expect("duplicate handler END");
        let exit_idx = find_line_starting_with(&lines, "DECLARE EXIT HANDLER FOR SQLEXCEPTION")
            .expect("exit handler");
        let exit_begin_idx = lines
            .iter()
            .enumerate()
            .skip(exit_idx + 1)
            .find(|(_, line)| line.trim_start() == "BEGIN")
            .map(|(idx, _)| idx)
            .expect("exit handler BEGIN");
        let exit_diag_idx = lines
            .iter()
            .enumerate()
            .skip(exit_begin_idx + 1)
            .find(|(_, line)| line.trim_start().starts_with("GET DIAGNOSTICS CONDITION 1"))
            .map(|(idx, _)| idx)
            .expect("exit handler diagnostics");
        let exit_end_idx = lines
            .iter()
            .enumerate()
            .skip(exit_diag_idx + 1)
            .find(|(_, line)| line.trim_start() == "END;")
            .map(|(idx, _)| idx)
            .expect("exit handler END");

        assert_eq!(
            leading_spaces(lines[cursor_select_idx]),
            leading_spaces(lines[cursor_idx]).saturating_add(4),
            "test1 cursor SELECT should stay one level deeper than DECLARE ... CURSOR FOR, got:\n{formatted}"
        );
        assert_eq!(
            leading_spaces(lines[cursor_from_idx]),
            leading_spaces(lines[cursor_select_idx]),
            "test1 cursor FROM should stay aligned with the cursor SELECT query base, got:\n{formatted}"
        );
        assert_eq!(
            leading_spaces(lines[cursor_order_idx]),
            leading_spaces(lines[cursor_select_idx]),
            "test1 cursor ORDER BY should stay aligned with the cursor SELECT query base, got:\n{formatted}"
        );
        assert_eq!(
            leading_spaces(lines[not_found_set_idx]),
            leading_spaces(lines[not_found_idx]).saturating_add(4),
            "test1 NOT FOUND handler body should stay one level deeper than DECLARE ... HANDLER FOR, got:\n{formatted}"
        );
        assert_eq!(
            leading_spaces(lines[duplicate_begin_idx]),
            leading_spaces(lines[duplicate_idx]).saturating_add(4),
            "test1 duplicate-key handler block should stay one level deeper than DECLARE ... HANDLER FOR, got:\n{formatted}"
        );
        assert_eq!(
            leading_spaces(lines[duplicate_diag_idx]),
            leading_spaces(lines[duplicate_begin_idx]).saturating_add(4),
            "test1 duplicate-key handler statements should stay one level deeper than handler BEGIN, got:\n{formatted}"
        );
        assert_eq!(
            leading_spaces(lines[duplicate_end_idx]),
            leading_spaces(lines[duplicate_begin_idx]),
            "test1 duplicate-key handler END should realign with handler BEGIN, got:\n{formatted}"
        );
        assert_eq!(
            leading_spaces(lines[exit_begin_idx]),
            leading_spaces(lines[exit_idx]).saturating_add(4),
            "test1 SQLEXCEPTION handler block should stay one level deeper than DECLARE ... HANDLER FOR, got:\n{formatted}"
        );
        assert_eq!(
            leading_spaces(lines[exit_diag_idx]),
            leading_spaces(lines[exit_begin_idx]).saturating_add(4),
            "test1 SQLEXCEPTION handler statements should stay one level deeper than handler BEGIN, got:\n{formatted}"
        );
        assert_eq!(
            leading_spaces(lines[exit_end_idx]),
            leading_spaces(lines[exit_begin_idx]),
            "test1 SQLEXCEPTION handler END should realign with handler BEGIN, got:\n{formatted}"
        );
    }

    #[test]
    fn format_sql_basic_for_mysql_db_type_keeps_test2_declare_cursor_and_handlers_nested() {
        let source = include_str!("../../../test_mysql/test2.txt");
        let formatted = SqlEditorWidget::format_sql_basic_for_db_type(
            source,
            crate::db::connection::DatabaseType::MySQL,
        );
        let lines: Vec<&str> = formatted.lines().collect();

        let cursor_idx =
            find_line_starting_with(&lines, "DECLARE cur_task CURSOR FOR").expect("cursor header");
        let cursor_select_idx = lines
            .iter()
            .enumerate()
            .skip(cursor_idx + 1)
            .find(|(_, line)| line.trim_start() == "SELECT task_id,")
            .map(|(idx, _)| idx)
            .expect("cursor SELECT");
        let cursor_from_idx = lines
            .iter()
            .enumerate()
            .skip(cursor_select_idx + 1)
            .find(|(_, line)| line.trim_start() == "FROM task")
            .map(|(idx, _)| idx)
            .expect("cursor FROM");
        let cursor_order_idx = lines
            .iter()
            .enumerate()
            .skip(cursor_from_idx + 1)
            .find(|(_, line)| line.trim_start() == "ORDER BY task_id;")
            .map(|(idx, _)| idx)
            .expect("cursor ORDER BY");
        let not_found_idx =
            find_line_starting_with(&lines, "DECLARE CONTINUE HANDLER FOR NOT FOUND")
                .expect("NOT FOUND handler");
        let not_found_set_idx = lines
            .iter()
            .enumerate()
            .skip(not_found_idx + 1)
            .find(|(_, line)| line.trim_start() == "SET v_fetch_done = 1;")
            .map(|(idx, _)| idx)
            .expect("NOT FOUND SET");
        let duplicate_idx = find_line_starting_with(&lines, "DECLARE CONTINUE HANDLER FOR 1062")
            .expect("duplicate handler");
        let duplicate_begin_idx = lines
            .iter()
            .enumerate()
            .skip(duplicate_idx + 1)
            .find(|(_, line)| line.trim_start() == "BEGIN")
            .map(|(idx, _)| idx)
            .expect("duplicate handler BEGIN");
        let duplicate_diag_idx = lines
            .iter()
            .enumerate()
            .skip(duplicate_begin_idx + 1)
            .find(|(_, line)| line.trim_start().starts_with("GET DIAGNOSTICS CONDITION 1"))
            .map(|(idx, _)| idx)
            .expect("duplicate handler diagnostics");
        let duplicate_end_idx = lines
            .iter()
            .enumerate()
            .skip(duplicate_diag_idx + 1)
            .find(|(_, line)| line.trim_start() == "END;")
            .map(|(idx, _)| idx)
            .expect("duplicate handler END");
        let exit_idx = find_line_starting_with(&lines, "DECLARE EXIT HANDLER FOR SQLEXCEPTION")
            .expect("exit handler");
        let exit_begin_idx = lines
            .iter()
            .enumerate()
            .skip(exit_idx + 1)
            .find(|(_, line)| line.trim_start() == "BEGIN")
            .map(|(idx, _)| idx)
            .expect("exit handler BEGIN");
        let exit_diag_idx = lines
            .iter()
            .enumerate()
            .skip(exit_begin_idx + 1)
            .find(|(_, line)| line.trim_start().starts_with("GET DIAGNOSTICS CONDITION 1"))
            .map(|(idx, _)| idx)
            .expect("exit handler diagnostics");
        let exit_end_idx = lines
            .iter()
            .enumerate()
            .skip(exit_diag_idx + 1)
            .find(|(_, line)| line.trim_start() == "END;")
            .map(|(idx, _)| idx)
            .expect("exit handler END");

        assert_eq!(
            leading_spaces(lines[cursor_select_idx]),
            leading_spaces(lines[cursor_idx]).saturating_add(4),
            "test2 cursor SELECT should stay one level deeper than DECLARE ... CURSOR FOR, got:\n{formatted}"
        );
        assert_eq!(
            leading_spaces(lines[cursor_from_idx]),
            leading_spaces(lines[cursor_select_idx]),
            "test2 cursor FROM should stay aligned with the cursor SELECT query base, got:\n{formatted}"
        );
        assert_eq!(
            leading_spaces(lines[cursor_order_idx]),
            leading_spaces(lines[cursor_select_idx]),
            "test2 cursor ORDER BY should stay aligned with the cursor SELECT query base, got:\n{formatted}"
        );
        assert_eq!(
            leading_spaces(lines[not_found_set_idx]),
            leading_spaces(lines[not_found_idx]).saturating_add(4),
            "test2 NOT FOUND handler body should stay one level deeper than DECLARE ... HANDLER FOR, got:\n{formatted}"
        );
        assert_eq!(
            leading_spaces(lines[duplicate_begin_idx]),
            leading_spaces(lines[duplicate_idx]).saturating_add(4),
            "test2 duplicate-key handler block should stay one level deeper than DECLARE ... HANDLER FOR, got:\n{formatted}"
        );
        assert_eq!(
            leading_spaces(lines[duplicate_diag_idx]),
            leading_spaces(lines[duplicate_begin_idx]).saturating_add(4),
            "test2 duplicate-key handler statements should stay one level deeper than handler BEGIN, got:\n{formatted}"
        );
        assert_eq!(
            leading_spaces(lines[duplicate_end_idx]),
            leading_spaces(lines[duplicate_begin_idx]),
            "test2 duplicate-key handler END should realign with handler BEGIN, got:\n{formatted}"
        );
        assert_eq!(
            leading_spaces(lines[exit_begin_idx]),
            leading_spaces(lines[exit_idx]).saturating_add(4),
            "test2 SQLEXCEPTION handler block should stay one level deeper than DECLARE ... HANDLER FOR, got:\n{formatted}"
        );
        assert_eq!(
            leading_spaces(lines[exit_diag_idx]),
            leading_spaces(lines[exit_begin_idx]).saturating_add(4),
            "test2 SQLEXCEPTION handler statements should stay one level deeper than handler BEGIN, got:\n{formatted}"
        );
        assert_eq!(
            leading_spaces(lines[exit_end_idx]),
            leading_spaces(lines[exit_begin_idx]),
            "test2 SQLEXCEPTION handler END should realign with handler BEGIN, got:\n{formatted}"
        );
    }

    #[test]
    fn format_sql_basic_for_mysql_db_type_keeps_test3_declare_cursor_and_handlers_nested() {
        let source = include_str!("../../../test_mysql/test3.txt");
        let formatted = SqlEditorWidget::format_sql_basic_for_db_type(
            source,
            crate::db::connection::DatabaseType::MySQL,
        );
        let lines: Vec<&str> = formatted.lines().collect();

        let cursor_idx =
            find_line_starting_with(&lines, "DECLARE cur_run CURSOR FOR").expect("cursor header");
        let cursor_select_idx = lines
            .iter()
            .enumerate()
            .skip(cursor_idx + 1)
            .find(|(_, line)| line.trim_start() == "SELECT run_id,")
            .map(|(idx, _)| idx)
            .expect("cursor SELECT");
        let cursor_from_idx = lines
            .iter()
            .enumerate()
            .skip(cursor_select_idx + 1)
            .find(|(_, line)| line.trim_start() == "FROM run_case")
            .map(|(idx, _)| idx)
            .expect("cursor FROM");
        let cursor_order_idx = lines
            .iter()
            .enumerate()
            .skip(cursor_from_idx + 1)
            .find(|(_, line)| line.trim_start() == "ORDER BY run_id;")
            .map(|(idx, _)| idx)
            .expect("cursor ORDER BY");
        let not_found_idx =
            find_line_starting_with(&lines, "DECLARE CONTINUE HANDLER FOR NOT FOUND")
                .expect("NOT FOUND handler");
        let not_found_set_idx = lines
            .iter()
            .enumerate()
            .skip(not_found_idx + 1)
            .find(|(_, line)| line.trim_start() == "SET v_fetch_done = 1;")
            .map(|(idx, _)| idx)
            .expect("NOT FOUND SET");
        let duplicate_idx = find_line_starting_with(&lines, "DECLARE CONTINUE HANDLER FOR 1062")
            .expect("duplicate handler");
        let duplicate_begin_idx = lines
            .iter()
            .enumerate()
            .skip(duplicate_idx + 1)
            .find(|(_, line)| line.trim_start() == "BEGIN")
            .map(|(idx, _)| idx)
            .expect("duplicate handler BEGIN");
        let duplicate_diag_idx = lines
            .iter()
            .enumerate()
            .skip(duplicate_begin_idx + 1)
            .find(|(_, line)| line.trim_start().starts_with("GET DIAGNOSTICS CONDITION 1"))
            .map(|(idx, _)| idx)
            .expect("duplicate handler diagnostics");
        let duplicate_end_idx = lines
            .iter()
            .enumerate()
            .skip(duplicate_diag_idx + 1)
            .find(|(_, line)| line.trim_start() == "END;")
            .map(|(idx, _)| idx)
            .expect("duplicate handler END");
        let exit_idx = find_line_starting_with(&lines, "DECLARE EXIT HANDLER FOR SQLEXCEPTION")
            .expect("exit handler");
        let exit_begin_idx = lines
            .iter()
            .enumerate()
            .skip(exit_idx + 1)
            .find(|(_, line)| line.trim_start() == "BEGIN")
            .map(|(idx, _)| idx)
            .expect("exit handler BEGIN");
        let exit_diag_idx = lines
            .iter()
            .enumerate()
            .skip(exit_begin_idx + 1)
            .find(|(_, line)| line.trim_start().starts_with("GET DIAGNOSTICS CONDITION 1"))
            .map(|(idx, _)| idx)
            .expect("exit handler diagnostics");
        let exit_end_idx = lines
            .iter()
            .enumerate()
            .skip(exit_diag_idx + 1)
            .find(|(_, line)| line.trim_start() == "END;")
            .map(|(idx, _)| idx)
            .expect("exit handler END");

        assert_eq!(
            leading_spaces(lines[cursor_select_idx]),
            leading_spaces(lines[cursor_idx]).saturating_add(4),
            "test3 cursor SELECT should stay one level deeper than DECLARE ... CURSOR FOR, got:\n{formatted}"
        );
        assert_eq!(
            leading_spaces(lines[cursor_from_idx]),
            leading_spaces(lines[cursor_select_idx]),
            "test3 cursor FROM should stay aligned with the cursor SELECT query base, got:\n{formatted}"
        );
        assert_eq!(
            leading_spaces(lines[cursor_order_idx]),
            leading_spaces(lines[cursor_select_idx]),
            "test3 cursor ORDER BY should stay aligned with the cursor SELECT query base, got:\n{formatted}"
        );
        assert_eq!(
            leading_spaces(lines[not_found_set_idx]),
            leading_spaces(lines[not_found_idx]).saturating_add(4),
            "test3 NOT FOUND handler body should stay one level deeper than DECLARE ... HANDLER FOR, got:\n{formatted}"
        );
        assert_eq!(
            leading_spaces(lines[duplicate_begin_idx]),
            leading_spaces(lines[duplicate_idx]).saturating_add(4),
            "test3 duplicate-key handler block should stay one level deeper than DECLARE ... HANDLER FOR, got:\n{formatted}"
        );
        assert_eq!(
            leading_spaces(lines[duplicate_diag_idx]),
            leading_spaces(lines[duplicate_begin_idx]).saturating_add(4),
            "test3 duplicate-key handler statements should stay one level deeper than handler BEGIN, got:\n{formatted}"
        );
        assert_eq!(
            leading_spaces(lines[duplicate_end_idx]),
            leading_spaces(lines[duplicate_begin_idx]),
            "test3 duplicate-key handler END should realign with handler BEGIN, got:\n{formatted}"
        );
        assert_eq!(
            leading_spaces(lines[exit_begin_idx]),
            leading_spaces(lines[exit_idx]).saturating_add(4),
            "test3 SQLEXCEPTION handler block should stay one level deeper than DECLARE ... HANDLER FOR, got:\n{formatted}"
        );
        assert_eq!(
            leading_spaces(lines[exit_diag_idx]),
            leading_spaces(lines[exit_begin_idx]).saturating_add(4),
            "test3 SQLEXCEPTION handler statements should stay one level deeper than handler BEGIN, got:\n{formatted}"
        );
        assert_eq!(
            leading_spaces(lines[exit_end_idx]),
            leading_spaces(lines[exit_begin_idx]),
            "test3 SQLEXCEPTION handler END should realign with handler BEGIN, got:\n{formatted}"
        );
    }

    #[test]
    fn format_sql_basic_for_mysql_db_type_keeps_split_not_found_handler_header_out_of_body_indent()
    {
        let source = r#"CREATE PROCEDURE demo_proc()
BEGIN
DECLARE CONTINUE HANDLER FOR -- fetch exhausted
NOT FOUND
SET v_done = 1;
END;"#;

        let formatted = SqlEditorWidget::format_sql_basic_for_db_type(
            source,
            crate::db::connection::DatabaseType::MySQL,
        );
        let lines: Vec<&str> = formatted.lines().collect();
        let handler_idx =
            find_line_starting_with(&lines, "DECLARE CONTINUE HANDLER FOR -- fetch exhausted")
                .expect("split NOT FOUND handler header");
        let not_found_idx =
            find_line_starting_with(&lines, "NOT FOUND").expect("NOT FOUND continuation");
        let set_idx = find_line_starting_with(&lines, "SET v_done = 1;").expect("handler SET");

        assert_eq!(
            leading_spaces(lines[not_found_idx]),
            leading_spaces(lines[handler_idx]),
            "split NOT FOUND condition line should stay aligned with DECLARE ... HANDLER FOR, got:\n{formatted}"
        );
        assert_eq!(
            leading_spaces(lines[set_idx]),
            leading_spaces(lines[handler_idx]).saturating_add(4),
            "split NOT FOUND handler body should remain one level deeper than its header, got:\n{formatted}"
        );
        assert_eq!(
            SqlEditorWidget::format_sql_basic_for_db_type(
                &formatted,
                crate::db::connection::DatabaseType::MySQL,
            ),
            formatted
        );
    }

    #[test]
    fn format_sql_basic_for_mysql_db_type_keeps_split_sqlstate_and_comma_handler_conditions_at_header_depth(
    ) {
        let source = r#"CREATE PROCEDURE demo_proc()
BEGIN
DECLARE EXIT HANDLER FOR SQLSTATE -- application error
VALUE '45000',
SQLEXCEPTION
BEGIN
RESIGNAL;
END;
END;"#;

        let formatted = SqlEditorWidget::format_sql_basic_for_db_type(
            source,
            crate::db::connection::DatabaseType::MySQL,
        );
        let lines: Vec<&str> = formatted.lines().collect();
        let handler_idx = find_line_starting_with(
            &lines,
            "DECLARE EXIT HANDLER FOR SQLSTATE -- application error",
        )
        .expect("split SQLSTATE handler header");
        let value_idx =
            find_line_starting_with(&lines, "VALUE '45000',").expect("VALUE continuation");
        let sql_exception_idx =
            find_line_starting_with(&lines, "SQLEXCEPTION").expect("SQLEXCEPTION continuation");
        let begin_idx = lines
            .iter()
            .enumerate()
            .skip(sql_exception_idx + 1)
            .find(|(_, line)| line.trim_start() == "BEGIN")
            .map(|(idx, _)| idx)
            .expect("handler BEGIN");

        assert_eq!(
            leading_spaces(lines[value_idx]),
            leading_spaces(lines[handler_idx]),
            "split SQLSTATE VALUE line should stay aligned with DECLARE ... HANDLER FOR, got:\n{formatted}"
        );
        assert_eq!(
            leading_spaces(lines[sql_exception_idx]),
            leading_spaces(lines[handler_idx]),
            "comma-separated handler condition line should stay aligned with DECLARE ... HANDLER FOR, got:\n{formatted}"
        );
        assert_eq!(
            leading_spaces(lines[begin_idx]),
            leading_spaces(lines[handler_idx]).saturating_add(4),
            "handler body should begin one level deeper than split condition lines, got:\n{formatted}"
        );
        assert_eq!(
            SqlEditorWidget::format_sql_basic_for_db_type(
                &formatted,
                crate::db::connection::DatabaseType::MySQL,
            ),
            formatted
        );
    }

    #[test]
    fn format_sql_basic_for_mysql_db_type_clears_continuation_carry_across_handler_block_boundaries(
    ) {
        let source = r#"CREATE PROCEDURE demo_proc()
BEGIN
DECLARE CONTINUE HANDLER FOR 1062
BEGIN
GET DIAGNOSTICS CONDITION 1 v_state = RETURNED_SQLSTATE,
v_errno = MYSQL_ERRNO,
v_msg = MESSAGE_TEXT;
INSERT INTO error_log (step_name, sql_state, mysql_errno, message_text)
VALUES ('EXPECTED_DUPLICATE', v_state, v_errno, LEFT(v_msg, 512));
END;
DECLARE EXIT HANDLER FOR SQLEXCEPTION
BEGIN
ROLLBACK;
END;
START TRANSACTION;
END;"#;

        let formatted = SqlEditorWidget::format_sql_basic_for_db_type(
            source,
            crate::db::connection::DatabaseType::MySQL,
        );
        let lines: Vec<&str> = formatted.lines().collect();
        let duplicate_idx = find_line_starting_with(&lines, "DECLARE CONTINUE HANDLER FOR 1062")
            .expect("duplicate handler");
        let duplicate_begin_idx = lines
            .iter()
            .enumerate()
            .skip(duplicate_idx + 1)
            .find(|(_, line)| line.trim_start() == "BEGIN")
            .map(|(idx, _)| idx)
            .expect("duplicate handler BEGIN");
        let duplicate_end_idx = lines
            .iter()
            .enumerate()
            .skip(duplicate_begin_idx + 1)
            .find(|(_, line)| line.trim_start() == "END;")
            .map(|(idx, _)| idx)
            .expect("duplicate handler END");
        let exit_idx = find_line_starting_with(&lines, "DECLARE EXIT HANDLER FOR SQLEXCEPTION")
            .expect("exit handler");
        let exit_begin_idx = lines
            .iter()
            .enumerate()
            .skip(exit_idx + 1)
            .find(|(_, line)| line.trim_start() == "BEGIN")
            .map(|(idx, _)| idx)
            .expect("exit handler BEGIN");
        let exit_end_idx = lines
            .iter()
            .enumerate()
            .skip(exit_begin_idx + 1)
            .find(|(_, line)| line.trim_start() == "END;")
            .map(|(idx, _)| idx)
            .expect("exit handler END");
        let start_tx_idx =
            find_line_starting_with(&lines, "START TRANSACTION;").expect("START TRANSACTION");

        assert_eq!(
            leading_spaces(lines[duplicate_end_idx]),
            leading_spaces(lines[duplicate_begin_idx]),
            "handler block END should realign with handler BEGIN instead of inheriting a carried continuation depth, got:\n{formatted}"
        );
        assert_eq!(
            leading_spaces(lines[exit_idx]),
            leading_spaces(lines[duplicate_idx]),
            "sibling handler header after a completed handler block should return to the DECLARE depth, got:\n{formatted}"
        );
        assert_eq!(
            leading_spaces(lines[exit_begin_idx]),
            leading_spaces(lines[exit_idx]).saturating_add(4),
            "sibling handler body should open one level deeper than the handler header, got:\n{formatted}"
        );
        assert_eq!(
            leading_spaces(lines[exit_end_idx]),
            leading_spaces(lines[exit_begin_idx]),
            "sibling handler END should realign with its handler BEGIN, got:\n{formatted}"
        );
        assert_eq!(
            leading_spaces(lines[start_tx_idx]),
            leading_spaces(lines[duplicate_idx]),
            "ordinary statement after handler declarations should not inherit handler-body continuation depth, got:\n{formatted}"
        );
        assert_eq!(
            SqlEditorWidget::format_sql_basic_for_db_type(
                &formatted,
                crate::db::connection::DatabaseType::MySQL,
            ),
            formatted
        );
    }

    #[test]
    fn format_sql_basic_for_mysql_db_type_preserves_create_table_keyword_canonicalization() {
        let source = "create table t (profile int not null, `rank` int, generated_col varchar(10) generated always as (concat(profile, `rank`))) engine = innodb;";
        let formatted = SqlEditorWidget::format_sql_basic_for_db_type(
            source,
            crate::db::connection::DatabaseType::MySQL,
        );

        let required_snippets = [
            "CREATE TABLE t (",
            "profile       INT         NOT NULL,",
            "`rank`        INT,",
            "generated_col VARCHAR(10) GENERATED ALWAYS AS (concat(profile, `rank`))",
            "ENGINE = innodb;",
        ];

        for snippet in required_snippets {
            assert!(
                formatted.contains(snippet),
                "formatted CREATE TABLE should keep canonical MySQL keyword rendering for `{snippet}`:\n{formatted}"
            );
        }

        let forbidden_snippets = [
            "create table t (",
            "profile       int         not null,",
            "generated_col varchar(10) generated always as(concat(profile, `rank`))",
            "engine = innodb;",
        ];

        for snippet in forbidden_snippets {
            assert!(
                !formatted.contains(snippet),
                "formatted CREATE TABLE should not regress to `{snippet}`:\n{formatted}"
            );
        }
    }

    #[test]
    fn format_sql_basic_for_mysql_db_type_preserves_keyword_like_identifiers_in_select_and_where() {
        let source = "select profile\nfrom t\nwhere profile = profile;";
        let formatted = SqlEditorWidget::format_sql_basic_for_db_type(
            source,
            crate::db::connection::DatabaseType::MySQL,
        );

        assert!(
            formatted.contains("SELECT profile\nFROM t\nWHERE profile = profile;"),
            "formatter should preserve lowercase keyword-like identifiers in SELECT/WHERE expression slots, got:\n{formatted}"
        );
        assert!(
            !formatted.contains("SELECT PROFILE\nFROM t\nWHERE PROFILE = PROFILE;"),
            "formatter should not uppercase keyword-like identifiers in SELECT/WHERE expression slots, got:\n{formatted}"
        );
    }

    #[test]
    fn format_sql_basic_for_mysql_db_type_preserves_keyword_like_aliases() {
        let source =
            "select profile as rank, profile window\nfrom metrics profile\nwhere profile.rank > 0;";
        let formatted = SqlEditorWidget::format_sql_basic_for_db_type(
            source,
            crate::db::connection::DatabaseType::MySQL,
        );

        let required_snippets = [
            "SELECT profile AS rank,",
            "    profile window",
            "FROM metrics profile",
            "WHERE profile.rank > 0;",
        ];

        for snippet in required_snippets {
            assert!(
                formatted.contains(snippet),
                "formatter should preserve lowercase keyword-like aliases for `{snippet}`, got:\n{formatted}"
            );
        }

        let forbidden_snippets = [
            "SELECT profile AS RANK,",
            "    profile WINDOW",
            "FROM metrics PROFILE",
            "WHERE PROFILE.rank > 0;",
        ];

        for snippet in forbidden_snippets {
            assert!(
                !formatted.contains(snippet),
                "formatter should not uppercase keyword-like aliases for `{snippet}`, got:\n{formatted}"
            );
        }
    }

    #[test]
    fn format_sql_basic_for_mysql_db_type_keeps_top_level_values_clause_structural() {
        let source = "insert into t (profile) values (1);";
        let formatted = SqlEditorWidget::format_sql_basic_for_db_type(
            source,
            crate::db::connection::DatabaseType::MySQL,
        );

        assert!(
            formatted.contains("INSERT INTO t (profile)\nVALUES (1);"),
            "formatter should keep top-level VALUES as a structural clause, got:\n{formatted}"
        );
        assert!(
            !formatted.contains("INSERT INTO t (profile)\nvalues (1);"),
            "formatter should canonicalize top-level VALUES clause, got:\n{formatted}"
        );
        assert!(
            !formatted.contains("VALUES(1);"),
            "formatter should not collapse top-level VALUES clause into function-like syntax, got:\n{formatted}"
        );
    }

    #[test]
    fn mysql_keyword_identifier_helper_detects_operand_and_alias_slots() {
        let tokens = SqlEditorWidget::tokenize_sql_with_mysql_compat(
            "select profile as rank, profile window from metrics profile where profile = profile",
            true,
        );

        let rank_idx = tokens
            .iter()
            .position(|token| matches!(token, SqlToken::Word(word) if word == "rank"))
            .expect("rank token");
        let second_profile_idx = tokens
            .iter()
            .enumerate()
            .filter_map(|(idx, token)| match token {
                SqlToken::Word(word) if word == "profile" => Some(idx),
                _ => None,
            })
            .nth(1)
            .expect("second profile token");
        let window_idx = tokens
            .iter()
            .position(|token| matches!(token, SqlToken::Word(word) if word == "window"))
            .expect("window token");

        assert!(SqlEditorWidget::mysql_keyword_should_render_as_identifier(
            &tokens,
            rank_idx,
            Some("SELECT"),
            false,
            None,
        ));
        assert!(SqlEditorWidget::mysql_keyword_should_render_as_identifier(
            &tokens,
            second_profile_idx,
            Some("SELECT"),
            false,
            None,
        ));
        assert!(SqlEditorWidget::mysql_keyword_should_render_as_identifier(
            &tokens,
            window_idx,
            Some("SELECT"),
            false,
            None,
        ));
    }

    #[test]
    fn mysql_keyword_identifier_helper_keeps_top_level_values_clause_structural() {
        let tokens = SqlEditorWidget::tokenize_sql_with_mysql_compat(
            "insert into t (profile) values (1)",
            true,
        );
        let values_idx = tokens
            .iter()
            .position(|token| matches!(token, SqlToken::Word(word) if word == "values"))
            .expect("values token");

        assert!(!SqlEditorWidget::mysql_keyword_should_render_as_identifier(
            &tokens,
            values_idx,
            Some("INTO"),
            false,
            None,
        ));
        assert!(SqlEditorWidget::mysql_keyword_should_render_as_identifier(
            &tokens,
            values_idx,
            Some("SET"),
            true,
            None,
        ));
    }

    #[test]
    fn format_sql_basic_for_mysql_db_type_keeps_custom_delimited_procedure_end_on_owner_depth() {
        let source = r#"DELIMITER $$
CREATE PROCEDURE demo_proc()
BEGIN
    DECLARE v_id INT DEFAULT 1;
    DECLARE EXIT HANDLER FOR SQLEXCEPTION
        BEGIN
            ROLLBACK;
            RESIGNAL;
        END;
    WHILE v_id <= 2 DO
        IF v_id = 1 THEN
            SET v_id = v_id + 1;
        ELSE
            SET v_id = v_id + 1;
        END IF;
    END WHILE;
END$$
DELIMITER ;"#;

        let formatted = SqlEditorWidget::format_sql_basic_for_db_type(
            source,
            crate::db::connection::DatabaseType::MySQL,
        );
        let lines: Vec<&str> = formatted.lines().collect();
        let indent = |line: &str| line.len().saturating_sub(line.trim_start().len());
        let header_idx = lines
            .iter()
            .position(|line| line.trim_start() == "CREATE PROCEDURE demo_proc()")
            .unwrap_or(0);
        let begin_idx = lines
            .iter()
            .enumerate()
            .skip(header_idx.saturating_add(1))
            .find(|(_, line)| line.trim_start() == "BEGIN")
            .map(|(idx, _)| idx)
            .unwrap_or(0);
        let end_idx = lines
            .iter()
            .enumerate()
            .skip(begin_idx.saturating_add(1))
            .find(|(_, line)| line.trim_start() == "END$$")
            .map(|(idx, _)| idx)
            .unwrap_or(0);

        assert_eq!(
            indent(lines[begin_idx]),
            0,
            "procedure BEGIN should stay on the owner depth, got:\n{formatted}"
        );
        assert_eq!(
            indent(lines[end_idx]),
            indent(lines[begin_idx]),
            "custom-delimited procedure END should realign with BEGIN, got:\n{formatted}"
        );
        assert_eq!(
            indent(lines[end_idx]),
            indent(lines[header_idx]),
            "custom-delimited procedure END should return to depth 0, got:\n{formatted}"
        );
        assert_eq!(
            SqlEditorWidget::format_sql_basic_for_db_type(
                &formatted,
                crate::db::connection::DatabaseType::MySQL,
            ),
            formatted
        );
    }

    #[test]
    fn format_sql_basic_for_mysql_db_type_keeps_nested_seed_procedure_end_at_depth_zero() {
        let source = r#"CREATE PROCEDURE sp_seed_final_boss()
BEGIN
    DECLARE v_customer_id INT DEFAULT 1;
    DECLARE v_product_id INT DEFAULT 1;
    DECLARE v_order_id INT DEFAULT 1;
    DECLARE v_item_no INT DEFAULT 1;
    DECLARE v_item_cnt INT DEFAULT 1;
    DECLARE v_prod_id INT DEFAULT 1;
    DECLARE v_order_total DECIMAL(18, 2) DEFAULT 0.00;
    DECLARE v_order_date DATE;
    DECLARE v_status VARCHAR(20);
    DECLARE EXIT HANDLER FOR SQLEXCEPTION
        BEGIN
            ROLLBACK;
            RESIGNAL;
        END;
    START TRANSACTION;
    INSERT INTO boss_region (region_id, parent_region_id, region_name, region_code)
    VALUES (1, NULL, 'GLOBAL', 'GLOBAL'),
        (2, 1, 'APAC', 'APAC'),
        (3, 1, 'EMEA', 'EMEA'),
        (4, 1, 'AMER', 'AMER'),
        (5, 2, 'KOREA', 'KR'),
        (6, 2, 'JAPAN', 'JP'),
        (7, 2, 'SINGAPORE', 'SG'),
        (8, 3, 'GERMANY', 'DE'),
        (9, 3, 'UK', 'UK'),
        (10, 3, 'FRANCE', 'FR'),
        (11, 4, 'USA_EAST', 'USE'),
        (12, 4, 'USA_WEST', 'USW'),
        (13, 4, 'CANADA', 'CA');
    WHILE v_customer_id <= 36 DO
        INSERT INTO boss_customer (customer_id, region_id, customer_name, segment, email, profile_json, created_at)
        VALUES (v_customer_id, 5 + MOD(v_customer_id - 1, 9), CONCAT('CUST_', LPAD(v_customer_id, 3, '0')), ELT(1 + MOD(v_customer_id - 1, 3), 'ENTERPRISE', 'SMB', 'PUBLIC'), CONCAT('cust', LPAD(v_customer_id, 3, '0'), '@boss.test'), JSON_OBJECT('tier', ELT(1 + MOD(v_customer_id - 1, 4), 'BRONZE', 'SILVER', 'GOLD', 'PLATINUM'), 'flags', JSON_ARRAY(CONCAT('f', MOD(v_customer_id, 5)), 'split;test', '//slash', '한글'), 'credits', v_customer_id * 111, 'memo', CONCAT('memo ; ', v_customer_id, ' -- not comment')), DATE_ADD('2024-01-01 08:00:00', INTERVAL v_customer_id DAY));
        SET v_customer_id = v_customer_id + 1;
    END WHILE;
    WHILE v_product_id <= 36 DO
        INSERT INTO boss_product (product_id, sku, category, product_name, unit_price, attrs_json, active_yn)
        VALUES (v_product_id, CONCAT('SKU-', LPAD(v_product_id, 4, '0')), ELT(1 + MOD(v_product_id - 1, 3), 'HW', 'SW', 'SV'), CONCAT(ELT(1 + MOD(v_product_id - 1, 3), 'HW', 'SW', 'SV'), '_PRODUCT_', LPAD(v_product_id, 2, '0')), ROUND(19 + (v_product_id * 7.35) + (MOD(v_product_id, 5) * 3.70), 2), JSON_OBJECT('color', ELT(1 + MOD(v_product_id - 1, 4), 'red', 'blue', 'black', 'silver'), 'weight', ROUND(0.5 + (v_product_id * 0.13), 2), 'tags', JSON_ARRAY(CONCAT('p', v_product_id), 'boss', 'json;test', CONCAT('cat:', ELT(1 + MOD(v_product_id - 1, 3), 'HW', 'SW', 'SV')))), 
        IF (MOD(v_product_id, 17) = 0, 'N', 'Y'));
            SET v_product_id = v_product_id + 1;
        END WHILE;
        WHILE v_order_id <= 240 DO
            SET v_status =
                CASE
                    WHEN MOD(v_order_id, 17) = 0 THEN
                        'CANCELLED'
                    WHEN MOD(v_order_id, 11) = 0 THEN
                        'HOLD'
                    WHEN MOD(v_order_id, 3) = 0 THEN
                        'OPEN'
                    ELSE
                        'COMPLETE'
                END;
            INSERT INTO boss_order (order_id, customer_id, order_no, order_date, status, meta_json)
            VALUES (v_order_id, 1 + MOD(v_order_id * 7, 36), CONCAT('ORD', DATE_FORMAT(DATE_ADD('2024-01-01', INTERVAL MOD(v_order_id * 5, 720) DAY), '%Y%m%d'), '-', LPAD(v_order_id, 5, '0')), DATE_ADD('2024-01-01', INTERVAL MOD(v_order_id * 5, 720) DAY), v_status, JSON_OBJECT('channel', ELT(1 + MOD(v_order_id, 4), 'WEB', 'PARTNER', 'DIRECT', 'MOBILE'), 'priority', MOD(v_order_id, 5), 'memo', CONCAT('semi; // ', v_order_id, ' /* literal */'), 'flag', 
            IF (MOD(v_order_id, 2) = 0, 'Y', 'N')));
                SET v_item_cnt = 1 + MOD(v_order_id, 5);
                SET v_item_no = 1;
                WHILE v_item_no <= v_item_cnt DO
                    SET v_prod_id = 1 + MOD(v_order_id * 7 + v_item_no * 11, 36);
                    INSERT INTO boss_order_item (order_id, line_no, product_id, quantity, unit_price, discount_rate, tax_rate, note, details_json)
                    VALUES (v_order_id, v_item_no, v_prod_id, 1 + MOD(v_order_id + v_item_no, 4), 
                            CASE
                                WHEN MOD(v_item_no, 2) = 0 THEN
                                    NULL
                                ELSE
                                    ROUND(19 + (v_prod_id * 7.35) + (MOD(v_prod_id, 5) * 3.70), 2)
                            END, 
                            CASE
                                WHEN MOD(v_item_no, 5) = 0 THEN
                                    0.2000
                                WHEN MOD(v_item_no, 3) = 0 THEN
                                    0.0700
                                ELSE
                                    ROUND((MOD(v_item_no + v_order_id, 4) * 0.0200), 4)
                            END, 
                            CASE
                                WHEN MOD(v_prod_id, 7) = 0 THEN
                                    0.0500
                                ELSE
                                    0.1000
                            END, CONCAT('note ; ', v_order_id, '/', v_item_no, ' // not delimiter'), JSON_OBJECT('gift', 
                    IF (MOD(v_order_id + v_item_no, 6) = 0, 'Y', 'N'), 'tags', JSON_ARRAY(CONCAT('o', v_order_id), CONCAT('i', v_item_no), 'semi;colon', 'slash//slash'), 'payload', CONCAT('payload-', v_order_id, '-', v_item_no)));
                        SET v_item_no = v_item_no + 1;
                    END WHILE;
                    SELECT order_date,
                        grand_total
                    INTO v_order_date,
                        v_order_total
                    FROM boss_order
                    WHERE order_id = v_order_id;
                    IF v_status <> 'CANCELLED' THEN
                        IF MOD(v_order_id, 4) = 0 THEN
                            INSERT INTO boss_payment (order_id, payment_date, amount, method, payload_json)
                            VALUES (v_order_id, DATE_ADD(CAST(CONCAT(v_order_date, ' 09:00:00') AS DATETIME), INTERVAL MOD(v_order_id, 48) HOUR), v_order_total, 'CARD', JSON_OBJECT('gateway', 'G1', 'memo', 'full payment ; // ok'));
                            ELSEIF MOD(v_order_id, 4) = 1 THEN
                            INSERT INTO boss_payment (order_id, payment_date, amount, method, payload_json)
                            VALUES (v_order_id, DATE_ADD(CAST(CONCAT(v_order_date, ' 10:00:00') AS DATETIME), INTERVAL MOD(v_order_id, 48) HOUR), ROUND(v_order_total * 0.35, 2), 'BANK', JSON_OBJECT('gateway', 'G2', 'memo', 'partial-1'));
                            INSERT INTO boss_payment (order_id, payment_date, amount, method, payload_json)
                            VALUES (v_order_id, DATE_ADD(CAST(CONCAT(v_order_date, ' 18:00:00') AS DATETIME), INTERVAL MOD(v_order_id, 72) HOUR), ROUND(v_order_total - ROUND(v_order_total * 0.35, 2), 2), 'BANK', JSON_OBJECT('gateway', 'G2', 'memo', 'partial-2'));
                            ELSEIF MOD(v_order_id, 4) = 2 THEN
                            INSERT INTO boss_payment (order_id, payment_date, amount, method, payload_json)
                            VALUES (v_order_id, DATE_ADD(CAST(CONCAT(v_order_date, ' 14:00:00') AS DATETIME), INTERVAL MOD(v_order_id, 36) HOUR), ROUND(v_order_total * 0.60, 2), 'CASH', JSON_OBJECT('gateway', 'G3', 'memo', '60% only'));
                        END IF;
                    END IF;
                    SET v_order_id = v_order_id + 1;
                END WHILE;
                UPDATE boss_customer c
                JOIN (
                    SELECT o.customer_id,
                        COUNT(*) AS lifetime_orders,
                        ROUND(SUM(o.grand_total), 2) AS lifetime_net_sales,
                        MAX(o.order_date) AS last_order_date
                    FROM boss_order o
                    WHERE o.status <> 'CANCELLED'
                    GROUP BY o.customer_id
                ) s
                    ON s.customer_id = c.customer_id
                SET c.profile_json = JSON_SET(c.profile_json, '$.lifetime_orders', s.lifetime_orders, '$.lifetime_net_sales', s.lifetime_net_sales, '$.last_order_date', DATE_FORMAT(s.last_order_date, '%Y-%m-%d'));
                INSERT INTO boss_audit (event_time, entity_type, entity_id, action_name, payload_json)
                SELECT NOW(),
                    'SYSTEM',
                    '0',
                    'SEED_COMPLETE',
                    JSON_OBJECT('regions', (
                    SELECT COUNT(*)
                    FROM boss_region
                    ), 'customers', (
                    SELECT COUNT(*)
                    FROM boss_customer
                    ), 'products', (
                    SELECT COUNT(*)
                    FROM boss_product
                    ), 'orders', (
                    SELECT COUNT(*)
                    FROM boss_order
                    ), 'items', (
                    SELECT COUNT(*)
                    FROM boss_order_item
                    ), 'payments', (
                    SELECT COUNT(*)
                    FROM boss_payment
                    ));
                COMMIT;
            END$$"#;

        let formatted = SqlEditorWidget::format_sql_basic_for_db_type(
            source,
            crate::db::connection::DatabaseType::MySQL,
        );
        let lines: Vec<&str> = formatted.lines().collect();
        let indent = |line: &str| line.len().saturating_sub(line.trim_start().len());
        let begin_idx = lines
            .iter()
            .position(|line| line.trim_start() == "BEGIN")
            .unwrap_or(0);
        let end_idx = lines
            .iter()
            .enumerate()
            .skip(begin_idx.saturating_add(1))
            .find(|(_, line)| line.trim_start() == "END$$")
            .map(|(idx, _)| idx)
            .unwrap_or(0);
        let start_tx_idx = lines
            .iter()
            .position(|line| line.trim_start() == "START TRANSACTION;")
            .unwrap_or(0);
        let commit_idx = lines
            .iter()
            .position(|line| line.trim_start() == "COMMIT;")
            .unwrap_or(0);

        assert_eq!(
            indent(lines[end_idx]),
            indent(lines[begin_idx]),
            "nested MariaDB seed procedure should close on the owner depth, got:\n{formatted}"
        );
        assert_eq!(
            indent(lines[commit_idx]),
            indent(lines[start_tx_idx]),
            "nested MariaDB seed procedure COMMIT should return to the same procedure-body frame depth as START TRANSACTION, got:\n{formatted}"
        );

        let auto_formatted = SqlEditorWidget::format_for_auto_formatting_with_db_type(
            source,
            false,
            Some(crate::db::connection::DatabaseType::MySQL),
        );
        let auto_lines: Vec<&str> = auto_formatted.lines().collect();
        let auto_begin_idx = auto_lines
            .iter()
            .position(|line| line.trim_start() == "BEGIN")
            .unwrap_or(0);
        let auto_end_idx = auto_lines
            .iter()
            .enumerate()
            .skip(auto_begin_idx.saturating_add(1))
            .find(|(_, line)| line.trim_start() == "END$$")
            .map(|(idx, _)| idx)
            .unwrap_or(0);
        let auto_start_tx_idx = auto_lines
            .iter()
            .position(|line| line.trim_start() == "START TRANSACTION;")
            .unwrap_or(0);
        let auto_commit_idx = auto_lines
            .iter()
            .position(|line| line.trim_start() == "COMMIT;")
            .unwrap_or(0);

        assert_eq!(
            indent(auto_lines[auto_end_idx]),
            indent(auto_lines[auto_begin_idx]),
            "auto-format nested MariaDB seed procedure should close on the owner depth, got:\n{auto_formatted}"
        );
        assert_eq!(
            indent(auto_lines[auto_commit_idx]),
            indent(auto_lines[auto_start_tx_idx]),
            "auto-format nested MariaDB seed procedure COMMIT should return to the procedure-body frame depth, got:\n{auto_formatted}"
        );
    }

    #[test]
    fn mysql_formatter_keeps_commit_on_procedure_body_depth_after_nested_insert_select() {
        let source = r#"CREATE PROCEDURE demo_proc()
BEGIN
    INSERT INTO boss_audit (event_time, payload_json)
    SELECT NOW(),
        JSON_OBJECT('regions', (
            SELECT COUNT(*)
            FROM boss_region
            ), 'customers', (
                SELECT COUNT(*)
                FROM boss_customer
            ));
                COMMIT;
END$$"#;

        for formatted in [
            SqlEditorWidget::format_sql_basic_for_db_type(
                source,
                crate::db::connection::DatabaseType::MySQL,
            ),
            SqlEditorWidget::format_for_auto_formatting_with_db_type(
                source,
                false,
                Some(crate::db::connection::DatabaseType::MySQL),
            ),
        ] {
            let lines: Vec<&str> = formatted.lines().collect();
            let indent = |line: &str| line.len().saturating_sub(line.trim_start().len());
            let begin_idx = lines
                .iter()
                .position(|line| line.trim_start() == "BEGIN")
                .unwrap_or(0);
            let insert_idx = lines
                .iter()
                .position(|line| {
                    line.trim_start() == "INSERT INTO boss_audit (event_time, payload_json)"
                })
                .unwrap_or(0);
            let commit_idx = lines
                .iter()
                .position(|line| line.trim_start() == "COMMIT;")
                .unwrap_or(0);

            assert_eq!(
                indent(lines[insert_idx]),
                indent(lines[begin_idx]).saturating_add(4),
                "INSERT head should stay one frame deeper than BEGIN, got:\n{formatted}"
            );
            assert_eq!(
                indent(lines[commit_idx]),
                indent(lines[insert_idx]),
                "COMMIT should return to the same procedure-body frame depth as INSERT after nested SELECT closes, got:\n{formatted}"
            );
        }
    }

    #[test]
    fn format_sql_basic_for_mysql_db_type_resets_depth_between_custom_delimited_routines() {
        let source = r#"DELIMITER $$
CREATE PROCEDURE first_proc()
BEGIN
    SET @a = 1;
END$$

CREATE PROCEDURE second_proc()
BEGIN
    SET @b = 2;
END$$
DELIMITER ;"#;

        let formatted = SqlEditorWidget::format_sql_basic_for_db_type(
            source,
            crate::db::connection::DatabaseType::MySQL,
        );
        let lines: Vec<&str> = formatted.lines().collect();
        let indent = |line: &str| line.len().saturating_sub(line.trim_start().len());
        let first_end_idx = lines
            .iter()
            .position(|line| line.trim_start() == "END$$")
            .unwrap_or(0);
        let second_header_idx = lines
            .iter()
            .position(|line| line.trim_start() == "CREATE PROCEDURE second_proc()")
            .unwrap_or(0);
        let second_begin_idx = lines
            .iter()
            .enumerate()
            .skip(second_header_idx.saturating_add(1))
            .find(|(_, line)| line.trim_start() == "BEGIN")
            .map(|(idx, _)| idx)
            .unwrap_or(0);
        let second_end_idx = lines
            .iter()
            .enumerate()
            .skip(second_begin_idx.saturating_add(1))
            .find(|(_, line)| line.trim_start() == "END$$")
            .map(|(idx, _)| idx)
            .unwrap_or(0);

        assert_eq!(
            indent(lines[first_end_idx]),
            0,
            "first routine should close back to depth 0, got:\n{formatted}"
        );
        assert_eq!(
            indent(lines[second_header_idx]),
            0,
            "second routine header should not inherit leaked depth, got:\n{formatted}"
        );
        assert_eq!(
            indent(lines[second_begin_idx]),
            0,
            "second routine BEGIN should stay on owner depth, got:\n{formatted}"
        );
        assert_eq!(
            indent(lines[second_end_idx]),
            0,
            "second routine END should also return to depth 0, got:\n{formatted}"
        );
        assert_eq!(
            SqlEditorWidget::format_sql_basic_for_db_type(
                &formatted,
                crate::db::connection::DatabaseType::MySQL,
            ),
            formatted
        );
    }

    #[test]
    fn format_sql_basic_for_mysql_db_type_keeps_while_do_owner_depth_with_if_function_body() {
        let source = r#"DELIMITER $$
CREATE PROCEDURE demo_proc()
BEGIN
    WHILE v_product_id <= 36 DO
        INSERT INTO boss_product (active_yn)
        VALUES (
            IF(MOD(v_product_id, 17) = 0, 'N', 'Y')
        );
        SET v_product_id = v_product_id + 1;
    END WHILE;
END$$
DELIMITER ;"#;

        let formatted = SqlEditorWidget::format_sql_basic_for_db_type(
            source,
            crate::db::connection::DatabaseType::MySQL,
        );
        let lines: Vec<&str> = formatted.lines().collect();
        let indent = |line: &str| line.len().saturating_sub(line.trim_start().len());
        let while_idx = lines
            .iter()
            .position(|line| line.trim_start() == "WHILE v_product_id <= 36 DO")
            .unwrap_or(0);
        let values_idx = lines
            .iter()
            .position(|line| {
                line.trim_start() == "VALUES (IF(MOD(v_product_id, 17) = 0, 'N', 'Y'));"
            })
            .unwrap_or(0);
        let set_idx = lines
            .iter()
            .position(|line| line.trim_start() == "SET v_product_id = v_product_id + 1;")
            .unwrap_or(0);
        let end_while_idx = lines
            .iter()
            .position(|line| line.trim_start() == "END WHILE;")
            .unwrap_or(0);
        let end_idx = lines
            .iter()
            .position(|line| line.trim_start() == "END$$")
            .unwrap_or(0);

        assert_eq!(
            indent(lines[values_idx]),
            indent(lines[while_idx]).saturating_add(4),
            "VALUES(IF(...)) line should stay on the WHILE body depth, got:\n{formatted}"
        );
        assert_eq!(
            indent(lines[set_idx]),
            indent(lines[while_idx]).saturating_add(4),
            "statement after IF() scalar function should stay on the WHILE body depth, got:\n{formatted}"
        );
        assert_eq!(
            indent(lines[end_while_idx]),
            indent(lines[while_idx]),
            "END WHILE should align with the WHILE header, got:\n{formatted}"
        );
        assert_eq!(
            indent(lines[end_idx]),
            0,
            "routine END should return to depth 0 after WHILE ... DO closes, got:\n{formatted}"
        );
    }

    #[test]
    fn format_sql_basic_for_mysql_db_type_reapplies_structural_depth_after_compound_select_sibling_realignment(
    ) {
        let source = r#"DELIMITER $$
CREATE PROCEDURE demo_proc()
BEGIN
SELECT
        ROW_NUMBER() OVER (
                    PARTITION BY deptno
                                ORDER BY sal
                    ) AS row_no,
                                                    DENSE_RANK() OVER (
                                                                        PARTITION BY deptno
                                                            ORDER BY sal
                                                    ) AS sal_rank
FROM emp;
END$$
DELIMITER ;"#;

        let formatted = SqlEditorWidget::format_sql_basic_for_db_type(
            source,
            crate::db::connection::DatabaseType::MySQL,
        );
        let lines: Vec<&str> = formatted.lines().collect();
        let indent = |line: &str| line.len().saturating_sub(line.trim_start().len());
        let select_idx = lines
            .iter()
            .position(|line| line.trim_start().starts_with("SELECT ROW_NUMBER() OVER ("))
            .unwrap_or(0);
        let dense_rank_idx = lines
            .iter()
            .enumerate()
            .skip(select_idx + 1)
            .find(|(_, line)| line.trim_start() == "DENSE_RANK() OVER (")
            .map(|(idx, _)| idx)
            .unwrap_or(0);
        let from_idx = lines
            .iter()
            .position(|line| line.trim_start() == "FROM emp;")
            .unwrap_or(0);

        assert_eq!(
            indent(lines[dense_rank_idx]),
            indent(lines[select_idx]).saturating_add(4),
            "compound analytic sibling should return to the SELECT owner frame body depth after the previous OVER frame closes, got:\n{formatted}"
        );
        assert!(
            indent(lines[from_idx]) < indent(lines[dense_rank_idx]),
            "FROM should clear the carried compound SELECT list depth, got:\n{formatted}"
        );
        assert_eq!(
            SqlEditorWidget::format_sql_basic_for_db_type(
                &formatted,
                crate::db::connection::DatabaseType::MySQL,
            ),
            formatted,
            "compound SELECT sibling normalization should stay idempotent once the structural frame depth is fixed"
        );
    }

    #[test]
    fn format_sql_basic_for_mysql_db_type_keeps_multiline_values_blocks_in_mariadb_regressions() {
        let cases = [
            (
                include_str!("../../../test_mysql/test1.txt"),
                "INSERT INTO order_audit (order_id, action_type, action_note)",
            ),
            (
                include_str!("../../../test_mysql/test2.txt"),
                "INSERT INTO task_log (task_id, log_type, old_status, new_status, log_note)",
            ),
            (
                include_str!("../../../test_mysql/test3.txt"),
                "INSERT INTO run_audit (run_id, audit_type, old_status, new_status, audit_note)",
            ),
            (
                include_str!("../../../test_mariadb/test6.txt"),
                "INSERT INTO boss_customer (customer_id, region_id, customer_name, segment, email, profile_json, created_at)",
            ),
        ];

        for (source, snippet) in cases {
            let formatted = SqlEditorWidget::format_sql_basic_for_db_type(
                source,
                crate::db::connection::DatabaseType::MySQL,
            );

            assert!(
                formatted.contains(snippet),
                "formatted MySQL/MariaDB INSERT signature should remain present, got:\n{formatted}"
            );
            assert!(
                formatted.contains("VALUES ("),
                "formatted MySQL/MariaDB INSERT should keep VALUES clause text, got:\n{formatted}"
            );
            assert_eq!(
                SqlEditorWidget::format_sql_basic_for_db_type(
                    &formatted,
                    crate::db::connection::DatabaseType::MySQL,
                ),
                formatted
            );
        }
    }

    #[test]
    fn format_sql_basic_for_mysql_db_type_keeps_test6_elseifs_on_if_owner_depth() {
        let source = include_str!("../../../test_mariadb/test6.txt");
        let formatted = SqlEditorWidget::format_sql_basic_for_db_type(
            source,
            crate::db::connection::DatabaseType::MySQL,
        );
        let lines: Vec<&str> = formatted.lines().collect();

        let if_idx = find_line_starting_with(&lines, "IF MOD(v_order_id, 4) = 0 THEN")
            .expect("test6 nested payment IF");
        let elseif_one_idx = find_line_starting_with(&lines, "ELSEIF MOD(v_order_id, 4) = 1 THEN")
            .expect("test6 first ELSEIF");
        let elseif_two_idx = find_line_starting_with(&lines, "ELSEIF MOD(v_order_id, 4) = 2 THEN")
            .expect("test6 second ELSEIF");
        let payment_insert_idx = find_line_starting_with(
            &lines,
            "INSERT INTO boss_payment (order_id, payment_date, amount, method, payload_json)",
        )
        .expect("test6 payment insert");

        assert_eq!(
            leading_spaces(lines[elseif_one_idx]),
            leading_spaces(lines[if_idx]),
            "test6 ELSEIF after multiline VALUES should realign with the IF owner depth, got:\n{formatted}"
        );
        assert_eq!(
            leading_spaces(lines[elseif_two_idx]),
            leading_spaces(lines[if_idx]),
            "test6 second ELSEIF after multiline VALUES should realign with the IF owner depth, got:\n{formatted}"
        );
        assert_eq!(
            leading_spaces(lines[payment_insert_idx]),
            leading_spaces(lines[if_idx]).saturating_add(4),
            "test6 INSERT in IF branch should stay one level deeper than IF/ELSEIF headers, got:\n{formatted}"
        );
    }

    #[test]
    fn format_sql_basic_for_mysql_db_type_keeps_test6_complex_close_and_sibling_alignment() {
        let source = include_str!("../../../test_mariadb/test6.txt");
        let formatted = SqlEditorWidget::format_sql_basic_for_db_type(
            source,
            crate::db::connection::DatabaseType::MySQL,
        );
        assert!(
            formatted.contains("WITH RECURSIVE region_tree AS ("),
            "test6 query #1 CTE header should remain present, got:\n{formatted}"
        );
        assert!(
            formatted.contains("DENSE_RANK() OVER ("),
            "test6 analytic OVER header should remain present, got:\n{formatted}"
        );
        assert!(
            formatted.contains("'stats', JSON_OBJECT("),
            "test6 JSON stats pair should remain present, got:\n{formatted}"
        );
        assert!(
            formatted.contains("'latest_orders', JSON_ARRAYAGG("),
            "test6 JSON latest_orders pair should remain present, got:\n{formatted}"
        );

        assert_eq!(
            SqlEditorWidget::format_sql_basic_for_db_type(
                &formatted,
                crate::db::connection::DatabaseType::MySQL,
            ),
            formatted
        );
    }

    #[test]
    fn format_sql_basic_for_mysql_db_type_keeps_test6_named_key_spacing_and_duplicate_key_clause() {
        let source = include_str!("../../../test_mariadb/test6.txt");
        let formatted = SqlEditorWidget::format_sql_basic_for_db_type(
            source,
            crate::db::connection::DatabaseType::MySQL,
        );
        for snippet in [
            "ix_boss_customer_region_segment",
            "ix_boss_product_category_active",
            "ix_boss_order_customer_date",
            "ix_boss_payment_order_date",
            "ON DUPLICATE KEY UPDATE",
        ] {
            assert!(
                formatted.contains(snippet),
                "test6 named KEY/INDEX and duplicate-key clause text should remain present, missing `{snippet}` in:\n{formatted}"
            );
        }

        assert_eq!(
            SqlEditorWidget::format_sql_basic_for_db_type(
                &formatted,
                crate::db::connection::DatabaseType::MySQL,
            ),
            formatted
        );
    }

    #[test]
    fn format_sql_basic_for_mysql_db_type_keeps_test4_window_function_siblings_aligned() {
        let source = include_str!("../../../test_mariadb/test4.txt");
        let formatted = SqlEditorWidget::format_sql_basic_for_db_type(
            source,
            crate::db::connection::DatabaseType::MySQL,
        );
        let lines: Vec<&str> = formatted.lines().collect();

        let row_number_idx =
            find_line_starting_with(&lines, "ROW_NUMBER() OVER (").expect("test4 ROW_NUMBER");
        let dense_rank_idx = lines
            .iter()
            .enumerate()
            .skip(row_number_idx + 1)
            .find(|(_, line)| line.trim_start() == "DENSE_RANK() OVER (")
            .map(|(idx, _)| idx)
            .expect("test4 DENSE_RANK");

        assert_eq!(
            leading_spaces(lines[dense_rank_idx]),
            leading_spaces(lines[row_number_idx]),
            "test4 analytic function siblings should return to the SELECT-item owner depth after the previous OVER clause closes, got:\n{formatted}"
        );
        assert_eq!(
            SqlEditorWidget::format_sql_basic_for_db_type(
                &formatted,
                crate::db::connection::DatabaseType::MySQL,
            ),
            formatted
        );
    }

    #[test]
    fn format_sql_basic_for_mysql_db_type_keeps_test4_multiline_call_arguments_on_call_depth() {
        let source = include_str!("../../../test_mariadb/test4.txt");
        let formatted = SqlEditorWidget::format_sql_basic_for_db_type(
            source,
            crate::db::connection::DatabaseType::MySQL,
        );
        let lines: Vec<&str> = formatted.lines().collect();

        let message_idx = lines
            .iter()
            .enumerate()
            .find(|(_, line)| line.trim_start() == "'rollup total_hours sum mismatch'")
            .map(|(idx, _)| idx)
            .expect("test4 rollup total_hours message");
        let call_idx = (0..message_idx)
            .rev()
            .find(|idx| lines[*idx].trim_start() == "CALL sp_assert_eq_bigint(")
            .expect("test4 CALL sp_assert_eq_bigint");
        let cast_indices: Vec<usize> = lines
            .iter()
            .enumerate()
            .skip(call_idx + 1)
            .take(message_idx.saturating_sub(call_idx + 1))
            .filter(|(_, line)| line.trim_start() == "CAST(ROUND(")
            .map(|(idx, _)| idx)
            .collect();

        assert_eq!(
            cast_indices.len(),
            2,
            "test4 should keep exactly two CAST arguments, got:\n{formatted}"
        );

        let desired_indent = leading_spaces(lines[call_idx]).saturating_add(4);
        assert_eq!(
            leading_spaces(lines[cast_indices[0]]),
            desired_indent,
            "test4 first multiline CAST argument should stay on the CALL body depth, got:\n{formatted}"
        );
        assert_eq!(
            leading_spaces(lines[cast_indices[1]]),
            desired_indent,
            "test4 second multiline CAST argument should realign with the CALL body depth after the previous argument closes, got:\n{formatted}"
        );
        assert_eq!(
            leading_spaces(lines[message_idx]),
            desired_indent,
            "test4 string literal argument should stay on the CALL body depth after multiline CAST siblings, got:\n{formatted}"
        );
    }

    #[test]
    fn format_sql_basic_for_mysql_db_type_keeps_test4_multiline_subquery_close_lines_on_argument_depth(
    ) {
        let source = include_str!("../../../test_mariadb/test4.txt");
        let formatted = SqlEditorWidget::format_sql_basic_for_db_type(
            source,
            crate::db::connection::DatabaseType::MySQL,
        );
        let lines: Vec<&str> = formatted.lines().collect();

        let message_idx = lines
            .iter()
            .enumerate()
            .find(|(_, line)| line.trim_start() == "'JSON tag expansion count mismatch'")
            .map(|(idx, _)| idx)
            .expect("test4 JSON tag expansion message");
        let call_idx = (0..message_idx)
            .rev()
            .find(|idx| lines[*idx].trim_start() == "CALL sp_assert_eq_bigint(")
            .expect("test4 JSON tag expansion CALL");
        let arg_open_indices: Vec<usize> = lines
            .iter()
            .enumerate()
            .skip(call_idx + 1)
            .take(message_idx.saturating_sub(call_idx + 1))
            .filter(|(_, line)| line.trim_start() == "(")
            .map(|(idx, _)| idx)
            .collect();
        let second_arg_open_idx = *arg_open_indices
            .get(1)
            .expect("test4 second multiline subquery argument");
        let json_table_join_idx = lines
            .iter()
            .enumerate()
            .skip(second_arg_open_idx + 1)
            .find(|(_, line)| line.trim_start().starts_with("JOIN JSON_TABLE(t.payload"))
            .map(|(idx, _)| idx)
            .expect("test4 JSON_TABLE join");
        let json_table_close_idx = lines
            .iter()
            .enumerate()
            .skip(json_table_join_idx + 1)
            .find(|(_, line)| line.trim_start() == ")) jt")
            .map(|(idx, _)| idx)
            .expect("test4 JSON_TABLE close");
        let arg_close_idx = lines
            .iter()
            .enumerate()
            .skip(json_table_close_idx + 1)
            .find(|(_, line)| line.trim_start() == "),")
            .map(|(idx, _)| idx)
            .expect("test4 second argument close");

        assert_eq!(
            leading_spaces(lines[json_table_close_idx]),
            leading_spaces(lines[json_table_join_idx]),
            "test4 multiline JSON_TABLE close should return to the JOIN owner depth, got:\n{formatted}"
        );
        assert_eq!(
            leading_spaces(lines[arg_close_idx]),
            leading_spaces(lines[second_arg_open_idx]),
            "test4 standalone subquery close should realign with the outer CALL argument owner depth, got:\n{formatted}"
        );
    }

    #[test]
    fn format_sql_basic_for_mysql_db_type_keeps_test4_over_body_after_select_list_sibling_realignment(
    ) {
        let source = include_str!("../../../test_mariadb/test4.txt");
        let formatted = SqlEditorWidget::format_sql_basic_for_db_type(
            source,
            crate::db::connection::DatabaseType::MySQL,
        );
        let lines: Vec<&str> = formatted.lines().collect();

        let dense_rank_idx = lines
            .iter()
            .enumerate()
            .find(|(_, line)| line.trim_start() == "DENSE_RANK() OVER (")
            .map(|(idx, _)| idx)
            .expect("test4 DENSE_RANK owner");
        let partition_idx = dense_rank_idx + 1;
        let order_idx = dense_rank_idx + 2;
        let close_idx = dense_rank_idx + 3;
        let sum_idx = dense_rank_idx + 4;

        assert_eq!(
            lines[partition_idx].trim_start(),
            "PARTITION BY d.project_id",
            "test4 DENSE_RANK PARTITION BY should remain immediately under the owner, got:\n{formatted}"
        );
        assert_eq!(
            lines[order_idx].trim_start(),
            "ORDER BY d.day_hours DESC",
            "test4 DENSE_RANK ORDER BY should remain immediately under the owner, got:\n{formatted}"
        );
        assert_eq!(
            lines[close_idx].trim_start(),
            ") AS dense_rnk,",
            "test4 DENSE_RANK close line should stay attached to the analytic owner, got:\n{formatted}"
        );
        assert_eq!(
            lines[sum_idx].trim_start(),
            "SUM(d.day_hours) OVER (",
            "test4 next analytic sibling should follow the previous close immediately, got:\n{formatted}"
        );

        let owner_indent = leading_spaces(lines[dense_rank_idx]);
        assert_eq!(
            leading_spaces(lines[partition_idx]),
            owner_indent.saturating_add(4),
            "test4 DENSE_RANK PARTITION BY should stay one level deeper than the analytic owner, got:\n{formatted}"
        );
        assert_eq!(
            leading_spaces(lines[order_idx]),
            owner_indent.saturating_add(4),
            "test4 DENSE_RANK ORDER BY should stay aligned with PARTITION BY, got:\n{formatted}"
        );
        assert_eq!(
            leading_spaces(lines[close_idx]),
            owner_indent,
            "test4 DENSE_RANK close should return to the analytic owner depth, got:\n{formatted}"
        );
        assert_eq!(
            leading_spaces(lines[sum_idx]),
            owner_indent,
            "test4 analytic sibling after DENSE_RANK should return to the SELECT-item owner depth, got:\n{formatted}"
        );
    }

    #[test]
    fn format_sql_basic_for_mysql_db_type_keeps_test5_nested_json_array_case_on_call_depth() {
        let source = include_str!("../../../test_mariadb/test5.txt");
        let formatted = SqlEditorWidget::format_sql_basic_for_db_type(
            source,
            crate::db::connection::DatabaseType::MySQL,
        );
        assert!(
            formatted.contains("'components', JSON_ARRAY("),
            "test5 JSON components key/value pair should remain present, got:\n{formatted}"
        );
        assert!(
            formatted.contains("CASE"),
            "test5 nested CASE expression should remain present, got:\n{formatted}"
        );
        assert_eq!(
            SqlEditorWidget::format_sql_basic_for_db_type(
                &formatted,
                crate::db::connection::DatabaseType::MySQL,
            ),
            formatted
        );
    }

    #[test]
    fn format_sql_basic_for_mysql_db_type_keeps_test5_select_into_targets_indented_under_into_owner(
    ) {
        let source = include_str!("../../../test_mariadb/test5.txt");
        let formatted = SqlEditorWidget::format_sql_basic_for_db_type(
            source,
            crate::db::connection::DatabaseType::MySQL,
        );
        let lines: Vec<&str> = formatted.lines().collect();

        let select_idx = find_line_starting_with(&lines, "SELECT p.project_code,")
            .expect("test5 project_code select");
        let into_idx = lines
            .iter()
            .enumerate()
            .skip(select_idx + 1)
            .find(|(_, line)| line.trim_start() == "INTO v_project_code,")
            .map(|(idx, _)| idx)
            .expect("test5 INTO v_project_code");
        let into_tail_idx = lines
            .iter()
            .enumerate()
            .skip(into_idx + 1)
            .find(|(_, line)| line.trim_start() == "v_owner_user_id")
            .map(|(idx, _)| idx)
            .expect("test5 INTO v_owner_user_id");

        assert_eq!(
            leading_spaces(lines[into_tail_idx]),
            leading_spaces(lines[into_idx]).saturating_add(4),
            "test5 multiline SELECT INTO target siblings should stay one level deeper than the INTO owner line, got:\n{formatted}"
        );
    }

    #[test]
    fn format_sql_basic_for_mysql_db_type_keeps_test5_window_function_siblings_and_over_body_aligned(
    ) {
        let source = include_str!("../../../test_mariadb/test5.txt");
        let formatted = SqlEditorWidget::format_sql_basic_for_db_type(
            source,
            crate::db::connection::DatabaseType::MySQL,
        );
        let lines: Vec<&str> = formatted.lines().collect();

        let dense_rank_idx = lines
            .iter()
            .enumerate()
            .find(|(_, line)| line.trim_start() == "DENSE_RANK() OVER (")
            .map(|(idx, _)| idx)
            .expect("test5 DENSE_RANK owner");
        let partition_idx = dense_rank_idx + 1;
        let partition_tail_idx = dense_rank_idx + 2;
        let order_idx = dense_rank_idx + 3;
        let close_idx = dense_rank_idx + 4;
        let sum_idx = dense_rank_idx + 5;
        let avg_idx = dense_rank_idx + 11;

        assert_eq!(
            lines[partition_idx].trim_start(),
            "PARTITION BY c.project_id,",
            "test5 DENSE_RANK PARTITION BY should stay immediately under the owner, got:\n{formatted}"
        );
        assert_eq!(
            lines[partition_tail_idx].trim_start(),
            "DATE_FORMAT(c.event_day, '%Y-%m')",
            "test5 multipart PARTITION BY should keep its sibling continuation, got:\n{formatted}"
        );
        assert_eq!(
            lines[order_idx].trim_start(),
            "ORDER BY c.day_hours DESC",
            "test5 DENSE_RANK ORDER BY should stay immediately under the owner, got:\n{formatted}"
        );
        assert_eq!(
            lines[close_idx].trim_start(),
            ") AS dense_rnk,",
            "test5 DENSE_RANK close should stay attached to the analytic owner, got:\n{formatted}"
        );
        assert_eq!(
            lines[sum_idx].trim_start(),
            "SUM(c.day_hours) OVER (",
            "test5 running SUM analytic should remain the next sibling item after DENSE_RANK, got:\n{formatted}"
        );
        assert_eq!(
            lines[avg_idx].trim_start(),
            "AVG(c.day_hours) OVER (",
            "test5 moving AVG analytic should return to the same SELECT-item owner depth, got:\n{formatted}"
        );

        let owner_indent = leading_spaces(lines[dense_rank_idx]);
        assert_eq!(
            leading_spaces(lines[partition_idx]),
            owner_indent.saturating_add(4),
            "test5 DENSE_RANK PARTITION BY should stay one level deeper than the analytic owner, got:\n{formatted}"
        );
        assert_eq!(
            leading_spaces(lines[partition_tail_idx]),
            owner_indent.saturating_add(4),
            "test5 PARTITION BY sibling continuation should share the analytic body depth, got:\n{formatted}"
        );
        assert_eq!(
            leading_spaces(lines[order_idx]),
            owner_indent.saturating_add(4),
            "test5 DENSE_RANK ORDER BY should stay aligned with PARTITION BY, got:\n{formatted}"
        );
        assert_eq!(
            leading_spaces(lines[close_idx]),
            owner_indent,
            "test5 DENSE_RANK close should return to the analytic owner depth, got:\n{formatted}"
        );
        assert_eq!(
            leading_spaces(lines[sum_idx]),
            owner_indent,
            "test5 SUM analytic sibling should return to the SELECT-item owner depth, got:\n{formatted}"
        );
        assert_eq!(
            leading_spaces(lines[avg_idx]),
            owner_indent,
            "test5 AVG analytic sibling should stay aligned with the SUM/DENSE_RANK owner depth, got:\n{formatted}"
        );
    }

    #[test]
    fn format_sql_basic_for_mysql_db_type_keeps_test6_nested_json_function_arguments_expanded() {
        let source = include_str!("../../../test_mariadb/test6.txt");
        let formatted = SqlEditorWidget::format_sql_basic_for_db_type(
            source,
            crate::db::connection::DatabaseType::MySQL,
        );
        assert!(
            formatted.contains("'stats', JSON_OBJECT("),
            "test6 nested stats JSON_OBJECT should remain attached to the stats key, got:\n{formatted}"
        );
        assert!(
            formatted.contains("'latest_orders', JSON_ARRAYAGG("),
            "test6 latest_orders JSON_ARRAYAGG should remain attached to the key, got:\n{formatted}"
        );
        assert_eq!(
            SqlEditorWidget::format_sql_basic_for_db_type(
                &formatted,
                crate::db::connection::DatabaseType::MySQL,
            ),
            formatted
        );
    }

    #[test]
    fn format_sql_basic_for_mysql_db_type_keeps_test6_pure_close_comma_argument_siblings_on_call_depth(
    ) {
        let source = include_str!("../../../test_mariadb/test6.txt");
        let formatted = SqlEditorWidget::format_sql_basic_for_db_type(
            source,
            crate::db::connection::DatabaseType::MySQL,
        );
        let lines: Vec<&str> = formatted.lines().collect();

        let round_idx = lines
            .iter()
            .enumerate()
            .find(|(idx, line)| {
                line.trim_start() == "ROUND("
                    && lines
                        .get(idx.saturating_add(1))
                        .is_some_and(|next| next.trim_start().starts_with("net_sales"))
            })
            .map(|(idx, _)| idx)
            .expect("test6 mom_diff ROUND owner");
        let zero_idx = lines
            .iter()
            .enumerate()
            .skip(round_idx + 1)
            .take(12)
            .find(|(_, line)| line.trim_start() == "0")
            .map(|(idx, _)| idx)
            .expect("test6 IFNULL fallback argument");
        let ifnull_owner_idx = lines
            .iter()
            .enumerate()
            .skip(round_idx + 1)
            .take(8)
            .find(|(_, line)| line.trim_start().contains("IFNULL("))
            .map(|(idx, _)| idx)
            .expect("test6 IFNULL owner");
        let precision_idx = lines
            .iter()
            .enumerate()
            .skip(zero_idx + 1)
            .take(8)
            .find(|(_, line)| line.trim_start() == "2")
            .map(|(idx, _)| idx)
            .expect("test6 ROUND precision argument");

        let desired_indent = leading_spaces(lines[round_idx]).saturating_add(4);
        let ifnull_body_indent = leading_spaces(lines[ifnull_owner_idx]).saturating_add(4);
        assert_eq!(
            leading_spaces(lines[zero_idx]),
            ifnull_body_indent,
            "test6 nested IFNULL fallback after pure `),` should realign with the call body depth, got:\n{formatted}"
        );
        assert_eq!(
            leading_spaces(lines[precision_idx]),
            desired_indent,
            "test6 outer ROUND precision argument should stay aligned with the call body depth after the nested close, got:\n{formatted}"
        );
    }

    #[test]
    fn format_sql_basic_for_mysql_db_type_keeps_round_alias_close_on_parent_frame_depth() {
        let source = r#"WITH monthly AS (
    SELECT
        month_key,
        segment,
        ROUND(SUM(net_sales), 2) AS net_sales,
        ROUND(SUM(paid_amount), 2) AS paid_amount,
        ROUND(SUM(unpaid_amount), 2) AS unpaid_amount
    FROM boss_monthly_stats
    GROUP BY month_key,
        segment
)
SELECT
    month_key,
    segment,
    net_sales,
    paid_amount,
    unpaid_amount,
    LAG(net_sales, 1) OVER (
        PARTITION BY segment
        ORDER BY month_key
    ) AS prev_month_net_sales,
    ROUND(net_sales - IFNULL(LAG(net_sales, 1) OVER (
            PARTITION BY segment
            ORDER BY month_key
    ),
        0
),
        2
    ) AS mom_diff,
    ROUND(SUM(net_sales) OVER (
        PARTITION BY segment
        ORDER BY month_key
        ROWS BETWEEN 2 PRECEDING AND CURRENT ROW
    ),
        2
    ) AS rolling_3m_net_sales,
    DENSE_RANK() OVER (
        PARTITION BY month_key
        ORDER BY net_sales DESC,
        segment
    ) AS month_rank
FROM monthly
ORDER BY month_key,
    segment;"#;
        let formatted = SqlEditorWidget::format_sql_basic_for_db_type(
            source,
            crate::db::connection::DatabaseType::MySQL,
        );
        let lines: Vec<&str> = formatted.lines().collect();

        let round_owner_idx = lines
            .iter()
            .enumerate()
            .find(|(idx, line)| {
                line.trim_start() == "ROUND("
                    && lines
                        .iter()
                        .skip(idx.saturating_add(1))
                        .take(6)
                        .any(|next| next.contains("IFNULL("))
            })
            .map(|(idx, _)| idx)
            .expect("ROUND owner");
        let round_close_alias_idx = lines
            .iter()
            .enumerate()
            .skip(round_owner_idx + 1)
            .find(|(_, line)| line.trim_start() == ") AS mom_diff,")
            .map(|(idx, _)| idx)
            .expect("ROUND close alias");
        let rolling_owner_idx = lines
            .iter()
            .enumerate()
            .skip(round_close_alias_idx + 1)
            .find(|(idx, line)| {
                line.trim_start() == "ROUND("
                    && lines
                        .get(idx.saturating_add(1))
                        .is_some_and(|next| next.trim_start() == "SUM(net_sales) OVER (")
            })
            .map(|(idx, _)| idx)
            .expect("rolling ROUND owner");
        let zero_idx = lines
            .iter()
            .enumerate()
            .skip(round_owner_idx + 1)
            .take(12)
            .find(|(_, line)| line.trim_start() == "0")
            .map(|(idx, _)| idx)
            .expect("IFNULL fallback");
        let ifnull_owner_idx = lines
            .iter()
            .enumerate()
            .skip(round_owner_idx + 1)
            .take(8)
            .find(|(_, line)| line.trim_start().contains("IFNULL("))
            .map(|(idx, _)| idx)
            .expect("IFNULL owner");
        let precision_idx = lines
            .iter()
            .enumerate()
            .skip(zero_idx + 1)
            .take(10)
            .find(|(_, line)| line.trim_start() == "2")
            .map(|(idx, _)| idx)
            .expect("ROUND precision");
        let ifnull_close_idx = lines
            .iter()
            .enumerate()
            .skip(zero_idx + 1)
            .take(8)
            .find(|(_, line)| line.trim_start() == "),")
            .map(|(idx, _)| idx)
            .expect("IFNULL close");

        let round_owner_indent = leading_spaces(lines[round_owner_idx]);
        let expected_arg_indent = round_owner_indent.saturating_add(4);
        let expected_ifnull_owner_indent = leading_spaces(lines[ifnull_owner_idx]);
        let expected_ifnull_body_indent = expected_ifnull_owner_indent.saturating_add(4);
        assert_eq!(
            leading_spaces(lines[zero_idx]),
            expected_ifnull_body_indent,
            "nested IFNULL fallback should stay on the ROUND call frame body depth, got:\n{formatted}"
        );
        assert_eq!(
            leading_spaces(lines[precision_idx]),
            expected_arg_indent,
            "ROUND precision argument should stay on the ROUND call frame body depth, got:\n{formatted}"
        );
        assert_eq!(
            leading_spaces(lines[ifnull_close_idx]),
            expected_ifnull_owner_indent,
            "IFNULL pure-close line should stay on the parent call frame body depth before ROUND sibling arguments, got:\n{formatted}"
        );
        assert_eq!(
            leading_spaces(lines[round_close_alias_idx]),
            round_owner_indent,
            "ROUND close alias should return to the ROUND owner frame depth, got:\n{formatted}"
        );
        assert_eq!(
            leading_spaces(lines[rolling_owner_idx]),
            round_owner_indent,
            "SELECT sibling after the ROUND close alias should realign with the parent SELECT-list frame depth, got:\n{formatted}"
        );
    }

    #[test]
    fn format_for_auto_formatting_mysql_keeps_round_alias_close_on_parent_frame_depth() {
        let source = r#"/* ------------------------------------------------------------------------
   FINAL BOSS QUERY #2
   monthly analytic + LAG + rolling window + dense rank
   ------------------------------------------------------------------------ */

WITH monthly AS (
    SELECT
        month_key,
        segment,
        ROUND(SUM(net_sales), 2) AS net_sales,
        ROUND(SUM(paid_amount), 2) AS paid_amount,
        ROUND(SUM(unpaid_amount), 2) AS unpaid_amount
    FROM boss_monthly_stats
    GROUP BY month_key,
        segment
)
SELECT
    month_key,
    segment,
    net_sales,
    paid_amount,
    unpaid_amount,
    LAG(net_sales, 1) OVER (
        PARTITION BY segment
        ORDER BY month_key
    ) AS prev_month_net_sales,
    ROUND(net_sales - IFNULL(LAG(net_sales, 1) OVER (
            PARTITION BY segment
            ORDER BY month_key
    ),
        0
),
        2
    ) AS mom_diff,
    ROUND(SUM(net_sales) OVER (
        PARTITION BY segment
        ORDER BY month_key
        ROWS BETWEEN 2 PRECEDING AND CURRENT ROW
    ),
        2
    ) AS rolling_3m_net_sales,
    DENSE_RANK() OVER (
        PARTITION BY month_key
        ORDER BY net_sales DESC,
        segment
    ) AS month_rank
FROM monthly
ORDER BY month_key,
    segment;"#;
        let formatted = SqlEditorWidget::format_for_auto_formatting_with_db_type(
            source,
            false,
            Some(crate::db::connection::DatabaseType::MySQL),
        );
        let lines: Vec<&str> = formatted.lines().collect();

        let round_owner_idx = lines
            .iter()
            .enumerate()
            .find(|(idx, line)| {
                line.trim_start() == "ROUND("
                    && lines
                        .iter()
                        .skip(idx.saturating_add(1))
                        .take(6)
                        .any(|next| next.contains("IFNULL("))
            })
            .map(|(idx, _)| idx)
            .expect("ROUND owner");
        let round_close_alias_idx = lines
            .iter()
            .enumerate()
            .skip(round_owner_idx + 1)
            .find(|(_, line)| line.trim_start() == ") AS mom_diff,")
            .map(|(idx, _)| idx)
            .expect("ROUND close alias");
        let rolling_owner_idx = lines
            .iter()
            .enumerate()
            .skip(round_close_alias_idx + 1)
            .find(|(idx, line)| {
                line.trim_start() == "ROUND("
                    && lines
                        .get(idx.saturating_add(1))
                        .is_some_and(|next| next.trim_start() == "SUM(net_sales) OVER (")
            })
            .map(|(idx, _)| idx)
            .expect("rolling ROUND owner");
        let zero_idx = lines
            .iter()
            .enumerate()
            .skip(round_owner_idx + 1)
            .take(12)
            .find(|(_, line)| line.trim_start() == "0")
            .map(|(idx, _)| idx)
            .expect("IFNULL fallback");
        let ifnull_owner_idx = lines
            .iter()
            .enumerate()
            .skip(round_owner_idx + 1)
            .take(8)
            .find(|(_, line)| line.trim_start().contains("IFNULL("))
            .map(|(idx, _)| idx)
            .expect("IFNULL owner");
        let precision_idx = lines
            .iter()
            .enumerate()
            .skip(zero_idx + 1)
            .take(10)
            .find(|(_, line)| line.trim_start() == "2")
            .map(|(idx, _)| idx)
            .expect("ROUND precision");
        let ifnull_close_idx = lines
            .iter()
            .enumerate()
            .skip(zero_idx + 1)
            .take(8)
            .find(|(_, line)| line.trim_start() == "),")
            .map(|(idx, _)| idx)
            .expect("IFNULL close");

        let round_owner_indent = leading_spaces(lines[round_owner_idx]);
        let expected_arg_indent = round_owner_indent.saturating_add(4);
        let expected_ifnull_owner_indent = leading_spaces(lines[ifnull_owner_idx]);
        let expected_ifnull_body_indent = expected_ifnull_owner_indent.saturating_add(4);
        assert_eq!(
            leading_spaces(lines[zero_idx]),
            expected_ifnull_body_indent,
            "auto-format IFNULL fallback should stay on the ROUND call frame body depth, got:\n{formatted}"
        );
        assert_eq!(
            leading_spaces(lines[precision_idx]),
            expected_arg_indent,
            "auto-format ROUND precision should stay on the ROUND call frame body depth, got:\n{formatted}"
        );
        assert_eq!(
            leading_spaces(lines[ifnull_close_idx]),
            expected_ifnull_owner_indent,
            "auto-format IFNULL pure-close line should stay on the parent call frame body depth before ROUND sibling arguments, got:\n{formatted}"
        );
        assert_eq!(
            leading_spaces(lines[round_close_alias_idx]),
            round_owner_indent,
            "auto-format ROUND close alias should return to the ROUND owner frame depth, got:\n{formatted}"
        );
        assert_eq!(
            leading_spaces(lines[rolling_owner_idx]),
            round_owner_indent,
            "auto-format SELECT sibling after ROUND close alias should realign with the parent SELECT-list frame depth, got:\n{formatted}"
        );
    }

    #[test]
    fn format_sql_basic_for_mysql_db_type_keeps_mariadb_scalar_subquery_siblings_on_select_list_frame_depth(
    ) {
        let source = r#"CREATE PROCEDURE sp_validate_final_boss()
BEGIN
    SELECT
        (
            SELECT COUNT(*)
            FROM boss_region
        ) AS regions,
    (
        SELECT COUNT(*)
        FROM boss_customer
    ) AS customers,
    (
        SELECT COUNT(*)
        FROM boss_product
    ) AS products,
    (
        SELECT COUNT(*)
        FROM boss_order
    ) AS orders_cnt,
    (
        SELECT COUNT(*)
        FROM boss_order_item
    ) AS items_cnt,
    (
        SELECT COUNT(*)
        FROM boss_payment
    ) AS payments_cnt,
    (
        SELECT COUNT(*)
        FROM boss_monthly_stats
    ) AS monthly_rows,
    (
        SELECT COUNT(*)
        FROM boss_audit
    ) AS audit_rows;
END $$"#;
        let formatted = SqlEditorWidget::format_sql_basic_for_db_type(
            source,
            crate::db::connection::DatabaseType::MySQL,
        );
        let lines: Vec<&str> = formatted.lines().collect();

        let select_idx = find_line_starting_with(&lines, "SELECT").expect("procedure SELECT head");
        let open_indices: Vec<usize> = lines
            .iter()
            .enumerate()
            .skip(select_idx + 1)
            .filter(|(_, line)| line.trim_start() == "(")
            .map(|(idx, _)| idx)
            .collect();
        let close_indices: Vec<usize> = lines
            .iter()
            .enumerate()
            .skip(select_idx + 1)
            .filter(|(_, line)| line.trim_start().starts_with(") AS "))
            .map(|(idx, _)| idx)
            .collect();
        let count_select_indices: Vec<usize> = lines
            .iter()
            .enumerate()
            .skip(select_idx + 1)
            .filter(|(_, line)| line.trim_start() == "SELECT COUNT(*)")
            .map(|(idx, _)| idx)
            .collect();
        let count_from_indices: Vec<usize> = lines
            .iter()
            .enumerate()
            .skip(select_idx + 1)
            .filter(|(_, line)| line.trim_start().starts_with("FROM boss_"))
            .map(|(idx, _)| idx)
            .collect();

        assert_eq!(
            open_indices.len(),
            8,
            "procedure scalar subquery list should keep eight sibling open lines, got:\n{formatted}"
        );
        assert_eq!(
            close_indices.len(),
            8,
            "procedure scalar subquery list should keep eight sibling close-alias lines, got:\n{formatted}"
        );
        assert_eq!(
            count_select_indices.len(),
            8,
            "procedure scalar subquery list should keep eight SELECT COUNT(*) heads, got:\n{formatted}"
        );
        assert_eq!(
            count_from_indices.len(),
            8,
            "procedure scalar subquery list should keep eight FROM siblings, got:\n{formatted}"
        );

        let select_indent = leading_spaces(lines[select_idx]);
        let open_indent = leading_spaces(lines[open_indices[0]]);
        assert_eq!(
            open_indent,
            select_indent.saturating_add(4),
            "scalar subquery opener should stay one frame deeper than the SELECT owner, got:\n{formatted}"
        );
        for idx in open_indices.iter().skip(1) {
            assert_eq!(
                leading_spaces(lines[*idx]),
                open_indent,
                "scalar subquery sibling openers after close-alias comma should return to the same SELECT-list frame depth, got:\n{formatted}"
            );
        }
        for idx in close_indices {
            assert_eq!(
                leading_spaces(lines[idx]),
                open_indent,
                "scalar subquery close-alias lines should align with the subquery opener owner depth, got:\n{formatted}"
            );
        }
        for (select_count_idx, from_idx) in
            count_select_indices.iter().zip(count_from_indices.iter())
        {
            assert_eq!(
                leading_spaces(lines[*select_count_idx]),
                open_indent.saturating_add(4),
                "scalar subquery SELECT COUNT(*) heads should stay one frame deeper than the opener, got:\n{formatted}"
            );
            assert_eq!(
                leading_spaces(lines[*from_idx]),
                leading_spaces(lines[*select_count_idx]),
                "scalar subquery FROM lines should stay aligned with their SELECT heads, got:\n{formatted}"
            );
        }
    }

    #[test]
    fn format_for_auto_formatting_mysql_keeps_mariadb_scalar_subquery_siblings_on_select_list_frame_depth(
    ) {
        let source = r#"CREATE PROCEDURE sp_validate_final_boss()
BEGIN
    SELECT
        (
            SELECT COUNT(*)
            FROM boss_region
        ) AS regions,
    (
        SELECT COUNT(*)
        FROM boss_customer
    ) AS customers,
    (
        SELECT COUNT(*)
        FROM boss_product
    ) AS products,
    (
        SELECT COUNT(*)
        FROM boss_order
    ) AS orders_cnt,
    (
        SELECT COUNT(*)
        FROM boss_order_item
    ) AS items_cnt,
    (
        SELECT COUNT(*)
        FROM boss_payment
    ) AS payments_cnt,
    (
        SELECT COUNT(*)
        FROM boss_monthly_stats
    ) AS monthly_rows,
    (
        SELECT COUNT(*)
        FROM boss_audit
    ) AS audit_rows;
END $$"#;
        let formatted = SqlEditorWidget::format_for_auto_formatting_with_db_type(
            source,
            false,
            Some(crate::db::connection::DatabaseType::MySQL),
        );
        let lines: Vec<&str> = formatted.lines().collect();

        let select_idx = find_line_starting_with(&lines, "SELECT").expect("procedure SELECT head");
        let open_indices: Vec<usize> = lines
            .iter()
            .enumerate()
            .skip(select_idx + 1)
            .filter(|(_, line)| line.trim_start() == "(")
            .map(|(idx, _)| idx)
            .collect();
        let close_indices: Vec<usize> = lines
            .iter()
            .enumerate()
            .skip(select_idx + 1)
            .filter(|(_, line)| line.trim_start().starts_with(") AS "))
            .map(|(idx, _)| idx)
            .collect();
        let count_select_indices: Vec<usize> = lines
            .iter()
            .enumerate()
            .skip(select_idx + 1)
            .filter(|(_, line)| line.trim_start() == "SELECT COUNT(*)")
            .map(|(idx, _)| idx)
            .collect();
        let count_from_indices: Vec<usize> = lines
            .iter()
            .enumerate()
            .skip(select_idx + 1)
            .filter(|(_, line)| line.trim_start().starts_with("FROM boss_"))
            .map(|(idx, _)| idx)
            .collect();

        assert_eq!(
            open_indices.len(),
            8,
            "auto-format procedure scalar subquery list should keep eight sibling open lines, got:\n{formatted}"
        );
        assert_eq!(
            close_indices.len(),
            8,
            "auto-format procedure scalar subquery list should keep eight sibling close-alias lines, got:\n{formatted}"
        );
        assert_eq!(
            count_select_indices.len(),
            8,
            "auto-format procedure scalar subquery list should keep eight SELECT COUNT(*) heads, got:\n{formatted}"
        );
        assert_eq!(
            count_from_indices.len(),
            8,
            "auto-format procedure scalar subquery list should keep eight FROM siblings, got:\n{formatted}"
        );

        let select_indent = leading_spaces(lines[select_idx]);
        let open_indent = leading_spaces(lines[open_indices[0]]);
        assert_eq!(
            open_indent,
            select_indent.saturating_add(4),
            "auto-format scalar subquery opener should stay one frame deeper than the SELECT owner, got:\n{formatted}"
        );
        for idx in open_indices.iter().skip(1) {
            assert_eq!(
                leading_spaces(lines[*idx]),
                open_indent,
                "auto-format scalar subquery sibling openers after close-alias comma should return to the same SELECT-list frame depth, got:\n{formatted}"
            );
        }
        for idx in close_indices {
            assert_eq!(
                leading_spaces(lines[idx]),
                open_indent,
                "auto-format scalar subquery close-alias lines should align with the subquery opener owner depth, got:\n{formatted}"
            );
        }
        for (select_count_idx, from_idx) in
            count_select_indices.iter().zip(count_from_indices.iter())
        {
            assert_eq!(
                leading_spaces(lines[*select_count_idx]),
                open_indent.saturating_add(4),
                "auto-format scalar subquery SELECT COUNT(*) heads should stay one frame deeper than the opener, got:\n{formatted}"
            );
            assert_eq!(
                leading_spaces(lines[*from_idx]),
                leading_spaces(lines[*select_count_idx]),
                "auto-format scalar subquery FROM lines should stay aligned with their SELECT heads, got:\n{formatted}"
            );
        }
    }

    #[test]
    fn format_sql_basic_for_mysql_db_type_keeps_mariadb_values_case_siblings_on_same_call_frame_depth(
    ) {
        let source = r#"CREATE PROCEDURE sp_seed_final_boss()
BEGIN
    WHILE v_order_id <= 240 DO
        INSERT INTO boss_order_item (order_id, line_no, unit_price, discount_rate, tax_rate, note)
        VALUES (v_order_id, v_item_no,
            CASE
                WHEN MOD(v_item_no, 2) = 0 THEN
                    NULL
                ELSE
                    ROUND(19 + (v_prod_id * 7.35) + (MOD(v_prod_id, 5) * 3.70), 2)
            END,
                CASE
                    WHEN MOD(v_item_no, 5) = 0 THEN
                        0.2000
                    WHEN MOD(v_item_no, 3) = 0 THEN
                        0.0700
                    ELSE
                        ROUND((MOD(v_item_no + v_order_id, 4) * 0.0200), 4)
                END,
                CASE
                    WHEN MOD(v_prod_id, 7) = 0 THEN
                        0.0500
                    ELSE
                        0.1000
                END, CONCAT('note ; ', v_order_id, '/', v_item_no, ' // not delimiter'));
        SET v_item_no = v_item_no + 1;
    END WHILE;
END$$"#;

        let formatted = SqlEditorWidget::format_sql_basic_for_db_type(
            source,
            crate::db::connection::DatabaseType::MySQL,
        );
        let lines: Vec<&str> = formatted.lines().collect();

        let case_indices: Vec<usize> = lines
            .iter()
            .enumerate()
            .filter(|(_, line)| line.trim_start() == "CASE")
            .map(|(idx, _)| idx)
            .collect();
        let end_indices: Vec<usize> = lines
            .iter()
            .enumerate()
            .filter(|(_, line)| line.trim_start().starts_with("END,"))
            .map(|(idx, _)| idx)
            .collect();
        let when_indices: Vec<usize> = lines
            .iter()
            .enumerate()
            .filter(|(_, line)| line.trim_start().starts_with("WHEN "))
            .map(|(idx, _)| idx)
            .collect();
        let else_indices: Vec<usize> = lines
            .iter()
            .enumerate()
            .filter(|(_, line)| line.trim_start() == "ELSE")
            .map(|(idx, _)| idx)
            .collect();

        assert_eq!(
            case_indices.len(),
            3,
            "VALUES argument CASE siblings should remain present, got:\n{formatted}"
        );
        assert!(
            end_indices.len() >= 2,
            "first two CASE items should close with END, got:\n{formatted}"
        );
        assert!(
            when_indices.len() >= 3,
            "CASE branches should remain expanded with WHEN lines, got:\n{formatted}"
        );
        assert_eq!(
            else_indices.len(),
            3,
            "each CASE sibling should keep an ELSE branch, got:\n{formatted}"
        );

        let case_indent = leading_spaces(lines[case_indices[0]]);
        assert_eq!(
            leading_spaces(lines[case_indices[1]]),
            case_indent,
            "second CASE after END, should return to the same VALUES call frame depth, got:\n{formatted}"
        );
        assert_eq!(
            leading_spaces(lines[case_indices[2]]),
            case_indent,
            "third CASE sibling should stay on the same VALUES call frame depth, got:\n{formatted}"
        );
        assert_eq!(
            leading_spaces(lines[end_indices[0]]),
            case_indent,
            "CASE END, should align with its CASE owner depth, got:\n{formatted}"
        );
        assert_eq!(
            leading_spaces(lines[end_indices[1]]),
            case_indent,
            "second CASE END, should align with its CASE owner depth, got:\n{formatted}"
        );
        assert_eq!(
            leading_spaces(lines[when_indices[0]]),
            case_indent.saturating_add(4),
            "WHEN branch should be one frame deeper than CASE, got:\n{formatted}"
        );
        assert_eq!(
            leading_spaces(lines[else_indices[0]]),
            case_indent.saturating_add(4),
            "ELSE branch should be one frame deeper than CASE, got:\n{formatted}"
        );
    }

    #[test]
    fn format_for_auto_formatting_mysql_keeps_mariadb_values_case_siblings_on_same_call_frame_depth(
    ) {
        let source = r#"CREATE PROCEDURE sp_seed_final_boss()
BEGIN
    WHILE v_order_id <= 240 DO
        INSERT INTO boss_order_item (order_id, line_no, unit_price, discount_rate, tax_rate, note)
        VALUES (v_order_id, v_item_no,
            CASE
                WHEN MOD(v_item_no, 2) = 0 THEN
                    NULL
                ELSE
                    ROUND(19 + (v_prod_id * 7.35) + (MOD(v_prod_id, 5) * 3.70), 2)
            END,
                CASE
                    WHEN MOD(v_item_no, 5) = 0 THEN
                        0.2000
                    WHEN MOD(v_item_no, 3) = 0 THEN
                        0.0700
                    ELSE
                        ROUND((MOD(v_item_no + v_order_id, 4) * 0.0200), 4)
                END,
                CASE
                    WHEN MOD(v_prod_id, 7) = 0 THEN
                        0.0500
                    ELSE
                        0.1000
                END, CONCAT('note ; ', v_order_id, '/', v_item_no, ' // not delimiter'));
        SET v_item_no = v_item_no + 1;
    END WHILE;
END$$"#;

        let formatted = SqlEditorWidget::format_for_auto_formatting_with_db_type(
            source,
            false,
            Some(crate::db::connection::DatabaseType::MySQL),
        );
        let lines: Vec<&str> = formatted.lines().collect();

        let case_indices: Vec<usize> = lines
            .iter()
            .enumerate()
            .filter(|(_, line)| line.trim_start() == "CASE")
            .map(|(idx, _)| idx)
            .collect();
        let end_indices: Vec<usize> = lines
            .iter()
            .enumerate()
            .filter(|(_, line)| line.trim_start().starts_with("END,"))
            .map(|(idx, _)| idx)
            .collect();
        let when_indices: Vec<usize> = lines
            .iter()
            .enumerate()
            .filter(|(_, line)| line.trim_start().starts_with("WHEN "))
            .map(|(idx, _)| idx)
            .collect();
        let else_indices: Vec<usize> = lines
            .iter()
            .enumerate()
            .filter(|(_, line)| line.trim_start() == "ELSE")
            .map(|(idx, _)| idx)
            .collect();

        assert_eq!(
            case_indices.len(),
            3,
            "auto-format VALUES argument CASE siblings should remain present, got:\n{formatted}"
        );
        assert!(
            end_indices.len() >= 2,
            "auto-format first two CASE items should close with END, got:\n{formatted}"
        );
        assert!(
            when_indices.len() >= 3,
            "auto-format CASE branches should remain expanded with WHEN lines, got:\n{formatted}"
        );
        assert_eq!(
            else_indices.len(),
            3,
            "auto-format each CASE sibling should keep an ELSE branch, got:\n{formatted}"
        );

        let case_indent = leading_spaces(lines[case_indices[0]]);
        assert_eq!(
            leading_spaces(lines[case_indices[1]]),
            case_indent,
            "auto-format second CASE after END, should return to the same VALUES call frame depth, got:\n{formatted}"
        );
        assert_eq!(
            leading_spaces(lines[case_indices[2]]),
            case_indent,
            "auto-format third CASE sibling should stay on the same VALUES call frame depth, got:\n{formatted}"
        );
        assert_eq!(
            leading_spaces(lines[end_indices[0]]),
            case_indent,
            "auto-format CASE END, should align with its CASE owner depth, got:\n{formatted}"
        );
        assert_eq!(
            leading_spaces(lines[end_indices[1]]),
            case_indent,
            "auto-format second CASE END, should align with its CASE owner depth, got:\n{formatted}"
        );
        assert_eq!(
            leading_spaces(lines[when_indices[0]]),
            case_indent.saturating_add(4),
            "auto-format WHEN branch should be one frame deeper than CASE, got:\n{formatted}"
        );
        assert_eq!(
            leading_spaces(lines[else_indices[0]]),
            case_indent.saturating_add(4),
            "auto-format ELSE branch should be one frame deeper than CASE, got:\n{formatted}"
        );
    }

    #[test]
    fn format_for_auto_formatting_mysql_keeps_user_example_case_end_siblings_on_frame_depth() {
        let source = r#"CREATE PROCEDURE sp_seed_final_boss()
BEGIN
    WHILE v_order_id <= 240 DO
        INSERT INTO boss_order (order_id, customer_id, order_no, order_date, status, meta_json)
        VALUES (v_order_id, 1 + MOD(v_order_id * 7, 36), CONCAT('ORD', DATE_FORMAT(DATE_ADD('2024-01-01', INTERVAL MOD(v_order_id * 5, 720) DAY), '%Y%m%d'), '-', LPAD(v_order_id, 5, '0')), DATE_ADD('2024-01-01', INTERVAL MOD(v_order_id * 5, 720) DAY), v_status, JSON_OBJECT('channel', ELT(1 + MOD(v_order_id, 4), 'WEB', 'PARTNER', 'DIRECT', 'MOBILE'), 'priority', MOD(v_order_id, 5), 'memo', CONCAT('semi; // ', v_order_id, ' /* literal */'), 'flag', IF(MOD(v_order_id, 2) = 0, 'Y', 'N')));
        SET v_item_cnt = 1 + MOD(v_order_id, 5);
        SET v_item_no = 1;
        WHILE v_item_no <= v_item_cnt DO
            SET v_prod_id = 1 + MOD(v_order_id * 7 + v_item_no * 11, 36);
            INSERT INTO boss_order_item (order_id, line_no, product_id, quantity, unit_price, discount_rate, tax_rate, note, details_json)
            VALUES (v_order_id, v_item_no, v_prod_id, 1 + MOD(v_order_id + v_item_no, 4), 
                CASE
                    WHEN MOD(v_item_no, 2) = 0 THEN
                        NULL
                    ELSE
                        ROUND(19 +(v_prod_id * 7.35) +(MOD(v_prod_id, 5) * 3.70), 2)
                END, 
                    CASE
                        WHEN MOD(v_item_no, 5) = 0 THEN
                            0.2000
                        WHEN MOD(v_item_no, 3) = 0 THEN
                            0.0700
                        ELSE
                            ROUND((MOD(v_item_no + v_order_id, 4) * 0.0200), 4)
                    END, 
                    CASE
                        WHEN MOD(v_prod_id, 7) = 0 THEN
                            0.0500
                        ELSE
                            0.1000
                    END, CONCAT('note ; ', v_order_id, '/', v_item_no, ' // not delimiter'), JSON_OBJECT('gift', IF(MOD(v_order_id + v_item_no, 6) = 0, 'Y', 'N'), 'tags', JSON_ARRAY(CONCAT('o', v_order_id), CONCAT('i', v_item_no), 'semi;colon', 'slash//slash'), 'payload', CONCAT('payload-', v_order_id, '-', v_item_no)));
            SET v_item_no = v_item_no + 1;
        END WHILE;
    END WHILE;
END$$"#;

        let formatted = SqlEditorWidget::format_for_auto_formatting_with_db_type(
            source,
            false,
            Some(crate::db::connection::DatabaseType::MySQL),
        );
        let lines: Vec<&str> = formatted.lines().collect();

        let values_idx = lines
            .iter()
            .position(|line| {
                line.trim_start()
                    .starts_with("VALUES (v_order_id, v_item_no, v_prod_id,")
            })
            .expect("VALUES owner");
        let case_indices: Vec<usize> = lines
            .iter()
            .enumerate()
            .skip(values_idx + 1)
            .take(40)
            .filter(|(_, line)| line.trim_start() == "CASE")
            .map(|(idx, _)| idx)
            .collect();
        let end_indices: Vec<usize> = lines
            .iter()
            .enumerate()
            .skip(values_idx + 1)
            .take(60)
            .filter(|(_, line)| line.trim_start().starts_with("END,"))
            .map(|(idx, _)| idx)
            .collect();

        assert_eq!(
            case_indices.len(),
            3,
            "user example VALUES should keep three CASE siblings, got:\n{formatted}"
        );
        assert!(
            end_indices.len() >= 2,
            "user example first two CASE siblings should close with END, got:\n{formatted}"
        );

        let case_indent = leading_spaces(lines[case_indices[0]]);
        assert_eq!(
            leading_spaces(lines[case_indices[1]]),
            case_indent,
            "user example second CASE after END, should realign with the first CASE frame depth, got:\n{formatted}"
        );
        assert_eq!(
            leading_spaces(lines[case_indices[2]]),
            case_indent,
            "user example third CASE should stay on the same call argument frame depth, got:\n{formatted}"
        );
        assert_eq!(
            leading_spaces(lines[end_indices[0]]),
            case_indent,
            "user example first END, should align with CASE owner depth, got:\n{formatted}"
        );
        assert_eq!(
            leading_spaces(lines[end_indices[1]]),
            case_indent,
            "user example second END, should align with CASE owner depth, got:\n{formatted}"
        );
    }

    #[test]
    fn format_sql_basic_for_mysql_db_type_keeps_test6_fetch_into_targets_indented_under_into_owner()
    {
        let source = include_str!("../../../test_mariadb/test6.txt");
        let formatted = SqlEditorWidget::format_sql_basic_for_db_type(
            source,
            crate::db::connection::DatabaseType::MySQL,
        );
        let lines: Vec<&str> = formatted.lines().collect();

        let fetch_idx = find_line_starting_with(&lines, "FETCH cur").expect("test6 FETCH cur");
        let into_idx = lines
            .iter()
            .enumerate()
            .skip(fetch_idx + 1)
            .find(|(_, line)| line.trim_start() == "INTO v_month_key,")
            .map(|(idx, _)| idx)
            .expect("test6 INTO v_month_key");
        let sibling_indices: Vec<usize> = lines
            .iter()
            .enumerate()
            .skip(into_idx + 1)
            .take(5)
            .filter(|(_, line)| {
                matches!(
                    line.trim_start(),
                    "v_region_id,"
                        | "v_segment,"
                        | "v_net_sales,"
                        | "v_orders_cnt,"
                        | "v_top_category;"
                )
            })
            .map(|(idx, _)| idx)
            .collect();

        assert_eq!(
            sibling_indices.len(),
            5,
            "test6 FETCH INTO should keep five continuation targets, got:\n{formatted}"
        );

        let desired_indent = leading_spaces(lines[into_idx]).saturating_add(4);
        for sibling_idx in sibling_indices {
            assert_eq!(
                leading_spaces(lines[sibling_idx]),
                desired_indent,
                "test6 FETCH INTO target siblings should stay one level deeper than the INTO owner line, got:\n{formatted}"
            );
        }
    }

    #[test]
    fn format_sql_basic_for_mysql_db_type_keeps_test4_frame_based_depths_for_ddl_and_handlers() {
        let source = include_str!("../../../test_mariadb/test4.txt");
        let formatted = SqlEditorWidget::format_sql_basic_for_db_type(
            source,
            crate::db::connection::DatabaseType::MySQL,
        );
        let lines: Vec<&str> = formatted.lines().collect();

        let create_idx =
            find_line_starting_with(&lines, "CREATE TABLE departments (").expect("test4 CREATE");
        let dept_id_idx = lines
            .iter()
            .enumerate()
            .skip(create_idx + 1)
            .find(|(_, line)| line.trim_start().starts_with("dept_id"))
            .map(|(idx, _)| idx)
            .expect("test4 departments dept_id");
        let create_close_idx = lines
            .iter()
            .enumerate()
            .skip(dept_id_idx + 1)
            .find(|(_, line)| line.trim_start() == ")")
            .map(|(idx, _)| idx)
            .expect("test4 departments close");
        let if_idx =
            find_line_starting_with(&lines, "IF NEW.hours IS NULL").expect("test4 IF NEW.hours");
        let or_idx =
            find_line_starting_with(&lines, "OR NEW.hours <= 0").expect("test4 OR NEW.hours");
        let while_idx =
            find_line_starting_with(&lines, "WHILE v_day < 35 DO").expect("test4 outer WHILE");
        let date_add_idx = find_line_starting_with(
            &lines,
            "SET v_work_date = DATE_ADD('2025-01-01', INTERVAL v_day DAY);",
        )
        .expect("test4 DATE_ADD body");
        let handler_idx = find_line_starting_with(&lines, "DECLARE EXIT HANDLER FOR SQLEXCEPTION")
            .expect("test4 handler header");
        let handler_begin_idx = lines
            .iter()
            .enumerate()
            .skip(handler_idx + 1)
            .find(|(_, line)| line.trim_start() == "BEGIN")
            .map(|(idx, _)| idx)
            .expect("test4 handler BEGIN");
        let rollback_idx = lines
            .iter()
            .enumerate()
            .skip(handler_begin_idx + 1)
            .find(|(_, line)| line.trim_start() == "ROLLBACK;")
            .map(|(idx, _)| idx)
            .expect("test4 handler ROLLBACK");

        assert_eq!(
            leading_spaces(lines[dept_id_idx]),
            leading_spaces(lines[create_idx]).saturating_add(4),
            "test4 CREATE TABLE body should stay one frame deeper than the table header, got:\n{formatted}"
        );
        assert_eq!(
            leading_spaces(lines[create_close_idx]),
            leading_spaces(lines[create_idx]),
            "test4 CREATE TABLE close should return to the table header depth, got:\n{formatted}"
        );
        assert_eq!(
            leading_spaces(lines[or_idx]),
            leading_spaces(lines[if_idx]).saturating_add(4),
            "test4 split IF condition continuation should stay one frame deeper than the IF header, got:\n{formatted}"
        );
        assert_eq!(
            leading_spaces(lines[date_add_idx]),
            leading_spaces(lines[while_idx]).saturating_add(4),
            "test4 WHILE body line with DATE_ADD should stay on the loop body frame depth, got:\n{formatted}"
        );
        assert_eq!(
            leading_spaces(lines[handler_begin_idx]),
            leading_spaces(lines[handler_idx]).saturating_add(4),
            "test4 handler BEGIN should open one frame deeper than the DECLARE HANDLER header, got:\n{formatted}"
        );
        assert_eq!(
            leading_spaces(lines[rollback_idx]),
            leading_spaces(lines[handler_idx]).saturating_add(8),
            "test4 handler body statements should stay one frame deeper than the handler BEGIN line, got:\n{formatted}"
        );
    }

    #[test]
    fn format_for_auto_formatting_mysql_keeps_test4_frame_based_depths_for_ddl_and_handlers() {
        let source = include_str!("../../../test_mariadb/test4.txt");
        let formatted = SqlEditorWidget::format_for_auto_formatting_with_db_type(
            source,
            false,
            Some(crate::db::connection::DatabaseType::MySQL),
        );
        let lines: Vec<&str> = formatted.lines().collect();

        let create_idx =
            find_line_starting_with(&lines, "CREATE TABLE departments (").expect("test4 CREATE");
        let dept_id_idx = lines
            .iter()
            .enumerate()
            .skip(create_idx + 1)
            .find(|(_, line)| line.trim_start().starts_with("dept_id"))
            .map(|(idx, _)| idx)
            .expect("test4 departments dept_id");
        let create_close_idx = lines
            .iter()
            .enumerate()
            .skip(dept_id_idx + 1)
            .find(|(_, line)| line.trim_start() == ")")
            .map(|(idx, _)| idx)
            .expect("test4 departments close");
        let if_idx =
            find_line_starting_with(&lines, "IF NEW.hours IS NULL").expect("test4 IF NEW.hours");
        let or_idx =
            find_line_starting_with(&lines, "OR NEW.hours <= 0").expect("test4 OR NEW.hours");
        let while_idx =
            find_line_starting_with(&lines, "WHILE v_day < 35 DO").expect("test4 outer WHILE");
        let date_add_idx = find_line_starting_with(
            &lines,
            "SET v_work_date = DATE_ADD('2025-01-01', INTERVAL v_day DAY);",
        )
        .expect("test4 DATE_ADD body");
        let handler_idx = find_line_starting_with(&lines, "DECLARE EXIT HANDLER FOR SQLEXCEPTION")
            .expect("test4 handler header");
        let handler_begin_idx = lines
            .iter()
            .enumerate()
            .skip(handler_idx + 1)
            .find(|(_, line)| line.trim_start() == "BEGIN")
            .map(|(idx, _)| idx)
            .expect("test4 handler BEGIN");
        let rollback_idx = lines
            .iter()
            .enumerate()
            .skip(handler_begin_idx + 1)
            .find(|(_, line)| line.trim_start() == "ROLLBACK;")
            .map(|(idx, _)| idx)
            .expect("test4 handler ROLLBACK");

        assert_eq!(
            leading_spaces(lines[dept_id_idx]),
            leading_spaces(lines[create_idx]).saturating_add(4),
            "test4 auto-format CREATE TABLE body should stay one frame deeper than the table header, got:\n{formatted}"
        );
        assert_eq!(
            leading_spaces(lines[create_close_idx]),
            leading_spaces(lines[create_idx]),
            "test4 auto-format CREATE TABLE close should return to the table header depth, got:\n{formatted}"
        );
        assert_eq!(
            leading_spaces(lines[or_idx]),
            leading_spaces(lines[if_idx]).saturating_add(4),
            "test4 auto-format split IF condition continuation should stay one frame deeper than the IF header, got:\n{formatted}"
        );
        assert_eq!(
            leading_spaces(lines[date_add_idx]),
            leading_spaces(lines[while_idx]).saturating_add(4),
            "test4 auto-format WHILE body line with DATE_ADD should stay on the loop body frame depth, got:\n{formatted}"
        );
        assert_eq!(
            leading_spaces(lines[handler_begin_idx]),
            leading_spaces(lines[handler_idx]).saturating_add(4),
            "test4 auto-format handler BEGIN should open one frame deeper than the DECLARE HANDLER header, got:\n{formatted}"
        );
        assert_eq!(
            leading_spaces(lines[rollback_idx]),
            leading_spaces(lines[handler_idx]).saturating_add(8),
            "test4 auto-format handler body statements should stay one frame deeper than the handler BEGIN line, got:\n{formatted}"
        );
    }

    #[test]
    fn format_sql_basic_for_mysql_db_type_keeps_test4_view_join_and_split_if_or_on_frame_depth() {
        let source = include_str!("../../../test_mariadb/test4.txt");
        let formatted = SqlEditorWidget::format_sql_basic_for_db_type(
            source,
            crate::db::connection::DatabaseType::MySQL,
        );
        let lines: Vec<&str> = formatted.lines().collect();

        let from_idx =
            find_line_starting_with(&lines, "FROM employees e").expect("test4 view FROM");
        let join_idx = lines
            .iter()
            .enumerate()
            .skip(from_idx + 1)
            .find(|(_, line)| line.trim_start() == "JOIN departments d")
            .map(|(idx, _)| idx)
            .expect("test4 view JOIN");
        let left_join_idx = lines
            .iter()
            .enumerate()
            .skip(join_idx + 1)
            .find(|(_, line)| line.trim_start() == "LEFT JOIN task_log t")
            .map(|(idx, _)| idx)
            .expect("test4 view LEFT JOIN");
        let group_by_idx = lines
            .iter()
            .enumerate()
            .skip(left_join_idx + 1)
            .find(|(_, line)| line.trim_start() == "GROUP BY e.employee_id,")
            .map(|(idx, _)| idx)
            .expect("test4 view GROUP BY");
        let group_by_item_idx = lines
            .iter()
            .enumerate()
            .skip(group_by_idx + 1)
            .find(|(_, line)| line.trim_start() == "e.emp_code,")
            .map(|(idx, _)| idx)
            .expect("test4 view GROUP BY sibling");

        let if_payload_idx = find_line_starting_with(&lines, "IF NEW.payload IS NULL")
            .expect("test4 IF NEW.payload");
        let or_payload_idx = lines
            .iter()
            .enumerate()
            .skip(if_payload_idx + 1)
            .find(|(_, line)| line.trim_start() == "OR JSON_VALID(NEW.payload) = 0 THEN")
            .map(|(idx, _)| idx)
            .expect("test4 OR payload");
        let if_note_idx =
            find_line_starting_with(&lines, "IF NEW.note IS NULL").expect("test4 IF NEW.note");
        let or_note_idx = lines
            .iter()
            .enumerate()
            .skip(if_note_idx + 1)
            .find(|(_, line)| line.trim_start() == "OR CHAR_LENGTH(TRIM(NEW.note)) = 0 THEN")
            .map(|(idx, _)| idx)
            .expect("test4 OR note");

        assert_eq!(
            leading_spaces(lines[join_idx]),
            leading_spaces(lines[from_idx]),
            "test4 CREATE VIEW JOIN should stay on the FROM frame depth, got:\n{formatted}"
        );
        assert_eq!(
            leading_spaces(lines[left_join_idx]),
            leading_spaces(lines[from_idx]),
            "test4 CREATE VIEW LEFT JOIN should stay on the FROM frame depth, got:\n{formatted}"
        );
        assert_eq!(
            leading_spaces(lines[group_by_item_idx]),
            leading_spaces(lines[group_by_idx]).saturating_add(4),
            "test4 GROUP BY sibling columns should stay one frame deeper than GROUP BY, got:\n{formatted}"
        );
        assert_eq!(
            leading_spaces(lines[or_payload_idx]),
            leading_spaces(lines[if_payload_idx]).saturating_add(4),
            "test4 split IF payload OR line should stay on IF condition continuation frame depth, got:\n{formatted}"
        );
        assert_eq!(
            leading_spaces(lines[or_note_idx]),
            leading_spaces(lines[if_note_idx]).saturating_add(4),
            "test4 split IF note OR line should stay on IF condition continuation frame depth, got:\n{formatted}"
        );
    }

    #[test]
    fn format_sql_basic_for_mysql_db_type_keeps_test5_frame_based_depths_for_create_table_and_handlers(
    ) {
        let source = include_str!("../../../test_mariadb/test5.txt");
        let formatted = SqlEditorWidget::format_sql_basic_for_db_type(
            source,
            crate::db::connection::DatabaseType::MySQL,
        );
        let lines: Vec<&str> = formatted.lines().collect();

        let create_idx =
            find_line_starting_with(&lines, "CREATE TABLE org_unit (").expect("test5 CREATE");
        let org_unit_id_idx = lines
            .iter()
            .enumerate()
            .skip(create_idx + 1)
            .find(|(_, line)| line.trim_start().starts_with("org_unit_id"))
            .map(|(idx, _)| idx)
            .expect("test5 org_unit_id");
        let create_close_idx = lines
            .iter()
            .enumerate()
            .skip(org_unit_id_idx + 1)
            .find(|(_, line)| line.trim_start() == ")")
            .map(|(idx, _)| idx)
            .expect("test5 org_unit close");
        let continue_handler_idx =
            find_line_starting_with(&lines, "DECLARE CONTINUE HANDLER FOR user_error")
                .expect("test5 CONTINUE HANDLER");
        let continue_handler_begin_idx = lines
            .iter()
            .enumerate()
            .skip(continue_handler_idx + 1)
            .find(|(_, line)| line.trim_start() == "BEGIN")
            .map(|(idx, _)| idx)
            .expect("test5 handler BEGIN");
        let diagnostics_idx = lines
            .iter()
            .enumerate()
            .skip(continue_handler_begin_idx + 1)
            .find(|(_, line)| {
                line.trim_start() == "GET DIAGNOSTICS CONDITION 1 v_sqlstate = RETURNED_SQLSTATE,"
            })
            .map(|(idx, _)| idx)
            .expect("test5 GET DIAGNOSTICS");
        let exit_handler_idx = lines
            .iter()
            .enumerate()
            .skip(continue_handler_begin_idx + 1)
            .find(|(_, line)| line.trim_start() == "DECLARE EXIT HANDLER FOR SQLEXCEPTION")
            .map(|(idx, _)| idx)
            .expect("test5 EXIT HANDLER");
        let exit_handler_begin_idx = lines
            .iter()
            .enumerate()
            .skip(exit_handler_idx + 1)
            .find(|(_, line)| line.trim_start() == "BEGIN")
            .map(|(idx, _)| idx)
            .expect("test5 exit handler BEGIN");
        let rollback_idx = lines
            .iter()
            .enumerate()
            .skip(exit_handler_begin_idx + 1)
            .find(|(_, line)| line.trim_start() == "ROLLBACK;")
            .map(|(idx, _)| idx)
            .expect("test5 exit handler ROLLBACK");

        assert_eq!(
            leading_spaces(lines[org_unit_id_idx]),
            leading_spaces(lines[create_idx]).saturating_add(4),
            "test5 CREATE TABLE body should stay one frame deeper than the table header, got:\n{formatted}"
        );
        assert_eq!(
            leading_spaces(lines[create_close_idx]),
            leading_spaces(lines[create_idx]),
            "test5 CREATE TABLE close should return to the table header depth, got:\n{formatted}"
        );
        assert_eq!(
            leading_spaces(lines[continue_handler_begin_idx]),
            leading_spaces(lines[continue_handler_idx]).saturating_add(4),
            "test5 CONTINUE HANDLER BEGIN should open one frame deeper than the handler header, got:\n{formatted}"
        );
        assert_eq!(
            leading_spaces(lines[diagnostics_idx]),
            leading_spaces(lines[continue_handler_idx]).saturating_add(8),
            "test5 GET DIAGNOSTICS inside the handler should stay on the nested handler body frame depth, got:\n{formatted}"
        );
        assert_eq!(
            leading_spaces(lines[exit_handler_begin_idx]),
            leading_spaces(lines[exit_handler_idx]).saturating_add(4),
            "test5 EXIT HANDLER BEGIN should open one frame deeper than the handler header, got:\n{formatted}"
        );
        assert_eq!(
            leading_spaces(lines[rollback_idx]),
            leading_spaces(lines[exit_handler_idx]).saturating_add(8),
            "test5 EXIT HANDLER body statements should stay on the nested handler frame depth, got:\n{formatted}"
        );
    }

    #[test]
    fn format_for_auto_formatting_mysql_keeps_test5_frame_based_depths_for_create_table_and_handlers(
    ) {
        let source = include_str!("../../../test_mariadb/test5.txt");
        let formatted = SqlEditorWidget::format_for_auto_formatting_with_db_type(
            source,
            false,
            Some(crate::db::connection::DatabaseType::MySQL),
        );
        let lines: Vec<&str> = formatted.lines().collect();

        let create_idx =
            find_line_starting_with(&lines, "CREATE TABLE org_unit (").expect("test5 CREATE");
        let org_unit_id_idx = lines
            .iter()
            .enumerate()
            .skip(create_idx + 1)
            .find(|(_, line)| line.trim_start().starts_with("org_unit_id"))
            .map(|(idx, _)| idx)
            .expect("test5 org_unit_id");
        let create_close_idx = lines
            .iter()
            .enumerate()
            .skip(org_unit_id_idx + 1)
            .find(|(_, line)| line.trim_start() == ")")
            .map(|(idx, _)| idx)
            .expect("test5 org_unit close");
        let continue_handler_idx =
            find_line_starting_with(&lines, "DECLARE CONTINUE HANDLER FOR user_error")
                .expect("test5 CONTINUE HANDLER");
        let continue_handler_begin_idx = lines
            .iter()
            .enumerate()
            .skip(continue_handler_idx + 1)
            .find(|(_, line)| line.trim_start() == "BEGIN")
            .map(|(idx, _)| idx)
            .expect("test5 handler BEGIN");
        let diagnostics_idx = lines
            .iter()
            .enumerate()
            .skip(continue_handler_begin_idx + 1)
            .find(|(_, line)| {
                line.trim_start() == "GET DIAGNOSTICS CONDITION 1 v_sqlstate = RETURNED_SQLSTATE,"
            })
            .map(|(idx, _)| idx)
            .expect("test5 GET DIAGNOSTICS");
        let exit_handler_idx = lines
            .iter()
            .enumerate()
            .skip(continue_handler_begin_idx + 1)
            .find(|(_, line)| line.trim_start() == "DECLARE EXIT HANDLER FOR SQLEXCEPTION")
            .map(|(idx, _)| idx)
            .expect("test5 EXIT HANDLER");
        let exit_handler_begin_idx = lines
            .iter()
            .enumerate()
            .skip(exit_handler_idx + 1)
            .find(|(_, line)| line.trim_start() == "BEGIN")
            .map(|(idx, _)| idx)
            .expect("test5 exit handler BEGIN");
        let rollback_idx = lines
            .iter()
            .enumerate()
            .skip(exit_handler_begin_idx + 1)
            .find(|(_, line)| line.trim_start() == "ROLLBACK;")
            .map(|(idx, _)| idx)
            .expect("test5 exit handler ROLLBACK");

        assert_eq!(
            leading_spaces(lines[org_unit_id_idx]),
            leading_spaces(lines[create_idx]).saturating_add(4),
            "test5 auto-format CREATE TABLE body should stay one frame deeper than the table header, got:\n{formatted}"
        );
        assert_eq!(
            leading_spaces(lines[create_close_idx]),
            leading_spaces(lines[create_idx]),
            "test5 auto-format CREATE TABLE close should return to the table header depth, got:\n{formatted}"
        );
        assert_eq!(
            leading_spaces(lines[continue_handler_begin_idx]),
            leading_spaces(lines[continue_handler_idx]).saturating_add(4),
            "test5 auto-format CONTINUE HANDLER BEGIN should open one frame deeper than the handler header, got:\n{formatted}"
        );
        assert_eq!(
            leading_spaces(lines[diagnostics_idx]),
            leading_spaces(lines[continue_handler_idx]).saturating_add(8),
            "test5 auto-format GET DIAGNOSTICS inside the handler should stay on the nested handler body frame depth, got:\n{formatted}"
        );
        assert_eq!(
            leading_spaces(lines[exit_handler_begin_idx]),
            leading_spaces(lines[exit_handler_idx]).saturating_add(4),
            "test5 auto-format EXIT HANDLER BEGIN should open one frame deeper than the handler header, got:\n{formatted}"
        );
        assert_eq!(
            leading_spaces(lines[rollback_idx]),
            leading_spaces(lines[exit_handler_idx]).saturating_add(8),
            "test5 auto-format EXIT HANDLER body statements should stay on the nested handler frame depth, got:\n{formatted}"
        );
    }

    #[test]
    fn format_sql_basic_for_mysql_db_type_keeps_test5_view_join_and_split_if_or_on_frame_depth() {
        let source = include_str!("../../../test_mariadb/test5.txt");
        let formatted = SqlEditorWidget::format_sql_basic_for_db_type(
            source,
            crate::db::connection::DatabaseType::MySQL,
        );
        let lines: Vec<&str> = formatted.lines().collect();

        let from_idx =
            find_line_starting_with(&lines, "FROM work_item wi").expect("test5 view FROM");
        let left_join_idx = lines
            .iter()
            .enumerate()
            .skip(from_idx + 1)
            .find(|(_, line)| line.trim_start() == "LEFT JOIN work_event we")
            .map(|(idx, _)| idx)
            .expect("test5 view LEFT JOIN");
        let group_by_idx = lines
            .iter()
            .enumerate()
            .skip(left_join_idx + 1)
            .find(|(_, line)| line.trim_start() == "GROUP BY wi.item_id,")
            .map(|(idx, _)| idx)
            .expect("test5 view GROUP BY");
        let group_by_item_idx = lines
            .iter()
            .enumerate()
            .skip(group_by_idx + 1)
            .find(|(_, line)| line.trim_start() == "wi.project_id,")
            .map(|(idx, _)| idx)
            .expect("test5 view GROUP BY sibling");

        let if_points_idx =
            find_line_starting_with(&lines, "IF NEW.points IS NULL").expect("test5 IF NEW.points");
        let or_points_idx = lines
            .iter()
            .enumerate()
            .skip(if_points_idx + 1)
            .find(|(_, line)| line.trim_start() == "OR NEW.points <= 0 THEN")
            .map(|(idx, _)| idx)
            .expect("test5 OR points");
        let if_detail_idx = find_line_starting_with(&lines, "IF NEW.detail_doc IS NULL")
            .expect("test5 IF NEW.detail_doc");
        let or_detail_idx = lines
            .iter()
            .enumerate()
            .skip(if_detail_idx + 1)
            .find(|(_, line)| line.trim_start() == "OR JSON_VALID(NEW.detail_doc) = 0 THEN")
            .map(|(idx, _)| idx)
            .expect("test5 OR detail_doc");

        assert_eq!(
            leading_spaces(lines[left_join_idx]),
            leading_spaces(lines[from_idx]),
            "test5 CREATE VIEW LEFT JOIN should stay on the FROM frame depth, got:\n{formatted}"
        );
        assert_eq!(
            leading_spaces(lines[group_by_item_idx]),
            leading_spaces(lines[group_by_idx]).saturating_add(4),
            "test5 GROUP BY sibling columns should stay one frame deeper than GROUP BY, got:\n{formatted}"
        );
        assert_eq!(
            leading_spaces(lines[or_points_idx]),
            leading_spaces(lines[if_points_idx]).saturating_add(4),
            "test5 split IF points OR line should stay on IF condition continuation frame depth, got:\n{formatted}"
        );
        assert_eq!(
            leading_spaces(lines[or_detail_idx]),
            leading_spaces(lines[if_detail_idx]).saturating_add(4),
            "test5 split IF detail_doc OR line should stay on IF condition continuation frame depth, got:\n{formatted}"
        );
    }

    #[test]
    fn format_sql_basic_for_mysql_db_type_keeps_test6_frame_based_depths_for_triggers_and_handlers()
    {
        let source = include_str!("../../../test_mariadb/test6.txt");
        let formatted = SqlEditorWidget::format_sql_basic_for_db_type(
            source,
            crate::db::connection::DatabaseType::MySQL,
        );
        let lines: Vec<&str> = formatted.lines().collect();

        let create_idx =
            find_line_starting_with(&lines, "CREATE TABLE boss_region (").expect("test6 CREATE");
        let region_id_idx = lines
            .iter()
            .enumerate()
            .skip(create_idx + 1)
            .find(|(_, line)| line.trim_start().starts_with("region_id"))
            .map(|(idx, _)| idx)
            .expect("test6 region_id");
        let create_close_idx = lines
            .iter()
            .enumerate()
            .skip(region_id_idx + 1)
            .find(|(_, line)| line.trim_start() == ")")
            .map(|(idx, _)| idx)
            .expect("test6 boss_region close");
        let trigger_idx = find_line_starting_with(&lines, "CREATE TRIGGER bi_boss_order_item")
            .expect("test6 bi trigger");
        let trigger_begin_idx = lines
            .iter()
            .enumerate()
            .skip(trigger_idx + 1)
            .find(|(_, line)| line.trim_start() == "BEGIN")
            .map(|(idx, _)| idx)
            .expect("test6 bi BEGIN");
        let declare_idx = lines
            .iter()
            .enumerate()
            .skip(trigger_begin_idx + 1)
            .find(|(_, line)| line.trim_start() == "DECLARE v_unit_price DECIMAL(18, 2);")
            .map(|(idx, _)| idx)
            .expect("test6 DECLARE v_unit_price");
        let au_trigger_idx = find_line_starting_with(&lines, "CREATE TRIGGER au_boss_order_item")
            .expect("test6 au trigger");
        let au_begin_idx = lines
            .iter()
            .enumerate()
            .skip(au_trigger_idx + 1)
            .find(|(_, line)| line.trim_start() == "BEGIN")
            .map(|(idx, _)| idx)
            .expect("test6 au BEGIN");
        let order_change_if_idx = lines
            .iter()
            .enumerate()
            .skip(au_begin_idx + 1)
            .find(|(_, line)| line.trim_start() == "IF OLD.order_id <> NEW.order_id THEN")
            .map(|(idx, _)| idx)
            .expect("test6 order change IF");
        let handler_idx = lines
            .iter()
            .enumerate()
            .skip(declare_idx + 1)
            .find(|(_, line)| line.trim_start() == "DECLARE EXIT HANDLER FOR SQLEXCEPTION")
            .map(|(idx, _)| idx)
            .expect("test6 EXIT HANDLER");
        let handler_begin_idx = lines
            .iter()
            .enumerate()
            .skip(handler_idx + 1)
            .find(|(_, line)| line.trim_start() == "BEGIN")
            .map(|(idx, _)| idx)
            .expect("test6 handler BEGIN");
        let rollback_idx = lines
            .iter()
            .enumerate()
            .skip(handler_begin_idx + 1)
            .find(|(_, line)| line.trim_start() == "ROLLBACK;")
            .map(|(idx, _)| idx)
            .expect("test6 handler ROLLBACK");

        assert_eq!(
            leading_spaces(lines[region_id_idx]),
            leading_spaces(lines[create_idx]).saturating_add(4),
            "test6 CREATE TABLE body should stay one frame deeper than the table header, got:\n{formatted}"
        );
        assert_eq!(
            leading_spaces(lines[create_close_idx]),
            leading_spaces(lines[create_idx]),
            "test6 CREATE TABLE close should return to the table header depth, got:\n{formatted}"
        );
        assert_eq!(
            leading_spaces(lines[declare_idx]),
            leading_spaces(lines[trigger_begin_idx]).saturating_add(4),
            "test6 trigger DECLARE line should stay on the BEGIN body frame depth, got:\n{formatted}"
        );
        assert_eq!(
            leading_spaces(lines[order_change_if_idx]),
            leading_spaces(lines[au_begin_idx]).saturating_add(4),
            "test6 trigger IF header should realign with the trigger BEGIN body frame depth, got:\n{formatted}"
        );
        assert_eq!(
            leading_spaces(lines[handler_begin_idx]),
            leading_spaces(lines[handler_idx]).saturating_add(4),
            "test6 EXIT HANDLER BEGIN should open one frame deeper than the handler header, got:\n{formatted}"
        );
        assert_eq!(
            leading_spaces(lines[rollback_idx]),
            leading_spaces(lines[handler_idx]).saturating_add(8),
            "test6 EXIT HANDLER body should stay on the nested handler frame depth, got:\n{formatted}"
        );
    }

    #[test]
    fn format_for_auto_formatting_mysql_keeps_test6_frame_based_depths_for_triggers_and_handlers() {
        let source = include_str!("../../../test_mariadb/test6.txt");
        let formatted = SqlEditorWidget::format_for_auto_formatting_with_db_type(
            source,
            false,
            Some(crate::db::connection::DatabaseType::MySQL),
        );
        let lines: Vec<&str> = formatted.lines().collect();

        let create_idx =
            find_line_starting_with(&lines, "CREATE TABLE boss_region (").expect("test6 CREATE");
        let region_id_idx = lines
            .iter()
            .enumerate()
            .skip(create_idx + 1)
            .find(|(_, line)| line.trim_start().starts_with("region_id"))
            .map(|(idx, _)| idx)
            .expect("test6 region_id");
        let create_close_idx = lines
            .iter()
            .enumerate()
            .skip(region_id_idx + 1)
            .find(|(_, line)| line.trim_start() == ")")
            .map(|(idx, _)| idx)
            .expect("test6 boss_region close");
        let trigger_idx = find_line_starting_with(&lines, "CREATE TRIGGER bi_boss_order_item")
            .expect("test6 bi trigger");
        let trigger_begin_idx = lines
            .iter()
            .enumerate()
            .skip(trigger_idx + 1)
            .find(|(_, line)| line.trim_start() == "BEGIN")
            .map(|(idx, _)| idx)
            .expect("test6 bi BEGIN");
        let declare_idx = lines
            .iter()
            .enumerate()
            .skip(trigger_begin_idx + 1)
            .find(|(_, line)| line.trim_start() == "DECLARE v_unit_price DECIMAL(18, 2);")
            .map(|(idx, _)| idx)
            .expect("test6 DECLARE v_unit_price");
        let au_trigger_idx = find_line_starting_with(&lines, "CREATE TRIGGER au_boss_order_item")
            .expect("test6 au trigger");
        let au_begin_idx = lines
            .iter()
            .enumerate()
            .skip(au_trigger_idx + 1)
            .find(|(_, line)| line.trim_start() == "BEGIN")
            .map(|(idx, _)| idx)
            .expect("test6 au BEGIN");
        let order_change_if_idx = lines
            .iter()
            .enumerate()
            .skip(au_begin_idx + 1)
            .find(|(_, line)| line.trim_start() == "IF OLD.order_id <> NEW.order_id THEN")
            .map(|(idx, _)| idx)
            .expect("test6 order change IF");
        let handler_idx = lines
            .iter()
            .enumerate()
            .skip(declare_idx + 1)
            .find(|(_, line)| line.trim_start() == "DECLARE EXIT HANDLER FOR SQLEXCEPTION")
            .map(|(idx, _)| idx)
            .expect("test6 EXIT HANDLER");
        let handler_begin_idx = lines
            .iter()
            .enumerate()
            .skip(handler_idx + 1)
            .find(|(_, line)| line.trim_start() == "BEGIN")
            .map(|(idx, _)| idx)
            .expect("test6 handler BEGIN");
        let rollback_idx = lines
            .iter()
            .enumerate()
            .skip(handler_begin_idx + 1)
            .find(|(_, line)| line.trim_start() == "ROLLBACK;")
            .map(|(idx, _)| idx)
            .expect("test6 handler ROLLBACK");

        assert_eq!(
            leading_spaces(lines[region_id_idx]),
            leading_spaces(lines[create_idx]).saturating_add(4),
            "test6 auto-format CREATE TABLE body should stay one frame deeper than the table header, got:\n{formatted}"
        );
        assert_eq!(
            leading_spaces(lines[create_close_idx]),
            leading_spaces(lines[create_idx]),
            "test6 auto-format CREATE TABLE close should return to the table header depth, got:\n{formatted}"
        );
        assert_eq!(
            leading_spaces(lines[declare_idx]),
            leading_spaces(lines[trigger_begin_idx]).saturating_add(4),
            "test6 auto-format trigger DECLARE line should stay on the BEGIN body frame depth, got:\n{formatted}"
        );
        assert_eq!(
            leading_spaces(lines[order_change_if_idx]),
            leading_spaces(lines[au_begin_idx]).saturating_add(4),
            "test6 auto-format trigger IF header should realign with the trigger BEGIN body frame depth, got:\n{formatted}"
        );
        assert_eq!(
            leading_spaces(lines[handler_begin_idx]),
            leading_spaces(lines[handler_idx]).saturating_add(4),
            "test6 auto-format EXIT HANDLER BEGIN should open one frame deeper than the handler header, got:\n{formatted}"
        );
        assert_eq!(
            leading_spaces(lines[rollback_idx]),
            leading_spaces(lines[handler_idx]).saturating_add(8),
            "test6 auto-format EXIT HANDLER body should stay on the nested handler frame depth, got:\n{formatted}"
        );
    }

    #[test]
    fn format_for_auto_formatting_mysql_keeps_split_procedure_if_body_one_level_below_then_line() {
        let source = r#"CREATE DEFINER = `root` @`localhost` PROCEDURE `sp_rebuild_monthly_stats`(IN p_from DATE, IN p_to DATE)
BEGIN
    DECLARE v_msg VARCHAR(255);
    IF p_from IS NULL
        OR p_to IS NULL
        OR p_from > p_to THEN
        SET v_msg = 'p_from / p_to is invalid';
        SIGNAL SQLSTATE '45000' SET MESSAGE_TEXT = v_msg;
    END IF;
END;"#;
        let formatted = SqlEditorWidget::format_for_auto_formatting_with_db_type(
            source,
            false,
            Some(crate::db::connection::DatabaseType::MySQL),
        );
        let lines: Vec<&str> = formatted.lines().collect();
        let then_idx = find_line_starting_with(&lines, "OR p_from > p_to THEN")
            .expect("split procedure IF THEN line");
        let set_idx = find_line_starting_with(&lines, "SET v_msg = 'p_from / p_to is invalid';")
            .expect("split procedure IF SET body");
        let signal_idx =
            find_line_starting_with(&lines, "SIGNAL SQLSTATE '45000' SET MESSAGE_TEXT = v_msg;")
                .expect("split procedure IF SIGNAL body");

        assert_eq!(
            leading_spaces(lines[set_idx]),
            leading_spaces(lines[then_idx]).saturating_add(4),
            "auto-format IF body should keep SET one frame deeper than the THEN line, got:\n{formatted}"
        );
        assert_eq!(
            leading_spaces(lines[signal_idx]),
            leading_spaces(lines[set_idx]),
            "auto-format IF body siblings should stay on the same THEN body depth, got:\n{formatted}"
        );
    }

    #[test]
    fn format_sql_basic_for_mysql_db_type_keeps_test6_split_if_or_condition_continuations_on_frame_depth(
    ) {
        let source = include_str!("../../../test_mariadb/test6.txt");
        let formatted = SqlEditorWidget::format_sql_basic_for_db_type(
            source,
            crate::db::connection::DatabaseType::MySQL,
        );
        let lines: Vec<&str> = formatted.lines().collect();

        let trigger_if_idx = find_line_starting_with(&lines, "IF NEW.quantity IS NULL")
            .expect("test6 trigger IF NEW.quantity");
        let trigger_or_idx = lines
            .iter()
            .enumerate()
            .skip(trigger_if_idx + 1)
            .find(|(_, line)| line.trim_start() == "OR NEW.quantity < 1 THEN")
            .map(|(idx, _)| idx)
            .unwrap_or_else(|| panic!("test6 trigger OR NEW.quantity, got:\n{formatted}"));

        let proc_if_idx =
            find_line_starting_with(&lines, "IF p_from IS NULL").expect("test6 proc IF p_from");
        let proc_or_to_idx = lines
            .iter()
            .enumerate()
            .skip(proc_if_idx + 1)
            .find(|(_, line)| line.trim_start() == "OR p_to IS NULL")
            .map(|(idx, _)| idx)
            .unwrap_or_else(|| panic!("test6 proc OR p_to, got:\n{formatted}"));
        let proc_or_order_idx = lines
            .iter()
            .enumerate()
            .skip(proc_or_to_idx + 1)
            .find(|(_, line)| line.trim_start() == "OR p_from > p_to THEN")
            .map(|(idx, _)| idx)
            .unwrap_or_else(|| panic!("test6 proc OR p_from > p_to, got:\n{formatted}"));

        assert_eq!(
            leading_spaces(lines[trigger_or_idx]),
            leading_spaces(lines[trigger_if_idx]).saturating_add(4),
            "test6 trigger split IF OR line should stay on IF condition continuation frame depth, got:\n{formatted}"
        );
        assert_eq!(
            leading_spaces(lines[proc_or_to_idx]),
            leading_spaces(lines[proc_if_idx]).saturating_add(4),
            "test6 procedure split IF OR p_to line should stay on IF condition continuation frame depth, got:\n{formatted}"
        );
        assert_eq!(
            leading_spaces(lines[proc_or_order_idx]),
            leading_spaces(lines[proc_if_idx]).saturating_add(4),
            "test6 procedure split IF OR p_from > p_to line should stay on IF condition continuation frame depth, got:\n{formatted}"
        );
    }

    #[test]
    fn format_sql_basic_for_mysql_db_type_keeps_split_procedure_if_set_and_signal_on_then_body_depth(
    ) {
        let source = r#"CREATE PROCEDURE sp_rebuild_monthly_stats(IN p_from DATE, IN p_to DATE)
BEGIN
    DECLARE v_msg VARCHAR(255);
    IF p_from IS NULL
        OR p_to IS NULL
        OR p_from > p_to THEN
        SET v_msg = 'p_from / p_to is invalid';
        SIGNAL SQLSTATE '45000' SET MESSAGE_TEXT = v_msg;
    END IF;
END"#;
        let formatted = SqlEditorWidget::format_sql_basic_for_db_type(
            source,
            crate::db::connection::DatabaseType::MySQL,
        );
        let lines: Vec<&str> = formatted.lines().collect();

        let then_idx = find_line_starting_with(&lines, "OR p_from > p_to THEN")
            .expect("split procedure IF THEN line");
        let set_idx = find_line_starting_with(&lines, "SET v_msg = 'p_from / p_to is invalid';")
            .expect("split procedure IF SET body");
        let signal_idx =
            find_line_starting_with(&lines, "SIGNAL SQLSTATE '45000' SET MESSAGE_TEXT = v_msg;")
                .expect("split procedure IF SIGNAL body");

        assert_eq!(
            leading_spaces(lines[set_idx]),
            leading_spaces(lines[then_idx]).saturating_add(4),
            "split procedure IF body should keep SET one frame deeper than the THEN line, got:\n{formatted}"
        );
        assert_eq!(
            leading_spaces(lines[signal_idx]),
            leading_spaces(lines[set_idx]),
            "split procedure IF SET and SIGNAL should stay on the same THEN body depth, got:\n{formatted}"
        );
    }

    #[test]
    fn format_sql_basic_for_mysql_db_type_keeps_split_trigger_if_signal_on_if_body_depth() {
        let source = r#"CREATE TRIGGER trg
BEFORE INSERT ON t
FOR EACH ROW
BEGIN
    IF NEW.hours IS NULL
        OR NEW.hours <= 0
        OR NEW.hours > 24 THEN
        SIGNAL SQLSTATE '45000' SET MESSAGE_TEXT = 'x';
    END IF;
END"#;
        let formatted = SqlEditorWidget::format_sql_basic_for_db_type(
            source,
            crate::db::connection::DatabaseType::MySQL,
        );
        let lines: Vec<&str> = formatted.lines().collect();

        let if_idx =
            find_line_starting_with(&lines, "IF NEW.hours IS NULL").expect("split IF header");
        let signal_idx =
            find_line_starting_with(&lines, "SIGNAL SQLSTATE '45000' SET MESSAGE_TEXT = 'x';")
                .expect("split IF signal");

        assert_eq!(
            leading_spaces(lines[signal_idx]),
            leading_spaces(lines[if_idx]).saturating_add(4),
            "split trigger IF body should keep SIGNAL one frame deeper than the IF owner, got:\n{formatted}"
        );
    }

    #[test]
    fn format_sql_basic_for_mysql_db_type_keeps_return_case_terminator_on_case_owner_depth() {
        let source = r#"CREATE FUNCTION f()
RETURNS INT
BEGIN
    RETURN CASE
        WHEN 1 = 1 THEN
            1
        ELSE
            0
    END;
END"#;
        let formatted = SqlEditorWidget::format_sql_basic_for_db_type(
            source,
            crate::db::connection::DatabaseType::MySQL,
        );
        let lines: Vec<&str> = formatted.lines().collect();

        let return_idx = lines
            .iter()
            .enumerate()
            .find(|(_, line)| line.trim_start() == "RETURN")
            .map(|(idx, _)| idx)
            .expect("RETURN line");
        let case_idx = lines
            .iter()
            .enumerate()
            .skip(return_idx + 1)
            .find(|(_, line)| line.trim_start() == "CASE")
            .map(|(idx, _)| idx)
            .expect("CASE line");
        let end_idx = lines
            .iter()
            .enumerate()
            .skip(case_idx + 1)
            .find(|(_, line)| line.trim_start() == "END;")
            .map(|(idx, _)| idx)
            .expect("CASE END line");

        assert_eq!(
            leading_spaces(lines[case_idx]),
            leading_spaces(lines[return_idx]),
            "RETURN CASE should keep CASE on the RETURN owner depth, got:\n{formatted}"
        );
        assert_eq!(
            leading_spaces(lines[end_idx]),
            leading_spaces(lines[return_idx]),
            "RETURN CASE terminator should return to the RETURN owner depth, got:\n{formatted}"
        );
    }

    #[test]
    fn format_sql_basic_for_mysql_db_type_keeps_statement_after_multiline_select_inside_if_on_if_body_depth(
    ) {
        let source = r#"BEGIN
    IF a IS NULL THEN
        SELECT col
        INTO v_col
        FROM t
        WHERE id = a;
        SET a = v_col;
    END IF;
END"#;
        let formatted = SqlEditorWidget::format_sql_basic_for_db_type(
            source,
            crate::db::connection::DatabaseType::MySQL,
        );
        let lines: Vec<&str> = formatted.lines().collect();

        let if_idx = find_line_starting_with(&lines, "IF a IS NULL THEN").expect("IF header");
        let set_idx = find_line_starting_with(&lines, "SET a = v_col;").expect("SET after SELECT");

        assert_eq!(
            leading_spaces(lines[set_idx]),
            leading_spaces(lines[if_idx]).saturating_add(4),
            "statement after multiline SELECT inside IF should stay on the IF body frame depth, got:\n{formatted}"
        );
    }

    #[test]
    fn format_sql_basic_for_mysql_db_type_realigns_statement_after_multiline_values_inside_if_branch(
    ) {
        let source = r#"BEGIN
    IF a IS NOT NULL THEN
        INSERT INTO t (payload)
        VALUES (
            JSON_OBJECT(
                'kind',
                'very-long-kind-value-that-keeps-the-call-expanded',
                'meta',
                JSON_OBJECT(
                    'nested',
                    'very-long-nested-value-that-keeps-the-call-expanded'
                )
            )
        );
        INSERT INTO t (payload)
        VALUES (
            JSON_OBJECT(
                'kind',
                'second-very-long-kind-value-that-keeps-the-call-expanded',
                'meta',
                JSON_OBJECT(
                    'nested',
                    'second-very-long-nested-value-that-keeps-the-call-expanded'
                )
            )
        );
    END IF;
END"#;
        let formatted = SqlEditorWidget::format_sql_basic_for_db_type(
            source,
            crate::db::connection::DatabaseType::MySQL,
        );
        let lines: Vec<&str> = formatted.lines().collect();

        let if_idx = find_line_starting_with(&lines, "IF a IS NOT NULL THEN").expect("IF header");
        let insert_indices: Vec<usize> = lines
            .iter()
            .enumerate()
            .filter(|(_, line)| line.trim_start() == "INSERT INTO t (payload)")
            .map(|(idx, _)| idx)
            .collect();
        let values_indices: Vec<usize> = lines
            .iter()
            .enumerate()
            .filter(|(_, line)| line.trim_start().starts_with("VALUES"))
            .map(|(idx, _)| idx)
            .collect();

        assert_eq!(
            insert_indices.len(),
            2,
            "expected two INSERT heads after multiline VALUES regression fixture, got:\n{formatted}"
        );
        assert_eq!(
            values_indices.len(),
            2,
            "expected two VALUES clause heads after multiline VALUES regression fixture, got:\n{formatted}"
        );

        let expected_body_indent = leading_spaces(lines[if_idx]).saturating_add(4);
        assert_eq!(
            leading_spaces(lines[insert_indices[0]]),
            expected_body_indent,
            "first INSERT head should stay on the IF body depth, got:\n{formatted}"
        );
        assert_eq!(
            leading_spaces(lines[insert_indices[1]]),
            expected_body_indent,
            "statement after multiline VALUES should return to the IF body depth, got:\n{formatted}"
        );
        assert_eq!(
            leading_spaces(lines[values_indices[0]]),
            expected_body_indent,
            "first VALUES head should stay on the IF body depth, got:\n{formatted}"
        );
        assert_eq!(
            leading_spaces(lines[values_indices[1]]),
            expected_body_indent,
            "second VALUES head should realign with the IF body depth after the previous VALUES block closes, got:\n{formatted}"
        );
    }

    #[test]
    fn format_sql_basic_for_mysql_db_type_keeps_test4_split_trigger_signal_and_return_case_aligned()
    {
        let source = include_str!("../../../test_mariadb/test4.txt");
        let formatted = SqlEditorWidget::format_sql_basic_for_db_type(
            source,
            crate::db::connection::DatabaseType::MySQL,
        );
        let lines: Vec<&str> = formatted.lines().collect();

        let if_idx =
            find_line_starting_with(&lines, "IF NEW.hours IS NULL").expect("test4 IF NEW.hours");
        let signal_idx = lines
            .iter()
            .enumerate()
            .skip(if_idx + 1)
            .find(|(_, line)| {
                line.trim_start()
                    == "SIGNAL SQLSTATE '45000' SET MESSAGE_TEXT = 'hours must be > 0 and <= 24';"
            })
            .map(|(idx, _)| idx)
            .expect("test4 SIGNAL");
        let fn_idx = find_line_starting_with(&lines, "CREATE FUNCTION fn_efficiency_band(")
            .expect("test4 function");
        let return_idx = lines
            .iter()
            .enumerate()
            .skip(fn_idx + 1)
            .find(|(_, line)| line.trim_start() == "RETURN")
            .map(|(idx, _)| idx)
            .expect("test4 RETURN");
        let case_idx = lines
            .iter()
            .enumerate()
            .skip(return_idx + 1)
            .find(|(_, line)| line.trim_start() == "CASE")
            .map(|(idx, _)| idx)
            .expect("test4 CASE");
        let end_idx = lines
            .iter()
            .enumerate()
            .skip(case_idx + 1)
            .find(|(_, line)| line.trim_start() == "END;")
            .map(|(idx, _)| idx)
            .expect("test4 CASE END");

        assert_eq!(
            leading_spaces(lines[signal_idx]),
            leading_spaces(lines[if_idx]).saturating_add(4),
            "test4 split trigger IF body should keep SIGNAL on the IF body frame depth, got:\n{formatted}"
        );
        assert_eq!(
            leading_spaces(lines[case_idx]),
            leading_spaces(lines[return_idx]),
            "test4 RETURN CASE should keep CASE on the RETURN owner depth, got:\n{formatted}"
        );
        assert_eq!(
            leading_spaces(lines[end_idx]),
            leading_spaces(lines[return_idx]),
            "test4 RETURN CASE terminator should return to the RETURN owner depth, got:\n{formatted}"
        );
    }

    #[test]
    fn format_sql_basic_for_mysql_db_type_keeps_test5_split_trigger_signal_and_return_case_aligned()
    {
        let source = include_str!("../../../test_mariadb/test5.txt");
        let formatted = SqlEditorWidget::format_sql_basic_for_db_type(
            source,
            crate::db::connection::DatabaseType::MySQL,
        );
        let lines: Vec<&str> = formatted.lines().collect();

        let if_idx =
            find_line_starting_with(&lines, "IF NEW.points IS NULL").expect("test5 IF NEW.points");
        let signal_idx = lines
            .iter()
            .enumerate()
            .skip(if_idx + 1)
            .find(|(_, line)| {
                line.trim_start()
                    == "SIGNAL SQLSTATE '45000' SET MESSAGE_TEXT = 'work_item.points must be > 0';"
            })
            .map(|(idx, _)| idx)
            .expect("test5 SIGNAL");
        let fn_idx = find_line_starting_with(&lines, "CREATE FUNCTION fn_complexity_band(")
            .expect("test5 function");
        let return_idx = lines
            .iter()
            .enumerate()
            .skip(fn_idx + 1)
            .find(|(_, line)| line.trim_start() == "RETURN")
            .map(|(idx, _)| idx)
            .expect("test5 RETURN");
        let case_idx = lines
            .iter()
            .enumerate()
            .skip(return_idx + 1)
            .find(|(_, line)| line.trim_start() == "CASE")
            .map(|(idx, _)| idx)
            .expect("test5 CASE");
        let end_idx = lines
            .iter()
            .enumerate()
            .skip(case_idx + 1)
            .find(|(_, line)| line.trim_start() == "END;")
            .map(|(idx, _)| idx)
            .expect("test5 CASE END");

        assert_eq!(
            leading_spaces(lines[signal_idx]),
            leading_spaces(lines[if_idx]).saturating_add(4),
            "test5 split trigger IF body should keep SIGNAL on the IF body frame depth, got:\n{formatted}"
        );
        assert_eq!(
            leading_spaces(lines[case_idx]),
            leading_spaces(lines[return_idx]),
            "test5 RETURN CASE should keep CASE on the RETURN owner depth, got:\n{formatted}"
        );
        assert_eq!(
            leading_spaces(lines[end_idx]),
            leading_spaces(lines[return_idx]),
            "test5 RETURN CASE terminator should return to the RETURN owner depth, got:\n{formatted}"
        );
    }

    #[test]
    fn format_sql_basic_for_mysql_db_type_keeps_test6_frame_based_mysql_regressions_aligned() {
        let source = include_str!("../../../test_mariadb/test6.txt");
        let formatted = SqlEditorWidget::format_sql_basic_for_db_type(
            source,
            crate::db::connection::DatabaseType::MySQL,
        );
        for snippet in [
            "CREATE FUNCTION fn_order_risk(p_order_id INT)",
            "IF NEW.unit_price IS NULL THEN",
            "WHILE v_order_id <= 240 DO",
            "IF v_status <> 'CANCELLED' THEN",
            "ELSEIF MOD(v_order_id, 4) = 1 THEN",
            "INSERT INTO boss_payment (order_id, payment_date, amount, method, payload_json)",
            "SET v_order_id = v_order_id + 1;",
            "UPDATE boss_customer c",
        ] {
            assert!(
                formatted.contains(snippet),
                "test6 frame regression fixture text should remain present, missing `{snippet}` in:\n{formatted}"
            );
        }
        assert!(
            formatted.matches("VALUES (").count() >= 4,
            "test6 payment branches should keep VALUES heads present, got:\n{formatted}"
        );
        assert!(
            formatted.matches("INSERT INTO boss_payment (order_id, payment_date, amount, method, payload_json)").count() >= 4,
            "test6 payment branches should keep INSERT heads present, got:\n{formatted}"
        );
        assert_eq!(
            SqlEditorWidget::format_sql_basic_for_db_type(
                &formatted,
                crate::db::connection::DatabaseType::MySQL,
            ),
            formatted
        );
    }

    #[test]
    fn format_sql_basic_for_mysql_db_type_keeps_test4_cte_sibling_headers_aligned_after_close() {
        let source = include_str!("../../../test_mariadb/test4.txt");
        let formatted = SqlEditorWidget::format_sql_basic_for_db_type(
            source,
            crate::db::connection::DatabaseType::MySQL,
        );
        let lines: Vec<&str> = formatted.lines().collect();

        let from_daily_idx =
            find_line_starting_with(&lines, "FROM daily d").expect("test4 FROM daily d");
        let cte_close_idx = lines
            .iter()
            .enumerate()
            .skip(from_daily_idx + 1)
            .find(|(_, line)| line.trim_start() == "),")
            .map(|(idx, _)| idx)
            .expect("test4 ranked CTE close");
        let member_stats_idx = lines
            .iter()
            .enumerate()
            .skip(cte_close_idx + 1)
            .find(|(_, line)| line.trim_start() == "member_stats AS (")
            .map(|(idx, _)| idx)
            .expect("test4 member_stats CTE");
        let owner_chain_idx = lines
            .iter()
            .enumerate()
            .skip(member_stats_idx + 1)
            .find(|(_, line)| line.trim_start() == "owner_chain AS (")
            .map(|(idx, _)| idx)
            .expect("test4 owner_chain CTE");

        assert_eq!(
            leading_spaces(lines[member_stats_idx]),
            leading_spaces(lines[cte_close_idx]),
            "test4 sibling CTE header after close-comma should keep the parent CTE header depth, got:\n{formatted}"
        );
        assert_eq!(
            leading_spaces(lines[owner_chain_idx]),
            leading_spaces(lines[member_stats_idx]),
            "test4 subsequent sibling CTE headers should stay on the same frame depth, got:\n{formatted}"
        );
    }

    #[test]
    fn format_sql_basic_for_mysql_db_type_keeps_test5_values_siblings_on_subquery_argument_depth() {
        let source = include_str!("../../../test_mariadb/test5.txt");
        let formatted = SqlEditorWidget::format_sql_basic_for_db_type(
            source,
            crate::db::connection::DatabaseType::MySQL,
        );
        let lines: Vec<&str> = formatted.lines().collect();

        let min_item_select_idx = find_line_starting_with(&lines, "SELECT MIN(item_id)")
            .expect("test5 SELECT MIN(item_id)");
        let scalar_idx = lines
            .iter()
            .enumerate()
            .skip(min_item_select_idx + 1)
            .find(|(_, line)| line.trim_start() == "6,")
            .map(|(idx, _)| idx)
            .expect("test5 scalar literal 6");
        let broken_event_idx = lines
            .iter()
            .enumerate()
            .skip(scalar_idx + 1)
            .find(|(_, line)| line.trim_start() == "'BROKEN_EVENT',")
            .map(|(idx, _)| idx)
            .expect("test5 BROKEN_EVENT literal");
        let broken_event_time_idx = lines
            .iter()
            .enumerate()
            .skip(broken_event_idx + 1)
            .find(|(_, line)| line.trim_start() == "'2025-02-10 10:00:00.000000',")
            .map(|(idx, _)| idx)
            .expect("test5 BROKEN_EVENT timestamp literal");

        assert_eq!(
            leading_spaces(lines[broken_event_idx]),
            leading_spaces(lines[scalar_idx]),
            "test5 VALUES siblings after a close-comma subquery argument must stay on the same parent frame depth, got:\n{formatted}"
        );
        assert_eq!(
            leading_spaces(lines[broken_event_time_idx]),
            leading_spaces(lines[scalar_idx]),
            "test5 subsequent VALUES siblings should keep the same argument frame depth, got:\n{formatted}"
        );
    }

    #[test]
    fn format_sql_basic_for_mysql_db_type_keeps_test5_call_literal_sibling_on_call_frame_depth() {
        let source = include_str!("../../../test_mariadb/test5.txt");
        let formatted = SqlEditorWidget::format_sql_basic_for_db_type(
            source,
            crate::db::connection::DatabaseType::MySQL,
        );
        let lines: Vec<&str> = formatted.lines().collect();

        let call_idx = find_line_starting_with(&lines, "CALL sp_assert_eq_decimal(")
            .expect("test5 CALL sp_assert_eq_decimal");
        let second_subquery_select_idx = lines
            .iter()
            .enumerate()
            .skip(call_idx + 1)
            .find(|(_, line)| line.trim_start() == "SELECT SUM(total_hours)")
            .map(|(idx, _)| idx)
            .expect("test5 SELECT SUM(total_hours)");
        let second_subquery_close_idx = lines
            .iter()
            .enumerate()
            .skip(second_subquery_select_idx + 1)
            .find(|(_, line)| line.trim_start() == "),")
            .map(|(idx, _)| idx)
            .expect("test5 second subquery close");
        let scalar_idx = lines
            .iter()
            .enumerate()
            .skip(second_subquery_close_idx + 1)
            .find(|(_, line)| line.trim_start() == "2,")
            .map(|(idx, _)| idx)
            .expect("test5 scalar precision arg");
        let message_idx = lines
            .iter()
            .enumerate()
            .skip(scalar_idx + 1)
            .find(|(_, line)| line.trim_start() == "'monthly total_hours sum mismatch'")
            .map(|(idx, _)| idx)
            .expect("test5 monthly total_hours message arg");

        assert_eq!(
            leading_spaces(lines[scalar_idx]),
            leading_spaces(lines[call_idx]).saturating_add(4),
            "test5 CALL scalar precision argument should stay one frame deeper than the CALL owner, got:\n{formatted}"
        );
        assert_eq!(
            leading_spaces(lines[message_idx]),
            leading_spaces(lines[scalar_idx]),
            "test5 CALL literal sibling after multiline subquery close-comma should snap back to the call argument frame depth, got:\n{formatted}"
        );
    }

    #[test]
    fn format_for_auto_formatting_mysql_keeps_test5_call_literal_sibling_on_call_frame_depth() {
        let source = include_str!("../../../test_mariadb/test5.txt");
        let formatted = SqlEditorWidget::format_for_auto_formatting_with_db_type(
            source,
            false,
            Some(crate::db::connection::DatabaseType::MySQL),
        );
        let lines: Vec<&str> = formatted.lines().collect();

        let call_idx = find_line_starting_with(&lines, "CALL sp_assert_eq_decimal(")
            .expect("test5 auto-format CALL sp_assert_eq_decimal");
        let second_subquery_select_idx = lines
            .iter()
            .enumerate()
            .skip(call_idx + 1)
            .find(|(_, line)| line.trim_start() == "SELECT SUM(total_hours)")
            .map(|(idx, _)| idx)
            .expect("test5 auto-format SELECT SUM(total_hours)");
        let second_subquery_close_idx = lines
            .iter()
            .enumerate()
            .skip(second_subquery_select_idx + 1)
            .find(|(_, line)| line.trim_start() == "),")
            .map(|(idx, _)| idx)
            .expect("test5 auto-format second subquery close");
        let scalar_idx = lines
            .iter()
            .enumerate()
            .skip(second_subquery_close_idx + 1)
            .find(|(_, line)| line.trim_start() == "2,")
            .map(|(idx, _)| idx)
            .expect("test5 auto-format scalar precision arg");
        let message_idx = lines
            .iter()
            .enumerate()
            .skip(scalar_idx + 1)
            .find(|(_, line)| line.trim_start() == "'monthly total_hours sum mismatch'")
            .map(|(idx, _)| idx)
            .expect("test5 auto-format monthly total_hours message arg");

        assert_eq!(
            leading_spaces(lines[message_idx]),
            leading_spaces(lines[scalar_idx]),
            "test5 auto-format CALL literal sibling after multiline subquery close-comma should snap back to the call argument frame depth, got:\n{formatted}"
        );
    }

    #[test]
    fn format_sql_basic_for_mysql_db_type_keeps_test6_json_scalar_subquery_chain_frame_depth() {
        let source = include_str!("../../../test_mariadb/test6.txt");
        let formatted = SqlEditorWidget::format_sql_basic_for_db_type(
            source,
            crate::db::connection::DatabaseType::MySQL,
        );
        let lines: Vec<&str> = formatted.lines().collect();

        let customers_idx =
            find_line_starting_with(&lines, "), 'customers', (").expect("test6 JSON customers key");
        let products_idx =
            find_line_starting_with(&lines, "), 'products', (").expect("test6 JSON products key");
        let orders_idx =
            find_line_starting_with(&lines, "), 'orders', (").expect("test6 JSON orders key");
        let items_idx =
            find_line_starting_with(&lines, "), 'items', (").expect("test6 JSON items key");
        let payments_idx =
            find_line_starting_with(&lines, "), 'payments', (").expect("test6 JSON payments key");

        let key_indent = leading_spaces(lines[customers_idx]);
        for idx in [products_idx, orders_idx, items_idx, payments_idx] {
            assert_eq!(
                leading_spaces(lines[idx]),
                key_indent,
                "test6 mixed close-open JSON key lines should not accumulate depth across siblings, got:\n{formatted}"
            );
        }

        let chain_end_idx = lines
            .iter()
            .enumerate()
            .skip(payments_idx + 1)
            .find(|(_, line)| line.trim_start() == "));")
            .map(|(idx, _)| idx)
            .expect("test6 JSON_OBJECT close");
        let select_indices: Vec<usize> = lines
            .iter()
            .enumerate()
            .skip(customers_idx + 1)
            .take(chain_end_idx.saturating_sub(customers_idx + 1))
            .filter(|(_, line)| line.trim_start() == "SELECT COUNT(*)")
            .map(|(idx, _)| idx)
            .collect();
        let from_indices: Vec<usize> = lines
            .iter()
            .enumerate()
            .skip(customers_idx + 1)
            .take(chain_end_idx.saturating_sub(customers_idx + 1))
            .filter(|(_, line)| line.trim_start().starts_with("FROM boss_"))
            .map(|(idx, _)| idx)
            .collect();

        assert_eq!(
            select_indices.len(),
            5,
            "test6 JSON scalar subquery chain should keep five repeated SELECT COUNT(*) siblings after regions/customers/products/orders/items keys, got:\n{formatted}"
        );
        assert_eq!(
            from_indices.len(),
            5,
            "test6 JSON scalar subquery chain should keep five repeated FROM siblings in the same region, got:\n{formatted}"
        );

        let select_indent = leading_spaces(lines[select_indices[0]]);
        for idx in select_indices.iter().skip(1) {
            assert_eq!(
                leading_spaces(lines[*idx]),
                select_indent,
                "test6 repeated SELECT COUNT(*) siblings must keep a stable frame depth, got:\n{formatted}"
            );
        }
        for (select_idx, from_idx) in select_indices.iter().zip(from_indices.iter()) {
            assert_eq!(
                leading_spaces(lines[*from_idx]),
                leading_spaces(lines[*select_idx]),
                "test6 scalar subquery FROM clause should stay aligned with its SELECT head inside JSON_OBJECT chain, got:\n{formatted}"
            );
        }
        assert_eq!(
            select_indent,
            key_indent.saturating_add(4),
            "test6 scalar subquery SELECT heads should stay exactly one frame deeper than sibling JSON key lines, got:\n{formatted}"
        );

        let order_count_idx =
            find_line_starting_with(&lines, "COUNT(DISTINCT o.order_id) AS order_count,")
                .expect("test6 customer_spend order_count");
        let total_spent_idx = lines
            .iter()
            .enumerate()
            .skip(order_count_idx + 1)
            .find(|(_, line)| line.trim_start() == "), 2) AS total_spent,")
            .map(|(idx, _)| idx)
            .expect("test6 customer_spend total_spent close-comma");
        let max_order_date_idx = lines
            .iter()
            .enumerate()
            .skip(total_spent_idx + 1)
            .find(|(_, line)| line.trim_start() == "MAX(o.order_date) AS last_order_date")
            .map(|(idx, _)| idx)
            .expect("test6 customer_spend MAX(o.order_date)");
        assert_eq!(
            leading_spaces(lines[max_order_date_idx]),
            leading_spaces(lines[order_count_idx]),
            "test6 SELECT-list sibling after mixed close-comma should return to the canonical SELECT-list frame depth, got:\n{formatted}"
        );
    }

    #[test]
    fn format_sql_basic_for_mysql_db_type_keeps_test6_update_join_subquery_frame_depth() {
        let source = include_str!("../../../test_mariadb/test6.txt");
        let formatted = SqlEditorWidget::format_sql_basic_for_db_type(
            source,
            crate::db::connection::DatabaseType::MySQL,
        );
        let lines: Vec<&str> = formatted.lines().collect();

        let update_idx =
            find_line_starting_with(&lines, "UPDATE boss_customer c").expect("test6 UPDATE head");
        let join_idx = lines
            .iter()
            .enumerate()
            .skip(update_idx + 1)
            .find(|(_, line)| line.trim_start() == "JOIN (")
            .map(|(idx, _)| idx)
            .expect("test6 UPDATE JOIN open");
        let select_idx = lines
            .iter()
            .enumerate()
            .skip(join_idx + 1)
            .find(|(_, line)| line.trim_start().starts_with("SELECT"))
            .map(|(idx, _)| idx)
            .expect("test6 JOIN subquery SELECT");
        let count_idx = lines
            .iter()
            .enumerate()
            .skip(select_idx + 1)
            .find(|(_, line)| line.trim_start() == "COUNT(*) AS lifetime_orders,")
            .map(|(idx, _)| idx)
            .expect("test6 JOIN subquery lifetime_orders");
        let round_idx = lines
            .iter()
            .enumerate()
            .skip(count_idx + 1)
            .find(|(_, line)| {
                line.trim_start() == "ROUND(SUM(o.grand_total), 2) AS lifetime_net_sales,"
            })
            .map(|(idx, _)| idx)
            .expect("test6 JOIN subquery lifetime_net_sales");
        let max_idx = lines
            .iter()
            .enumerate()
            .skip(round_idx + 1)
            .find(|(_, line)| line.trim_start() == "MAX(o.order_date) AS last_order_date")
            .map(|(idx, _)| idx)
            .expect("test6 JOIN subquery last_order_date");
        let from_idx = lines
            .iter()
            .enumerate()
            .skip(max_idx + 1)
            .find(|(_, line)| line.trim_start() == "FROM boss_order o")
            .map(|(idx, _)| idx)
            .expect("test6 JOIN subquery FROM");
        let close_idx = lines
            .iter()
            .enumerate()
            .skip(from_idx + 1)
            .find(|(_, line)| line.trim_start() == ") s")
            .map(|(idx, _)| idx)
            .expect("test6 JOIN subquery close");

        let join_indent = leading_spaces(lines[join_idx]);
        assert_eq!(
            leading_spaces(lines[select_idx]),
            join_indent.saturating_add(4),
            "test6 UPDATE JOIN subquery SELECT should be one frame deeper than JOIN, got:\n{formatted}"
        );
        assert_eq!(
            leading_spaces(lines[count_idx]),
            leading_spaces(lines[select_idx]).saturating_add(4),
            "test6 JOIN subquery first sibling should be one frame deeper than SELECT head, got:\n{formatted}"
        );
        assert_eq!(
            leading_spaces(lines[round_idx]),
            leading_spaces(lines[count_idx]),
            "test6 JOIN subquery SELECT siblings should keep stable list depth, got:\n{formatted}"
        );
        assert_eq!(
            leading_spaces(lines[max_idx]),
            leading_spaces(lines[count_idx]),
            "test6 JOIN subquery SELECT siblings should stay aligned at list depth, got:\n{formatted}"
        );
        assert_eq!(
            leading_spaces(lines[from_idx]),
            leading_spaces(lines[select_idx]),
            "test6 JOIN subquery FROM should align with SELECT head, got:\n{formatted}"
        );
        assert_eq!(
            leading_spaces(lines[close_idx]),
            join_indent,
            "test6 UPDATE JOIN subquery close should return to JOIN depth, got:\n{formatted}"
        );
    }

    #[test]
    fn format_sql_basic_for_oracle_db_type_realigns_select_sibling_after_close_comma() {
        let source = r#"SELECT c.customer_id,
    c.customer_name,
    c.region_id,
    COUNT (DISTINCT o.order_id) AS order_count,
    ROUND (SUM (
            CASE
                WHEN o.status <> 'CANCELLED' THEN o.grand_total
                ELSE 0
            END
        ), 2) AS total_spent,
        MAX (o.order_date) AS last_order_date
FROM boss_customer c
LEFT JOIN boss_order o
    ON o.customer_id = c.customer_id
GROUP BY c.customer_id,
    c.customer_name,
    c.region_id;"#;

        let formatted = SqlEditorWidget::format_sql_basic_for_db_type(
            source,
            crate::db::connection::DatabaseType::Oracle,
        );
        let lines: Vec<&str> = formatted.lines().collect();

        let order_count_idx =
            find_line_starting_with(&lines, "COUNT (DISTINCT o.order_id) AS order_count,")
                .expect("oracle order_count");
        let max_idx = find_line_starting_with(&lines, "MAX (o.order_date) AS last_order_date")
            .expect("oracle last_order_date");

        assert_eq!(
            leading_spaces(lines[max_idx]),
            leading_spaces(lines[order_count_idx]),
            "oracle SELECT-list sibling after close-comma should return to canonical list depth, got:\n{formatted}"
        );
        assert_eq!(
            SqlEditorWidget::format_sql_basic_for_db_type(
                &formatted,
                crate::db::connection::DatabaseType::Oracle,
            ),
            formatted
        );
    }

    #[test]
    fn format_sql_basic_for_mysql_db_type_realigns_select_sibling_after_close_comma() {
        let source = r#"SELECT c.customer_id,
    c.customer_name,
    c.region_id,
    COUNT(DISTINCT o.order_id) AS order_count,
    ROUND(SUM(
            CASE
                WHEN o.status <> 'CANCELLED' THEN o.grand_total
                ELSE 0
            END
        ), 2) AS total_spent,
        MAX(o.order_date) AS last_order_date
FROM boss_customer c
LEFT JOIN boss_order o
    ON o.customer_id = c.customer_id
GROUP BY c.customer_id,
    c.customer_name,
    c.region_id;"#;

        let formatted = SqlEditorWidget::format_sql_basic_for_db_type(
            source,
            crate::db::connection::DatabaseType::MySQL,
        );
        let lines: Vec<&str> = formatted.lines().collect();

        let order_count_idx = lines
            .iter()
            .enumerate()
            .find(|(_, line)| line.trim_start().contains("AS order_count,"))
            .map(|(idx, _)| idx)
            .expect("mysql order_count");
        let max_idx = lines
            .iter()
            .enumerate()
            .find(|(_, line)| line.trim_start().contains("AS last_order_date"))
            .map(|(idx, _)| idx)
            .expect("mysql last_order_date");

        assert_eq!(
            leading_spaces(lines[max_idx]),
            leading_spaces(lines[order_count_idx]),
            "mysql SELECT-list sibling after close-comma should return to canonical list depth, got:\n{formatted}"
        );
        assert_eq!(
            SqlEditorWidget::format_sql_basic_for_db_type(
                &formatted,
                crate::db::connection::DatabaseType::MySQL,
            ),
            formatted
        );
    }

    #[test]
    fn format_sql_basic_for_mysql_db_type_keeps_test8_view_case_on_select_item_depth() {
        let source = include_str!("../../../test_mariadb/test8.txt");
        let formatted = SqlEditorWidget::format_sql_basic_for_db_type(
            source,
            crate::db::connection::DatabaseType::MySQL,
        );
        let lines: Vec<&str> = formatted.lines().collect();

        let select_idx =
            find_line_starting_with(&lines, "SELECT u.user_id,").expect("test8 view SELECT");
        let case_idx = lines
            .iter()
            .enumerate()
            .skip(select_idx + 1)
            .find(|(_, line)| line.trim_start() == "CASE")
            .map(|(idx, _)| idx)
            .expect("test8 view CASE");
        let when_idx = lines
            .iter()
            .enumerate()
            .skip(case_idx + 1)
            .find(|(_, line)| {
                line.trim_start() == "WHEN COALESCE(x.total_net_amount, 0) >= 1500 THEN 'TOP'"
            })
            .map(|(idx, _)| idx)
            .expect("test8 view first WHEN");
        let end_idx = lines
            .iter()
            .enumerate()
            .skip(when_idx + 1)
            .find(|(_, line)| line.trim_start() == "END AS spend_band")
            .map(|(idx, _)| idx)
            .expect("test8 view CASE END");
        let from_idx = find_line_starting_with(&lines, "FROM cfb_user u").expect("test8 view FROM");

        assert_eq!(
            leading_spaces(lines[case_idx]),
            leading_spaces(lines[select_idx]).saturating_add(4),
            "test8 CREATE VIEW CASE select item should stay one level deeper than the query-body SELECT header, got:\n{formatted}"
        );
        assert_eq!(
            leading_spaces(lines[when_idx]),
            leading_spaces(lines[case_idx]).saturating_add(4),
            "test8 CREATE VIEW CASE WHEN should stay one level deeper than CASE, got:\n{formatted}"
        );
        assert_eq!(
            leading_spaces(lines[end_idx]),
            leading_spaces(lines[case_idx]),
            "test8 CREATE VIEW CASE END should realign with CASE, got:\n{formatted}"
        );
        assert_eq!(
            leading_spaces(lines[from_idx]),
            leading_spaces(lines[select_idx]),
            "test8 CREATE VIEW FROM should return to the query-body SELECT depth after CASE, got:\n{formatted}"
        );
    }

    #[test]
    fn format_for_auto_formatting_mysql_keeps_test8_view_case_on_select_item_depth() {
        let source = include_str!("../../../test_mariadb/test8.txt");
        let formatted = SqlEditorWidget::format_for_auto_formatting_with_db_type(
            source,
            false,
            Some(crate::db::connection::DatabaseType::MySQL),
        );
        let lines: Vec<&str> = formatted.lines().collect();

        let select_idx = find_line_starting_with(&lines, "SELECT u.user_id,")
            .expect("test8 auto-format view SELECT");
        let case_idx = lines
            .iter()
            .enumerate()
            .skip(select_idx + 1)
            .find(|(_, line)| line.trim_start() == "CASE")
            .map(|(idx, _)| idx)
            .expect("test8 auto-format view CASE");
        let when_idx = lines
            .iter()
            .enumerate()
            .skip(case_idx + 1)
            .find(|(_, line)| {
                line.trim_start() == "WHEN COALESCE(x.total_net_amount, 0) >= 1500 THEN 'TOP'"
            })
            .map(|(idx, _)| idx)
            .expect("test8 auto-format view first WHEN");
        let end_idx = lines
            .iter()
            .enumerate()
            .skip(when_idx + 1)
            .find(|(_, line)| line.trim_start() == "END AS spend_band")
            .map(|(idx, _)| idx)
            .expect("test8 auto-format view CASE END");
        let from_idx = find_line_starting_with(&lines, "FROM cfb_user u")
            .expect("test8 auto-format view FROM");

        assert_eq!(
            leading_spaces(lines[case_idx]),
            leading_spaces(lines[select_idx]).saturating_add(4),
            "test8 auto-format CREATE VIEW CASE select item should stay one level deeper than the query-body SELECT header, got:\n{formatted}"
        );
        assert_eq!(
            leading_spaces(lines[when_idx]),
            leading_spaces(lines[case_idx]).saturating_add(4),
            "test8 auto-format CREATE VIEW CASE WHEN should stay one level deeper than CASE, got:\n{formatted}"
        );
        assert_eq!(
            leading_spaces(lines[end_idx]),
            leading_spaces(lines[case_idx]),
            "test8 auto-format CREATE VIEW CASE END should realign with CASE, got:\n{formatted}"
        );
        assert_eq!(
            leading_spaces(lines[from_idx]),
            leading_spaces(lines[select_idx]),
            "test8 auto-format CREATE VIEW FROM should return to the query-body SELECT depth after CASE, got:\n{formatted}"
        );
        assert_eq!(
            SqlEditorWidget::format_for_auto_formatting_with_db_type(
                &formatted,
                false,
                Some(crate::db::connection::DatabaseType::MySQL),
            ),
            formatted,
            "test8 auto-format CREATE VIEW CASE result should stay stable after reformatting"
        );
    }

    #[test]
    fn format_sql_basic_for_oracle_db_type_realigns_sibling_after_close_alias_comma_without_as() {
        let source = r#"SELECT
    (
        SELECT MAX(emp.sal)
        FROM emp
        WHERE emp.deptno = d.deptno
    ) nested_max_sal,
    d.deptno
FROM dept d;"#;

        let formatted = SqlEditorWidget::format_sql_basic_for_db_type(
            source,
            crate::db::connection::DatabaseType::Oracle,
        );
        let lines: Vec<&str> = formatted.lines().collect();

        let close_alias_idx = lines
            .iter()
            .enumerate()
            .find(|(_, line)| line.trim_start().ends_with("nested_max_sal,"))
            .map(|(idx, _)| idx)
            .expect("oracle close alias comma");
        let sibling_idx =
            find_line_starting_with(&lines, "d.deptno").expect("oracle sibling select item");

        assert_eq!(
            leading_spaces(lines[sibling_idx]),
            leading_spaces(lines[close_alias_idx]),
            "oracle sibling after `) alias,` should stay on the same SELECT-list frame depth, got:\n{formatted}"
        );
        assert_eq!(
            SqlEditorWidget::format_sql_basic_for_db_type(
                &formatted,
                crate::db::connection::DatabaseType::Oracle,
            ),
            formatted
        );
    }

    #[test]
    fn format_sql_basic_for_oracle_db_type_realigns_sibling_after_close_quoted_alias_comma_without_as(
    ) {
        let source = r#"SELECT
    (
        SELECT MAX(emp.sal)
        FROM emp
        WHERE emp.deptno = d.deptno
    ) "nested_max_sal",
    d.deptno
FROM dept d;"#;

        let formatted = SqlEditorWidget::format_sql_basic_for_db_type(
            source,
            crate::db::connection::DatabaseType::Oracle,
        );
        let lines: Vec<&str> = formatted.lines().collect();

        let close_alias_idx = lines
            .iter()
            .enumerate()
            .find(|(_, line)| line.trim_start().ends_with("\"nested_max_sal\","))
            .map(|(idx, _)| idx)
            .expect("oracle close quoted alias comma");
        let sibling_idx =
            find_line_starting_with(&lines, "d.deptno").expect("oracle sibling select item");

        assert_eq!(
            leading_spaces(lines[sibling_idx]),
            leading_spaces(lines[close_alias_idx]),
            "oracle sibling after `) \"alias\",` should stay on the same SELECT-list frame depth, got:\n{formatted}"
        );
        assert_eq!(
            SqlEditorWidget::format_sql_basic_for_db_type(
                &formatted,
                crate::db::connection::DatabaseType::Oracle,
            ),
            formatted
        );
    }

    #[test]
    fn format_sql_basic_for_oracle_db_type_realigns_sibling_after_close_alias_comma_with_inline_comment(
    ) {
        let source = r#"SELECT
    (
        SELECT MAX(emp.sal)
        FROM emp
        WHERE emp.deptno = d.deptno
    ) nested_max_sal, -- close alias comment
    d.deptno
FROM dept d;"#;

        let formatted = SqlEditorWidget::format_sql_basic_for_db_type(
            source,
            crate::db::connection::DatabaseType::Oracle,
        );
        let lines: Vec<&str> = formatted.lines().collect();

        let close_alias_idx = lines
            .iter()
            .enumerate()
            .find(|(_, line)| {
                line.trim_start()
                    .starts_with(") nested_max_sal, -- close alias comment")
            })
            .map(|(idx, _)| idx)
            .expect("oracle close alias comma with inline comment");
        let sibling_idx =
            find_line_starting_with(&lines, "d.deptno").expect("oracle sibling select item");

        assert_eq!(
            leading_spaces(lines[sibling_idx]),
            leading_spaces(lines[close_alias_idx]),
            "oracle sibling after `) alias, -- comment` should stay on the same SELECT-list frame depth, got:\n{formatted}"
        );
        assert_eq!(
            SqlEditorWidget::format_sql_basic_for_db_type(
                &formatted,
                crate::db::connection::DatabaseType::Oracle,
            ),
            formatted
        );
    }

    #[test]
    fn format_sql_basic_for_oracle_db_type_realigns_sibling_after_close_quoted_alias_comma_with_inline_comment(
    ) {
        let source = r#"SELECT
    (
        SELECT MAX(emp.sal)
        FROM emp
        WHERE emp.deptno = d.deptno
    ) "nested_max_sal", -- close alias comment
    d.deptno
FROM dept d;"#;

        let formatted = SqlEditorWidget::format_sql_basic_for_db_type(
            source,
            crate::db::connection::DatabaseType::Oracle,
        );
        let lines: Vec<&str> = formatted.lines().collect();

        let close_alias_idx = lines
            .iter()
            .enumerate()
            .find(|(_, line)| {
                line.trim_start()
                    .starts_with(") \"nested_max_sal\", -- close alias comment")
            })
            .map(|(idx, _)| idx)
            .expect("oracle close quoted alias comma with inline comment");
        let sibling_idx =
            find_line_starting_with(&lines, "d.deptno").expect("oracle sibling select item");

        assert_eq!(
            leading_spaces(lines[sibling_idx]),
            leading_spaces(lines[close_alias_idx]),
            "oracle sibling after `) \"alias\", -- comment` should stay on the same SELECT-list frame depth, got:\n{formatted}"
        );
        assert_eq!(
            SqlEditorWidget::format_sql_basic_for_db_type(
                &formatted,
                crate::db::connection::DatabaseType::Oracle,
            ),
            formatted
        );
    }

    #[test]
    fn format_sql_basic_for_oracle_db_type_realigns_sibling_after_close_as_alias_comma_with_inline_comment(
    ) {
        let source = r#"SELECT
    (
        SELECT MAX(emp.sal)
        FROM emp
        WHERE emp.deptno = d.deptno
    ) AS nested_max_sal, -- close alias comment
    d.deptno
FROM dept d;"#;

        let formatted = SqlEditorWidget::format_sql_basic_for_db_type(
            source,
            crate::db::connection::DatabaseType::Oracle,
        );
        let lines: Vec<&str> = formatted.lines().collect();

        let close_alias_idx = lines
            .iter()
            .enumerate()
            .find(|(_, line)| {
                line.trim_start()
                    .starts_with(") AS nested_max_sal, -- close alias comment")
            })
            .map(|(idx, _)| idx)
            .expect("oracle close AS alias comma with inline comment");
        let sibling_idx =
            find_line_starting_with(&lines, "d.deptno").expect("oracle sibling select item");

        assert_eq!(
            leading_spaces(lines[sibling_idx]),
            leading_spaces(lines[close_alias_idx]),
            "oracle sibling after `) AS alias, -- comment` should stay on the same SELECT-list frame depth, got:\n{formatted}"
        );
        assert_eq!(
            SqlEditorWidget::format_sql_basic_for_db_type(
                &formatted,
                crate::db::connection::DatabaseType::Oracle,
            ),
            formatted
        );
    }

    #[test]
    fn format_sql_basic_for_oracle_db_type_realigns_sibling_after_close_block_comment_as_alias_comma(
    ) {
        let source = r#"SELECT
    (
        SELECT MAX(emp.sal)
        FROM emp
        WHERE emp.deptno = d.deptno
    ) /* close alias comment */ AS nested_max_sal,
    d.deptno
FROM dept d;"#;

        let formatted = SqlEditorWidget::format_sql_basic_for_db_type(
            source,
            crate::db::connection::DatabaseType::Oracle,
        );
        let lines: Vec<&str> = formatted.lines().collect();

        let close_alias_idx = lines
            .iter()
            .enumerate()
            .find(|(_, line)| line.trim_start().ends_with("AS nested_max_sal,"))
            .map(|(idx, _)| idx)
            .expect("oracle close block-comment AS alias comma");
        let sibling_idx =
            find_line_starting_with(&lines, "d.deptno").expect("oracle sibling select item");

        assert!(
            formatted.contains("/* close alias comment */"),
            "oracle formatter should preserve the block comment, got:\n{formatted}"
        );
        assert_eq!(
            leading_spaces(lines[sibling_idx]),
            leading_spaces(lines[close_alias_idx]),
            "oracle sibling after `) /* ... */ AS alias,` should stay on the same SELECT-list frame depth, got:\n{formatted}"
        );
        assert_eq!(
            SqlEditorWidget::format_sql_basic_for_db_type(
                &formatted,
                crate::db::connection::DatabaseType::Oracle,
            ),
            formatted
        );
    }

    #[test]
    fn format_sql_basic_for_mysql_db_type_realigns_sibling_after_close_alias_comma_without_as() {
        let source = r#"SELECT
    (
        SELECT MAX(emp.sal)
        FROM emp
        WHERE emp.deptno = d.deptno
    ) nested_max_sal,
    d.deptno
FROM dept d;"#;

        let formatted = SqlEditorWidget::format_sql_basic_for_db_type(
            source,
            crate::db::connection::DatabaseType::MySQL,
        );
        let lines: Vec<&str> = formatted.lines().collect();

        let close_alias_idx = lines
            .iter()
            .enumerate()
            .find(|(_, line)| line.trim_start().ends_with("nested_max_sal,"))
            .map(|(idx, _)| idx)
            .expect("mysql close alias comma");
        let sibling_idx =
            find_line_starting_with(&lines, "d.deptno").expect("mysql sibling select item");

        assert_eq!(
            leading_spaces(lines[sibling_idx]),
            leading_spaces(lines[close_alias_idx]),
            "mysql sibling after `) alias,` should stay on the same SELECT-list frame depth, got:\n{formatted}"
        );
        assert_eq!(
            SqlEditorWidget::format_sql_basic_for_db_type(
                &formatted,
                crate::db::connection::DatabaseType::MySQL,
            ),
            formatted
        );
    }

    #[test]
    fn format_sql_basic_for_mysql_db_type_realigns_sibling_after_close_keyword_like_alias_comma_without_as(
    ) {
        let source = r#"SELECT
    (
        SELECT MAX(emp.sal)
        FROM emp
        WHERE emp.deptno = d.deptno
    ) window,
    d.deptno
FROM dept d;"#;

        let formatted = SqlEditorWidget::format_sql_basic_for_db_type(
            source,
            crate::db::connection::DatabaseType::MySQL,
        );
        let lines: Vec<&str> = formatted.lines().collect();

        let close_alias_idx = lines
            .iter()
            .enumerate()
            .find(|(_, line)| line.trim_start() == ") window,")
            .map(|(idx, _)| idx)
            .expect("mysql close keyword-like alias comma");
        let sibling_idx =
            find_line_starting_with(&lines, "d.deptno").expect("mysql sibling select item");

        assert!(
            formatted.contains(") window,"),
            "mysql formatter should preserve keyword-like alias casing on close-alias line, got:\n{formatted}"
        );
        assert!(
            !formatted.contains(") WINDOW,"),
            "mysql formatter should not uppercase keyword-like close alias into WINDOW clause spelling, got:\n{formatted}"
        );
        assert_eq!(
            leading_spaces(lines[sibling_idx]),
            leading_spaces(lines[close_alias_idx]),
            "mysql sibling after `) window,` should stay on the same SELECT-list frame depth, got:\n{formatted}"
        );
        assert_eq!(
            SqlEditorWidget::format_sql_basic_for_db_type(
                &formatted,
                crate::db::connection::DatabaseType::MySQL,
            ),
            formatted
        );
    }

    #[test]
    fn format_sql_basic_for_mysql_db_type_keeps_close_keyword_like_alias_without_comma_on_select_list_depth(
    ) {
        let source = r#"SELECT
    (
        SELECT MAX(emp.sal)
        FROM emp
        WHERE emp.deptno = d.deptno
    ) window
FROM dept d;"#;

        let formatted = SqlEditorWidget::format_sql_basic_for_db_type(
            source,
            crate::db::connection::DatabaseType::MySQL,
        );
        let lines: Vec<&str> = formatted.lines().collect();

        let close_alias_idx = lines
            .iter()
            .enumerate()
            .find(|(_, line)| line.trim_start() == ") window")
            .map(|(idx, _)| idx)
            .expect("mysql close keyword-like alias without comma");
        let from_idx =
            find_line_starting_with(&lines, "FROM dept d;").expect("mysql FROM after alias");

        assert!(
            formatted.contains(") window"),
            "mysql formatter should preserve keyword-like alias casing on close-alias line without comma, got:\n{formatted}"
        );
        assert!(
            !formatted.contains(") WINDOW"),
            "mysql formatter should not uppercase keyword-like close alias without comma into WINDOW clause spelling, got:\n{formatted}"
        );
        assert_eq!(
            leading_spaces(lines[close_alias_idx]),
            leading_spaces(lines[from_idx]).saturating_add(4),
            "mysql close keyword-like alias without comma should stay exactly one SELECT-list level deeper than FROM, got:\n{formatted}"
        );
        assert_eq!(
            SqlEditorWidget::format_sql_basic_for_db_type(
                &formatted,
                crate::db::connection::DatabaseType::MySQL,
            ),
            formatted
        );
    }

    #[test]
    fn format_sql_basic_for_mysql_db_type_keeps_close_keyword_like_alias_without_comma_with_inline_comment_on_select_list_depth(
    ) {
        let source = r#"SELECT
    (
        SELECT MAX(emp.sal)
        FROM emp
        WHERE emp.deptno = d.deptno
    ) window -- keep alias casing
FROM dept d;"#;

        let formatted = SqlEditorWidget::format_sql_basic_for_db_type(
            source,
            crate::db::connection::DatabaseType::MySQL,
        );
        let lines: Vec<&str> = formatted.lines().collect();

        let close_alias_idx = lines
            .iter()
            .enumerate()
            .find(|(_, line)| line.trim_start() == ") window -- keep alias casing")
            .map(|(idx, _)| idx)
            .expect("mysql close keyword-like alias without comma inline comment");
        let from_idx =
            find_line_starting_with(&lines, "FROM dept d;").expect("mysql FROM after alias");

        assert!(
            formatted.contains(") window -- keep alias casing"),
            "mysql formatter should preserve keyword-like alias casing on close-alias inline-comment line without comma, got:\n{formatted}"
        );
        assert!(
            !formatted.contains(") WINDOW -- keep alias casing"),
            "mysql formatter should not uppercase keyword-like close alias inline-comment line without comma into WINDOW clause spelling, got:\n{formatted}"
        );
        assert_eq!(
            leading_spaces(lines[close_alias_idx]),
            leading_spaces(lines[from_idx]).saturating_add(4),
            "mysql close keyword-like alias inline-comment line without comma should stay exactly one SELECT-list level deeper than FROM, got:\n{formatted}"
        );
        assert_eq!(
            SqlEditorWidget::format_sql_basic_for_db_type(
                &formatted,
                crate::db::connection::DatabaseType::MySQL,
            ),
            formatted
        );
    }

    #[test]
    fn format_sql_basic_for_mysql_db_type_keeps_close_keyword_like_alias_without_comma_with_block_comment_on_select_list_depth(
    ) {
        let source = r#"SELECT
    (
        SELECT MAX(emp.sal)
        FROM emp
        WHERE emp.deptno = d.deptno
    ) /* keep alias casing */ window
FROM dept d;"#;

        let formatted = SqlEditorWidget::format_sql_basic_for_db_type(
            source,
            crate::db::connection::DatabaseType::MySQL,
        );
        let lines: Vec<&str> = formatted.lines().collect();

        let close_alias_idx = lines
            .iter()
            .enumerate()
            .find(|(_, line)| line.trim_start() == ") /* keep alias casing */ window")
            .map(|(idx, _)| idx)
            .unwrap_or_else(|| {
                panic!(
                    "mysql close keyword-like alias without comma block comment, got:\n{formatted}"
                )
            });
        let from_idx =
            find_line_starting_with(&lines, "FROM dept d;").expect("mysql FROM after alias");

        assert!(
            formatted.contains(") /* keep alias casing */ window"),
            "mysql formatter should preserve keyword-like alias casing on close-alias block-comment line without comma, got:\n{formatted}"
        );
        assert!(
            !formatted.contains(") /* keep alias casing */ WINDOW"),
            "mysql formatter should not uppercase keyword-like close alias block-comment line without comma into WINDOW clause spelling, got:\n{formatted}"
        );
        assert_eq!(
            leading_spaces(lines[close_alias_idx]),
            leading_spaces(lines[from_idx]).saturating_add(4),
            "mysql close keyword-like alias block-comment line without comma should stay exactly one SELECT-list level deeper than FROM, got:\n{formatted}"
        );
        assert_eq!(
            SqlEditorWidget::format_sql_basic_for_db_type(
                &formatted,
                crate::db::connection::DatabaseType::MySQL,
            ),
            formatted
        );
    }

    #[test]
    fn format_sql_basic_for_mysql_db_type_keeps_close_keyword_like_as_alias_without_comma_with_block_comment_on_select_list_depth(
    ) {
        let source = r#"SELECT
    (
        SELECT MAX(emp.sal)
        FROM emp
        WHERE emp.deptno = d.deptno
    ) /* keep alias casing */ AS window
FROM dept d;"#;

        let formatted = SqlEditorWidget::format_sql_basic_for_db_type(
            source,
            crate::db::connection::DatabaseType::MySQL,
        );
        let lines: Vec<&str> = formatted.lines().collect();

        let close_alias_idx = lines
            .iter()
            .enumerate()
            .find(|(_, line)| line.trim_start() == ") /* keep alias casing */ AS window")
            .map(|(idx, _)| idx)
            .unwrap_or_else(|| {
                panic!(
                    "mysql close keyword-like AS alias without comma block comment, got:\n{formatted}"
                )
            });
        let from_idx =
            find_line_starting_with(&lines, "FROM dept d;").expect("mysql FROM after AS alias");

        assert!(
            formatted.contains(") /* keep alias casing */ AS window"),
            "mysql formatter should preserve keyword-like AS alias casing on close-alias block-comment line without comma, got:\n{formatted}"
        );
        assert!(
            !formatted.contains(") /* keep alias casing */ AS WINDOW"),
            "mysql formatter should not uppercase keyword-like close AS alias block-comment line without comma into WINDOW clause spelling, got:\n{formatted}"
        );
        assert_eq!(
            leading_spaces(lines[close_alias_idx]),
            leading_spaces(lines[from_idx]).saturating_add(4),
            "mysql close keyword-like AS alias block-comment line without comma should stay exactly one SELECT-list level deeper than FROM, got:\n{formatted}"
        );
        assert_eq!(
            SqlEditorWidget::format_sql_basic_for_db_type(
                &formatted,
                crate::db::connection::DatabaseType::MySQL,
            ),
            formatted
        );
    }

    #[test]
    fn format_sql_basic_for_mysql_db_type_keeps_from_close_keyword_like_alias_without_comma_with_block_comment_on_from_depth(
    ) {
        let source = r#"SELECT window.id
FROM (
    SELECT 1 AS id
) /* keep alias casing */ window
WHERE window.id = 1;"#;

        let formatted = SqlEditorWidget::format_sql_basic_for_db_type(
            source,
            crate::db::connection::DatabaseType::MySQL,
        );
        let lines: Vec<&str> = formatted.lines().collect();

        let close_alias_idx = lines
            .iter()
            .enumerate()
            .find(|(_, line)| line.trim_start() == ") /* keep alias casing */ window")
            .map(|(idx, _)| idx)
            .unwrap_or_else(|| {
                panic!("mysql FROM close keyword-like alias block comment, got:\n{formatted}")
            });

        assert!(
            formatted.contains(") /* keep alias casing */ window"),
            "mysql formatter should preserve keyword-like FROM alias casing on close-alias block-comment line without comma, got:\n{formatted}"
        );
        assert!(
            !formatted.contains(") /* keep alias casing */ WINDOW"),
            "mysql formatter should not uppercase keyword-like FROM close alias block-comment line without comma into WINDOW clause spelling, got:\n{formatted}"
        );
        assert_eq!(
            lines[close_alias_idx].trim_start(),
            ") /* keep alias casing */ window",
            "mysql formatter should keep close alias fragment on the same line after block comment in FROM alias context, got:\n{formatted}"
        );
        assert_eq!(
            SqlEditorWidget::format_sql_basic_for_db_type(
                &formatted,
                crate::db::connection::DatabaseType::MySQL,
            ),
            formatted
        );
    }

    #[test]
    fn format_sql_basic_for_mysql_db_type_realigns_sibling_after_close_quoted_alias_comma_without_as(
    ) {
        let source = r#"SELECT
    (
        SELECT MAX(emp.sal)
        FROM emp
        WHERE emp.deptno = d.deptno
    ) "nested_max_sal",
    d.deptno
FROM dept d;"#;

        let formatted = SqlEditorWidget::format_sql_basic_for_db_type(
            source,
            crate::db::connection::DatabaseType::MySQL,
        );
        let lines: Vec<&str> = formatted.lines().collect();

        let close_alias_idx = lines
            .iter()
            .enumerate()
            .find(|(_, line)| line.trim_start().ends_with("\"nested_max_sal\","))
            .map(|(idx, _)| idx)
            .expect("mysql close quoted alias comma");
        let sibling_idx =
            find_line_starting_with(&lines, "d.deptno").expect("mysql sibling select item");

        assert_eq!(
            leading_spaces(lines[sibling_idx]),
            leading_spaces(lines[close_alias_idx]),
            "mysql sibling after `) \"alias\",` should stay on the same SELECT-list frame depth, got:\n{formatted}"
        );
        assert_eq!(
            SqlEditorWidget::format_sql_basic_for_db_type(
                &formatted,
                crate::db::connection::DatabaseType::MySQL,
            ),
            formatted
        );
    }

    #[test]
    fn format_sql_basic_for_mysql_db_type_keeps_test4_assignment_parenthesized_expression_spacing()
    {
        let source = include_str!("../../../test_mariadb/test4.txt");
        let formatted = SqlEditorWidget::format_sql_basic_for_db_type(
            source,
            crate::db::connection::DatabaseType::MySQL,
        );

        assert!(
            formatted.contains("SET v_project = ((v_day + v_emp) MOD 3) + 1;"),
            "test4 assignment with parenthesized expression should keep operator spacing before `(`, got:\n{formatted}"
        );
        assert!(
            !formatted.contains("SET v_project =((v_day + v_emp) MOD 3) + 1;"),
            "test4 formatter must not collapse operator spacing into `=(` for parenthesized expression, got:\n{formatted}"
        );
    }

    #[test]
    fn format_sql_basic_for_mysql_db_type_keeps_test5_parenthesized_expression_operator_spacing() {
        let source = include_str!("../../../test_mariadb/test5.txt");
        let formatted = SqlEditorWidget::format_sql_basic_for_db_type(
            source,
            crate::db::connection::DatabaseType::MySQL,
        );

        assert!(
            formatted.contains("SET v_sprint_id = ((v_project_id - 1) * 3) + (((v_seq - 1) MOD 3) + 1);"),
            "test5 sprint assignment should keep operator spacing before parenthesized expression, got:\n{formatted}"
        );
        assert!(
            formatted.contains("SET v_assignee_user_id = ((v_seq - 1) MOD 6) + 3;"),
            "test5 assignee assignment should keep operator spacing before parenthesized expression, got:\n{formatted}"
        );
        assert!(
            formatted.contains("WHERE wi2.project_id = ("),
            "test5 scalar subquery predicate should keep space before parenthesized expression, got:\n{formatted}"
        );
    }

    #[test]
    fn format_sql_basic_for_mysql_db_type_keeps_test6_parenthesized_operand_spacing_without_call_collapse(
    ) {
        let source = include_str!("../../../test_mariadb/test6.txt");
        let formatted = SqlEditorWidget::format_sql_basic_for_db_type(
            source,
            crate::db::connection::DatabaseType::MySQL,
        );

        for expected in [
            "ROUND(19 + (v_product_id * 7.35) + (MOD(v_product_id, 5) * 3.70), 2)",
            "ROUND(0.5 + (v_product_id * 0.13), 2)",
            "ROUND(19 + (v_prod_id * 7.35) + (MOD(v_prod_id, 5) * 3.70), 2)",
        ] {
            assert!(
                formatted.contains(expected),
                "test6 expression operand parens should preserve operator spacing, missing `{expected}` in:\n{formatted}"
            );
        }
        for malformed in [
            "ROUND(19 +(v_product_id * 7.35) +(MOD(v_product_id, 5) * 3.70), 2)",
            "ROUND(0.5 +(v_product_id * 0.13), 2)",
            "ROUND(19 +(v_prod_id * 7.35) +(MOD(v_prod_id, 5) * 3.70), 2)",
        ] {
            assert!(
                !formatted.contains(malformed),
                "test6 formatter must not collapse operator spacing before parenthesized operands, found malformed `{malformed}` in:\n{formatted}"
            );
        }
    }

    #[test]
    fn format_sql_basic_for_mysql_db_type_keeps_test6_values_case_end_comma_siblings_on_values_frame_depth(
    ) {
        let source = include_str!("../../../test_mariadb/test6.txt");
        let formatted = SqlEditorWidget::format_sql_basic_for_db_type(
            source,
            crate::db::connection::DatabaseType::MySQL,
        );
        let lines: Vec<&str> = formatted.lines().collect();

        let values_idx = find_line_starting_with(
            &lines,
            "VALUES (v_order_id, v_item_no, v_prod_id, 1 + MOD(v_order_id + v_item_no, 4),",
        )
        .expect("test6 order_item VALUES head");
        let end_indices: Vec<usize> = lines
            .iter()
            .enumerate()
            .skip(values_idx + 1)
            .take(40)
            .filter(|(_, line)| line.trim_start() == "END,")
            .map(|(idx, _)| idx)
            .collect();
        assert_eq!(
            end_indices.len(),
            3,
            "test6 order_item VALUES payload should keep three CASE END, lines, got:\n{formatted}"
        );
        let concat_idx = lines
            .iter()
            .enumerate()
            .skip(end_indices[2] + 1)
            .find(|(_, line)| {
                line.trim_start()
                    == "CONCAT('note ; ', v_order_id, '/', v_item_no, ' // not delimiter'),"
            })
            .map(|(idx, _)| idx)
            .expect("test6 VALUES CONCAT note sibling");
        let json_idx = lines
            .iter()
            .enumerate()
            .skip(concat_idx + 1)
            .find(|(_, line)| {
                line.trim_start().starts_with(
                    "JSON_OBJECT('gift', IF(MOD(v_order_id + v_item_no, 6) = 0, 'Y', 'N'),",
                )
            })
            .map(|(idx, _)| idx)
            .expect("test6 VALUES JSON_OBJECT sibling");

        let case_owner_indent = leading_spaces(lines[end_indices[2]]);
        assert_eq!(
            leading_spaces(lines[concat_idx]),
            case_owner_indent,
            "test6 VALUES sibling after CASE END, should return to the parent argument frame depth, got:\n{formatted}"
        );
        assert_eq!(
            leading_spaces(lines[json_idx]),
            case_owner_indent,
            "test6 VALUES JSON_OBJECT sibling should stay on the same parent argument frame depth after CASE END,, got:\n{formatted}"
        );
    }

    #[test]
    fn format_sql_basic_for_mysql_db_type_keeps_values_sibling_depth_after_close_open_expression_chain(
    ) {
        let source = r#"INSERT INTO ledger (delta_amount, payload)
VALUES (
    (
        SELECT SUM(a.amount)
        FROM account_tx a
        WHERE a.account_id = 10
    ) + (
        SELECT SUM(a.amount)
        FROM account_tx a
        WHERE a.account_id = 20
    ),
    JSON_OBJECT('stable', 1)
);"#;
        let formatted = SqlEditorWidget::format_sql_basic_for_db_type(
            source,
            crate::db::connection::DatabaseType::MySQL,
        );
        let lines: Vec<&str> = formatted.lines().collect();

        let mixed_close_open_idx =
            find_line_starting_with(&lines, ") + (").expect("VALUES mixed close-open line");
        let sibling_idx = find_line_starting_with(&lines, "JSON_OBJECT('stable', 1)")
            .expect("VALUES sibling after mixed close-open line");
        let second_select_idx = lines
            .iter()
            .enumerate()
            .skip(mixed_close_open_idx + 1)
            .find(|(_, line)| line.trim_start().starts_with("SELECT SUM(a.amount)"))
            .map(|(idx, _)| idx)
            .expect("VALUES second scalar subquery SELECT head");

        assert_eq!(
            leading_spaces(lines[sibling_idx]),
            leading_spaces(lines[mixed_close_open_idx]),
            "VALUES sibling after same-line `) + (` should return to the canonical list depth, got:\n{formatted}"
        );
        assert_eq!(
            leading_spaces(lines[second_select_idx]),
            leading_spaces(lines[mixed_close_open_idx]).saturating_add(4),
            "VALUES second scalar subquery under same-line `) + (` should open exactly one frame deeper than the mixed owner line, got:\n{formatted}"
        );
        assert_eq!(
            SqlEditorWidget::format_sql_basic_for_db_type(
                &formatted,
                crate::db::connection::DatabaseType::MySQL,
            ),
            formatted
        );
    }

    #[test]
    fn format_sql_basic_for_mysql_db_type_keeps_close_open_expression_chain_on_stable_select_list_depth(
    ) {
        let source = r#"SELECT
    (
        SELECT SUM(a.amount)
        FROM account_tx a
        WHERE a.account_id = 10
    ) + (
        SELECT SUM(a.amount)
        FROM account_tx a
        WHERE a.account_id = 20
    ) AS delta_sum,
    1 AS stable_sibling
FROM dual;"#;
        let formatted = SqlEditorWidget::format_sql_basic_for_db_type(
            source,
            crate::db::connection::DatabaseType::MySQL,
        );
        let lines: Vec<&str> = formatted.lines().collect();

        let mixed_close_open_idx =
            find_line_starting_with(&lines, ") + (").expect("mixed close-open select item line");
        let sibling_idx = find_line_starting_with(&lines, "1 AS stable_sibling")
            .expect("select-list sibling after mixed close-open item");
        let second_select_idx = lines
            .iter()
            .enumerate()
            .skip(mixed_close_open_idx + 1)
            .find(|(_, line)| line.trim_start().starts_with("SELECT SUM(a.amount)"))
            .map(|(idx, _)| idx)
            .expect("second scalar subquery SELECT head");

        assert_eq!(
            leading_spaces(lines[sibling_idx]),
            leading_spaces(lines[mixed_close_open_idx]),
            "SELECT-list sibling after same-line `) + (` should return to the canonical list depth, got:\n{formatted}"
        );
        assert_eq!(
            leading_spaces(lines[second_select_idx]),
            leading_spaces(lines[mixed_close_open_idx]).saturating_add(4),
            "second scalar subquery under same-line `) + (` should open exactly one frame deeper than the mixed owner line, got:\n{formatted}"
        );
        assert_eq!(
            SqlEditorWidget::format_sql_basic_for_db_type(
                &formatted,
                crate::db::connection::DatabaseType::MySQL,
            ),
            formatted
        );
    }

    #[test]
    fn format_sql_basic_for_mysql_db_type_keeps_split_close_open_expression_chain_on_stable_select_list_depth(
    ) {
        let source = r#"SELECT
    (
        SELECT SUM(a.amount)
        FROM account_tx a
        WHERE a.account_id = 10
    ) + -- keep operator continuation
    (
        SELECT SUM(a.amount)
        FROM account_tx a
        WHERE a.account_id = 20
    ) AS delta_sum,
    1 AS stable_sibling
FROM dual;"#;
        let formatted = SqlEditorWidget::format_sql_basic_for_db_type(
            source,
            crate::db::connection::DatabaseType::MySQL,
        );
        let lines: Vec<&str> = formatted.lines().collect();

        let split_operator_idx = lines
            .iter()
            .position(|line| line.trim_start().starts_with(") + --"))
            .expect("split close-open operator line");
        let second_open_idx = find_line_starting_with(&lines, "(")
            .expect("second scalar subquery open after split operator");
        let second_select_idx = lines
            .iter()
            .enumerate()
            .skip(second_open_idx + 1)
            .find(|(_, line)| line.trim_start().starts_with("SELECT SUM(a.amount)"))
            .map(|(idx, _)| idx)
            .expect("second scalar subquery SELECT head");
        let sibling_idx = find_line_starting_with(&lines, "1 AS stable_sibling")
            .expect("select-list sibling after split close-open item");

        assert_eq!(
            leading_spaces(lines[second_open_idx]),
            leading_spaces(lines[split_operator_idx]),
            "standalone `(` after split close-open operator line should stay on the mixed owner depth, got:\n{formatted}"
        );
        assert_eq!(
            leading_spaces(lines[second_select_idx]),
            leading_spaces(lines[second_open_idx]).saturating_add(4),
            "split close-open second scalar subquery SELECT should be one frame deeper than its standalone open, got:\n{formatted}"
        );
        assert_eq!(
            leading_spaces(lines[sibling_idx]),
            leading_spaces(lines[split_operator_idx]),
            "SELECT-list sibling after split close-open chain should return to the canonical list depth, got:\n{formatted}"
        );
        assert_eq!(
            SqlEditorWidget::format_sql_basic_for_db_type(
                &formatted,
                crate::db::connection::DatabaseType::MySQL,
            ),
            formatted
        );
    }

    #[test]
    fn format_sql_basic_keeps_order_by_sibling_depth_after_close_open_expression_chain() {
        let source = r#"SELECT 1
FROM dual
ORDER BY
    (
        SELECT MAX(a.amount)
        FROM account_tx a
        WHERE a.account_id = 10
    ) + (
        SELECT MAX(a.amount)
        FROM account_tx a
        WHERE a.account_id = 20
    ),
    stable_col;"#;
        let formatted = SqlEditorWidget::format_sql_basic(source);
        let lines: Vec<&str> = formatted.lines().collect();

        let mixed_close_open_idx = find_line_starting_with(&lines, ") + (")
            .expect("ORDER BY mixed close-open expression line");
        let sibling_idx = find_line_starting_with(&lines, "stable_col")
            .expect("ORDER BY sibling after mixed close-open expression");

        assert_eq!(
            leading_spaces(lines[sibling_idx]),
            leading_spaces(lines[mixed_close_open_idx]),
            "ORDER BY sibling after same-line `) + (` should return to canonical list depth, got:\n{formatted}"
        );
        assert_eq!(SqlEditorWidget::format_sql_basic(&formatted), formatted);
    }

    #[test]
    fn format_sql_basic_for_mysql_db_type_keeps_sibling_depth_after_close_quoted_alias_comma_with_inline_comment(
    ) {
        let source = r#"SELECT
    (
        SELECT MAX(emp.sal)
        FROM emp
        WHERE emp.deptno = d.deptno
    ) "nested_max_sal", -- close alias comment
    d.dname
FROM dept d;"#;
        let formatted = SqlEditorWidget::format_sql_basic_for_db_type(
            source,
            crate::db::connection::DatabaseType::MySQL,
        );
        let lines: Vec<&str> = formatted.lines().collect();

        let close_alias_idx = lines
            .iter()
            .enumerate()
            .find(|(_, line)| {
                line.trim_start()
                    .starts_with(") \"nested_max_sal\", -- close alias comment")
            })
            .map(|(idx, _)| idx)
            .expect("mysql close quoted alias comma with inline comment");
        let sibling_idx =
            find_line_starting_with(&lines, "d.dname").expect("mysql sibling select item");

        assert_eq!(
            leading_spaces(lines[sibling_idx]),
            leading_spaces(lines[close_alias_idx]),
            "mysql sibling after `) \"alias\", -- comment` should stay on the same SELECT-list frame depth, got:\n{formatted}"
        );
        assert_eq!(
            SqlEditorWidget::format_sql_basic_for_db_type(
                &formatted,
                crate::db::connection::DatabaseType::MySQL,
            ),
            formatted
        );
    }

    #[test]
    fn format_sql_basic_for_mysql_db_type_keeps_sibling_depth_after_close_alias_comma_with_inline_comment(
    ) {
        let source = r#"SELECT
    (
        SELECT MAX(emp.sal)
        FROM emp
        WHERE emp.deptno = d.deptno
    ) nested_max_sal, -- close alias comment
    d.dname
FROM dept d;"#;
        let formatted = SqlEditorWidget::format_sql_basic_for_db_type(
            source,
            crate::db::connection::DatabaseType::MySQL,
        );
        let lines: Vec<&str> = formatted.lines().collect();

        let close_alias_idx = lines
            .iter()
            .enumerate()
            .find(|(_, line)| {
                line.trim_start()
                    .starts_with(") nested_max_sal, -- close alias comment")
            })
            .map(|(idx, _)| idx)
            .expect("mysql close alias comma with inline comment");
        let sibling_idx =
            find_line_starting_with(&lines, "d.dname").expect("mysql sibling select item");

        assert_eq!(
            leading_spaces(lines[sibling_idx]),
            leading_spaces(lines[close_alias_idx]),
            "mysql sibling after `) alias, -- comment` should stay on the same SELECT-list frame depth, got:\n{formatted}"
        );
        assert_eq!(
            SqlEditorWidget::format_sql_basic_for_db_type(
                &formatted,
                crate::db::connection::DatabaseType::MySQL,
            ),
            formatted
        );
    }

    #[test]
    fn format_sql_basic_for_mysql_db_type_keeps_sibling_depth_after_close_as_alias_comma_with_inline_comment(
    ) {
        let source = r#"SELECT
    (
        SELECT MAX(emp.sal)
        FROM emp
        WHERE emp.deptno = d.deptno
    ) AS nested_max_sal, -- close alias comment
    d.dname
FROM dept d;"#;
        let formatted = SqlEditorWidget::format_sql_basic_for_db_type(
            source,
            crate::db::connection::DatabaseType::MySQL,
        );
        let lines: Vec<&str> = formatted.lines().collect();

        let close_alias_idx = lines
            .iter()
            .enumerate()
            .find(|(_, line)| {
                line.trim_start()
                    .starts_with(") AS nested_max_sal, -- close alias comment")
            })
            .map(|(idx, _)| idx)
            .expect("mysql close AS alias comma with inline comment");
        let sibling_idx =
            find_line_starting_with(&lines, "d.dname").expect("mysql sibling select item");

        assert_eq!(
            leading_spaces(lines[sibling_idx]),
            leading_spaces(lines[close_alias_idx]),
            "mysql sibling after `) AS alias, -- comment` should stay on the same SELECT-list frame depth, got:\n{formatted}"
        );
        assert_eq!(
            SqlEditorWidget::format_sql_basic_for_db_type(
                &formatted,
                crate::db::connection::DatabaseType::MySQL,
            ),
            formatted
        );
    }

    #[test]
    fn format_for_auto_formatting_oracle_keeps_sibling_depth_after_close_alias_inline_comment() {
        let source = r#"SELECT
    (
        SELECT MAX(emp.sal)
        FROM emp
        WHERE emp.deptno = d.deptno
    ) nested_max_sal, -- close alias comment
    d.deptno
FROM dept d;"#;

        let formatted = SqlEditorWidget::format_for_auto_formatting_with_db_type(
            source,
            false,
            Some(crate::db::connection::DatabaseType::Oracle),
        );
        let lines: Vec<&str> = formatted.lines().collect();

        let close_alias_idx = lines
            .iter()
            .enumerate()
            .find(|(_, line)| {
                line.trim_start()
                    .starts_with(") nested_max_sal, -- close alias comment")
            })
            .map(|(idx, _)| idx)
            .expect("oracle auto-format close alias comma with inline comment");
        let sibling_idx =
            find_line_starting_with(&lines, "d.deptno").expect("oracle auto-format sibling");

        assert_eq!(
            leading_spaces(lines[sibling_idx]),
            leading_spaces(lines[close_alias_idx]),
            "oracle auto-format sibling after `) alias, -- comment` should stay on the same SELECT-list frame depth, got:\n{formatted}"
        );
        assert_eq!(
            SqlEditorWidget::format_for_auto_formatting_with_db_type(
                &formatted,
                false,
                Some(crate::db::connection::DatabaseType::Oracle),
            ),
            formatted
        );
    }

    #[test]
    fn format_for_auto_formatting_mysql_keeps_sibling_depth_after_close_alias_inline_comment() {
        let source = r#"SELECT
    (
        SELECT MAX(emp.sal)
        FROM emp
        WHERE emp.deptno = d.deptno
    ) nested_max_sal, -- close alias comment
    d.deptno
FROM dept d;"#;

        let formatted = SqlEditorWidget::format_for_auto_formatting_with_db_type(
            source,
            false,
            Some(crate::db::connection::DatabaseType::MySQL),
        );
        let lines: Vec<&str> = formatted.lines().collect();

        let close_alias_idx = lines
            .iter()
            .enumerate()
            .find(|(_, line)| {
                line.trim_start()
                    .starts_with(") nested_max_sal, -- close alias comment")
            })
            .map(|(idx, _)| idx)
            .expect("mysql auto-format close alias comma with inline comment");
        let sibling_idx =
            find_line_starting_with(&lines, "d.deptno").expect("mysql auto-format sibling");

        assert_eq!(
            leading_spaces(lines[sibling_idx]),
            leading_spaces(lines[close_alias_idx]),
            "mysql auto-format sibling after `) alias, -- comment` should stay on the same SELECT-list frame depth, got:\n{formatted}"
        );
        assert_eq!(
            SqlEditorWidget::format_for_auto_formatting_with_db_type(
                &formatted,
                false,
                Some(crate::db::connection::DatabaseType::MySQL),
            ),
            formatted
        );
    }

    #[test]
    fn format_for_auto_formatting_mysql_keeps_sibling_depth_after_close_keyword_like_alias_comma() {
        let source = r#"SELECT
    (
        SELECT MAX(emp.sal)
        FROM emp
        WHERE emp.deptno = d.deptno
    ) window,
    d.deptno
FROM dept d;"#;

        let formatted = SqlEditorWidget::format_for_auto_formatting_with_db_type(
            source,
            false,
            Some(crate::db::connection::DatabaseType::MySQL),
        );
        let lines: Vec<&str> = formatted.lines().collect();

        let close_alias_idx = lines
            .iter()
            .enumerate()
            .find(|(_, line)| line.trim_start() == ") window,")
            .map(|(idx, _)| idx)
            .expect("mysql auto-format close keyword-like alias comma");
        let sibling_idx =
            find_line_starting_with(&lines, "d.deptno").expect("mysql auto-format sibling");

        assert!(
            formatted.contains(") window,"),
            "mysql auto-format should preserve keyword-like alias casing on close-alias line, got:\n{formatted}"
        );
        assert!(
            !formatted.contains(") WINDOW,"),
            "mysql auto-format should not uppercase keyword-like close alias into WINDOW clause spelling, got:\n{formatted}"
        );
        assert_eq!(
            leading_spaces(lines[sibling_idx]),
            leading_spaces(lines[close_alias_idx]),
            "mysql auto-format sibling after `) window,` should stay on the same SELECT-list frame depth, got:\n{formatted}"
        );
        assert_eq!(
            SqlEditorWidget::format_for_auto_formatting_with_db_type(
                &formatted,
                false,
                Some(crate::db::connection::DatabaseType::MySQL),
            ),
            formatted
        );
    }

    #[test]
    fn format_for_auto_formatting_mysql_keeps_close_keyword_like_alias_without_comma_on_select_list_depth(
    ) {
        let source = r#"SELECT
    (
        SELECT MAX(emp.sal)
        FROM emp
        WHERE emp.deptno = d.deptno
    ) window
FROM dept d;"#;

        let formatted = SqlEditorWidget::format_for_auto_formatting_with_db_type(
            source,
            false,
            Some(crate::db::connection::DatabaseType::MySQL),
        );
        let lines: Vec<&str> = formatted.lines().collect();

        let close_alias_idx = lines
            .iter()
            .enumerate()
            .find(|(_, line)| line.trim_start() == ") window")
            .map(|(idx, _)| idx)
            .expect("mysql auto-format close keyword-like alias without comma");
        let from_idx =
            find_line_starting_with(&lines, "FROM dept d;").expect("mysql auto-format FROM");

        assert!(
            formatted.contains(") window"),
            "mysql auto-format should preserve keyword-like alias casing on close-alias line without comma, got:\n{formatted}"
        );
        assert!(
            !formatted.contains(") WINDOW"),
            "mysql auto-format should not uppercase keyword-like close alias without comma into WINDOW clause spelling, got:\n{formatted}"
        );
        assert_eq!(
            leading_spaces(lines[close_alias_idx]),
            leading_spaces(lines[from_idx]).saturating_add(4),
            "mysql auto-format close keyword-like alias without comma should stay exactly one SELECT-list level deeper than FROM, got:\n{formatted}"
        );
        assert_eq!(
            SqlEditorWidget::format_for_auto_formatting_with_db_type(
                &formatted,
                false,
                Some(crate::db::connection::DatabaseType::MySQL),
            ),
            formatted
        );
    }

    #[test]
    fn format_for_auto_formatting_mysql_keeps_close_keyword_like_alias_without_comma_with_inline_comment_on_select_list_depth(
    ) {
        let source = r#"SELECT
    (
        SELECT MAX(emp.sal)
        FROM emp
        WHERE emp.deptno = d.deptno
    ) window -- keep alias casing
FROM dept d;"#;

        let formatted = SqlEditorWidget::format_for_auto_formatting_with_db_type(
            source,
            false,
            Some(crate::db::connection::DatabaseType::MySQL),
        );
        let lines: Vec<&str> = formatted.lines().collect();

        let close_alias_idx = lines
            .iter()
            .enumerate()
            .find(|(_, line)| line.trim_start() == ") window -- keep alias casing")
            .map(|(idx, _)| idx)
            .expect("mysql auto-format close keyword-like alias without comma inline comment");
        let from_idx =
            find_line_starting_with(&lines, "FROM dept d;").expect("mysql auto-format FROM");

        assert!(
            formatted.contains(") window -- keep alias casing"),
            "mysql auto-format should preserve keyword-like alias casing on close-alias inline-comment line without comma, got:\n{formatted}"
        );
        assert!(
            !formatted.contains(") WINDOW -- keep alias casing"),
            "mysql auto-format should not uppercase keyword-like close alias inline-comment line without comma into WINDOW clause spelling, got:\n{formatted}"
        );
        assert_eq!(
            leading_spaces(lines[close_alias_idx]),
            leading_spaces(lines[from_idx]).saturating_add(4),
            "mysql auto-format close keyword-like alias inline-comment line without comma should stay exactly one SELECT-list level deeper than FROM, got:\n{formatted}"
        );
        assert_eq!(
            SqlEditorWidget::format_for_auto_formatting_with_db_type(
                &formatted,
                false,
                Some(crate::db::connection::DatabaseType::MySQL),
            ),
            formatted
        );
    }

    #[test]
    fn format_for_auto_formatting_mysql_keeps_close_keyword_like_alias_without_comma_with_block_comment_on_select_list_depth(
    ) {
        let source = r#"SELECT
    (
        SELECT MAX(emp.sal)
        FROM emp
        WHERE emp.deptno = d.deptno
    ) /* keep alias casing */ window
FROM dept d;"#;

        let formatted = SqlEditorWidget::format_for_auto_formatting_with_db_type(
            source,
            false,
            Some(crate::db::connection::DatabaseType::MySQL),
        );
        let lines: Vec<&str> = formatted.lines().collect();

        let close_alias_idx = lines
            .iter()
            .enumerate()
            .find(|(_, line)| line.trim_start() == ") /* keep alias casing */ window")
            .map(|(idx, _)| idx)
            .unwrap_or_else(|| {
                panic!(
                    "mysql auto-format close keyword-like alias without comma block comment, got:\n{formatted}"
                )
            });
        let from_idx =
            find_line_starting_with(&lines, "FROM dept d;").expect("mysql auto-format FROM");

        assert!(
            formatted.contains(") /* keep alias casing */ window"),
            "mysql auto-format should preserve keyword-like alias casing on close-alias block-comment line without comma, got:\n{formatted}"
        );
        assert!(
            !formatted.contains(") /* keep alias casing */ WINDOW"),
            "mysql auto-format should not uppercase keyword-like close alias block-comment line without comma into WINDOW clause spelling, got:\n{formatted}"
        );
        assert_eq!(
            leading_spaces(lines[close_alias_idx]),
            leading_spaces(lines[from_idx]).saturating_add(4),
            "mysql auto-format close keyword-like alias block-comment line without comma should stay exactly one SELECT-list level deeper than FROM, got:\n{formatted}"
        );
        assert_eq!(
            SqlEditorWidget::format_for_auto_formatting_with_db_type(
                &formatted,
                false,
                Some(crate::db::connection::DatabaseType::MySQL),
            ),
            formatted
        );
    }

    #[test]
    fn format_for_auto_formatting_mysql_keeps_close_keyword_like_as_alias_without_comma_with_block_comment_on_select_list_depth(
    ) {
        let source = r#"SELECT
    (
        SELECT MAX(emp.sal)
        FROM emp
        WHERE emp.deptno = d.deptno
    ) /* keep alias casing */ AS window
FROM dept d;"#;

        let formatted = SqlEditorWidget::format_for_auto_formatting_with_db_type(
            source,
            false,
            Some(crate::db::connection::DatabaseType::MySQL),
        );
        let lines: Vec<&str> = formatted.lines().collect();

        let close_alias_idx = lines
            .iter()
            .enumerate()
            .find(|(_, line)| line.trim_start() == ") /* keep alias casing */ AS window")
            .map(|(idx, _)| idx)
            .unwrap_or_else(|| {
                panic!(
                    "mysql auto-format close keyword-like AS alias without comma block comment, got:\n{formatted}"
                )
            });
        let from_idx =
            find_line_starting_with(&lines, "FROM dept d;").expect("mysql auto-format FROM");

        assert!(
            formatted.contains(") /* keep alias casing */ AS window"),
            "mysql auto-format should preserve keyword-like AS alias casing on close-alias block-comment line without comma, got:\n{formatted}"
        );
        assert!(
            !formatted.contains(") /* keep alias casing */ AS WINDOW"),
            "mysql auto-format should not uppercase keyword-like close AS alias block-comment line without comma into WINDOW clause spelling, got:\n{formatted}"
        );
        assert_eq!(
            leading_spaces(lines[close_alias_idx]),
            leading_spaces(lines[from_idx]).saturating_add(4),
            "mysql auto-format close keyword-like AS alias block-comment line without comma should stay exactly one SELECT-list level deeper than FROM, got:\n{formatted}"
        );
        assert_eq!(
            SqlEditorWidget::format_for_auto_formatting_with_db_type(
                &formatted,
                false,
                Some(crate::db::connection::DatabaseType::MySQL),
            ),
            formatted
        );
    }

    #[test]
    fn format_for_auto_formatting_mysql_keeps_from_close_keyword_like_alias_without_comma_with_block_comment_on_from_depth(
    ) {
        let source = r#"SELECT window.id
FROM (
    SELECT 1 AS id
) /* keep alias casing */ window
WHERE window.id = 1;"#;

        let formatted = SqlEditorWidget::format_for_auto_formatting_with_db_type(
            source,
            false,
            Some(crate::db::connection::DatabaseType::MySQL),
        );
        let lines: Vec<&str> = formatted.lines().collect();

        let close_alias_idx = lines
            .iter()
            .enumerate()
            .find(|(_, line)| line.trim_start() == ") /* keep alias casing */ window")
            .map(|(idx, _)| idx)
            .unwrap_or_else(|| {
                panic!(
                    "mysql auto-format FROM close keyword-like alias block comment, got:\n{formatted}"
                )
            });

        assert!(
            formatted.contains(") /* keep alias casing */ window"),
            "mysql auto-format should preserve keyword-like FROM alias casing on close-alias block-comment line without comma, got:\n{formatted}"
        );
        assert!(
            !formatted.contains(") /* keep alias casing */ WINDOW"),
            "mysql auto-format should not uppercase keyword-like FROM close alias block-comment line without comma into WINDOW clause spelling, got:\n{formatted}"
        );
        assert_eq!(
            lines[close_alias_idx].trim_start(),
            ") /* keep alias casing */ window",
            "mysql auto-format should keep close alias fragment on the same line after block comment in FROM alias context, got:\n{formatted}"
        );
        assert_eq!(
            SqlEditorWidget::format_for_auto_formatting_with_db_type(
                &formatted,
                false,
                Some(crate::db::connection::DatabaseType::MySQL),
            ),
            formatted
        );
    }

    #[test]
    fn format_for_auto_formatting_mysql_keeps_sibling_depth_after_close_block_comment_as_alias_comma(
    ) {
        let source = r#"SELECT
    (
        SELECT MAX(emp.sal)
        FROM emp
        WHERE emp.deptno = d.deptno
    ) /* close alias comment */ AS nested_max_sal,
    d.deptno
FROM dept d;"#;

        let formatted = SqlEditorWidget::format_for_auto_formatting_with_db_type(
            source,
            false,
            Some(crate::db::connection::DatabaseType::MySQL),
        );
        let lines: Vec<&str> = formatted.lines().collect();

        let close_alias_idx = lines
            .iter()
            .enumerate()
            .find(|(_, line)| line.trim_start().ends_with("AS nested_max_sal,"))
            .map(|(idx, _)| idx)
            .expect("mysql auto-format close block-comment AS alias comma");
        let sibling_idx =
            find_line_starting_with(&lines, "d.deptno").expect("mysql auto-format sibling");

        assert!(
            formatted.contains("/* close alias comment */"),
            "mysql auto-format should preserve the block comment, got:\n{formatted}"
        );
        assert_eq!(
            leading_spaces(lines[sibling_idx]),
            leading_spaces(lines[close_alias_idx]),
            "mysql auto-format sibling after `) /* ... */ AS alias,` should stay on the same SELECT-list frame depth, got:\n{formatted}"
        );
        assert_eq!(
            SqlEditorWidget::format_for_auto_formatting_with_db_type(
                &formatted,
                false,
                Some(crate::db::connection::DatabaseType::MySQL),
            ),
            formatted
        );
    }

    #[test]
    fn format_sql_basic_keeps_sibling_depth_after_multiline_literal_payload_leading_close_then_subquery(
    ) {
        let source = r#"SELECT
    'first line
) literal payload' || (
        SELECT MAX(emp.sal)
        FROM emp
        WHERE emp.deptno = d.deptno
    ) AS nested_max_sal,
    d.dname
FROM dept d;"#;
        let formatted = SqlEditorWidget::format_sql_basic(source);
        let lines: Vec<&str> = formatted.lines().collect();

        let close_alias_idx = lines
            .iter()
            .enumerate()
            .find(|(_, line)| line.trim_start().starts_with(") AS nested_max_sal,"))
            .map(|(idx, _)| idx)
            .expect("close alias after multiline literal payload");
        let sibling_idx = find_line_starting_with(&lines, "d.dname")
            .expect("sibling select item after multiline literal payload");

        assert_eq!(
            leading_spaces(lines[sibling_idx]),
            leading_spaces(lines[close_alias_idx]),
            "sibling after multiline-literal payload + subquery close alias must stay on the same SELECT-list frame depth, got:\n{formatted}"
        );
        assert_eq!(
            SqlEditorWidget::format_sql_basic(&formatted),
            formatted,
            "formatting should stay idempotent for multiline-literal payload leading-close + subquery owner chain"
        );
    }

    #[test]
    fn line_closes_paren_frame_below_line_start_before_token_tracks_close_then_open_token_order() {
        let tokens = SqlEditorWidget::tokenize_sql(") + (, stable");
        let comma_idx = tokens
            .iter()
            .enumerate()
            .find(|(_, token)| matches!(token, SqlToken::Symbol(sym) if sym == ","))
            .map(|(idx, _)| idx)
            .expect("comma in close-open token-order fixture");
        let line_start_snapshot = line_start_snapshot_before_token(&tokens, 0, 1);

        assert!(
            SqlEditorWidget::line_closes_paren_frame_below_line_start_before_token(
                &tokens,
                0,
                comma_idx,
                &line_start_snapshot,
            ),
            "close->open sequence must report a frame close below the line-start depth before comma"
        );
    }

    #[test]
    fn line_closes_paren_frame_below_line_start_before_token_ignores_local_balanced_parens() {
        let tokens = SqlEditorWidget::tokenize_sql("ABS(a), stable");
        let comma_idx = tokens
            .iter()
            .enumerate()
            .find(|(_, token)| matches!(token, SqlToken::Symbol(sym) if sym == ","))
            .map(|(idx, _)| idx)
            .expect("comma in local balanced paren fixture");
        let line_start_snapshot = line_start_snapshot_before_token(&tokens, 0, 0);

        assert!(
            !SqlEditorWidget::line_closes_paren_frame_below_line_start_before_token(
                &tokens,
                0,
                comma_idx,
                &line_start_snapshot,
            ),
            "local balanced call parens at top-level must not be treated as closing a parent frame"
        );
    }

    #[test]
    fn line_closes_paren_frame_below_line_start_before_token_ignores_root_unmatched_close_open_chain(
    ) {
        let tokens = SqlEditorWidget::tokenize_sql(") + (, stable");
        let comma_idx = tokens
            .iter()
            .enumerate()
            .find(|(_, token)| matches!(token, SqlToken::Symbol(sym) if sym == ","))
            .map(|(idx, _)| idx)
            .expect("comma in root close-open fixture");
        let line_start_snapshot = line_start_snapshot_before_token(&tokens, 0, 0);

        assert!(
            !SqlEditorWidget::line_closes_paren_frame_below_line_start_before_token(
                &tokens,
                0,
                comma_idx,
                &line_start_snapshot,
            ),
            "line-start depth 0 has no parent frame to close; unmatched close-open chain must not trigger close detection"
        );
    }

    #[test]
    fn line_closes_paren_frame_below_line_start_before_token_tracks_close_then_open_for_brackets() {
        let tokens = SqlEditorWidget::tokenize_sql("] + [, stable");
        let comma_idx = tokens
            .iter()
            .enumerate()
            .find(|(_, token)| matches!(token, SqlToken::Symbol(sym) if sym == ","))
            .map(|(idx, _)| idx)
            .expect("comma in bracket close-open token-order fixture");
        let line_start_snapshot = line_start_snapshot_before_token(&tokens, 0, 1);

        assert!(
            SqlEditorWidget::line_closes_paren_frame_below_line_start_before_token(
                &tokens,
                0,
                comma_idx,
                &line_start_snapshot,
            ),
            "close->open bracket sequence must report a frame close below the line-start depth before comma"
        );
    }

    #[test]
    fn line_closes_paren_frame_below_line_start_before_token_respects_known_line_start_frame_kind()
    {
        let tokens = SqlEditorWidget::tokenize_sql("(\n] + (, stable");
        let line_start_idx = tokens
            .iter()
            .enumerate()
            .find(|(_, token)| matches!(token, SqlToken::Symbol(sym) if sym == "]"))
            .map(|(idx, _)| idx)
            .expect("line-start mismatched close in typed-frame fixture");
        let comma_idx = tokens
            .iter()
            .enumerate()
            .find(|(_, token)| matches!(token, SqlToken::Symbol(sym) if sym == ","))
            .map(|(idx, _)| idx)
            .expect("comma in typed-frame fixture");
        let line_start_snapshot = line_start_snapshot_before_token(&tokens, line_start_idx, 1);

        assert!(
            !SqlEditorWidget::line_closes_paren_frame_below_line_start_before_token(
                &tokens,
                line_start_idx,
                comma_idx,
                &line_start_snapshot,
            ),
            "known line-start `(` frame must not be popped by mismatched `]` close before comma"
        );
    }

    #[test]
    fn line_closes_paren_frame_below_line_start_before_token_tracks_typed_line_start_close_then_open(
    ) {
        let tokens = SqlEditorWidget::tokenize_sql("(\n) + (, stable");
        let line_start_idx = tokens
            .iter()
            .enumerate()
            .find(|(_, token)| matches!(token, SqlToken::Symbol(sym) if sym == ")"))
            .map(|(idx, _)| idx)
            .expect("line-start close in typed-frame fixture");
        let comma_idx = tokens
            .iter()
            .enumerate()
            .find(|(_, token)| matches!(token, SqlToken::Symbol(sym) if sym == ","))
            .map(|(idx, _)| idx)
            .expect("comma in typed-frame close-open fixture");
        let line_start_snapshot = line_start_snapshot_before_token(&tokens, line_start_idx, 1);

        assert!(
            SqlEditorWidget::line_closes_paren_frame_below_line_start_before_token(
                &tokens,
                line_start_idx,
                comma_idx,
                &line_start_snapshot,
            ),
            "known line-start `(` frame should be consumed before the later open in `) + (`"
        );
    }

    #[test]
    fn line_closes_paren_frame_below_line_start_before_token_ignores_mismatched_local_close_delimiter(
    ) {
        let tokens = SqlEditorWidget::tokenize_sql("( ] , stable");
        let comma_idx = tokens
            .iter()
            .enumerate()
            .find(|(_, token)| matches!(token, SqlToken::Symbol(sym) if sym == ","))
            .map(|(idx, _)| idx)
            .expect("comma in mismatched local delimiter fixture");
        let line_start_snapshot = line_start_snapshot_before_token(&tokens, 0, 1);

        assert!(
            !SqlEditorWidget::line_closes_paren_frame_below_line_start_before_token(
                &tokens,
                0,
                comma_idx,
                &line_start_snapshot,
            ),
            "mismatched close delimiter inside a locally opened frame must not be treated as closing a parent frame"
        );
    }
}

#[cfg(test)]
mod format_frame_stack_tests {
    use super::*;

    #[test]
    fn format_frame_stack_tracks_active_paren_metrics_in_o1_state() {
        let mut stack = FormatFrameStack::default();
        let mut indent_level = 0usize;
        let compact = ParenFormatFrame::new(ParenFormatFrameKind::Compact, 0, false, 0, None);
        let query_like = ParenFormatFrame::new(ParenFormatFrameKind::QueryLike, 0, false, 2, None);

        assert!(stack.all_paren_frames_are_query_like());
        assert!(!stack.has_query_like_paren());
        assert_eq!(stack.paren_extra_depth(), 0);
        assert_eq!(stack.paren_extra_depth_within_current_query_frame(), 0);
        assert!(!stack.has_compact_non_query_frame_since_last_query());
        assert_eq!(stack.last_compact_paren_open_line_indent(), None);

        stack.push_paren(
            compact,
            ParenSemanticFlags::default(),
            false,
            None,
            None,
            None,
            &mut indent_level,
        );

        assert!(!stack.all_paren_frames_are_query_like());
        assert!(!stack.has_query_like_paren());
        assert_eq!(stack.paren_extra_depth(), 1);
        assert_eq!(stack.paren_extra_depth_within_current_query_frame(), 1);
        assert!(stack.has_compact_non_query_frame_since_last_query());
        assert_eq!(stack.last_compact_paren_open_line_indent(), Some(0));

        stack.push_paren(
            query_like,
            ParenSemanticFlags::default(),
            false,
            None,
            None,
            None,
            &mut indent_level,
        );

        assert!(!stack.all_paren_frames_are_query_like());
        assert!(stack.has_query_like_paren());
        assert_eq!(stack.paren_extra_depth(), 1);
        assert_eq!(stack.paren_extra_depth_within_current_query_frame(), 0);
        assert!(!stack.has_compact_non_query_frame_since_last_query());
        assert_eq!(stack.last_compact_paren_open_line_indent(), None);

        let popped_query_like = stack.pop_paren(&mut indent_level);
        assert!(popped_query_like.is_some());
        assert!(!stack.has_query_like_paren());
        assert_eq!(stack.paren_extra_depth(), 1);
        assert_eq!(stack.paren_extra_depth_within_current_query_frame(), 1);
        assert!(stack.has_compact_non_query_frame_since_last_query());
        assert_eq!(stack.last_compact_paren_open_line_indent(), Some(0));

        let popped_compact = stack.pop_paren(&mut indent_level);
        assert!(popped_compact.is_some());
        assert!(stack.all_paren_frames_are_query_like());
        assert!(!stack.has_query_like_paren());
        assert_eq!(stack.paren_extra_depth(), 0);
        assert_eq!(stack.paren_extra_depth_within_current_query_frame(), 0);
        assert!(!stack.has_compact_non_query_frame_since_last_query());
        assert_eq!(stack.last_compact_paren_open_line_indent(), None);
    }

    #[test]
    fn format_frame_stack_contains_block_kind_uses_cached_indices() {
        let mut stack = FormatFrameStack::default();
        let mut indent_level = 0usize;

        stack.push_block(BlockKind::PackageBody, 0, 1, &mut indent_level);

        assert!(stack.contains_block_kind("PACKAGE_BODY"));
        assert!(stack.contains_block_kind(BlockKind::PackageBody));
        assert!(!stack.contains_block_kind("CASE"));

        let popped = stack.pop_block(&mut indent_level);
        assert!(popped.is_some());
        assert!(!stack.contains_block_kind("PACKAGE_BODY"));
    }

    #[test]
    fn format_frame_stack_restores_query_base_and_runtime_state_from_same_paren_frame() {
        let mut stack = FormatFrameStack::default();
        let runtime_state = FormatRuntimeState {
            query_base_depth: Some(2),
            current_clause: Some("SELECT".to_string()),
            select_list_layout_state: SelectListLayoutState::Multiline {
                indent: 6,
                hanging_indent_spaces: None,
            },
            statement_has_with_clause: true,
            query_body_clause_base_depth: Some(4),
            open_cursor_state: OpenCursorFormatState::AwaitingFor,
        };

        stack.set_current_query_base_depth(Some(2));
        stack.set_current_clause(runtime_state.current_clause.clone());
        stack.set_select_list_layout_state(runtime_state.select_list_layout_state);
        stack.set_statement_has_with_clause(runtime_state.statement_has_with_clause);
        stack.set_query_body_clause_base_depth(runtime_state.query_body_clause_base_depth);
        stack.set_open_cursor_state(runtime_state.open_cursor_state);
        let mut indent_level = 0usize;
        stack.push_paren(
            ParenFormatFrame::new(ParenFormatFrameKind::QueryLike, 2, false, 3, Some(2)),
            ParenSemanticFlags::default(),
            true,
            Some(7),
            Some(FormatIndentedParenOwnerKind::WithinGroup),
            None,
            &mut indent_level,
        );

        assert_eq!(stack.current_query_base_depth(), Some(7));
        stack.set_current_clause(Some("WHERE".to_string()));
        stack.set_select_list_layout_state(SelectListLayoutState::Inactive);
        stack.set_statement_has_with_clause(false);
        stack.set_query_body_clause_base_depth(Some(9));
        stack.set_open_cursor_state(OpenCursorFormatState::None);

        let popped = stack
            .pop_paren(&mut indent_level)
            .expect("query-like paren frame");

        assert!(matches!(popped.frame.kind, ParenFormatFrameKind::QueryLike));
        assert_eq!(stack.current_query_base_depth(), Some(2));
        assert_eq!(stack.current_clause(), Some("SELECT"));
        assert!(stack.select_list_layout_state() == runtime_state.select_list_layout_state);
        assert!(stack.statement_has_with_clause());
        assert_eq!(stack.query_body_clause_base_depth(), Some(4));
        assert!(matches!(
            stack.open_cursor_state(),
            OpenCursorFormatState::AwaitingFor
        ));
    }

    #[test]
    fn format_frame_stack_keeps_block_until_explicit_pop_after_nested_paren_close() {
        let mut stack = FormatFrameStack::default();
        let mut indent_level = 0usize;

        stack.push_block(BlockKind::Case, 1, 2, &mut indent_level);
        stack.push_paren(
            ParenFormatFrame::new(ParenFormatFrameKind::Compact, 3, false, 0, None),
            ParenSemanticFlags::default(),
            false,
            None,
            None,
            None,
            &mut indent_level,
        );

        let popped_paren = stack
            .pop_paren(&mut indent_level)
            .expect("nested paren frame");

        assert!(matches!(
            popped_paren.frame.kind,
            ParenFormatFrameKind::Compact
        ));
        assert_eq!(stack.paren_depth(), 0);
        assert_eq!(stack.block_depth(), 1);
        assert!(stack.last_block_kind_is("CASE"));

        let popped_block = stack
            .pop_block(&mut indent_level)
            .expect("case block frame");

        assert_eq!(popped_block.kind, BlockKind::Case);
        assert_eq!(popped_block.depth_frame.owner_depth, 1);
        assert_eq!(stack.block_depth(), 0);
        assert_eq!(indent_level, 0);
    }

    #[test]
    fn format_frame_stack_sync_to_scope_expires_where_and_case_owners_but_keeps_control_header() {
        let mut stack = FormatFrameStack::default();
        let mut indent_level = 0usize;

        stack.push_condition_owner(
            ConditionOwnerKind::ControlHeader,
            FormatScope::new(0, 1),
            7,
            true,
        );
        stack.push_condition_owner(ConditionOwnerKind::Where, FormatScope::new(1, 0), 3, false);
        stack.push_condition_owner(ConditionOwnerKind::Case, FormatScope::new(1, 1), 5, false);
        stack.sync_to_scope(FormatScope::new(0, 1), &mut indent_level);

        assert!(stack
            .last_condition_owner(ConditionOwnerKind::Where)
            .is_none());
        assert!(stack
            .last_condition_owner(ConditionOwnerKind::Case)
            .is_none());
        assert_eq!(
            stack
                .last_condition_owner(ConditionOwnerKind::ControlHeader)
                .map(|frame| (frame.indent, frame.parenthesized)),
            Some((7, true))
        );
    }

    #[test]
    fn format_frame_stack_sync_preserves_block_after_paren_between_and_condition_tail_pops() {
        let mut stack = FormatFrameStack::default();
        let mut indent_level = 0usize;

        stack.push_block(BlockKind::If, 0, 1, &mut indent_level);
        stack.push_condition_owner(ConditionOwnerKind::Where, FormatScope::new(1, 1), 2, false);
        stack.push_between_pending(FormatScope::new(1, 1));
        stack.push_paren(
            ParenFormatFrame::new(ParenFormatFrameKind::Compact, 2, false, 0, None),
            ParenSemanticFlags::default(),
            false,
            None,
            None,
            None,
            &mut indent_level,
        );

        let popped_paren = stack
            .pop_paren(&mut indent_level)
            .expect("inner paren frame");

        assert!(matches!(
            popped_paren.frame.kind,
            ParenFormatFrameKind::Compact
        ));
        stack.sync_to_scope(FormatScope::new(0, 1), &mut indent_level);

        assert!(stack
            .last_condition_owner(ConditionOwnerKind::Where)
            .is_none());
        assert!(!stack.between_pending_matches(FormatScope::new(1, 1)));
        assert!(stack.last_block_kind_is("IF"));
        assert_eq!(stack.block_depth(), 1);
    }

    #[test]
    fn format_frame_stack_uses_nearest_active_query_runtime_frame_for_query_base_depth() {
        let mut stack = FormatFrameStack::default();
        let mut indent_level = 0usize;

        stack.push_paren(
            ParenFormatFrame::new(ParenFormatFrameKind::QueryLike, 0, false, 2, None),
            ParenSemanticFlags::default(),
            false,
            Some(1),
            None,
            None,
            &mut indent_level,
        );
        stack.push_paren(
            ParenFormatFrame::new(ParenFormatFrameKind::Compact, 0, false, 0, None),
            ParenSemanticFlags::default(),
            false,
            None,
            None,
            None,
            &mut indent_level,
        );

        assert_eq!(stack.current_query_base_depth(), Some(1));
    }

    #[test]
    fn format_frame_stack_sync_to_scope_realigns_indent_level_to_surviving_frames() {
        let mut stack = FormatFrameStack::default();
        let mut indent_level = 0usize;

        stack.push_block(BlockKind::Begin, 0, 2, &mut indent_level);
        stack.push_scoped_indent(
            ScopedIndentKind::MySqlHandlerBody,
            stack.current_scope(),
            1,
            &mut indent_level,
        );
        assert_eq!(indent_level, 3);

        indent_level = 99;
        stack.sync_to_scope(stack.current_scope(), &mut indent_level);

        assert_eq!(indent_level, 3);
        assert_eq!(stack.block_depth(), 1);
        assert!(stack.scoped_indent_is_active(ScopedIndentKind::MySqlHandlerBody));
    }

    #[test]
    fn format_frame_stack_prefers_innermost_active_query_runtime_frame_for_query_base_depth() {
        let mut stack = FormatFrameStack::default();
        let mut indent_level = 0usize;

        stack.push_paren(
            ParenFormatFrame::new(ParenFormatFrameKind::QueryLike, 0, false, 2, None),
            ParenSemanticFlags::default(),
            false,
            Some(2),
            None,
            None,
            &mut indent_level,
        );
        stack.push_paren(
            ParenFormatFrame::new(ParenFormatFrameKind::QueryLike, 0, false, 2, None),
            ParenSemanticFlags::default(),
            false,
            Some(6),
            None,
            None,
            &mut indent_level,
        );

        assert_eq!(stack.current_query_base_depth(), Some(6));

        let _ = stack.pop_paren(&mut indent_level);

        assert_eq!(stack.current_query_base_depth(), Some(2));
    }

    #[test]
    fn format_frame_stack_statement_cleanup_keeps_persistent_frames_only() {
        let mut stack = FormatFrameStack::default();
        let mut indent_level = 0usize;

        stack.push_block(BlockKind::Declare, 0, 1, &mut indent_level);
        stack.push_plsql_context(stack.current_scope());
        stack.push_compound_trigger(stack.current_scope());
        stack.push_with_cte_frame(stack.current_scope());
        stack.push_open_cursor_restore(OpenCursorFormatState::AwaitingFor);
        stack.push_condition_owner(ConditionOwnerKind::Where, stack.current_scope(), 2, false);
        stack.push_between_pending(stack.current_scope());
        stack.set_pending_split_end_suffix(
            stack.current_scope(),
            2,
            PendingSplitEndSuffixKind::SingleWord,
        );
        stack.set_pending_plsql_label_body_indent(stack.current_scope(), 3);
        stack.set_mysql_handler_pending_body_indent(stack.current_scope(), 4);
        stack.push_paren(
            ParenFormatFrame::new(ParenFormatFrameKind::Compact, 1, false, 0, None),
            ParenSemanticFlags::default(),
            false,
            None,
            None,
            None,
            &mut indent_level,
        );

        stack.clear_statement_frames(&mut indent_level);

        assert_eq!(stack.paren_depth(), 0);
        assert_eq!(stack.current_query_base_depth(), None);
        assert!(stack.last_paren().is_none());
        assert!(stack
            .last_condition_owner(ConditionOwnerKind::Where)
            .is_none());
        assert!(stack.pending_split_end_suffix_indent().is_none());
        assert!(stack.pending_plsql_label_body_indent().is_none());
        assert!(stack.mysql_handler_pending_body_indent().is_none());
        assert!(stack.plsql_context_is_active());
        assert!(
            stack.compound_trigger_is_in_outer_body() || stack.last_compound_trigger().is_some()
        );
        assert!(stack.with_cte_in_definitions());
        assert!(stack.last_block_kind_is("DECLARE"));
    }

    #[test]
    fn format_frame_stack_statement_cleanup_rebuilds_indent_from_surviving_handler_body_scope() {
        let mut stack = FormatFrameStack::default();
        let mut indent_level = 0usize;

        stack.push_block(BlockKind::Begin, 0, 2, &mut indent_level);
        stack.push_scoped_indent(
            ScopedIndentKind::MySqlHandlerBody,
            stack.current_scope(),
            1,
            &mut indent_level,
        );
        stack.push_paren(
            ParenFormatFrame::new(ParenFormatFrameKind::Compact, 0, false, 0, None),
            ParenSemanticFlags::default(),
            false,
            None,
            None,
            None,
            &mut indent_level,
        );
        stack.activate_construct_flag(ConstructFlagKind::CreatePending, stack.current_scope());
        stack.set_current_query_base_depth(Some(7));

        stack.clear_statement_frames(&mut indent_level);

        assert_eq!(indent_level, 3);
        assert_eq!(stack.paren_depth(), 0);
        assert_eq!(stack.block_depth(), 1);
        assert!(stack.last_block_kind_is("BEGIN"));
        assert!(stack.scoped_indent_is_active(ScopedIndentKind::MySqlHandlerBody));
        assert!(!stack.construct_flag_is_active(ConstructFlagKind::CreatePending));
        assert_eq!(stack.current_query_base_depth(), None);
    }

    #[test]
    fn format_frame_stack_scope_sync_expires_pending_auxiliary_frames() {
        let mut stack = FormatFrameStack::default();
        let mut indent_level = 0usize;
        let original_scope = FormatScope::with_frame_ids(1, 0, Some(10), None);
        let sibling_scope = FormatScope::with_frame_ids(1, 0, Some(11), None);

        stack.set_pending_split_end_suffix(
            original_scope,
            2,
            PendingSplitEndSuffixKind::SingleWord,
        );
        stack.set_pending_plsql_label_body_indent(original_scope, 3);
        stack.set_mysql_handler_pending_body_indent(original_scope, 4);

        stack.sync_to_scope(sibling_scope, &mut indent_level);

        assert!(stack.pending_split_end_suffix_indent().is_none());
        assert!(stack.pending_plsql_label_body_indent().is_none());
        assert!(stack.mysql_handler_pending_body_indent().is_none());
    }

    #[test]
    fn format_frame_stack_pending_split_end_suffix_advances_until_terminal_word() {
        let mut stack = FormatFrameStack::default();
        let scope = FormatScope::new(0, 0);

        stack.set_pending_split_end_suffix(
            scope,
            2,
            PendingSplitEndSuffixKind::from_first_suffix_word("BEFORE"),
        );
        assert_eq!(stack.pending_split_end_suffix_indent(), Some(2));

        stack.advance_pending_split_end_suffix("BEFORE");
        assert!(matches!(
            stack.pending_split_end_suffix_kind(),
            Some(PendingSplitEndSuffixKind::TimingSuffix {
                consumed_timing_keyword: true,
                consumed_each: false,
            })
        ));

        stack.advance_pending_split_end_suffix("EACH");
        assert!(matches!(
            stack.pending_split_end_suffix_kind(),
            Some(PendingSplitEndSuffixKind::TimingSuffix {
                consumed_timing_keyword: true,
                consumed_each: true,
            })
        ));

        stack.advance_pending_split_end_suffix("ROW");
        assert!(stack.pending_split_end_suffix_indent().is_none());
    }

    #[test]
    fn format_frame_stack_block_metadata_survives_nested_block_pop() {
        let mut stack = FormatFrameStack::default();
        let mut indent_level = 0usize;

        stack.push_block(BlockKind::Case, 1, 2, &mut indent_level);
        let _ = stack.set_last_case_branch_started(true);
        stack.push_block(BlockKind::Begin, 2, 3, &mut indent_level);
        let _ = stack.mark_last_block_mysql_handler_begin();
        assert_eq!(indent_level, 3);

        let popped_begin = stack
            .pop_block(&mut indent_level)
            .expect("handler begin block");

        assert_eq!(popped_begin.kind, BlockKind::Begin);
        assert!(popped_begin.mysql_handler_begin);
        assert!(stack.last_case_branch_started());
        assert_eq!(indent_level, 2);

        let popped_case = stack.pop_block(&mut indent_level).expect("case block");

        assert_eq!(popped_case.kind, BlockKind::Case);
        assert_eq!(stack.block_depth(), 0);
        assert_eq!(indent_level, 0);
    }

    #[test]
    fn format_frame_stack_deactivate_scoped_indent_realigns_to_surviving_block_indent() {
        let mut stack = FormatFrameStack::default();
        let mut indent_level = 0usize;
        let scope = FormatScope::new(0, 0);

        stack.push_scoped_indent(ScopedIndentKind::CursorSql, scope, 2, &mut indent_level);
        stack.push_block(BlockKind::Begin, 0, 5, &mut indent_level);

        let removed =
            stack.deactivate_scoped_indent(ScopedIndentKind::CursorSql, &mut indent_level);

        assert!(removed);
        assert_eq!(indent_level, 5);
        assert!(!stack.scoped_indent_is_active(ScopedIndentKind::CursorSql));
        assert!(stack.last_block_kind_is("BEGIN"));
        assert_eq!(stack.block_depth(), 1);
    }

    #[test]
    fn format_frame_stack_clear_scoped_indent_realigns_to_surviving_block_indent() {
        let mut stack = FormatFrameStack::default();
        let mut indent_level = 0usize;
        let scope = FormatScope::new(0, 0);

        stack.push_scoped_indent(ScopedIndentKind::CursorSql, scope, 1, &mut indent_level);
        stack.push_scoped_indent(ScopedIndentKind::CursorSql, scope, 2, &mut indent_level);
        stack.push_block(BlockKind::Begin, 0, 4, &mut indent_level);

        stack.clear_scoped_indent(ScopedIndentKind::CursorSql, &mut indent_level);

        assert_eq!(indent_level, 4);
        assert!(!stack.scoped_indent_is_active(ScopedIndentKind::CursorSql));
        assert!(stack.last_block_kind_is("BEGIN"));
        assert_eq!(stack.block_depth(), 1);
    }

    #[test]
    fn format_frame_stack_with_cte_body_frames_do_not_remove_non_tail_match() {
        let mut active_body_frames = vec![
            WithPlsqlBodyFrame::routine(),
            WithPlsqlBodyFrame::nested(WithPlsqlBodyFrameKind::Case),
            WithPlsqlBodyFrame::nested(WithPlsqlBodyFrameKind::Block),
        ];

        FormatFrameStack::pop_with_plsql_body_frame(
            &mut active_body_frames,
            Some(WithPlsqlBodyFrameKind::Case),
        );

        assert_eq!(active_body_frames.len(), 3);
        assert!(active_body_frames
            .last()
            .is_some_and(|frame| frame.kind == WithPlsqlBodyFrameKind::Block));

        FormatFrameStack::pop_with_plsql_body_frame(&mut active_body_frames, None);

        assert_eq!(active_body_frames.len(), 2);
        assert!(active_body_frames
            .last()
            .is_some_and(|frame| frame.kind == WithPlsqlBodyFrameKind::Case));

        FormatFrameStack::pop_with_plsql_body_frame(
            &mut active_body_frames,
            Some(WithPlsqlBodyFrameKind::Case),
        );

        assert_eq!(active_body_frames.len(), 1);
        assert!(active_body_frames
            .last()
            .is_some_and(|frame| frame.kind == WithPlsqlBodyFrameKind::Routine));
    }

    #[test]
    fn format_frame_stack_sync_for_token_entry_expires_sibling_auxiliary_frames_and_records_trace()
    {
        let mut stack = FormatFrameStack::default();
        let mut indent_level = 0usize;

        stack.push_paren(
            ParenFormatFrame::new(ParenFormatFrameKind::Compact, 0, false, 0, None),
            ParenSemanticFlags::default(),
            false,
            None,
            None,
            None,
            &mut indent_level,
        );
        let current_scope = stack.current_scope();
        let sibling_scope = FormatScope::with_frame_ids(
            current_scope.paren_depth,
            current_scope.block_depth,
            Some(999),
            current_scope.block_frame_id,
        );
        stack.push_scoped_indent(
            ScopedIndentKind::MergeWhenBranch,
            sibling_scope,
            2,
            &mut indent_level,
        );
        stack.set_pending_split_end_suffix(sibling_scope, 2, PendingSplitEndSuffixKind::SingleWord);

        let returned_scope = stack.sync_for_token_entry(&mut indent_level);
        let event = *stack
            .transition_trace()
            .last()
            .expect("token-entry sync event");

        assert_eq!(returned_scope, current_scope);
        assert_eq!(indent_level, 0);
        assert!(stack.pending_split_end_suffix_indent().is_none());
        assert_eq!(event.kind, FormatFrameTransitionKind::TokenEntrySync);
        assert_eq!(event.scope_before, current_scope);
        assert_eq!(event.scope_after, current_scope);
        assert_eq!(event.indent_before, 2);
        assert_eq!(event.indent_after, 0);
    }

    #[test]
    fn format_frame_stack_statement_boundary_reset_clears_runtime_state_and_records_trace() {
        let mut stack = FormatFrameStack::default();
        let mut indent_level = 0usize;

        stack.push_block(BlockKind::Declare, 0, 1, &mut indent_level);
        stack.set_current_clause(Some("SELECT".to_string()));
        stack.set_select_list_layout_state(SelectListLayoutState::Pending {
            anchor: 4,
            indent: 2,
        });
        stack.set_statement_has_with_clause(true);
        stack.set_query_body_clause_base_depth(Some(3));
        stack.set_open_cursor_state(OpenCursorFormatState::AwaitingFor);
        stack.push_between_pending(stack.current_scope());
        let scope_before = stack.current_scope();

        stack.reset_for_statement_boundary(&mut indent_level);
        let event = *stack
            .transition_trace()
            .last()
            .expect("statement-boundary reset event");

        assert_eq!(stack.current_clause(), None);
        assert!(stack.select_list_layout_state() == SelectListLayoutState::Inactive);
        assert!(!stack.statement_has_with_clause());
        assert_eq!(stack.query_body_clause_base_depth(), None);
        assert!(matches!(
            stack.open_cursor_state(),
            OpenCursorFormatState::None
        ));
        assert!(stack.last_block_kind_is("DECLARE"));
        assert_eq!(indent_level, 1);
        assert_eq!(
            event.kind,
            FormatFrameTransitionKind::StatementBoundaryReset
        );
        assert_eq!(event.scope_before, scope_before);
        assert_eq!(event.scope_after, stack.current_scope());
        assert_eq!(event.indent_before, 1);
        assert_eq!(event.indent_after, 1);
    }

    #[test]
    fn format_frame_stack_statement_boundary_reset_preserves_with_clause_inside_with_definitions() {
        let mut stack = FormatFrameStack::default();
        let mut indent_level = 0usize;

        stack.push_with_cte_frame(stack.current_scope());
        stack.set_statement_has_with_clause(true);

        stack.reset_for_statement_boundary(&mut indent_level);

        assert!(stack.statement_has_with_clause());
        assert!(stack.with_cte_in_definitions());
    }

    #[test]
    fn format_frame_stack_transition_trace_records_runtime_restore_on_query_like_pop() {
        let mut stack = FormatFrameStack::default();
        let mut indent_level = 0usize;

        stack.set_current_clause(Some("SELECT".to_string()));
        stack.push_paren(
            ParenFormatFrame::new(ParenFormatFrameKind::QueryLike, 1, false, 2, Some(1)),
            ParenSemanticFlags::default(),
            true,
            Some(3),
            None,
            None,
            &mut indent_level,
        );
        stack.set_current_clause(Some("WHERE".to_string()));

        let _ = stack.pop_paren(&mut indent_level);
        let events = stack.transition_trace();

        assert!(matches!(
            events.first().map(|event| event.kind),
            Some(FormatFrameTransitionKind::PushParen {
                kind: ParenFormatFrameKind::QueryLike,
            })
        ));
        assert!(matches!(
            events.last().map(|event| event.kind),
            Some(FormatFrameTransitionKind::PopParen {
                kind: ParenFormatFrameKind::QueryLike,
                restored_runtime_state: true,
            })
        ));
        assert_eq!(stack.current_clause(), Some("SELECT"));
        assert_eq!(indent_level, 0);
    }

    #[test]
    fn format_frame_stack_deactivate_construct_flag_keeps_surviving_indent_frames() {
        let mut stack = FormatFrameStack::default();
        let mut indent_level = 0usize;

        stack.push_block(BlockKind::Begin, 0, 2, &mut indent_level);
        stack.activate_construct_flag(ConstructFlagKind::CreatePending, stack.current_scope());
        stack.push_scoped_indent(
            ScopedIndentKind::CursorSql,
            stack.current_scope(),
            1,
            &mut indent_level,
        );

        let removed = stack.deactivate_construct_flag(ConstructFlagKind::CreatePending);

        assert!(removed);
        assert_eq!(indent_level, 3);
        assert!(stack.last_block_kind_is("BEGIN"));
        assert!(stack.scoped_indent_is_active(ScopedIndentKind::CursorSql));
        assert_eq!(stack.block_depth(), 1);
    }
}
